# Local reference inputs — not committed (see README.md)
.crd-dumps/
.installer-dumps/

# Archives if dropped outside .installer-dumps/ by mistake
*.tar.gz

# Python cache from scripts/build_inventory.py
__pycache__/
*.py[cod]
