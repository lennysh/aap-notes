# AAP 2.6 — Rpm Install Playbook Tasks

Task execution order for the AAP install playbook, expanded recursively through included roles and task files. Conditional tasks include a `When` column; tasks inside blocks inherit the block condition. **Progress** runs from 0% (first row) to 100% (last row) based on position in the expanded task list.

| Field | Value |
|-------|-------|
| Playbook command | `./setup.sh -i inventory` |
| Source playbook | `collections/ansible_collections/ansible/automation_platform_installer/playbooks/install.yml` |
| Installer dump | `.installer-dumps/2.6/ansible-automation-platform-setup-2.6-7` |
| Total tasks (expanded) | 2484 |

## Task table

<!-- prettier-ignore-start -->
<!-- Do not format this table — editors pad columns to the widest cell. Regenerate instead. -->
| # | Progress | Location | Task | Description | When |
|---:|---:|---|---|---|---|
| 1 | 0.0% | install.yml | Warn if no core component is enabled | Play targeting `localhost` | — |
| 2 | 0.0% | install.yml → Warn if no core component is enabled | Warn if no core component is enabled | Warn if no core component is enabled | not groups['automationgateway'] \| default([]) \| length; not groups['automationcontroller'] \| default([]) \| length; not groups['automationhub'] \| default([]) \| length; not groups['automationedacontroller'] \| default([]) \| length; not groups['database'] \| default([]) \| length |
| 3 | 0.1% | install.yml | Group all valid hosts for AAP installation | Play targeting `tower:automationgateway:automationcontroller:automationhub:automationedacontroller:database:execution_nodes` | — |
| 4 | 0.1% | install.yml → Group all valid hosts for AAP installation | group hosts used in AAP installer | group hosts used in AAP installer | — |
| 5 | 0.2% | install.yml | unnamed play | Play targeting `aap_valid_hosts` | — |
| 6 | 0.2% | install.yml → unnamed play | include_role | Include and run tasks from an Ansible role (role `check_config_static`) | — |
| 7 | 0.2% | install.yml → unnamed play → check_config_static | role `check_config_static` | Enter role task list | — |
| 8 | 0.3% | install.yml → unnamed play → check_config_static → check_config_static | file `main.yml` | Task file `ansible/automation_platform_installer/roles/check_config_static/tasks/main.yml` | — |
| 9 | 0.3% | install.yml → unnamed play → check_config_static → check_config_static | Ensure cluster hosts are not localhost | Ensure cluster hosts are not localhost | groups['automationcontroller'] \| default([]) \| length > 1; ansible_host is match("127.*") or ansible_host is match("localhost*") or ansible_host == "::1" |
| 10 | 0.4% | install.yml → unnamed play → check_config_static → check_config_static | Check that group names do not conflict with instance names | Check that group names do not conflict with instance names | ('instance_group_' + ansible_host) in groups |
| 11 | 0.4% | install.yml → unnamed play → check_config_static → check_config_static | service_facts | Run `service_facts` | — |
| 12 | 0.4% | install.yml → unnamed play → check_config_static → check_config_static | block | Begin conditional or grouped task block | ansible_facts.services['fapolicyd.service'] \| default([]) \| length; ansible_facts.services['fapolicyd.service'].state == 'running' |
| 13 | 0.5% | install.yml → unnamed play → check_config_static → check_config_static → block | Check if fapolicyd is permissive | Check if fapolicyd is permissive | ansible_facts.services['fapolicyd.service'] \| default([]) \| length; ansible_facts.services['fapolicyd.service'].state == 'running' |
| 14 | 0.5% | install.yml → unnamed play → check_config_static → check_config_static → block | fail | Fail the play with an error message | ansible_facts.services['fapolicyd.service'] \| default([]) \| length; ansible_facts.services['fapolicyd.service'].state == 'running'; fapolicy_permissive is failed |
| 15 | 0.6% | install.yml → unnamed play → check_config_static → check_config_static | Perform inventory sanity checks | Begin conditional or grouped task block | — |
| 16 | 0.6% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Detect pre-2.x inventory and offer a migration | Detect pre-2.x inventory and offer a migration | — |
| 17 | 0.6% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Detect unsupported HA inventory file | Detect unsupported HA inventory file | 'primary' in groups or 'secondary' in groups |
| 18 | 0.7% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Check that no instance groups are named automationcontroller | Check that no instance groups are named automationcontroller | item != "automationcontroller"; item.replace('instance_group_', '') == "automationcontroller" |
| 19 | 0.7% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure only one database host exists | Ensure only one database host exists | groups['database'] \| default([]) \| length > 1 |
| 20 | 0.8% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure redis mode is a valid choice | Ensure redis mode is a valid choice | — |
| 21 | 0.8% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure redis cluster is correctly configured | Ensure redis cluster is correctly configured | _redis_mode == 'cluster' |
| 22 | 0.8% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure redis use sockets is not set globally | Ensure redis use sockets is not set globally | redis_use_sockets \| default('') \| string \| length > 0 |
| 23 | 0.9% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure postgres_extra_settings is a dictonary | Ensure postgres_extra_settings is a dictonary | postgres_extra_settings is defined |
| 24 | 0.9% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Validate feature flags | Begin conditional or grouped task block | feature_flags is defined |
| 25 | 1.0% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks → Validate feature flags | Ensure feature_flags is a dictonary | Ensure feature_flags is a dictonary | feature_flags is defined |
| 26 | 1.0% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks → Validate feature flags | Ensure feature_flags keys/values are valid | Ensure feature_flags keys/values are valid | feature_flags is defined |
| 27 | 1.0% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure automationhub_pg related variables are defined if more than one component is enabled | Ensure automationhub_pg related variables are defined if more than one component is enabled | groups['automationhub'] \| default([]) \| length; groups['automationcontroller'] \| default([]) \| length or groups['automationedacontroller'] \| default([]) \| length; not automationhub_pg_host \| default('') \| length; not automationhub_db_template \| default([]) \| length |
| 28 | 1.1% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure automationedacontroller_pg related variables are defined if more than one component is enabled | Ensure automationedacontroller_pg related variables are defined if more than one component is enabled | groups['automationedacontroller'] \| default([]) \| length; groups['automationhub'] \| default([]) \| length or groups['automationcontroller'] \| default([]) \| length; not automationedacontroller_pg_host \| default('') \| length; not automationedacontroller_db_template \| default([]) \| length |
| 29 | 1.1% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Fail if Automation Controller host is localhost | Fail if Automation Controller host is localhost | item is match("127.*") or item is match("localhost*") or item == "::1" |
| 30 | 1.2% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Fail if Automation Hub host is localhost | Fail if Automation Hub host is localhost | item is match("127.*") or item is match("localhost*") or item == "::1" |
| 31 | 1.2% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Fail if Automation EDA Controller host is localhost | Fail if Automation EDA Controller host is localhost | item is match("127.*") or item is match("localhost*") or item == "::1" |
| 32 | 1.2% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure that when a database host is specified, that component hosts and ports are defined | Begin conditional or grouped task block | groups['database'] \| default([]) \| length |
| 33 | 1.3% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks → Ensure that when a database host is specified, that component hosts and ports are defined | Ensure for Controller that when a database host is specificed that pg_host and pg_port are specified | Ensure for Controller that when a database host is specificed that pg_host and pg_port are specified | groups['database'] \| default([]) \| length; groups['automationcontroller'] \| default([]) \| length; not pg_host \| default('') \| length or not _pg_port \| default('') \| string \| length; not automationcontroller_db_template \| default([]) \| length |
| 34 | 1.3% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks → Ensure that when a database host is specified, that component hosts and ports are defined | Ensure for Automation Hub that when a database host is specificed that pg_host and pg_port are specified | Ensure for Automation Hub that when a database host is specificed that pg_host and pg_port are specified | groups['database'] \| default([]) \| length; groups['automationhub'] \| default([]) \| length; not automationhub_pg_host \| default('') \| length or not _automationhub_pg_port \| default('') \| string \| length; not automationhub_db_template \| default([]) \| length |
| 35 | 1.4% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks → Ensure that when a database host is specified, that component hosts and ports are defined | Ensure for Automation EDA Controller that when a database host is specificed that pg_host and pg_port are specified | Ensure for Automation EDA Controller that when a database host is specificed that pg_host and pg_port are specified | groups['database'] \| default([]) \| length; groups['automationedacontroller'] \| default([]) \| length; not automationedacontroller_pg_host \| default('') \| length or not _automationedacontroller_pg_port \| default('') \| string \| length; not automationedacontroller_db_template \| default([]) \| length |
| 36 | 1.4% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure that pg_hashed_password is not set | Ensure that pg_hashed_password is not set | pg_hashed_password is defined |
| 37 | 1.4% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure certificates are provided when pg_sslmode is verify-ca | Ensure certificates are provided when pg_sslmode is verify-ca | _pg_sslmode == 'verify-ca'; not pgclient_sslcert \| default('') \| length or not pgclient_sslkey \| default('') \| length |
| 38 | 1.5% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure certificates are provided when automationhub_pg_sslmode is verify-ca | Ensure certificates are provided when automationhub_pg_sslmode is verify-ca | _automationhub_pg_sslmode == 'verify-ca'; not automationhub_pgclient_sslcert \| default('') \| length or not automationhub_pgclient_sslkey \| default('') \| length |
| 39 | 1.5% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure certificates are provided when automationedacontroller_pg_sslmode is verify-ca | Ensure certificates are provided when automationedacontroller_pg_sslmode is verify-ca | _automationedacontroller_pg_sslmode == 'verify-ca'; not automationedacontroller_pgclient_sslcert \| default('') \| length or not automationedacontroller_pgclient_sslkey \| default('') \| length |
| 40 | 1.6% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure certificates are provided when automationgateway_pg_sslmode is verify-ca | Ensure certificates are provided when automationgateway_pg_sslmode is verify-ca | _automationgateway_pg_sslmode == 'verify-ca'; not automationgateway_pgclient_sslcert \| default('') \| length or not automationgateway_pgclient_sslkey \| default('') \| length |
| 41 | 1.6% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure that if we have web cert, we also have key (Controller) | Ensure that if we have web cert, we also have key (Controller) | groups['automationcontroller'] \| default([]) \| length; (web_server_ssl_cert is defined and web_server_ssl_key is not defined) or (web_server_ssl_key is defined and web_server_ssl_cert is not defined) |
| 42 | 1.7% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure that if we have web cert, we also have key (Hub) | Ensure that if we have web cert, we also have key (Hub) | groups['automationhub'] \| default([]) \| length; (automationhub_ssl_cert is defined and automationhub_ssl_key is not defined) or (automationhub_ssl_key is defined and automationhub_ssl_cert is not defined) |
| 43 | 1.7% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure that if we have web cert, we also have key (Gateway) | Ensure that if we have web cert, we also have key (Gateway) | groups['automationgateway'] \| default([]) \| length; (automationgateway_ssl_cert is defined and automationgateway_ssl_key is not defined) or (automationgateway_ssl_key is defined and automationgateway_ssl_cert is not defined) |
| 44 | 1.7% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure that a key is provided if a CA certificate file is also provided for the internally managed CA | Ensure that a key is provided if a CA certificate file is also provided for the internally managed CA | aap_ca_cert_file is defined and aap_ca_key_file is not defined |
| 45 | 1.8% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Preflight check - verify aap_ca_server_hostname is one of the platform components | Preflight check - verify aap_ca_server_hostname is one of the platform components | aap_ca_server_hostname is defined; aap_ca_server_hostname not in ((groups['automationgateway']  \| default([])) + (groups['automationcontroller']  \| default([])) + (groups['automationhub']  \| default([])) + (groups['automationedacontroller']  \| default([]))) |
| 46 | 1.8% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure Gateway host exists | Ensure Gateway host exists | not groups['automationgateway'] \| default([]) \| length |
| 47 | 1.9% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure all items in EDA CSRF_TRUSTED_ORIGINS list have the required http/s schema | Ensure all items in EDA CSRF_TRUSTED_ORIGINS list have the required http/s schema | automationedacontroller_trusted_origins \| default([]) \| list \| length; item is not regex("^https?://.*") |
| 48 | 1.9% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | HA mode requires an external postgres database with pg_host defined | HA mode requires an external postgres database with pg_host defined | groups['automationcontroller'] \| default([]) \| length > 1; not pg_host \| default('') \| length; not automationcontroller_db_template \| default([]) \| length |
| 49 | 1.9% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | HA mode requires an external postgres database with pg_port defined | HA mode requires an external postgres database with pg_port defined | groups['automationcontroller'] \| default([]) \| length > 1; not _pg_port \| default(0) \| int; not automationcontroller_db_template \| default([]) \| length |
| 50 | 2.0% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Hub HA mode requires an external postgres database with automationhub_pg_host defined | Hub HA mode requires an external postgres database with automationhub_pg_host defined | groups['automationhub'] \| default([]) \| length > 1; not automationhub_pg_host \| default('') \| length; not automationhub_db_template \| default([]) \| length |
| 51 | 2.0% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Hub HA mode requires an external postgres database with automationhub_pg_port defined | Hub HA mode requires an external postgres database with automationhub_pg_port defined | groups['automationhub'] \| default([]) \| length > 1; not automationhub_pg_port \| default(0) \| int; not automationhub_db_template \| default([]) \| length |
| 52 | 2.1% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure registry_username and registry_password are set for registry.redhat.io | Ensure registry_username and registry_password are set for registry.redhat.io | groups['automationcontroller'] \| default([]) \| length or groups['automationhub'] \| default([]) \| length; _registry_url == 'registry.redhat.io'; not registry_username \| default('') \| length or not registry_password \| default('') \| length; not bundle_install \| bool or ( bundle_install \| bool and not _ee_from_hub_only \| bool ) |
| 53 | 2.1% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure that Automation Hub signing script and signing keys are provided when collection signing is enabled | Ensure that Automation Hub signing script and signing keys are provided when collection signing is enabled | groups['automationhub'] \| default([]) \| length; automationhub_create_default_collection_signing_service \| default(false) \| bool; not automationhub_collection_signing_service_key \| default('') \| length or not automationhub_collection_signing_service_script \| default('') \| length |
| 54 | 2.1% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure that Automation Hub signing script and signing keys are provided when container signing is enabled | Ensure that Automation Hub signing script and signing keys are provided when container signing is enabled | groups['automationhub'] \| default([]) \| length; automationhub_create_default_container_signing_service \| default(false) \| bool; not automationhub_container_signing_service_key \| default('') \| length or not automationhub_container_signing_service_script \| default('') \| length |
| 55 | 2.2% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure that Automation Hub collection signing is enabled when auto signing is enabled | Ensure that Automation Hub collection signing is enabled when auto signing is enabled | groups['automationhub'] \| default([]) \| length; automationhub_auto_sign_collections \| default(false) \| bool; not automationhub_create_default_collection_signing_service \| default(false) \| bool |
| 56 | 2.2% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Check if automationgateway_main_url starts with a http protocol | Check if automationgateway_main_url starts with a http protocol | automationgateway_main_url \| default('') \| length; automationgateway_main_url is not regex("^https?://.*") |
| 57 | 2.3% | install.yml → unnamed play | include_role | Include and run tasks from an Ansible role (role `config_dynamic`) | — |
| 58 | 2.3% | install.yml → unnamed play → config_dynamic | role `config_dynamic` | Enter role task list | — |
| 59 | 2.3% | install.yml → unnamed play → config_dynamic → config_dynamic | file `main.yml` | Task file `ansible/automation_platform_installer/roles/config_dynamic/tasks/main.yml` | — |
| 60 | 2.4% | install.yml → unnamed play → config_dynamic → config_dynamic | set_fact | Set a host fact used by later tasks | not routable_hostname \| default('') \| length |
| 61 | 2.4% | install.yml → unnamed play → config_dynamic → config_dynamic | Check that only eligible hosts are part of the redis group | Check that only eligible hosts are part of the redis group | groups.get('redis', []) \| intersect(aap_ineligible_redis_nodes) \| length > 0 |
| 62 | 2.5% | install.yml → unnamed play → config_dynamic → config_dynamic | Set database to internal or external | Set database to internal or external | — |
| 63 | 2.5% | install.yml → unnamed play → config_dynamic → config_dynamic | Hub set database to internal or external | Hub set database to internal or external | — |
| 64 | 2.5% | install.yml → unnamed play → config_dynamic → config_dynamic | Automation EDA Controller set database to internal or external | Automation EDA Controller set database to internal or external | — |
| 65 | 2.6% | install.yml → unnamed play → config_dynamic → config_dynamic | Automation Gateway set database to internal or external | Automation Gateway set database to internal or external | — |
| 66 | 2.6% | install.yml → unnamed play → config_dynamic → config_dynamic | Controller database decision | Controller database decision | — |
| 67 | 2.7% | install.yml → unnamed play → config_dynamic → config_dynamic | Hub database decision | Hub database decision | — |
| 68 | 2.7% | install.yml → unnamed play → config_dynamic → config_dynamic | EDA Controller database decision | EDA Controller database decision | — |
| 69 | 2.7% | install.yml → unnamed play → config_dynamic → config_dynamic | Gateway database decision | Gateway database decision | — |
| 70 | 2.8% | install.yml → unnamed play → config_dynamic → config_dynamic | Set postgres host and port to local if not set | Set postgres host and port to local if not set | not pg_host \| default('') \| length or _pg_port == '' |
| 71 | 2.8% | install.yml → unnamed play → config_dynamic → config_dynamic | Set Automation EDA Controller postgres host and port to local if not set | Set Automation EDA Controller postgres host and port to local if not set | not automationedacontroller_pg_host \| default('') \| length or _automationedacontroller_pg_port == '' |
| 72 | 2.9% | install.yml → unnamed play → config_dynamic → config_dynamic | Set Automation Gateway postgres host and port to local if not set | Set Automation Gateway postgres host and port to local if not set | not automationgateway_pg_host \| default('') \| length or _automationgateway_pg_port == '' |
| 73 | 2.9% | install.yml → unnamed play → config_dynamic → config_dynamic | Automation Controller postgres client cert auth decision | Automation Controller postgres client cert auth decision | groups['automationcontroller'] \| default([]) \| length; _pg_sslmode is in ['verify-ca', 'verify-full']; pgclient_sslcert \| default('') \| length; pgclient_sslkey \| default('') \| length |
| 74 | 2.9% | install.yml → unnamed play → config_dynamic → config_dynamic | Automationhub postgres client cert auth decision | Automationhub postgres client cert auth decision | groups['automationhub'] \| default([]) \| length; _automationhub_pg_sslmode is in ['verify-ca', 'verify-full']; automationhub_pgclient_sslcert \| default('') \| length; automationhub_pgclient_sslkey \| default('') \| length |
| 75 | 3.0% | install.yml → unnamed play → config_dynamic → config_dynamic | Automation EDA Controller postgres client cert auth decision | Automation EDA Controller postgres client cert auth decision | groups['automationedacontroller'] \| default([]) \| length; _automationedacontroller_pg_sslmode is in ['verify-ca', 'verify-full']; automationedacontroller_pgclient_sslcert \| default('') \| length; automationedacontroller_pgclient_sslkey \| default('') \| length |
| 76 | 3.0% | install.yml → unnamed play → config_dynamic → config_dynamic | Automation Gateway postgres client cert auth decision | Automation Gateway postgres client cert auth decision | groups['automationgateway'] \| default([]) \| length; _automationgateway_pg_sslmode is in ['verify-ca', 'verify-full']; automationgateway_pgclient_sslcert \| default('') \| length; automationgateway_pgclient_sslkey \| default('') \| length |
| 77 | 3.1% | install.yml → unnamed play → config_dynamic → config_dynamic | Configure postgres certificates for verify-ca | Configure postgres certificates for verify-ca | awx_pg_cert_auth |
| 78 | 3.1% | install.yml → unnamed play → config_dynamic → config_dynamic | Configure automationhub postgres certificates for verify-ca | Configure automationhub postgres certificates for verify-ca | automationhub_pg_cert_auth |
| 79 | 3.1% | install.yml → unnamed play → config_dynamic → config_dynamic | Configure automationedacontroller postgres certificates for verify-ca | Configure automationedacontroller postgres certificates for verify-ca | automationedacontroller_pg_cert_auth |
| 80 | 3.2% | install.yml → unnamed play → config_dynamic → config_dynamic | Configure automationgateway postgres certificates for verify-ca | Configure automationgateway postgres certificates for verify-ca | automationgateway_pg_cert_auth |
| 81 | 3.2% | install.yml → unnamed play → config_dynamic → config_dynamic | Ensure connectivity to hosts and gather facts | Ensure connectivity to hosts and gather facts | — |
| 82 | 3.3% | install.yml → unnamed play → config_dynamic → config_dynamic | Ensure user is root | Ensure user is root | ansible_effective_user_id != config_dynamic_user |
| 83 | 3.3% | install.yml → unnamed play → config_dynamic → config_dynamic | Check if node is registered with RHSM | Check if node is registered with RHSM | — |
| 84 | 3.3% | install.yml → unnamed play → config_dynamic → config_dynamic | set_fact | Set a host fact used by later tasks | — |
| 85 | 3.4% | install.yml → unnamed play → config_dynamic → config_dynamic | Collect ansible_hostname for localhost | Collect ansible_hostname for localhost | — |
| 86 | 3.4% | install.yml → unnamed play → config_dynamic → config_dynamic | Exclude {{ ansible_package_name }} from package upgrade if control node | Exclude {{ ansible_package_name }} from package upgrade if control node | ansible_hostname == hostvars['localhost']['ansible_hostname'] |
| 87 | 3.5% | install.yml → unnamed play → config_dynamic → config_dynamic | include_role | Include and run tasks from an Ansible role (role `automationhub`, tasks `dynamic_hostname_facts`) | inventory_hostname in groups.get('automationhub', []) |
| 88 | 3.5% | install.yml → unnamed play → config_dynamic → config_dynamic → automationhub | role `automationhub` (`dynamic_hostname_facts`) | Enter role task list | inventory_hostname in groups.get('automationhub', []) |
| 89 | 3.5% | install.yml → unnamed play → config_dynamic → config_dynamic → automationhub → automationhub | file `dynamic_hostname_facts.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/dynamic_hostname_facts.yml` | inventory_hostname in groups.get('automationhub', []) |
| 90 | 3.6% | install.yml → unnamed play → config_dynamic → config_dynamic → automationhub → automationhub | Set Hub hostname | Set Hub hostname | inventory_hostname in groups.get('automationhub', []) |
| 91 | 3.6% | install.yml → unnamed play → config_dynamic → config_dynamic → automationhub → automationhub | Determine proper protocol and port | Determine proper protocol and port | inventory_hostname in groups.get('automationhub', []) |
| 92 | 3.7% | install.yml → unnamed play → config_dynamic → config_dynamic → automationhub → automationhub | Determine Automation Hub main URL | Determine Automation Hub main URL | inventory_hostname in groups.get('automationhub', []) |
| 93 | 3.7% | install.yml → unnamed play → config_dynamic → config_dynamic → automationhub → automationhub | Check if automationhub_main_url starts with a http protocol | Check if automationhub_main_url starts with a http protocol | inventory_hostname in groups.get('automationhub', []); _automationhub_main_url is not regex("^https?://.*") |
| 94 | 3.7% | install.yml → unnamed play → config_dynamic → config_dynamic | include_role | Include and run tasks from an Ansible role (role `automationcontroller`, tasks `dynamic_hostname_facts`) | inventory_hostname in groups.get('automationcontroller', []) |
| 95 | 3.8% | install.yml → unnamed play → config_dynamic → config_dynamic → automationcontroller | role `automationcontroller` (`dynamic_hostname_facts`) | Enter role task list | inventory_hostname in groups.get('automationcontroller', []) |
| 96 | 3.8% | install.yml → unnamed play → config_dynamic → config_dynamic → automationcontroller → automationcontroller | file `dynamic_hostname_facts.yml` | Task file `ansible/automation_platform_installer/roles/automationcontroller/tasks/dynamic_hostname_facts.yml` | inventory_hostname in groups.get('automationcontroller', []) |
| 97 | 3.9% | install.yml → unnamed play → config_dynamic → config_dynamic → automationcontroller → automationcontroller | Set controller hostname | Set controller hostname | inventory_hostname in groups.get('automationcontroller', []) |
| 98 | 3.9% | install.yml → unnamed play → config_dynamic → config_dynamic → automationcontroller → automationcontroller | set_fact | Set a host fact used by later tasks | inventory_hostname in groups.get('automationcontroller', []); (_nginx_disable_https \| bool and _nginx_http_port \| string != '80') or (not _nginx_disable_https \| bool and _nginx_https_port \| string != '443') |
| 99 | 3.9% | install.yml → unnamed play → config_dynamic → config_dynamic → automationcontroller → automationcontroller | set_fact | Set a host fact used by later tasks | inventory_hostname in groups.get('automationcontroller', []) |
| 100 | 4.0% | install.yml → unnamed play → config_dynamic → config_dynamic | include_role | Include and run tasks from an Ansible role (role `automationgateway`, tasks `dynamic_hostname_facts`) | inventory_hostname in groups.get('automationgateway', []) |
| 101 | 4.0% | install.yml → unnamed play → config_dynamic → config_dynamic → automationgateway | role `automationgateway` (`dynamic_hostname_facts`) | Enter role task list | inventory_hostname in groups.get('automationgateway', []) |
| 102 | 4.1% | install.yml → unnamed play → config_dynamic → config_dynamic → automationgateway → automationgateway | file `dynamic_hostname_facts.yml` | Task file `ansible/automation_platform_installer/roles/automationgateway/tasks/dynamic_hostname_facts.yml` | inventory_hostname in groups.get('automationgateway', []) |
| 103 | 4.1% | install.yml → unnamed play → config_dynamic → config_dynamic → automationgateway → automationgateway | Set automation gateway hostname and port | Set automation gateway hostname and port | inventory_hostname in groups.get('automationgateway', []) |
| 104 | 4.1% | install.yml → unnamed play → config_dynamic → config_dynamic → automationgateway → automationgateway | Set automation gateway admin URL | Set automation gateway admin URL | inventory_hostname in groups.get('automationgateway', []) |
| 105 | 4.2% | install.yml → unnamed play → config_dynamic → config_dynamic | include_role | Include and run tasks from an Ansible role (role `automationgateway`, tasks `main_url_facts`) | 'automationgateway' in ansible_play_hosts \| map('extract', hostvars) \| map(attribute='group_names') \| flatten; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('automationhub', [])) \| union(groups.get('automationedacontroller', [])) \| union(groups.get('automationgateway', [])) |
| 106 | 4.2% | install.yml → unnamed play → config_dynamic → config_dynamic → automationgateway | role `automationgateway` (`main_url_facts`) | Enter role task list | 'automationgateway' in ansible_play_hosts \| map('extract', hostvars) \| map(attribute='group_names') \| flatten; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('automationhub', [])) \| union(groups.get('automationedacontroller', [])) \| union(groups.get('automationgateway', [])) |
| 107 | 4.3% | install.yml → unnamed play → config_dynamic → config_dynamic → automationgateway → automationgateway | file `main_url_facts.yml` | Task file `ansible/automation_platform_installer/roles/automationgateway/tasks/main_url_facts.yml` | 'automationgateway' in ansible_play_hosts \| map('extract', hostvars) \| map(attribute='group_names') \| flatten; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('automationhub', [])) \| union(groups.get('automationedacontroller', [])) \| union(groups.get('automationgateway', [])) |
| 108 | 4.3% | install.yml → unnamed play → config_dynamic → config_dynamic → automationgateway → automationgateway | Set automation gateway main URL | Set automation gateway main URL | 'automationgateway' in ansible_play_hosts \| map('extract', hostvars) \| map(attribute='group_names') \| flatten; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('automationhub', [])) \| union(groups.get('automationedacontroller', [])) \| union(groups.get('automationgateway', [])) |
| 109 | 4.3% | install.yml → unnamed play → config_dynamic → config_dynamic → automationgateway → automationgateway | Set automation gateway main hostname/port | Set automation gateway main hostname/port | 'automationgateway' in ansible_play_hosts \| map('extract', hostvars) \| map(attribute='group_names') \| flatten; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('automationhub', [])) \| union(groups.get('automationedacontroller', [])) \| union(groups.get('automationgateway', [])) |
| 110 | 4.4% | install.yml → unnamed play → config_dynamic → config_dynamic | include_role | Include and run tasks from an Ansible role (role `automationedacontroller`, tasks `dynamic_hostname_facts`) | inventory_hostname in groups.get('automationedacontroller', []) |
| 111 | 4.4% | install.yml → unnamed play → config_dynamic → config_dynamic → automationedacontroller | role `automationedacontroller` (`dynamic_hostname_facts`) | Enter role task list | inventory_hostname in groups.get('automationedacontroller', []) |
| 112 | 4.5% | install.yml → unnamed play → config_dynamic → config_dynamic → automationedacontroller → automationedacontroller | file `dynamic_hostname_facts.yml` | Task file `ansible/automation_platform_installer/roles/automationedacontroller/tasks/dynamic_hostname_facts.yml` | inventory_hostname in groups.get('automationedacontroller', []) |
| 113 | 4.5% | install.yml → unnamed play → config_dynamic → config_dynamic → automationedacontroller → automationedacontroller | Set controller hostname | Set controller hostname | inventory_hostname in groups.get('automationedacontroller', []) |
| 114 | 4.6% | install.yml → unnamed play → config_dynamic → config_dynamic → automationedacontroller → automationedacontroller | Set EDA hostname | Set EDA hostname | inventory_hostname in groups.get('automationedacontroller', []) |
| 115 | 4.6% | install.yml → unnamed play → config_dynamic → config_dynamic → automationedacontroller → automationedacontroller | Check if provided event-stream public URL begins with a http protocol | Check if provided event-stream public URL begins with a http protocol | inventory_hostname in groups.get('automationedacontroller', []); _automationedacontroller_public_event_stream_url is not regex("^https?://.*") |
| 116 | 4.6% | install.yml → unnamed play → config_dynamic → config_dynamic | include_role | Include and run tasks from an Ansible role (role `automationedacontroller`, tasks `check_node_type`) | — |
| 117 | 4.7% | install.yml → unnamed play → config_dynamic → config_dynamic → automationedacontroller | role `automationedacontroller` (`check_node_type`) | Enter role task list | — |
| 118 | 4.7% | install.yml → unnamed play → config_dynamic → config_dynamic → automationedacontroller → automationedacontroller | file `check_node_type.yml` | Task file `ansible/automation_platform_installer/roles/automationedacontroller/tasks/check_node_type.yml` | — |
| 119 | 4.8% | install.yml → unnamed play → config_dynamic → config_dynamic → automationedacontroller → automationedacontroller | block | Begin conditional or grouped task block | inventory_hostname in groups.get('automationedacontroller', []) |
| 120 | 4.8% | install.yml → unnamed play → config_dynamic → config_dynamic → automationedacontroller → automationedacontroller → block | Set eda_node_type if not defined | Set eda_node_type if not defined | inventory_hostname in groups.get('automationedacontroller', []); not eda_node_type \| default('') \| length |
| 121 | 4.8% | install.yml → unnamed play → config_dynamic → config_dynamic → automationedacontroller → automationedacontroller → block | Check that an eda_node_type exists for all automationedacontroller nodes | Check that an eda_node_type exists for all automationedacontroller nodes | inventory_hostname in groups.get('automationedacontroller', []); eda_node_type not in ['hybrid', 'api', 'worker', 'event-stream'] |
| 122 | 4.9% | install.yml → unnamed play → config_dynamic → config_dynamic → automationedacontroller → automationedacontroller | set_fact | Set a host fact used by later tasks | — |
| 123 | 4.9% | install.yml → unnamed play → config_dynamic → config_dynamic | Ensure that a worker and an api node exist if no hybrid nodes are present for automationedacontroller | Ensure that a worker and an api node exist if no hybrid nodes are present for automationedacontroller | groups['automationedacontroller'] \| default([]) \| length > 0; _automationedacontroller_hybrid_hosts \| length == 0; _automationedacontroller_web_hosts \| length < 1 or _automationedacontroller_worker_hosts \| length < 1 |
| 124 | 5.0% | install.yml → unnamed play → config_dynamic → config_dynamic | Ensure that at least 1 event_stream node exists in inventory with Event-Driven Ansible controller | Ensure that at least 1 event_stream node exists in inventory with Event-Driven Ansible controller | groups['automationedacontroller'] \| default([]) \| length > 0; _automationedacontroller_event_stream_hosts \| length < 1 |
| 125 | 5.0% | install.yml → unnamed play → config_dynamic → config_dynamic | block | Begin conditional or grouped task block | inventory_hostname in groups.get('automationgateway', []) |
| 126 | 5.0% | install.yml → unnamed play → config_dynamic → config_dynamic → block | include_role | Include and run tasks from an Ansible role (role `automationcontroller`, tasks `service_port`) | inventory_hostname in groups.get('automationgateway', []); groups.get('automationcontroller', []) \| length > 0 |
| 127 | 5.1% | install.yml → unnamed play → config_dynamic → config_dynamic → block → automationcontroller | role `automationcontroller` (`service_port`) | Enter role task list | inventory_hostname in groups.get('automationgateway', []); groups.get('automationcontroller', []) \| length > 0 |
| 128 | 5.1% | install.yml → unnamed play → config_dynamic → config_dynamic → block → automationcontroller → automationcontroller | file `service_port.yml` | Task file `ansible/automation_platform_installer/roles/automationcontroller/tasks/service_port.yml` | inventory_hostname in groups.get('automationgateway', []); groups.get('automationcontroller', []) \| length > 0 |
| 129 | 5.2% | install.yml → unnamed play → config_dynamic → config_dynamic → block → automationcontroller → automationcontroller | Set Automation Controller service port for Platform Gateway | Set Automation Controller service port for Platform Gateway | inventory_hostname in groups.get('automationgateway', []); groups.get('automationcontroller', []) \| length > 0 |
| 130 | 5.2% | install.yml → unnamed play → config_dynamic → config_dynamic → block | include_role | Include and run tasks from an Ansible role (role `automationhub`, tasks `service_port`) | inventory_hostname in groups.get('automationgateway', []); groups.get('automationhub', []) \| length > 0 |
| 131 | 5.2% | install.yml → unnamed play → config_dynamic → config_dynamic → block → automationhub | role `automationhub` (`service_port`) | Enter role task list | inventory_hostname in groups.get('automationgateway', []); groups.get('automationhub', []) \| length > 0 |
| 132 | 5.3% | install.yml → unnamed play → config_dynamic → config_dynamic → block → automationhub → automationhub | file `service_port.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/service_port.yml` | inventory_hostname in groups.get('automationgateway', []); groups.get('automationhub', []) \| length > 0 |
| 133 | 5.3% | install.yml → unnamed play → config_dynamic → config_dynamic → block → automationhub → automationhub | Set Automation Hub service port for Platform Gateway | Set Automation Hub service port for Platform Gateway | inventory_hostname in groups.get('automationgateway', []); groups.get('automationhub', []) \| length > 0 |
| 134 | 5.4% | install.yml → unnamed play → config_dynamic → config_dynamic → block | include_role | Include and run tasks from an Ansible role (role `automationedacontroller`, tasks `service_port`) | inventory_hostname in groups.get('automationgateway', []); groups.get('automationedacontroller', []) \| length > 0 |
| 135 | 5.4% | install.yml → unnamed play → config_dynamic → config_dynamic → block → automationedacontroller | role `automationedacontroller` (`service_port`) | Enter role task list | inventory_hostname in groups.get('automationgateway', []); groups.get('automationedacontroller', []) \| length > 0 |
| 136 | 5.4% | install.yml → unnamed play → config_dynamic → config_dynamic → block → automationedacontroller → automationedacontroller | file `service_port.yml` | Task file `ansible/automation_platform_installer/roles/automationedacontroller/tasks/service_port.yml` | inventory_hostname in groups.get('automationgateway', []); groups.get('automationedacontroller', []) \| length > 0 |
| 137 | 5.5% | install.yml → unnamed play → config_dynamic → config_dynamic → block → automationedacontroller → automationedacontroller | Set Automation EDA Controller service port for Platform Gateway | Set Automation EDA Controller service port for Platform Gateway | inventory_hostname in groups.get('automationgateway', []); groups.get('automationedacontroller', []) \| length > 0 |
| 138 | 5.5% | install.yml → unnamed play → config_dynamic → config_dynamic → block → automationedacontroller → automationedacontroller | Set Automation EDA Controller event-stream service port for Platform Gateway | Set Automation EDA Controller event-stream service port for Platform Gateway | inventory_hostname in groups.get('automationgateway', []); groups.get('automationedacontroller', []) \| length > 0 |
| 139 | 5.6% | install.yml | Group supported distributions | Play targeting `aap_valid_hosts` | — |
| 140 | 5.6% | install.yml → Group supported distributions | group hosts for supported distributions | group hosts for supported distributions | ansible_distribution == "RedHat"; ansible_distribution_major_version == "9"; ansible_distribution_version is version_compare('9.6', '>=') |
| 141 | 5.6% | install.yml | Group deprecated distributions | Play targeting `none` | — |
| 142 | 5.7% | install.yml → Group deprecated distributions | group hosts for supported distributions | group hosts for supported distributions | — |
| 143 | 5.7% | install.yml | Ensure all node OS distributions are supported | Play targeting `aap_valid_hosts:!supported` | — |
| 144 | 5.8% | install.yml → Ensure all node OS distributions are supported | fail for unsupported distribution | fail for unsupported distribution | — |
| 145 | 5.8% | install.yml | Check for deprecated platforms | Play targeting `aap_valid_hosts:&deprecated` | — |
| 146 | 5.8% | install.yml → Check for deprecated platforms | warn for deprecated distribution | warn for deprecated distribution | — |
| 147 | 5.9% | install.yml → Check for deprecated platforms | pause for warning | pause for warning | — |
| 148 | 5.9% | install.yml | Define role discovered variables, usable throughout the playbook | Play targeting `automationgateway:automationcontroller:automationedacontroller:automationhub:database` | — |
| 149 | 6.0% | install.yml → Define role discovered variables, usable throughout the playbook | role `postgres` | Enter role task list | — |
| 150 | 6.0% | install.yml → Define role discovered variables, usable throughout the playbook → postgres | file `main.yml` | Task file `ansible/automation_platform_installer/roles/postgres/tasks/main.yml` | — |
| 151 | 6.0% | install.yml → Define role discovered variables, usable throughout the playbook → postgres | include_tasks | Include a task file from the current role or playbook (file `vars.yml`) | — |
| 152 | 6.1% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → vars.yml | file `vars.yml` | Task file `ansible/automation_platform_installer/roles/postgres/tasks/vars.yml` | — |
| 153 | 6.1% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → vars.yml | include_vars | Load variables from a file | — |
| 154 | 6.2% | install.yml → Define role discovered variables, usable throughout the playbook → postgres | Ensure all locales in system are in UTF-8 | Ensure all locales in system are in UTF-8 (role `preflight`, tasks `locale.yaml`) | — |
| 155 | 6.2% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → preflight | role `preflight` (`locale.yaml`) | Enter role task list | — |
| 156 | 6.2% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → preflight → preflight | file `locale.yaml` | Task file `ansible/automation_platform_installer/roles/preflight/tasks/locale.yaml` | — |
| 157 | 6.3% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → preflight → preflight | Load /etc/locale.conf | Load /etc/locale.conf | — |
| 158 | 6.3% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → preflight → preflight | fail | Fail the play with an error message | item is not regex(".*(utf8\|UTF-8)") |
| 159 | 6.4% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → preflight → preflight | Get the LANG environment variable | Get the LANG environment variable | item is regex("^LANG=.*") |
| 160 | 6.4% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → preflight → preflight | Check if locale is properly installed | Check if locale is properly installed | — |
| 161 | 6.4% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → preflight → preflight | fail | Fail the play with an error message | _locale.rc != 0 |
| 162 | 6.5% | install.yml → Define role discovered variables, usable throughout the playbook → postgres | include_tasks | Include a task file from the current role or playbook (file `preflight.yml`) | not postgres_exec_vars_only \| bool |
| 163 | 6.5% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → preflight.yml | file `preflight.yml` | Task file `ansible/automation_platform_installer/roles/postgres/tasks/preflight.yml` | not postgres_exec_vars_only \| bool |
| 164 | 6.6% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → preflight.yml | Preflight check - Ensure that the Postgres data dir is not a mount point | Preflight check - Ensure that the Postgres data dir is not a mount point | not postgres_exec_vars_only \| bool |
| 165 | 6.6% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → preflight.yml | Fail if the data dir is a mount point | Fail if the data dir is a mount point | not postgres_exec_vars_only \| bool; is_pg_data_mount.rc  \| int == 0 |
| 166 | 6.6% | install.yml → Define role discovered variables, usable throughout the playbook → postgres | include_tasks | Include a task file from the current role or playbook (file `init.yml`) | not postgres_exec_vars_only \| bool |
| 167 | 6.7% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → init.yml | file `init.yml` | Task file `ansible/automation_platform_installer/roles/postgres/tasks/init.yml` | not postgres_exec_vars_only \| bool |
| 168 | 6.7% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → init.yml | init postgresql | init postgresql | not postgres_exec_vars_only \| bool |
| 169 | 6.8% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → init.yml | Open up postgres port permissions | Open up postgres port permissions | not postgres_exec_vars_only \| bool; ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 170 | 6.8% | install.yml → Define role discovered variables, usable throughout the playbook → postgres | include_tasks | Include a task file from the current role or playbook (file `conf.yml`) | not postgres_exec_vars_only \| bool |
| 171 | 6.8% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml | file `conf.yml` | Task file `ansible/automation_platform_installer/roles/postgres/tasks/conf.yml` | not postgres_exec_vars_only \| bool |
| 172 | 6.9% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml | Update postgresql authentication settings | Update postgresql authentication settings (dest `{{ pg_hba_location }}`) | not postgres_exec_vars_only \| bool |
| 173 | 6.9% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml | Apply postgresql general settings | Apply postgresql general settings (dest `{{ pg_conf_location }}`) | not postgres_exec_vars_only \| bool |
| 174 | 7.0% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml | Copy server SSL certificate | Copy server SSL certificate (dest `{{ pg_conf_dir }}/server.crt`) | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is defined; postgres_ssl_key is defined |
| 175 | 7.0% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml | Copy server SSL private key | Copy server SSL private key (dest `{{ pg_conf_dir }}/server.key`) | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is defined; postgres_ssl_key is defined |
| 176 | 7.0% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml | block | Begin conditional or grouped task block | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined |
| 177 | 7.1% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block | Use internal CA for internal database | Use internal CA for internal database (role `certificate_authority`, tasks `sign_service.yml`) | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 178 | 7.1% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority | role `certificate_authority` (`sign_service.yml`) | Enter role task list | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 179 | 7.2% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority | file `sign_service.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/sign_service.yml` | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 180 | 7.2% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority | include_tasks | Include a task file from the current role or playbook (file `init.yml`) | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 181 | 7.2% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority → init.yml | file `init.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/init.yml` | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 182 | 7.3% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority → init.yml | Ensure python cryptography libraries are installed | Ensure python cryptography libraries are installed | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 183 | 7.3% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority | Create {{ aap_ca_servicename }} private key | Create {{ aap_ca_servicename }} private key | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 184 | 7.4% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority | set_fact | Set a host fact used by later tasks | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 185 | 7.4% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority | Check if {{ aap_ca_servicename }} public key exists | Check if {{ aap_ca_servicename }} public key exists | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 186 | 7.5% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; certificate_stat.stat.exists \| bool |
| 187 | 7.5% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Check contents of {{ aap_ca_servicename }} public key | Check contents of {{ aap_ca_servicename }} public key | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; certificate_stat.stat.exists \| bool |
| 188 | 7.5% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; certificate_stat.stat.exists \| bool; aap_service_san_records_internal \| union(aap_service_san_records) \| difference(certificate_check.subject_alt_name) \| length > 0 |
| 189 | 7.6% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 190 | 7.6% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false); aap_service_is_client |
| 191 | 7.7% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Create CSR for {{ aap_ca_servicename }} node/s | Create CSR for {{ aap_ca_servicename }} node/s | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 192 | 7.7% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Sign {{ aap_ca_servicename }} public key | Sign {{ aap_ca_servicename }} public key | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 193 | 7.7% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Write public key to {{ aap_ca_servicename }} node/s | Write public key to {{ aap_ca_servicename }} node/s (dest `{{ aap_service_cert_fullpath }}`) | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 194 | 7.8% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority | Signal restart if certificate pair changes | Signal restart if certificate pair changes | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; (write_key is changed or write_certificate is changed) |
| 195 | 7.8% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block | Use internal CA for external database | Use internal CA for external database (role `certificate_authority`, tasks `sign_service.yml`) | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 196 | 7.9% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority | role `certificate_authority` (`sign_service.yml`) | Enter role task list | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 197 | 7.9% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority | file `sign_service.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/sign_service.yml` | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 198 | 7.9% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority | include_tasks | Include a task file from the current role or playbook (file `init.yml`) | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 199 | 8.0% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority → init.yml | file `init.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/init.yml` | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 200 | 8.0% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority → init.yml | Ensure python cryptography libraries are installed | Ensure python cryptography libraries are installed | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 201 | 8.1% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority | Create {{ aap_ca_servicename }} private key | Create {{ aap_ca_servicename }} private key | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 202 | 8.1% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority | set_fact | Set a host fact used by later tasks | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 203 | 8.1% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority | Check if {{ aap_ca_servicename }} public key exists | Check if {{ aap_ca_servicename }} public key exists | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 204 | 8.2% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; certificate_stat.stat.exists \| bool |
| 205 | 8.2% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Check contents of {{ aap_ca_servicename }} public key | Check contents of {{ aap_ca_servicename }} public key | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; certificate_stat.stat.exists \| bool |
| 206 | 8.3% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; certificate_stat.stat.exists \| bool; aap_service_san_records_internal \| union(aap_service_san_records) \| difference(certificate_check.subject_alt_name) \| length > 0 |
| 207 | 8.3% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 208 | 8.3% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false); aap_service_is_client |
| 209 | 8.4% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Create CSR for {{ aap_ca_servicename }} node/s | Create CSR for {{ aap_ca_servicename }} node/s | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 210 | 8.4% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Sign {{ aap_ca_servicename }} public key | Sign {{ aap_ca_servicename }} public key | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 211 | 8.5% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Write public key to {{ aap_ca_servicename }} node/s | Write public key to {{ aap_ca_servicename }} node/s (dest `{{ aap_service_cert_fullpath }}`) | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 212 | 8.5% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority | Signal restart if certificate pair changes | Signal restart if certificate pair changes | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; (write_key is changed or write_certificate is changed) |
| 213 | 8.5% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml | Create unix socket directory | Create unix socket directory | not postgres_exec_vars_only \| bool |
| 214 | 8.6% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml | Configure postgresql tmpfiles.d | Configure postgresql tmpfiles.d (dest `/usr/lib/tmpfiles.d/postgresql.conf`) | not postgres_exec_vars_only \| bool |
| 215 | 8.6% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml | set_fact | Set a host fact used by later tasks | not postgres_exec_vars_only \| bool; aap_service_restart \| default(false) or pg_hba_conf is changed or pg_general_conf is changed |
| 216 | 8.7% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml | ensure postgresql is running and restart when authentication settings change | ensure postgresql is running and restart when authentication settings change (service `{{ postgres_init_name }}`) | not postgres_exec_vars_only \| bool |
| 217 | 8.7% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml | wait for postgresql restart | wait for postgresql restart | not postgres_exec_vars_only \| bool; postgresql_restart is changed |
| 218 | 8.7% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml | configure postgresql to start automatically | configure postgresql to start automatically (service `{{ postgres_init_name }}`) | not postgres_exec_vars_only \| bool |
| 219 | 8.8% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml | block | Begin conditional or grouped task block | not postgres_exec_vars_only \| bool; groups['automationcontroller'] \| default([]) \| length |
| 220 | 8.8% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block | create the awx postgres user | create the awx postgres user | not postgres_exec_vars_only \| bool; groups['automationcontroller'] \| default([]) \| length; postgres_username is defined and postgres_username != ''; postgres_password is defined and postgres_password != '' |
| 221 | 8.9% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block | create the postgresql database for awx | create the postgresql database for awx | not postgres_exec_vars_only \| bool; groups['automationcontroller'] \| default([]) \| length; postgres_username is defined and postgres_username != ''; postgres_database is defined and postgres_database != '' |
| 222 | 8.9% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml | block | Begin conditional or grouped task block | not postgres_exec_vars_only \| bool; groups['automationhub'] \| default([]) \| length |
| 223 | 8.9% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block | create the automationhub postgres user | create the automationhub postgres user | not postgres_exec_vars_only \| bool; groups['automationhub'] \| default([]) \| length; automationhub_postgres_username is defined and automationhub_postgres_username != ''; automationhub_postgres_password is defined and automationhub_postgres_password != '' |
| 224 | 9.0% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block | create the postgresql database for automationhub | create the postgresql database for automationhub | not postgres_exec_vars_only \| bool; groups['automationhub'] \| default([]) \| length; automationhub_postgres_username is defined and automationhub_postgres_username != ''; automationhub_postgres_database is defined and automationhub_postgres_database != '' |
| 225 | 9.0% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block | enable the postgresql hstore extension | enable the postgresql hstore extension | not postgres_exec_vars_only \| bool; groups['automationhub'] \| default([]) \| length |
| 226 | 9.1% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml | block | Begin conditional or grouped task block | not postgres_exec_vars_only \| bool; groups['automationedacontroller'] \| default([]) \| length |
| 227 | 9.1% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block | create the automationedacontroller postgres user | create the automationedacontroller postgres user | not postgres_exec_vars_only \| bool; groups['automationedacontroller'] \| default([]) \| length; automationedacontroller_postgres_username is defined and automationedacontroller_postgres_username != ''; automationedacontroller_postgres_password is defined and automationedacontroller_postgres_password != '' |
| 228 | 9.1% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block | create the postgresql database for automationedacontroller | create the postgresql database for automationedacontroller | not postgres_exec_vars_only \| bool; groups['automationedacontroller'] \| default([]) \| length; automationedacontroller_postgres_username is defined and automationedacontroller_postgres_username != ''; automationedacontroller_postgres_database is defined and automationedacontroller_postgres_database != '' |
| 229 | 9.2% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml | block | Begin conditional or grouped task block | not postgres_exec_vars_only \| bool; groups['automationgateway'] \| default([]) \| length |
| 230 | 9.2% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block | create the automationgateway postgres user | create the automationgateway postgres user | not postgres_exec_vars_only \| bool; groups['automationgateway'] \| default([]) \| length; automationgateway_postgres_username is defined and automationgateway_postgres_username != ''; automationgateway_postgres_password is defined and automationgateway_postgres_password != '' |
| 231 | 9.3% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block | create the postgresql database for automationgateway | create the postgresql database for automationgateway | not postgres_exec_vars_only \| bool; groups['automationgateway'] \| default([]) \| length; automationgateway_postgres_username is defined and automationgateway_postgres_username != ''; automationgateway_postgres_database is defined and automationgateway_postgres_database != '' |
| 232 | 9.3% | install.yml | Sanity check | Play targeting `automationgateway:automationcontroller:automationedacontroller:automationhub` | — |
| 233 | 9.3% | install.yml → Sanity check | include_role | Include and run tasks from an Ansible role (role `preflight`) | — |
| 234 | 9.4% | install.yml → Sanity check → preflight | role `preflight` | Enter role task list | — |
| 235 | 9.4% | install.yml → Sanity check → preflight → preflight | file `main.yml` | Task file `ansible/automation_platform_installer/roles/preflight/tasks/main.yml` | — |
| 236 | 9.5% | install.yml → Sanity check → preflight → preflight | Check if service nodes are collocated on the same host | Check if service nodes are collocated on the same host | aap_component_nodes \| map('extract', hostvars, 'routable_hostname') \| list \| unique \| length \| int != aap_component_node_count \| int |
| 237 | 9.5% | install.yml → Sanity check → preflight → preflight | Preflight check - Check if AAP VERSION file exists | Preflight check - Check if AAP VERSION file exists | — |
| 238 | 9.5% | install.yml → Sanity check → preflight → preflight | block | Begin conditional or grouped task block | _aap_version_file.stat.exists \| bool |
| 239 | 9.6% | install.yml → Sanity check → preflight → preflight → block | Preflight check - Obtain AAP version | Preflight check - Obtain AAP version | _aap_version_file.stat.exists \| bool |
| 240 | 9.6% | install.yml → Sanity check → preflight → preflight → block | Preflight check - User must be on AAP 2.4 to upgrade | Preflight check - User must be on AAP 2.4 to upgrade | _aap_version_file.stat.exists \| bool; _aap_version['content'] \| b64decode \| trim is version('2.4', '<') |
| 241 | 9.7% | install.yml → Sanity check → preflight → preflight → block | Preflight check - Ensure AAP 2.4 EDA is not included | Preflight check - Ensure AAP 2.4 EDA is not included | _aap_version_file.stat.exists \| bool; inventory_hostname in groups.get('automationedacontroller', []); _aap_version['content'] \| b64decode \| trim is version('2.4', '==') |
| 242 | 9.7% | install.yml → Sanity check → preflight → preflight | Preflight check - Fail if this machine lacks sufficient RAM. | Preflight check - Fail if this machine lacks sufficient RAM. | ansible_memtotal_mb < required_ram\|int; not ignore_preflight_errors \| bool |
| 243 | 9.7% | install.yml → Sanity check → preflight → preflight | Preflight check - Ensure all locales in system are in UTF-8 | Preflight check - Ensure all locales in system are in UTF-8 (file `locale.yaml`) | — |
| 244 | 9.8% | install.yml → Sanity check → preflight → preflight → locale.yaml | file `locale.yaml` | Task file `ansible/automation_platform_installer/roles/preflight/tasks/locale.yaml` | — |
| 245 | 9.8% | install.yml → Sanity check → preflight → preflight → locale.yaml | Load /etc/locale.conf | Load /etc/locale.conf | — |
| 246 | 9.9% | install.yml → Sanity check → preflight → preflight → locale.yaml | fail | Fail the play with an error message | item is not regex(".*(utf8\|UTF-8)") |
| 247 | 9.9% | install.yml → Sanity check → preflight → preflight → locale.yaml | Get the LANG environment variable | Get the LANG environment variable | item is regex("^LANG=.*") |
| 248 | 9.9% | install.yml → Sanity check → preflight → preflight → locale.yaml | Check if locale is properly installed | Check if locale is properly installed | — |
| 249 | 10.0% | install.yml → Sanity check → preflight → preflight → locale.yaml | fail | Fail the play with an error message | _locale.rc != 0 |
| 250 | 10.0% | install.yml → Sanity check → preflight → preflight | Preflight check - Get /var/log permissions | Preflight check - Get /var/log permissions | — |
| 251 | 10.1% | install.yml → Sanity check → preflight → preflight | Preflight check - clock management services | Preflight check - clock management services | groups.get('aap_valid_hosts') \| length > 1; not ansible_facts.services.get('chronyd.service', {}) or ansible_facts.services['chronyd.service'].state != 'running'; not ansible_facts.services.get('ntpd.service', {}) or ansible_facts.services['ntpd.service'].state != 'running'; not ansible_facts.services.get('systemd-timesyncd.service', {}) or ansible_facts.services['systemd-timesyncd.service'].state != 'running' |
| 252 | 10.1% | install.yml → Sanity check → preflight → preflight | Preflight check - Does /var/log have appropriate permissions? | Preflight check - Does /var/log have appropriate permissions? | not vl_st.stat.roth |
| 253 | 10.1% | install.yml → Sanity check → preflight → preflight | include_role | Include and run tasks from an Ansible role (role `redis`, tasks `check_cluster`) | — |
| 254 | 10.2% | install.yml → Sanity check → preflight → preflight → redis | role `redis` (`check_cluster`) | Enter role task list | — |
| 255 | 10.2% | install.yml → Sanity check → preflight → preflight → redis → redis | file `check_cluster.yml` | Task file `ansible/automation_platform_installer/roles/redis/tasks/check_cluster.yml` | — |
| 256 | 10.3% | install.yml → Sanity check → preflight → preflight → redis → redis | Check for cluster init file on all redis nodes | Check for cluster init file on all redis nodes | — |
| 257 | 10.3% | install.yml → Sanity check → preflight → preflight → redis → redis | Determine if redis was previously installed in cluster mode | Determine if redis was previously installed in cluster mode | redis_cluster_init_file.stat.exists \| bool |
| 258 | 10.4% | install.yml → Sanity check → preflight → preflight → redis → redis | Set redis cluster determination | Set redis cluster determination | — |
| 259 | 10.4% | install.yml → Sanity check → preflight → preflight | Check for standalone and if redis was previously clustered | Check for standalone and if redis was previously clustered | _redis_mode == 'standalone'; _redis_is_clustered |
| 260 | 10.4% | install.yml → Sanity check → preflight → preflight | Preflight check - Fail if installer is configured for postgres certificate auth and if the postgres database is managed by the installer | Preflight check - Fail if installer is configured for postgres certificate auth and if the postgres database is managed by the installer | groups['database'] \| default([]) \| length; awx_pg_cert_auth or automationhub_pg_cert_auth or automationedacontroller_pg_cert_auth or automationgateway_pg_cert_auth |
| 261 | 10.5% | install.yml → Sanity check → preflight → preflight | Preflight - Fail if Automation Gateway hostname is not FQDN or IP address | Preflight - Fail if Automation Gateway hostname is not FQDN or IP address | not _automationgateway_main_url \| urlsplit('hostname') is search("\.") |
| 262 | 10.5% | install.yml → Sanity check → preflight → preflight | Preflight check - Read in controller version | Preflight check - Read in controller version | inventory_hostname not in groups['automationhub'] \| default([]); inventory_hostname not in groups['automationedacontroller'] \| default([]) |
| 263 | 10.6% | install.yml → Sanity check → preflight → preflight | Preflight check - Perform fresh install checks | Begin conditional or grouped task block | result.content is not defined |
| 264 | 10.6% | install.yml → Sanity check → preflight → preflight → Preflight check - Perform fresh install checks | Preflight check - Admin password must be defined for a fresh install | Preflight check - Admin password must be defined for a fresh install | result.content is not defined; admin_password\|default('') == ''; groups['automationcontroller'] \| default([]) \| length |
| 265 | 10.6% | install.yml → Sanity check → preflight → preflight → Preflight check - Perform fresh install checks | Preflight check - Postgres password must be defined for a fresh install | Preflight check - Postgres password must be defined for a fresh install | result.content is not defined; pg_password\|default('') == ''; not awx_pg_cert_auth; not automationcontroller_db_template \| default([]) \| length; groups['automationcontroller'] \| default([]) \| length |
| 266 | 10.7% | install.yml → Sanity check → preflight → preflight → Preflight check - Perform fresh install checks | Preflight check - Automation hub admin password must be defined for a fresh install (if hub is enabled) | Preflight check - Automation hub admin password must be defined for a fresh install (if hub is enabled) | result.content is not defined; automationhub_admin_password\|default('') == ''; groups['automationhub'] \| default([]) \| length |
| 267 | 10.7% | install.yml → Sanity check → preflight → preflight → Preflight check - Perform fresh install checks | Preflight check - Automation hub postgres password must be defined for a fresh install (if hub is enabled and not using client certificate authentication) | Preflight check - Automation hub postgres password must be defined for a fresh install (if hub is enabled and not using client certificate authentication) | result.content is not defined; automationhub_pg_password\|default('') == ''; not automationhub_pg_cert_auth; not automationhub_db_template \| default([]) \| length; groups['automationhub'] \| default([]) \| length |
| 268 | 10.8% | install.yml → Sanity check → preflight → preflight → Preflight check - Perform fresh install checks | Preflight check - Automation EDA controller database password must be defined for a fresh install (if eda is enabled) | Preflight check - Automation EDA controller database password must be defined for a fresh install (if eda is enabled) | result.content is not defined; not automationedacontroller_pg_password \| default('') \| length; not automationedacontroller_pg_cert_auth; not automationedacontroller_db_template \| default([]) \| length; groups['automationedacontroller'] \| default([]) \| length |
| 269 | 10.8% | install.yml → Sanity check → preflight → preflight → Preflight check - Perform fresh install checks | Find mount options for /var, /var/tmp, and /tmp | Find mount options for /var, /var/tmp, and /tmp | result.content is not defined |
| 270 | 10.8% | install.yml → Sanity check → preflight → preflight → Preflight check - Perform fresh install checks | Fail if the filesystem is mounted with noexec | Fail if the filesystem is mounted with noexec | result.content is not defined; ((item is changed) or (item is failed)); not ignore_preflight_errors \| bool |
| 271 | 10.9% | install.yml → Sanity check → preflight → preflight → Preflight check - Perform fresh install checks | Ensure hostname on nodes with receptor installed is not localhost | Ensure hostname on nodes with receptor installed is not localhost | result.content is not defined; ansible_hostname is match('localhost*'); inventory_hostname in groups['automationcontroller'] \| default([]) or inventory_hostname in groups['execution_nodes'] \| default([]) |
| 272 | 10.9% | install.yml → Sanity check → preflight → preflight | Preflight check - Perform upgrade checks | Begin conditional or grouped task block | result.content is defined |
| 273 | 11.0% | install.yml → Sanity check → preflight → preflight → Preflight check - Perform upgrade checks | Preflight check - User must be on Automation controller 4.5 to upgrade | Preflight check - User must be on Automation controller 4.5 to upgrade | result.content is defined; result.content \| b64decode is version_compare('4.5', '<') |
| 274 | 11.0% | install.yml → Sanity check → preflight → preflight → Preflight check - Perform upgrade checks | Preflight check - check for custom virtual environments | Preflight check - check for custom virtual environments | result.content is defined |
| 275 | 11.0% | install.yml → Sanity check → preflight → preflight → Preflight check - Perform upgrade checks | Preflight check - check for custom virtual environment path setting | Preflight check - check for custom virtual environment path setting | result.content is defined |
| 276 | 11.1% | install.yml → Sanity check → preflight → preflight → Preflight check - Perform upgrade checks | Preflight check - warn if custom virtual environments present | Preflight check - warn if custom virtual environments present | result.content is defined; custom_venvs.matched > 0 or custom_venv_paths.stdout != '[]' |
| 277 | 11.1% | install.yml → Sanity check → preflight → preflight → Preflight check - Perform upgrade checks | Preflight check - pause if warning | Preflight check - pause if warning | result.content is defined; custom_venvs.matched > 0 or custom_venv_paths.stdout != '[]' |
| 278 | 11.2% | install.yml → Sanity check → preflight → preflight → Preflight check - Perform upgrade checks | Preflight check - Detect partial installation or upgrade | Preflight check - Detect partial installation or upgrade | result.content is defined |
| 279 | 11.2% | install.yml → Sanity check → preflight → preflight → Preflight check - Perform upgrade checks | Preflight check - check license type | Preflight check - check license type | result.content is defined; migrations_up_to_date.rc == 0 |
| 280 | 11.2% | install.yml → Sanity check → preflight → preflight → Preflight check - Perform upgrade checks | Preflight check - check license type (detailed) | Preflight check - check license type (detailed) | result.content is defined; migrations_up_to_date.rc == 0 |
| 281 | 11.3% | install.yml → Sanity check → preflight → preflight → Preflight check - Perform upgrade checks | Preflight check - check license validity for upgrades | Preflight check - check license validity for upgrades | result.content is defined; 'stdout' in license_type and license_type.stdout == 'open' |
| 282 | 11.3% | install.yml → Sanity check → preflight → preflight | block | Begin conditional or grouped task block | groups['automationcontroller'] \| default([]) \| length; not (groups['database'] \| default([]) \| length); config_dynamic_database == "external"; inventory_hostname == groups['automationcontroller'][0] |
| 283 | 11.4% | install.yml → Sanity check → preflight → preflight → block | Preflight check - Ensure that psycopg2 python library is installed | Preflight check - Ensure that psycopg2 python library is installed | groups['automationcontroller'] \| default([]) \| length; not (groups['database'] \| default([]) \| length); config_dynamic_database == "external"; inventory_hostname == groups['automationcontroller'][0] |
| 284 | 11.4% | install.yml → Sanity check → preflight → preflight → block | set_fact | Set a host fact used by later tasks | groups['automationcontroller'] \| default([]) \| length; not (groups['database'] \| default([]) \| length); config_dynamic_database == "external"; inventory_hostname == groups['automationcontroller'][0]; custom_ca_cert \| default('') \| length; awx_pg_cert_auth |
| 285 | 11.4% | install.yml → Sanity check → preflight → preflight → block | Preflight check - Copy cacert when pg sslmode supports certificate auth | Preflight check - Copy cacert when pg sslmode supports certificate auth (dest `/root/cacert.pem`) | groups['automationcontroller'] \| default([]) \| length; not (groups['database'] \| default([]) \| length); config_dynamic_database == "external"; inventory_hostname == groups['automationcontroller'][0]; verify_custom_ca_cert \| default(false) |
| 286 | 11.5% | install.yml → Sanity check → preflight → preflight → block | Preflight check - Copy pg sslcert when pg sslmode supports certificate auth | Preflight check - Copy pg sslcert when pg sslmode supports certificate auth (dest `/root/pg_client.crt`) | groups['automationcontroller'] \| default([]) \| length; not (groups['database'] \| default([]) \| length); config_dynamic_database == "external"; inventory_hostname == groups['automationcontroller'][0]; awx_pg_cert_auth |
| 287 | 11.5% | install.yml → Sanity check → preflight → preflight → block | Preflight check - Copy pg sslkey when pg sslmode supports certificate auth | Preflight check - Copy pg sslkey when pg sslmode supports certificate auth (dest `/root/pg_client.key`) | groups['automationcontroller'] \| default([]) \| length; not (groups['database'] \| default([]) \| length); config_dynamic_database == "external"; inventory_hostname == groups['automationcontroller'][0]; awx_pg_cert_auth |
| 288 | 11.6% | install.yml → Sanity check → preflight → preflight → block | Preflight check - Ensure User-Provided Postgres Instance is at Version 15 or Higher | Preflight check - Ensure User-Provided Postgres Instance is at Version 15 or Higher | groups['automationcontroller'] \| default([]) \| length; not (groups['database'] \| default([]) \| length); config_dynamic_database == "external"; inventory_hostname == groups['automationcontroller'][0] |
| 289 | 11.6% | install.yml → Sanity check → preflight → preflight → block | set_fact | Set a host fact used by later tasks | groups['automationcontroller'] \| default([]) \| length; not (groups['database'] \| default([]) \| length); config_dynamic_database == "external"; inventory_hostname == groups['automationcontroller'][0] |
| 290 | 11.6% | install.yml → Sanity check → preflight → preflight → block | assert | Validate a condition and fail if it is not met | groups['automationcontroller'] \| default([]) \| length; not (groups['database'] \| default([]) \| length); config_dynamic_database == "external"; inventory_hostname == groups['automationcontroller'][0] |
| 291 | 11.7% | install.yml → Sanity check → preflight → preflight → block | Preflight check - Ensure User-Provided Postgres Instance is UTF-8 | Preflight check - Ensure User-Provided Postgres Instance is UTF-8 | groups['automationcontroller'] \| default([]) \| length; not (groups['database'] \| default([]) \| length); config_dynamic_database == "external"; inventory_hostname == groups['automationcontroller'][0] |
| 292 | 11.7% | install.yml → Sanity check → preflight → preflight → block | Preflight check - remove temporary cacert when its provided | Preflight check - remove temporary cacert when its provided | groups['automationcontroller'] \| default([]) \| length; not (groups['database'] \| default([]) \| length); config_dynamic_database == "external"; inventory_hostname == groups['automationcontroller'][0]; verify_custom_ca_cert \| default(false) |
| 293 | 11.8% | install.yml → Sanity check → preflight → preflight → block | Preflight check - remove temporary pg sslcert and sslkey for pg sslmode when it supports certificate auth | Preflight check - remove temporary pg sslcert and sslkey for pg sslmode when it supports certificate auth | groups['automationcontroller'] \| default([]) \| length; not (groups['database'] \| default([]) \| length); config_dynamic_database == "external"; inventory_hostname == groups['automationcontroller'][0]; awx_pg_cert_auth |
| 294 | 11.8% | install.yml → Sanity check → preflight → preflight → block | set_fact | Set a host fact used by later tasks | groups['automationcontroller'] \| default([]) \| length; not (groups['database'] \| default([]) \| length); config_dynamic_database == "external"; inventory_hostname == groups['automationcontroller'][0] |
| 295 | 11.8% | install.yml → Sanity check → preflight → preflight → block | assert | Validate a condition and fail if it is not met | groups['automationcontroller'] \| default([]) \| length; not (groups['database'] \| default([]) \| length); config_dynamic_database == "external"; inventory_hostname == groups['automationcontroller'][0] |
| 296 | 11.9% | install.yml → Sanity check → preflight → preflight | block | Begin conditional or grouped task block | groups['automationhub'] \| default([]) \| length > 1; inventory_hostname in groups['automationhub'] |
| 297 | 11.9% | install.yml → Sanity check → preflight → preflight → block | Preflight check - Check if /var/lib/pulp exists for Hub HA mode | Preflight check - Check if /var/lib/pulp exists for Hub HA mode | groups['automationhub'] \| default([]) \| length > 1; inventory_hostname in groups['automationhub'] |
| 298 | 12.0% | install.yml → Sanity check → preflight → preflight → block | Preflight check - /var/lib/pulp must exist for Hub HA mode | Preflight check - /var/lib/pulp must exist for Hub HA mode | groups['automationhub'] \| default([]) \| length > 1; inventory_hostname in groups['automationhub']; not var_lib_pulp_dir.stat.exists |
| 299 | 12.0% | install.yml → Sanity check → preflight → preflight | Preflight check - Generate a hub token if fresh install | Preflight check - Generate a hub token if fresh install | groups['automationcontroller'] \| default([]) \| length; groups['automationhub'] \| default([]) \| length; inventory_hostname == groups['automationhub'][0]; not automationhub_api_token \| default('') \| length; generate_automationhub_token is not defined; not _aap_version_file.stat.exists \| bool |
| 300 | 12.0% | install.yml → Sanity check → preflight → preflight | Obtain enabled repo list | Obtain enabled repo list | — |
| 301 | 12.1% | install.yml → Sanity check → preflight → preflight | Ensure EPEL is not enabled | Ensure EPEL is not enabled | item \| trim \| regex_search('^epel.*') |
| 302 | 12.1% | install.yml → Sanity check → preflight → preflight | Set host AAP version before upgrade | Set host AAP version before upgrade | inventory_hostname in groups.get('automationcontroller', []) |
| 303 | 12.2% | install.yml → Sanity check → preflight → preflight | Check if upgrade_from file exists | Check if upgrade_from file exists | — |
| 304 | 12.2% | install.yml → Sanity check → preflight → preflight | block | Begin conditional or grouped task block | _upgrade_from_file.stat.exists \| bool |
| 305 | 12.2% | install.yml → Sanity check → preflight → preflight → block | Obtain upgrade from version | Obtain upgrade from version | _upgrade_from_file.stat.exists \| bool |
| 306 | 12.3% | install.yml → Sanity check → preflight → preflight → block | Set host AAP version before upgrade | Set host AAP version before upgrade | _upgrade_from_file.stat.exists \| bool |
| 307 | 12.3% | install.yml → Sanity check → preflight → preflight | Set host AAP version before upgrade | Set host AAP version before upgrade | not _upgrade_from_file.stat.exists \| bool |
| 308 | 12.4% | install.yml → Sanity check → preflight → preflight | Set AAP version before upgrade | Set AAP version before upgrade | — |
| 309 | 12.4% | install.yml → Sanity check → preflight → preflight | Ensure AAP directory exists | Ensure AAP directory exists | — |
| 310 | 12.4% | install.yml → Sanity check → preflight → preflight | Record AAP version before upgrade | Record AAP version before upgrade | — |
| 311 | 12.5% | install.yml → Sanity check → preflight → preflight | Check if upgrade from 2.4 | Check if upgrade from 2.4 | — |
| 312 | 12.5% | install.yml → Sanity check → preflight → preflight | Check event-stream URL when mTLS enabled | Check event-stream URL when mTLS enabled (role `automationedacontroller`, tasks `check_event_stream_url.yml`) | _automationedacontroller_event_stream_hosts \| length > 0 |
| 313 | 12.6% | install.yml → Sanity check → preflight → preflight → automationedacontroller | role `automationedacontroller` (`check_event_stream_url.yml`) | Enter role task list | _automationedacontroller_event_stream_hosts \| length > 0 |
| 314 | 12.6% | install.yml → Sanity check → preflight → preflight → automationedacontroller → automationedacontroller | file `check_event_stream_url.yml` | Task file `ansible/automation_platform_installer/roles/automationedacontroller/tasks/check_event_stream_url.yml` | _automationedacontroller_event_stream_hosts \| length > 0 |
| 315 | 12.6% | install.yml → Sanity check → preflight → preflight → automationedacontroller → automationedacontroller | Fail if event-stream URL and mTLS URL match | Fail if event-stream URL and mTLS URL match | _automationedacontroller_event_stream_hosts \| length > 0; _automationedacontroller_event_stream_mtls \| bool; _automationedacontroller_public_event_stream_url == _automationedacontroller_public_event_stream_mtls_url; _automationedacontroller_event_stream_mtls_path == _automationedacontroller_event_stream_path |
| 316 | 12.7% | install.yml | Receptor Sanity check | Play targeting `automationcontroller:execution_nodes` | — |
| 317 | 12.7% | install.yml → Receptor Sanity check | Generate Mesh dot graph topology | Begin conditional or grouped task block | — |
| 318 | 12.8% | install.yml → Receptor Sanity check → Generate Mesh dot graph topology | set_fact | Set a host fact used by later tasks | — |
| 319 | 12.8% | install.yml → Receptor Sanity check → Generate Mesh dot graph topology | Parse Mesh | Parse Mesh | — |
| 320 | 12.8% | install.yml → Receptor Sanity check → Generate Mesh dot graph topology | debug | Print debug information | — |
| 321 | 12.9% | install.yml → Receptor Sanity check | set_fact | Set a host fact used by later tasks | — |
| 322 | 12.9% | install.yml → Receptor Sanity check | include_role | Include and run tasks from an Ansible role (role `receptor`, tasks `preflight`) | — |
| 323 | 13.0% | install.yml → Receptor Sanity check → receptor | role `receptor` (`preflight`) | Enter role task list | — |
| 324 | 13.0% | install.yml → Receptor Sanity check → receptor → receptor | file `preflight.yml` | Task file `ansible/automation_platform_installer/roles/receptor/tasks/preflight.yml` | — |
| 325 | 13.0% | install.yml → Receptor Sanity check → receptor → receptor | Include calculate_mesh | Include calculate_mesh (file `calculate_mesh.yml`) | — |
| 326 | 13.1% | install.yml → Receptor Sanity check → receptor → receptor → calculate_mesh.yml | file `calculate_mesh.yml` | Task file `ansible/automation_platform_installer/roles/receptor/tasks/calculate_mesh.yml` | — |
| 327 | 13.1% | install.yml → Receptor Sanity check → receptor → receptor → calculate_mesh.yml | Calculate Mesh Topology | Calculate Mesh Topology | — |
| 328 | 13.2% | install.yml → Receptor Sanity check → receptor → receptor → calculate_mesh.yml | Review Mesh dot graph topology | Review Mesh dot graph topology | generate_dot_file \| default([]) \| length |
| 329 | 13.2% | install.yml → Receptor Sanity check → receptor → receptor → calculate_mesh.yml | Store Receptor Mesh metadata | Store Receptor Mesh metadata | — |
| 330 | 13.3% | install.yml → Receptor Sanity check → receptor → receptor → calculate_mesh.yml | Store Receptor Deprovision Mesh metadata | Store Receptor Deprovision Mesh metadata | — |
| 331 | 13.3% | install.yml → Receptor Sanity check → receptor → receptor → calculate_mesh.yml | Define receptor mesh facts | Define receptor mesh facts | mesh is defined; mesh.keys() \| default([]) \| length; inventory_hostname in mesh.keys() |
| 332 | 13.3% | install.yml → Receptor Sanity check → receptor → receptor → calculate_mesh.yml | Define receptor deprovision mesh facts | Define receptor deprovision mesh facts | deprovision_mesh is defined; deprovision_mesh.keys() \| default([]) \| length; inventory_hostname in deprovision_mesh.keys() |
| 333 | 13.4% | install.yml → Receptor Sanity check → receptor → receptor | Include DNS asserts | Include DNS asserts (file `assert_dns.yml`) | — |
| 334 | 13.4% | install.yml → Receptor Sanity check → receptor → receptor → assert_dns.yml | file `assert_dns.yml` | Task file `ansible/automation_platform_installer/roles/receptor/tasks/assert_dns.yml` | — |
| 335 | 13.5% | install.yml → Receptor Sanity check → receptor → receptor → assert_dns.yml | Validate if receptor peer name is DNS resolvable | Validate if receptor peer name is DNS resolvable | item \| default([]) \| length; hostvars[item]['node_state'] == 'active'; hostvars[item]['receptor_host_identifier'] \| default([]) \| length; hostvars[item]['receptor_host_identifier'] \| regex_search(regexp_ip) == '' or hostvars[item]['receptor_host_identifier'] \| regex_search(regexp_ip) == None |
| 336 | 13.5% | install.yml → Receptor Sanity check → receptor → receptor → assert_dns.yml | Report error when a receptor node cannot resolve a connected peer name | Report error when a receptor node cannot resolve a connected peer name | _getent.failed \| default(False) \| bool; item.rc != 0 |
| 337 | 13.5% | install.yml → Receptor Sanity check → receptor → receptor | Include SSL asserts | Include SSL asserts (file `assert_ssl.yml`) | — |
| 338 | 13.6% | install.yml → Receptor Sanity check → receptor → receptor → assert_ssl.yml | file `assert_ssl.yml` | Task file `ansible/automation_platform_installer/roles/receptor/tasks/assert_ssl.yml` | — |
| 339 | 13.6% | install.yml → Receptor Sanity check → receptor → receptor → assert_ssl.yml | Ensure custom SSL Certificate Authority files were provided | Ensure custom SSL Certificate Authority files were provided | mesh_ca_certfile is defined or mesh_ca_keyfile is defined |
| 340 | 13.7% | install.yml → Receptor Sanity check → receptor → receptor → assert_ssl.yml | Ensure custom SSL Certificate Authority files matches | Begin conditional or grouped task block | mesh_ca_certfile is defined; mesh_ca_keyfile is defined |
| 341 | 13.7% | install.yml → Receptor Sanity check → receptor → receptor → assert_ssl.yml → Ensure custom SSL Certificate Authority files matches | Verify custom node SSL private key | Verify custom node SSL private key | mesh_ca_certfile is defined; mesh_ca_keyfile is defined |
| 342 | 13.7% | install.yml → Receptor Sanity check → receptor → receptor → assert_ssl.yml → Ensure custom SSL Certificate Authority files matches | Verify custom node SSL x509 key | Verify custom node SSL x509 key | mesh_ca_certfile is defined; mesh_ca_keyfile is defined |
| 343 | 13.8% | install.yml → Receptor Sanity check → receptor → receptor → assert_ssl.yml → Ensure custom SSL Certificate Authority files matches | Ensure custom SSL pair matches | Ensure custom SSL pair matches | mesh_ca_certfile is defined; mesh_ca_keyfile is defined |
| 344 | 13.8% | install.yml → Receptor Sanity check → receptor → receptor → assert_ssl.yml | Ensure custom node SSL files are valid when provided | Ensure custom node SSL files are valid when provided | mesh_node_certfile is defined or mesh_node_keyfile is defined |
| 345 | 13.9% | install.yml → Receptor Sanity check → receptor → receptor → assert_ssl.yml | Ensure custom node SSL files matches | Begin conditional or grouped task block | mesh_node_certfile is defined; mesh_node_keyfile is defined |
| 346 | 13.9% | install.yml → Receptor Sanity check → receptor → receptor → assert_ssl.yml → Ensure custom node SSL files matches | Verify custom node SSL private key | Verify custom node SSL private key | mesh_node_certfile is defined; mesh_node_keyfile is defined |
| 347 | 13.9% | install.yml → Receptor Sanity check → receptor → receptor → assert_ssl.yml → Ensure custom node SSL files matches | Verify custom node SSL x509 key | Verify custom node SSL x509 key | mesh_node_certfile is defined; mesh_node_keyfile is defined |
| 348 | 14.0% | install.yml → Receptor Sanity check → receptor → receptor → assert_ssl.yml → Ensure custom node SSL files matches | Ensure custom SSL pair matches | Ensure custom SSL pair matches | mesh_node_certfile is defined; mesh_node_keyfile is defined |
| 349 | 14.0% | install.yml → Receptor Sanity check → receptor → receptor → assert_ssl.yml → Ensure custom node SSL files matches | Ensure x509 certificate was signed by the expected Certificate Authority | Ensure x509 certificate was signed by the expected Certificate Authority | mesh_node_certfile is defined; mesh_node_keyfile is defined |
| 350 | 14.1% | install.yml | Deactivate and uninstall desired nodes | Play targeting `automationcontroller:execution_nodes` | — |
| 351 | 14.1% | install.yml → Deactivate and uninstall desired nodes | set_fact | Set a host fact used by later tasks | — |
| 352 | 14.1% | install.yml → Deactivate and uninstall desired nodes | Deprovision nodes | Deprovision nodes (role `deprovision`) | node_state is defined; node_state == 'deprovision' |
| 353 | 14.2% | install.yml → Deactivate and uninstall desired nodes → deprovision | role `deprovision` | Enter role task list | node_state is defined; node_state == 'deprovision' |
| 354 | 14.2% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision | file `main.yml` | Task file `ansible/automation_platform_installer/roles/deprovision/tasks/main.yml` | node_state is defined; node_state == 'deprovision' |
| 355 | 14.3% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision | include receptor variables | include receptor variables | node_state is defined; node_state == 'deprovision' |
| 356 | 14.3% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision | include redis variables | include redis variables | node_state is defined; node_state == 'deprovision' |
| 357 | 14.3% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision | List hosts marked for deprovisioning | List hosts marked for deprovisioning | node_state is defined; node_state == 'deprovision' |
| 358 | 14.4% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision | Show deprovision warning disclaimer | Show deprovision warning disclaimer | node_state is defined; node_state == 'deprovision'; deprovision_disclaimer \| bool |
| 359 | 14.4% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision | Ensure that user accepted the deprovision terms | Ensure that user accepted the deprovision terms | node_state is defined; node_state == 'deprovision'; deprovision_disclaimer \| bool |
| 360 | 14.5% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision | Determine if {{ receptor_package_name }} RPM is installed | Determine if {{ receptor_package_name }} RPM is installed | node_state is defined; node_state == 'deprovision' |
| 361 | 14.5% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision | Determine if {{ controller_package_name }} RPM is installed | Determine if {{ controller_package_name }} RPM is installed | node_state is defined; node_state == 'deprovision'; inventory_hostname in groups['automationcontroller'] |
| 362 | 14.5% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision | Determine if Nginx RPM is installed | Determine if Nginx RPM is installed | node_state is defined; node_state == 'deprovision'; inventory_hostname in groups['automationcontroller'] |
| 363 | 14.6% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision | Deprovision node instance from the database. | Deprovision node instance from the database. | node_state is defined; node_state == 'deprovision'; controller_installed is changed or receptor_installed is changed |
| 364 | 14.6% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision | Deprovision Nginx | Deprovision Nginx (file `nginx.yml`) | node_state is defined; node_state == 'deprovision'; nginx_installed is changed |
| 365 | 14.7% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → nginx.yml | file `nginx.yml` | Task file `ansible/automation_platform_installer/roles/deprovision/tasks/nginx.yml` | node_state is defined; node_state == 'deprovision'; nginx_installed is changed |
| 366 | 14.7% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → nginx.yml | Stop and disable Nginx Services | Stop and disable Nginx Services (service `nginx`) | node_state is defined; node_state == 'deprovision'; nginx_installed is changed |
| 367 | 14.7% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → nginx.yml | Remove controller nginx config file | Remove controller nginx config file | node_state is defined; node_state == 'deprovision'; nginx_installed is changed |
| 368 | 14.8% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → nginx.yml | Check for Existence of Nginx.conf File | Check for Existence of Nginx.conf File | node_state is defined; node_state == 'deprovision'; nginx_installed is changed |
| 369 | 14.8% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → nginx.yml | Backup Current Nginx File and Restore Default Configuration File | Begin conditional or grouped task block | node_state is defined; node_state == 'deprovision'; nginx_installed is changed; nginx_conf_file.stat.exists |
| 370 | 14.9% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → nginx.yml → Backup Current Nginx File and Restore Default Configuration File | ansible.builtin.copy | Run `ansible.builtin.copy` | node_state is defined; node_state == 'deprovision'; nginx_installed is changed; nginx_conf_file.stat.exists |
| 371 | 14.9% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → nginx.yml → Backup Current Nginx File and Restore Default Configuration File | file | Manage files, directories, or symlinks | node_state is defined; node_state == 'deprovision'; nginx_installed is changed; nginx_conf_file.stat.exists |
| 372 | 14.9% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → nginx.yml | Check for Existence of nginx.conf.default File | Check for Existence of nginx.conf.default File | node_state is defined; node_state == 'deprovision'; nginx_installed is changed |
| 373 | 15.0% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → nginx.yml | Ensure that Nginx Default Config File is Restored [If Exists] | Ensure that Nginx Default Config File is Restored [If Exists] | node_state is defined; node_state == 'deprovision'; nginx_installed is changed; nginx_conf_default_file.stat.exists |
| 374 | 15.0% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision | Deprovision Receptor | Deprovision Receptor (file `receptor.yml`) | node_state is defined; node_state == 'deprovision'; receptor_installed is changed |
| 375 | 15.1% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → receptor.yml | file `receptor.yml` | Task file `ansible/automation_platform_installer/roles/deprovision/tasks/receptor.yml` | node_state is defined; node_state == 'deprovision'; receptor_installed is changed |
| 376 | 15.1% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → receptor.yml | Stop and disable receptor service | Stop and disable receptor service (service `{{ receptor_package_name }}`) | node_state is defined; node_state == 'deprovision'; receptor_installed is changed |
| 377 | 15.1% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → receptor.yml | Remove Receptor related RPMs | Remove Receptor related RPMs | node_state is defined; node_state == 'deprovision'; receptor_installed is changed |
| 378 | 15.2% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → receptor.yml | Cleanup Receptor files | Cleanup Receptor files (dest `{{ item }}`) | node_state is defined; node_state == 'deprovision'; receptor_installed is changed |
| 379 | 15.2% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → receptor.yml | Remove receptor user | Remove receptor user | node_state is defined; node_state == 'deprovision'; receptor_installed is changed |
| 380 | 15.3% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → receptor.yml | Remove awx user | Remove awx user | node_state is defined; node_state == 'deprovision'; receptor_installed is changed; inventory_hostname in groups['execution_nodes'] |
| 381 | 15.3% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision | Deprovision Automation Controller | Deprovision Automation Controller (file `controller.yml`) | node_state is defined; node_state == 'deprovision'; inventory_hostname in groups['automationcontroller'] and controller_installed.changed \| default(False) \| bool |
| 382 | 15.3% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → controller.yml | file `controller.yml` | Task file `ansible/automation_platform_installer/roles/deprovision/tasks/controller.yml` | node_state is defined; node_state == 'deprovision'; inventory_hostname in groups['automationcontroller'] and controller_installed.changed \| default(False) \| bool |
| 383 | 15.4% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → controller.yml | Determine if {{ controller_package_name }} RPM is installed | Determine if {{ controller_package_name }} RPM is installed | node_state is defined; node_state == 'deprovision'; inventory_hostname in groups['automationcontroller'] and controller_installed.changed \| default(False) \| bool |
| 384 | 15.4% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → controller.yml | Stop and disable controller services | Stop and disable controller services (service `{{ item }}`) | node_state is defined; node_state == 'deprovision'; inventory_hostname in groups['automationcontroller'] and controller_installed.changed \| default(False) \| bool |
| 385 | 15.5% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → controller.yml | Remove controller related RPMs | Remove controller related RPMs | node_state is defined; node_state == 'deprovision'; inventory_hostname in groups['automationcontroller'] and controller_installed.changed \| default(False) \| bool |
| 386 | 15.5% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → controller.yml | Cleanup controller files | Cleanup controller files (dest `{{ item }}`) | node_state is defined; node_state == 'deprovision'; inventory_hostname in groups['automationcontroller'] and controller_installed.changed \| default(False) \| bool |
| 387 | 15.5% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → controller.yml | include_role | Include and run tasks from an Ansible role (role `redis`, tasks `deprovision.yml`) | node_state is defined; node_state == 'deprovision'; inventory_hostname in groups['automationcontroller'] and controller_installed.changed \| default(False) \| bool |
| 388 | 15.6% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → controller.yml → redis | role `redis` (`deprovision.yml`) | Enter role task list | node_state is defined; node_state == 'deprovision'; inventory_hostname in groups['automationcontroller'] and controller_installed.changed \| default(False) \| bool |
| 389 | 15.6% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → controller.yml → redis → redis | file `deprovision.yml` | Task file `ansible/automation_platform_installer/roles/redis/tasks/deprovision.yml` | node_state is defined; node_state == 'deprovision'; inventory_hostname in groups['automationcontroller'] and controller_installed.changed \| default(False) \| bool |
| 390 | 15.7% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → controller.yml → redis → redis | Remove redis rpm | Remove redis rpm | node_state is defined; node_state == 'deprovision'; inventory_hostname in groups['automationcontroller'] and controller_installed.changed \| default(False) \| bool |
| 391 | 15.7% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → controller.yml → redis → redis | Remove remaining redis files and directories | Remove remaining redis files and directories | node_state is defined; node_state == 'deprovision'; inventory_hostname in groups['automationcontroller'] and controller_installed.changed \| default(False) \| bool |
| 392 | 15.7% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → controller.yml | Gather supervisord log files | Gather supervisord log files | node_state is defined; node_state == 'deprovision'; inventory_hostname in groups['automationcontroller'] and controller_installed.changed \| default(False) \| bool |
| 393 | 15.8% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → controller.yml | Cleanup supervisord logs | Cleanup supervisord logs (dest `{{ item.path }}`) | node_state is defined; node_state == 'deprovision'; inventory_hostname in groups['automationcontroller'] and controller_installed.changed \| default(False) \| bool |
| 394 | 15.8% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → controller.yml | Check for awx user | Check for awx user | node_state is defined; node_state == 'deprovision'; inventory_hostname in groups['automationcontroller'] and controller_installed.changed \| default(False) \| bool |
| 395 | 15.9% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → controller.yml | Remove ACL rule for awx user to access rhsm.conf | Remove ACL rule for awx user to access rhsm.conf | node_state is defined; node_state == 'deprovision'; inventory_hostname in groups['automationcontroller'] and controller_installed.changed \| default(False) \| bool; getent_passwd['awx'] \| default([]) \| length |
| 396 | 15.9% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → controller.yml | Disable lingering systemd sessions for awx user | Disable lingering systemd sessions for awx user | node_state is defined; node_state == 'deprovision'; inventory_hostname in groups['automationcontroller'] and controller_installed.changed \| default(False) \| bool |
| 397 | 15.9% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → controller.yml | Pause to allow lingering sessions and processes to stop | Pause to allow lingering sessions and processes to stop | node_state is defined; node_state == 'deprovision'; inventory_hostname in groups['automationcontroller'] and controller_installed.changed \| default(False) \| bool |
| 398 | 16.0% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → controller.yml | Remove awx user | Remove awx user | node_state is defined; node_state == 'deprovision'; inventory_hostname in groups['automationcontroller'] and controller_installed.changed \| default(False) \| bool |
| 399 | 16.0% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision | Deprovision firewall | Deprovision firewall (role `firewall`, tasks `deprovision.yml`) | node_state is defined; node_state == 'deprovision' |
| 400 | 16.1% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → firewall | role `firewall` (`deprovision.yml`) | Enter role task list | node_state is defined; node_state == 'deprovision' |
| 401 | 16.1% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → firewall → firewall | file `deprovision.yml` | Task file `ansible/automation_platform_installer/roles/firewall/tasks/deprovision.yml` | node_state is defined; node_state == 'deprovision' |
| 402 | 16.1% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → firewall → firewall | include_tasks | Include a task file from the current role or playbook (file `discover.yml`) | node_state is defined; node_state == 'deprovision' |
| 403 | 16.2% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → firewall → firewall → discover.yml | file `discover.yml` | Task file `ansible/automation_platform_installer/roles/firewall/tasks/discover.yml` | node_state is defined; node_state == 'deprovision' |
| 404 | 16.2% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → firewall → firewall → discover.yml | Determine if firewalld is running | Determine if firewalld is running | node_state is defined; node_state == 'deprovision' |
| 405 | 16.3% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → firewall → firewall → discover.yml | Determine if firewalld is enabled | Determine if firewalld is enabled | node_state is defined; node_state == 'deprovision' |
| 406 | 16.3% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → firewall → firewall → discover.yml | Determine if iptables is installed | Determine if iptables is installed | node_state is defined; node_state == 'deprovision' |
| 407 | 16.4% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → firewall → firewall → discover.yml | Determine if iptables service is installed | Determine if iptables service is installed | node_state is defined; node_state == 'deprovision' |
| 408 | 16.4% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → firewall → firewall → discover.yml | Check to see if there is an iptables config file | Check to see if there is an iptables config file | node_state is defined; node_state == 'deprovision'; iptables_installed.rc == 0 |
| 409 | 16.4% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → firewall → firewall → discover.yml | Select firewalld | Select firewalld | node_state is defined; node_state == 'deprovision'; firewalld_running.rc == 0 or systemctl_unit_files.stdout_lines \| select('match','firewalld.service\s+enabled') \| list \| length == 1 |
| 410 | 16.5% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → firewall → firewall → discover.yml | Select iptables | Select iptables | node_state is defined; node_state == 'deprovision'; iptables_installed.rc == 0; iptables_exists.stat.exists; iptables_service.stat.exists |
| 411 | 16.5% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → firewall → firewall | set_fact | Set a host fact used by later tasks | node_state is defined; node_state == 'deprovision' |
| 412 | 16.6% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → firewall → firewall | block | Begin conditional or grouped task block | node_state is defined; node_state == 'deprovision'; component_firewall_type \| string == "firewalld" |
| 413 | 16.6% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → firewall → firewall → block | Disable base {{ firewall_component_name }} firewall ports | Disable base {{ firewall_component_name }} firewall ports | node_state is defined; node_state == 'deprovision'; component_firewall_type \| string == "firewalld"; firewall_component_name != "database" |
| 414 | 16.6% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → firewall → firewall → block | Disable standalone database firewall ports for {{ firewall_component_name }} | Disable standalone database firewall ports for {{ firewall_component_name }} | node_state is defined; node_state == 'deprovision'; component_firewall_type \| string == "firewalld"; firewall_component_name != "database" |
| 415 | 16.7% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → firewall → firewall → block | Disable standalone database firewall ports for {{ firewall_component_name }} | Disable standalone database firewall ports for {{ firewall_component_name }} | node_state is defined; node_state == 'deprovision'; component_firewall_type \| string == "firewalld"; firewall_component_name == "database" |
| 416 | 16.7% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → firewall → firewall → block | Disable execution node firewall ports | Disable execution node firewall ports | node_state is defined; node_state == 'deprovision'; component_firewall_type \| string == "firewalld"; listener \| default(False) \| bool and (inventory_hostname in groups['execution_nodes'] \| default([]) or  inventory_hostname in groups['automationcontroller'] \| default([])) |
| 417 | 16.8% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → firewall → firewall | Remove iptables rules for {{ firewall_component_name }} | Remove iptables rules for {{ firewall_component_name }} | node_state is defined; node_state == 'deprovision'; component_firewall_type \| string == "iptables" |
| 418 | 16.8% | install.yml → Deactivate and uninstall desired nodes | Prepare former isolated_nodes for upgrade path | Prepare former isolated_nodes for upgrade path (role `deprovision`, tasks `isolated_nodes`) | node_state is defined; node_state == 'iso_migrate'; inventory_hostname in groups['execution_nodes'] |
| 419 | 16.8% | install.yml → Deactivate and uninstall desired nodes → deprovision | role `deprovision` (`isolated_nodes`) | Enter role task list | node_state is defined; node_state == 'iso_migrate'; inventory_hostname in groups['execution_nodes'] |
| 420 | 16.9% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision | file `isolated_nodes.yml` | Task file `ansible/automation_platform_installer/roles/deprovision/tasks/isolated_nodes.yml` | node_state is defined; node_state == 'iso_migrate'; inventory_hostname in groups['execution_nodes'] |
| 421 | 16.9% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision | Deprovision former isolated node instance from the database. | Deprovision former isolated node instance from the database. | node_state is defined; node_state == 'iso_migrate'; inventory_hostname in groups['execution_nodes'] |
| 422 | 17.0% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision | Determine if the ansible-tower-isolated RPM is installed | Determine if the ansible-tower-isolated RPM is installed | node_state is defined; node_state == 'iso_migrate'; inventory_hostname in groups['execution_nodes'] |
| 423 | 17.0% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision | Proceed with the isolated_nodes migration | Begin conditional or grouped task block | node_state is defined; node_state == 'iso_migrate'; inventory_hostname in groups['execution_nodes']; tower_iso_installed is changed |
| 424 | 17.0% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → Proceed with the isolated_nodes migration | Remove isolated_node related RPMs | Remove isolated_node related RPMs | node_state is defined; node_state == 'iso_migrate'; inventory_hostname in groups['execution_nodes']; tower_iso_installed is changed |
| 425 | 17.1% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → Proceed with the isolated_nodes migration | Cleanup isolated_nodes files | Cleanup isolated_nodes files (dest `{{ item }}`) | node_state is defined; node_state == 'iso_migrate'; inventory_hostname in groups['execution_nodes']; tower_iso_installed is changed |
| 426 | 17.1% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → Proceed with the isolated_nodes migration | Recreate awx home directory | Recreate awx home directory | node_state is defined; node_state == 'iso_migrate'; inventory_hostname in groups['execution_nodes']; tower_iso_installed is changed |
| 427 | 17.2% | install.yml | Prep Tower node(s) | Play targeting `automationcontroller` | — |
| 428 | 17.2% | install.yml → Prep Tower node(s) | role `migrations` | Enter role task list | — |
| 429 | 17.2% | install.yml → Prep Tower node(s) → migrations | file `main.yml` | Task file `ansible/automation_platform_installer/roles/migrations/tasks/main.yml` | — |
| 430 | 17.3% | install.yml | Initialize Ansible Automation Platform internal CA server and service variables | Play targeting `automationgateway:automationcontroller:automationhub:automationedacontroller` | — |
| 431 | 17.3% | install.yml → Initialize Ansible Automation Platform internal CA server and service variables | Build aap_ca_server group | Build aap_ca_server group (role `certificate_authority`, tasks `discover.yml`) | eda_node_type \| default('') != 'worker' |
| 432 | 17.4% | install.yml → Initialize Ansible Automation Platform internal CA server and service variables → certificate_authority | role `certificate_authority` (`discover.yml`) | Enter role task list | eda_node_type \| default('') != 'worker' |
| 433 | 17.4% | install.yml → Initialize Ansible Automation Platform internal CA server and service variables → certificate_authority → certificate_authority | file `discover.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/discover.yml` | eda_node_type \| default('') != 'worker' |
| 434 | 17.4% | install.yml → Initialize Ansible Automation Platform internal CA server and service variables → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | eda_node_type \| default('') != 'worker'; not aap_ca_server_hostname \| default('') \| length |
| 435 | 17.5% | install.yml → Initialize Ansible Automation Platform internal CA server and service variables → certificate_authority → certificate_authority → block | Find an existing certificate authority | Find an existing certificate authority | eda_node_type \| default('') != 'worker'; not aap_ca_server_hostname \| default('') \| length |
| 436 | 17.5% | install.yml → Initialize Ansible Automation Platform internal CA server and service variables → certificate_authority → certificate_authority → block | Generate a list of existing certificate authority results | Generate a list of existing certificate authority results | eda_node_type \| default('') != 'worker'; not aap_ca_server_hostname \| default('') \| length; aap_ca_server_exists.stat.exists |
| 437 | 17.6% | install.yml → Initialize Ansible Automation Platform internal CA server and service variables → certificate_authority → certificate_authority → block | Build list of eligible hosts if CA does not exist | Build list of eligible hosts if CA does not exist | eda_node_type \| default('') != 'worker'; not aap_ca_server_hostname \| default('') \| length; not hostvars['localhost']['aap_ca_server_hosts'] \| default([]) \| length; not aap_ca_backuprestore \| bool |
| 438 | 17.6% | install.yml → Initialize Ansible Automation Platform internal CA server and service variables → certificate_authority → certificate_authority → block | Select the first eliglible host for CA group | Select the first eliglible host for CA group | eda_node_type \| default('') != 'worker'; not aap_ca_server_hostname \| default('') \| length; hostvars['localhost']['aap_ca_server_hosts'] \| default([]) \| length or not aap_ca_backuprestore \| bool |
| 439 | 17.6% | install.yml → Initialize Ansible Automation Platform internal CA server and service variables → certificate_authority → certificate_authority | Add CA server host to inventory group | Add CA server host to inventory group | eda_node_type \| default('') != 'worker'; aap_ca_server_hostname \| default('') \| length |
| 440 | 17.7% | install.yml | Install Ansible Automation Platform internal CA | Play targeting `aap_ca_server[0]` | — |
| 441 | 17.7% | install.yml → Install Ansible Automation Platform internal CA | Configure internal CA | Configure internal CA (role `certificate_authority`) | — |
| 442 | 17.8% | install.yml → Install Ansible Automation Platform internal CA → certificate_authority | role `certificate_authority` | Enter role task list | — |
| 443 | 17.8% | install.yml → Install Ansible Automation Platform internal CA → certificate_authority → certificate_authority | file `main.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/main.yml` | — |
| 444 | 17.8% | install.yml → Install Ansible Automation Platform internal CA → certificate_authority → certificate_authority | include_tasks | Include a task file from the current role or playbook (file `init.yml`) | — |
| 445 | 17.9% | install.yml → Install Ansible Automation Platform internal CA → certificate_authority → certificate_authority → init.yml | file `init.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/init.yml` | — |
| 446 | 17.9% | install.yml → Install Ansible Automation Platform internal CA → certificate_authority → certificate_authority → init.yml | Ensure python cryptography libraries are installed | Ensure python cryptography libraries are installed | — |
| 447 | 18.0% | install.yml → Install Ansible Automation Platform internal CA → certificate_authority → certificate_authority | Create CA directory | Create CA directory | — |
| 448 | 18.0% | install.yml → Install Ansible Automation Platform internal CA → certificate_authority → certificate_authority | Set cipher if needed | Set cipher if needed | aap_ca_pass is defined |
| 449 | 18.0% | install.yml → Install Ansible Automation Platform internal CA → certificate_authority → certificate_authority | Copy user-provided CA key file, if provided | Copy user-provided CA key file, if provided (dest `/etc/ansible-automation-platform/ca/ansible-automation-platform-managed-ca-key.key`) | aap_ca_key_file \| length |
| 450 | 18.1% | install.yml → Install Ansible Automation Platform internal CA → certificate_authority → certificate_authority | Copy user-provided CA certificate file, if provided | Copy user-provided CA certificate file, if provided (dest `/etc/ansible-automation-platform/ca/ansible-automation-platform-managed-ca-cert.crt`) | aap_ca_cert_file \| length |
| 451 | 18.1% | install.yml → Install Ansible Automation Platform internal CA → certificate_authority → certificate_authority | Generate root CA key | Generate root CA key | not aap_ca_key_file \| length |
| 452 | 18.2% | install.yml → Install Ansible Automation Platform internal CA → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | not aap_ca_cert_file \| length |
| 453 | 18.2% | install.yml → Install Ansible Automation Platform internal CA → certificate_authority → certificate_authority → block | Create CSR for CA certificate | Create CSR for CA certificate | not aap_ca_cert_file \| length |
| 454 | 18.2% | install.yml → Install Ansible Automation Platform internal CA → certificate_authority → certificate_authority → block | Create AAP CA certificate | Create AAP CA certificate | not aap_ca_cert_file \| length |
| 455 | 18.3% | install.yml → Install Ansible Automation Platform internal CA → certificate_authority → certificate_authority | Get CA certificate SKI for Signing | Get CA certificate SKI for Signing (file `get_cacert.yml`) | — |
| 456 | 18.3% | install.yml → Install Ansible Automation Platform internal CA → certificate_authority → certificate_authority → get_cacert.yml | file `get_cacert.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/get_cacert.yml` | — |
| 457 | 18.4% | install.yml → Install Ansible Automation Platform internal CA → certificate_authority → certificate_authority → get_cacert.yml | Get AAP CA certificate info | Get AAP CA certificate info | — |
| 458 | 18.4% | install.yml → Install Ansible Automation Platform internal CA → certificate_authority → certificate_authority → get_cacert.yml | set_fact | Set a host fact used by later tasks | — |
| 459 | 18.4% | install.yml → Install Ansible Automation Platform internal CA → certificate_authority → certificate_authority → get_cacert.yml | Slurp AAP CA certificate | Slurp AAP CA certificate | — |
| 460 | 18.5% | install.yml | Install postgres database node | Play targeting `database:automationgateway:automationcontroller:automationedacontroller:automationhub` | — |
| 461 | 18.5% | install.yml → Install postgres database node | Install postgres database node | Begin conditional or grouped task block | — |
| 462 | 18.6% | install.yml → Install postgres database node → Install postgres database node | block | Begin conditional or grouped task block | not groups['database'] \| default([]) \| length |
| 463 | 18.6% | install.yml → Install postgres database node → Install postgres database node → block | set_fact | Set a host fact used by later tasks | not groups['database'] \| default([]) \| length; groups['automationcontroller'] \| default([]) \| length; inventory_hostname == groups['automationcontroller'][0]; config_dynamic_database == 'internal' |
| 464 | 18.6% | install.yml → Install postgres database node → Install postgres database node → block | set_fact | Set a host fact used by later tasks | not groups['database'] \| default([]) \| length; inventory_hostname == groups['automationgateway'][0]; automationgateway_config_dynamic_database == 'internal'; not groups.get('automationcontroller', []) \| length |
| 465 | 18.7% | install.yml → Install postgres database node → Install postgres database node | set_fact | Set a host fact used by later tasks | _do_run_db_install \| default(false) \| bool; groups.get('aap_valid_hosts') \| length > 1 |
| 466 | 18.7% | install.yml → Install postgres database node → Install postgres database node | Add internal CA certificate to trust store | Add internal CA certificate to trust store (role `certificate_authority`, tasks `add_cacert.yml`) | — |
| 467 | 18.8% | install.yml → Install postgres database node → Install postgres database node → certificate_authority | role `certificate_authority` (`add_cacert.yml`) | Enter role task list | — |
| 468 | 18.8% | install.yml → Install postgres database node → Install postgres database node → certificate_authority → certificate_authority | file `add_cacert.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/add_cacert.yml` | — |
| 469 | 18.8% | install.yml → Install postgres database node → Install postgres database node → certificate_authority → certificate_authority | Ensure ca-certificates is installed | Ensure ca-certificates is installed | — |
| 470 | 18.9% | install.yml → Install postgres database node → Install postgres database node → certificate_authority → certificate_authority | Copy custom CA cert | Copy custom CA cert (dest `/etc/pki/ca-trust/source/anchors/ansible-automation-platform-custom-ca-cert.crt`) | custom_ca_cert is defined |
| 471 | 18.9% | install.yml → Install postgres database node → Install postgres database node → certificate_authority → certificate_authority | Place AAP CA certificate | Place AAP CA certificate (dest `/etc/pki/ca-trust/source/anchors/ansible-automation-platform-managed-ca-cert.crt`) | — |
| 472 | 19.0% | install.yml → Install postgres database node → Install postgres database node → certificate_authority → certificate_authority | Update CA trust | Update CA trust | — |
| 473 | 19.0% | install.yml → Install postgres database node → Install postgres database node | Run repo_management (setup) | Run repo_management (setup) (role `repo_management`, tasks `setup`) | — |
| 474 | 19.0% | install.yml → Install postgres database node → Install postgres database node → repo_management | role `repo_management` (`setup`) | Enter role task list | — |
| 475 | 19.1% | install.yml → Install postgres database node → Install postgres database node → repo_management → repo_management | file `setup.yml` | Task file `ansible/automation_platform_installer/roles/repo_management/tasks/setup.yml` | — |
| 476 | 19.1% | install.yml → Install postgres database node → Install postgres database node → repo_management → repo_management | include_vars | Load variables from a file | — |
| 477 | 19.2% | install.yml → Install postgres database node → Install postgres database node → repo_management → repo_management | Install yum repo keys | Install yum repo keys (dest `/etc/pki/rpm-gpg/{{ item }}`) | — |
| 478 | 19.2% | install.yml → Install postgres database node → Install postgres database node → repo_management → repo_management | Install subscription-manager-rhsm-certificates | Install subscription-manager-rhsm-certificates | — |
| 479 | 19.3% | install.yml → Install postgres database node → Install postgres database node → repo_management → repo_management | Determine if rhsm repository exists | Determine if rhsm repository exists | — |
| 480 | 19.3% | install.yml → Install postgres database node → Install postgres database node → repo_management → repo_management | Enable Automation Platform rhsm repository | Enable Automation Platform rhsm repository | ansible_distribution == 'RedHat'; rhsm_repo.stat.exists; not bundle_install \| bool; aw_repo_url is undefined |
| 481 | 19.3% | install.yml → Install postgres database node → Install postgres database node → repo_management → repo_management | Explain Automation Platform rhsm repository failure | Explain Automation Platform rhsm repository failure | aap_rhsm_repo_enable.failed \| default(false) |
| 482 | 19.4% | install.yml → Install postgres database node → Install postgres database node → repo_management → repo_management | block | Begin conditional or grouped task block | aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 483 | 19.4% | install.yml → Install postgres database node → Install postgres database node → repo_management → repo_management → block | Enable module hotfixes flag | Enable module hotfixes flag | aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 484 | 19.5% | install.yml → Install postgres database node → Install postgres database node → repo_management → repo_management → block | Set AAP repo for update | Set AAP repo for update | aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 485 | 19.5% | install.yml → Install postgres database node → Install postgres database node → repo_management → repo_management | Ensure rsync is installed | Ensure rsync is installed | bundle_install \| bool |
| 486 | 19.5% | install.yml → Install postgres database node → Install postgres database node → repo_management → repo_management | Copy bundle packages to repo source directory | Begin conditional or grouped task block | bundle_install \| bool |
| 487 | 19.6% | install.yml → Install postgres database node → Install postgres database node → repo_management → repo_management → Copy bundle packages to repo source directory | Copy bundle packages to repo source directory | Copy bundle packages to repo source directory | bundle_install \| bool |
| 488 | 19.6% | install.yml → Install postgres database node → Install postgres database node → repo_management → repo_management | Copy bundle packages to repo source directory (rescue) | Run rescue tasks for block | bundle_install \| bool |
| 489 | 19.7% | install.yml → Install postgres database node → Install postgres database node → repo_management → repo_management → Copy bundle packages to repo source directory (rescue) | Remove old bundle packages source directory (legacy) | Remove old bundle packages source directory (legacy) | bundle_install \| bool |
| 490 | 19.7% | install.yml → Install postgres database node → Install postgres database node → repo_management → repo_management → Copy bundle packages to repo source directory (rescue) | Copy bundle packages to repo source directory (legacy) | Copy bundle packages to repo source directory (legacy) (dest `{{ bundle_install_folder }}`) | bundle_install \| bool |
| 491 | 19.7% | install.yml → Install postgres database node → Install postgres database node → repo_management → repo_management | Install the Automation Platform bundle repository | Install the Automation Platform bundle repository (dest `/etc/yum.repos.d/ansible-automation-platform.repo`) | bundle_install \| bool |
| 492 | 19.8% | install.yml → Install postgres database node → Install postgres database node → repo_management → repo_management | Remove legacy ansible-tower repository | Remove legacy ansible-tower repository | — |
| 493 | 19.8% | install.yml → Install postgres database node → Install postgres database node → repo_management → repo_management | Install the Automation Platform yum repository. | Install the Automation Platform yum repository. (dest `/etc/yum.repos.d/ansible-automation-platform.repo`) | aw_repo_url is defined; not bundle_install \| bool |
| 494 | 19.9% | install.yml → Install postgres database node → Install postgres database node → repo_management → repo_management | Set repo list for bundle install | Set repo list for bundle install | bundle_install \| bool |
| 495 | 19.9% | install.yml → Install postgres database node → Install postgres database node → repo_management → repo_management | Set repo list for online install | Set repo list for online install | yum_repo is succeeded; yum_repo is not skipped |
| 496 | 19.9% | install.yml → Install postgres database node → Install postgres database node → repo_management → repo_management | Enable Ansible Automation Platform repo | Enable Ansible Automation Platform repo | extra_repos is defined |
| 497 | 20.0% | install.yml → Install postgres database node → Install postgres database node | Run packages_el | Run packages_el (role `packages_el`) | — |
| 498 | 20.0% | install.yml → Install postgres database node → Install postgres database node → packages_el | role `packages_el` | Enter role task list | — |
| 499 | 20.1% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el | file `main.yml` | Task file `ansible/automation_platform_installer/roles/packages_el/tasks/main.yml` | — |
| 500 | 20.1% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el | include packages_el variables | include packages_el variables | — |
| 501 | 20.1% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el | include_tasks | Include a task file from the current role or playbook (file `install_deps.yml`) | — |
| 502 | 20.2% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_deps.yml | file `install_deps.yml` | Task file `ansible/automation_platform_installer/roles/packages_el/tasks/install_deps.yml` | — |
| 503 | 20.2% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_deps.yml | Install playbook dependencies | Install playbook dependencies | — |
| 504 | 20.3% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el | include_tasks | Include a task file from the current role or playbook (file `upgrade_postgres_client.yml`) | packages_el_install_tower \| bool |
| 505 | 20.3% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → upgrade_postgres_client.yml | file `upgrade_postgres_client.yml` | Task file `ansible/automation_platform_installer/roles/packages_el/tasks/upgrade_postgres_client.yml` | packages_el_install_tower \| bool |
| 506 | 20.3% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → upgrade_postgres_client.yml | Uninstall existing PostgreSQL Client | Uninstall existing PostgreSQL Client | packages_el_install_tower \| bool |
| 507 | 20.4% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → upgrade_postgres_client.yml | Enable PostgreSQL {{ automationcontroller_postgresql_version }} dnf modules | Enable PostgreSQL {{ automationcontroller_postgresql_version }} dnf modules | packages_el_install_tower \| bool |
| 508 | 20.4% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → upgrade_postgres_client.yml | Install PostgreSQL Client | Install PostgreSQL Client | packages_el_install_tower \| bool |
| 509 | 20.5% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el | include_tasks | Include a task file from the current role or playbook (file `install_controller.yml`) | packages_el_install_tower \| bool |
| 510 | 20.5% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_controller.yml | file `install_controller.yml` | Task file `ansible/automation_platform_installer/roles/packages_el/tasks/install_controller.yml` | packages_el_install_tower \| bool |
| 511 | 20.5% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_controller.yml | Clean Yum Cache for the Automation Platform Repo | Clean Yum Cache for the Automation Platform Repo | packages_el_install_tower \| bool |
| 512 | 20.6% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_controller.yml | Install the Automation Controller RPM. | Install the Automation Controller RPM. | packages_el_install_tower \| bool |
| 513 | 20.6% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_controller.yml | Create is_aap_24 file if AAP is on version 2.4 | Create is_aap_24 file if AAP is on version 2.4 | packages_el_install_tower \| bool; groups['automationcontroller'] \| map('extract', hostvars, '_aap_version_on_host') \| min is version('2.4', '==') |
| 514 | 20.7% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_controller.yml | Install fapolicyd rules for Automation Controller if applicable | Install fapolicyd rules for Automation Controller if applicable | packages_el_install_tower \| bool; 'fapolicyd.service' in ansible_facts.services |
| 515 | 20.7% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_controller.yml | Remove stale packages | Remove stale packages | packages_el_install_tower \| bool |
| 516 | 20.7% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_controller.yml | Update existing dependent packages | Update existing dependent packages (role `repo_management`, tasks `update_dependency`) | packages_el_install_tower \| bool |
| 517 | 20.8% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_controller.yml → repo_management | role `repo_management` (`update_dependency`) | Enter role task list | packages_el_install_tower \| bool |
| 518 | 20.8% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_controller.yml → repo_management → repo_management | file `update_dependency.yml` | Task file `ansible/automation_platform_installer/roles/repo_management/tasks/update_dependency.yml` | packages_el_install_tower \| bool |
| 519 | 20.9% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_controller.yml → repo_management → repo_management | Ensure AAP repo for update is set | Ensure AAP repo for update is set | packages_el_install_tower \| bool; not aap_repo_for_update \| default('') \| length |
| 520 | 20.9% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_controller.yml → repo_management → repo_management | Generate a list of AAP packages with an update | Generate a list of AAP packages with an update | packages_el_install_tower \| bool |
| 521 | 20.9% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_controller.yml → repo_management → repo_management | Set AAP packages to update | Set AAP packages to update | packages_el_install_tower \| bool |
| 522 | 21.0% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_controller.yml → repo_management → repo_management | Update packages | Update packages | packages_el_install_tower \| bool; _packages_to_update \| length |
| 523 | 21.0% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_controller.yml | Re-install aap-metrics-utility | Begin conditional or grouped task block | packages_el_install_tower \| bool; _controller_rpm_install is changed |
| 524 | 21.1% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_controller.yml → Re-install aap-metrics-utility | Verify aap-metrics-utility package | Verify aap-metrics-utility package | packages_el_install_tower \| bool; _controller_rpm_install is changed |
| 525 | 21.1% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_controller.yml → Re-install aap-metrics-utility | Re-install aap-metrics-utility | Re-install aap-metrics-utility | packages_el_install_tower \| bool; _controller_rpm_install is changed; _metrics_utility_rpm_verify is failed |
| 526 | 21.1% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el | Uninstall nginx-all-modules | Uninstall nginx-all-modules | — |
| 527 | 21.2% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el | include_tasks | Include a task file from the current role or playbook (file `install_postgres.yml`) | packages_el_install_postgres \| bool |
| 528 | 21.2% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml | file `install_postgres.yml` | Task file `ansible/automation_platform_installer/roles/packages_el/tasks/install_postgres.yml` | packages_el_install_postgres \| bool |
| 529 | 21.3% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml | Check for old PostgreSQL data | Check for old PostgreSQL data | packages_el_install_postgres \| bool |
| 530 | 21.3% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml | Read PG_VERSION file contents | Read PG_VERSION file contents | packages_el_install_postgres \| bool; old_pg_version_file.stat.exists |
| 531 | 21.3% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml | Check if PostgreSQL has already been upgraded to 15 | Check if PostgreSQL has already been upgraded to 15 | packages_el_install_postgres \| bool |
| 532 | 21.4% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml | Check if PostgreSQL has already been upgraded to 13 | Check if PostgreSQL has already been upgraded to 13 | packages_el_install_postgres \| bool |
| 533 | 21.4% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml | Check if PostgreSQL has already been upgraded to 12 | Check if PostgreSQL has already been upgraded to 12 | packages_el_install_postgres \| bool |
| 534 | 21.5% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml | Determine if PostgreSQL 10 is present | Determine if PostgreSQL 10 is present | packages_el_install_postgres \| bool |
| 535 | 21.5% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml | Determine if PostgreSQL 12 is present | Determine if PostgreSQL 12 is present | packages_el_install_postgres \| bool |
| 536 | 21.5% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml | Determine if we should upgrade PostgreSQL | Determine if we should upgrade PostgreSQL | packages_el_install_postgres \| bool |
| 537 | 21.6% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml | block | Begin conditional or grouped task block | packages_el_install_postgres \| bool; not upgrade_postgres \| bool; not upgrade_from_postgres_12 \| bool; not upgrade_from_postgres_10 \| bool |
| 538 | 21.6% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → block | Enable PostgreSQL {{ automationcontroller_postgresql_version }} dnf modules | Enable PostgreSQL {{ automationcontroller_postgresql_version }} dnf modules | packages_el_install_postgres \| bool; not upgrade_postgres \| bool; not upgrade_from_postgres_12 \| bool; not upgrade_from_postgres_10 \| bool |
| 539 | 21.7% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → block | Install PostgreSQL | Install PostgreSQL | packages_el_install_postgres \| bool; not upgrade_postgres \| bool; not upgrade_from_postgres_12 \| bool; not upgrade_from_postgres_10 \| bool |
| 540 | 21.7% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml | Upgrade PostgreSQL data from 13 --> 15 | Upgrade PostgreSQL data from 13 --> 15 (file `upgrade_postgres_from_13.yml`) | packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 541 | 21.7% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml | file `upgrade_postgres_from_13.yml` | Task file `ansible/automation_platform_installer/roles/packages_el/tasks/upgrade_postgres_from_13.yml` | packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 542 | 21.8% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml | Stop PostgreSQL 13 | Stop PostgreSQL 13 (service `{{ pg_systemd_name }}`) | packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 543 | 21.8% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml | Reset PostgreSQL module | Reset PostgreSQL module | packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 544 | 21.9% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml | Enable PostgreSQL 15 dnf modules | Enable PostgreSQL 15 dnf modules | packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 545 | 21.9% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml | Install PostgreSQL 15 | Install PostgreSQL 15 | packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 546 | 21.9% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml | Upgrade to PostgreSQL 15 | Upgrade to PostgreSQL 15 | packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 547 | 22.0% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml | Uninstall postgresql-upgrade package | Uninstall postgresql-upgrade package | packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 548 | 22.0% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml | include_tasks | Include a task file from the current role or playbook (file `install_deps.yml`) | packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 549 | 22.1% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml → install_deps.yml | file `install_deps.yml` | Task file `ansible/automation_platform_installer/roles/packages_el/tasks/install_deps.yml` | packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 550 | 22.1% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml → install_deps.yml | Install playbook dependencies | Install playbook dependencies | packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 551 | 22.2% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml | Create file to signify PostgreSQL has been upgraded | Create file to signify PostgreSQL has been upgraded | packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 552 | 22.2% | install.yml → Install postgres database node → Install postgres database node | meta | Ansible meta task (flush handlers, clear facts, etc.) (flush_handlers) | — |
| 553 | 22.2% | install.yml → Install postgres database node → Install postgres database node | include_role | Include and run tasks from an Ansible role (role `postgres`) | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]) |
| 554 | 22.3% | install.yml → Install postgres database node → Install postgres database node → postgres | role `postgres` | Enter role task list | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]) |
| 555 | 22.3% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres | file `main.yml` | Task file `ansible/automation_platform_installer/roles/postgres/tasks/main.yml` | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]) |
| 556 | 22.4% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres | include_tasks | Include a task file from the current role or playbook (file `vars.yml`) | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]) |
| 557 | 22.4% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → vars.yml | file `vars.yml` | Task file `ansible/automation_platform_installer/roles/postgres/tasks/vars.yml` | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]) |
| 558 | 22.4% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → vars.yml | include_vars | Load variables from a file | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]) |
| 559 | 22.5% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres | Ensure all locales in system are in UTF-8 | Ensure all locales in system are in UTF-8 (role `preflight`, tasks `locale.yaml`) | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]) |
| 560 | 22.5% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → preflight | role `preflight` (`locale.yaml`) | Enter role task list | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]) |
| 561 | 22.6% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → preflight → preflight | file `locale.yaml` | Task file `ansible/automation_platform_installer/roles/preflight/tasks/locale.yaml` | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]) |
| 562 | 22.6% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → preflight → preflight | Load /etc/locale.conf | Load /etc/locale.conf | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]) |
| 563 | 22.6% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → preflight → preflight | fail | Fail the play with an error message | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); item is not regex(".*(utf8\|UTF-8)") |
| 564 | 22.7% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → preflight → preflight | Get the LANG environment variable | Get the LANG environment variable | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); item is regex("^LANG=.*") |
| 565 | 22.7% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → preflight → preflight | Check if locale is properly installed | Check if locale is properly installed | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]) |
| 566 | 22.8% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → preflight → preflight | fail | Fail the play with an error message | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); _locale.rc != 0 |
| 567 | 22.8% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres | include_tasks | Include a task file from the current role or playbook (file `preflight.yml`) | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool |
| 568 | 22.8% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → preflight.yml | file `preflight.yml` | Task file `ansible/automation_platform_installer/roles/postgres/tasks/preflight.yml` | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool |
| 569 | 22.9% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → preflight.yml | Preflight check - Ensure that the Postgres data dir is not a mount point | Preflight check - Ensure that the Postgres data dir is not a mount point | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool |
| 570 | 22.9% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → preflight.yml | Fail if the data dir is a mount point | Fail if the data dir is a mount point | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; is_pg_data_mount.rc  \| int == 0 |
| 571 | 23.0% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres | include_tasks | Include a task file from the current role or playbook (file `init.yml`) | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool |
| 572 | 23.0% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → init.yml | file `init.yml` | Task file `ansible/automation_platform_installer/roles/postgres/tasks/init.yml` | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool |
| 573 | 23.0% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → init.yml | init postgresql | init postgresql | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool |
| 574 | 23.1% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → init.yml | Open up postgres port permissions | Open up postgres port permissions | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 575 | 23.1% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres | include_tasks | Include a task file from the current role or playbook (file `conf.yml`) | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool |
| 576 | 23.2% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | file `conf.yml` | Task file `ansible/automation_platform_installer/roles/postgres/tasks/conf.yml` | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool |
| 577 | 23.2% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | Update postgresql authentication settings | Update postgresql authentication settings (dest `{{ pg_hba_location }}`) | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool |
| 578 | 23.2% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | Apply postgresql general settings | Apply postgresql general settings (dest `{{ pg_conf_location }}`) | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool |
| 579 | 23.3% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | Copy server SSL certificate | Copy server SSL certificate (dest `{{ pg_conf_dir }}/server.crt`) | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is defined; postgres_ssl_key is defined |
| 580 | 23.3% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | Copy server SSL private key | Copy server SSL private key (dest `{{ pg_conf_dir }}/server.key`) | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is defined; postgres_ssl_key is defined |
| 581 | 23.4% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | block | Begin conditional or grouped task block | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined |
| 582 | 23.4% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block | Use internal CA for internal database | Use internal CA for internal database (role `certificate_authority`, tasks `sign_service.yml`) | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 583 | 23.4% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority | role `certificate_authority` (`sign_service.yml`) | Enter role task list | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 584 | 23.5% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | file `sign_service.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/sign_service.yml` | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 585 | 23.5% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | include_tasks | Include a task file from the current role or playbook (file `init.yml`) | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 586 | 23.6% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → init.yml | file `init.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/init.yml` | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 587 | 23.6% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → init.yml | Ensure python cryptography libraries are installed | Ensure python cryptography libraries are installed | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 588 | 23.6% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | Create {{ aap_ca_servicename }} private key | Create {{ aap_ca_servicename }} private key | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 589 | 23.7% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | set_fact | Set a host fact used by later tasks | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 590 | 23.7% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | Check if {{ aap_ca_servicename }} public key exists | Check if {{ aap_ca_servicename }} public key exists | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 591 | 23.8% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; certificate_stat.stat.exists \| bool |
| 592 | 23.8% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Check contents of {{ aap_ca_servicename }} public key | Check contents of {{ aap_ca_servicename }} public key | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; certificate_stat.stat.exists \| bool |
| 593 | 23.8% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; certificate_stat.stat.exists \| bool; aap_service_san_records_internal \| union(aap_service_san_records) \| difference(certificate_check.subject_alt_name) \| length > 0 |
| 594 | 23.9% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 595 | 23.9% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false); aap_service_is_client |
| 596 | 24.0% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Create CSR for {{ aap_ca_servicename }} node/s | Create CSR for {{ aap_ca_servicename }} node/s | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 597 | 24.0% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Sign {{ aap_ca_servicename }} public key | Sign {{ aap_ca_servicename }} public key | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 598 | 24.0% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Write public key to {{ aap_ca_servicename }} node/s | Write public key to {{ aap_ca_servicename }} node/s (dest `{{ aap_service_cert_fullpath }}`) | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 599 | 24.1% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | Signal restart if certificate pair changes | Signal restart if certificate pair changes | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; (write_key is changed or write_certificate is changed) |
| 600 | 24.1% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block | Use internal CA for external database | Use internal CA for external database (role `certificate_authority`, tasks `sign_service.yml`) | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 601 | 24.2% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority | role `certificate_authority` (`sign_service.yml`) | Enter role task list | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 602 | 24.2% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | file `sign_service.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/sign_service.yml` | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 603 | 24.2% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | include_tasks | Include a task file from the current role or playbook (file `init.yml`) | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 604 | 24.3% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → init.yml | file `init.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/init.yml` | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 605 | 24.3% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → init.yml | Ensure python cryptography libraries are installed | Ensure python cryptography libraries are installed | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 606 | 24.4% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | Create {{ aap_ca_servicename }} private key | Create {{ aap_ca_servicename }} private key | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 607 | 24.4% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | set_fact | Set a host fact used by later tasks | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 608 | 24.4% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | Check if {{ aap_ca_servicename }} public key exists | Check if {{ aap_ca_servicename }} public key exists | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 609 | 24.5% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; certificate_stat.stat.exists \| bool |
| 610 | 24.5% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Check contents of {{ aap_ca_servicename }} public key | Check contents of {{ aap_ca_servicename }} public key | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; certificate_stat.stat.exists \| bool |
| 611 | 24.6% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; certificate_stat.stat.exists \| bool; aap_service_san_records_internal \| union(aap_service_san_records) \| difference(certificate_check.subject_alt_name) \| length > 0 |
| 612 | 24.6% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 613 | 24.6% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false); aap_service_is_client |
| 614 | 24.7% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Create CSR for {{ aap_ca_servicename }} node/s | Create CSR for {{ aap_ca_servicename }} node/s | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 615 | 24.7% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Sign {{ aap_ca_servicename }} public key | Sign {{ aap_ca_servicename }} public key | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 616 | 24.8% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Write public key to {{ aap_ca_servicename }} node/s | Write public key to {{ aap_ca_servicename }} node/s (dest `{{ aap_service_cert_fullpath }}`) | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 617 | 24.8% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | Signal restart if certificate pair changes | Signal restart if certificate pair changes | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; (write_key is changed or write_certificate is changed) |
| 618 | 24.8% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | Create unix socket directory | Create unix socket directory | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool |
| 619 | 24.9% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | Configure postgresql tmpfiles.d | Configure postgresql tmpfiles.d (dest `/usr/lib/tmpfiles.d/postgresql.conf`) | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool |
| 620 | 24.9% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | set_fact | Set a host fact used by later tasks | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; aap_service_restart \| default(false) or pg_hba_conf is changed or pg_general_conf is changed |
| 621 | 25.0% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | ensure postgresql is running and restart when authentication settings change | ensure postgresql is running and restart when authentication settings change (service `{{ postgres_init_name }}`) | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool |
| 622 | 25.0% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | wait for postgresql restart | wait for postgresql restart | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; postgresql_restart is changed |
| 623 | 25.1% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | configure postgresql to start automatically | configure postgresql to start automatically (service `{{ postgres_init_name }}`) | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool |
| 624 | 25.1% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | block | Begin conditional or grouped task block | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; groups['automationcontroller'] \| default([]) \| length |
| 625 | 25.1% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block | create the awx postgres user | create the awx postgres user | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; groups['automationcontroller'] \| default([]) \| length; postgres_username is defined and postgres_username != ''; postgres_password is defined and postgres_password != '' |
| 626 | 25.2% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block | create the postgresql database for awx | create the postgresql database for awx | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; groups['automationcontroller'] \| default([]) \| length; postgres_username is defined and postgres_username != ''; postgres_database is defined and postgres_database != '' |
| 627 | 25.2% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | block | Begin conditional or grouped task block | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; groups['automationhub'] \| default([]) \| length |
| 628 | 25.3% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block | create the automationhub postgres user | create the automationhub postgres user | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; groups['automationhub'] \| default([]) \| length; automationhub_postgres_username is defined and automationhub_postgres_username != ''; automationhub_postgres_password is defined and automationhub_postgres_password != '' |
| 629 | 25.3% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block | create the postgresql database for automationhub | create the postgresql database for automationhub | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; groups['automationhub'] \| default([]) \| length; automationhub_postgres_username is defined and automationhub_postgres_username != ''; automationhub_postgres_database is defined and automationhub_postgres_database != '' |
| 630 | 25.3% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block | enable the postgresql hstore extension | enable the postgresql hstore extension | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; groups['automationhub'] \| default([]) \| length |
| 631 | 25.4% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | block | Begin conditional or grouped task block | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; groups['automationedacontroller'] \| default([]) \| length |
| 632 | 25.4% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block | create the automationedacontroller postgres user | create the automationedacontroller postgres user | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; groups['automationedacontroller'] \| default([]) \| length; automationedacontroller_postgres_username is defined and automationedacontroller_postgres_username != ''; automationedacontroller_postgres_password is defined and automationedacontroller_postgres_password != '' |
| 633 | 25.5% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block | create the postgresql database for automationedacontroller | create the postgresql database for automationedacontroller | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; groups['automationedacontroller'] \| default([]) \| length; automationedacontroller_postgres_username is defined and automationedacontroller_postgres_username != ''; automationedacontroller_postgres_database is defined and automationedacontroller_postgres_database != '' |
| 634 | 25.5% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | block | Begin conditional or grouped task block | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; groups['automationgateway'] \| default([]) \| length |
| 635 | 25.5% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block | create the automationgateway postgres user | create the automationgateway postgres user | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; groups['automationgateway'] \| default([]) \| length; automationgateway_postgres_username is defined and automationgateway_postgres_username != ''; automationgateway_postgres_password is defined and automationgateway_postgres_password != '' |
| 636 | 25.6% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block | create the postgresql database for automationgateway | create the postgresql database for automationgateway | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; groups['automationgateway'] \| default([]) \| length; automationgateway_postgres_username is defined and automationgateway_postgres_username != ''; automationgateway_postgres_database is defined and automationgateway_postgres_database != '' |
| 637 | 25.6% | install.yml → Install postgres database node → Install postgres database node | include_role | Include and run tasks from an Ansible role (role `postgres`) | _do_run_db_install \| default(false) \| bool |
| 638 | 25.7% | install.yml → Install postgres database node → Install postgres database node → postgres | role `postgres` | Enter role task list | _do_run_db_install \| default(false) \| bool |
| 639 | 25.7% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres | file `main.yml` | Task file `ansible/automation_platform_installer/roles/postgres/tasks/main.yml` | _do_run_db_install \| default(false) \| bool |
| 640 | 25.7% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres | include_tasks | Include a task file from the current role or playbook (file `vars.yml`) | _do_run_db_install \| default(false) \| bool |
| 641 | 25.8% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → vars.yml | file `vars.yml` | Task file `ansible/automation_platform_installer/roles/postgres/tasks/vars.yml` | _do_run_db_install \| default(false) \| bool |
| 642 | 25.8% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → vars.yml | include_vars | Load variables from a file | _do_run_db_install \| default(false) \| bool |
| 643 | 25.9% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres | Ensure all locales in system are in UTF-8 | Ensure all locales in system are in UTF-8 (role `preflight`, tasks `locale.yaml`) | _do_run_db_install \| default(false) \| bool |
| 644 | 25.9% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → preflight | role `preflight` (`locale.yaml`) | Enter role task list | _do_run_db_install \| default(false) \| bool |
| 645 | 25.9% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → preflight → preflight | file `locale.yaml` | Task file `ansible/automation_platform_installer/roles/preflight/tasks/locale.yaml` | _do_run_db_install \| default(false) \| bool |
| 646 | 26.0% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → preflight → preflight | Load /etc/locale.conf | Load /etc/locale.conf | _do_run_db_install \| default(false) \| bool |
| 647 | 26.0% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → preflight → preflight | fail | Fail the play with an error message | _do_run_db_install \| default(false) \| bool; item is not regex(".*(utf8\|UTF-8)") |
| 648 | 26.1% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → preflight → preflight | Get the LANG environment variable | Get the LANG environment variable | _do_run_db_install \| default(false) \| bool; item is regex("^LANG=.*") |
| 649 | 26.1% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → preflight → preflight | Check if locale is properly installed | Check if locale is properly installed | _do_run_db_install \| default(false) \| bool |
| 650 | 26.1% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → preflight → preflight | fail | Fail the play with an error message | _do_run_db_install \| default(false) \| bool; _locale.rc != 0 |
| 651 | 26.2% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres | include_tasks | Include a task file from the current role or playbook (file `preflight.yml`) | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool |
| 652 | 26.2% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → preflight.yml | file `preflight.yml` | Task file `ansible/automation_platform_installer/roles/postgres/tasks/preflight.yml` | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool |
| 653 | 26.3% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → preflight.yml | Preflight check - Ensure that the Postgres data dir is not a mount point | Preflight check - Ensure that the Postgres data dir is not a mount point | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool |
| 654 | 26.3% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → preflight.yml | Fail if the data dir is a mount point | Fail if the data dir is a mount point | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; is_pg_data_mount.rc  \| int == 0 |
| 655 | 26.3% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres | include_tasks | Include a task file from the current role or playbook (file `init.yml`) | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool |
| 656 | 26.4% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → init.yml | file `init.yml` | Task file `ansible/automation_platform_installer/roles/postgres/tasks/init.yml` | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool |
| 657 | 26.4% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → init.yml | init postgresql | init postgresql | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool |
| 658 | 26.5% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → init.yml | Open up postgres port permissions | Open up postgres port permissions | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 659 | 26.5% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres | include_tasks | Include a task file from the current role or playbook (file `conf.yml`) | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool |
| 660 | 26.5% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | file `conf.yml` | Task file `ansible/automation_platform_installer/roles/postgres/tasks/conf.yml` | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool |
| 661 | 26.6% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | Update postgresql authentication settings | Update postgresql authentication settings (dest `{{ pg_hba_location }}`) | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool |
| 662 | 26.6% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | Apply postgresql general settings | Apply postgresql general settings (dest `{{ pg_conf_location }}`) | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool |
| 663 | 26.7% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | Copy server SSL certificate | Copy server SSL certificate (dest `{{ pg_conf_dir }}/server.crt`) | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is defined; postgres_ssl_key is defined |
| 664 | 26.7% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | Copy server SSL private key | Copy server SSL private key (dest `{{ pg_conf_dir }}/server.key`) | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is defined; postgres_ssl_key is defined |
| 665 | 26.7% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | block | Begin conditional or grouped task block | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined |
| 666 | 26.8% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block | Use internal CA for internal database | Use internal CA for internal database (role `certificate_authority`, tasks `sign_service.yml`) | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 667 | 26.8% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority | role `certificate_authority` (`sign_service.yml`) | Enter role task list | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 668 | 26.9% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | file `sign_service.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/sign_service.yml` | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 669 | 26.9% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | include_tasks | Include a task file from the current role or playbook (file `init.yml`) | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 670 | 26.9% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → init.yml | file `init.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/init.yml` | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 671 | 27.0% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → init.yml | Ensure python cryptography libraries are installed | Ensure python cryptography libraries are installed | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 672 | 27.0% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | Create {{ aap_ca_servicename }} private key | Create {{ aap_ca_servicename }} private key | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 673 | 27.1% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | set_fact | Set a host fact used by later tasks | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 674 | 27.1% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | Check if {{ aap_ca_servicename }} public key exists | Check if {{ aap_ca_servicename }} public key exists | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 675 | 27.1% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; certificate_stat.stat.exists \| bool |
| 676 | 27.2% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Check contents of {{ aap_ca_servicename }} public key | Check contents of {{ aap_ca_servicename }} public key | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; certificate_stat.stat.exists \| bool |
| 677 | 27.2% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; certificate_stat.stat.exists \| bool; aap_service_san_records_internal \| union(aap_service_san_records) \| difference(certificate_check.subject_alt_name) \| length > 0 |
| 678 | 27.3% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 679 | 27.3% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false); aap_service_is_client |
| 680 | 27.3% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Create CSR for {{ aap_ca_servicename }} node/s | Create CSR for {{ aap_ca_servicename }} node/s | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 681 | 27.4% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Sign {{ aap_ca_servicename }} public key | Sign {{ aap_ca_servicename }} public key | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 682 | 27.4% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Write public key to {{ aap_ca_servicename }} node/s | Write public key to {{ aap_ca_servicename }} node/s (dest `{{ aap_service_cert_fullpath }}`) | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 683 | 27.5% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | Signal restart if certificate pair changes | Signal restart if certificate pair changes | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; (write_key is changed or write_certificate is changed) |
| 684 | 27.5% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block | Use internal CA for external database | Use internal CA for external database (role `certificate_authority`, tasks `sign_service.yml`) | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 685 | 27.5% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority | role `certificate_authority` (`sign_service.yml`) | Enter role task list | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 686 | 27.6% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | file `sign_service.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/sign_service.yml` | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 687 | 27.6% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | include_tasks | Include a task file from the current role or playbook (file `init.yml`) | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 688 | 27.7% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → init.yml | file `init.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/init.yml` | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 689 | 27.7% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → init.yml | Ensure python cryptography libraries are installed | Ensure python cryptography libraries are installed | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 690 | 27.7% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | Create {{ aap_ca_servicename }} private key | Create {{ aap_ca_servicename }} private key | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 691 | 27.8% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | set_fact | Set a host fact used by later tasks | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 692 | 27.8% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | Check if {{ aap_ca_servicename }} public key exists | Check if {{ aap_ca_servicename }} public key exists | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 693 | 27.9% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; certificate_stat.stat.exists \| bool |
| 694 | 27.9% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Check contents of {{ aap_ca_servicename }} public key | Check contents of {{ aap_ca_servicename }} public key | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; certificate_stat.stat.exists \| bool |
| 695 | 28.0% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; certificate_stat.stat.exists \| bool; aap_service_san_records_internal \| union(aap_service_san_records) \| difference(certificate_check.subject_alt_name) \| length > 0 |
| 696 | 28.0% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 697 | 28.0% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false); aap_service_is_client |
| 698 | 28.1% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Create CSR for {{ aap_ca_servicename }} node/s | Create CSR for {{ aap_ca_servicename }} node/s | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 699 | 28.1% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Sign {{ aap_ca_servicename }} public key | Sign {{ aap_ca_servicename }} public key | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 700 | 28.2% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Write public key to {{ aap_ca_servicename }} node/s | Write public key to {{ aap_ca_servicename }} node/s (dest `{{ aap_service_cert_fullpath }}`) | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 701 | 28.2% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | Signal restart if certificate pair changes | Signal restart if certificate pair changes | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; (write_key is changed or write_certificate is changed) |
| 702 | 28.2% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | Create unix socket directory | Create unix socket directory | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool |
| 703 | 28.3% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | Configure postgresql tmpfiles.d | Configure postgresql tmpfiles.d (dest `/usr/lib/tmpfiles.d/postgresql.conf`) | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool |
| 704 | 28.3% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | set_fact | Set a host fact used by later tasks | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; aap_service_restart \| default(false) or pg_hba_conf is changed or pg_general_conf is changed |
| 705 | 28.4% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | ensure postgresql is running and restart when authentication settings change | ensure postgresql is running and restart when authentication settings change (service `{{ postgres_init_name }}`) | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool |
| 706 | 28.4% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | wait for postgresql restart | wait for postgresql restart | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; postgresql_restart is changed |
| 707 | 28.4% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | configure postgresql to start automatically | configure postgresql to start automatically (service `{{ postgres_init_name }}`) | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool |
| 708 | 28.5% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | block | Begin conditional or grouped task block | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; groups['automationcontroller'] \| default([]) \| length |
| 709 | 28.5% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block | create the awx postgres user | create the awx postgres user | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; groups['automationcontroller'] \| default([]) \| length; postgres_username is defined and postgres_username != ''; postgres_password is defined and postgres_password != '' |
| 710 | 28.6% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block | create the postgresql database for awx | create the postgresql database for awx | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; groups['automationcontroller'] \| default([]) \| length; postgres_username is defined and postgres_username != ''; postgres_database is defined and postgres_database != '' |
| 711 | 28.6% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | block | Begin conditional or grouped task block | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; groups['automationhub'] \| default([]) \| length |
| 712 | 28.6% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block | create the automationhub postgres user | create the automationhub postgres user | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; groups['automationhub'] \| default([]) \| length; automationhub_postgres_username is defined and automationhub_postgres_username != ''; automationhub_postgres_password is defined and automationhub_postgres_password != '' |
| 713 | 28.7% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block | create the postgresql database for automationhub | create the postgresql database for automationhub | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; groups['automationhub'] \| default([]) \| length; automationhub_postgres_username is defined and automationhub_postgres_username != ''; automationhub_postgres_database is defined and automationhub_postgres_database != '' |
| 714 | 28.7% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block | enable the postgresql hstore extension | enable the postgresql hstore extension | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; groups['automationhub'] \| default([]) \| length |
| 715 | 28.8% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | block | Begin conditional or grouped task block | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; groups['automationedacontroller'] \| default([]) \| length |
| 716 | 28.8% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block | create the automationedacontroller postgres user | create the automationedacontroller postgres user | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; groups['automationedacontroller'] \| default([]) \| length; automationedacontroller_postgres_username is defined and automationedacontroller_postgres_username != ''; automationedacontroller_postgres_password is defined and automationedacontroller_postgres_password != '' |
| 717 | 28.8% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block | create the postgresql database for automationedacontroller | create the postgresql database for automationedacontroller | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; groups['automationedacontroller'] \| default([]) \| length; automationedacontroller_postgres_username is defined and automationedacontroller_postgres_username != ''; automationedacontroller_postgres_database is defined and automationedacontroller_postgres_database != '' |
| 718 | 28.9% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | block | Begin conditional or grouped task block | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; groups['automationgateway'] \| default([]) \| length |
| 719 | 28.9% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block | create the automationgateway postgres user | create the automationgateway postgres user | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; groups['automationgateway'] \| default([]) \| length; automationgateway_postgres_username is defined and automationgateway_postgres_username != ''; automationgateway_postgres_password is defined and automationgateway_postgres_password != '' |
| 720 | 29.0% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block | create the postgresql database for automationgateway | create the postgresql database for automationgateway | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; groups['automationgateway'] \| default([]) \| length; automationgateway_postgres_username is defined and automationgateway_postgres_username != ''; automationgateway_postgres_database is defined and automationgateway_postgres_database != '' |
| 721 | 29.0% | install.yml → Install postgres database node → Install postgres database node | include_role | Include and run tasks from an Ansible role (role `firewall`) | — |
| 722 | 29.0% | install.yml → Install postgres database node → Install postgres database node → firewall | role `firewall` | Enter role task list | — |
| 723 | 29.1% | install.yml → Install postgres database node → Install postgres database node → firewall → firewall | file `main.yml` | Task file `ansible/automation_platform_installer/roles/firewall/tasks/main.yml` | — |
| 724 | 29.1% | install.yml → Install postgres database node → Install postgres database node → firewall → firewall | include_tasks | Include a task file from the current role or playbook (file `discover.yml`) | — |
| 725 | 29.2% | install.yml → Install postgres database node → Install postgres database node → firewall → firewall → discover.yml | file `discover.yml` | Task file `ansible/automation_platform_installer/roles/firewall/tasks/discover.yml` | — |
| 726 | 29.2% | install.yml → Install postgres database node → Install postgres database node → firewall → firewall → discover.yml | Determine if firewalld is running | Determine if firewalld is running | — |
| 727 | 29.2% | install.yml → Install postgres database node → Install postgres database node → firewall → firewall → discover.yml | Determine if firewalld is enabled | Determine if firewalld is enabled | — |
| 728 | 29.3% | install.yml → Install postgres database node → Install postgres database node → firewall → firewall → discover.yml | Determine if iptables is installed | Determine if iptables is installed | — |
| 729 | 29.3% | install.yml → Install postgres database node → Install postgres database node → firewall → firewall → discover.yml | Determine if iptables service is installed | Determine if iptables service is installed | — |
| 730 | 29.4% | install.yml → Install postgres database node → Install postgres database node → firewall → firewall → discover.yml | Check to see if there is an iptables config file | Check to see if there is an iptables config file | iptables_installed.rc == 0 |
| 731 | 29.4% | install.yml → Install postgres database node → Install postgres database node → firewall → firewall → discover.yml | Select firewalld | Select firewalld | firewalld_running.rc == 0 or systemctl_unit_files.stdout_lines \| select('match','firewalld.service\s+enabled') \| list \| length == 1 |
| 732 | 29.4% | install.yml → Install postgres database node → Install postgres database node → firewall → firewall → discover.yml | Select iptables | Select iptables | iptables_installed.rc == 0; iptables_exists.stat.exists; iptables_service.stat.exists |
| 733 | 29.5% | install.yml → Install postgres database node → Install postgres database node → firewall → firewall | Include {{ 'no' if component_firewall_type == 'none' else component_firewall_type }} tasks | Include {{ 'no' if component_firewall_type == 'none' else component_firewall_type }} tasks (file `{{ component_firewall_type }}.yml`) | firewalld_running.rc == 0 or systemctl_unit_files.stdout_lines \| select('match','firewalld.service\s+enabled') \| list \| length == 1 or ( iptables_installed.rc == 0 and iptables_exists.stat.exists and iptables_service.stat.exists ) |
| 734 | 29.5% | install.yml → Install postgres database node → Install postgres database node → firewall → firewall → {{ component_firewall_type }}.yml | {{ component_firewall_type }}.yml | Include task file `{{ component_firewall_type }}.yml` (relative to current role task directory) | firewalld_running.rc == 0 or systemctl_unit_files.stdout_lines \| select('match','firewalld.service\s+enabled') \| list \| length == 1 or ( iptables_installed.rc == 0 and iptables_exists.stat.exists and iptables_service.stat.exists ) |
| 735 | 29.6% | install.yml → Install postgres database node | Install postgres database node (always) | Run always tasks for block | — |
| 736 | 29.6% | install.yml → Install postgres database node → Install postgres database node (always) | Run repo_management (teardown) | Run repo_management (teardown) (role `repo_management`, tasks `teardown`) | — |
| 737 | 29.6% | install.yml → Install postgres database node → Install postgres database node (always) → repo_management | role `repo_management` (`teardown`) | Enter role task list | — |
| 738 | 29.7% | install.yml → Install postgres database node → Install postgres database node (always) → repo_management → repo_management | file `teardown.yml` | Task file `ansible/automation_platform_installer/roles/repo_management/tasks/teardown.yml` | — |
| 739 | 29.7% | install.yml → Install postgres database node → Install postgres database node (always) → repo_management → repo_management | Disable Automation Platform repository | Disable Automation Platform repository | extra_repos is defined |
| 740 | 29.8% | install.yml → Install postgres database node → Install postgres database node (always) → repo_management → repo_management | Disable Automation Platform RHSM repository | Disable Automation Platform RHSM repository | aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 741 | 29.8% | install.yml | Prepare nodes for redis installation | Play targeting `redis` | — |
| 742 | 29.8% | install.yml → Prepare nodes for redis installation | ansible.builtin.include_role | Include and run tasks from an Ansible role (role `redis`, tasks `deprovision`) | _redis_mode == "cluster"; inventory_hostname in groups.get("automationgateway", []) \| union(groups.get("automationedacontroller", [])); not ansible_facts.services.get('redis-platform.service', {}) |
| 743 | 29.9% | install.yml → Prepare nodes for redis installation → redis | role `redis` (`deprovision`) | Enter role task list | _redis_mode == "cluster"; inventory_hostname in groups.get("automationgateway", []) \| union(groups.get("automationedacontroller", [])); not ansible_facts.services.get('redis-platform.service', {}) |
| 744 | 29.9% | install.yml → Prepare nodes for redis installation → redis → redis | file `deprovision.yml` | Task file `ansible/automation_platform_installer/roles/redis/tasks/deprovision.yml` | _redis_mode == "cluster"; inventory_hostname in groups.get("automationgateway", []) \| union(groups.get("automationedacontroller", [])); not ansible_facts.services.get('redis-platform.service', {}) |
| 745 | 30.0% | install.yml → Prepare nodes for redis installation → redis → redis | Remove redis rpm | Remove redis rpm | _redis_mode == "cluster"; inventory_hostname in groups.get("automationgateway", []) \| union(groups.get("automationedacontroller", [])); not ansible_facts.services.get('redis-platform.service', {}) |
| 746 | 30.0% | install.yml → Prepare nodes for redis installation → redis → redis | Remove remaining redis files and directories | Remove remaining redis files and directories | _redis_mode == "cluster"; inventory_hostname in groups.get("automationgateway", []) \| union(groups.get("automationedacontroller", [])); not ansible_facts.services.get('redis-platform.service', {}) |
| 747 | 30.0% | install.yml | Install cluster redis | Play targeting `redis` | — |
| 748 | 30.1% | install.yml → Install cluster redis | block | Begin conditional or grouped task block | _redis_mode == "cluster" |
| 749 | 30.1% | install.yml → Install cluster redis → block | Run repo_management (setup) | Run repo_management (setup) (role `repo_management`, tasks `setup`) | _redis_mode == "cluster" |
| 750 | 30.2% | install.yml → Install cluster redis → block → repo_management | role `repo_management` (`setup`) | Enter role task list | _redis_mode == "cluster" |
| 751 | 30.2% | install.yml → Install cluster redis → block → repo_management → repo_management | file `setup.yml` | Task file `ansible/automation_platform_installer/roles/repo_management/tasks/setup.yml` | _redis_mode == "cluster" |
| 752 | 30.2% | install.yml → Install cluster redis → block → repo_management → repo_management | include_vars | Load variables from a file | _redis_mode == "cluster" |
| 753 | 30.3% | install.yml → Install cluster redis → block → repo_management → repo_management | Install yum repo keys | Install yum repo keys (dest `/etc/pki/rpm-gpg/{{ item }}`) | _redis_mode == "cluster" |
| 754 | 30.3% | install.yml → Install cluster redis → block → repo_management → repo_management | Install subscription-manager-rhsm-certificates | Install subscription-manager-rhsm-certificates | _redis_mode == "cluster" |
| 755 | 30.4% | install.yml → Install cluster redis → block → repo_management → repo_management | Determine if rhsm repository exists | Determine if rhsm repository exists | _redis_mode == "cluster" |
| 756 | 30.4% | install.yml → Install cluster redis → block → repo_management → repo_management | Enable Automation Platform rhsm repository | Enable Automation Platform rhsm repository | _redis_mode == "cluster"; ansible_distribution == 'RedHat'; rhsm_repo.stat.exists; not bundle_install \| bool; aw_repo_url is undefined |
| 757 | 30.4% | install.yml → Install cluster redis → block → repo_management → repo_management | Explain Automation Platform rhsm repository failure | Explain Automation Platform rhsm repository failure | _redis_mode == "cluster"; aap_rhsm_repo_enable.failed \| default(false) |
| 758 | 30.5% | install.yml → Install cluster redis → block → repo_management → repo_management | block | Begin conditional or grouped task block | _redis_mode == "cluster"; aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 759 | 30.5% | install.yml → Install cluster redis → block → repo_management → repo_management → block | Enable module hotfixes flag | Enable module hotfixes flag | _redis_mode == "cluster"; aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 760 | 30.6% | install.yml → Install cluster redis → block → repo_management → repo_management → block | Set AAP repo for update | Set AAP repo for update | _redis_mode == "cluster"; aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 761 | 30.6% | install.yml → Install cluster redis → block → repo_management → repo_management | Ensure rsync is installed | Ensure rsync is installed | _redis_mode == "cluster"; bundle_install \| bool |
| 762 | 30.6% | install.yml → Install cluster redis → block → repo_management → repo_management | Copy bundle packages to repo source directory | Begin conditional or grouped task block | _redis_mode == "cluster"; bundle_install \| bool |
| 763 | 30.7% | install.yml → Install cluster redis → block → repo_management → repo_management → Copy bundle packages to repo source directory | Copy bundle packages to repo source directory | Copy bundle packages to repo source directory | _redis_mode == "cluster"; bundle_install \| bool |
| 764 | 30.7% | install.yml → Install cluster redis → block → repo_management → repo_management | Copy bundle packages to repo source directory (rescue) | Run rescue tasks for block | _redis_mode == "cluster"; bundle_install \| bool |
| 765 | 30.8% | install.yml → Install cluster redis → block → repo_management → repo_management → Copy bundle packages to repo source directory (rescue) | Remove old bundle packages source directory (legacy) | Remove old bundle packages source directory (legacy) | _redis_mode == "cluster"; bundle_install \| bool |
| 766 | 30.8% | install.yml → Install cluster redis → block → repo_management → repo_management → Copy bundle packages to repo source directory (rescue) | Copy bundle packages to repo source directory (legacy) | Copy bundle packages to repo source directory (legacy) (dest `{{ bundle_install_folder }}`) | _redis_mode == "cluster"; bundle_install \| bool |
| 767 | 30.8% | install.yml → Install cluster redis → block → repo_management → repo_management | Install the Automation Platform bundle repository | Install the Automation Platform bundle repository (dest `/etc/yum.repos.d/ansible-automation-platform.repo`) | _redis_mode == "cluster"; bundle_install \| bool |
| 768 | 30.9% | install.yml → Install cluster redis → block → repo_management → repo_management | Remove legacy ansible-tower repository | Remove legacy ansible-tower repository | _redis_mode == "cluster" |
| 769 | 30.9% | install.yml → Install cluster redis → block → repo_management → repo_management | Install the Automation Platform yum repository. | Install the Automation Platform yum repository. (dest `/etc/yum.repos.d/ansible-automation-platform.repo`) | _redis_mode == "cluster"; aw_repo_url is defined; not bundle_install \| bool |
| 770 | 31.0% | install.yml → Install cluster redis → block → repo_management → repo_management | Set repo list for bundle install | Set repo list for bundle install | _redis_mode == "cluster"; bundle_install \| bool |
| 771 | 31.0% | install.yml → Install cluster redis → block → repo_management → repo_management | Set repo list for online install | Set repo list for online install | _redis_mode == "cluster"; yum_repo is succeeded; yum_repo is not skipped |
| 772 | 31.1% | install.yml → Install cluster redis → block → repo_management → repo_management | Enable Ansible Automation Platform repo | Enable Ansible Automation Platform repo | _redis_mode == "cluster"; extra_repos is defined |
| 773 | 31.1% | install.yml → Install cluster redis → block | include_role | Include and run tasks from an Ansible role (role `redis`) | _redis_mode == "cluster" |
| 774 | 31.1% | install.yml → Install cluster redis → block → redis | role `redis` | Enter role task list | _redis_mode == "cluster" |
| 775 | 31.2% | install.yml → Install cluster redis → block → redis → redis | file `main.yml` | Task file `ansible/automation_platform_installer/roles/redis/tasks/main.yml` | _redis_mode == "cluster" |
| 776 | 31.2% | install.yml → Install cluster redis → block → redis → redis | include_tasks | Include a task file from the current role or playbook (file `vars.yml`) | _redis_mode == "cluster" |
| 777 | 31.3% | install.yml → Install cluster redis → block → redis → redis → vars.yml | file `vars.yml` | Task file `ansible/automation_platform_installer/roles/redis/tasks/vars.yml` | _redis_mode == "cluster" |
| 778 | 31.3% | install.yml → Install cluster redis → block → redis → redis → vars.yml | include_vars | Load variables from a file | _redis_mode == "cluster" |
| 779 | 31.3% | install.yml → Install cluster redis → block → redis → redis → vars.yml | include_vars | Load variables from a file | _redis_mode == "cluster"; install_redis_mode == "cluster" |
| 780 | 31.4% | install.yml → Install cluster redis → block → redis → redis | include_tasks | Include a task file from the current role or playbook (file `tasks.yml`) | _redis_mode == "cluster" |
| 781 | 31.4% | install.yml → Install cluster redis → block → redis → redis → tasks.yml | file `tasks.yml` | Task file `ansible/automation_platform_installer/roles/redis/tasks/tasks.yml` | _redis_mode == "cluster" |
| 782 | 31.5% | install.yml → Install cluster redis → block → redis → redis → tasks.yml | Ensure redis package is installed | Ensure redis package is installed | _redis_mode == "cluster" |
| 783 | 31.5% | install.yml → Install cluster redis → block → redis → redis → tasks.yml | Ensure AAP directory exists | Ensure AAP directory exists | _redis_mode == "cluster" |
| 784 | 31.5% | install.yml → Install cluster redis → block → redis → redis → tasks.yml | Ensure redis cert directory exists | Ensure redis cert directory exists | _redis_mode == "cluster" |
| 785 | 31.6% | install.yml → Install cluster redis → block → redis → redis → tasks.yml | Configure TLS certificates for redis | Begin conditional or grouped task block | _redis_mode == "cluster"; not _redis_disable_tls \| bool |
| 786 | 31.6% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → Configure TLS certificates for redis | Copy user provided SSL cert | Begin conditional or grouped task block | _redis_mode == "cluster"; not _redis_disable_tls \| bool; redis_tls_cert \| length |
| 787 | 31.7% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → Configure TLS certificates for redis → Copy user provided SSL cert | Import specified TLS certificate | Import specified TLS certificate | _redis_mode == "cluster"; not _redis_disable_tls \| bool; redis_tls_cert \| length |
| 788 | 31.7% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → Configure TLS certificates for redis → Copy user provided SSL cert | Import specified TLS private key | Import specified TLS private key | _redis_mode == "cluster"; not _redis_disable_tls \| bool; redis_tls_cert \| length |
| 789 | 31.7% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → Configure TLS certificates for redis | Use internal CA | Use internal CA (role `certificate_authority`, tasks `sign_service.yml`) | _redis_mode == "cluster"; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 790 | 31.8% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority | role `certificate_authority` (`sign_service.yml`) | Enter role task list | _redis_mode == "cluster"; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 791 | 31.8% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | file `sign_service.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/sign_service.yml` | _redis_mode == "cluster"; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 792 | 31.9% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | include_tasks | Include a task file from the current role or playbook (file `init.yml`) | _redis_mode == "cluster"; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 793 | 31.9% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → init.yml | file `init.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/init.yml` | _redis_mode == "cluster"; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 794 | 31.9% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → init.yml | Ensure python cryptography libraries are installed | Ensure python cryptography libraries are installed | _redis_mode == "cluster"; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 795 | 32.0% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | Create {{ aap_ca_servicename }} private key | Create {{ aap_ca_servicename }} private key | _redis_mode == "cluster"; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 796 | 32.0% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | set_fact | Set a host fact used by later tasks | _redis_mode == "cluster"; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 797 | 32.1% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | Check if {{ aap_ca_servicename }} public key exists | Check if {{ aap_ca_servicename }} public key exists | _redis_mode == "cluster"; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 798 | 32.1% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | _redis_mode == "cluster"; not _redis_disable_tls \| bool; not redis_tls_cert \| length; certificate_stat.stat.exists \| bool |
| 799 | 32.1% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | Check contents of {{ aap_ca_servicename }} public key | Check contents of {{ aap_ca_servicename }} public key | _redis_mode == "cluster"; not _redis_disable_tls \| bool; not redis_tls_cert \| length; certificate_stat.stat.exists \| bool |
| 800 | 32.2% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | _redis_mode == "cluster"; not _redis_disable_tls \| bool; not redis_tls_cert \| length; certificate_stat.stat.exists \| bool; aap_service_san_records_internal \| union(aap_service_san_records) \| difference(certificate_check.subject_alt_name) \| length > 0 |
| 801 | 32.2% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | _redis_mode == "cluster"; not _redis_disable_tls \| bool; not redis_tls_cert \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 802 | 32.3% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | _redis_mode == "cluster"; not _redis_disable_tls \| bool; not redis_tls_cert \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false); aap_service_is_client |
| 803 | 32.3% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | Create CSR for {{ aap_ca_servicename }} node/s | Create CSR for {{ aap_ca_servicename }} node/s | _redis_mode == "cluster"; not _redis_disable_tls \| bool; not redis_tls_cert \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 804 | 32.3% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | Sign {{ aap_ca_servicename }} public key | Sign {{ aap_ca_servicename }} public key | _redis_mode == "cluster"; not _redis_disable_tls \| bool; not redis_tls_cert \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 805 | 32.4% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | Write public key to {{ aap_ca_servicename }} node/s | Write public key to {{ aap_ca_servicename }} node/s (dest `{{ aap_service_cert_fullpath }}`) | _redis_mode == "cluster"; not _redis_disable_tls \| bool; not redis_tls_cert \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 806 | 32.4% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | Signal restart if certificate pair changes | Signal restart if certificate pair changes | _redis_mode == "cluster"; not _redis_disable_tls \| bool; not redis_tls_cert \| length; (write_key is changed or write_certificate is changed) |
| 807 | 32.5% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → Configure TLS certificates for redis | Trigger redis notifier for internally managed certificate changes | Trigger redis notifier for internally managed certificate changes | _redis_mode == "cluster"; not _redis_disable_tls \| bool; aap_service_restart \| default(false) |
| 808 | 32.5% | install.yml → Install cluster redis → block → redis → redis → tasks.yml | Ensure redis socket directory exists | Ensure redis socket directory exists | _redis_mode == "cluster" |
| 809 | 32.5% | install.yml → Install cluster redis → block → redis → redis → tasks.yml | Ensure Redis socket directory has the correct SELinux context | Ensure Redis socket directory has the correct SELinux context | _redis_mode == "cluster" |
| 810 | 32.6% | install.yml → Install cluster redis → block → redis → redis → tasks.yml | Create tmpfiles.d entry for redis socket directory | Create tmpfiles.d entry for redis socket directory (dest `/etc/tmpfiles.d/redis.conf`) | _redis_mode == "cluster" |
| 811 | 32.6% | install.yml → Install cluster redis → block → redis → redis → tasks.yml | Install redis.conf | Install redis.conf (dest `{{ redis_conf_location }}`) | _redis_mode == "cluster" |
| 812 | 32.7% | install.yml → Install cluster redis → block → redis → redis → tasks.yml | block | Begin conditional or grouped task block | _redis_mode == "cluster"; not redis_use_sockets \| bool |
| 813 | 32.7% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → block | Set gateway redis credentials for clustered | Set gateway redis credentials for clustered | _redis_mode == "cluster"; not redis_use_sockets \| bool |
| 814 | 32.7% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → block | Set Event-Driven Ansible redis credentials | Set Event-Driven Ansible redis credentials | _redis_mode == "cluster"; not redis_use_sockets \| bool; groups.get("automationedacontroller", []) \| length > 0 |
| 815 | 32.8% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → block | Create the redis user ACL file | Create the redis user ACL file | _redis_mode == "cluster"; not redis_use_sockets \| bool |
| 816 | 32.8% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → block | block | Begin conditional or grouped task block | _redis_mode == "cluster"; not redis_use_sockets \| bool; install_redis_mode == "cluster" |
| 817 | 32.9% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → block → block | Set redis hostname | Set redis hostname | _redis_mode == "cluster"; not redis_use_sockets \| bool; install_redis_mode == "cluster" |
| 818 | 32.9% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → block → block | Map redis node hostnames | Map redis node hostnames | _redis_mode == "cluster"; not redis_use_sockets \| bool; install_redis_mode == "cluster" |
| 819 | 32.9% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → block → block | Create redis nodes file | Create redis nodes file | _redis_mode == "cluster"; not redis_use_sockets \| bool; install_redis_mode == "cluster" |
| 820 | 33.0% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → block → block | Install {{ redis_init_name }} service file | Install {{ redis_init_name }} service file (dest `/lib/systemd/system/redis-platform.service`) | _redis_mode == "cluster"; not redis_use_sockets \| bool; install_redis_mode == "cluster" |
| 821 | 33.0% | install.yml → Install cluster redis → block → redis → redis → tasks.yml | start redis and configure to startup automatically | start redis and configure to startup automatically (service `{{ redis_init_name }}`) | _redis_mode == "cluster" |
| 822 | 33.1% | install.yml → Install cluster redis → block | Configure firewall for platform redis | Configure firewall for platform redis (role `firewall`) | _redis_mode == "cluster" |
| 823 | 33.1% | install.yml → Install cluster redis → block → firewall | role `firewall` | Enter role task list | _redis_mode == "cluster" |
| 824 | 33.1% | install.yml → Install cluster redis → block → firewall → firewall | file `main.yml` | Task file `ansible/automation_platform_installer/roles/firewall/tasks/main.yml` | _redis_mode == "cluster" |
| 825 | 33.2% | install.yml → Install cluster redis → block → firewall → firewall | include_tasks | Include a task file from the current role or playbook (file `discover.yml`) | _redis_mode == "cluster" |
| 826 | 33.2% | install.yml → Install cluster redis → block → firewall → firewall → discover.yml | file `discover.yml` | Task file `ansible/automation_platform_installer/roles/firewall/tasks/discover.yml` | _redis_mode == "cluster" |
| 827 | 33.3% | install.yml → Install cluster redis → block → firewall → firewall → discover.yml | Determine if firewalld is running | Determine if firewalld is running | _redis_mode == "cluster" |
| 828 | 33.3% | install.yml → Install cluster redis → block → firewall → firewall → discover.yml | Determine if firewalld is enabled | Determine if firewalld is enabled | _redis_mode == "cluster" |
| 829 | 33.3% | install.yml → Install cluster redis → block → firewall → firewall → discover.yml | Determine if iptables is installed | Determine if iptables is installed | _redis_mode == "cluster" |
| 830 | 33.4% | install.yml → Install cluster redis → block → firewall → firewall → discover.yml | Determine if iptables service is installed | Determine if iptables service is installed | _redis_mode == "cluster" |
| 831 | 33.4% | install.yml → Install cluster redis → block → firewall → firewall → discover.yml | Check to see if there is an iptables config file | Check to see if there is an iptables config file | _redis_mode == "cluster"; iptables_installed.rc == 0 |
| 832 | 33.5% | install.yml → Install cluster redis → block → firewall → firewall → discover.yml | Select firewalld | Select firewalld | _redis_mode == "cluster"; firewalld_running.rc == 0 or systemctl_unit_files.stdout_lines \| select('match','firewalld.service\s+enabled') \| list \| length == 1 |
| 833 | 33.5% | install.yml → Install cluster redis → block → firewall → firewall → discover.yml | Select iptables | Select iptables | _redis_mode == "cluster"; iptables_installed.rc == 0; iptables_exists.stat.exists; iptables_service.stat.exists |
| 834 | 33.5% | install.yml → Install cluster redis → block → firewall → firewall | Include {{ 'no' if component_firewall_type == 'none' else component_firewall_type }} tasks | Include {{ 'no' if component_firewall_type == 'none' else component_firewall_type }} tasks (file `{{ component_firewall_type }}.yml`) | _redis_mode == "cluster"; firewalld_running.rc == 0 or systemctl_unit_files.stdout_lines \| select('match','firewalld.service\s+enabled') \| list \| length == 1 or ( iptables_installed.rc == 0 and iptables_exists.stat.exists and iptables_service.stat.exists ) |
| 835 | 33.6% | install.yml → Install cluster redis → block → firewall → firewall → {{ component_firewall_type }}.yml | {{ component_firewall_type }}.yml | Include task file `{{ component_firewall_type }}.yml` (relative to current role task directory) | _redis_mode == "cluster"; firewalld_running.rc == 0 or systemctl_unit_files.stdout_lines \| select('match','firewalld.service\s+enabled') \| list \| length == 1 or ( iptables_installed.rc == 0 and iptables_exists.stat.exists and iptables_service.stat.exists ) |
| 836 | 33.6% | install.yml → Install cluster redis → block | Force handlers for redis | Force handlers for redis (flush_handlers) | _redis_mode == "cluster" |
| 837 | 33.7% | install.yml → Install cluster redis → block | include_role | Include and run tasks from an Ansible role (role `redis`, tasks `cluster`) | _redis_mode == "cluster"; not _redis_is_clustered |
| 838 | 33.7% | install.yml → Install cluster redis → block → redis | role `redis` (`cluster`) | Enter role task list | _redis_mode == "cluster"; not _redis_is_clustered |
| 839 | 33.7% | install.yml → Install cluster redis → block → redis → redis | file `cluster.yml` | Task file `ansible/automation_platform_installer/roles/redis/tasks/cluster.yml` | _redis_mode == "cluster"; not _redis_is_clustered |
| 840 | 33.8% | install.yml → Install cluster redis → block → redis → redis | Set redis cluster IP address per host | Set redis cluster IP address per host | _redis_mode == "cluster"; not _redis_is_clustered |
| 841 | 33.8% | install.yml → Install cluster redis → block → redis → redis | Create redis ipv4 address list | Create redis ipv4 address list | _redis_mode == "cluster"; not _redis_is_clustered |
| 842 | 33.9% | install.yml → Install cluster redis → block → redis → redis | Create cluster ips list | Create cluster ips list | _redis_mode == "cluster"; not _redis_is_clustered |
| 843 | 33.9% | install.yml → Install cluster redis → block → redis → redis | block | Begin conditional or grouped task block | _redis_mode == "cluster"; not _redis_is_clustered; inventory_hostname == groups["redis"][0] |
| 844 | 34.0% | install.yml → Install cluster redis → block → redis → redis → block | Expand redis-cli options for mTLS if necessary | Expand redis-cli options for mTLS if necessary | _redis_mode == "cluster"; not _redis_is_clustered; inventory_hostname == groups["redis"][0]; not _redis_disable_mtls |
| 845 | 34.0% | install.yml → Install cluster redis → block → redis → redis → block | Create the redis cluster | Create the redis cluster | _redis_mode == "cluster"; not _redis_is_clustered; inventory_hostname == groups["redis"][0] |
| 846 | 34.0% | install.yml → Install cluster redis → block → redis → redis | Create file to track cluster successfully configured | Create file to track cluster successfully configured | _redis_mode == "cluster"; not _redis_is_clustered |
| 847 | 34.1% | install.yml → Install cluster redis | block (always) | Run always tasks for block | _redis_mode == "cluster" |
| 848 | 34.1% | install.yml → Install cluster redis → block (always) | Run repo_management (teardown) | Run repo_management (teardown) (role `repo_management`, tasks `teardown`) | _redis_mode == "cluster"; _redis_mode == "cluster" |
| 849 | 34.2% | install.yml → Install cluster redis → block (always) → repo_management | role `repo_management` (`teardown`) | Enter role task list | _redis_mode == "cluster"; _redis_mode == "cluster" |
| 850 | 34.2% | install.yml → Install cluster redis → block (always) → repo_management → repo_management | file `teardown.yml` | Task file `ansible/automation_platform_installer/roles/repo_management/tasks/teardown.yml` | _redis_mode == "cluster"; _redis_mode == "cluster" |
| 851 | 34.2% | install.yml → Install cluster redis → block (always) → repo_management → repo_management | Disable Automation Platform repository | Disable Automation Platform repository | _redis_mode == "cluster"; _redis_mode == "cluster"; extra_repos is defined |
| 852 | 34.3% | install.yml → Install cluster redis → block (always) → repo_management → repo_management | Disable Automation Platform RHSM repository | Disable Automation Platform RHSM repository | _redis_mode == "cluster"; _redis_mode == "cluster"; aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 853 | 34.3% | install.yml | Install Automation Gateway | Play targeting `automationgateway` | — |
| 854 | 34.4% | install.yml → Install Automation Gateway | block | Begin conditional or grouped task block | — |
| 855 | 34.4% | install.yml → Install Automation Gateway → block | Run repo_management (setup) | Run repo_management (setup) (role `repo_management`, tasks `setup`) | — |
| 856 | 34.4% | install.yml → Install Automation Gateway → block → repo_management | role `repo_management` (`setup`) | Enter role task list | — |
| 857 | 34.5% | install.yml → Install Automation Gateway → block → repo_management → repo_management | file `setup.yml` | Task file `ansible/automation_platform_installer/roles/repo_management/tasks/setup.yml` | — |
| 858 | 34.5% | install.yml → Install Automation Gateway → block → repo_management → repo_management | include_vars | Load variables from a file | — |
| 859 | 34.6% | install.yml → Install Automation Gateway → block → repo_management → repo_management | Install yum repo keys | Install yum repo keys (dest `/etc/pki/rpm-gpg/{{ item }}`) | — |
| 860 | 34.6% | install.yml → Install Automation Gateway → block → repo_management → repo_management | Install subscription-manager-rhsm-certificates | Install subscription-manager-rhsm-certificates | — |
| 861 | 34.6% | install.yml → Install Automation Gateway → block → repo_management → repo_management | Determine if rhsm repository exists | Determine if rhsm repository exists | — |
| 862 | 34.7% | install.yml → Install Automation Gateway → block → repo_management → repo_management | Enable Automation Platform rhsm repository | Enable Automation Platform rhsm repository | ansible_distribution == 'RedHat'; rhsm_repo.stat.exists; not bundle_install \| bool; aw_repo_url is undefined |
| 863 | 34.7% | install.yml → Install Automation Gateway → block → repo_management → repo_management | Explain Automation Platform rhsm repository failure | Explain Automation Platform rhsm repository failure | aap_rhsm_repo_enable.failed \| default(false) |
| 864 | 34.8% | install.yml → Install Automation Gateway → block → repo_management → repo_management | block | Begin conditional or grouped task block | aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 865 | 34.8% | install.yml → Install Automation Gateway → block → repo_management → repo_management → block | Enable module hotfixes flag | Enable module hotfixes flag | aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 866 | 34.8% | install.yml → Install Automation Gateway → block → repo_management → repo_management → block | Set AAP repo for update | Set AAP repo for update | aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 867 | 34.9% | install.yml → Install Automation Gateway → block → repo_management → repo_management | Ensure rsync is installed | Ensure rsync is installed | bundle_install \| bool |
| 868 | 34.9% | install.yml → Install Automation Gateway → block → repo_management → repo_management | Copy bundle packages to repo source directory | Begin conditional or grouped task block | bundle_install \| bool |
| 869 | 35.0% | install.yml → Install Automation Gateway → block → repo_management → repo_management → Copy bundle packages to repo source directory | Copy bundle packages to repo source directory | Copy bundle packages to repo source directory | bundle_install \| bool |
| 870 | 35.0% | install.yml → Install Automation Gateway → block → repo_management → repo_management | Copy bundle packages to repo source directory (rescue) | Run rescue tasks for block | bundle_install \| bool |
| 871 | 35.0% | install.yml → Install Automation Gateway → block → repo_management → repo_management → Copy bundle packages to repo source directory (rescue) | Remove old bundle packages source directory (legacy) | Remove old bundle packages source directory (legacy) | bundle_install \| bool |
| 872 | 35.1% | install.yml → Install Automation Gateway → block → repo_management → repo_management → Copy bundle packages to repo source directory (rescue) | Copy bundle packages to repo source directory (legacy) | Copy bundle packages to repo source directory (legacy) (dest `{{ bundle_install_folder }}`) | bundle_install \| bool |
| 873 | 35.1% | install.yml → Install Automation Gateway → block → repo_management → repo_management | Install the Automation Platform bundle repository | Install the Automation Platform bundle repository (dest `/etc/yum.repos.d/ansible-automation-platform.repo`) | bundle_install \| bool |
| 874 | 35.2% | install.yml → Install Automation Gateway → block → repo_management → repo_management | Remove legacy ansible-tower repository | Remove legacy ansible-tower repository | — |
| 875 | 35.2% | install.yml → Install Automation Gateway → block → repo_management → repo_management | Install the Automation Platform yum repository. | Install the Automation Platform yum repository. (dest `/etc/yum.repos.d/ansible-automation-platform.repo`) | aw_repo_url is defined; not bundle_install \| bool |
| 876 | 35.2% | install.yml → Install Automation Gateway → block → repo_management → repo_management | Set repo list for bundle install | Set repo list for bundle install | bundle_install \| bool |
| 877 | 35.3% | install.yml → Install Automation Gateway → block → repo_management → repo_management | Set repo list for online install | Set repo list for online install | yum_repo is succeeded; yum_repo is not skipped |
| 878 | 35.3% | install.yml → Install Automation Gateway → block → repo_management → repo_management | Enable Ansible Automation Platform repo | Enable Ansible Automation Platform repo | extra_repos is defined |
| 879 | 35.4% | install.yml → Install Automation Gateway → block | block | Begin conditional or grouped task block | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone' |
| 880 | 35.4% | install.yml → Install Automation Gateway → block → block | include_role | Include and run tasks from an Ansible role (role `redis`) | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone' |
| 881 | 35.4% | install.yml → Install Automation Gateway → block → block → redis | role `redis` | Enter role task list | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone' |
| 882 | 35.5% | install.yml → Install Automation Gateway → block → block → redis → redis | file `main.yml` | Task file `ansible/automation_platform_installer/roles/redis/tasks/main.yml` | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone' |
| 883 | 35.5% | install.yml → Install Automation Gateway → block → block → redis → redis | include_tasks | Include a task file from the current role or playbook (file `vars.yml`) | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone' |
| 884 | 35.6% | install.yml → Install Automation Gateway → block → block → redis → redis → vars.yml | file `vars.yml` | Task file `ansible/automation_platform_installer/roles/redis/tasks/vars.yml` | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone' |
| 885 | 35.6% | install.yml → Install Automation Gateway → block → block → redis → redis → vars.yml | include_vars | Load variables from a file | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone' |
| 886 | 35.6% | install.yml → Install Automation Gateway → block → block → redis → redis → vars.yml | include_vars | Load variables from a file | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; install_redis_mode == "cluster" |
| 887 | 35.7% | install.yml → Install Automation Gateway → block → block → redis → redis | include_tasks | Include a task file from the current role or playbook (file `tasks.yml`) | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone' |
| 888 | 35.7% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml | file `tasks.yml` | Task file `ansible/automation_platform_installer/roles/redis/tasks/tasks.yml` | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone' |
| 889 | 35.8% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml | Ensure redis package is installed | Ensure redis package is installed | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone' |
| 890 | 35.8% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml | Ensure AAP directory exists | Ensure AAP directory exists | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone' |
| 891 | 35.8% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml | Ensure redis cert directory exists | Ensure redis cert directory exists | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone' |
| 892 | 35.9% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml | Configure TLS certificates for redis | Begin conditional or grouped task block | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not _redis_disable_tls \| bool |
| 893 | 35.9% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → Configure TLS certificates for redis | Copy user provided SSL cert | Begin conditional or grouped task block | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not _redis_disable_tls \| bool; redis_tls_cert \| length |
| 894 | 36.0% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → Configure TLS certificates for redis → Copy user provided SSL cert | Import specified TLS certificate | Import specified TLS certificate | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not _redis_disable_tls \| bool; redis_tls_cert \| length |
| 895 | 36.0% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → Configure TLS certificates for redis → Copy user provided SSL cert | Import specified TLS private key | Import specified TLS private key | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not _redis_disable_tls \| bool; redis_tls_cert \| length |
| 896 | 36.0% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → Configure TLS certificates for redis | Use internal CA | Use internal CA (role `certificate_authority`, tasks `sign_service.yml`) | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 897 | 36.1% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority | role `certificate_authority` (`sign_service.yml`) | Enter role task list | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 898 | 36.1% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | file `sign_service.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/sign_service.yml` | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 899 | 36.2% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | include_tasks | Include a task file from the current role or playbook (file `init.yml`) | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 900 | 36.2% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → init.yml | file `init.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/init.yml` | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 901 | 36.2% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → init.yml | Ensure python cryptography libraries are installed | Ensure python cryptography libraries are installed | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 902 | 36.3% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | Create {{ aap_ca_servicename }} private key | Create {{ aap_ca_servicename }} private key | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 903 | 36.3% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | set_fact | Set a host fact used by later tasks | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 904 | 36.4% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | Check if {{ aap_ca_servicename }} public key exists | Check if {{ aap_ca_servicename }} public key exists | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 905 | 36.4% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not _redis_disable_tls \| bool; not redis_tls_cert \| length; certificate_stat.stat.exists \| bool |
| 906 | 36.4% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | Check contents of {{ aap_ca_servicename }} public key | Check contents of {{ aap_ca_servicename }} public key | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not _redis_disable_tls \| bool; not redis_tls_cert \| length; certificate_stat.stat.exists \| bool |
| 907 | 36.5% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not _redis_disable_tls \| bool; not redis_tls_cert \| length; certificate_stat.stat.exists \| bool; aap_service_san_records_internal \| union(aap_service_san_records) \| difference(certificate_check.subject_alt_name) \| length > 0 |
| 908 | 36.5% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not _redis_disable_tls \| bool; not redis_tls_cert \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 909 | 36.6% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not _redis_disable_tls \| bool; not redis_tls_cert \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false); aap_service_is_client |
| 910 | 36.6% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | Create CSR for {{ aap_ca_servicename }} node/s | Create CSR for {{ aap_ca_servicename }} node/s | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not _redis_disable_tls \| bool; not redis_tls_cert \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 911 | 36.6% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | Sign {{ aap_ca_servicename }} public key | Sign {{ aap_ca_servicename }} public key | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not _redis_disable_tls \| bool; not redis_tls_cert \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 912 | 36.7% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | Write public key to {{ aap_ca_servicename }} node/s | Write public key to {{ aap_ca_servicename }} node/s (dest `{{ aap_service_cert_fullpath }}`) | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not _redis_disable_tls \| bool; not redis_tls_cert \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 913 | 36.7% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | Signal restart if certificate pair changes | Signal restart if certificate pair changes | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not _redis_disable_tls \| bool; not redis_tls_cert \| length; (write_key is changed or write_certificate is changed) |
| 914 | 36.8% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → Configure TLS certificates for redis | Trigger redis notifier for internally managed certificate changes | Trigger redis notifier for internally managed certificate changes | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not _redis_disable_tls \| bool; aap_service_restart \| default(false) |
| 915 | 36.8% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml | Ensure redis socket directory exists | Ensure redis socket directory exists | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone' |
| 916 | 36.9% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml | Ensure Redis socket directory has the correct SELinux context | Ensure Redis socket directory has the correct SELinux context | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone' |
| 917 | 36.9% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml | Create tmpfiles.d entry for redis socket directory | Create tmpfiles.d entry for redis socket directory (dest `/etc/tmpfiles.d/redis.conf`) | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone' |
| 918 | 36.9% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml | Install redis.conf | Install redis.conf (dest `{{ redis_conf_location }}`) | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone' |
| 919 | 37.0% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml | block | Begin conditional or grouped task block | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not redis_use_sockets \| bool |
| 920 | 37.0% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → block | Set gateway redis credentials for clustered | Set gateway redis credentials for clustered | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not redis_use_sockets \| bool |
| 921 | 37.1% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → block | Set Event-Driven Ansible redis credentials | Set Event-Driven Ansible redis credentials | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not redis_use_sockets \| bool; groups.get("automationedacontroller", []) \| length > 0 |
| 922 | 37.1% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → block | Create the redis user ACL file | Create the redis user ACL file | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not redis_use_sockets \| bool |
| 923 | 37.1% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → block | block | Begin conditional or grouped task block | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not redis_use_sockets \| bool; install_redis_mode == "cluster" |
| 924 | 37.2% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → block → block | Set redis hostname | Set redis hostname | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not redis_use_sockets \| bool; install_redis_mode == "cluster" |
| 925 | 37.2% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → block → block | Map redis node hostnames | Map redis node hostnames | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not redis_use_sockets \| bool; install_redis_mode == "cluster" |
| 926 | 37.3% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → block → block | Create redis nodes file | Create redis nodes file | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not redis_use_sockets \| bool; install_redis_mode == "cluster" |
| 927 | 37.3% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → block → block | Install {{ redis_init_name }} service file | Install {{ redis_init_name }} service file (dest `/lib/systemd/system/redis-platform.service`) | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not redis_use_sockets \| bool; install_redis_mode == "cluster" |
| 928 | 37.3% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml | start redis and configure to startup automatically | start redis and configure to startup automatically (service `{{ redis_init_name }}`) | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone' |
| 929 | 37.4% | install.yml → Install Automation Gateway → block → block | set_fact | Set a host fact used by later tasks | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone' |
| 930 | 37.4% | install.yml → Install Automation Gateway → block → block | Force handlers for redis | Force handlers for redis (flush_handlers) | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone' |
| 931 | 37.5% | install.yml → Install Automation Gateway → block | include_role | Include and run tasks from an Ansible role (role `nginx`) | — |
| 932 | 37.5% | install.yml → Install Automation Gateway → block → nginx | role `nginx` | Enter role task list | — |
| 933 | 37.5% | install.yml → Install Automation Gateway → block → nginx → nginx | file `main.yml` | Task file `ansible/automation_platform_installer/roles/nginx/tasks/main.yml` | — |
| 934 | 37.6% | install.yml → Install Automation Gateway → block → nginx → nginx | include_tasks | Include a task file from the current role or playbook (file `module.yml`) | — |
| 935 | 37.6% | install.yml → Install Automation Gateway → block → nginx → nginx → module.yml | file `module.yml` | Task file `ansible/automation_platform_installer/roles/nginx/tasks/module.yml` | — |
| 936 | 37.7% | install.yml → Install Automation Gateway → block → nginx → nginx → module.yml | Enable nginx module stream | Begin conditional or grouped task block | — |
| 937 | 37.7% | install.yml → Install Automation Gateway → block → nginx → nginx → module.yml → Enable nginx module stream | Disable Nginx module | Disable Nginx module | — |
| 938 | 37.7% | install.yml → Install Automation Gateway → block → nginx → nginx → module.yml → Enable nginx module stream | Enable Nginx module | Enable Nginx module | — |
| 939 | 37.8% | install.yml → Install Automation Gateway → block → nginx → nginx | include_tasks | Include a task file from the current role or playbook (file `tasks.yml`) | — |
| 940 | 37.8% | install.yml → Install Automation Gateway → block → nginx → nginx → tasks.yml | file `tasks.yml` | Task file `ansible/automation_platform_installer/roles/nginx/tasks/tasks.yml` | — |
| 941 | 37.9% | install.yml → Install Automation Gateway → block → nginx → nginx → tasks.yml | Install nginx | Install nginx | — |
| 942 | 37.9% | install.yml → Install Automation Gateway → block → nginx → nginx → tasks.yml | SELinux configuration for nginx | Begin conditional or grouped task block | ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 943 | 37.9% | install.yml → Install Automation Gateway → block → nginx → nginx → tasks.yml → SELinux configuration for nginx | Ensure libsemanage-python is installed | Ensure libsemanage-python is installed | ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 944 | 38.0% | install.yml → Install Automation Gateway → block → nginx → nginx → tasks.yml → SELinux configuration for nginx | Open up permissions on nginx. | Open up permissions on nginx. | ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 945 | 38.0% | install.yml → Install Automation Gateway → block | include_role | Include and run tasks from an Ansible role (role `automationgateway`) | — |
| 946 | 38.1% | install.yml → Install Automation Gateway → block → automationgateway | role `automationgateway` | Enter role task list | — |
| 947 | 38.1% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | file `main.yml` | Task file `ansible/automation_platform_installer/roles/automationgateway/tasks/main.yml` | — |
| 948 | 38.1% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Map redis url to Gateway for non-clustered redis | Map redis url to Gateway for non-clustered redis | _redis_mode != "cluster" |
| 949 | 38.2% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Map redis url to Gateway for clustered redis | Map redis url to Gateway for clustered redis | _redis_mode == "cluster" |
| 950 | 38.2% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Map redis hostnames to Gateway | Map redis hostnames to Gateway | _redis_mode == "cluster" |
| 951 | 38.3% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Check installed version of PG | Check installed version of PG | — |
| 952 | 38.3% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | block | Begin conditional or grouped task block | pg_installed_version.failed or pg_installed_version.stdout \| trim \| int != automationgateway_postgresql_version \| int |
| 953 | 38.3% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → block | Enable PostgreSQL {{ automationgateway_postgresql_version }} dnf modules | Enable PostgreSQL {{ automationgateway_postgresql_version }} dnf modules | pg_installed_version.failed or pg_installed_version.stdout \| trim \| int != automationgateway_postgresql_version \| int |
| 954 | 38.4% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → block | Install PostgreSQL client {{ automationgateway_postgresql_version }} | Install PostgreSQL client {{ automationgateway_postgresql_version }} | pg_installed_version.failed or pg_installed_version.stdout \| trim \| int != automationgateway_postgresql_version \| int |
| 955 | 38.4% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Install automation-gateway | Install automation-gateway | — |
| 956 | 38.5% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Update existing dependent packages | Update existing dependent packages (role `repo_management`, tasks `update_dependency`) | — |
| 957 | 38.5% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → repo_management | role `repo_management` (`update_dependency`) | Enter role task list | — |
| 958 | 38.5% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → repo_management → repo_management | file `update_dependency.yml` | Task file `ansible/automation_platform_installer/roles/repo_management/tasks/update_dependency.yml` | — |
| 959 | 38.6% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → repo_management → repo_management | Ensure AAP repo for update is set | Ensure AAP repo for update is set | not aap_repo_for_update \| default('') \| length |
| 960 | 38.6% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → repo_management → repo_management | Generate a list of AAP packages with an update | Generate a list of AAP packages with an update | — |
| 961 | 38.7% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → repo_management → repo_management | Set AAP packages to update | Set AAP packages to update | — |
| 962 | 38.7% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → repo_management → repo_management | Update packages | Update packages | _packages_to_update \| length |
| 963 | 38.7% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Trigger service restart if package changed | Trigger service restart if package changed | _package_update_result is changed |
| 964 | 38.8% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Apply envoy config | Apply envoy config | — |
| 965 | 38.8% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Modify dependent service list for postgresql | Modify dependent service list for postgresql (role `systemd`, tasks `postgresql`) | — |
| 966 | 38.9% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → systemd | role `systemd` (`postgresql`) | Enter role task list | — |
| 967 | 38.9% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → systemd → systemd | file `postgresql.yml` | Task file `ansible/automation_platform_installer/roles/systemd/tasks/postgresql.yml` | — |
| 968 | 38.9% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → systemd → systemd | block | Begin conditional or grouped task block | _internal_database \| bool |
| 969 | 39.0% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → systemd → systemd → block | Create directory for service configuration override | Create directory for service configuration override | _internal_database \| bool |
| 970 | 39.0% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → systemd → systemd → block | Add postgresql to dependent service list | Add postgresql to dependent service list | _internal_database \| bool |
| 971 | 39.1% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → systemd → systemd | Ensure postgresql isn't set as a dependent service | Ensure postgresql isn't set as a dependent service | not _internal_database \| bool |
| 972 | 39.1% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Make dependent services part of gateway target | Make dependent services part of gateway target (role `systemd`) | — |
| 973 | 39.1% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → systemd | role `systemd` | Enter role task list | — |
| 974 | 39.2% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → systemd → systemd | file `main.yml` | Task file `ansible/automation_platform_installer/roles/systemd/tasks/main.yml` | — |
| 975 | 39.2% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → systemd → systemd | Create directory for dependent service configuration override | Create directory for dependent service configuration override | — |
| 976 | 39.3% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → systemd → systemd | Add dependent service configuration override | Add dependent service configuration override | — |
| 977 | 39.3% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Enable automation-gateway target and service | Enable automation-gateway target and service | — |
| 978 | 39.3% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | include_tasks | Include a task file from the current role or playbook (file `secret_key.yml`) | — |
| 979 | 39.4% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → secret_key.yml | file `secret_key.yml` | Task file `ansible/automation_platform_installer/roles/automationgateway/tasks/secret_key.yml` | — |
| 980 | 39.4% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → secret_key.yml | Check for SECRET_KEY on all nodes | Check for SECRET_KEY on all nodes | — |
| 981 | 39.5% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → secret_key.yml | block | Begin conditional or grouped task block | automationgateway_secret_key_path.stat.exists |
| 982 | 39.5% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → secret_key.yml → block | Get SECRET_KEY if found | Get SECRET_KEY if found | automationgateway_secret_key_path.stat.exists |
| 983 | 39.5% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → secret_key.yml → block | Set SECRET_KEY if found | Set SECRET_KEY if found | automationgateway_secret_key_path.stat.exists; automationgateway_secret_key_slurp['content'] \| default('') \| length |
| 984 | 39.6% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → secret_key.yml | Set SECRET_KEY if not found | Set SECRET_KEY if not found | not automationgateway_secret_key_slurp['content'] \| default('') \| length |
| 985 | 39.6% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → secret_key.yml | Set SECRET_KEY for all nodes | Set SECRET_KEY for all nodes | — |
| 986 | 39.7% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → secret_key.yml | Set SECRET_KEY file | Set SECRET_KEY file (dest `/etc/ansible-automation-platform/gateway/SECRET_KEY`) | — |
| 987 | 39.7% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | include_tasks | Include a task file from the current role or playbook (file `certificates.yml`) | — |
| 988 | 39.8% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml | file `certificates.yml` | Task file `ansible/automation_platform_installer/roles/automationgateway/tasks/certificates.yml` | — |
| 989 | 39.8% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml | Configure postgres client certs | Begin conditional or grouped task block | automationgateway_pg_cert_auth |
| 990 | 39.8% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Configure postgres client certs | Copy postgres client cert | Copy postgres client cert (dest `/etc/ansible-automation-platform/gateway/pg_client.crt`) | automationgateway_pg_cert_auth |
| 991 | 39.9% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Configure postgres client certs | Copy postgres client key | Copy postgres client key (dest `/etc/ansible-automation-platform/gateway/pg_client.key`) | automationgateway_pg_cert_auth |
| 992 | 39.9% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml | block | Begin conditional or grouped task block | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool |
| 993 | 40.0% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → block | Copy user-provided SSL certificates | Begin conditional or grouped task block | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool; automationgateway_ssl_cert \| default('') \| length |
| 994 | 40.0% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → block → Copy user-provided SSL certificates | Copy SSL certificate | Copy SSL certificate (dest `/etc/ansible-automation-platform/gateway/gateway.cert`) | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool; automationgateway_ssl_cert \| default('') \| length |
| 995 | 40.0% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → block → Copy user-provided SSL certificates | Copy SSL private key | Copy SSL private key (dest `/etc/ansible-automation-platform/gateway/gateway.key`) | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool; automationgateway_ssl_cert \| default('') \| length |
| 996 | 40.1% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → block | Use internal CA | Use internal CA (role `certificate_authority`, tasks `sign_service.yml`) | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool; automationgateway_ssl_cert is not defined |
| 997 | 40.1% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → block → certificate_authority | role `certificate_authority` (`sign_service.yml`) | Enter role task list | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool; automationgateway_ssl_cert is not defined |
| 998 | 40.2% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → block → certificate_authority → certificate_authority | file `sign_service.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/sign_service.yml` | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool; automationgateway_ssl_cert is not defined |
| 999 | 40.2% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → block → certificate_authority → certificate_authority | include_tasks | Include a task file from the current role or playbook (file `init.yml`) | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool; automationgateway_ssl_cert is not defined |
| 1000 | 40.2% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → block → certificate_authority → certificate_authority → init.yml | file `init.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/init.yml` | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool; automationgateway_ssl_cert is not defined |
| 1001 | 40.3% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → block → certificate_authority → certificate_authority → init.yml | Ensure python cryptography libraries are installed | Ensure python cryptography libraries are installed | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool; automationgateway_ssl_cert is not defined |
| 1002 | 40.3% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → block → certificate_authority → certificate_authority | Create {{ aap_ca_servicename }} private key | Create {{ aap_ca_servicename }} private key | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool; automationgateway_ssl_cert is not defined |
| 1003 | 40.4% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → block → certificate_authority → certificate_authority | set_fact | Set a host fact used by later tasks | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool; automationgateway_ssl_cert is not defined |
| 1004 | 40.4% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → block → certificate_authority → certificate_authority | Check if {{ aap_ca_servicename }} public key exists | Check if {{ aap_ca_servicename }} public key exists | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool; automationgateway_ssl_cert is not defined |
| 1005 | 40.4% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → block → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool; automationgateway_ssl_cert is not defined; certificate_stat.stat.exists \| bool |
| 1006 | 40.5% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → block → certificate_authority → certificate_authority → block | Check contents of {{ aap_ca_servicename }} public key | Check contents of {{ aap_ca_servicename }} public key | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool; automationgateway_ssl_cert is not defined; certificate_stat.stat.exists \| bool |
| 1007 | 40.5% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → block → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool; automationgateway_ssl_cert is not defined; certificate_stat.stat.exists \| bool; aap_service_san_records_internal \| union(aap_service_san_records) \| difference(certificate_check.subject_alt_name) \| length > 0 |
| 1008 | 40.6% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → block → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool; automationgateway_ssl_cert is not defined; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1009 | 40.6% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → block → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool; automationgateway_ssl_cert is not defined; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false); aap_service_is_client |
| 1010 | 40.6% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → block → certificate_authority → certificate_authority → block | Create CSR for {{ aap_ca_servicename }} node/s | Create CSR for {{ aap_ca_servicename }} node/s | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool; automationgateway_ssl_cert is not defined; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1011 | 40.7% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → block → certificate_authority → certificate_authority → block | Sign {{ aap_ca_servicename }} public key | Sign {{ aap_ca_servicename }} public key | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool; automationgateway_ssl_cert is not defined; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1012 | 40.7% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → block → certificate_authority → certificate_authority → block | Write public key to {{ aap_ca_servicename }} node/s | Write public key to {{ aap_ca_servicename }} node/s (dest `{{ aap_service_cert_fullpath }}`) | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool; automationgateway_ssl_cert is not defined; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1013 | 40.8% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → block → certificate_authority → certificate_authority | Signal restart if certificate pair changes | Signal restart if certificate pair changes | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool; automationgateway_ssl_cert is not defined; (write_key is changed or write_certificate is changed) |
| 1014 | 40.8% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → block | Trigger nginx notifier for internally managed certificate changes | Trigger nginx notifier for internally managed certificate changes | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool; aap_service_restart \| default(false) |
| 1015 | 40.8% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → block | set_fact | Set a host fact used by later tasks | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool |
| 1016 | 40.9% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml | Handle caching client certs when mTLS is enabled for AAP caching | Begin conditional or grouped task block | not _redis_disable_mtls |
| 1017 | 40.9% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching | Copy user-provided cache client certificates | Begin conditional or grouped task block | not _redis_disable_mtls; automationgateway_cache_cert \| default('') \| length |
| 1018 | 41.0% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → Copy user-provided cache client certificates | Copy caching mTLS certificate | Copy caching mTLS certificate (dest `/etc/ansible-automation-platform/gateway/cache.cert`) | not _redis_disable_mtls; automationgateway_cache_cert \| default('') \| length |
| 1019 | 41.0% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → Copy user-provided cache client certificates | Copy caching mTLS private key | Copy caching mTLS private key (dest `/etc/ansible-automation-platform/gateway/cache.key`) | not _redis_disable_mtls; automationgateway_cache_cert \| default('') \| length |
| 1020 | 41.0% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching | Sign caching client certs | Sign caching client certs (role `certificate_authority`, tasks `sign_service.yml`) | not _redis_disable_mtls; automationgateway_cache_cert is not defined |
| 1021 | 41.1% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority | role `certificate_authority` (`sign_service.yml`) | Enter role task list | not _redis_disable_mtls; automationgateway_cache_cert is not defined |
| 1022 | 41.1% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority | file `sign_service.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/sign_service.yml` | not _redis_disable_mtls; automationgateway_cache_cert is not defined |
| 1023 | 41.2% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority | include_tasks | Include a task file from the current role or playbook (file `init.yml`) | not _redis_disable_mtls; automationgateway_cache_cert is not defined |
| 1024 | 41.2% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority → init.yml | file `init.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/init.yml` | not _redis_disable_mtls; automationgateway_cache_cert is not defined |
| 1025 | 41.2% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority → init.yml | Ensure python cryptography libraries are installed | Ensure python cryptography libraries are installed | not _redis_disable_mtls; automationgateway_cache_cert is not defined |
| 1026 | 41.3% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority | Create {{ aap_ca_servicename }} private key | Create {{ aap_ca_servicename }} private key | not _redis_disable_mtls; automationgateway_cache_cert is not defined |
| 1027 | 41.3% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority | set_fact | Set a host fact used by later tasks | not _redis_disable_mtls; automationgateway_cache_cert is not defined |
| 1028 | 41.4% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority | Check if {{ aap_ca_servicename }} public key exists | Check if {{ aap_ca_servicename }} public key exists | not _redis_disable_mtls; automationgateway_cache_cert is not defined |
| 1029 | 41.4% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | not _redis_disable_mtls; automationgateway_cache_cert is not defined; certificate_stat.stat.exists \| bool |
| 1030 | 41.4% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority → block | Check contents of {{ aap_ca_servicename }} public key | Check contents of {{ aap_ca_servicename }} public key | not _redis_disable_mtls; automationgateway_cache_cert is not defined; certificate_stat.stat.exists \| bool |
| 1031 | 41.5% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | not _redis_disable_mtls; automationgateway_cache_cert is not defined; certificate_stat.stat.exists \| bool; aap_service_san_records_internal \| union(aap_service_san_records) \| difference(certificate_check.subject_alt_name) \| length > 0 |
| 1032 | 41.5% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | not _redis_disable_mtls; automationgateway_cache_cert is not defined; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1033 | 41.6% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | not _redis_disable_mtls; automationgateway_cache_cert is not defined; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false); aap_service_is_client |
| 1034 | 41.6% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority → block | Create CSR for {{ aap_ca_servicename }} node/s | Create CSR for {{ aap_ca_servicename }} node/s | not _redis_disable_mtls; automationgateway_cache_cert is not defined; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1035 | 41.6% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority → block | Sign {{ aap_ca_servicename }} public key | Sign {{ aap_ca_servicename }} public key | not _redis_disable_mtls; automationgateway_cache_cert is not defined; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1036 | 41.7% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority → block | Write public key to {{ aap_ca_servicename }} node/s | Write public key to {{ aap_ca_servicename }} node/s (dest `{{ aap_service_cert_fullpath }}`) | not _redis_disable_mtls; automationgateway_cache_cert is not defined; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1037 | 41.7% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority | Signal restart if certificate pair changes | Signal restart if certificate pair changes | not _redis_disable_mtls; automationgateway_cache_cert is not defined; (write_key is changed or write_certificate is changed) |
| 1038 | 41.8% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching | Trigger automation-gateway notifier for internally managed certificate changes | Trigger automation-gateway notifier for internally managed certificate changes | not _redis_disable_mtls; aap_service_restart \| default(false) |
| 1039 | 41.8% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching | set_fact | Set a host fact used by later tasks | not _redis_disable_mtls |
| 1040 | 41.8% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Get stat of gateway log files | Get stat of gateway log files | — |
| 1041 | 41.9% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Rename old gateway log file | Begin conditional or grouped task block | gateway_log_files.results[0].stat.exists == true and gateway_log_files.results[1].stat.exists == false |
| 1042 | 41.9% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → Rename old gateway log file | Copy gateway log file to a new name | Copy gateway log file to a new name | gateway_log_files.results[0].stat.exists == true and gateway_log_files.results[1].stat.exists == false |
| 1043 | 42.0% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → Rename old gateway log file | Remove the old gateway log file | Remove the old gateway log file | gateway_log_files.results[0].stat.exists == true and gateway_log_files.results[1].stat.exists == false |
| 1044 | 42.0% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Apply gateway supervisor config | Apply gateway supervisor config (dest `/etc/supervisord.d/gateway.ini`) | — |
| 1045 | 42.0% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Configure the log directory | Configure the log directory | — |
| 1046 | 42.1% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Ensure gateway log file exists with correct ownership | Ensure gateway log file exists with correct ownership | — |
| 1047 | 42.1% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Apply gateway uwsgi config | Apply gateway uwsgi config (dest `/etc/ansible-automation-platform/gateway/uwsgi.ini`) | — |
| 1048 | 42.2% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Apply gateway nginx config | Apply gateway nginx config (dest `/etc/nginx/conf.d/automation-gateway.conf`) | — |
| 1049 | 42.2% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | set_fact | Set a host fact used by later tasks | not automationgateway_db_template \| default([]) \| length |
| 1050 | 42.2% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Define and check cache directory | Define and check cache directory | — |
| 1051 | 42.3% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Configure Gateway settings file | Configure Gateway settings file (dest `{{ automationgateway_settings_file }}`) | — |
| 1052 | 42.3% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Configure Gateway GRPC settings file | Configure Gateway GRPC settings file (dest `{{ automationgateway_grpc_settings_file }}`) | — |
| 1053 | 42.4% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Push Gateway proxy config file | Push Gateway proxy config file | — |
| 1054 | 42.4% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Migrate the gateway database schema | Migrate the gateway database schema | — |
| 1055 | 42.4% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | include_tasks | Include a task file from the current role or playbook (file `super_user.yml`) | inventory_hostname == groups['automationgateway'][0] |
| 1056 | 42.5% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → super_user.yml | file `super_user.yml` | Task file `ansible/automation_platform_installer/roles/automationgateway/tasks/super_user.yml` | inventory_hostname == groups['automationgateway'][0] |
| 1057 | 42.5% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → super_user.yml | create django super user | create django super user | inventory_hostname == groups['automationgateway'][0] |
| 1058 | 42.6% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → super_user.yml | update django super user password | update django super user password | inventory_hostname == groups['automationgateway'][0]; automationgateway_install_admin_password != '' |
| 1059 | 42.6% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Configure the static directory | Configure the static directory | — |
| 1060 | 42.7% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Collect static | Collect static | — |
| 1061 | 42.7% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Ensure gateway services are started | Ensure gateway services are started | — |
| 1062 | 42.7% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Ensure supervisord service is enabled/started | Ensure supervisord service is enabled/started | — |
| 1063 | 42.8% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | include_role | Include and run tasks from an Ansible role (role `nginx`, tasks `finalize.yml`) | — |
| 1064 | 42.8% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → nginx | role `nginx` (`finalize.yml`) | Enter role task list | — |
| 1065 | 42.9% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → nginx → nginx | file `finalize.yml` | Task file `ansible/automation_platform_installer/roles/nginx/tasks/finalize.yml` | — |
| 1066 | 42.9% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → nginx → nginx | Install nginx.conf | Install nginx.conf (dest `/etc/nginx/nginx.conf`) | — |
| 1067 | 42.9% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → nginx → nginx | Start nginx and configure to startup automatically | Start nginx and configure to startup automatically (service `nginx`) | — |
| 1068 | 43.0% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Flush handlers | Flush handlers (flush_handlers) | — |
| 1069 | 43.0% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Initialize authenticators | Initialize authenticators | — |
| 1070 | 43.1% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | include_tasks | Include a task file from the current role or playbook (file `deregister_service_nodes.yml`) | inventory_hostname == groups['automationgateway'][0] |
| 1071 | 43.1% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → deregister_service_nodes.yml | file `deregister_service_nodes.yml` | Task file `ansible/automation_platform_installer/roles/automationgateway/tasks/deregister_service_nodes.yml` | inventory_hostname == groups['automationgateway'][0] |
| 1072 | 43.1% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → deregister_service_nodes.yml | Get service nodes | Get service nodes | inventory_hostname == groups['automationgateway'][0] |
| 1073 | 43.2% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → deregister_service_nodes.yml | ansible.builtin.set_fact | Run `ansible.builtin.set_fact` | inventory_hostname == groups['automationgateway'][0] |
| 1074 | 43.2% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → deregister_service_nodes.yml | Deregister unused automation platform gateway service nodes | Deregister unused automation platform gateway service nodes | inventory_hostname == groups['automationgateway'][0]; item not in gateway_proxy_config.gateway_service_nodes \| d([]) \| map(attribute='name') \| list |
| 1075 | 43.3% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | include_tasks | Include a task file from the current role or playbook (file `register_services.yml`) | — |
| 1076 | 43.3% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → register_services.yml | file `register_services.yml` | Task file `ansible/automation_platform_installer/roles/automationgateway/tasks/register_services.yml` | — |
| 1077 | 43.3% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → register_services.yml | Register services | Begin conditional or grouped task block | — |
| 1078 | 43.4% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → register_services.yml → Register services | Update automation platform gateway settings | Update automation platform gateway settings | — |
| 1079 | 43.4% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → register_services.yml → Register services | Update automation platform gateway http ports | Update automation platform gateway http ports | — |
| 1080 | 43.5% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → register_services.yml → Register services | Update automation platform gateway service clusters | Update automation platform gateway service clusters | — |
| 1081 | 43.5% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → register_services.yml → Register services | Update automation platform gateway service nodes | Update automation platform gateway service nodes | — |
| 1082 | 43.5% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → register_services.yml → Register services | Update automation platform gateway services | Update automation platform gateway services | — |
| 1083 | 43.6% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → register_services.yml → Register services | Update automation platform gateway routes | Update automation platform gateway routes | — |
| 1084 | 43.6% | install.yml → Install Automation Gateway → block | Configure firewall | Configure firewall (role `firewall`) | — |
| 1085 | 43.7% | install.yml → Install Automation Gateway → block → firewall | role `firewall` | Enter role task list | — |
| 1086 | 43.7% | install.yml → Install Automation Gateway → block → firewall → firewall | file `main.yml` | Task file `ansible/automation_platform_installer/roles/firewall/tasks/main.yml` | — |
| 1087 | 43.7% | install.yml → Install Automation Gateway → block → firewall → firewall | include_tasks | Include a task file from the current role or playbook (file `discover.yml`) | — |
| 1088 | 43.8% | install.yml → Install Automation Gateway → block → firewall → firewall → discover.yml | file `discover.yml` | Task file `ansible/automation_platform_installer/roles/firewall/tasks/discover.yml` | — |
| 1089 | 43.8% | install.yml → Install Automation Gateway → block → firewall → firewall → discover.yml | Determine if firewalld is running | Determine if firewalld is running | — |
| 1090 | 43.9% | install.yml → Install Automation Gateway → block → firewall → firewall → discover.yml | Determine if firewalld is enabled | Determine if firewalld is enabled | — |
| 1091 | 43.9% | install.yml → Install Automation Gateway → block → firewall → firewall → discover.yml | Determine if iptables is installed | Determine if iptables is installed | — |
| 1092 | 43.9% | install.yml → Install Automation Gateway → block → firewall → firewall → discover.yml | Determine if iptables service is installed | Determine if iptables service is installed | — |
| 1093 | 44.0% | install.yml → Install Automation Gateway → block → firewall → firewall → discover.yml | Check to see if there is an iptables config file | Check to see if there is an iptables config file | iptables_installed.rc == 0 |
| 1094 | 44.0% | install.yml → Install Automation Gateway → block → firewall → firewall → discover.yml | Select firewalld | Select firewalld | firewalld_running.rc == 0 or systemctl_unit_files.stdout_lines \| select('match','firewalld.service\s+enabled') \| list \| length == 1 |
| 1095 | 44.1% | install.yml → Install Automation Gateway → block → firewall → firewall → discover.yml | Select iptables | Select iptables | iptables_installed.rc == 0; iptables_exists.stat.exists; iptables_service.stat.exists |
| 1096 | 44.1% | install.yml → Install Automation Gateway → block → firewall → firewall | Include {{ 'no' if component_firewall_type == 'none' else component_firewall_type }} tasks | Include {{ 'no' if component_firewall_type == 'none' else component_firewall_type }} tasks (file `{{ component_firewall_type }}.yml`) | firewalld_running.rc == 0 or systemctl_unit_files.stdout_lines \| select('match','firewalld.service\s+enabled') \| list \| length == 1 or ( iptables_installed.rc == 0 and iptables_exists.stat.exists and iptables_service.stat.exists ) |
| 1097 | 44.1% | install.yml → Install Automation Gateway → block → firewall → firewall → {{ component_firewall_type }}.yml | {{ component_firewall_type }}.yml | Include task file `{{ component_firewall_type }}.yml` (relative to current role task directory) | firewalld_running.rc == 0 or systemctl_unit_files.stdout_lines \| select('match','firewalld.service\s+enabled') \| list \| length == 1 or ( iptables_installed.rc == 0 and iptables_exists.stat.exists and iptables_service.stat.exists ) |
| 1098 | 44.2% | install.yml → Install Automation Gateway | block (always) | Run always tasks for block | — |
| 1099 | 44.2% | install.yml → Install Automation Gateway → block (always) | Run repo_management (teardown) | Run repo_management (teardown) (role `repo_management`, tasks `teardown`) | — |
| 1100 | 44.3% | install.yml → Install Automation Gateway → block (always) → repo_management | role `repo_management` (`teardown`) | Enter role task list | — |
| 1101 | 44.3% | install.yml → Install Automation Gateway → block (always) → repo_management → repo_management | file `teardown.yml` | Task file `ansible/automation_platform_installer/roles/repo_management/tasks/teardown.yml` | — |
| 1102 | 44.3% | install.yml → Install Automation Gateway → block (always) → repo_management → repo_management | Disable Automation Platform repository | Disable Automation Platform repository | extra_repos is defined |
| 1103 | 44.4% | install.yml → Install Automation Gateway → block (always) → repo_management → repo_management | Disable Automation Platform RHSM repository | Disable Automation Platform RHSM repository | aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 1104 | 44.4% | install.yml | Install Automation Controller and Execution nodes | Play targeting `automationcontroller:execution_nodes` | — |
| 1105 | 44.5% | install.yml → Install Automation Controller and Execution nodes | Install Automation Controller and Execution nodes | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1106 | 44.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes | set_fact | Set a host fact used by later tasks | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1107 | 44.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes | Add internal CA certificate to trust store | Add internal CA certificate to trust store (role `certificate_authority`, tasks `add_cacert.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1108 | 44.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → certificate_authority | role `certificate_authority` (`add_cacert.yml`) | Enter role task list | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1109 | 44.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → certificate_authority → certificate_authority | file `add_cacert.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/add_cacert.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1110 | 44.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → certificate_authority → certificate_authority | Ensure ca-certificates is installed | Ensure ca-certificates is installed | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1111 | 44.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → certificate_authority → certificate_authority | Copy custom CA cert | Copy custom CA cert (dest `/etc/pki/ca-trust/source/anchors/ansible-automation-platform-custom-ca-cert.crt`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); custom_ca_cert is defined |
| 1112 | 44.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → certificate_authority → certificate_authority | Place AAP CA certificate | Place AAP CA certificate (dest `/etc/pki/ca-trust/source/anchors/ansible-automation-platform-managed-ca-cert.crt`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1113 | 44.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → certificate_authority → certificate_authority | Update CA trust | Update CA trust | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1114 | 44.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes | Flush handlers | Flush handlers (flush_handlers) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1115 | 44.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes | Run repo_management (setup) | Run repo_management (setup) (role `repo_management`, tasks `setup`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1116 | 44.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → repo_management | role `repo_management` (`setup`) | Enter role task list | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1117 | 44.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → repo_management → repo_management | file `setup.yml` | Task file `ansible/automation_platform_installer/roles/repo_management/tasks/setup.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1118 | 45.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → repo_management → repo_management | include_vars | Load variables from a file | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1119 | 45.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → repo_management → repo_management | Install yum repo keys | Install yum repo keys (dest `/etc/pki/rpm-gpg/{{ item }}`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1120 | 45.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → repo_management → repo_management | Install subscription-manager-rhsm-certificates | Install subscription-manager-rhsm-certificates | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1121 | 45.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → repo_management → repo_management | Determine if rhsm repository exists | Determine if rhsm repository exists | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1122 | 45.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → repo_management → repo_management | Enable Automation Platform rhsm repository | Enable Automation Platform rhsm repository | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); ansible_distribution == 'RedHat'; rhsm_repo.stat.exists; not bundle_install \| bool; aw_repo_url is undefined |
| 1123 | 45.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → repo_management → repo_management | Explain Automation Platform rhsm repository failure | Explain Automation Platform rhsm repository failure | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); aap_rhsm_repo_enable.failed \| default(false) |
| 1124 | 45.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → repo_management → repo_management | block | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 1125 | 45.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → repo_management → repo_management → block | Enable module hotfixes flag | Enable module hotfixes flag | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 1126 | 45.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → repo_management → repo_management → block | Set AAP repo for update | Set AAP repo for update | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 1127 | 45.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → repo_management → repo_management | Ensure rsync is installed | Ensure rsync is installed | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); bundle_install \| bool |
| 1128 | 45.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → repo_management → repo_management | Copy bundle packages to repo source directory | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); bundle_install \| bool |
| 1129 | 45.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → repo_management → repo_management → Copy bundle packages to repo source directory | Copy bundle packages to repo source directory | Copy bundle packages to repo source directory | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); bundle_install \| bool |
| 1130 | 45.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → repo_management → repo_management | Copy bundle packages to repo source directory (rescue) | Run rescue tasks for block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); bundle_install \| bool |
| 1131 | 45.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → repo_management → repo_management → Copy bundle packages to repo source directory (rescue) | Remove old bundle packages source directory (legacy) | Remove old bundle packages source directory (legacy) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); bundle_install \| bool |
| 1132 | 45.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → repo_management → repo_management → Copy bundle packages to repo source directory (rescue) | Copy bundle packages to repo source directory (legacy) | Copy bundle packages to repo source directory (legacy) (dest `{{ bundle_install_folder }}`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); bundle_install \| bool |
| 1133 | 45.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → repo_management → repo_management | Install the Automation Platform bundle repository | Install the Automation Platform bundle repository (dest `/etc/yum.repos.d/ansible-automation-platform.repo`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); bundle_install \| bool |
| 1134 | 45.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → repo_management → repo_management | Remove legacy ansible-tower repository | Remove legacy ansible-tower repository | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1135 | 45.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → repo_management → repo_management | Install the Automation Platform yum repository. | Install the Automation Platform yum repository. (dest `/etc/yum.repos.d/ansible-automation-platform.repo`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); aw_repo_url is defined; not bundle_install \| bool |
| 1136 | 45.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → repo_management → repo_management | Set repo list for bundle install | Set repo list for bundle install | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); bundle_install \| bool |
| 1137 | 45.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → repo_management → repo_management | Set repo list for online install | Set repo list for online install | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); yum_repo is succeeded; yum_repo is not skipped |
| 1138 | 45.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → repo_management → repo_management | Enable Ansible Automation Platform repo | Enable Ansible Automation Platform repo | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); extra_repos is defined |
| 1139 | 45.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes | Automation Controller only tasks | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1140 | 45.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks | include_role | Include and run tasks from an Ansible role (role `redis`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1141 | 45.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis | role `redis` | Enter role task list | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1142 | 46.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis | file `main.yml` | Task file `ansible/automation_platform_installer/roles/redis/tasks/main.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1143 | 46.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis | include_tasks | Include a task file from the current role or playbook (file `vars.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1144 | 46.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → vars.yml | file `vars.yml` | Task file `ansible/automation_platform_installer/roles/redis/tasks/vars.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1145 | 46.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → vars.yml | include_vars | Load variables from a file | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1146 | 46.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → vars.yml | include_vars | Load variables from a file | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; install_redis_mode == "cluster" |
| 1147 | 46.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis | include_tasks | Include a task file from the current role or playbook (file `tasks.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1148 | 46.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml | file `tasks.yml` | Task file `ansible/automation_platform_installer/roles/redis/tasks/tasks.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1149 | 46.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml | Ensure redis package is installed | Ensure redis package is installed | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1150 | 46.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml | Ensure AAP directory exists | Ensure AAP directory exists | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1151 | 46.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml | Ensure redis cert directory exists | Ensure redis cert directory exists | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1152 | 46.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml | Configure TLS certificates for redis | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _redis_disable_tls \| bool |
| 1153 | 46.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → Configure TLS certificates for redis | Copy user provided SSL cert | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _redis_disable_tls \| bool; redis_tls_cert \| length |
| 1154 | 46.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → Configure TLS certificates for redis → Copy user provided SSL cert | Import specified TLS certificate | Import specified TLS certificate | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _redis_disable_tls \| bool; redis_tls_cert \| length |
| 1155 | 46.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → Configure TLS certificates for redis → Copy user provided SSL cert | Import specified TLS private key | Import specified TLS private key | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _redis_disable_tls \| bool; redis_tls_cert \| length |
| 1156 | 46.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → Configure TLS certificates for redis | Use internal CA | Use internal CA (role `certificate_authority`, tasks `sign_service.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 1157 | 46.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority | role `certificate_authority` (`sign_service.yml`) | Enter role task list | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 1158 | 46.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | file `sign_service.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/sign_service.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 1159 | 46.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | include_tasks | Include a task file from the current role or playbook (file `init.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 1160 | 46.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → init.yml | file `init.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/init.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 1161 | 46.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → init.yml | Ensure python cryptography libraries are installed | Ensure python cryptography libraries are installed | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 1162 | 46.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | Create {{ aap_ca_servicename }} private key | Create {{ aap_ca_servicename }} private key | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 1163 | 46.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | set_fact | Set a host fact used by later tasks | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 1164 | 46.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | Check if {{ aap_ca_servicename }} public key exists | Check if {{ aap_ca_servicename }} public key exists | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 1165 | 46.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _redis_disable_tls \| bool; not redis_tls_cert \| length; certificate_stat.stat.exists \| bool |
| 1166 | 46.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | Check contents of {{ aap_ca_servicename }} public key | Check contents of {{ aap_ca_servicename }} public key | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _redis_disable_tls \| bool; not redis_tls_cert \| length; certificate_stat.stat.exists \| bool |
| 1167 | 47.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _redis_disable_tls \| bool; not redis_tls_cert \| length; certificate_stat.stat.exists \| bool; aap_service_san_records_internal \| union(aap_service_san_records) \| difference(certificate_check.subject_alt_name) \| length > 0 |
| 1168 | 47.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _redis_disable_tls \| bool; not redis_tls_cert \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1169 | 47.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _redis_disable_tls \| bool; not redis_tls_cert \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false); aap_service_is_client |
| 1170 | 47.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | Create CSR for {{ aap_ca_servicename }} node/s | Create CSR for {{ aap_ca_servicename }} node/s | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _redis_disable_tls \| bool; not redis_tls_cert \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1171 | 47.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | Sign {{ aap_ca_servicename }} public key | Sign {{ aap_ca_servicename }} public key | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _redis_disable_tls \| bool; not redis_tls_cert \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1172 | 47.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | Write public key to {{ aap_ca_servicename }} node/s | Write public key to {{ aap_ca_servicename }} node/s (dest `{{ aap_service_cert_fullpath }}`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _redis_disable_tls \| bool; not redis_tls_cert \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1173 | 47.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | Signal restart if certificate pair changes | Signal restart if certificate pair changes | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _redis_disable_tls \| bool; not redis_tls_cert \| length; (write_key is changed or write_certificate is changed) |
| 1174 | 47.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → Configure TLS certificates for redis | Trigger redis notifier for internally managed certificate changes | Trigger redis notifier for internally managed certificate changes | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _redis_disable_tls \| bool; aap_service_restart \| default(false) |
| 1175 | 47.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml | Ensure redis socket directory exists | Ensure redis socket directory exists | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1176 | 47.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml | Ensure Redis socket directory has the correct SELinux context | Ensure Redis socket directory has the correct SELinux context | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1177 | 47.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml | Create tmpfiles.d entry for redis socket directory | Create tmpfiles.d entry for redis socket directory (dest `/etc/tmpfiles.d/redis.conf`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1178 | 47.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml | Install redis.conf | Install redis.conf (dest `{{ redis_conf_location }}`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1179 | 47.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml | block | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not redis_use_sockets \| bool |
| 1180 | 47.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → block | Set gateway redis credentials for clustered | Set gateway redis credentials for clustered | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not redis_use_sockets \| bool |
| 1181 | 47.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → block | Set Event-Driven Ansible redis credentials | Set Event-Driven Ansible redis credentials | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not redis_use_sockets \| bool; groups.get("automationedacontroller", []) \| length > 0 |
| 1182 | 47.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → block | Create the redis user ACL file | Create the redis user ACL file | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not redis_use_sockets \| bool |
| 1183 | 47.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → block | block | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not redis_use_sockets \| bool; install_redis_mode == "cluster" |
| 1184 | 47.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → block → block | Set redis hostname | Set redis hostname | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not redis_use_sockets \| bool; install_redis_mode == "cluster" |
| 1185 | 47.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → block → block | Map redis node hostnames | Map redis node hostnames | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not redis_use_sockets \| bool; install_redis_mode == "cluster" |
| 1186 | 47.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → block → block | Create redis nodes file | Create redis nodes file | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not redis_use_sockets \| bool; install_redis_mode == "cluster" |
| 1187 | 47.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → block → block | Install {{ redis_init_name }} service file | Install {{ redis_init_name }} service file (dest `/lib/systemd/system/redis-platform.service`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not redis_use_sockets \| bool; install_redis_mode == "cluster" |
| 1188 | 47.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml | start redis and configure to startup automatically | start redis and configure to startup automatically (service `{{ redis_init_name }}`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1189 | 47.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks | include_role | Include and run tasks from an Ansible role (role `nginx`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1190 | 47.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → nginx | role `nginx` | Enter role task list | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1191 | 47.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → nginx → nginx | file `main.yml` | Task file `ansible/automation_platform_installer/roles/nginx/tasks/main.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1192 | 48.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → nginx → nginx | include_tasks | Include a task file from the current role or playbook (file `module.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1193 | 48.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → nginx → nginx → module.yml | file `module.yml` | Task file `ansible/automation_platform_installer/roles/nginx/tasks/module.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1194 | 48.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → nginx → nginx → module.yml | Enable nginx module stream | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1195 | 48.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → nginx → nginx → module.yml → Enable nginx module stream | Disable Nginx module | Disable Nginx module | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1196 | 48.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → nginx → nginx → module.yml → Enable nginx module stream | Enable Nginx module | Enable Nginx module | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1197 | 48.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → nginx → nginx | include_tasks | Include a task file from the current role or playbook (file `tasks.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1198 | 48.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → nginx → nginx → tasks.yml | file `tasks.yml` | Task file `ansible/automation_platform_installer/roles/nginx/tasks/tasks.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1199 | 48.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → nginx → nginx → tasks.yml | Install nginx | Install nginx | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1200 | 48.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → nginx → nginx → tasks.yml | SELinux configuration for nginx | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 1201 | 48.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → nginx → nginx → tasks.yml → SELinux configuration for nginx | Ensure libsemanage-python is installed | Ensure libsemanage-python is installed | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 1202 | 48.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → nginx → nginx → tasks.yml → SELinux configuration for nginx | Open up permissions on nginx. | Open up permissions on nginx. | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 1203 | 48.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks | Run packages_el | Run packages_el (role `packages_el`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1204 | 48.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el | role `packages_el` | Enter role task list | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1205 | 48.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el | file `main.yml` | Task file `ansible/automation_platform_installer/roles/packages_el/tasks/main.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1206 | 48.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el | include packages_el variables | include packages_el variables | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1207 | 48.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el | include_tasks | Include a task file from the current role or playbook (file `install_deps.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1208 | 48.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_deps.yml | file `install_deps.yml` | Task file `ansible/automation_platform_installer/roles/packages_el/tasks/install_deps.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1209 | 48.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_deps.yml | Install playbook dependencies | Install playbook dependencies | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1210 | 48.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el | include_tasks | Include a task file from the current role or playbook (file `upgrade_postgres_client.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_tower \| bool |
| 1211 | 48.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → upgrade_postgres_client.yml | file `upgrade_postgres_client.yml` | Task file `ansible/automation_platform_installer/roles/packages_el/tasks/upgrade_postgres_client.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_tower \| bool |
| 1212 | 48.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → upgrade_postgres_client.yml | Uninstall existing PostgreSQL Client | Uninstall existing PostgreSQL Client | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_tower \| bool |
| 1213 | 48.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → upgrade_postgres_client.yml | Enable PostgreSQL {{ automationcontroller_postgresql_version }} dnf modules | Enable PostgreSQL {{ automationcontroller_postgresql_version }} dnf modules | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_tower \| bool |
| 1214 | 48.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → upgrade_postgres_client.yml | Install PostgreSQL Client | Install PostgreSQL Client | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_tower \| bool |
| 1215 | 48.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el | include_tasks | Include a task file from the current role or playbook (file `install_controller.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_tower \| bool |
| 1216 | 48.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_controller.yml | file `install_controller.yml` | Task file `ansible/automation_platform_installer/roles/packages_el/tasks/install_controller.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_tower \| bool |
| 1217 | 49.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_controller.yml | Clean Yum Cache for the Automation Platform Repo | Clean Yum Cache for the Automation Platform Repo | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_tower \| bool |
| 1218 | 49.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_controller.yml | Install the Automation Controller RPM. | Install the Automation Controller RPM. | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_tower \| bool |
| 1219 | 49.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_controller.yml | Create is_aap_24 file if AAP is on version 2.4 | Create is_aap_24 file if AAP is on version 2.4 | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_tower \| bool; groups['automationcontroller'] \| map('extract', hostvars, '_aap_version_on_host') \| min is version('2.4', '==') |
| 1220 | 49.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_controller.yml | Install fapolicyd rules for Automation Controller if applicable | Install fapolicyd rules for Automation Controller if applicable | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_tower \| bool; 'fapolicyd.service' in ansible_facts.services |
| 1221 | 49.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_controller.yml | Remove stale packages | Remove stale packages | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_tower \| bool |
| 1222 | 49.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_controller.yml | Update existing dependent packages | Update existing dependent packages (role `repo_management`, tasks `update_dependency`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_tower \| bool |
| 1223 | 49.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_controller.yml → repo_management | role `repo_management` (`update_dependency`) | Enter role task list | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_tower \| bool |
| 1224 | 49.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_controller.yml → repo_management → repo_management | file `update_dependency.yml` | Task file `ansible/automation_platform_installer/roles/repo_management/tasks/update_dependency.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_tower \| bool |
| 1225 | 49.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_controller.yml → repo_management → repo_management | Ensure AAP repo for update is set | Ensure AAP repo for update is set | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_tower \| bool; not aap_repo_for_update \| default('') \| length |
| 1226 | 49.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_controller.yml → repo_management → repo_management | Generate a list of AAP packages with an update | Generate a list of AAP packages with an update | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_tower \| bool |
| 1227 | 49.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_controller.yml → repo_management → repo_management | Set AAP packages to update | Set AAP packages to update | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_tower \| bool |
| 1228 | 49.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_controller.yml → repo_management → repo_management | Update packages | Update packages | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_tower \| bool; _packages_to_update \| length |
| 1229 | 49.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_controller.yml | Re-install aap-metrics-utility | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_tower \| bool; _controller_rpm_install is changed |
| 1230 | 49.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_controller.yml → Re-install aap-metrics-utility | Verify aap-metrics-utility package | Verify aap-metrics-utility package | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_tower \| bool; _controller_rpm_install is changed |
| 1231 | 49.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_controller.yml → Re-install aap-metrics-utility | Re-install aap-metrics-utility | Re-install aap-metrics-utility | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_tower \| bool; _controller_rpm_install is changed; _metrics_utility_rpm_verify is failed |
| 1232 | 49.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el | Uninstall nginx-all-modules | Uninstall nginx-all-modules | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1233 | 49.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el | include_tasks | Include a task file from the current role or playbook (file `install_postgres.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool |
| 1234 | 49.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml | file `install_postgres.yml` | Task file `ansible/automation_platform_installer/roles/packages_el/tasks/install_postgres.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool |
| 1235 | 49.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml | Check for old PostgreSQL data | Check for old PostgreSQL data | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool |
| 1236 | 49.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml | Read PG_VERSION file contents | Read PG_VERSION file contents | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; old_pg_version_file.stat.exists |
| 1237 | 49.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml | Check if PostgreSQL has already been upgraded to 15 | Check if PostgreSQL has already been upgraded to 15 | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool |
| 1238 | 49.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml | Check if PostgreSQL has already been upgraded to 13 | Check if PostgreSQL has already been upgraded to 13 | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool |
| 1239 | 49.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml | Check if PostgreSQL has already been upgraded to 12 | Check if PostgreSQL has already been upgraded to 12 | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool |
| 1240 | 49.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml | Determine if PostgreSQL 10 is present | Determine if PostgreSQL 10 is present | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool |
| 1241 | 49.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml | Determine if PostgreSQL 12 is present | Determine if PostgreSQL 12 is present | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool |
| 1242 | 50.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml | Determine if we should upgrade PostgreSQL | Determine if we should upgrade PostgreSQL | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool |
| 1243 | 50.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml | block | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; not upgrade_postgres \| bool; not upgrade_from_postgres_12 \| bool; not upgrade_from_postgres_10 \| bool |
| 1244 | 50.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → block | Enable PostgreSQL {{ automationcontroller_postgresql_version }} dnf modules | Enable PostgreSQL {{ automationcontroller_postgresql_version }} dnf modules | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; not upgrade_postgres \| bool; not upgrade_from_postgres_12 \| bool; not upgrade_from_postgres_10 \| bool |
| 1245 | 50.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → block | Install PostgreSQL | Install PostgreSQL | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; not upgrade_postgres \| bool; not upgrade_from_postgres_12 \| bool; not upgrade_from_postgres_10 \| bool |
| 1246 | 50.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml | Upgrade PostgreSQL data from 13 --> 15 | Upgrade PostgreSQL data from 13 --> 15 (file `upgrade_postgres_from_13.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 1247 | 50.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml | file `upgrade_postgres_from_13.yml` | Task file `ansible/automation_platform_installer/roles/packages_el/tasks/upgrade_postgres_from_13.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 1248 | 50.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml | Stop PostgreSQL 13 | Stop PostgreSQL 13 (service `{{ pg_systemd_name }}`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 1249 | 50.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml | Reset PostgreSQL module | Reset PostgreSQL module | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 1250 | 50.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml | Enable PostgreSQL 15 dnf modules | Enable PostgreSQL 15 dnf modules | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 1251 | 50.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml | Install PostgreSQL 15 | Install PostgreSQL 15 | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 1252 | 50.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml | Upgrade to PostgreSQL 15 | Upgrade to PostgreSQL 15 | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 1253 | 50.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml | Uninstall postgresql-upgrade package | Uninstall postgresql-upgrade package | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 1254 | 50.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml | include_tasks | Include a task file from the current role or playbook (file `install_deps.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 1255 | 50.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml → install_deps.yml | file `install_deps.yml` | Task file `ansible/automation_platform_installer/roles/packages_el/tasks/install_deps.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 1256 | 50.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml → install_deps.yml | Install playbook dependencies | Install playbook dependencies | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 1257 | 50.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml | Create file to signify PostgreSQL has been upgraded | Create file to signify PostgreSQL has been upgraded | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 1258 | 50.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks | meta | Ansible meta task (flush handlers, clear facts, etc.) (flush_handlers) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1259 | 50.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks | include_role | Include and run tasks from an Ansible role (role `rsyslog`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1260 | 50.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → rsyslog | role `rsyslog` | Enter role task list | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1261 | 50.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → rsyslog → rsyslog | file `main.yml` | Task file `ansible/automation_platform_installer/roles/rsyslog/tasks/main.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1262 | 50.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → rsyslog → rsyslog | Configure the Rsyslog socket directory. | Configure the Rsyslog socket directory. | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1263 | 50.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → rsyslog → rsyslog | Create tmpfiles.d entry for rsyslog socket directory | Create tmpfiles.d entry for rsyslog socket directory (dest `/etc/tmpfiles.d/rsyslog.conf`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1264 | 50.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks | include_role | Include and run tasks from an Ansible role (role `automationcontroller`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1265 | 50.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller | role `automationcontroller` | Enter role task list | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1266 | 50.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller | file `main.yml` | Task file `ansible/automation_platform_installer/roles/automationcontroller/tasks/main.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1267 | 51.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller | include_tasks | Include a task file from the current role or playbook (file `tasks.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1268 | 51.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | file `tasks.yml` | Task file `ansible/automation_platform_installer/roles/automationcontroller/tasks/tasks.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1269 | 51.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | add awx to the redis group so it can access the socket | add awx to the redis group so it can access the socket | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1270 | 51.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Configure the settings directory. | Configure the settings directory. | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1271 | 51.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Configure the socket directory. | Configure the socket directory. | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1272 | 51.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Configure the socket directory for tmpfiles.d | Configure the socket directory for tmpfiles.d (dest `/etc/tmpfiles.d/tower.conf`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1273 | 51.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Configure the system user's home directory. | Configure the system user's home directory. | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1274 | 51.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Configure the system user's .ssh directory. | Configure the system user's .ssh directory. | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1275 | 51.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Configure the projects directory. | Configure the projects directory. | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1276 | 51.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Configure the job output directory. | Configure the job output directory. | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1277 | 51.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Configure the static directory. | Configure the static directory. | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1278 | 51.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Add `conf.d` directory. | Add `conf.d` directory. | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1279 | 51.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Configure the log directory. | Configure the log directory. | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1280 | 51.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Update awx_supervisord logrotate config file | Update awx_supervisord logrotate config file (dest `/etc/logrotate.d/awx_supervisord`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1281 | 51.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Update hourly cron logrotate invocation | Update hourly cron logrotate invocation (dest `/etc/cron.hourly/logrotate`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1282 | 51.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Update automation-controller-service configuration | Update automation-controller-service configuration (dest `/etc/sysconfig/automation-controller`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1283 | 51.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Update automation-controller service configuration | Update automation-controller service configuration (dest `/etc/systemd/system/automation-controller.service`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1284 | 51.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Update automation-controller service configuration (postgresql) | Update automation-controller service configuration (postgresql) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1285 | 51.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Update dependent service configuration | Update dependent service configuration | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1286 | 51.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Update dependent service configuration | Update dependent service configuration (dest `/etc/systemd/system/{{ item }}.service.d/override.conf`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1287 | 51.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Reload systemd configuration | Reload systemd configuration | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1288 | 51.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Enable the automation-controller service | Enable the automation-controller service (service `automation-controller`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1289 | 51.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Check if this is a fresh installation | Check if this is a fresh installation | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1290 | 51.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Replace the secret key if secret_key_override is defined and installation is new | Replace the secret key if secret_key_override is defined and installation is new (dest `/etc/tower/SECRET_KEY`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; inventory_hostname == groups['automationcontroller'][0]; secret_key_override is defined; previous_install_status.stat.exists == false |
| 1291 | 52.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Fetch the secret key from the primary node. | Fetch the secret key from the primary node. | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; inventory_hostname == groups['automationcontroller'][0] |
| 1292 | 52.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Fail play when grabbing SECRET_KEY fails | Fail play when grabbing SECRET_KEY fails | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; hostvars[groups['automationcontroller'][0]]['secret_key'].content is not defined |
| 1293 | 52.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Place the secret key on secondary nodes. | Place the secret key on secondary nodes. (dest `/etc/tower/SECRET_KEY`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; inventory_hostname != groups['automationcontroller'][0] |
| 1294 | 52.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | block | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not automationcontroller_db_template \| default([]) \| length |
| 1295 | 52.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml → block | Get database settings | Get database settings | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not automationcontroller_db_template \| default([]) \| length |
| 1296 | 52.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml → block | Ingest database settings | Ingest database settings | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not automationcontroller_db_template \| default([]) \| length; results.rc == 0 |
| 1297 | 52.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml → block | Set database vars | Set database vars | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not automationcontroller_db_template \| default([]) \| length; results.rc != 0 |
| 1298 | 52.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml → block | Set database vars | Set database vars | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not automationcontroller_db_template \| default([]) \| length; results.rc == 0 |
| 1299 | 52.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml → block | Set database template using default password | Set database template using default password | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not automationcontroller_db_template \| default([]) \| length |
| 1300 | 52.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Configure postgres client certs | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; awx_pg_cert_auth |
| 1301 | 52.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml → Configure postgres client certs | Copy postgres client cert | Copy postgres client cert (dest `/etc/tower/pg_client.crt`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; awx_pg_cert_auth |
| 1302 | 52.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml → Configure postgres client certs | Copy postgres client key | Copy postgres client key (dest `/etc/tower/pg_client.key`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; awx_pg_cert_auth |
| 1303 | 52.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Configure database settings. | Configure database settings. (dest `/etc/tower/conf.d/postgres.py`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1304 | 52.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Handles /etc/tower/conf.d/channels.py | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; inventory_hostname == groups['automationcontroller'][0] |
| 1305 | 52.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml → Handles /etc/tower/conf.d/channels.py | Check for existance of /etc/tower/conf.d/channels.py | Check for existance of /etc/tower/conf.d/channels.py | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; inventory_hostname == groups['automationcontroller'][0] |
| 1306 | 52.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml → Handles /etc/tower/conf.d/channels.py | Generate broadcast websocket secret when /etc/tower/conf.d/channels.py is not present | Generate broadcast websocket secret when /etc/tower/conf.d/channels.py is not present | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; inventory_hostname == groups['automationcontroller'][0]; not channels_py.stat.exists |
| 1307 | 52.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml → Handles /etc/tower/conf.d/channels.py | Deploy /etc/tower/conf.d/channels.py | Deploy /etc/tower/conf.d/channels.py (dest `/etc/tower/conf.d/channels.py`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; inventory_hostname == groups['automationcontroller'][0]; channels_py.stat.exists == false and broadcast_websocket_secret is defined |
| 1308 | 52.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml → Handles /etc/tower/conf.d/channels.py | Slurp /etc/tower/conf.d/channels.py | Slurp /etc/tower/conf.d/channels.py | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; inventory_hostname == groups['automationcontroller'][0] |
| 1309 | 52.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Sync /etc/tower/conf.d/channels.py to all '[automationcontroller]' nodes | Sync /etc/tower/conf.d/channels.py to all '[automationcontroller]' nodes (dest `/etc/tower/conf.d/channels.py`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; groups['automationcontroller'] \| default([]) \| length; hostvars[groups['automationcontroller'][0]]['channels_py_override'] \| default([]) \| length; inventory_hostname in groups['automationcontroller']; inventory_hostname != groups['automationcontroller'][0] |
| 1310 | 52.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Configure the Insights URL | Configure the Insights URL (dest `/etc/tower/conf.d/insights.py`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1311 | 52.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Configure Subscription usage model | Configure Subscription usage model (dest `/etc/tower/conf.d/subscription_usage_model.py`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1312 | 52.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Initialize instance groups for host | Initialize instance groups for host | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1313 | 52.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Determine instance groups for host | Determine instance groups for host | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; item is match("(^automationcontroller\|^instance_group_.+)$") |
| 1314 | 52.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Configure CLUSTER_HOST_ID setting | Configure CLUSTER_HOST_ID setting (dest `/etc/tower/conf.d/cluster_host_id.py`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1315 | 52.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Remove memcached settings | Remove memcached settings | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1316 | 53.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Install settings file if it does not already exist. | Install settings file if it does not already exist. (dest `/etc/tower/settings.py`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1317 | 53.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Configure callback receiver workers | Configure callback receiver workers (dest `/etc/tower/conf.d/callback_receiver_workers.py`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; ansible_processor_vcpus > 4 |
| 1318 | 53.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Generate gateway secret | Generate gateway secret (role `automationgateway`, tasks `service_token`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1319 | 53.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml → automationgateway | role `automationgateway` (`service_token`) | Enter role task list | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1320 | 53.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml → automationgateway → automationgateway | file `service_token.yml` | Task file `ansible/automation_platform_installer/roles/automationgateway/tasks/service_token.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1321 | 53.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml → automationgateway → automationgateway | Generate service secret | Generate service secret | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1322 | 53.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Configure automation gateway | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; groups.get('automationgateway', []) \| length > 0 |
| 1323 | 53.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml → Configure automation gateway | template | Render a Jinja2 template to a file on the host (dest `/etc/tower/conf.d/gateway.py`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; groups.get('automationgateway', []) \| length > 0 |
| 1324 | 53.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Check for pending migrations | Check for pending migrations | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; inventory_hostname == groups['automationcontroller'][0] |
| 1325 | 53.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Handle the DB migration | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; hostvars[groups['automationcontroller'][0]]['pending_db_migration'] is defined; hostvars[groups['automationcontroller'][0]]['pending_db_migration'].stdout\|int != 0 |
| 1326 | 53.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml → Handle the DB migration | Stop supervisor due to database schema upgrade | Stop supervisor due to database schema upgrade (service `supervisord`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; hostvars[groups['automationcontroller'][0]]['pending_db_migration'] is defined; hostvars[groups['automationcontroller'][0]]['pending_db_migration'].stdout\|int != 0 |
| 1327 | 53.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml → Handle the DB migration | Wait for supervisor to stop. | Wait for supervisor to stop. | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; hostvars[groups['automationcontroller'][0]]['pending_db_migration'] is defined; hostvars[groups['automationcontroller'][0]]['pending_db_migration'].stdout\|int != 0 |
| 1328 | 53.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml → Handle the DB migration | Migrate the database schema (may take awhile when upgrading). | Migrate the database schema (may take awhile when upgrading). | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; hostvars[groups['automationcontroller'][0]]['pending_db_migration'] is defined; hostvars[groups['automationcontroller'][0]]['pending_db_migration'].stdout\|int != 0; inventory_hostname == groups['automationcontroller'][0] |
| 1329 | 53.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Handle the DB migration (rescue) | Run rescue tasks for block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; hostvars[groups['automationcontroller'][0]]['pending_db_migration'] is defined; hostvars[groups['automationcontroller'][0]]['pending_db_migration'].stdout\|int != 0 |
| 1330 | 53.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml → Handle the DB migration (rescue) | Database Migration failed, please check the suggest KCS | Database Migration failed, please check the suggest KCS | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; hostvars[groups['automationcontroller'][0]]['pending_db_migration'] is defined; hostvars[groups['automationcontroller'][0]]['pending_db_migration'].stdout\|int != 0; tower_migrate is defined; inventory_hostname == groups['automationcontroller'][0]; 'cryptography.fernet.InvalidToken' in tower_migrate.stderr |
| 1331 | 53.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Checking for RBAC migration log | Checking for RBAC migration log | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1332 | 53.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Moving RBAC migration log | Moving RBAC migration log | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; rbac_log.stat.exists |
| 1333 | 53.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Get the controller version | Get the controller version | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1334 | 53.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Store the controller version | Store the controller version (dest `/var/lib/awx/.tower_version`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1335 | 53.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Get configuration files to set permissions on | Get configuration files to set permissions on | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1336 | 53.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Clean up any poorly-permissioned configuration files on upgrade | Clean up any poorly-permissioned configuration files on upgrade | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1337 | 53.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Set ACL rule for awx user to access rhsm.conf | Set ACL rule for awx user to access rhsm.conf | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1338 | 53.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Check for super users | Check for super users | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; inventory_hostname == groups['automationcontroller'][0] |
| 1339 | 53.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | create django super user if it does not exist | create django super user if it does not exist | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; inventory_hostname == groups['automationcontroller'][0] and super_check.rc > 0 |
| 1340 | 53.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | update django super user password | update django super user password | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; awx_install_admin_password != '' and inventory_hostname == groups['automationcontroller'][0] |
| 1341 | 54.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Collect static files. | Collect static files. | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1342 | 54.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Fixup log directory permissions post-migration | Fixup log directory permissions post-migration | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1343 | 54.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Copy Candlepin RHSM Certificate | Copy Candlepin RHSM Certificate (dest `/etc/tower/candlepin-redhat-ca.crt`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1344 | 54.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller | include_tasks | Include a task file from the current role or playbook (file `nginx.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1345 | 54.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml | file `nginx.yml` | Task file `ansible/automation_platform_installer/roles/automationcontroller/tasks/nginx.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1346 | 54.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml | Ensure awx is in the nginx group | Ensure awx is in the nginx group | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1347 | 54.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml | Get static root | Get static root | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1348 | 54.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml | Find custom error pages | Find custom error pages | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1349 | 54.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml | Set custom error pages | Set custom error pages | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1350 | 54.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml | Get base directory | Get base directory | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1351 | 54.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml | Copy redirect page to index.html | Copy redirect page to index.html | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1352 | 54.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml | Copy logo file | Copy logo file | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1353 | 54.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml | block | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool |
| 1354 | 54.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml → block | Copy user-provided SSL certificates, if we have them | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool; web_server_ssl_cert is defined |
| 1355 | 54.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml → block → Copy user-provided SSL certificates, if we have them | Copy SSL certificate | Copy SSL certificate (dest `/etc/tower/tower.cert`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool; web_server_ssl_cert is defined |
| 1356 | 54.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml → block → Copy user-provided SSL certificates, if we have them | Copy SSL private key | Copy SSL private key (dest `/etc/tower/tower.key`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool; web_server_ssl_cert is defined |
| 1357 | 54.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml → block | Use internal CA | Use internal CA (role `certificate_authority`, tasks `sign_service.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool; web_server_ssl_cert is not defined |
| 1358 | 54.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml → block → certificate_authority | role `certificate_authority` (`sign_service.yml`) | Enter role task list | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool; web_server_ssl_cert is not defined |
| 1359 | 54.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml → block → certificate_authority → certificate_authority | file `sign_service.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/sign_service.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool; web_server_ssl_cert is not defined |
| 1360 | 54.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml → block → certificate_authority → certificate_authority | include_tasks | Include a task file from the current role or playbook (file `init.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool; web_server_ssl_cert is not defined |
| 1361 | 54.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml → block → certificate_authority → certificate_authority → init.yml | file `init.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/init.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool; web_server_ssl_cert is not defined |
| 1362 | 54.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml → block → certificate_authority → certificate_authority → init.yml | Ensure python cryptography libraries are installed | Ensure python cryptography libraries are installed | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool; web_server_ssl_cert is not defined |
| 1363 | 54.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml → block → certificate_authority → certificate_authority | Create {{ aap_ca_servicename }} private key | Create {{ aap_ca_servicename }} private key | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool; web_server_ssl_cert is not defined |
| 1364 | 54.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml → block → certificate_authority → certificate_authority | set_fact | Set a host fact used by later tasks | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool; web_server_ssl_cert is not defined |
| 1365 | 54.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml → block → certificate_authority → certificate_authority | Check if {{ aap_ca_servicename }} public key exists | Check if {{ aap_ca_servicename }} public key exists | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool; web_server_ssl_cert is not defined |
| 1366 | 55.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml → block → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool; web_server_ssl_cert is not defined; certificate_stat.stat.exists \| bool |
| 1367 | 55.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml → block → certificate_authority → certificate_authority → block | Check contents of {{ aap_ca_servicename }} public key | Check contents of {{ aap_ca_servicename }} public key | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool; web_server_ssl_cert is not defined; certificate_stat.stat.exists \| bool |
| 1368 | 55.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml → block → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool; web_server_ssl_cert is not defined; certificate_stat.stat.exists \| bool; aap_service_san_records_internal \| union(aap_service_san_records) \| difference(certificate_check.subject_alt_name) \| length > 0 |
| 1369 | 55.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml → block → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool; web_server_ssl_cert is not defined; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1370 | 55.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml → block → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool; web_server_ssl_cert is not defined; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false); aap_service_is_client |
| 1371 | 55.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml → block → certificate_authority → certificate_authority → block | Create CSR for {{ aap_ca_servicename }} node/s | Create CSR for {{ aap_ca_servicename }} node/s | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool; web_server_ssl_cert is not defined; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1372 | 55.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml → block → certificate_authority → certificate_authority → block | Sign {{ aap_ca_servicename }} public key | Sign {{ aap_ca_servicename }} public key | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool; web_server_ssl_cert is not defined; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1373 | 55.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml → block → certificate_authority → certificate_authority → block | Write public key to {{ aap_ca_servicename }} node/s | Write public key to {{ aap_ca_servicename }} node/s (dest `{{ aap_service_cert_fullpath }}`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool; web_server_ssl_cert is not defined; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1374 | 55.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml → block → certificate_authority → certificate_authority | Signal restart if certificate pair changes | Signal restart if certificate pair changes | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool; web_server_ssl_cert is not defined; (write_key is changed or write_certificate is changed) |
| 1375 | 55.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml → block | Trigger nginx notifier for internally managed certificate changes | Trigger nginx notifier for internally managed certificate changes | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool; aap_service_webserver_restart \| default(False) |
| 1376 | 55.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml → block | set_fact | Set a host fact used by later tasks | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool |
| 1377 | 55.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml | Add automation controller nginx configuration | Add automation controller nginx configuration | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1378 | 55.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks | include_role | Include and run tasks from an Ansible role (role `supervisor`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1379 | 55.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → supervisor | role `supervisor` | Enter role task list | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1380 | 55.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → supervisor → supervisor | file `main.yml` | Task file `ansible/automation_platform_installer/roles/supervisor/tasks/main.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1381 | 55.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → supervisor → supervisor | include_tasks | Include a task file from the current role or playbook (file `vars.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1382 | 55.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → supervisor → supervisor → vars.yml | file `vars.yml` | Task file `ansible/automation_platform_installer/roles/supervisor/tasks/vars.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1383 | 55.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → supervisor → supervisor → vars.yml | include supervisor variables | include supervisor variables | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1384 | 55.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → supervisor → supervisor | include_tasks | Include a task file from the current role or playbook (file `tasks.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not supervisor_exec_vars_only \| bool |
| 1385 | 55.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → supervisor → supervisor → tasks.yml | file `tasks.yml` | Task file `ansible/automation_platform_installer/roles/supervisor/tasks/tasks.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not supervisor_exec_vars_only \| bool |
| 1386 | 55.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → supervisor → supervisor → tasks.yml | check current somaxconn value | check current somaxconn value | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not supervisor_exec_vars_only \| bool |
| 1387 | 55.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → supervisor → supervisor → tasks.yml | check current net.ipv4.tcp_max_syn_backlog value | check current net.ipv4.tcp_max_syn_backlog value | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not supervisor_exec_vars_only \| bool |
| 1388 | 55.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → supervisor → supervisor → tasks.yml | Set somaxconn to value greater than or equal to uwsgi_listen_queue_size in supervisor config | Set somaxconn to value greater than or equal to uwsgi_listen_queue_size in supervisor config | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not supervisor_exec_vars_only \| bool; (uwsgi_listen_queue_size\|int) > (somaxconn['content'] \| b64decode \| int) |
| 1389 | 55.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → supervisor → supervisor → tasks.yml | Set tcp_max_syn_backlog to value greater than or equal to uwsgi_listen_queue_size in supervisor config | Set tcp_max_syn_backlog to value greater than or equal to uwsgi_listen_queue_size in supervisor config | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not supervisor_exec_vars_only \| bool; (uwsgi_listen_queue_size\|int) > (tcp_max_syn_backlog['content'] \| b64decode \| int) |
| 1390 | 55.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → supervisor → supervisor → tasks.yml | install uwsgi config | install uwsgi config (dest `/etc/tower/uwsgi.ini`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not supervisor_exec_vars_only \| bool |
| 1391 | 56.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → supervisor → supervisor → tasks.yml | Enforce libffi shared memory | Enforce libffi shared memory | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not supervisor_exec_vars_only \| bool |
| 1392 | 56.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → supervisor → supervisor → tasks.yml | install supervisor config | install supervisor config (dest `/etc/supervisord.d/tower.ini`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not supervisor_exec_vars_only \| bool |
| 1393 | 56.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → supervisor → supervisor → tasks.yml | Override supervisord logrotate rules | Override supervisord logrotate rules (dest `/etc/logrotate.d/supervisor`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not supervisor_exec_vars_only \| bool |
| 1394 | 56.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → supervisor → supervisor → tasks.yml | Recreate systemd tmp files | Recreate systemd tmp files | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not supervisor_exec_vars_only \| bool |
| 1395 | 56.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → supervisor → supervisor → tasks.yml | start/enable supervisord process | start/enable supervisord process (service `supervisord`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not supervisor_exec_vars_only \| bool |
| 1396 | 56.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → supervisor → supervisor → tasks.yml | Wait for the supervisor socket | Wait for the supervisor socket | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not supervisor_exec_vars_only \| bool |
| 1397 | 56.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks | include_role | Include and run tasks from an Ansible role (role `nginx`, tasks `finalize.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1398 | 56.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → nginx | role `nginx` (`finalize.yml`) | Enter role task list | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1399 | 56.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → nginx → nginx | file `finalize.yml` | Task file `ansible/automation_platform_installer/roles/nginx/tasks/finalize.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1400 | 56.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → nginx → nginx | Install nginx.conf | Install nginx.conf (dest `/etc/nginx/nginx.conf`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1401 | 56.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → nginx → nginx | Start nginx and configure to startup automatically | Start nginx and configure to startup automatically (service `nginx`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1402 | 56.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks | include_role | Include and run tasks from an Ansible role (role `misc`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1403 | 56.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → misc | role `misc` | Enter role task list | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1404 | 56.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → misc → misc | file `main.yml` | Task file `ansible/automation_platform_installer/roles/misc/tasks/main.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1405 | 56.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → misc → misc | Remove the old celeryd.py config file (if it exists) | Remove the old celeryd.py config file (if it exists) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1406 | 56.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → misc → misc | Check if /etc/tower/conf.d/ha.py exists | Check if /etc/tower/conf.d/ha.py exists | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1407 | 56.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → misc → misc | If Ansible does not provide a system UUID, get a random one. | If Ansible does not provide a system UUID, get a random one. | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; ha_py.failed \| bool |
| 1408 | 56.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → misc → misc | Register the uuid from the command line | Register the uuid from the command line | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; ha_py.failed \| bool and generated_uuid is defined |
| 1409 | 56.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → misc → misc | Read the uuid from /etc/tower/conf.d/ha.py | Read the uuid from /etc/tower/conf.d/ha.py | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not ha_py.failed \| bool |
| 1410 | 56.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → misc → misc | Store uuid from /etc/tower/conf.d/ha.py | Store uuid from /etc/tower/conf.d/ha.py | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not ha_py.failed \| bool; ha_py_content is defined |
| 1411 | 56.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → misc → misc | Write a ha.conf file to /etc/tower that contains the system UUID. | Write a ha.conf file to /etc/tower that contains the system UUID. (dest `/etc/tower/conf.d/ha.py`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; ha_py.failed \| bool |
| 1412 | 56.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → misc → misc | Check if tower queue is present | Check if tower queue is present | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1413 | 56.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → misc → misc | Unregister tower queue | Unregister tower queue | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; '[tower capacity=' in register_tower.stdout |
| 1414 | 56.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes | Ensure ansible-automation-platform-common exists on Execution Nodes | Ensure ansible-automation-platform-common exists on Execution Nodes | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['execution_nodes'] \| default([]) |
| 1415 | 56.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes | include_role | Include and run tasks from an Ansible role (role `receptor`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1416 | 57.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor | role `receptor` | Enter role task list | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1417 | 57.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | file `main.yml` | Task file `ansible/automation_platform_installer/roles/receptor/tasks/main.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1418 | 57.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Assert Receptor Network Mesh data | Assert Receptor Network Mesh data (file `preflight.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined |
| 1419 | 57.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml | file `preflight.yml` | Task file `ansible/automation_platform_installer/roles/receptor/tasks/preflight.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined |
| 1420 | 57.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml | Include calculate_mesh | Include calculate_mesh (file `calculate_mesh.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined |
| 1421 | 57.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml → calculate_mesh.yml | file `calculate_mesh.yml` | Task file `ansible/automation_platform_installer/roles/receptor/tasks/calculate_mesh.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined |
| 1422 | 57.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml → calculate_mesh.yml | Calculate Mesh Topology | Calculate Mesh Topology | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined |
| 1423 | 57.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml → calculate_mesh.yml | Review Mesh dot graph topology | Review Mesh dot graph topology | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined; generate_dot_file \| default([]) \| length |
| 1424 | 57.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml → calculate_mesh.yml | Store Receptor Mesh metadata | Store Receptor Mesh metadata | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined |
| 1425 | 57.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml → calculate_mesh.yml | Store Receptor Deprovision Mesh metadata | Store Receptor Deprovision Mesh metadata | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined |
| 1426 | 57.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml → calculate_mesh.yml | Define receptor mesh facts | Define receptor mesh facts | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined; mesh is defined; mesh.keys() \| default([]) \| length; inventory_hostname in mesh.keys() |
| 1427 | 57.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml → calculate_mesh.yml | Define receptor deprovision mesh facts | Define receptor deprovision mesh facts | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined; deprovision_mesh is defined; deprovision_mesh.keys() \| default([]) \| length; inventory_hostname in deprovision_mesh.keys() |
| 1428 | 57.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml | Include DNS asserts | Include DNS asserts (file `assert_dns.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined |
| 1429 | 57.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml → assert_dns.yml | file `assert_dns.yml` | Task file `ansible/automation_platform_installer/roles/receptor/tasks/assert_dns.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined |
| 1430 | 57.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml → assert_dns.yml | Validate if receptor peer name is DNS resolvable | Validate if receptor peer name is DNS resolvable | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined; item \| default([]) \| length; hostvars[item]['node_state'] == 'active'; hostvars[item]['receptor_host_identifier'] \| default([]) \| length; hostvars[item]['receptor_host_identifier'] \| regex_search(regexp_ip) == '' or hostvars[item]['receptor_host_identifier'] \| regex_search(regexp_ip) == None |
| 1431 | 57.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml → assert_dns.yml | Report error when a receptor node cannot resolve a connected peer name | Report error when a receptor node cannot resolve a connected peer name | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined; _getent.failed \| default(False) \| bool; item.rc != 0 |
| 1432 | 57.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml | Include SSL asserts | Include SSL asserts (file `assert_ssl.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined |
| 1433 | 57.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml → assert_ssl.yml | file `assert_ssl.yml` | Task file `ansible/automation_platform_installer/roles/receptor/tasks/assert_ssl.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined |
| 1434 | 57.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml → assert_ssl.yml | Ensure custom SSL Certificate Authority files were provided | Ensure custom SSL Certificate Authority files were provided | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined; mesh_ca_certfile is defined or mesh_ca_keyfile is defined |
| 1435 | 57.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml → assert_ssl.yml | Ensure custom SSL Certificate Authority files matches | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined; mesh_ca_certfile is defined; mesh_ca_keyfile is defined |
| 1436 | 57.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml → assert_ssl.yml → Ensure custom SSL Certificate Authority files matches | Verify custom node SSL private key | Verify custom node SSL private key | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined; mesh_ca_certfile is defined; mesh_ca_keyfile is defined |
| 1437 | 57.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml → assert_ssl.yml → Ensure custom SSL Certificate Authority files matches | Verify custom node SSL x509 key | Verify custom node SSL x509 key | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined; mesh_ca_certfile is defined; mesh_ca_keyfile is defined |
| 1438 | 57.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml → assert_ssl.yml → Ensure custom SSL Certificate Authority files matches | Ensure custom SSL pair matches | Ensure custom SSL pair matches | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined; mesh_ca_certfile is defined; mesh_ca_keyfile is defined |
| 1439 | 57.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml → assert_ssl.yml | Ensure custom node SSL files are valid when provided | Ensure custom node SSL files are valid when provided | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined; mesh_node_certfile is defined or mesh_node_keyfile is defined |
| 1440 | 58.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml → assert_ssl.yml | Ensure custom node SSL files matches | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined; mesh_node_certfile is defined; mesh_node_keyfile is defined |
| 1441 | 58.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml → assert_ssl.yml → Ensure custom node SSL files matches | Verify custom node SSL private key | Verify custom node SSL private key | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined; mesh_node_certfile is defined; mesh_node_keyfile is defined |
| 1442 | 58.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml → assert_ssl.yml → Ensure custom node SSL files matches | Verify custom node SSL x509 key | Verify custom node SSL x509 key | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined; mesh_node_certfile is defined; mesh_node_keyfile is defined |
| 1443 | 58.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml → assert_ssl.yml → Ensure custom node SSL files matches | Ensure custom SSL pair matches | Ensure custom SSL pair matches | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined; mesh_node_certfile is defined; mesh_node_keyfile is defined |
| 1444 | 58.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml → assert_ssl.yml → Ensure custom node SSL files matches | Ensure x509 certificate was signed by the expected Certificate Authority | Ensure x509 certificate was signed by the expected Certificate Authority | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined; mesh_node_certfile is defined; mesh_node_keyfile is defined |
| 1445 | 58.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Install receptor | Install receptor | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1446 | 58.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Install receptor dependencies | Install receptor dependencies | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_workable_dependencies is defined; node_type in receptor_workable_types |
| 1447 | 58.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Install ansible-runner for execution plane | Install ansible-runner for execution plane | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type == "execution" |
| 1448 | 58.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Update existing dependent packages | Update existing dependent packages (role `repo_management`, tasks `update_dependency`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1449 | 58.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → repo_management | role `repo_management` (`update_dependency`) | Enter role task list | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1450 | 58.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → repo_management → repo_management | file `update_dependency.yml` | Task file `ansible/automation_platform_installer/roles/repo_management/tasks/update_dependency.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1451 | 58.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → repo_management → repo_management | Ensure AAP repo for update is set | Ensure AAP repo for update is set | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); not aap_repo_for_update \| default('') \| length |
| 1452 | 58.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → repo_management → repo_management | Generate a list of AAP packages with an update | Generate a list of AAP packages with an update | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1453 | 58.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → repo_management → repo_management | Set AAP packages to update | Set AAP packages to update | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1454 | 58.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → repo_management → repo_management | Update packages | Update packages | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); _packages_to_update \| length |
| 1455 | 58.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Invoke Receptor TLS | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool |
| 1456 | 58.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS | Reconfigure Receptor CA (forced) | Reconfigure Receptor CA (forced) (dest `{{ receptor_tls_dir }}`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; (receptor_tls_reconfigure \| bool) or (mesh_ca_certfile \| default('') \| length) or (mesh_ca_keyfile \| default('') \| length) |
| 1457 | 58.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS | Create Receptor CA directory | Create Receptor CA directory (dest `{{ receptor_tls_ca_dir }}`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool |
| 1458 | 58.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS | Configure Receptor Certificate Authority | Configure Receptor Certificate Authority (file `tls_ca.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; groups['automationcontroller'] \| default([]) \| length; inventory_hostname == groups['automationcontroller'][0] |
| 1459 | 58.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca.yml | file `tls_ca.yml` | Task file `ansible/automation_platform_installer/roles/receptor/tasks/tls_ca.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; groups['automationcontroller'] \| default([]) \| length; inventory_hostname == groups['automationcontroller'][0] |
| 1460 | 58.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca.yml | Copy custom SSL Certificate Authority files | Copy custom SSL Certificate Authority files (dest `{{ item.dest }}`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; groups['automationcontroller'] \| default([]) \| length; inventory_hostname == groups['automationcontroller'][0]; mesh_ca_certfile \| default('') \| length; mesh_ca_keyfile \| default('') \| length |
| 1461 | 58.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca.yml | Verify if Receptor Mesh CA exists | Verify if Receptor Mesh CA exists | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; groups['automationcontroller'] \| default([]) \| length; inventory_hostname == groups['automationcontroller'][0] |
| 1462 | 58.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca.yml | Create Receptor Mesh CA | Create Receptor Mesh CA | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; groups['automationcontroller'] \| default([]) \| length; inventory_hostname == groups['automationcontroller'][0]; receptor_ca_exists is failed |
| 1463 | 58.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca.yml | Set permissions for the Receptor Mesh CA files | Set permissions for the Receptor Mesh CA files (dest `{{ item }}`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; groups['automationcontroller'] \| default([]) \| length; inventory_hostname == groups['automationcontroller'][0] |
| 1464 | 58.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca.yml | Gather Receptor Mesh CA Metadata | Gather Receptor Mesh CA Metadata | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; groups['automationcontroller'] \| default([]) \| length; inventory_hostname == groups['automationcontroller'][0] |
| 1465 | 59.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca.yml | Store Receptor Mesh CA Expiry Date as a Fact | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; groups['automationcontroller'] \| default([]) \| length; inventory_hostname == groups['automationcontroller'][0] |
| 1466 | 59.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca.yml → Store Receptor Mesh CA Expiry Date as a Fact | set_fact | Set a host fact used by later tasks | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; groups['automationcontroller'] \| default([]) \| length; inventory_hostname == groups['automationcontroller'][0] |
| 1467 | 59.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca.yml → Store Receptor Mesh CA Expiry Date as a Fact | set_fact | Set a host fact used by later tasks | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; groups['automationcontroller'] \| default([]) \| length; inventory_hostname == groups['automationcontroller'][0] |
| 1468 | 59.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca.yml → Store Receptor Mesh CA Expiry Date as a Fact | set_fact | Set a host fact used by later tasks | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; groups['automationcontroller'] \| default([]) \| length; inventory_hostname == groups['automationcontroller'][0] |
| 1469 | 59.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca.yml → Store Receptor Mesh CA Expiry Date as a Fact | set_fact | Set a host fact used by later tasks | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; groups['automationcontroller'] \| default([]) \| length; inventory_hostname == groups['automationcontroller'][0] |
| 1470 | 59.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca.yml → Store Receptor Mesh CA Expiry Date as a Fact | set_fact | Set a host fact used by later tasks | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; groups['automationcontroller'] \| default([]) \| length; inventory_hostname == groups['automationcontroller'][0] |
| 1471 | 59.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca.yml | Re-check if Receptor Mesh CA files are present | Re-check if Receptor Mesh CA files are present | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; groups['automationcontroller'] \| default([]) \| length; inventory_hostname == groups['automationcontroller'][0] |
| 1472 | 59.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca.yml | Assert if any Receptor Mesh CA file is missing | Assert if any Receptor Mesh CA file is missing | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; groups['automationcontroller'] \| default([]) \| length; inventory_hostname == groups['automationcontroller'][0] |
| 1473 | 59.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca.yml | Slurp Receptor Mesh CA files | Slurp Receptor Mesh CA files | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; groups['automationcontroller'] \| default([]) \| length; inventory_hostname == groups['automationcontroller'][0] |
| 1474 | 59.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS | Generate Receptor Mesh Nodes certificates | Generate Receptor Mesh Nodes certificates (file `tls_nodes.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool |
| 1475 | 59.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_nodes.yml | file `tls_nodes.yml` | Task file `ansible/automation_platform_installer/roles/receptor/tasks/tls_nodes.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool |
| 1476 | 59.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_nodes.yml | Copy custom node SSL files | Copy custom node SSL files (dest `{{ item.dest }}`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; mesh_node_certfile \| default('') \| length; mesh_node_keyfile \| default('') \| length |
| 1477 | 59.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_nodes.yml | Verify if Mesh Nodes certificate exists | Verify if Mesh Nodes certificate exists | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool |
| 1478 | 59.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_nodes.yml | Obtain all ipv4_addresses from node | Obtain all ipv4_addresses from node | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool |
| 1479 | 59.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_nodes.yml | Validate if receptor_host_identifier is an ipaddress | Validate if receptor_host_identifier is an ipaddress | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool |
| 1480 | 59.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_nodes.yml | Include the iPAddress x509 extension on the receptor certificate when receptor_host_identifier is an IP address and is not assigned to any NIC at the node | Include the iPAddress x509 extension on the receptor certificate when receptor_host_identifier is an IP address and is not assigned to any NIC at the node | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; _dnsname_valid_ipadddr != None and _dnsname_valid_ipadddr \| length > 0; receptor_host_identifier not in receptor_ipaddress_cmd |
| 1481 | 59.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_nodes.yml | Adds dnsname option to the receptor certificate | Adds dnsname option to the receptor certificate | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; _dnsname_valid_ipadddr == '' or _dnsname_valid_ipadddr == None |
| 1482 | 59.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_nodes.yml | Create Mesh node certificate | Create Mesh node certificate | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; _mesh_node_crt is failed |
| 1483 | 59.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_nodes.yml | Adjust Mesh Node private key permission | Adjust Mesh Node private key permission (dest `{{ receptor_tls_keyfile }}`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool |
| 1484 | 59.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_nodes.yml | Slurp Mesh Node CSR | Slurp Mesh Node CSR | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; receptor_tls_csr_pending is changed |
| 1485 | 59.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_nodes.yml | Transfer Receptor Mesh Node CSR to be signed | Transfer Receptor Mesh Node CSR to be signed (dest `{{ hostvars[inventory_hostname]['receptor_tls_csr']['source'] }}`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; receptor_tls_csr \| default([]) \| length; receptor_tls_csr_pending is changed |
| 1486 | 59.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS | Sign Receptor Mesh Node Certificates | Sign Receptor Mesh Node Certificates (file `tls_ca_sign.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool |
| 1487 | 59.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca_sign.yml | file `tls_ca_sign.yml` | Task file `ansible/automation_platform_installer/roles/receptor/tasks/tls_ca_sign.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool |
| 1488 | 59.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca_sign.yml | Receptor Mesh CA pending CSR(s) | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; receptor_tls_csr \| default([]) \| length; receptor_tls_csr_pending is changed |
| 1489 | 59.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca_sign.yml → Receptor Mesh CA pending CSR(s) | Sign Receptor Mesh node certificate | Sign Receptor Mesh node certificate | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; receptor_tls_csr \| default([]) \| length; receptor_tls_csr_pending is changed |
| 1490 | 60.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca_sign.yml → Receptor Mesh CA pending CSR(s) | Slurp Receptor Mesh CA certificates | Slurp Receptor Mesh CA certificates | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; receptor_tls_csr \| default([]) \| length; receptor_tls_csr_pending is changed |
| 1491 | 60.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca_sign.yml → Receptor Mesh CA pending CSR(s) | Copy Receptor Mesh certificate to final node | Copy Receptor Mesh certificate to final node (dest `{{ mesh_ca_nodes_data['source'] }}`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; receptor_tls_csr \| default([]) \| length; receptor_tls_csr_pending is changed; mesh_ca_nodes_data \| default([]) \| length |
| 1492 | 60.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca_sign.yml → Receptor Mesh CA pending CSR(s) | Cleanup signed Receptor Mesh Certificates | Cleanup signed Receptor Mesh Certificates (dest `{{ mesh_ca_nodes_data['source'] }}`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; receptor_tls_csr \| default([]) \| length; receptor_tls_csr_pending is changed; inventory_hostname != groups['automationcontroller'][0] |
| 1493 | 60.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca_sign.yml → Receptor Mesh CA pending CSR(s) | Gather Certificate Sign Requests files to be removed | Gather Certificate Sign Requests files to be removed | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; receptor_tls_csr \| default([]) \| length; receptor_tls_csr_pending is changed |
| 1494 | 60.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca_sign.yml → Receptor Mesh CA pending CSR(s) | Remove directory content | Remove directory content | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; receptor_tls_csr \| default([]) \| length; receptor_tls_csr_pending is changed |
| 1495 | 60.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca_sign.yml | Distribute Receptor Mesh CA to automationcontroller nodes | Distribute Receptor Mesh CA to automationcontroller nodes (dest `{{ item['source'] }}`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; hostvars[groups['automationcontroller'][0]]['mesh_ca_data']['results'] \| default([]) \| length; groups['automationcontroller'] \| default([]) \| length; inventory_hostname in groups['automationcontroller']; inventory_hostname != groups['automationcontroller'][0] |
| 1496 | 60.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca_sign.yml | Distribute Receptor Mesh CA certificate to all execution_nodes | Distribute Receptor Mesh CA certificate to all execution_nodes (dest `{{ item['source'] }}`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; hostvars[groups['automationcontroller'][0]]['mesh_ca_data']['results'] \| default([]) \| length; groups['execution_nodes'] \| default([]) \| length; inventory_hostname in groups['execution_nodes']; item['source'] == receptor_ca_mesh_certfile |
| 1497 | 60.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Create awx group | Create awx group | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1498 | 60.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Adds awx user to receptor group | Adds awx user to receptor group | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1499 | 60.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Configure the user's home directory | Configure the user's home directory | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1500 | 60.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Include datadir asserts | Include datadir asserts (file `assert_datadir.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_datadir is defined; receptor_datadir != '' |
| 1501 | 60.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → assert_datadir.yml | file `assert_datadir.yml` | Task file `ansible/automation_platform_installer/roles/receptor/tasks/assert_datadir.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_datadir is defined; receptor_datadir != '' |
| 1502 | 60.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → assert_datadir.yml | Ensure receptor_datadir exists with correct permissions | Ensure receptor_datadir exists with correct permissions | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_datadir is defined; receptor_datadir != '' |
| 1503 | 60.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → assert_datadir.yml | Check if '{{ run_receptor_as }}' user can write to receptor_datadir | Check if '{{ run_receptor_as }}' user can write to receptor_datadir | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_datadir is defined; receptor_datadir != '' |
| 1504 | 60.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → assert_datadir.yml | Fail if '{{ run_receptor_as }}' user cannot write to receptor_datadir | Fail if '{{ run_receptor_as }}' user cannot write to receptor_datadir | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_datadir is defined; receptor_datadir != ''; awx_dir_stat.failed \| bool |
| 1505 | 60.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → assert_datadir.yml | Ensure util-linux package is installed for findmnt availability | Ensure util-linux package is installed for findmnt availability | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_datadir is defined; receptor_datadir != '' |
| 1506 | 60.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → assert_datadir.yml | Check if receptor_datadir is on tmpfs using findmnt | Check if receptor_datadir is on tmpfs using findmnt | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_datadir is defined; receptor_datadir != '' |
| 1507 | 60.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → assert_datadir.yml | Create tmpfiles.d configuration if receptor_datadir is on tmpfs | Create tmpfiles.d configuration if receptor_datadir is on tmpfs | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_datadir is defined; receptor_datadir != ''; fstype_output.stdout != ''; fstype_output.stdout.strip() == 'tmpfs' |
| 1508 | 60.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Ensure soft/hard file descriptors limits | Ensure soft/hard file descriptors limits (dest `/etc/security/limits.d/awx.conf`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1509 | 60.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Create and and distribute keys for signing work | Create and and distribute keys for signing work (file `work_sign_keys.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1510 | 60.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → work_sign_keys.yml | file `work_sign_keys.yml` | Task file `ansible/automation_platform_installer/roles/receptor/tasks/work_sign_keys.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1511 | 60.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → work_sign_keys.yml | Create Work signing keys | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); groups['automationcontroller'] \| default([]) \| length; inventory_hostname == groups['automationcontroller'][0] |
| 1512 | 60.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → work_sign_keys.yml → Create Work signing keys | Generate Private RSA key for signing work | Generate Private RSA key for signing work | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); groups['automationcontroller'] \| default([]) \| length; inventory_hostname == groups['automationcontroller'][0] |
| 1513 | 60.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → work_sign_keys.yml → Create Work signing keys | Generate public RSA key for signing work | Generate public RSA key for signing work | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); groups['automationcontroller'] \| default([]) \| length; inventory_hostname == groups['automationcontroller'][0] |
| 1514 | 60.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → work_sign_keys.yml → Create Work signing keys | Slurp work signing keys | Slurp work signing keys | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); groups['automationcontroller'] \| default([]) \| length; inventory_hostname == groups['automationcontroller'][0] |
| 1515 | 61.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → work_sign_keys.yml | Distribute Work Sign keys to automationcontroller nodes | Distribute Work Sign keys to automationcontroller nodes (dest `{{ item['source'] }}`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); groups['automationcontroller'] \| default([]) \| length; hostvars[groups['automationcontroller'][0]]['signing_keys']['results'] \| default([]) \| length; inventory_hostname in groups['automationcontroller']; inventory_hostname != groups['automationcontroller'][0] |
| 1516 | 61.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → work_sign_keys.yml | Distribute work sign public keys to all execution_nodes | Distribute work sign public keys to all execution_nodes (dest `{{ item['source'] }}`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); groups['automationcontroller'] \| default([]) \| length; hostvars[groups['automationcontroller'][0]]['signing_keys']['results'] \| default([]) \| length; groups['execution_nodes'] \| default([]) \| length; inventory_hostname in groups['execution_nodes']; item['source'] == work_sign_public_keyfile; node_type == 'execution' |
| 1517 | 61.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Allow awx user to read receptor configuration | Allow awx user to read receptor configuration (dest `/etc/receptor`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1518 | 61.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Ensure systemd override directory exists | Ensure systemd override directory exists (dest `/etc/systemd/system/receptor.service.d`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1519 | 61.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Override receptor's systemd service runuser | Override receptor's systemd service runuser (dest `/etc/systemd/system/receptor.service.d/override.conf`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1520 | 61.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Configure the awx-receptor socket directory. | Configure the awx-receptor socket directory. | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1521 | 61.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Create tmpfiles.d entry for awx-receptor socket directory | Create tmpfiles.d entry for awx-receptor socket directory (dest `/etc/tmpfiles.d/awx-receptor.conf`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1522 | 61.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Create tmpfiles.d entry for receptor socket directory | Create tmpfiles.d entry for receptor socket directory (dest `/etc/tmpfiles.d/receptor.conf`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1523 | 61.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Configure firewall | Configure firewall (role `firewall`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1524 | 61.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → firewall | role `firewall` | Enter role task list | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1525 | 61.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → firewall → firewall | file `main.yml` | Task file `ansible/automation_platform_installer/roles/firewall/tasks/main.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1526 | 61.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → firewall → firewall | include_tasks | Include a task file from the current role or playbook (file `discover.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1527 | 61.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → firewall → firewall → discover.yml | file `discover.yml` | Task file `ansible/automation_platform_installer/roles/firewall/tasks/discover.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1528 | 61.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → firewall → firewall → discover.yml | Determine if firewalld is running | Determine if firewalld is running | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1529 | 61.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → firewall → firewall → discover.yml | Determine if firewalld is enabled | Determine if firewalld is enabled | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1530 | 61.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → firewall → firewall → discover.yml | Determine if iptables is installed | Determine if iptables is installed | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1531 | 61.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → firewall → firewall → discover.yml | Determine if iptables service is installed | Determine if iptables service is installed | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1532 | 61.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → firewall → firewall → discover.yml | Check to see if there is an iptables config file | Check to see if there is an iptables config file | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); iptables_installed.rc == 0 |
| 1533 | 61.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → firewall → firewall → discover.yml | Select firewalld | Select firewalld | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); firewalld_running.rc == 0 or systemctl_unit_files.stdout_lines \| select('match','firewalld.service\s+enabled') \| list \| length == 1 |
| 1534 | 61.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → firewall → firewall → discover.yml | Select iptables | Select iptables | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); iptables_installed.rc == 0; iptables_exists.stat.exists; iptables_service.stat.exists |
| 1535 | 61.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → firewall → firewall | Include {{ 'no' if component_firewall_type == 'none' else component_firewall_type }} tasks | Include {{ 'no' if component_firewall_type == 'none' else component_firewall_type }} tasks (file `{{ component_firewall_type }}.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); firewalld_running.rc == 0 or systemctl_unit_files.stdout_lines \| select('match','firewalld.service\s+enabled') \| list \| length == 1 or ( iptables_installed.rc == 0 and iptables_exists.stat.exists and iptables_service.stat.exists ) |
| 1536 | 61.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → firewall → firewall → {{ component_firewall_type }}.yml | {{ component_firewall_type }}.yml | Include task file `{{ component_firewall_type }}.yml` (relative to current role task directory) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); firewalld_running.rc == 0 or systemctl_unit_files.stdout_lines \| select('match','firewalld.service\s+enabled') \| list \| length == 1 or ( iptables_installed.rc == 0 and iptables_exists.stat.exists and iptables_service.stat.exists ) |
| 1537 | 61.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Configure podman | Configure podman (role `podman`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types |
| 1538 | 61.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman | role `podman` | Enter role task list | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types |
| 1539 | 61.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman | file `main.yml` | Task file `ansible/automation_platform_installer/roles/podman/tasks/main.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types |
| 1540 | 62.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman | Allocate subuids for {{ run_podman_as }} user | Allocate subuids for {{ run_podman_as }} user | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types |
| 1541 | 62.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman | Allocate subgids for {{ run_podman_as }} user | Allocate subgids for {{ run_podman_as }} user | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types |
| 1542 | 62.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman | Configure keyring size if needed | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types; (ansible_memtotal_mb // 100) > 200 |
| 1543 | 62.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman → Configure keyring size if needed | Set maxkeys | Set maxkeys | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types; (ansible_memtotal_mb // 100) > 200 |
| 1544 | 62.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman → Configure keyring size if needed | Set maxbytes | Set maxbytes | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types; (ansible_memtotal_mb // 100) > 200 |
| 1545 | 62.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman | Create directory for crun runtime config | Create directory for crun runtime config | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types |
| 1546 | 62.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman | Configure podman default runtime | Configure podman default runtime (dest `{{ run_podman_home_prefix }}/{{ run_podman_as }}/.config/containers/containers.conf`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types |
| 1547 | 62.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman | Create mounts.conf from template | Create mounts.conf from template (dest `{{ run_podman_home_prefix }}/{{ run_podman_as }}/.config/containers/mounts.conf`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types; not podman_copy_mounts_conf \| bool |
| 1548 | 62.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman | Copy user provided mounts.conf | Copy user provided mounts.conf (dest `{{ run_podman_home_prefix }}/{{ run_podman_as }}/.config/containers/mounts.conf`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types; podman_copy_mounts_conf \| bool |
| 1549 | 62.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman | Configure SELinux context mapping | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types; ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 1550 | 62.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman → Configure SELinux context mapping | Ensure policycoreutils-python-utils is installed | Ensure policycoreutils-python-utils is installed | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types; ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 1551 | 62.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman → Configure SELinux context mapping | Determine podman container storage dir variable | Determine podman container storage dir variable | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types; ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 1552 | 62.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman → Configure SELinux context mapping | Check current SELinux context mapping | Check current SELinux context mapping | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types; ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 1553 | 62.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman → Configure SELinux context mapping | Delete custom SELinux context mapping | Delete custom SELinux context mapping | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types; ansible_selinux is defined; ansible_selinux.status == "enabled"; semanage_current.stdout_lines\|length; "/var/lib/containers" not in semanage_current |
| 1554 | 62.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman → Configure SELinux context mapping | Register expected SELinux context mapping | Register expected SELinux context mapping | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types; ansible_selinux is defined; ansible_selinux.status == "enabled"; semanage_current.failed \| bool or semanage_deleted.changed \| bool |
| 1555 | 62.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman → Configure SELinux context mapping | Check if container storage dir exists | Check if container storage dir exists | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types; ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 1556 | 62.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman → Configure SELinux context mapping | Perform SELinux relabeling for existing files | Perform SELinux relabeling for existing files | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types; ansible_selinux is defined; ansible_selinux.status == "enabled"; _podman_container_storage_stat.stat.exists \| bool and (podman_force_selinux_relabel \| bool or selinux_manage.changed \| bool) |
| 1557 | 62.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman | Ensure registries.conf.d exists | Ensure registries.conf.d exists | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types |
| 1558 | 62.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman | Force fully qualified image names to be provided to podman pull | Force fully qualified image names to be provided to podman pull (dest `/etc/containers/registries.conf.d/force-fully-qualified-images.conf`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types |
| 1559 | 62.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman | Create tmpfiles.d entry for podman | Create tmpfiles.d entry for podman | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types |
| 1560 | 62.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman | block | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types; subuids is changed or subgids is changed |
| 1561 | 62.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman → block | Create temporary directory for podman | Create temporary directory for podman | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types; subuids is changed or subgids is changed |
| 1562 | 62.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman → block | Ensure changes are applied to podman | Ensure changes are applied to podman | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types; subuids is changed or subgids is changed |
| 1563 | 62.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman | block (always) | Run always tasks for block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types; subuids is changed or subgids is changed |
| 1564 | 62.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman → block (always) | Remove podman temporary directory | Remove podman temporary directory | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types; subuids is changed or subgids is changed; podman_tmp.path is defined |
| 1565 | 63.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman | Configure XDG_RUNTIME_DIR Environment variable for "{{ run_podman_as }}" user | Configure XDG_RUNTIME_DIR Environment variable for "{{ run_podman_as }}" user | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types |
| 1566 | 63.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman | Configure DBUS_SESSION_BUS_ADDRESS Environment variable for "{{ run_podman_as }}" user | Configure DBUS_SESSION_BUS_ADDRESS Environment variable for "{{ run_podman_as }}" user | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types |
| 1567 | 63.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman | Enable lingering systemd sessions for {{ run_podman_as }} user | Enable lingering systemd sessions for {{ run_podman_as }} user | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types |
| 1568 | 63.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman | Reset podman for {{ run_podman_as }} user to apply configs | Reset podman for {{ run_podman_as }} user to apply configs | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types |
| 1569 | 63.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman | Enable podman socket for {{ run_podman_as }} and restart | Enable podman socket for {{ run_podman_as }} and restart (service `podman.socket`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types; podman_enable_socket \| bool |
| 1570 | 63.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | set_fact | Set a host fact used by later tasks | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1571 | 63.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Disable minTLS13 if any node is FIPS enabled | Disable minTLS13 if any node is FIPS enabled | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1572 | 63.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Deploy receptor config | Deploy receptor config (dest `/etc/receptor/receptor.conf`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1573 | 63.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Start Receptor service | Start Receptor service (service `receptor`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1574 | 63.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Register instances and queues and check mesh connectivity | Register instances and queues and check mesh connectivity (file `register_instances.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1575 | 63.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → register_instances.yml | file `register_instances.yml` | Task file `ansible/automation_platform_installer/roles/receptor/tasks/register_instances.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1576 | 63.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → register_instances.yml | Get UUID for execution nodes | Get UUID for execution nodes | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type == 'execution' |
| 1577 | 63.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → register_instances.yml | Set UUID option for execution_nodes | Set UUID option for execution_nodes | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type == 'execution' |
| 1578 | 63.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → register_instances.yml | Generate UUID for hop nodes | Generate UUID for hop nodes | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type == 'hop' |
| 1579 | 63.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → register_instances.yml | Set UUID option for hop nodes | Set UUID option for hop nodes | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type == 'hop' |
| 1580 | 63.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → register_instances.yml | Set UUID option for control / hybrid nodes | Set UUID option for control / hybrid nodes | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in ['control', 'hybrid'] and system_uuid is defined |
| 1581 | 63.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → register_instances.yml | Register the node instance in the database. | Register the node instance in the database. | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1582 | 63.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → register_instances.yml | Set listener port option for nodes | Set listener port option for nodes | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); listener is defined and listener |
| 1583 | 63.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → register_instances.yml | Register controlplane nodes to controlplane queue | Register controlplane nodes to controlplane queue | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1584 | 63.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → register_instances.yml | Register work nodes to default queue | Register work nodes to default queue | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1585 | 63.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → register_instances.yml | Register instance_group_* groups | Register instance_group_* groups | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); item.startswith('instance_group_'); mesh_groups[item] \| length |
| 1586 | 63.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → register_instances.yml | Flush handlers | Flush handlers (flush_handlers) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1587 | 63.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → register_instances.yml | Validate connectivity for Mesh peers | Validate connectivity for Mesh peers | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1588 | 63.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → register_instances.yml | Compose Mesh Report | Compose Mesh Report | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1589 | 64.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → register_instances.yml | Receptor Mesh Report | Receptor Mesh Report | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh_report is defined |
| 1590 | 64.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → register_instances.yml | Report error when mesh connectivity issue is detected | Report error when mesh connectivity issue is detected | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh_ping.failed \| default(False) \| bool |
| 1591 | 64.0% | install.yml → Install Automation Controller and Execution nodes | Install Automation Controller and Execution nodes (always) | Run always tasks for block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1592 | 64.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes (always) | Run repo_management (teardown) | Run repo_management (teardown) (role `repo_management`, tasks `teardown`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1593 | 64.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes (always) → repo_management | role `repo_management` (`teardown`) | Enter role task list | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1594 | 64.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes (always) → repo_management → repo_management | file `teardown.yml` | Task file `ansible/automation_platform_installer/roles/repo_management/tasks/teardown.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1595 | 64.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes (always) → repo_management → repo_management | Disable Automation Platform repository | Disable Automation Platform repository | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); extra_repos is defined |
| 1596 | 64.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes (always) → repo_management → repo_management | Disable Automation Platform RHSM repository | Disable Automation Platform RHSM repository | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 1597 | 64.3% | install.yml | Post-Receptor setup | Play targeting `automationcontroller:execution_nodes` | — |
| 1598 | 64.3% | install.yml → Post-Receptor setup | Register Automation Controller and Execution node links | Begin conditional or grouped task block | node_state is defined; node_state == 'active' |
| 1599 | 64.4% | install.yml → Post-Receptor setup → Register Automation Controller and Execution node links | set_fact | Set a host fact used by later tasks | node_state is defined; node_state == 'active' |
| 1600 | 64.4% | install.yml → Post-Receptor setup → Register Automation Controller and Execution node links | include_role | Include and run tasks from an Ansible role (role `receptor`, tasks `peers`) | node_state is defined; node_state == 'active' |
| 1601 | 64.4% | install.yml → Post-Receptor setup → Register Automation Controller and Execution node links → receptor | role `receptor` (`peers`) | Enter role task list | node_state is defined; node_state == 'active' |
| 1602 | 64.5% | install.yml → Post-Receptor setup → Register Automation Controller and Execution node links → receptor → receptor | file `peers.yml` | Task file `ansible/automation_platform_installer/roles/receptor/tasks/peers.yml` | node_state is defined; node_state == 'active' |
| 1603 | 64.5% | install.yml → Post-Receptor setup → Register Automation Controller and Execution node links → receptor → receptor | register the peers for a node, with removal of old peers | register the peers for a node, with removal of old peers | node_state is defined; node_state == 'active' |
| 1604 | 64.6% | install.yml | Install Automation Hub node | Play targeting `automationhub` | — |
| 1605 | 64.6% | install.yml → Install Automation Hub node | block | Begin conditional or grouped task block | — |
| 1606 | 64.6% | install.yml → Install Automation Hub node → block | Add internal CA certificate to trust store | Add internal CA certificate to trust store (role `certificate_authority`, tasks `add_cacert.yml`) | — |
| 1607 | 64.7% | install.yml → Install Automation Hub node → block → certificate_authority | role `certificate_authority` (`add_cacert.yml`) | Enter role task list | — |
| 1608 | 64.7% | install.yml → Install Automation Hub node → block → certificate_authority → certificate_authority | file `add_cacert.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/add_cacert.yml` | — |
| 1609 | 64.8% | install.yml → Install Automation Hub node → block → certificate_authority → certificate_authority | Ensure ca-certificates is installed | Ensure ca-certificates is installed | — |
| 1610 | 64.8% | install.yml → Install Automation Hub node → block → certificate_authority → certificate_authority | Copy custom CA cert | Copy custom CA cert (dest `/etc/pki/ca-trust/source/anchors/ansible-automation-platform-custom-ca-cert.crt`) | custom_ca_cert is defined |
| 1611 | 64.8% | install.yml → Install Automation Hub node → block → certificate_authority → certificate_authority | Place AAP CA certificate | Place AAP CA certificate (dest `/etc/pki/ca-trust/source/anchors/ansible-automation-platform-managed-ca-cert.crt`) | — |
| 1612 | 64.9% | install.yml → Install Automation Hub node → block → certificate_authority → certificate_authority | Update CA trust | Update CA trust | — |
| 1613 | 64.9% | install.yml → Install Automation Hub node → block | Run repo_management (setup) | Run repo_management (setup) (role `repo_management`, tasks `setup`) | — |
| 1614 | 65.0% | install.yml → Install Automation Hub node → block → repo_management | role `repo_management` (`setup`) | Enter role task list | — |
| 1615 | 65.0% | install.yml → Install Automation Hub node → block → repo_management → repo_management | file `setup.yml` | Task file `ansible/automation_platform_installer/roles/repo_management/tasks/setup.yml` | — |
| 1616 | 65.0% | install.yml → Install Automation Hub node → block → repo_management → repo_management | include_vars | Load variables from a file | — |
| 1617 | 65.1% | install.yml → Install Automation Hub node → block → repo_management → repo_management | Install yum repo keys | Install yum repo keys (dest `/etc/pki/rpm-gpg/{{ item }}`) | — |
| 1618 | 65.1% | install.yml → Install Automation Hub node → block → repo_management → repo_management | Install subscription-manager-rhsm-certificates | Install subscription-manager-rhsm-certificates | — |
| 1619 | 65.2% | install.yml → Install Automation Hub node → block → repo_management → repo_management | Determine if rhsm repository exists | Determine if rhsm repository exists | — |
| 1620 | 65.2% | install.yml → Install Automation Hub node → block → repo_management → repo_management | Enable Automation Platform rhsm repository | Enable Automation Platform rhsm repository | ansible_distribution == 'RedHat'; rhsm_repo.stat.exists; not bundle_install \| bool; aw_repo_url is undefined |
| 1621 | 65.2% | install.yml → Install Automation Hub node → block → repo_management → repo_management | Explain Automation Platform rhsm repository failure | Explain Automation Platform rhsm repository failure | aap_rhsm_repo_enable.failed \| default(false) |
| 1622 | 65.3% | install.yml → Install Automation Hub node → block → repo_management → repo_management | block | Begin conditional or grouped task block | aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 1623 | 65.3% | install.yml → Install Automation Hub node → block → repo_management → repo_management → block | Enable module hotfixes flag | Enable module hotfixes flag | aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 1624 | 65.4% | install.yml → Install Automation Hub node → block → repo_management → repo_management → block | Set AAP repo for update | Set AAP repo for update | aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 1625 | 65.4% | install.yml → Install Automation Hub node → block → repo_management → repo_management | Ensure rsync is installed | Ensure rsync is installed | bundle_install \| bool |
| 1626 | 65.4% | install.yml → Install Automation Hub node → block → repo_management → repo_management | Copy bundle packages to repo source directory | Begin conditional or grouped task block | bundle_install \| bool |
| 1627 | 65.5% | install.yml → Install Automation Hub node → block → repo_management → repo_management → Copy bundle packages to repo source directory | Copy bundle packages to repo source directory | Copy bundle packages to repo source directory | bundle_install \| bool |
| 1628 | 65.5% | install.yml → Install Automation Hub node → block → repo_management → repo_management | Copy bundle packages to repo source directory (rescue) | Run rescue tasks for block | bundle_install \| bool |
| 1629 | 65.6% | install.yml → Install Automation Hub node → block → repo_management → repo_management → Copy bundle packages to repo source directory (rescue) | Remove old bundle packages source directory (legacy) | Remove old bundle packages source directory (legacy) | bundle_install \| bool |
| 1630 | 65.6% | install.yml → Install Automation Hub node → block → repo_management → repo_management → Copy bundle packages to repo source directory (rescue) | Copy bundle packages to repo source directory (legacy) | Copy bundle packages to repo source directory (legacy) (dest `{{ bundle_install_folder }}`) | bundle_install \| bool |
| 1631 | 65.6% | install.yml → Install Automation Hub node → block → repo_management → repo_management | Install the Automation Platform bundle repository | Install the Automation Platform bundle repository (dest `/etc/yum.repos.d/ansible-automation-platform.repo`) | bundle_install \| bool |
| 1632 | 65.7% | install.yml → Install Automation Hub node → block → repo_management → repo_management | Remove legacy ansible-tower repository | Remove legacy ansible-tower repository | — |
| 1633 | 65.7% | install.yml → Install Automation Hub node → block → repo_management → repo_management | Install the Automation Platform yum repository. | Install the Automation Platform yum repository. (dest `/etc/yum.repos.d/ansible-automation-platform.repo`) | aw_repo_url is defined; not bundle_install \| bool |
| 1634 | 65.8% | install.yml → Install Automation Hub node → block → repo_management → repo_management | Set repo list for bundle install | Set repo list for bundle install | bundle_install \| bool |
| 1635 | 65.8% | install.yml → Install Automation Hub node → block → repo_management → repo_management | Set repo list for online install | Set repo list for online install | yum_repo is succeeded; yum_repo is not skipped |
| 1636 | 65.8% | install.yml → Install Automation Hub node → block → repo_management → repo_management | Enable Ansible Automation Platform repo | Enable Ansible Automation Platform repo | extra_repos is defined |
| 1637 | 65.9% | install.yml → Install Automation Hub node → block | include_role | Include and run tasks from an Ansible role (role `redis`) | — |
| 1638 | 65.9% | install.yml → Install Automation Hub node → block → redis | role `redis` | Enter role task list | — |
| 1639 | 66.0% | install.yml → Install Automation Hub node → block → redis → redis | file `main.yml` | Task file `ansible/automation_platform_installer/roles/redis/tasks/main.yml` | — |
| 1640 | 66.0% | install.yml → Install Automation Hub node → block → redis → redis | include_tasks | Include a task file from the current role or playbook (file `vars.yml`) | — |
| 1641 | 66.0% | install.yml → Install Automation Hub node → block → redis → redis → vars.yml | file `vars.yml` | Task file `ansible/automation_platform_installer/roles/redis/tasks/vars.yml` | — |
| 1642 | 66.1% | install.yml → Install Automation Hub node → block → redis → redis → vars.yml | include_vars | Load variables from a file | — |
| 1643 | 66.1% | install.yml → Install Automation Hub node → block → redis → redis → vars.yml | include_vars | Load variables from a file | install_redis_mode == "cluster" |
| 1644 | 66.2% | install.yml → Install Automation Hub node → block → redis → redis | include_tasks | Include a task file from the current role or playbook (file `tasks.yml`) | — |
| 1645 | 66.2% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml | file `tasks.yml` | Task file `ansible/automation_platform_installer/roles/redis/tasks/tasks.yml` | — |
| 1646 | 66.3% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml | Ensure redis package is installed | Ensure redis package is installed | — |
| 1647 | 66.3% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml | Ensure AAP directory exists | Ensure AAP directory exists | — |
| 1648 | 66.3% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml | Ensure redis cert directory exists | Ensure redis cert directory exists | — |
| 1649 | 66.4% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml | Configure TLS certificates for redis | Begin conditional or grouped task block | not _redis_disable_tls \| bool |
| 1650 | 66.4% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → Configure TLS certificates for redis | Copy user provided SSL cert | Begin conditional or grouped task block | not _redis_disable_tls \| bool; redis_tls_cert \| length |
| 1651 | 66.5% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → Configure TLS certificates for redis → Copy user provided SSL cert | Import specified TLS certificate | Import specified TLS certificate | not _redis_disable_tls \| bool; redis_tls_cert \| length |
| 1652 | 66.5% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → Configure TLS certificates for redis → Copy user provided SSL cert | Import specified TLS private key | Import specified TLS private key | not _redis_disable_tls \| bool; redis_tls_cert \| length |
| 1653 | 66.5% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → Configure TLS certificates for redis | Use internal CA | Use internal CA (role `certificate_authority`, tasks `sign_service.yml`) | not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 1654 | 66.6% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority | role `certificate_authority` (`sign_service.yml`) | Enter role task list | not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 1655 | 66.6% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | file `sign_service.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/sign_service.yml` | not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 1656 | 66.7% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | include_tasks | Include a task file from the current role or playbook (file `init.yml`) | not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 1657 | 66.7% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → init.yml | file `init.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/init.yml` | not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 1658 | 66.7% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → init.yml | Ensure python cryptography libraries are installed | Ensure python cryptography libraries are installed | not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 1659 | 66.8% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | Create {{ aap_ca_servicename }} private key | Create {{ aap_ca_servicename }} private key | not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 1660 | 66.8% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | set_fact | Set a host fact used by later tasks | not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 1661 | 66.9% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | Check if {{ aap_ca_servicename }} public key exists | Check if {{ aap_ca_servicename }} public key exists | not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 1662 | 66.9% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | not _redis_disable_tls \| bool; not redis_tls_cert \| length; certificate_stat.stat.exists \| bool |
| 1663 | 66.9% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | Check contents of {{ aap_ca_servicename }} public key | Check contents of {{ aap_ca_servicename }} public key | not _redis_disable_tls \| bool; not redis_tls_cert \| length; certificate_stat.stat.exists \| bool |
| 1664 | 67.0% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | not _redis_disable_tls \| bool; not redis_tls_cert \| length; certificate_stat.stat.exists \| bool; aap_service_san_records_internal \| union(aap_service_san_records) \| difference(certificate_check.subject_alt_name) \| length > 0 |
| 1665 | 67.0% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | not _redis_disable_tls \| bool; not redis_tls_cert \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1666 | 67.1% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | not _redis_disable_tls \| bool; not redis_tls_cert \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false); aap_service_is_client |
| 1667 | 67.1% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | Create CSR for {{ aap_ca_servicename }} node/s | Create CSR for {{ aap_ca_servicename }} node/s | not _redis_disable_tls \| bool; not redis_tls_cert \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1668 | 67.1% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | Sign {{ aap_ca_servicename }} public key | Sign {{ aap_ca_servicename }} public key | not _redis_disable_tls \| bool; not redis_tls_cert \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1669 | 67.2% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | Write public key to {{ aap_ca_servicename }} node/s | Write public key to {{ aap_ca_servicename }} node/s (dest `{{ aap_service_cert_fullpath }}`) | not _redis_disable_tls \| bool; not redis_tls_cert \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1670 | 67.2% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | Signal restart if certificate pair changes | Signal restart if certificate pair changes | not _redis_disable_tls \| bool; not redis_tls_cert \| length; (write_key is changed or write_certificate is changed) |
| 1671 | 67.3% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → Configure TLS certificates for redis | Trigger redis notifier for internally managed certificate changes | Trigger redis notifier for internally managed certificate changes | not _redis_disable_tls \| bool; aap_service_restart \| default(false) |
| 1672 | 67.3% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml | Ensure redis socket directory exists | Ensure redis socket directory exists | — |
| 1673 | 67.3% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml | Ensure Redis socket directory has the correct SELinux context | Ensure Redis socket directory has the correct SELinux context | — |
| 1674 | 67.4% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml | Create tmpfiles.d entry for redis socket directory | Create tmpfiles.d entry for redis socket directory (dest `/etc/tmpfiles.d/redis.conf`) | — |
| 1675 | 67.4% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml | Install redis.conf | Install redis.conf (dest `{{ redis_conf_location }}`) | — |
| 1676 | 67.5% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml | block | Begin conditional or grouped task block | not redis_use_sockets \| bool |
| 1677 | 67.5% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → block | Set gateway redis credentials for clustered | Set gateway redis credentials for clustered | not redis_use_sockets \| bool |
| 1678 | 67.5% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → block | Set Event-Driven Ansible redis credentials | Set Event-Driven Ansible redis credentials | not redis_use_sockets \| bool; groups.get("automationedacontroller", []) \| length > 0 |
| 1679 | 67.6% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → block | Create the redis user ACL file | Create the redis user ACL file | not redis_use_sockets \| bool |
| 1680 | 67.6% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → block | block | Begin conditional or grouped task block | not redis_use_sockets \| bool; install_redis_mode == "cluster" |
| 1681 | 67.7% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → block → block | Set redis hostname | Set redis hostname | not redis_use_sockets \| bool; install_redis_mode == "cluster" |
| 1682 | 67.7% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → block → block | Map redis node hostnames | Map redis node hostnames | not redis_use_sockets \| bool; install_redis_mode == "cluster" |
| 1683 | 67.7% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → block → block | Create redis nodes file | Create redis nodes file | not redis_use_sockets \| bool; install_redis_mode == "cluster" |
| 1684 | 67.8% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → block → block | Install {{ redis_init_name }} service file | Install {{ redis_init_name }} service file (dest `/lib/systemd/system/redis-platform.service`) | not redis_use_sockets \| bool; install_redis_mode == "cluster" |
| 1685 | 67.8% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml | start redis and configure to startup automatically | start redis and configure to startup automatically (service `{{ redis_init_name }}`) | — |
| 1686 | 67.9% | install.yml → Install Automation Hub node → block | include_role | Include and run tasks from an Ansible role (role `nginx`) | — |
| 1687 | 67.9% | install.yml → Install Automation Hub node → block → nginx | role `nginx` | Enter role task list | — |
| 1688 | 67.9% | install.yml → Install Automation Hub node → block → nginx → nginx | file `main.yml` | Task file `ansible/automation_platform_installer/roles/nginx/tasks/main.yml` | — |
| 1689 | 68.0% | install.yml → Install Automation Hub node → block → nginx → nginx | include_tasks | Include a task file from the current role or playbook (file `module.yml`) | — |
| 1690 | 68.0% | install.yml → Install Automation Hub node → block → nginx → nginx → module.yml | file `module.yml` | Task file `ansible/automation_platform_installer/roles/nginx/tasks/module.yml` | — |
| 1691 | 68.1% | install.yml → Install Automation Hub node → block → nginx → nginx → module.yml | Enable nginx module stream | Begin conditional or grouped task block | — |
| 1692 | 68.1% | install.yml → Install Automation Hub node → block → nginx → nginx → module.yml → Enable nginx module stream | Disable Nginx module | Disable Nginx module | — |
| 1693 | 68.1% | install.yml → Install Automation Hub node → block → nginx → nginx → module.yml → Enable nginx module stream | Enable Nginx module | Enable Nginx module | — |
| 1694 | 68.2% | install.yml → Install Automation Hub node → block → nginx → nginx | include_tasks | Include a task file from the current role or playbook (file `tasks.yml`) | — |
| 1695 | 68.2% | install.yml → Install Automation Hub node → block → nginx → nginx → tasks.yml | file `tasks.yml` | Task file `ansible/automation_platform_installer/roles/nginx/tasks/tasks.yml` | — |
| 1696 | 68.3% | install.yml → Install Automation Hub node → block → nginx → nginx → tasks.yml | Install nginx | Install nginx | — |
| 1697 | 68.3% | install.yml → Install Automation Hub node → block → nginx → nginx → tasks.yml | SELinux configuration for nginx | Begin conditional or grouped task block | ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 1698 | 68.3% | install.yml → Install Automation Hub node → block → nginx → nginx → tasks.yml → SELinux configuration for nginx | Ensure libsemanage-python is installed | Ensure libsemanage-python is installed | ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 1699 | 68.4% | install.yml → Install Automation Hub node → block → nginx → nginx → tasks.yml → SELinux configuration for nginx | Open up permissions on nginx. | Open up permissions on nginx. | ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 1700 | 68.4% | install.yml → Install Automation Hub node → block | include_role | Include and run tasks from an Ansible role (role `automationhub`) | — |
| 1701 | 68.5% | install.yml → Install Automation Hub node → block → automationhub | role `automationhub` | Enter role task list | — |
| 1702 | 68.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub | file `main.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/main.yml` | — |
| 1703 | 68.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub | Check installed version of PG | Check installed version of PG | — |
| 1704 | 68.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub | block | Begin conditional or grouped task block | pg_installed_version.failed or pg_installed_version.stdout \| trim \| int != automationhub_postgresql_version \| int |
| 1705 | 68.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub → block | Enable PostgreSQL {{ automationhub_postgresql_version }} | Enable PostgreSQL {{ automationhub_postgresql_version }} | pg_installed_version.failed or pg_installed_version.stdout \| trim \| int != automationhub_postgresql_version \| int |
| 1706 | 68.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub → block | Install PostgreSQL {{ automationhub_postgresql_version }} | Install PostgreSQL {{ automationhub_postgresql_version }} | pg_installed_version.failed or pg_installed_version.stdout \| trim \| int != automationhub_postgresql_version \| int |
| 1707 | 68.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub | Generate a Pulp secret | Generate a Pulp secret | — |
| 1708 | 68.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub | Determine plugins list | Determine plugins list | — |
| 1709 | 68.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub | Generate gateway secret | Generate gateway secret (role `automationgateway`, tasks `service_token`) | — |
| 1710 | 68.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → automationgateway | role `automationgateway` (`service_token`) | Enter role task list | — |
| 1711 | 68.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub → automationgateway → automationgateway | file `service_token.yml` | Task file `ansible/automation_platform_installer/roles/automationgateway/tasks/service_token.yml` | — |
| 1712 | 68.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub → automationgateway → automationgateway | Generate service secret | Generate service secret | — |
| 1713 | 68.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub | Default pulp settings | Default pulp settings | — |
| 1714 | 69.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub | Override working_directory when using shared storage | Override working_directory when using shared storage | groups['automationhub'] \| length > 1 |
| 1715 | 69.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub | Combine pulp settings with additional settings | Combine pulp settings with additional settings | — |
| 1716 | 69.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub | Combine pulp settings with feature flags if they exist | Combine pulp settings with feature flags if they exist | feature_flags is defined |
| 1717 | 69.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub | set_fact | Set a host fact used by later tasks | automationhub_importer_settings is defined |
| 1718 | 69.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub | Configure Pulp | Configure Pulp (file `pulp_common.yml`) | — |
| 1719 | 69.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml | file `pulp_common.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/pulp_common.yml` | — |
| 1720 | 69.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml | pulp_install_plugins_normalized_yml | pulp_install_plugins_normalized_yml | — |
| 1721 | 69.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml | pulp_install_plugins_normalized | pulp_install_plugins_normalized | — |
| 1722 | 69.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml | Load OS specific variables | Load OS specific variables | — |
| 1723 | 69.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml | Gather package facts | Gather package facts | — |
| 1724 | 69.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml | Enumerate default system PATH | Enumerate default system PATH | — |
| 1725 | 69.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml | Set the default system PATH as a fact | Set the default system PATH as a fact | — |
| 1726 | 69.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml | import_tasks | Import a task file (static include) (file `user.yml`) | — |
| 1727 | 69.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → user.yml | file `user.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/user.yml` | — |
| 1728 | 69.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → user.yml | Make sure {{ pulp_group }} group exists | Make sure {{ pulp_group }} group exists | — |
| 1729 | 69.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → user.yml | Create user {{ pulp_user }} | Create user {{ pulp_user }} | — |
| 1730 | 69.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → user.yml | Check if the redis group exists | Check if the redis group exists | — |
| 1731 | 69.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → user.yml | Add user pulp to the redis group | Add user pulp to the redis group | redis_group.rc == 0 |
| 1732 | 69.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → user.yml | Reset ssh conn to allow user changes to affect when ssh user and pulp user are the same | Reset ssh conn to allow user changes to affect when ssh user and pulp user are the same (reset_connection) | — |
| 1733 | 69.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml | import_tasks | Import a task file (static include) (file `dirs.yml`) | — |
| 1734 | 69.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → dirs.yml | file `dirs.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/dirs.yml` | — |
| 1735 | 69.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → dirs.yml | Make sure {{ pulp_user_home }} is world executable, and exists | Make sure {{ pulp_user_home }} is world executable, and exists | — |
| 1736 | 69.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → dirs.yml | Create deploy root dir for pulp | Create deploy root dir for pulp | — |
| 1737 | 69.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → dirs.yml | Create media dir for Pulp | Create media dir for Pulp | __pulp_common_merged_pulp_settings.media_root \| length > 0 |
| 1738 | 70.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → dirs.yml | Create cache dir for Pulp | Create cache dir for Pulp | — |
| 1739 | 70.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → dirs.yml | Create file upload temp dir for Pulp | Create file upload temp dir for Pulp | — |
| 1740 | 70.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → dirs.yml | Create assets (static) dir for Pulp | Create assets (static) dir for Pulp | — |
| 1741 | 70.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → dirs.yml | Check if artifact directory exists at old path | Check if artifact directory exists at old path | — |
| 1742 | 70.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → dirs.yml | Move artifact directory inside MEDIA_ROOT | Move artifact directory inside MEDIA_ROOT | old_artifact_dir_stat.stat.exists |
| 1743 | 70.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → dirs.yml | Create artifact dir for Pulp | Create artifact dir for Pulp | — |
| 1744 | 70.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → dirs.yml | Create scripts directory to hold user-provided scripts | Create scripts directory to hold user-provided scripts | — |
| 1745 | 70.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → dirs.yml | Create cert directory to hold token authentication key | Create cert directory to hold token authentication key | — |
| 1746 | 70.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml | import_tasks | Import a task file (static include) (file `install.yml`) | — |
| 1747 | 70.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml | file `install.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/install.yml` | — |
| 1748 | 70.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml | Install prerequisites | Install prerequisites | — |
| 1749 | 70.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml | Gather package facts | Gather package facts | — |
| 1750 | 70.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml | Installed packages | Installed packages | — |
| 1751 | 70.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml | Get PATH | Get PATH | — |
| 1752 | 70.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml | Set PATH as a fact | Set PATH as a fact | — |
| 1753 | 70.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml | Get LD_LIBRARY_PATH | Get LD_LIBRARY_PATH | — |
| 1754 | 70.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml | Set LD_LIBRARY_PATH as a fact | Set LD_LIBRARY_PATH as a fact | — |
| 1755 | 70.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml | install pulp | install pulp (file `install_packages.yml`) | — |
| 1756 | 70.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → install_packages.yml | file `install_packages.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/install_packages.yml` | — |
| 1757 | 70.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → install_packages.yml | Set a fact for the version of pulp-container installed prior to upgrade | Set a fact for the version of pulp-container installed prior to upgrade | ansible_facts.packages[item] is defined |
| 1758 | 70.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → install_packages.yml | Install the packages | Install the packages | — |
| 1759 | 70.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → install_packages.yml | Update existing dependent packages | Update existing dependent packages (role `repo_management`, tasks `update_dependency`) | — |
| 1760 | 70.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → install_packages.yml → repo_management | role `repo_management` (`update_dependency`) | Enter role task list | — |
| 1761 | 70.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → install_packages.yml → repo_management → repo_management | file `update_dependency.yml` | Task file `ansible/automation_platform_installer/roles/repo_management/tasks/update_dependency.yml` | — |
| 1762 | 70.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → install_packages.yml → repo_management → repo_management | Ensure AAP repo for update is set | Ensure AAP repo for update is set | not aap_repo_for_update \| default('') \| length |
| 1763 | 71.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → install_packages.yml → repo_management → repo_management | Generate a list of AAP packages with an update | Generate a list of AAP packages with an update | — |
| 1764 | 71.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → install_packages.yml → repo_management → repo_management | Set AAP packages to update | Set AAP packages to update | — |
| 1765 | 71.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → install_packages.yml → repo_management → repo_management | Update packages | Update packages | _packages_to_update \| length |
| 1766 | 71.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → install_packages.yml | Trigger collectstatic and pulpcore service restart if package changed | Trigger collectstatic and pulpcore service restart if package changed | _package_update_result is changed |
| 1767 | 71.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → install_packages.yml | Gather package facts | Gather package facts | — |
| 1768 | 71.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → install_packages.yml | Set __pulpcore_version to the RPM that was installed | Set __pulpcore_version to the RPM that was installed | pulpcore_version \| default('') \| length == 0 |
| 1769 | 71.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml | Refresh facts in case /var/lib/pulp was mounted earlier | Refresh facts in case /var/lib/pulp was mounted earlier | — |
| 1770 | 71.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml | Include tasks to ensure that /var/lib/pulp subdirs have the SELinux label | Include tasks to ensure that /var/lib/pulp subdirs have the SELinux label (file `selinux_remount.yml`) | pulp_install_selinux_policies\|bool or (ansible_facts.selinux.status == "enabled" and pulp_install_selinux_policies == "auto"); ansible_facts.mounts \| selectattr('mount', 'match', '^/var/lib/pulp$') \| list \| count == 1 |
| 1771 | 71.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → selinux_remount.yml | file `selinux_remount.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/selinux_remount.yml` | pulp_install_selinux_policies\|bool or (ansible_facts.selinux.status == "enabled" and pulp_install_selinux_policies == "auto"); ansible_facts.mounts \| selectattr('mount', 'match', '^/var/lib/pulp$') \| list \| count == 1 |
| 1772 | 71.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → selinux_remount.yml | Test setting SELinux context on /var/lib/pulp/.selinux-test | Test setting SELinux context on /var/lib/pulp/.selinux-test (dest `/var/lib/pulp/.selinux-test`) | pulp_install_selinux_policies\|bool or (ansible_facts.selinux.status == "enabled" and pulp_install_selinux_policies == "auto"); ansible_facts.mounts \| selectattr('mount', 'match', '^/var/lib/pulp$') \| list \| count == 1 |
| 1773 | 71.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → selinux_remount.yml | /etc/fstab: Set opt "{{ __remount_opt }}" for mount point {{ __remount_point }} | /etc/fstab: Set opt "{{ __remount_opt }}" for mount point {{ __remount_point }} | pulp_install_selinux_policies\|bool or (ansible_facts.selinux.status == "enabled" and pulp_install_selinux_policies == "auto"); ansible_facts.mounts \| selectattr('mount', 'match', '^/var/lib/pulp$') \| list \| count == 1; not pulp_selinux_test.secontext \| regex_search('pulpcore_var_lib_t'); not ansible_facts.mounts \| selectattr('mount', 'match', '^/var/lib/pulp$') \| map(attribute='options') \| first \| regex_search('context=') |
| 1774 | 71.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → selinux_remount.yml | Remount the static dir | Begin conditional or grouped task block | pulp_install_selinux_policies\|bool or (ansible_facts.selinux.status == "enabled" and pulp_install_selinux_policies == "auto"); ansible_facts.mounts \| selectattr('mount', 'match', '^/var/lib/pulp$') \| list \| count == 1; fstab.changed |
| 1775 | 71.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → selinux_remount.yml → Remount the static dir | If {{ __remount_point }} changed, unmount | If {{ __remount_point }} changed, unmount | pulp_install_selinux_policies\|bool or (ansible_facts.selinux.status == "enabled" and pulp_install_selinux_policies == "auto"); ansible_facts.mounts \| selectattr('mount', 'match', '^/var/lib/pulp$') \| list \| count == 1; fstab.changed |
| 1776 | 71.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → selinux_remount.yml → Remount the static dir | If {{ __remount_point }} changed, remount | If {{ __remount_point }} changed, remount | pulp_install_selinux_policies\|bool or (ansible_facts.selinux.status == "enabled" and pulp_install_selinux_policies == "auto"); ansible_facts.mounts \| selectattr('mount', 'match', '^/var/lib/pulp$') \| list \| count == 1; fstab.changed |
| 1777 | 71.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → selinux_remount.yml | Remount the static dir (rescue) | Run rescue tasks for block | pulp_install_selinux_policies\|bool or (ansible_facts.selinux.status == "enabled" and pulp_install_selinux_policies == "auto"); ansible_facts.mounts \| selectattr('mount', 'match', '^/var/lib/pulp$') \| list \| count == 1; fstab.changed |
| 1778 | 71.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → selinux_remount.yml → Remount the static dir (rescue) | Stop pulpcore service | Stop pulpcore service (service `pulpcore.service`) | pulp_install_selinux_policies\|bool or (ansible_facts.selinux.status == "enabled" and pulp_install_selinux_policies == "auto"); ansible_facts.mounts \| selectattr('mount', 'match', '^/var/lib/pulp$') \| list \| count == 1; fstab.changed |
| 1779 | 71.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → selinux_remount.yml → Remount the static dir (rescue) | Unmount /var/lib/pulp/pulpcore_static | Unmount /var/lib/pulp/pulpcore_static | pulp_install_selinux_policies\|bool or (ansible_facts.selinux.status == "enabled" and pulp_install_selinux_policies == "auto"); ansible_facts.mounts \| selectattr('mount', 'match', '^/var/lib/pulp$') \| list \| count == 1; fstab.changed |
| 1780 | 71.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → selinux_remount.yml → Remount the static dir (rescue) | If {{ __remount_point }} changed, unmount (retry) | If {{ __remount_point }} changed, unmount (retry) | pulp_install_selinux_policies\|bool or (ansible_facts.selinux.status == "enabled" and pulp_install_selinux_policies == "auto"); ansible_facts.mounts \| selectattr('mount', 'match', '^/var/lib/pulp$') \| list \| count == 1; fstab.changed |
| 1781 | 71.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → selinux_remount.yml → Remount the static dir (rescue) | If {{ __remount_point }} changed, remount (retry) | If {{ __remount_point }} changed, remount (retry) | pulp_install_selinux_policies\|bool or (ansible_facts.selinux.status == "enabled" and pulp_install_selinux_policies == "auto"); ansible_facts.mounts \| selectattr('mount', 'match', '^/var/lib/pulp$') \| list \| count == 1; fstab.changed |
| 1782 | 71.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → selinux_remount.yml → Remount the static dir (rescue) | Remount /var/lib/pulp/pulpcore_static | Remount /var/lib/pulp/pulpcore_static | pulp_install_selinux_policies\|bool or (ansible_facts.selinux.status == "enabled" and pulp_install_selinux_policies == "auto"); ansible_facts.mounts \| selectattr('mount', 'match', '^/var/lib/pulp$') \| list \| count == 1; fstab.changed; static_dir_unmount.changed |
| 1783 | 71.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → selinux_remount.yml → Remount the static dir (rescue) | Start pulpcore service | Start pulpcore service (service `pulpcore.service`) | pulp_install_selinux_policies\|bool or (ansible_facts.selinux.status == "enabled" and pulp_install_selinux_policies == "auto"); ansible_facts.mounts \| selectattr('mount', 'match', '^/var/lib/pulp$') \| list \| count == 1; fstab.changed |
| 1784 | 71.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml | import_tasks | Import a task file (static include) (file `configure.yml`) | — |
| 1785 | 71.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml | file `configure.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/configure.yml` | — |
| 1786 | 71.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml | Obtain current pulp settings | Begin conditional or grouped task block | _aap24_upgrade \| bool |
| 1787 | 71.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → Obtain current pulp settings | Check if pulp configuration file exists | Check if pulp configuration file exists | _aap24_upgrade \| bool |
| 1788 | 72.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → Obtain current pulp settings | Obtain current pulp settings | Obtain current pulp settings | _aap24_upgrade \| bool; _pulp_settings_file.stat.exists \| bool |
| 1789 | 72.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml | Create configuration directory for Pulp | Create configuration directory for Pulp | — |
| 1790 | 72.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml | Create configuration file for Pulp | Create configuration file for Pulp (dest `{{ pulp_settings_file }}`) | — |
| 1791 | 72.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml | copy external auth settings when upgrading from AAP 2.4 | copy external auth settings when upgrading from AAP 2.4 (file `copy_external_auth_settings.yml`) | _aap24_upgrade \| bool |
| 1792 | 72.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → copy_external_auth_settings.yml | file `copy_external_auth_settings.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/copy_external_auth_settings.yml` | _aap24_upgrade \| bool |
| 1793 | 72.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → copy_external_auth_settings.yml | Obtain external auth settings (SSO) | Begin conditional or grouped task block | _aap24_upgrade \| bool; _aap24_pulp_settings.get('content', '') \| b64decode \| regex_search('KEYCLOAK_HOST') |
| 1794 | 72.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → copy_external_auth_settings.yml → Obtain external auth settings (SSO) | Load SSO variables | Load SSO variables | _aap24_upgrade \| bool; _aap24_pulp_settings.get('content', '') \| b64decode \| regex_search('KEYCLOAK_HOST') |
| 1795 | 72.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → copy_external_auth_settings.yml → Obtain external auth settings (SSO) | ansible.builtin.set_fact | Run `ansible.builtin.set_fact` | _aap24_upgrade \| bool; _aap24_pulp_settings.get('content', '') \| b64decode \| regex_search('KEYCLOAK_HOST'); item \| split \| first \| lower in sso_settings_for_upgrade |
| 1796 | 72.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → copy_external_auth_settings.yml | Obtain external auth settings (LDAP) | Begin conditional or grouped task block | _aap24_upgrade \| bool; _aap24_pulp_settings.get('content', '') \| b64decode \| regex_search('AUTHENTICATION_BACKEND_PRESET.*ldap') |
| 1797 | 72.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → copy_external_auth_settings.yml → Obtain external auth settings (LDAP) | ansible.builtin.set_fact | Run `ansible.builtin.set_fact` | _aap24_upgrade \| bool; _aap24_pulp_settings.get('content', '') \| b64decode \| regex_search('AUTHENTICATION_BACKEND_PRESET.*ldap') |
| 1798 | 72.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → copy_external_auth_settings.yml → Obtain external auth settings (LDAP) | ansible.builtin.set_fact | Run `ansible.builtin.set_fact` | _aap24_upgrade \| bool; _aap24_pulp_settings.get('content', '') \| b64decode \| regex_search('AUTHENTICATION_BACKEND_PRESET.*ldap'); item.startswith('AUTH_LDAP') |
| 1799 | 72.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → copy_external_auth_settings.yml | Gather external auth setting | Gather external auth setting | _aap24_upgrade \| bool |
| 1800 | 72.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → copy_external_auth_settings.yml | Update pulp settings file | Update pulp settings file | _aap24_upgrade \| bool; _hub_external_auth \| length |
| 1801 | 72.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → copy_external_auth_settings.yml | Save external auth settings | Save external auth settings | _aap24_upgrade \| bool; _hub_external_auth \| length |
| 1802 | 72.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml | Create local configuration file for Pulp | Create local configuration file for Pulp | — |
| 1803 | 72.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml | Create wrapper to call pulpcore-manager | Create wrapper to call pulpcore-manager (dest `/usr/local/bin/pulpcore-manager`) | — |
| 1804 | 72.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml | Set up galaxy-importer config | Set up galaxy-importer config (file `galaxy_importer.yml`) | galaxy_importer_settings is defined |
| 1805 | 72.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → galaxy_importer.yml | file `galaxy_importer.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/galaxy_importer.yml` | galaxy_importer_settings is defined |
| 1806 | 72.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → galaxy_importer.yml | Create galaxy-importer directory | Create galaxy-importer directory | galaxy_importer_settings is defined |
| 1807 | 72.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → galaxy_importer.yml | Write galaxy-importer config | Write galaxy-importer config (dest `/etc/galaxy-importer/galaxy-importer.cfg`) | galaxy_importer_settings is defined |
| 1808 | 72.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml | Set up API access log | Set up API access log (file `setup_galaxy_api_access_log.yml`) | — |
| 1809 | 72.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → setup_galaxy_api_access_log.yml | file `setup_galaxy_api_access_log.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/setup_galaxy_api_access_log.yml` | — |
| 1810 | 72.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → setup_galaxy_api_access_log.yml | Create Galaxy api access log file | Create Galaxy api access log file | — |
| 1811 | 72.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → setup_galaxy_api_access_log.yml | Populate service facts | Populate service facts | — |
| 1812 | 72.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → setup_galaxy_api_access_log.yml | Set environment variable for Galaxy api access logging | Set environment variable for Galaxy api access logging | — |
| 1813 | 73.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → setup_galaxy_api_access_log.yml | Trigger pulpcore-api restart if service exists | Trigger pulpcore-api restart if service exists | ansible_facts.services['pulpcore-api.service'] is defined |
| 1814 | 73.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml | Set gpg socket directory for Hub HA | Set gpg socket directory for Hub HA | groups['automationhub'] \| length > 1; not pulp_gnupg_socket_dir \| default('') \| length |
| 1815 | 73.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml | Override gpg socket directory | Begin conditional or grouped task block | pulp_gnupg_socket_dir \| default('') \| length |
| 1816 | 73.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → Override gpg socket directory | Create gpg socket directory | Create gpg socket directory | pulp_gnupg_socket_dir \| default('') \| length |
| 1817 | 73.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → Override gpg socket directory | Create tmpfiles.d entry for gpg socket directory | Create tmpfiles.d entry for gpg socket directory | pulp_gnupg_socket_dir \| default('') \| length |
| 1818 | 73.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → Override gpg socket directory | Create {{ pulp_user }} gpg directory | Create {{ pulp_user }} gpg directory | pulp_gnupg_socket_dir \| default('') \| length |
| 1819 | 73.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → Override gpg socket directory | Set gpg socket path | Set gpg socket path | pulp_gnupg_socket_dir \| default('') \| length |
| 1820 | 73.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml | Set up collection signing | Set up collection signing (file `collection_signing_service.yml`) | automationhub_create_default_collection_signing_service \| bool |
| 1821 | 73.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → collection_signing_service.yml | file `collection_signing_service.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/collection_signing_service.yml` | automationhub_create_default_collection_signing_service \| bool |
| 1822 | 73.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → collection_signing_service.yml | Import the collection signing service gpg key file | Import the collection signing service gpg key file (dest `{{ pulp_certs_dir }}/galaxy_signing_service.gpg`) | automationhub_create_default_collection_signing_service \| bool |
| 1823 | 73.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → collection_signing_service.yml | Import the collection signing service script | Import the collection signing service script (dest `{{ pulp_scripts_dir }}/collection_sign.sh`) | automationhub_create_default_collection_signing_service \| bool |
| 1824 | 73.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → collection_signing_service.yml | Commands run as pulp_user | Begin conditional or grouped task block | automationhub_create_default_collection_signing_service \| bool |
| 1825 | 73.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → collection_signing_service.yml → Commands run as pulp_user | Inspect collection signing service GPG key file | Inspect collection signing service GPG key file | automationhub_create_default_collection_signing_service \| bool |
| 1826 | 73.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → collection_signing_service.yml → Commands run as pulp_user | Import collection signing service GPG key file | Import collection signing service GPG key file | automationhub_create_default_collection_signing_service \| bool |
| 1827 | 73.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → collection_signing_service.yml → Commands run as pulp_user | Trust the collection signing service GPG key | Trust the collection signing service GPG key | automationhub_create_default_collection_signing_service \| bool |
| 1828 | 73.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml | Set up container signing | Set up container signing (file `container_signing_service.yml`) | automationhub_create_default_container_signing_service \| bool |
| 1829 | 73.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → container_signing_service.yml | file `container_signing_service.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/container_signing_service.yml` | automationhub_create_default_container_signing_service \| bool |
| 1830 | 73.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → container_signing_service.yml | Import the container signing service gpg key file | Import the container signing service gpg key file (dest `{{ pulp_certs_dir }}/container_signing_service.gpg`) | automationhub_create_default_container_signing_service \| bool |
| 1831 | 73.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → container_signing_service.yml | Import the container signing service script | Import the container signing service script (dest `{{ pulp_scripts_dir }}/container_sign.sh`) | automationhub_create_default_container_signing_service \| bool |
| 1832 | 73.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → container_signing_service.yml | Commands run as pulp_user | Begin conditional or grouped task block | automationhub_create_default_container_signing_service \| bool |
| 1833 | 73.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → container_signing_service.yml → Commands run as pulp_user | Inspect container signing service GPG key file | Inspect container signing service GPG key file | automationhub_create_default_container_signing_service \| bool |
| 1834 | 73.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → container_signing_service.yml → Commands run as pulp_user | Import container signing service GPG key file | Import container signing service GPG key file | automationhub_create_default_container_signing_service \| bool |
| 1835 | 73.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → container_signing_service.yml → Commands run as pulp_user | Trust the container signing service GPG key | Trust the container signing service GPG key | automationhub_create_default_container_signing_service \| bool |
| 1836 | 73.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml | Install pulpcore service | Install pulpcore service (dest `/lib/systemd/system/pulpcore.service`) | — |
| 1837 | 73.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml | Flush handlers | Flush handlers (flush_handlers) | — |
| 1838 | 74.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml | Set state of pulpcore app (block) | Begin conditional or grouped task block | — |
| 1839 | 74.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → Set state of pulpcore app (block) | Set state of pulpcore app | Set state of pulpcore app (service `pulpcore.service`) | — |
| 1840 | 74.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml | Set state of pulpcore app (block) (rescue) | Run rescue tasks for block | — |
| 1841 | 74.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → Set state of pulpcore app (block) (rescue) | Set state of pulpcore app (retry) | Set state of pulpcore app (retry) (service `pulpcore.service`) | — |
| 1842 | 74.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → Set state of pulpcore app (block) (rescue) | Print status of pulpcore services and fail | Print status of pulpcore services and fail (file `print_status_and_fail.yml`) | result_restart_service is not defined |
| 1843 | 74.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → Set state of pulpcore app (block) (rescue) → print_status_and_fail.yml | file `print_status_and_fail.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/print_status_and_fail.yml` | result_restart_service is not defined |
| 1844 | 74.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → Set state of pulpcore app (block) (rescue) → print_status_and_fail.yml | Get status of pulp services | Get status of pulp services | result_restart_service is not defined |
| 1845 | 74.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → Set state of pulpcore app (block) (rescue) → print_status_and_fail.yml | Show output of pulp services | Show output of pulp services | result_restart_service is not defined |
| 1846 | 74.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → Set state of pulpcore app (block) (rescue) → print_status_and_fail.yml | Fail | Fail | result_restart_service is not defined |
| 1847 | 74.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub | Configure certificates | Configure certificates (file `certificates.yml`) | — |
| 1848 | 74.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml | file `certificates.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/certificates.yml` | — |
| 1849 | 74.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml | Create cert directory to hold certificates | Create cert directory to hold certificates | — |
| 1850 | 74.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml | Configure SSL certificates for web server | Begin conditional or grouped task block | not automationhub_disable_https \| bool |
| 1851 | 74.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Configure SSL certificates for web server | Copy user provided SSL cert | Begin conditional or grouped task block | not automationhub_disable_https \| bool; automationhub_ssl_cert \| default('') \| length |
| 1852 | 74.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Configure SSL certificates for web server → Copy user provided SSL cert | Import specified TLS certificate | Import specified TLS certificate | not automationhub_disable_https \| bool; automationhub_ssl_cert \| default('') \| length |
| 1853 | 74.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Configure SSL certificates for web server → Copy user provided SSL cert | Import specified TLS private key | Import specified TLS private key | not automationhub_disable_https \| bool; automationhub_ssl_cert \| default('') \| length |
| 1854 | 74.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Configure SSL certificates for web server | Use internal CA | Use internal CA (role `certificate_authority`, tasks `sign_service.yml`) | not automationhub_disable_https \| bool; not automationhub_ssl_cert \| default('') \| length |
| 1855 | 74.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Configure SSL certificates for web server → certificate_authority | role `certificate_authority` (`sign_service.yml`) | Enter role task list | not automationhub_disable_https \| bool; not automationhub_ssl_cert \| default('') \| length |
| 1856 | 74.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Configure SSL certificates for web server → certificate_authority → certificate_authority | file `sign_service.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/sign_service.yml` | not automationhub_disable_https \| bool; not automationhub_ssl_cert \| default('') \| length |
| 1857 | 74.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Configure SSL certificates for web server → certificate_authority → certificate_authority | include_tasks | Include a task file from the current role or playbook (file `init.yml`) | not automationhub_disable_https \| bool; not automationhub_ssl_cert \| default('') \| length |
| 1858 | 74.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Configure SSL certificates for web server → certificate_authority → certificate_authority → init.yml | file `init.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/init.yml` | not automationhub_disable_https \| bool; not automationhub_ssl_cert \| default('') \| length |
| 1859 | 74.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Configure SSL certificates for web server → certificate_authority → certificate_authority → init.yml | Ensure python cryptography libraries are installed | Ensure python cryptography libraries are installed | not automationhub_disable_https \| bool; not automationhub_ssl_cert \| default('') \| length |
| 1860 | 74.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Configure SSL certificates for web server → certificate_authority → certificate_authority | Create {{ aap_ca_servicename }} private key | Create {{ aap_ca_servicename }} private key | not automationhub_disable_https \| bool; not automationhub_ssl_cert \| default('') \| length |
| 1861 | 74.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Configure SSL certificates for web server → certificate_authority → certificate_authority | set_fact | Set a host fact used by later tasks | not automationhub_disable_https \| bool; not automationhub_ssl_cert \| default('') \| length |
| 1862 | 74.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Configure SSL certificates for web server → certificate_authority → certificate_authority | Check if {{ aap_ca_servicename }} public key exists | Check if {{ aap_ca_servicename }} public key exists | not automationhub_disable_https \| bool; not automationhub_ssl_cert \| default('') \| length |
| 1863 | 75.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Configure SSL certificates for web server → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | not automationhub_disable_https \| bool; not automationhub_ssl_cert \| default('') \| length; certificate_stat.stat.exists \| bool |
| 1864 | 75.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Configure SSL certificates for web server → certificate_authority → certificate_authority → block | Check contents of {{ aap_ca_servicename }} public key | Check contents of {{ aap_ca_servicename }} public key | not automationhub_disable_https \| bool; not automationhub_ssl_cert \| default('') \| length; certificate_stat.stat.exists \| bool |
| 1865 | 75.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Configure SSL certificates for web server → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | not automationhub_disable_https \| bool; not automationhub_ssl_cert \| default('') \| length; certificate_stat.stat.exists \| bool; aap_service_san_records_internal \| union(aap_service_san_records) \| difference(certificate_check.subject_alt_name) \| length > 0 |
| 1866 | 75.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Configure SSL certificates for web server → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | not automationhub_disable_https \| bool; not automationhub_ssl_cert \| default('') \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1867 | 75.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Configure SSL certificates for web server → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | not automationhub_disable_https \| bool; not automationhub_ssl_cert \| default('') \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false); aap_service_is_client |
| 1868 | 75.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Configure SSL certificates for web server → certificate_authority → certificate_authority → block | Create CSR for {{ aap_ca_servicename }} node/s | Create CSR for {{ aap_ca_servicename }} node/s | not automationhub_disable_https \| bool; not automationhub_ssl_cert \| default('') \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1869 | 75.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Configure SSL certificates for web server → certificate_authority → certificate_authority → block | Sign {{ aap_ca_servicename }} public key | Sign {{ aap_ca_servicename }} public key | not automationhub_disable_https \| bool; not automationhub_ssl_cert \| default('') \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1870 | 75.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Configure SSL certificates for web server → certificate_authority → certificate_authority → block | Write public key to {{ aap_ca_servicename }} node/s | Write public key to {{ aap_ca_servicename }} node/s (dest `{{ aap_service_cert_fullpath }}`) | not automationhub_disable_https \| bool; not automationhub_ssl_cert \| default('') \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1871 | 75.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Configure SSL certificates for web server → certificate_authority → certificate_authority | Signal restart if certificate pair changes | Signal restart if certificate pair changes | not automationhub_disable_https \| bool; not automationhub_ssl_cert \| default('') \| length; (write_key is changed or write_certificate is changed) |
| 1872 | 75.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Configure SSL certificates for web server | Trigger nginx notifier for internally managed certificate changes | Trigger nginx notifier for internally managed certificate changes | not automationhub_disable_https \| bool; aap_service_restart \| default(false) |
| 1873 | 75.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Configure SSL certificates for web server | set_fact | Set a host fact used by later tasks | not automationhub_disable_https \| bool |
| 1874 | 75.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml | Copy pg client certs for verify-ca | Begin conditional or grouped task block | automationhub_pg_cert_auth |
| 1875 | 75.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Copy pg client certs for verify-ca | Copy postgres client cert | Copy postgres client cert (dest `{{ pulp_certs_dir }}/pg_client.crt`) | automationhub_pg_cert_auth |
| 1876 | 75.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Copy pg client certs for verify-ca | Copy postgres client key | Copy postgres client key (dest `{{ pulp_certs_dir }}/pg_client.key`) | automationhub_pg_cert_auth |
| 1877 | 75.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub | Configure database for Pulp | Configure database for Pulp (file `pulp_database_config.yml`) | — |
| 1878 | 75.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml | file `pulp_database_config.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/pulp_database_config.yml` | — |
| 1879 | 75.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml | Pick & set the correct host to run pulp_database_config based on existing keys | Begin conditional or grouped task block | not pulp_db_fields_key \| length |
| 1880 | 75.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml → Pick & set the correct host to run pulp_database_config based on existing keys | Check if any hosts already have the database fields encryption key | Check if any hosts already have the database fields encryption key | not pulp_db_fields_key \| length |
| 1881 | 75.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml → Pick & set the correct host to run pulp_database_config based on existing keys | Pick & set the sole host used to run the remainder of the pulp_database_config role, if any hosts have the key | Pick & set the sole host used to run the remainder of the pulp_database_config role, if any hosts have the key | not pulp_db_fields_key \| length; item.stat.exists |
| 1882 | 75.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml | Pick & set the sole host to run pulp_database_config if no host has the key, or if the key is to be imported | Pick & set the sole host to run pulp_database_config if no host has the key, or if the key is to be imported | hostvars['localhost']['pulp_database_config_host'] is not defined |
| 1883 | 75.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml | Import Pulp Database Encryption Key | Import Pulp Database Encryption Key (dest `{{ __pulp_common_merged_pulp_settings.db_encryption_key }}`) | pulp_db_fields_key \| length |
| 1884 | 75.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml | Generate Pulp Database Encryption Key | Begin conditional or grouped task block | not pulp_db_fields_key \| length |
| 1885 | 75.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml → Generate Pulp Database Encryption Key | import_tasks | Import a task file (static include) (file `generate_database_fields_key.yml`) | not pulp_db_fields_key \| length |
| 1886 | 75.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml → Generate Pulp Database Encryption Key → generate_database_fields_key.yml | file `generate_database_fields_key.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/generate_database_fields_key.yml` | not pulp_db_fields_key \| length |
| 1887 | 76.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml → Generate Pulp Database Encryption Key → generate_database_fields_key.yml | Check for existing Pulp Database Encryption Key | Check for existing Pulp Database Encryption Key | not pulp_db_fields_key \| length |
| 1888 | 76.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml → Generate Pulp Database Encryption Key → generate_database_fields_key.yml | Install the openssl command | Install the openssl command | not pulp_db_fields_key \| length; not __pulp_db_fields_key_file.stat.exists |
| 1889 | 76.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml → Generate Pulp Database Encryption Key → generate_database_fields_key.yml | Generate Pulp Database Encryption Key | Generate Pulp Database Encryption Key | not pulp_db_fields_key \| length; not __pulp_db_fields_key_file.stat.exists |
| 1890 | 76.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml → Generate Pulp Database Encryption Key → generate_database_fields_key.yml | Ensure correct permissions on new Pulp Database Encryption Key | Ensure correct permissions on new Pulp Database Encryption Key | not pulp_db_fields_key \| length |
| 1891 | 76.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml → Generate Pulp Database Encryption Key | Get the database fields key | Get the database fields key | not pulp_db_fields_key \| length |
| 1892 | 76.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml → Generate Pulp Database Encryption Key | Copy Pulp Database Encryption Key | Copy Pulp Database Encryption Key (dest `{{ __pulp_common_merged_pulp_settings.db_encryption_key }}`) | not pulp_db_fields_key \| length |
| 1893 | 76.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml | Run database migrations, manage the admin account and set sync task | Begin conditional or grouped task block | — |
| 1894 | 76.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml → Run database migrations, manage the admin account and set sync task | Run database migrations | Run database migrations | — |
| 1895 | 76.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml → Run database migrations, manage the admin account and set sync task | Check if admin account has been created | Check if admin account has been created | — |
| 1896 | 76.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml → Run database migrations, manage the admin account and set sync task | Set the Pulp admin user's password | Set the Pulp admin user's password | ((__pulp_database_config_admin_account_exists.stdout == "False") \| default(false)) or automationhub_force_change_admin_password \| bool |
| 1897 | 76.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml → Run database migrations, manage the admin account and set sync task | Schedule resource sync task | Schedule resource sync task | — |
| 1898 | 76.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml | Run the pulpcore-manager container-repair-media-type command (packages mode) | Run the pulpcore-manager container-repair-media-type command (packages mode) | pulp_container_old_version is defined; automationhub_container_repair_media_type \| bool or (automationhub_container_repair_media_type == "auto" and ((pulp_container_old_version is version("2.9.5", "<=")) or (pulp_container_old_version is version("2.10.0", ">=") and pulp_container_old_version is version("2.10.4", "<=")) or (pulp_container_old_version is version("2.11.0", ">=") and pulp_container_old_version is version("2.11.1", "<=")) or (pulp_container_old_version is version("2.12.0", ">=") and pulp_container_old_version is version("2.12.2", "<=")) or pulp_container_old_version is version("2.13.0", "==")) and ((ansible_facts.packages[pulp_pkg_name_prefix + 'pulp-container'][0].version is version("2.9.7", ">=") and ansible_facts.packages[pulp_pkg_name_prefix + 'pulp-container'][0].version is version("2.10.0", "<") ) or (ansible_facts.packages[pulp_pkg_name_prefix + 'pulp-container'][0].version is version("2.10.8", ">=") and ansible_facts.packages[pulp_pkg_name_prefix + 'pulp-container'][0].version is version("2.11.0", "<")) or (ansible_facts.packages[pulp_pkg_name_prefix + 'pulp-container'][0].version is version("2.11.1", ">=") and ansible_facts.packages[pulp_pkg_name_prefix + 'pulp-container'][0].version is version("2.12.0", "<")) or (ansible_facts.packages[pulp_pkg_name_prefix + 'pulp-container'][0].version is version("2.12.3", ">=") and ansible_facts.packages[pulp_pkg_name_prefix + 'pulp-container'][0].version is version("2.13.0", "<")) or ansible_facts.packages[pulp_pkg_name_prefix + 'pulp-container'][0].version is version("2.13.2", ">=")) ) |
| 1899 | 76.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml | Show output from the repair command | Show output from the repair command | — |
| 1900 | 76.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml | Add the galaxy collection signing service to the Pulp application | Add the galaxy collection signing service to the Pulp application | automationhub_create_default_collection_signing_service |
| 1901 | 76.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml | Add the galaxy container signing service to the Pulp application | Add the galaxy container signing service to the Pulp application | automationhub_create_default_container_signing_service |
| 1902 | 76.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub | Set up Pulp API | Set up Pulp API (file `pulp_api.yml`) | — |
| 1903 | 76.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_api.yml | file `pulp_api.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/pulp_api.yml` | — |
| 1904 | 76.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_api.yml | Ensure python-cryptography is installed | Ensure python-cryptography is installed | — |
| 1905 | 76.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_api.yml | import_tasks | Import a task file (static include) (file `generate_token_auth_key.yml`) | pulp_token_auth_key is undefined |
| 1906 | 76.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_api.yml → generate_token_auth_key.yml | file `generate_token_auth_key.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/generate_token_auth_key.yml` | pulp_token_auth_key is undefined |
| 1907 | 76.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_api.yml → generate_token_auth_key.yml | Check if any of said hosts already have the token authentication private key | Check if any of said hosts already have the token authentication private key | pulp_token_auth_key is undefined |
| 1908 | 76.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_api.yml → generate_token_auth_key.yml | Pick & set the host to copy the token auth private key from, if any hosts have the key | Pick & set the host to copy the token auth private key from, if any hosts have the key | pulp_token_auth_key is undefined; hostvars['localhost']['__pulp_token_auth_key_host'] is not defined; item.stat.exists |
| 1909 | 76.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_api.yml → generate_token_auth_key.yml | Pick & set the sole host to generate the token auth private key if no host has the key | Pick & set the sole host to generate the token auth private key if no host has the key | pulp_token_auth_key is undefined; hostvars['localhost']['__pulp_token_auth_key_host'] is not defined |
| 1910 | 76.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_api.yml → generate_token_auth_key.yml | Generate the token auth private key | Generate the token auth private key | pulp_token_auth_key is undefined |
| 1911 | 76.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_api.yml → generate_token_auth_key.yml | Get the token auth private key from the host that has it or that was used to generate it | Get the token auth private key from the host that has it or that was used to generate it | pulp_token_auth_key is undefined |
| 1912 | 77.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_api.yml → generate_token_auth_key.yml | Copy the token auth private key to all the other hosts | Copy the token auth private key to all the other hosts (dest `{{ pulp_certs_dir }}/token_private_key.pem`) | pulp_token_auth_key is undefined |
| 1913 | 77.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_api.yml | import_tasks | Import a task file (static include) (file `import_token_auth_key.yml`) | pulp_token_auth_key is defined |
| 1914 | 77.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_api.yml → import_token_auth_key.yml | file `import_token_auth_key.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/import_token_auth_key.yml` | pulp_token_auth_key is defined |
| 1915 | 77.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_api.yml → import_token_auth_key.yml | Import specified token authentication key | Import specified token authentication key (dest `{{ pulp_certs_dir }}/token_private_key.pem`) | pulp_token_auth_key is defined |
| 1916 | 77.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_api.yml | Extract token authentication public key | Extract token authentication public key | — |
| 1917 | 77.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_api.yml | Install pulpcore-api service files | Install pulpcore-api service files (dest `/lib/systemd/system/pulpcore-api.service`) | — |
| 1918 | 77.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_api.yml | Set the pulpcore-api service state | Set the pulpcore-api service state (service `pulpcore-api.service`) | — |
| 1919 | 77.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub | Set up Pulp Content | Set up Pulp Content (file `pulp_content.yml`) | — |
| 1920 | 77.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_content.yml | file `pulp_content.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/pulp_content.yml` | — |
| 1921 | 77.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_content.yml | Install pulpcore-content service file | Install pulpcore-content service file (dest `/lib/systemd/system/pulpcore-content.service`) | — |
| 1922 | 77.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_content.yml | Set state of pulp content app | Set state of pulp content app (service `pulpcore-content.service`) | — |
| 1923 | 77.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub | Set up Pulp Workers | Set up Pulp Workers (file `pulp_workers.yml`) | — |
| 1924 | 77.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_workers.yml | file `pulp_workers.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/pulp_workers.yml` | — |
| 1925 | 77.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_workers.yml | Install service files | Install service files (dest `/lib/systemd/system/pulpcore-worker@.service`) | — |
| 1926 | 77.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_workers.yml | Get services | Get services | — |
| 1927 | 77.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_workers.yml | pulpcore resource manager | Begin conditional or grouped task block | ansible_facts.services['pulpcore-resource-manager.service'] is defined; __pulpcore_version is version('3.14', '>=') |
| 1928 | 77.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_workers.yml → pulpcore resource manager | Disable pulpcore-resource-manager service | Disable pulpcore-resource-manager service (service `pulpcore-resource-manager.service`) | ansible_facts.services['pulpcore-resource-manager.service'] is defined; __pulpcore_version is version('3.14', '>=') |
| 1929 | 77.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_workers.yml → pulpcore resource manager | Remove pulpcore-resource-manager service | Remove pulpcore-resource-manager service | ansible_facts.services['pulpcore-resource-manager.service'] is defined; __pulpcore_version is version('3.14', '>=') |
| 1930 | 77.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_workers.yml → pulpcore resource manager | Reload systemd for the pulpcore-resource-manager removal | Reload systemd for the pulpcore-resource-manager removal | ansible_facts.services['pulpcore-resource-manager.service'] is defined; __pulpcore_version is version('3.14', '>=') |
| 1931 | 77.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_workers.yml | Get already existing workers | Get already existing workers | — |
| 1932 | 77.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_workers.yml | Get the highest worker ID | Get the highest worker ID | __pulp_workers_worker_svcs is defined |
| 1933 | 77.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_workers.yml | Scale down workers | Scale down workers (service `pulpcore-worker@{{ __pulp_workers_worker_index }}.service`) | __pulp_workers_worker_max is defined; __pulp_workers_worker_max\|int + 1 > pulp_workers + 1 |
| 1934 | 77.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_workers.yml | Set states of new pulp workers | Set states of new pulp workers (service `pulpcore-worker@{{ __pulp_workers_worker_index }}.service`) | — |
| 1935 | 77.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub | Configure Nginx for automation hub | Configure Nginx for automation hub (file `nginx.yml`) | — |
| 1936 | 77.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub → nginx.yml | file `nginx.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/nginx.yml` | — |
| 1937 | 78.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → nginx.yml | Create static dir & ACME dir | Create static dir & ACME dir | — |
| 1938 | 78.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → nginx.yml | Create directory for Pulp Nginx snippets | Create directory for Pulp Nginx snippets | — |
| 1939 | 78.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub → nginx.yml | Find pulp obsolete symlinks | Find pulp obsolete symlinks | — |
| 1940 | 78.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub → nginx.yml | Remove pulp obsolete symlinks | Remove pulp obsolete symlinks | — |
| 1941 | 78.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub → nginx.yml | Set python interpreter | Set python interpreter | — |
| 1942 | 78.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub → nginx.yml | Get webserver snippets path | Get webserver snippets path | — |
| 1943 | 78.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub → nginx.yml | Install webserver snippets | Install webserver snippets | — |
| 1944 | 78.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub → nginx.yml | Replace X-Forwarded-Proto header | Replace X-Forwarded-Proto header | — |
| 1945 | 78.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub → nginx.yml | Add automation hub nginx configuration | Add automation hub nginx configuration | — |
| 1946 | 78.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub → nginx.yml | Configure UI redirect page | Begin conditional or grouped task block | — |
| 1947 | 78.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → nginx.yml → Configure UI redirect page | Ensure directory exists for UI redirect | Ensure directory exists for UI redirect | — |
| 1948 | 78.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → nginx.yml → Configure UI redirect page | Copy index.html | Copy index.html | — |
| 1949 | 78.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub → nginx.yml → Configure UI redirect page | Ensure static media directory exists | Ensure static media directory exists | — |
| 1950 | 78.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub → nginx.yml → Configure UI redirect page | Copy logo file | Copy logo file | — |
| 1951 | 78.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub → nginx.yml | Trigger pulpcore-api restart on all hosts | Trigger pulpcore-api restart on all hosts | _hub_redirect_result is changed |
| 1952 | 78.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub | Create the automation-hub service symlink | Create the automation-hub service symlink | — |
| 1953 | 78.6% | install.yml → Install Automation Hub node → block | include_role | Include and run tasks from an Ansible role (role `nginx`, tasks `finalize.yml`) | — |
| 1954 | 78.7% | install.yml → Install Automation Hub node → block → nginx | role `nginx` (`finalize.yml`) | Enter role task list | — |
| 1955 | 78.7% | install.yml → Install Automation Hub node → block → nginx → nginx | file `finalize.yml` | Task file `ansible/automation_platform_installer/roles/nginx/tasks/finalize.yml` | — |
| 1956 | 78.7% | install.yml → Install Automation Hub node → block → nginx → nginx | Install nginx.conf | Install nginx.conf (dest `/etc/nginx/nginx.conf`) | — |
| 1957 | 78.8% | install.yml → Install Automation Hub node → block → nginx → nginx | Start nginx and configure to startup automatically | Start nginx and configure to startup automatically (service `nginx`) | — |
| 1958 | 78.8% | install.yml → Install Automation Hub node → block | Enable firewall | Enable firewall (role `firewall`) | — |
| 1959 | 78.9% | install.yml → Install Automation Hub node → block → firewall | role `firewall` | Enter role task list | — |
| 1960 | 78.9% | install.yml → Install Automation Hub node → block → firewall → firewall | file `main.yml` | Task file `ansible/automation_platform_installer/roles/firewall/tasks/main.yml` | — |
| 1961 | 78.9% | install.yml → Install Automation Hub node → block → firewall → firewall | include_tasks | Include a task file from the current role or playbook (file `discover.yml`) | — |
| 1962 | 79.0% | install.yml → Install Automation Hub node → block → firewall → firewall → discover.yml | file `discover.yml` | Task file `ansible/automation_platform_installer/roles/firewall/tasks/discover.yml` | — |
| 1963 | 79.0% | install.yml → Install Automation Hub node → block → firewall → firewall → discover.yml | Determine if firewalld is running | Determine if firewalld is running | — |
| 1964 | 79.1% | install.yml → Install Automation Hub node → block → firewall → firewall → discover.yml | Determine if firewalld is enabled | Determine if firewalld is enabled | — |
| 1965 | 79.1% | install.yml → Install Automation Hub node → block → firewall → firewall → discover.yml | Determine if iptables is installed | Determine if iptables is installed | — |
| 1966 | 79.1% | install.yml → Install Automation Hub node → block → firewall → firewall → discover.yml | Determine if iptables service is installed | Determine if iptables service is installed | — |
| 1967 | 79.2% | install.yml → Install Automation Hub node → block → firewall → firewall → discover.yml | Check to see if there is an iptables config file | Check to see if there is an iptables config file | iptables_installed.rc == 0 |
| 1968 | 79.2% | install.yml → Install Automation Hub node → block → firewall → firewall → discover.yml | Select firewalld | Select firewalld | firewalld_running.rc == 0 or systemctl_unit_files.stdout_lines \| select('match','firewalld.service\s+enabled') \| list \| length == 1 |
| 1969 | 79.3% | install.yml → Install Automation Hub node → block → firewall → firewall → discover.yml | Select iptables | Select iptables | iptables_installed.rc == 0; iptables_exists.stat.exists; iptables_service.stat.exists |
| 1970 | 79.3% | install.yml → Install Automation Hub node → block → firewall → firewall | Include {{ 'no' if component_firewall_type == 'none' else component_firewall_type }} tasks | Include {{ 'no' if component_firewall_type == 'none' else component_firewall_type }} tasks (file `{{ component_firewall_type }}.yml`) | firewalld_running.rc == 0 or systemctl_unit_files.stdout_lines \| select('match','firewalld.service\s+enabled') \| list \| length == 1 or ( iptables_installed.rc == 0 and iptables_exists.stat.exists and iptables_service.stat.exists ) |
| 1971 | 79.3% | install.yml → Install Automation Hub node → block → firewall → firewall → {{ component_firewall_type }}.yml | {{ component_firewall_type }}.yml | Include task file `{{ component_firewall_type }}.yml` (relative to current role task directory) | firewalld_running.rc == 0 or systemctl_unit_files.stdout_lines \| select('match','firewalld.service\s+enabled') \| list \| length == 1 or ( iptables_installed.rc == 0 and iptables_exists.stat.exists and iptables_service.stat.exists ) |
| 1972 | 79.4% | install.yml → Install Automation Hub node | block (always) | Run always tasks for block | — |
| 1973 | 79.4% | install.yml → Install Automation Hub node → block (always) | Run repo_management (teardown) | Run repo_management (teardown) (role `repo_management`, tasks `teardown`) | — |
| 1974 | 79.5% | install.yml → Install Automation Hub node → block (always) → repo_management | role `repo_management` (`teardown`) | Enter role task list | — |
| 1975 | 79.5% | install.yml → Install Automation Hub node → block (always) → repo_management → repo_management | file `teardown.yml` | Task file `ansible/automation_platform_installer/roles/repo_management/tasks/teardown.yml` | — |
| 1976 | 79.5% | install.yml → Install Automation Hub node → block (always) → repo_management → repo_management | Disable Automation Platform repository | Disable Automation Platform repository | extra_repos is defined |
| 1977 | 79.6% | install.yml → Install Automation Hub node → block (always) → repo_management → repo_management | Disable Automation Platform RHSM repository | Disable Automation Platform RHSM repository | aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 1978 | 79.6% | install.yml | Install Automation EDA Controller | Play targeting `automationedacontroller` | — |
| 1979 | 79.7% | install.yml → Install Automation EDA Controller | block | Begin conditional or grouped task block | — |
| 1980 | 79.7% | install.yml → Install Automation EDA Controller → block | Run repo_management (setup) | Run repo_management (setup) (role `repo_management`, tasks `setup`) | — |
| 1981 | 79.7% | install.yml → Install Automation EDA Controller → block → repo_management | role `repo_management` (`setup`) | Enter role task list | — |
| 1982 | 79.8% | install.yml → Install Automation EDA Controller → block → repo_management → repo_management | file `setup.yml` | Task file `ansible/automation_platform_installer/roles/repo_management/tasks/setup.yml` | — |
| 1983 | 79.8% | install.yml → Install Automation EDA Controller → block → repo_management → repo_management | include_vars | Load variables from a file | — |
| 1984 | 79.9% | install.yml → Install Automation EDA Controller → block → repo_management → repo_management | Install yum repo keys | Install yum repo keys (dest `/etc/pki/rpm-gpg/{{ item }}`) | — |
| 1985 | 79.9% | install.yml → Install Automation EDA Controller → block → repo_management → repo_management | Install subscription-manager-rhsm-certificates | Install subscription-manager-rhsm-certificates | — |
| 1986 | 79.9% | install.yml → Install Automation EDA Controller → block → repo_management → repo_management | Determine if rhsm repository exists | Determine if rhsm repository exists | — |
| 1987 | 80.0% | install.yml → Install Automation EDA Controller → block → repo_management → repo_management | Enable Automation Platform rhsm repository | Enable Automation Platform rhsm repository | ansible_distribution == 'RedHat'; rhsm_repo.stat.exists; not bundle_install \| bool; aw_repo_url is undefined |
| 1988 | 80.0% | install.yml → Install Automation EDA Controller → block → repo_management → repo_management | Explain Automation Platform rhsm repository failure | Explain Automation Platform rhsm repository failure | aap_rhsm_repo_enable.failed \| default(false) |
| 1989 | 80.1% | install.yml → Install Automation EDA Controller → block → repo_management → repo_management | block | Begin conditional or grouped task block | aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 1990 | 80.1% | install.yml → Install Automation EDA Controller → block → repo_management → repo_management → block | Enable module hotfixes flag | Enable module hotfixes flag | aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 1991 | 80.1% | install.yml → Install Automation EDA Controller → block → repo_management → repo_management → block | Set AAP repo for update | Set AAP repo for update | aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 1992 | 80.2% | install.yml → Install Automation EDA Controller → block → repo_management → repo_management | Ensure rsync is installed | Ensure rsync is installed | bundle_install \| bool |
| 1993 | 80.2% | install.yml → Install Automation EDA Controller → block → repo_management → repo_management | Copy bundle packages to repo source directory | Begin conditional or grouped task block | bundle_install \| bool |
| 1994 | 80.3% | install.yml → Install Automation EDA Controller → block → repo_management → repo_management → Copy bundle packages to repo source directory | Copy bundle packages to repo source directory | Copy bundle packages to repo source directory | bundle_install \| bool |
| 1995 | 80.3% | install.yml → Install Automation EDA Controller → block → repo_management → repo_management | Copy bundle packages to repo source directory (rescue) | Run rescue tasks for block | bundle_install \| bool |
| 1996 | 80.3% | install.yml → Install Automation EDA Controller → block → repo_management → repo_management → Copy bundle packages to repo source directory (rescue) | Remove old bundle packages source directory (legacy) | Remove old bundle packages source directory (legacy) | bundle_install \| bool |
| 1997 | 80.4% | install.yml → Install Automation EDA Controller → block → repo_management → repo_management → Copy bundle packages to repo source directory (rescue) | Copy bundle packages to repo source directory (legacy) | Copy bundle packages to repo source directory (legacy) (dest `{{ bundle_install_folder }}`) | bundle_install \| bool |
| 1998 | 80.4% | install.yml → Install Automation EDA Controller → block → repo_management → repo_management | Install the Automation Platform bundle repository | Install the Automation Platform bundle repository (dest `/etc/yum.repos.d/ansible-automation-platform.repo`) | bundle_install \| bool |
| 1999 | 80.5% | install.yml → Install Automation EDA Controller → block → repo_management → repo_management | Remove legacy ansible-tower repository | Remove legacy ansible-tower repository | — |
| 2000 | 80.5% | install.yml → Install Automation EDA Controller → block → repo_management → repo_management | Install the Automation Platform yum repository. | Install the Automation Platform yum repository. (dest `/etc/yum.repos.d/ansible-automation-platform.repo`) | aw_repo_url is defined; not bundle_install \| bool |
| 2001 | 80.5% | install.yml → Install Automation EDA Controller → block → repo_management → repo_management | Set repo list for bundle install | Set repo list for bundle install | bundle_install \| bool |
| 2002 | 80.6% | install.yml → Install Automation EDA Controller → block → repo_management → repo_management | Set repo list for online install | Set repo list for online install | yum_repo is succeeded; yum_repo is not skipped |
| 2003 | 80.6% | install.yml → Install Automation EDA Controller → block → repo_management → repo_management | Enable Ansible Automation Platform repo | Enable Ansible Automation Platform repo | extra_repos is defined |
| 2004 | 80.7% | install.yml → Install Automation EDA Controller → block | include_role | Include and run tasks from an Ansible role (role `nginx`) | — |
| 2005 | 80.7% | install.yml → Install Automation EDA Controller → block → nginx | role `nginx` | Enter role task list | — |
| 2006 | 80.7% | install.yml → Install Automation EDA Controller → block → nginx → nginx | file `main.yml` | Task file `ansible/automation_platform_installer/roles/nginx/tasks/main.yml` | — |
| 2007 | 80.8% | install.yml → Install Automation EDA Controller → block → nginx → nginx | include_tasks | Include a task file from the current role or playbook (file `module.yml`) | — |
| 2008 | 80.8% | install.yml → Install Automation EDA Controller → block → nginx → nginx → module.yml | file `module.yml` | Task file `ansible/automation_platform_installer/roles/nginx/tasks/module.yml` | — |
| 2009 | 80.9% | install.yml → Install Automation EDA Controller → block → nginx → nginx → module.yml | Enable nginx module stream | Begin conditional or grouped task block | — |
| 2010 | 80.9% | install.yml → Install Automation EDA Controller → block → nginx → nginx → module.yml → Enable nginx module stream | Disable Nginx module | Disable Nginx module | — |
| 2011 | 81.0% | install.yml → Install Automation EDA Controller → block → nginx → nginx → module.yml → Enable nginx module stream | Enable Nginx module | Enable Nginx module | — |
| 2012 | 81.0% | install.yml → Install Automation EDA Controller → block → nginx → nginx | include_tasks | Include a task file from the current role or playbook (file `tasks.yml`) | — |
| 2013 | 81.0% | install.yml → Install Automation EDA Controller → block → nginx → nginx → tasks.yml | file `tasks.yml` | Task file `ansible/automation_platform_installer/roles/nginx/tasks/tasks.yml` | — |
| 2014 | 81.1% | install.yml → Install Automation EDA Controller → block → nginx → nginx → tasks.yml | Install nginx | Install nginx | — |
| 2015 | 81.1% | install.yml → Install Automation EDA Controller → block → nginx → nginx → tasks.yml | SELinux configuration for nginx | Begin conditional or grouped task block | ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 2016 | 81.2% | install.yml → Install Automation EDA Controller → block → nginx → nginx → tasks.yml → SELinux configuration for nginx | Ensure libsemanage-python is installed | Ensure libsemanage-python is installed | ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 2017 | 81.2% | install.yml → Install Automation EDA Controller → block → nginx → nginx → tasks.yml → SELinux configuration for nginx | Open up permissions on nginx. | Open up permissions on nginx. | ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 2018 | 81.2% | install.yml → Install Automation EDA Controller → block | include_role | Include and run tasks from an Ansible role (role `automationedacontroller`) | — |
| 2019 | 81.3% | install.yml → Install Automation EDA Controller → block → automationedacontroller | role `automationedacontroller` | Enter role task list | — |
| 2020 | 81.3% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | file `main.yml` | Task file `ansible/automation_platform_installer/roles/automationedacontroller/tasks/main.yml` | — |
| 2021 | 81.4% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | Load OS specific variables | Load OS specific variables | — |
| 2022 | 81.4% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | set_fact | Set a host fact used by later tasks | — |
| 2023 | 81.4% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | Map redis hostnames to Event-Driven Ansible | Map redis hostnames to Event-Driven Ansible | _redis_mode == "cluster" |
| 2024 | 81.5% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | Check installed version of PG | Check installed version of PG | — |
| 2025 | 81.5% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | block | Begin conditional or grouped task block | pg_installed_version.failed or pg_installed_version.stdout \| trim \| int != automationedacontroller_postgresql_version \| int |
| 2026 | 81.6% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → block | Enable PostgreSQL {{ automationedacontroller_postgresql_version }} dnf modules | Enable PostgreSQL {{ automationedacontroller_postgresql_version }} dnf modules | pg_installed_version.failed or pg_installed_version.stdout \| trim \| int != automationedacontroller_postgresql_version \| int |
| 2027 | 81.6% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → block | Install PostgreSQL client {{ automationedacontroller_postgresql_version }} | Install PostgreSQL client {{ automationedacontroller_postgresql_version }} | pg_installed_version.failed or pg_installed_version.stdout \| trim \| int != automationedacontroller_postgresql_version \| int |
| 2028 | 81.6% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | include_tasks | Include a task file from the current role or playbook (file `deprovision.yml`) | — |
| 2029 | 81.7% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → deprovision.yml | file `deprovision.yml` | Task file `ansible/automation_platform_installer/roles/automationedacontroller/tasks/deprovision.yml` | — |
| 2030 | 81.7% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → deprovision.yml | Remove old automation-eda-controller nginx config | Remove old automation-eda-controller nginx config | — |
| 2031 | 81.8% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → deprovision.yml | Ensure API nginx is not installed on worker nodes | Ensure API nginx is not installed on worker nodes | eda_node_type in ['event-stream', 'worker'] |
| 2032 | 81.8% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → deprovision.yml | Ensure event stream nginx is not installed on api or worker specific nodes | Ensure event stream nginx is not installed on api or worker specific nodes | eda_node_type in ['api', 'worker'] |
| 2033 | 81.8% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → deprovision.yml | block | Begin conditional or grouped task block | eda_node_type in ['api', 'event-stream'] |
| 2034 | 81.9% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → deprovision.yml → block | Ensure worker nginx is only installed on worker nodes | Ensure worker nginx is only installed on worker nodes | eda_node_type in ['api', 'event-stream'] |
| 2035 | 81.9% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → deprovision.yml → block | Stop and remove all running activations on non-worker nodes | Stop and remove all running activations on non-worker nodes | eda_node_type in ['api', 'event-stream']; ansible_facts.services['podman.service'] \| default([]) \| length; ansible_facts.services['podman.service'].state == 'running' |
| 2036 | 82.0% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → deprovision.yml → block | Disable linger on all non-worker nodes | Disable linger on all non-worker nodes | eda_node_type in ['api', 'event-stream'] |
| 2037 | 82.0% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → deprovision.yml → block | Ensure worker service RPMs are absent | Ensure worker service RPMs are absent | eda_node_type in ['api', 'event-stream'] |
| 2038 | 82.0% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → deprovision.yml | Remove event stream specific packages from non event stream nodes | Remove event stream specific packages from non event stream nodes | inventory_hostname not in _automationedacontroller_event_stream_hosts |
| 2039 | 82.1% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → deprovision.yml | Ensure standard service package is not installed on event stream only nodes | Ensure standard service package is not installed on event stream only nodes | eda_node_type == 'event-stream' |
| 2040 | 82.1% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | Check if EDA user exists | Check if EDA user exists | — |
| 2041 | 82.2% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | block | Begin conditional or grouped task block | eda_check_user is not changed |
| 2042 | 82.2% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → block | Ensure EDA user is part of systemd-journal group | Ensure EDA user is part of systemd-journal group | eda_check_user is not changed |
| 2043 | 82.2% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → block | Temporarily disable linger if EDA is not part of systemd-journal group | Temporarily disable linger if EDA is not part of systemd-journal group | eda_check_user is not changed; eda_systemd_journal_group is changed; inventory_hostname in _automationedacontroller_worker_hosts |
| 2044 | 82.3% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | Install automation-eda-controller | Install automation-eda-controller | — |
| 2045 | 82.3% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | Install base services RPM for all nodes except event stream only | Install base services RPM for all nodes except event stream only | eda_node_type != 'event-stream' |
| 2046 | 82.4% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | Install services RPM for worker nodes | Install services RPM for worker nodes | inventory_hostname in _automationedacontroller_worker_hosts |
| 2047 | 82.4% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | Install services RPM for event stream nodes | Install services RPM for event stream nodes | inventory_hostname in _automationedacontroller_event_stream_hosts |
| 2048 | 82.4% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | Update existing dependent packages | Update existing dependent packages (role `repo_management`, tasks `update_dependency`) | — |
| 2049 | 82.5% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → repo_management | role `repo_management` (`update_dependency`) | Enter role task list | — |
| 2050 | 82.5% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → repo_management → repo_management | file `update_dependency.yml` | Task file `ansible/automation_platform_installer/roles/repo_management/tasks/update_dependency.yml` | — |
| 2051 | 82.6% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → repo_management → repo_management | Ensure AAP repo for update is set | Ensure AAP repo for update is set | not aap_repo_for_update \| default('') \| length |
| 2052 | 82.6% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → repo_management → repo_management | Generate a list of AAP packages with an update | Generate a list of AAP packages with an update | — |
| 2053 | 82.6% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → repo_management → repo_management | Set AAP packages to update | Set AAP packages to update | — |
| 2054 | 82.7% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → repo_management → repo_management | Update packages | Update packages | _packages_to_update \| length |
| 2055 | 82.7% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | include_tasks | Include a task file from the current role or playbook (file `provision_worker.yml`) | inventory_hostname in _automationedacontroller_worker_hosts |
| 2056 | 82.8% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml | file `provision_worker.yml` | Task file `ansible/automation_platform_installer/roles/automationedacontroller/tasks/provision_worker.yml` | inventory_hostname in _automationedacontroller_worker_hosts |
| 2057 | 82.8% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml | Tune down number of gunicorn workers on worker nodes | Tune down number of gunicorn workers on worker nodes | inventory_hostname in _automationedacontroller_worker_hosts; eda_node_type == 'worker' |
| 2058 | 82.8% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml | Apply EDA worker nginx config | Apply EDA worker nginx config (dest `/etc/nginx/conf.d/automation-eda-controller-worker.conf`) | inventory_hostname in _automationedacontroller_worker_hosts |
| 2059 | 82.9% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml | Modify dependent service list for postgresql | Modify dependent service list for postgresql (role `systemd`, tasks `postgresql`) | inventory_hostname in _automationedacontroller_worker_hosts |
| 2060 | 82.9% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → systemd | role `systemd` (`postgresql`) | Enter role task list | inventory_hostname in _automationedacontroller_worker_hosts |
| 2061 | 83.0% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → systemd → systemd | file `postgresql.yml` | Task file `ansible/automation_platform_installer/roles/systemd/tasks/postgresql.yml` | inventory_hostname in _automationedacontroller_worker_hosts |
| 2062 | 83.0% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → systemd → systemd | block | Begin conditional or grouped task block | inventory_hostname in _automationedacontroller_worker_hosts; _internal_database \| bool |
| 2063 | 83.0% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → systemd → systemd → block | Create directory for service configuration override | Create directory for service configuration override | inventory_hostname in _automationedacontroller_worker_hosts; _internal_database \| bool |
| 2064 | 83.1% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → systemd → systemd → block | Add postgresql to dependent service list | Add postgresql to dependent service list | inventory_hostname in _automationedacontroller_worker_hosts; _internal_database \| bool |
| 2065 | 83.1% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → systemd → systemd | Ensure postgresql isn't set as a dependent service | Ensure postgresql isn't set as a dependent service | inventory_hostname in _automationedacontroller_worker_hosts; not _internal_database \| bool |
| 2066 | 83.2% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml | Make dependent services part of EDA target | Make dependent services part of EDA target (role `systemd`) | inventory_hostname in _automationedacontroller_worker_hosts |
| 2067 | 83.2% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → systemd | role `systemd` | Enter role task list | inventory_hostname in _automationedacontroller_worker_hosts |
| 2068 | 83.2% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → systemd → systemd | file `main.yml` | Task file `ansible/automation_platform_installer/roles/systemd/tasks/main.yml` | inventory_hostname in _automationedacontroller_worker_hosts |
| 2069 | 83.3% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → systemd → systemd | Create directory for dependent service configuration override | Create directory for dependent service configuration override | inventory_hostname in _automationedacontroller_worker_hosts |
| 2070 | 83.3% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → systemd → systemd | Add dependent service configuration override | Add dependent service configuration override | inventory_hostname in _automationedacontroller_worker_hosts |
| 2071 | 83.4% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml | Stop automation-eda-controller workers | Stop automation-eda-controller workers | inventory_hostname in _automationedacontroller_worker_hosts |
| 2072 | 83.4% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml | Enable automation-eda-controller workers | Enable automation-eda-controller workers (service `automation-eda-controller-default-worker@{{ item }}.service`) | inventory_hostname in _automationedacontroller_worker_hosts |
| 2073 | 83.4% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml | Enable automation-eda-controller activation workers | Enable automation-eda-controller activation workers (service `automation-eda-controller-activation-worker@{{ item }}.service`) | inventory_hostname in _automationedacontroller_worker_hosts |
| 2074 | 83.5% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml | include_role | Include and run tasks from an Ansible role (role `podman`) | inventory_hostname in _automationedacontroller_worker_hosts |
| 2075 | 83.5% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman | role `podman` | Enter role task list | inventory_hostname in _automationedacontroller_worker_hosts |
| 2076 | 83.6% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman | file `main.yml` | Task file `ansible/automation_platform_installer/roles/podman/tasks/main.yml` | inventory_hostname in _automationedacontroller_worker_hosts |
| 2077 | 83.6% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman | Allocate subuids for {{ run_podman_as }} user | Allocate subuids for {{ run_podman_as }} user | inventory_hostname in _automationedacontroller_worker_hosts |
| 2078 | 83.6% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman | Allocate subgids for {{ run_podman_as }} user | Allocate subgids for {{ run_podman_as }} user | inventory_hostname in _automationedacontroller_worker_hosts |
| 2079 | 83.7% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman | Configure keyring size if needed | Begin conditional or grouped task block | inventory_hostname in _automationedacontroller_worker_hosts; (ansible_memtotal_mb // 100) > 200 |
| 2080 | 83.7% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman → Configure keyring size if needed | Set maxkeys | Set maxkeys | inventory_hostname in _automationedacontroller_worker_hosts; (ansible_memtotal_mb // 100) > 200 |
| 2081 | 83.8% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman → Configure keyring size if needed | Set maxbytes | Set maxbytes | inventory_hostname in _automationedacontroller_worker_hosts; (ansible_memtotal_mb // 100) > 200 |
| 2082 | 83.8% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman | Create directory for crun runtime config | Create directory for crun runtime config | inventory_hostname in _automationedacontroller_worker_hosts |
| 2083 | 83.9% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman | Configure podman default runtime | Configure podman default runtime (dest `{{ run_podman_home_prefix }}/{{ run_podman_as }}/.config/containers/containers.conf`) | inventory_hostname in _automationedacontroller_worker_hosts |
| 2084 | 83.9% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman | Create mounts.conf from template | Create mounts.conf from template (dest `{{ run_podman_home_prefix }}/{{ run_podman_as }}/.config/containers/mounts.conf`) | inventory_hostname in _automationedacontroller_worker_hosts; not podman_copy_mounts_conf \| bool |
| 2085 | 83.9% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman | Copy user provided mounts.conf | Copy user provided mounts.conf (dest `{{ run_podman_home_prefix }}/{{ run_podman_as }}/.config/containers/mounts.conf`) | inventory_hostname in _automationedacontroller_worker_hosts; podman_copy_mounts_conf \| bool |
| 2086 | 84.0% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman | Configure SELinux context mapping | Begin conditional or grouped task block | inventory_hostname in _automationedacontroller_worker_hosts; ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 2087 | 84.0% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman → Configure SELinux context mapping | Ensure policycoreutils-python-utils is installed | Ensure policycoreutils-python-utils is installed | inventory_hostname in _automationedacontroller_worker_hosts; ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 2088 | 84.1% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman → Configure SELinux context mapping | Determine podman container storage dir variable | Determine podman container storage dir variable | inventory_hostname in _automationedacontroller_worker_hosts; ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 2089 | 84.1% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman → Configure SELinux context mapping | Check current SELinux context mapping | Check current SELinux context mapping | inventory_hostname in _automationedacontroller_worker_hosts; ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 2090 | 84.1% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman → Configure SELinux context mapping | Delete custom SELinux context mapping | Delete custom SELinux context mapping | inventory_hostname in _automationedacontroller_worker_hosts; ansible_selinux is defined; ansible_selinux.status == "enabled"; semanage_current.stdout_lines\|length; "/var/lib/containers" not in semanage_current |
| 2091 | 84.2% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman → Configure SELinux context mapping | Register expected SELinux context mapping | Register expected SELinux context mapping | inventory_hostname in _automationedacontroller_worker_hosts; ansible_selinux is defined; ansible_selinux.status == "enabled"; semanage_current.failed \| bool or semanage_deleted.changed \| bool |
| 2092 | 84.2% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman → Configure SELinux context mapping | Check if container storage dir exists | Check if container storage dir exists | inventory_hostname in _automationedacontroller_worker_hosts; ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 2093 | 84.3% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman → Configure SELinux context mapping | Perform SELinux relabeling for existing files | Perform SELinux relabeling for existing files | inventory_hostname in _automationedacontroller_worker_hosts; ansible_selinux is defined; ansible_selinux.status == "enabled"; _podman_container_storage_stat.stat.exists \| bool and (podman_force_selinux_relabel \| bool or selinux_manage.changed \| bool) |
| 2094 | 84.3% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman | Ensure registries.conf.d exists | Ensure registries.conf.d exists | inventory_hostname in _automationedacontroller_worker_hosts |
| 2095 | 84.3% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman | Force fully qualified image names to be provided to podman pull | Force fully qualified image names to be provided to podman pull (dest `/etc/containers/registries.conf.d/force-fully-qualified-images.conf`) | inventory_hostname in _automationedacontroller_worker_hosts |
| 2096 | 84.4% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman | Create tmpfiles.d entry for podman | Create tmpfiles.d entry for podman | inventory_hostname in _automationedacontroller_worker_hosts |
| 2097 | 84.4% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman | block | Begin conditional or grouped task block | inventory_hostname in _automationedacontroller_worker_hosts; subuids is changed or subgids is changed |
| 2098 | 84.5% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman → block | Create temporary directory for podman | Create temporary directory for podman | inventory_hostname in _automationedacontroller_worker_hosts; subuids is changed or subgids is changed |
| 2099 | 84.5% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman → block | Ensure changes are applied to podman | Ensure changes are applied to podman | inventory_hostname in _automationedacontroller_worker_hosts; subuids is changed or subgids is changed |
| 2100 | 84.5% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman | block (always) | Run always tasks for block | inventory_hostname in _automationedacontroller_worker_hosts; subuids is changed or subgids is changed |
| 2101 | 84.6% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman → block (always) | Remove podman temporary directory | Remove podman temporary directory | inventory_hostname in _automationedacontroller_worker_hosts; subuids is changed or subgids is changed; podman_tmp.path is defined |
| 2102 | 84.6% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman | Configure XDG_RUNTIME_DIR Environment variable for "{{ run_podman_as }}" user | Configure XDG_RUNTIME_DIR Environment variable for "{{ run_podman_as }}" user | inventory_hostname in _automationedacontroller_worker_hosts |
| 2103 | 84.7% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman | Configure DBUS_SESSION_BUS_ADDRESS Environment variable for "{{ run_podman_as }}" user | Configure DBUS_SESSION_BUS_ADDRESS Environment variable for "{{ run_podman_as }}" user | inventory_hostname in _automationedacontroller_worker_hosts |
| 2104 | 84.7% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman | Enable lingering systemd sessions for {{ run_podman_as }} user | Enable lingering systemd sessions for {{ run_podman_as }} user | inventory_hostname in _automationedacontroller_worker_hosts |
| 2105 | 84.7% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman | Reset podman for {{ run_podman_as }} user to apply configs | Reset podman for {{ run_podman_as }} user to apply configs | inventory_hostname in _automationedacontroller_worker_hosts |
| 2106 | 84.8% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman | Enable podman socket for {{ run_podman_as }} and restart | Enable podman socket for {{ run_podman_as }} and restart (service `podman.socket`) | inventory_hostname in _automationedacontroller_worker_hosts; podman_enable_socket \| bool |
| 2107 | 84.8% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml | Define automation-eda-controller-worker queue name | Define automation-eda-controller-worker queue name | inventory_hostname in _automationedacontroller_worker_hosts |
| 2108 | 84.9% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | include_tasks | Include a task file from the current role or playbook (file `secret_key.yml`) | — |
| 2109 | 84.9% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → secret_key.yml | file `secret_key.yml` | Task file `ansible/automation_platform_installer/roles/automationedacontroller/tasks/secret_key.yml` | — |
| 2110 | 84.9% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → secret_key.yml | Check for SECRET_KEY on all nodes | Check for SECRET_KEY on all nodes | — |
| 2111 | 85.0% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → secret_key.yml | block | Begin conditional or grouped task block | automationedacontroller_secret_key_path.stat.exists |
| 2112 | 85.0% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → secret_key.yml → block | Get SECRET_KEY if found | Get SECRET_KEY if found | automationedacontroller_secret_key_path.stat.exists |
| 2113 | 85.1% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → secret_key.yml → block | Set SECRET_KEY if found | Set SECRET_KEY if found | automationedacontroller_secret_key_path.stat.exists; automationedacontroller_secret_key_slurp['content'] \| default('') \| length |
| 2114 | 85.1% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → secret_key.yml | Set SECRET_KEY if not found | Set SECRET_KEY if not found | not automationedacontroller_secret_key_slurp['content'] \| default('') \| length |
| 2115 | 85.1% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → secret_key.yml | Set SECRET_KEY for all nodes | Set SECRET_KEY for all nodes | — |
| 2116 | 85.2% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → secret_key.yml | Set SECRET_KEY file | Set SECRET_KEY file (dest `/etc/ansible-automation-platform/eda/SECRET_KEY`) | — |
| 2117 | 85.2% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | include_tasks | Include a task file from the current role or playbook (file `settings.yml`) | — |
| 2118 | 85.3% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → settings.yml | file `settings.yml` | Task file `ansible/automation_platform_installer/roles/automationedacontroller/tasks/settings.yml` | — |
| 2119 | 85.3% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → settings.yml | Delete environment file for settings migration | Delete environment file for settings migration | — |
| 2120 | 85.3% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → settings.yml | set_fact | Set a host fact used by later tasks | not automationedacontroller_db_template \| default('') \| length |
| 2121 | 85.4% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → settings.yml | Generate gateway secret | Generate gateway secret (role `automationgateway`, tasks `service_token`) | — |
| 2122 | 85.4% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → settings.yml → automationgateway | role `automationgateway` (`service_token`) | Enter role task list | — |
| 2123 | 85.5% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → settings.yml → automationgateway → automationgateway | file `service_token.yml` | Task file `ansible/automation_platform_installer/roles/automationgateway/tasks/service_token.yml` | — |
| 2124 | 85.5% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → settings.yml → automationgateway → automationgateway | Generate service secret | Generate service secret | — |
| 2125 | 85.5% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → settings.yml | Configure EDA Controller settings file | Configure EDA Controller settings file (dest `/etc/ansible-automation-platform/eda/settings.yaml`) | — |
| 2126 | 85.6% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | include_tasks | Include a task file from the current role or playbook (file `certificates.yml`) | — |
| 2127 | 85.6% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml | file `certificates.yml` | Task file `ansible/automation_platform_installer/roles/automationedacontroller/tasks/certificates.yml` | — |
| 2128 | 85.7% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml | Configure postgres client certs | Begin conditional or grouped task block | automationedacontroller_pg_cert_auth |
| 2129 | 85.7% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Configure postgres client certs | Copy postgres client cert | Copy postgres client cert (dest `/etc/ansible-automation-platform/eda/pg_client.crt`) | automationedacontroller_pg_cert_auth |
| 2130 | 85.7% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Configure postgres client certs | Copy postgres client key | Copy postgres client key (dest `/etc/ansible-automation-platform/eda/pg_client.key`) | automationedacontroller_pg_cert_auth |
| 2131 | 85.8% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml | block | Begin conditional or grouped task block | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts) |
| 2132 | 85.8% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block | Copy user-provided SSL certificates | Begin conditional or grouped task block | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts); automationedacontroller_ssl_cert \| default('') \| length |
| 2133 | 85.9% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → Copy user-provided SSL certificates | Copy SSL certificate | Copy SSL certificate (dest `/etc/ansible-automation-platform/eda/server.cert`) | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts); automationedacontroller_ssl_cert \| default('') \| length |
| 2134 | 85.9% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → Copy user-provided SSL certificates | Copy SSL private key | Copy SSL private key (dest `/etc/ansible-automation-platform/eda/server.key`) | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts); automationedacontroller_ssl_cert \| default('') \| length |
| 2135 | 85.9% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block | Use internal CA | Use internal CA (role `certificate_authority`, tasks `sign_service.yml`) | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts); automationedacontroller_ssl_cert is not defined |
| 2136 | 86.0% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority | role `certificate_authority` (`sign_service.yml`) | Enter role task list | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts); automationedacontroller_ssl_cert is not defined |
| 2137 | 86.0% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority | file `sign_service.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/sign_service.yml` | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts); automationedacontroller_ssl_cert is not defined |
| 2138 | 86.1% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority | include_tasks | Include a task file from the current role or playbook (file `init.yml`) | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts); automationedacontroller_ssl_cert is not defined |
| 2139 | 86.1% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority → init.yml | file `init.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/init.yml` | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts); automationedacontroller_ssl_cert is not defined |
| 2140 | 86.1% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority → init.yml | Ensure python cryptography libraries are installed | Ensure python cryptography libraries are installed | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts); automationedacontroller_ssl_cert is not defined |
| 2141 | 86.2% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority | Create {{ aap_ca_servicename }} private key | Create {{ aap_ca_servicename }} private key | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts); automationedacontroller_ssl_cert is not defined |
| 2142 | 86.2% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority | set_fact | Set a host fact used by later tasks | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts); automationedacontroller_ssl_cert is not defined |
| 2143 | 86.3% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority | Check if {{ aap_ca_servicename }} public key exists | Check if {{ aap_ca_servicename }} public key exists | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts); automationedacontroller_ssl_cert is not defined |
| 2144 | 86.3% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts); automationedacontroller_ssl_cert is not defined; certificate_stat.stat.exists \| bool |
| 2145 | 86.3% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority → block | Check contents of {{ aap_ca_servicename }} public key | Check contents of {{ aap_ca_servicename }} public key | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts); automationedacontroller_ssl_cert is not defined; certificate_stat.stat.exists \| bool |
| 2146 | 86.4% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts); automationedacontroller_ssl_cert is not defined; certificate_stat.stat.exists \| bool; aap_service_san_records_internal \| union(aap_service_san_records) \| difference(certificate_check.subject_alt_name) \| length > 0 |
| 2147 | 86.4% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts); automationedacontroller_ssl_cert is not defined; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 2148 | 86.5% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts); automationedacontroller_ssl_cert is not defined; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false); aap_service_is_client |
| 2149 | 86.5% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority → block | Create CSR for {{ aap_ca_servicename }} node/s | Create CSR for {{ aap_ca_servicename }} node/s | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts); automationedacontroller_ssl_cert is not defined; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 2150 | 86.5% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority → block | Sign {{ aap_ca_servicename }} public key | Sign {{ aap_ca_servicename }} public key | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts); automationedacontroller_ssl_cert is not defined; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 2151 | 86.6% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority → block | Write public key to {{ aap_ca_servicename }} node/s | Write public key to {{ aap_ca_servicename }} node/s (dest `{{ aap_service_cert_fullpath }}`) | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts); automationedacontroller_ssl_cert is not defined; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 2152 | 86.6% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority | Signal restart if certificate pair changes | Signal restart if certificate pair changes | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts); automationedacontroller_ssl_cert is not defined; (write_key is changed or write_certificate is changed) |
| 2153 | 86.7% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block | Trigger nginx notifier for internally managed certificate changes | Trigger nginx notifier for internally managed certificate changes | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts); aap_service_restart \| default(false) |
| 2154 | 86.7% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block | set_fact | Set a host fact used by later tasks | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts) |
| 2155 | 86.7% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml | block | Begin conditional or grouped task block | inventory_hostname in _automationedacontroller_worker_hosts |
| 2156 | 86.8% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block | Sign internal websocket certificates | Sign internal websocket certificates (role `certificate_authority`, tasks `sign_service.yml`) | inventory_hostname in _automationedacontroller_worker_hosts |
| 2157 | 86.8% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority | role `certificate_authority` (`sign_service.yml`) | Enter role task list | inventory_hostname in _automationedacontroller_worker_hosts |
| 2158 | 86.9% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority | file `sign_service.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/sign_service.yml` | inventory_hostname in _automationedacontroller_worker_hosts |
| 2159 | 86.9% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority | include_tasks | Include a task file from the current role or playbook (file `init.yml`) | inventory_hostname in _automationedacontroller_worker_hosts |
| 2160 | 87.0% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority → init.yml | file `init.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/init.yml` | inventory_hostname in _automationedacontroller_worker_hosts |
| 2161 | 87.0% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority → init.yml | Ensure python cryptography libraries are installed | Ensure python cryptography libraries are installed | inventory_hostname in _automationedacontroller_worker_hosts |
| 2162 | 87.0% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority | Create {{ aap_ca_servicename }} private key | Create {{ aap_ca_servicename }} private key | inventory_hostname in _automationedacontroller_worker_hosts |
| 2163 | 87.1% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority | set_fact | Set a host fact used by later tasks | inventory_hostname in _automationedacontroller_worker_hosts |
| 2164 | 87.1% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority | Check if {{ aap_ca_servicename }} public key exists | Check if {{ aap_ca_servicename }} public key exists | inventory_hostname in _automationedacontroller_worker_hosts |
| 2165 | 87.2% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | inventory_hostname in _automationedacontroller_worker_hosts; certificate_stat.stat.exists \| bool |
| 2166 | 87.2% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority → block | Check contents of {{ aap_ca_servicename }} public key | Check contents of {{ aap_ca_servicename }} public key | inventory_hostname in _automationedacontroller_worker_hosts; certificate_stat.stat.exists \| bool |
| 2167 | 87.2% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | inventory_hostname in _automationedacontroller_worker_hosts; certificate_stat.stat.exists \| bool; aap_service_san_records_internal \| union(aap_service_san_records) \| difference(certificate_check.subject_alt_name) \| length > 0 |
| 2168 | 87.3% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | inventory_hostname in _automationedacontroller_worker_hosts; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 2169 | 87.3% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | inventory_hostname in _automationedacontroller_worker_hosts; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false); aap_service_is_client |
| 2170 | 87.4% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority → block | Create CSR for {{ aap_ca_servicename }} node/s | Create CSR for {{ aap_ca_servicename }} node/s | inventory_hostname in _automationedacontroller_worker_hosts; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 2171 | 87.4% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority → block | Sign {{ aap_ca_servicename }} public key | Sign {{ aap_ca_servicename }} public key | inventory_hostname in _automationedacontroller_worker_hosts; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 2172 | 87.4% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority → block | Write public key to {{ aap_ca_servicename }} node/s | Write public key to {{ aap_ca_servicename }} node/s (dest `{{ aap_service_cert_fullpath }}`) | inventory_hostname in _automationedacontroller_worker_hosts; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 2173 | 87.5% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority | Signal restart if certificate pair changes | Signal restart if certificate pair changes | inventory_hostname in _automationedacontroller_worker_hosts; (write_key is changed or write_certificate is changed) |
| 2174 | 87.5% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block | Trigger automation-eda-controller notifier for internally managed certificate changes | Trigger automation-eda-controller notifier for internally managed certificate changes | inventory_hostname in _automationedacontroller_worker_hosts; aap_service_restart \| default(false) |
| 2175 | 87.6% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block | set_fact | Set a host fact used by later tasks | inventory_hostname in _automationedacontroller_worker_hosts |
| 2176 | 87.6% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml | Handle caching client certs when mTLS is enabled for AAP caching | Begin conditional or grouped task block | not _redis_disable_mtls |
| 2177 | 87.6% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching | Copy user-provided cache client certificates | Begin conditional or grouped task block | not _redis_disable_mtls; automationedacontroller_cache_cert \| default('') \| length |
| 2178 | 87.7% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → Copy user-provided cache client certificates | Copy caching mTLS certificate | Copy caching mTLS certificate (dest `/etc/ansible-automation-platform/eda/cache.cert`) | not _redis_disable_mtls; automationedacontroller_cache_cert \| default('') \| length |
| 2179 | 87.7% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → Copy user-provided cache client certificates | Copy caching mTLS private key | Copy caching mTLS private key (dest `/etc/ansible-automation-platform/eda/cache.key`) | not _redis_disable_mtls; automationedacontroller_cache_cert \| default('') \| length |
| 2180 | 87.8% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching | Sign caching client certs | Sign caching client certs (role `certificate_authority`, tasks `sign_service.yml`) | not _redis_disable_mtls; not automationedacontroller_cache_cert \| default('') \| length |
| 2181 | 87.8% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority | role `certificate_authority` (`sign_service.yml`) | Enter role task list | not _redis_disable_mtls; not automationedacontroller_cache_cert \| default('') \| length |
| 2182 | 87.8% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority | file `sign_service.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/sign_service.yml` | not _redis_disable_mtls; not automationedacontroller_cache_cert \| default('') \| length |
| 2183 | 87.9% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority | include_tasks | Include a task file from the current role or playbook (file `init.yml`) | not _redis_disable_mtls; not automationedacontroller_cache_cert \| default('') \| length |
| 2184 | 87.9% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority → init.yml | file `init.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/init.yml` | not _redis_disable_mtls; not automationedacontroller_cache_cert \| default('') \| length |
| 2185 | 88.0% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority → init.yml | Ensure python cryptography libraries are installed | Ensure python cryptography libraries are installed | not _redis_disable_mtls; not automationedacontroller_cache_cert \| default('') \| length |
| 2186 | 88.0% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority | Create {{ aap_ca_servicename }} private key | Create {{ aap_ca_servicename }} private key | not _redis_disable_mtls; not automationedacontroller_cache_cert \| default('') \| length |
| 2187 | 88.0% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority | set_fact | Set a host fact used by later tasks | not _redis_disable_mtls; not automationedacontroller_cache_cert \| default('') \| length |
| 2188 | 88.1% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority | Check if {{ aap_ca_servicename }} public key exists | Check if {{ aap_ca_servicename }} public key exists | not _redis_disable_mtls; not automationedacontroller_cache_cert \| default('') \| length |
| 2189 | 88.1% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | not _redis_disable_mtls; not automationedacontroller_cache_cert \| default('') \| length; certificate_stat.stat.exists \| bool |
| 2190 | 88.2% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority → block | Check contents of {{ aap_ca_servicename }} public key | Check contents of {{ aap_ca_servicename }} public key | not _redis_disable_mtls; not automationedacontroller_cache_cert \| default('') \| length; certificate_stat.stat.exists \| bool |
| 2191 | 88.2% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | not _redis_disable_mtls; not automationedacontroller_cache_cert \| default('') \| length; certificate_stat.stat.exists \| bool; aap_service_san_records_internal \| union(aap_service_san_records) \| difference(certificate_check.subject_alt_name) \| length > 0 |
| 2192 | 88.2% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | not _redis_disable_mtls; not automationedacontroller_cache_cert \| default('') \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 2193 | 88.3% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | not _redis_disable_mtls; not automationedacontroller_cache_cert \| default('') \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false); aap_service_is_client |
| 2194 | 88.3% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority → block | Create CSR for {{ aap_ca_servicename }} node/s | Create CSR for {{ aap_ca_servicename }} node/s | not _redis_disable_mtls; not automationedacontroller_cache_cert \| default('') \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 2195 | 88.4% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority → block | Sign {{ aap_ca_servicename }} public key | Sign {{ aap_ca_servicename }} public key | not _redis_disable_mtls; not automationedacontroller_cache_cert \| default('') \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 2196 | 88.4% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority → block | Write public key to {{ aap_ca_servicename }} node/s | Write public key to {{ aap_ca_servicename }} node/s (dest `{{ aap_service_cert_fullpath }}`) | not _redis_disable_mtls; not automationedacontroller_cache_cert \| default('') \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 2197 | 88.4% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority | Signal restart if certificate pair changes | Signal restart if certificate pair changes | not _redis_disable_mtls; not automationedacontroller_cache_cert \| default('') \| length; (write_key is changed or write_certificate is changed) |
| 2198 | 88.5% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching | Trigger automation-eda-controller notifier for internally managed certificate changes | Trigger automation-eda-controller notifier for internally managed certificate changes | not _redis_disable_mtls; aap_service_restart \| default(false) |
| 2199 | 88.5% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | include_tasks | Include a task file from the current role or playbook (file `provision_api.yml`) | inventory_hostname in _automationedacontroller_web_hosts |
| 2200 | 88.6% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_api.yml | file `provision_api.yml` | Task file `ansible/automation_platform_installer/roles/automationedacontroller/tasks/provision_api.yml` | inventory_hostname in _automationedacontroller_web_hosts |
| 2201 | 88.6% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_api.yml | Apply EDA API nginx config | Apply EDA API nginx config (dest `/etc/nginx/conf.d/automation-eda-controller-api.conf`) | inventory_hostname in _automationedacontroller_web_hosts |
| 2202 | 88.6% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_api.yml | Configure the static directory | Configure the static directory | inventory_hostname in _automationedacontroller_web_hosts |
| 2203 | 88.7% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_api.yml | Collect static files | Collect static files | inventory_hostname in _automationedacontroller_web_hosts |
| 2204 | 88.7% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_api.yml | Copy index.html | Copy index.html | inventory_hostname in _automationedacontroller_web_hosts |
| 2205 | 88.8% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_api.yml | Ensure static media directory exists | Ensure static media directory exists | inventory_hostname in _automationedacontroller_web_hosts |
| 2206 | 88.8% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_api.yml | Copy logo file | Copy logo file | inventory_hostname in _automationedacontroller_web_hosts |
| 2207 | 88.8% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_api.yml | block | Begin conditional or grouped task block | inventory_hostname in _automationedacontroller_web_hosts; inventory_hostname == _automationedacontroller_web_hosts[0] |
| 2208 | 88.9% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_api.yml → block | Migrate the EDA Controller database schema | Migrate the EDA Controller database schema | inventory_hostname in _automationedacontroller_web_hosts; inventory_hostname == _automationedacontroller_web_hosts[0] |
| 2209 | 88.9% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_api.yml → block | Populate initial data | Populate initial data | inventory_hostname in _automationedacontroller_web_hosts; inventory_hostname == _automationedacontroller_web_hosts[0] |
| 2210 | 89.0% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_api.yml → block | create django super user | create django super user | inventory_hostname in _automationedacontroller_web_hosts; inventory_hostname == _automationedacontroller_web_hosts[0] |
| 2211 | 89.0% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_api.yml → block | update django super user password | update django super user password | inventory_hostname in _automationedacontroller_web_hosts; inventory_hostname == _automationedacontroller_web_hosts[0]; automationedacontroller_install_admin_password != '' |
| 2212 | 89.0% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | include_tasks | Include a task file from the current role or playbook (file `provision_event_stream.yml`) | inventory_hostname in _automationedacontroller_event_stream_hosts |
| 2213 | 89.1% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_event_stream.yml | file `provision_event_stream.yml` | Task file `ansible/automation_platform_installer/roles/automationedacontroller/tasks/provision_event_stream.yml` | inventory_hostname in _automationedacontroller_event_stream_hosts |
| 2214 | 89.1% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_event_stream.yml | Apply EDA event stream nginx config | Apply EDA event stream nginx config (dest `/etc/nginx/conf.d/automation-eda-controller-event-stream.conf`) | inventory_hostname in _automationedacontroller_event_stream_hosts |
| 2215 | 89.2% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_event_stream.yml | Create directory for dependent service configuration override | Create directory for dependent service configuration override | inventory_hostname in _automationedacontroller_event_stream_hosts |
| 2216 | 89.2% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_event_stream.yml | Add environment variable for gunicorn worker | Add environment variable for gunicorn worker | inventory_hostname in _automationedacontroller_event_stream_hosts |
| 2217 | 89.2% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | Create systemd override directory | Create systemd override directory | — |
| 2218 | 89.3% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | Create systemd override file | Create systemd override file (dest `/etc/systemd/system/automation-eda-controller.service.d/override.conf`) | — |
| 2219 | 89.3% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | Reload systemd daemon | Reload systemd daemon | eda_systemd_override is changed |
| 2220 | 89.4% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | block | Begin conditional or grouped task block | eda_node_type != 'event-stream' |
| 2221 | 89.4% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → block | Enable automation-eda-controller services | Enable automation-eda-controller services | eda_node_type != 'event-stream' |
| 2222 | 89.4% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → block | Ensure automation-eda-controller services are started after install | Ensure automation-eda-controller services are started after install | eda_node_type != 'event-stream' |
| 2223 | 89.5% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | block | Begin conditional or grouped task block | inventory_hostname in _automationedacontroller_event_stream_hosts |
| 2224 | 89.5% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → block | Enable automation-eda-controller-event-stream services | Enable automation-eda-controller-event-stream services | inventory_hostname in _automationedacontroller_event_stream_hosts |
| 2225 | 89.6% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → block | Ensure automation-eda-controller-event-stream services are started after install | Ensure automation-eda-controller-event-stream services are started after install | inventory_hostname in _automationedacontroller_event_stream_hosts |
| 2226 | 89.6% | install.yml → Install Automation EDA Controller → block | include_role | Include and run tasks from an Ansible role (role `nginx`, tasks `finalize.yml`) | — |
| 2227 | 89.6% | install.yml → Install Automation EDA Controller → block → nginx | role `nginx` (`finalize.yml`) | Enter role task list | — |
| 2228 | 89.7% | install.yml → Install Automation EDA Controller → block → nginx → nginx | file `finalize.yml` | Task file `ansible/automation_platform_installer/roles/nginx/tasks/finalize.yml` | — |
| 2229 | 89.7% | install.yml → Install Automation EDA Controller → block → nginx → nginx | Install nginx.conf | Install nginx.conf (dest `/etc/nginx/nginx.conf`) | — |
| 2230 | 89.8% | install.yml → Install Automation EDA Controller → block → nginx → nginx | Start nginx and configure to startup automatically | Start nginx and configure to startup automatically (service `nginx`) | — |
| 2231 | 89.8% | install.yml → Install Automation EDA Controller → block | Configure firewall | Configure firewall (role `firewall`) | — |
| 2232 | 89.9% | install.yml → Install Automation EDA Controller → block → firewall | role `firewall` | Enter role task list | — |
| 2233 | 89.9% | install.yml → Install Automation EDA Controller → block → firewall → firewall | file `main.yml` | Task file `ansible/automation_platform_installer/roles/firewall/tasks/main.yml` | — |
| 2234 | 89.9% | install.yml → Install Automation EDA Controller → block → firewall → firewall | include_tasks | Include a task file from the current role or playbook (file `discover.yml`) | — |
| 2235 | 90.0% | install.yml → Install Automation EDA Controller → block → firewall → firewall → discover.yml | file `discover.yml` | Task file `ansible/automation_platform_installer/roles/firewall/tasks/discover.yml` | — |
| 2236 | 90.0% | install.yml → Install Automation EDA Controller → block → firewall → firewall → discover.yml | Determine if firewalld is running | Determine if firewalld is running | — |
| 2237 | 90.1% | install.yml → Install Automation EDA Controller → block → firewall → firewall → discover.yml | Determine if firewalld is enabled | Determine if firewalld is enabled | — |
| 2238 | 90.1% | install.yml → Install Automation EDA Controller → block → firewall → firewall → discover.yml | Determine if iptables is installed | Determine if iptables is installed | — |
| 2239 | 90.1% | install.yml → Install Automation EDA Controller → block → firewall → firewall → discover.yml | Determine if iptables service is installed | Determine if iptables service is installed | — |
| 2240 | 90.2% | install.yml → Install Automation EDA Controller → block → firewall → firewall → discover.yml | Check to see if there is an iptables config file | Check to see if there is an iptables config file | iptables_installed.rc == 0 |
| 2241 | 90.2% | install.yml → Install Automation EDA Controller → block → firewall → firewall → discover.yml | Select firewalld | Select firewalld | firewalld_running.rc == 0 or systemctl_unit_files.stdout_lines \| select('match','firewalld.service\s+enabled') \| list \| length == 1 |
| 2242 | 90.3% | install.yml → Install Automation EDA Controller → block → firewall → firewall → discover.yml | Select iptables | Select iptables | iptables_installed.rc == 0; iptables_exists.stat.exists; iptables_service.stat.exists |
| 2243 | 90.3% | install.yml → Install Automation EDA Controller → block → firewall → firewall | Include {{ 'no' if component_firewall_type == 'none' else component_firewall_type }} tasks | Include {{ 'no' if component_firewall_type == 'none' else component_firewall_type }} tasks (file `{{ component_firewall_type }}.yml`) | firewalld_running.rc == 0 or systemctl_unit_files.stdout_lines \| select('match','firewalld.service\s+enabled') \| list \| length == 1 or ( iptables_installed.rc == 0 and iptables_exists.stat.exists and iptables_service.stat.exists ) |
| 2244 | 90.3% | install.yml → Install Automation EDA Controller → block → firewall → firewall → {{ component_firewall_type }}.yml | {{ component_firewall_type }}.yml | Include task file `{{ component_firewall_type }}.yml` (relative to current role task directory) | firewalld_running.rc == 0 or systemctl_unit_files.stdout_lines \| select('match','firewalld.service\s+enabled') \| list \| length == 1 or ( iptables_installed.rc == 0 and iptables_exists.stat.exists and iptables_service.stat.exists ) |
| 2245 | 90.4% | install.yml → Install Automation EDA Controller | block (always) | Run always tasks for block | — |
| 2246 | 90.4% | install.yml → Install Automation EDA Controller → block (always) | Run repo_management (teardown) | Run repo_management (teardown) (role `repo_management`, tasks `teardown`) | — |
| 2247 | 90.5% | install.yml → Install Automation EDA Controller → block (always) → repo_management | role `repo_management` (`teardown`) | Enter role task list | — |
| 2248 | 90.5% | install.yml → Install Automation EDA Controller → block (always) → repo_management → repo_management | file `teardown.yml` | Task file `ansible/automation_platform_installer/roles/repo_management/tasks/teardown.yml` | — |
| 2249 | 90.5% | install.yml → Install Automation EDA Controller → block (always) → repo_management → repo_management | Disable Automation Platform repository | Disable Automation Platform repository | extra_repos is defined |
| 2250 | 90.6% | install.yml → Install Automation EDA Controller → block (always) → repo_management → repo_management | Disable Automation Platform RHSM repository | Disable Automation Platform RHSM repository | aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 2251 | 90.6% | install.yml | Post-install setup | Play targeting `automationgateway:automationcontroller:automationedacontroller:automationhub[0]:execution_nodes` | — |
| 2252 | 90.7% | install.yml → Post-install setup | Set automation hub image base path | Set automation hub image base path | groups['automationhub'] \| default([]) \| length |
| 2253 | 90.7% | install.yml → Post-install setup | include_role | Include and run tasks from an Ansible role (role `misc`, tasks `setup_ee_images`) | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool) |
| 2254 | 90.7% | install.yml → Post-install setup → misc | role `misc` (`setup_ee_images`) | Enter role task list | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool) |
| 2255 | 90.8% | install.yml → Post-install setup → misc → misc | file `setup_ee_images.yml` | Task file `ansible/automation_platform_installer/roles/misc/tasks/setup_ee_images.yml` | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool) |
| 2256 | 90.8% | install.yml → Post-install setup → misc → misc | block | Begin conditional or grouped task block | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool) |
| 2257 | 90.9% | install.yml → Post-install setup → misc → misc → block | Ensure skopeo exists | Ensure skopeo exists | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool) |
| 2258 | 90.9% | install.yml → Post-install setup → misc → misc → block | Set list of images to upload for controller and execution nodes | Set list of images to upload for controller and execution nodes | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('execution_nodes', [])) |
| 2259 | 90.9% | install.yml → Post-install setup → misc → misc → block | Set list of images to upload for hub | Set list of images to upload for hub | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); inventory_hostname in groups.get('automationhub', []) |
| 2260 | 91.0% | install.yml → Post-install setup → misc → misc → block | Set list of images to upload for EDA | Set list of images to upload for EDA | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); inventory_hostname in _automationedacontroller_web_hosts |
| 2261 | 91.0% | install.yml → Post-install setup → misc → misc → block | Wait until Pulp workers are ready | Wait until Pulp workers are ready | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); groups.get('automationhub', []) \| length > 0 |
| 2262 | 91.1% | install.yml → Post-install setup → misc → misc → block | Set up images | Begin conditional or grouped task block | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); inventory_hostname in groups['automationhub'] \| default([]); not bundle_install \| default(false) \| bool |
| 2263 | 91.1% | install.yml → Post-install setup → misc → misc → block → Set up images | Set source registry credential (username) | Set source registry credential (username) | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); inventory_hostname in groups['automationhub'] \| default([]); not bundle_install \| default(false) \| bool; registry_username \| default('') \| length; not registry_password \| default('') \| length |
| 2264 | 91.1% | install.yml → Post-install setup → misc → misc → block → Set up images | Set source registry credential (username and password) | Set source registry credential (username and password) | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); inventory_hostname in groups['automationhub'] \| default([]); not bundle_install \| default(false) \| bool; registry_username \| default('') \| length; registry_password \| default('') \| length |
| 2265 | 91.2% | install.yml → Post-install setup → misc → misc → block → Set up images | Push images to Automation Hub registry | Push images to Automation Hub registry | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); inventory_hostname in groups['automationhub'] \| default([]); not bundle_install \| default(false) \| bool |
| 2266 | 91.2% | install.yml → Post-install setup → misc → misc → block → Set up images | Catch error when not all images are processed | Catch error when not all images are processed | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); inventory_hostname in groups['automationhub'] \| default([]); not bundle_install \| default(false) \| bool; _push_ee_result.results is not defined |
| 2267 | 91.3% | install.yml → Post-install setup → misc → misc → block → Set up images | Parse error messages | Parse error messages | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); inventory_hostname in groups['automationhub'] \| default([]); not bundle_install \| default(false) \| bool; item \| default({}) is failed |
| 2268 | 91.3% | install.yml → Post-install setup → misc → misc → block | Set up images (bundle) | Set up images (bundle) (file `push_bundle_ee_image.yml`) | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool |
| 2269 | 91.3% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml | file `push_bundle_ee_image.yml` | Task file `ansible/automation_platform_installer/roles/misc/tasks/push_bundle_ee_image.yml` | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool |
| 2270 | 91.4% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml | block | Begin conditional or grouped task block | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool |
| 2271 | 91.4% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block | Create temporary directory for EE image | Create temporary directory for EE image | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool |
| 2272 | 91.5% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block | Copy bundled EE image | Begin conditional or grouped task block | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool |
| 2273 | 91.5% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block → Copy bundled EE image | Copy bundled EE image | Copy bundled EE image | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool |
| 2274 | 91.5% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block | Copy bundled EE image (rescue) | Run rescue tasks for block | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool |
| 2275 | 91.6% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block → Copy bundled EE image (rescue) | Copy bundled EE image (legacy) | Copy bundled EE image (legacy) (dest `{{ ee_image_tmp.path }}`) | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool |
| 2276 | 91.6% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block | Uncompress bundled EE image | Uncompress bundled EE image | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool |
| 2277 | 91.7% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block | Set permission for the uncompressed images | Set permission for the uncompressed images | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool |
| 2278 | 91.7% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block | Push EE image to Automation Hub registry | Push EE image to Automation Hub registry | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; inventory_hostname in groups['automationhub'] \| default([]) |
| 2279 | 91.7% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block | Parse error message | Parse error message | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool |
| 2280 | 91.8% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block | set_fact | Set a host fact used by later tasks | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool |
| 2281 | 91.8% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block | Set up EE image locally on controller and execution nodes | Set up EE image locally on controller and execution nodes (file `push_bundle_image_locally.yml`) | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('execution_nodes', [])) |
| 2282 | 91.9% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block → push_bundle_image_locally.yml | file `push_bundle_image_locally.yml` | Task file `ansible/automation_platform_installer/roles/misc/tasks/push_bundle_image_locally.yml` | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('execution_nodes', [])) |
| 2283 | 91.9% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block → push_bundle_image_locally.yml | block | Begin conditional or grouped task block | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('execution_nodes', [])) |
| 2284 | 91.9% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block → push_bundle_image_locally.yml → block | Create temporary directory for skopeo | Create temporary directory for skopeo | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('execution_nodes', [])) |
| 2285 | 92.0% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block → push_bundle_image_locally.yml → block | Set local push path for registry_url | Set local push path for registry_url | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('execution_nodes', [])); not (_ee_from_hub_only \| bool and groups['automationhub'] \| default([]) \| length) |
| 2286 | 92.0% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block → push_bundle_image_locally.yml → block | Set local push path for hub | Set local push path for hub | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('execution_nodes', [])); groups['automationhub'] \| default([]) \| length |
| 2287 | 92.1% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block → push_bundle_image_locally.yml → block | Push {{ item }} locally | Push {{ item }} locally | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('execution_nodes', [])) |
| 2288 | 92.1% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block → push_bundle_image_locally.yml → block | Catch error when not all images are processed | Catch error when not all images are processed | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('execution_nodes', [])); _push_local_result.results is not defined |
| 2289 | 92.1% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block → push_bundle_image_locally.yml → block | Parse error messages | Parse error messages | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('execution_nodes', [])); _ee_local_push_error \| default({}) is failed |
| 2290 | 92.2% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block → push_bundle_image_locally.yml | block (always) | Run always tasks for block | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('execution_nodes', [])) |
| 2291 | 92.2% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block → push_bundle_image_locally.yml → block (always) | Remove skopeo temporary directory | Remove skopeo temporary directory | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('execution_nodes', [])); _skopeo_tmp.path is defined |
| 2292 | 92.3% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block | Set up DE image locally on EDA controller node | Set up DE image locally on EDA controller node (file `push_bundle_image_locally.yml`) | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; inventory_hostname in _automationedacontroller_worker_hosts |
| 2293 | 92.3% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block → push_bundle_image_locally.yml | file `push_bundle_image_locally.yml` | Task file `ansible/automation_platform_installer/roles/misc/tasks/push_bundle_image_locally.yml` | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; inventory_hostname in _automationedacontroller_worker_hosts |
| 2294 | 92.3% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block → push_bundle_image_locally.yml | block | Begin conditional or grouped task block | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; inventory_hostname in _automationedacontroller_worker_hosts |
| 2295 | 92.4% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block → push_bundle_image_locally.yml → block | Create temporary directory for skopeo | Create temporary directory for skopeo | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; inventory_hostname in _automationedacontroller_worker_hosts |
| 2296 | 92.4% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block → push_bundle_image_locally.yml → block | Set local push path for registry_url | Set local push path for registry_url | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; inventory_hostname in _automationedacontroller_worker_hosts; not (_ee_from_hub_only \| bool and groups['automationhub'] \| default([]) \| length) |
| 2297 | 92.5% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block → push_bundle_image_locally.yml → block | Set local push path for hub | Set local push path for hub | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; inventory_hostname in _automationedacontroller_worker_hosts; groups['automationhub'] \| default([]) \| length |
| 2298 | 92.5% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block → push_bundle_image_locally.yml → block | Push {{ item }} locally | Push {{ item }} locally | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; inventory_hostname in _automationedacontroller_worker_hosts |
| 2299 | 92.5% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block → push_bundle_image_locally.yml → block | Catch error when not all images are processed | Catch error when not all images are processed | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; inventory_hostname in _automationedacontroller_worker_hosts; _push_local_result.results is not defined |
| 2300 | 92.6% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block → push_bundle_image_locally.yml → block | Parse error messages | Parse error messages | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; inventory_hostname in _automationedacontroller_worker_hosts; _ee_local_push_error \| default({}) is failed |
| 2301 | 92.6% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block → push_bundle_image_locally.yml | block (always) | Run always tasks for block | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; inventory_hostname in _automationedacontroller_worker_hosts |
| 2302 | 92.7% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block → push_bundle_image_locally.yml → block (always) | Remove skopeo temporary directory | Remove skopeo temporary directory | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; inventory_hostname in _automationedacontroller_worker_hosts; _skopeo_tmp.path is defined |
| 2303 | 92.7% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block | set_fact | Set a host fact used by later tasks | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; _ee_error_msg \| length |
| 2304 | 92.8% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml | block (always) | Run always tasks for block | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool |
| 2305 | 92.8% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block (always) | Remove copied EE image | Remove copied EE image | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; ee_image_tmp.path is defined |
| 2306 | 92.8% | install.yml → Post-install setup → misc → misc → block | Parse error messages | Parse error messages | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool) |
| 2307 | 92.9% | install.yml → Post-install setup → misc → misc → block | Ensure images are pushed | Ensure images are pushed | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); _image_push_error_messages \| default([]) \| length |
| 2308 | 92.9% | install.yml → Post-install setup → misc → misc | block (always) | Run always tasks for block | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool) |
| 2309 | 93.0% | install.yml → Post-install setup → misc → misc → block (always) | Remove skopeo if added by installer | Remove skopeo if added by installer | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); installed_skopeo is changed |
| 2310 | 93.0% | install.yml → Post-install setup | Execute Post-Installation Steps | Begin conditional or grouped task block | node_state is defined; node_state == 'active' |
| 2311 | 93.0% | install.yml → Post-install setup → Execute Post-Installation Steps | include_role | Include and run tasks from an Ansible role (role `automationcontroller`, tasks `post_install_setup`) | node_state is defined; node_state == 'active'; inventory_hostname in groups['automationcontroller'] |
| 2312 | 93.1% | install.yml → Post-install setup → Execute Post-Installation Steps → automationcontroller | role `automationcontroller` (`post_install_setup`) | Enter role task list | node_state is defined; node_state == 'active'; inventory_hostname in groups['automationcontroller'] |
| 2313 | 93.1% | install.yml → Post-install setup → Execute Post-Installation Steps → automationcontroller → automationcontroller | file `post_install_setup.yml` | Task file `ansible/automation_platform_installer/roles/automationcontroller/tasks/post_install_setup.yml` | node_state is defined; node_state == 'active'; inventory_hostname in groups['automationcontroller'] |
| 2314 | 93.2% | install.yml → Post-install setup → Execute Post-Installation Steps → automationcontroller → automationcontroller | block | Begin conditional or grouped task block | node_state is defined; node_state == 'active'; inventory_hostname in groups['automationcontroller']; groups['automationhub'] \| default([]) \| length |
| 2315 | 93.2% | install.yml → Post-install setup → Execute Post-Installation Steps → automationcontroller → automationcontroller → block | Create execution environment list for Hub | Create execution environment list for Hub | node_state is defined; node_state == 'active'; inventory_hostname in groups['automationcontroller']; groups['automationhub'] \| default([]) \| length |
| 2316 | 93.2% | install.yml → Post-install setup → Execute Post-Installation Steps → automationcontroller → automationcontroller → block | Update global_job_execution_environments for Hub (non-bundle) | Update global_job_execution_environments for Hub (non-bundle) | node_state is defined; node_state == 'active'; inventory_hostname in groups['automationcontroller']; groups['automationhub'] \| default([]) \| length; not bundle_install \| bool; not _ee_from_hub_only \| bool |
| 2317 | 93.3% | install.yml → Post-install setup → Execute Post-Installation Steps → automationcontroller → automationcontroller → block | Update execution environment images for Hub (bundle) | Update execution environment images for Hub (bundle) | node_state is defined; node_state == 'active'; inventory_hostname in groups['automationcontroller']; groups['automationhub'] \| default([]) \| length; bundle_install \| bool; not _ee_from_hub_only \| bool |
| 2318 | 93.3% | install.yml → Post-install setup → Execute Post-Installation Steps → automationcontroller → automationcontroller → block | Update execution environment images for Hub registry only | Update execution environment images for Hub registry only | node_state is defined; node_state == 'active'; inventory_hostname in groups['automationcontroller']; groups['automationhub'] \| default([]) \| length; _ee_from_hub_only \| bool |
| 2319 | 93.4% | install.yml → Post-install setup → Execute Post-Installation Steps → automationcontroller → automationcontroller | Set Execution Environment configuration | Set Execution Environment configuration (dest `/etc/tower/conf.d/execution_environments.py`) | node_state is defined; node_state == 'active'; inventory_hostname in groups['automationcontroller'] |
| 2320 | 93.4% | install.yml → Post-install setup → Execute Post-Installation Steps → automationcontroller → automationcontroller | Set default image for Container Groups | Set default image for Container Groups (dest `/etc/tower/conf.d/container_groups.py`) | node_state is defined; node_state == 'active'; inventory_hostname in groups['automationcontroller'] |
| 2321 | 93.4% | install.yml → Post-install setup → Execute Post-Installation Steps | include_role | Include and run tasks from an Ansible role (role `misc`, tasks `post_install_setup`) | node_state is defined; node_state == 'active'; inventory_hostname == groups['automationcontroller'][0] |
| 2322 | 93.5% | install.yml → Post-install setup → Execute Post-Installation Steps → misc | role `misc` (`post_install_setup`) | Enter role task list | node_state is defined; node_state == 'active'; inventory_hostname == groups['automationcontroller'][0] |
| 2323 | 93.5% | install.yml → Post-install setup → Execute Post-Installation Steps → misc → misc | file `post_install_setup.yml` | Task file `ansible/automation_platform_installer/roles/misc/tasks/post_install_setup.yml` | node_state is defined; node_state == 'active'; inventory_hostname == groups['automationcontroller'][0] |
| 2324 | 93.6% | install.yml → Post-install setup → Execute Post-Installation Steps → misc → misc | Create the default organization if it is needed. | Create the default organization if it is needed. | node_state is defined; node_state == 'active'; inventory_hostname == groups['automationcontroller'][0]; create_preload_data \| bool |
| 2325 | 93.6% | install.yml → Post-install setup → Execute Post-Installation Steps → misc → misc | Create the default execution environments if needed (without authentication). | Create the default execution environments if needed (without authentication). | node_state is defined; node_state == 'active'; inventory_hostname == groups['automationcontroller'][0]; registry_username is not defined or bundle_install \| default(false) \| bool |
| 2326 | 93.6% | install.yml → Post-install setup → Execute Post-Installation Steps → misc → misc | Create the default execution environments if needed (with authentication). | Create the default execution environments if needed (with authentication). | node_state is defined; node_state == 'active'; inventory_hostname == groups['automationcontroller'][0]; registry_username is defined |
| 2327 | 93.7% | install.yml → Post-install setup → Execute Post-Installation Steps | Start the automation-controller service | Start the automation-controller service (service `automation-controller`) | node_state is defined; node_state == 'active'; inventory_hostname in groups['automationcontroller'] |
| 2328 | 93.7% | install.yml → Post-install setup | include_role | Include and run tasks from an Ansible role (role `automationhub`, tasks `post_install_setup.yml`) | inventory_hostname in groups['automationhub'] \| default([]) |
| 2329 | 93.8% | install.yml → Post-install setup → automationhub | role `automationhub` (`post_install_setup.yml`) | Enter role task list | inventory_hostname in groups['automationhub'] \| default([]) |
| 2330 | 93.8% | install.yml → Post-install setup → automationhub → automationhub | file `post_install_setup.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/post_install_setup.yml` | inventory_hostname in groups['automationhub'] \| default([]) |
| 2331 | 93.8% | install.yml → Post-install setup → automationhub → automationhub | Create credentials and change collection order for 'Default' organization | Begin conditional or grouped task block | inventory_hostname in groups['automationhub'] \| default([]); groups['automationcontroller'] \| default([]) \| length |
| 2332 | 93.9% | install.yml → Post-install setup → automationhub → automationhub → Create credentials and change collection order for 'Default' organization | include_tasks | Include a task file from the current role or playbook (file `generate_token.yml`) | inventory_hostname in groups['automationhub'] \| default([]); groups['automationcontroller'] \| default([]) \| length; not automationhub_api_token \| default('') \| length; generate_automationhub_token \| bool |
| 2333 | 93.9% | install.yml → Post-install setup → automationhub → automationhub → Create credentials and change collection order for 'Default' organization → generate_token.yml | file `generate_token.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/generate_token.yml` | inventory_hostname in groups['automationhub'] \| default([]); groups['automationcontroller'] \| default([]) \| length; not automationhub_api_token \| default('') \| length; generate_automationhub_token \| bool |
| 2334 | 94.0% | install.yml → Post-install setup → automationhub → automationhub → Create credentials and change collection order for 'Default' organization → generate_token.yml | Get Hub API token | Get Hub API token | inventory_hostname in groups['automationhub'] \| default([]); groups['automationcontroller'] \| default([]) \| length; not automationhub_api_token \| default('') \| length; generate_automationhub_token \| bool |
| 2335 | 94.0% | install.yml → Post-install setup → automationhub → automationhub → Create credentials and change collection order for 'Default' organization → generate_token.yml | set_fact | Set a host fact used by later tasks | inventory_hostname in groups['automationhub'] \| default([]); groups['automationcontroller'] \| default([]) \| length; not automationhub_api_token \| default('') \| length; generate_automationhub_token \| bool |
| 2336 | 94.0% | install.yml → Post-install setup → automationhub → automationhub → Create credentials and change collection order for 'Default' organization → generate_token.yml | Save Hub token | Save Hub token (dest `{{ pulp_user_home + '/admin_token' }}`) | inventory_hostname in groups['automationhub'] \| default([]); groups['automationcontroller'] \| default([]) \| length; not automationhub_api_token \| default('') \| length; generate_automationhub_token \| bool |
| 2337 | 94.1% | install.yml → Post-install setup → automationhub → automationhub → Create credentials and change collection order for 'Default' organization | Create Hub credentials and associate with EEs | Create Hub credentials and associate with EEs | inventory_hostname in groups['automationhub'] \| default([]); groups['automationcontroller'] \| default([]) \| length |
| 2338 | 94.1% | install.yml → Post-install setup | include_role | Include and run tasks from an Ansible role (role `automationedacontroller`, tasks `post_install_setup.yml`) | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0] |
| 2339 | 94.2% | install.yml → Post-install setup → automationedacontroller | role `automationedacontroller` (`post_install_setup.yml`) | Enter role task list | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0] |
| 2340 | 94.2% | install.yml → Post-install setup → automationedacontroller → automationedacontroller | file `post_install_setup.yml` | Task file `ansible/automation_platform_installer/roles/automationedacontroller/tasks/post_install_setup.yml` | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0] |
| 2341 | 94.2% | install.yml → Post-install setup → automationedacontroller → automationedacontroller | Set credential for registry_url | Set credential for registry_url | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; not _ee_from_hub_only \| bool; registry_username \| default() \| length; registry_password \| default() \| length |
| 2342 | 94.3% | install.yml → Post-install setup → automationedacontroller → automationedacontroller | Set hub info | Begin conditional or grouped task block | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; groups['automationhub'] \| default([]) \| length |
| 2343 | 94.3% | install.yml → Post-install setup → automationedacontroller → automationedacontroller → Set hub info | ansible.builtin.set_fact | Run `ansible.builtin.set_fact` | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; groups['automationhub'] \| default([]) \| length |
| 2344 | 94.4% | install.yml → Post-install setup → automationedacontroller → automationedacontroller → Set hub info | ansible.builtin.set_fact | Run `ansible.builtin.set_fact` | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; groups['automationhub'] \| default([]) \| length |
| 2345 | 94.4% | install.yml → Post-install setup → automationedacontroller → automationedacontroller → Set hub info | Set credential for Hub | Set credential for Hub | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; groups['automationhub'] \| default([]) \| length |
| 2346 | 94.4% | install.yml → Post-install setup → automationedacontroller → automationedacontroller → Set hub info | Set DE for Hub | Set DE for Hub | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; groups['automationhub'] \| default([]) \| length |
| 2347 | 94.5% | install.yml → Post-install setup → automationedacontroller → automationedacontroller | Set DE and Credential list for registry_url only | Set DE and Credential list for registry_url only | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; not groups['automationhub'] \| default([]) \| length |
| 2348 | 94.5% | install.yml → Post-install setup → automationedacontroller → automationedacontroller | Set DE and Credential list for Hub only | Set DE and Credential list for Hub only | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; groups['automationhub'] \| default([]) \| length; _ee_from_hub_only \| bool |
| 2349 | 94.6% | install.yml → Post-install setup → automationedacontroller → automationedacontroller | Set DE and Credential list for registry_url and Hub | Set DE and Credential list for registry_url and Hub | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; groups['automationhub'] \| default([]) \| length; not _ee_from_hub_only \| bool |
| 2350 | 94.6% | install.yml → Post-install setup → automationedacontroller → automationedacontroller | Create credentials | Begin conditional or grouped task block | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; _cred_list \| length |
| 2351 | 94.6% | install.yml → Post-install setup → automationedacontroller → automationedacontroller → Create credentials | Create credentials | Create credentials | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; _cred_list \| length |
| 2352 | 94.7% | install.yml → Post-install setup → automationedacontroller → automationedacontroller | Create credentials (rescue) | Run rescue tasks for block | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; _cred_list \| length |
| 2353 | 94.7% | install.yml → Post-install setup → automationedacontroller → automationedacontroller → Create credentials (rescue) | Parse messages | Parse messages | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; _cred_list \| length; item is failed |
| 2354 | 94.8% | install.yml → Post-install setup → automationedacontroller → automationedacontroller → Create credentials (rescue) | Print error messages from credential creation | Print error messages from credential creation | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; _cred_list \| length; _eda_cred_error_messages \| default('') \| length |
| 2355 | 94.8% | install.yml → Post-install setup → automationedacontroller → automationedacontroller | Create credentials (always) | Run always tasks for block | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; _cred_list \| length |
| 2356 | 94.8% | install.yml → Post-install setup → automationedacontroller → automationedacontroller → Create credentials (always) | Parse messages | Parse messages | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; _cred_list \| length; item is succeeded |
| 2357 | 94.9% | install.yml → Post-install setup → automationedacontroller → automationedacontroller → Create credentials (always) | Print messages from credential creation | Print messages from credential creation | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; _cred_list \| length; _eda_cred_messages \| default('') \| length |
| 2358 | 94.9% | install.yml → Post-install setup → automationedacontroller → automationedacontroller | Create decision environments | Begin conditional or grouped task block | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; _de_list \| length |
| 2359 | 95.0% | install.yml → Post-install setup → automationedacontroller → automationedacontroller → Create decision environments | Create decision environments | Create decision environments | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; _de_list \| length |
| 2360 | 95.0% | install.yml → Post-install setup → automationedacontroller → automationedacontroller | Create decision environments (rescue) | Run rescue tasks for block | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; _de_list \| length |
| 2361 | 95.0% | install.yml → Post-install setup → automationedacontroller → automationedacontroller → Create decision environments (rescue) | Parse messages | Parse messages | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; _de_list \| length; item is failed |
| 2362 | 95.1% | install.yml → Post-install setup → automationedacontroller → automationedacontroller → Create decision environments (rescue) | Print error messages from DE creation | Print error messages from DE creation | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; _de_list \| length; _eda_de_error_messages \| default('') \| length |
| 2363 | 95.1% | install.yml → Post-install setup → automationedacontroller → automationedacontroller | Create decision environments (always) | Run always tasks for block | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; _de_list \| length |
| 2364 | 95.2% | install.yml → Post-install setup → automationedacontroller → automationedacontroller → Create decision environments (always) | Parse messages | Parse messages | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; _de_list \| length; item is succeeded |
| 2365 | 95.2% | install.yml → Post-install setup → automationedacontroller → automationedacontroller → Create decision environments (always) | Print messages from DE creation | Print messages from DE creation | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; _de_list \| length; _eda_de_messages \| default('') \| length |
| 2366 | 95.2% | install.yml → Post-install setup | include_role | Include and run tasks from an Ansible role (role `automationgateway`, tasks `post_install_setup`) | inventory_hostname in groups['automationgateway'] |
| 2367 | 95.3% | install.yml → Post-install setup → automationgateway | role `automationgateway` (`post_install_setup`) | Enter role task list | inventory_hostname in groups['automationgateway'] |
| 2368 | 95.3% | install.yml → Post-install setup → automationgateway → automationgateway | file `post_install_setup.yml` | Task file `ansible/automation_platform_installer/roles/automationgateway/tasks/post_install_setup.yml` | inventory_hostname in groups['automationgateway'] |
| 2369 | 95.4% | install.yml → Post-install setup → automationgateway → automationgateway | Migrate data | Migrate data | inventory_hostname in groups['automationgateway'] |
| 2370 | 95.4% | install.yml → Post-install setup | include_role | Include and run tasks from an Ansible role (role `automationcontroller`, tasks `post_migration_setup`) | inventory_hostname in groups['automationcontroller'] |
| 2371 | 95.4% | install.yml → Post-install setup → automationcontroller | role `automationcontroller` (`post_migration_setup`) | Enter role task list | inventory_hostname in groups['automationcontroller'] |
| 2372 | 95.5% | install.yml → Post-install setup → automationcontroller → automationcontroller | file `post_migration_setup.yml` | Task file `ansible/automation_platform_installer/roles/automationcontroller/tasks/post_migration_setup.yml` | inventory_hostname in groups['automationcontroller'] |
| 2373 | 95.5% | install.yml → Post-install setup → automationcontroller → automationcontroller | Import auth config to gateway | Begin conditional or grouped task block | inventory_hostname in groups['automationcontroller'] |
| 2374 | 95.6% | install.yml → Post-install setup → automationcontroller → automationcontroller → Import auth config to gateway | Check if AAP 2.4 marker file exists | Check if AAP 2.4 marker file exists | inventory_hostname in groups['automationcontroller'] |
| 2375 | 95.6% | install.yml → Post-install setup → automationcontroller → automationcontroller → Import auth config to gateway | Import auth config to gateway | Import auth config to gateway | inventory_hostname in groups['automationcontroller']; aap_24_marker.stat.exists |
| 2376 | 95.7% | install.yml → Post-install setup → automationcontroller → automationcontroller → Import auth config to gateway | Remove AAP 2.4 marker file | Remove AAP 2.4 marker file | inventory_hostname in groups['automationcontroller']; aap_24_marker.stat.exists |
| 2377 | 95.7% | install.yml → Post-install setup | block | Begin conditional or grouped task block | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool |
| 2378 | 95.7% | install.yml → Post-install setup → block | Set supported repositories for seeding | Set supported repositories for seeding | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool |
| 2379 | 95.8% | install.yml → Post-install setup → block | Seed collections | Seed collections (role `automationhub`, tasks `seed_collection_for_repository`) | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool |
| 2380 | 95.8% | install.yml → Post-install setup → block → automationhub | role `automationhub` (`seed_collection_for_repository`) | Enter role task list | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool |
| 2381 | 95.9% | install.yml → Post-install setup → block → automationhub → automationhub | file `seed_collection_for_repository.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/seed_collection_for_repository.yml` | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool |
| 2382 | 95.9% | install.yml → Post-install setup → block → automationhub → automationhub | Ensure repository is valid for seeding | Ensure repository is valid for seeding | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; destination_repository not in _collection_seed_supported_repositories |
| 2383 | 95.9% | install.yml → Post-install setup → block → automationhub → automationhub | include_tasks | Include a task file from the current role or playbook (file `generate_token.yml`) | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; not automationhub_api_token \| default('') \| length; generate_automationhub_token \| bool |
| 2384 | 96.0% | install.yml → Post-install setup → block → automationhub → automationhub → generate_token.yml | file `generate_token.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/generate_token.yml` | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; not automationhub_api_token \| default('') \| length; generate_automationhub_token \| bool |
| 2385 | 96.0% | install.yml → Post-install setup → block → automationhub → automationhub → generate_token.yml | Get Hub API token | Get Hub API token | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; not automationhub_api_token \| default('') \| length; generate_automationhub_token \| bool |
| 2386 | 96.1% | install.yml → Post-install setup → block → automationhub → automationhub → generate_token.yml | set_fact | Set a host fact used by later tasks | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; not automationhub_api_token \| default('') \| length; generate_automationhub_token \| bool |
| 2387 | 96.1% | install.yml → Post-install setup → block → automationhub → automationhub → generate_token.yml | Save Hub token | Save Hub token (dest `{{ pulp_user_home + '/admin_token' }}`) | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; not automationhub_api_token \| default('') \| length; generate_automationhub_token \| bool |
| 2388 | 96.1% | install.yml → Post-install setup → block → automationhub → automationhub | Ensure credential is provided | Ensure credential is provided | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; not automationhub_api_token \| default('') \| length; not automationgateway_admin_password \| default('') \| length |
| 2389 | 96.2% | install.yml → Post-install setup → block → automationhub → automationhub | set_fact | Set a host fact used by later tasks | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; not _bundled_collection_root \| default('') \| length |
| 2390 | 96.2% | install.yml → Post-install setup → block → automationhub → automationhub | set_fact | Set a host fact used by later tasks | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; not _collection_seed_log_root \| default('') \| length |
| 2391 | 96.3% | install.yml → Post-install setup → block → automationhub → automationhub | set_fact | Set a host fact used by later tasks | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool |
| 2392 | 96.3% | install.yml → Post-install setup → block → automationhub → automationhub | set_fact | Set a host fact used by later tasks | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool |
| 2393 | 96.3% | install.yml → Post-install setup → block → automationhub → automationhub | block | Begin conditional or grouped task block | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool |
| 2394 | 96.4% | install.yml → Post-install setup → block → automationhub → automationhub → block | Check for requirements.yml | Check for requirements.yml | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool |
| 2395 | 96.4% | install.yml → Post-install setup → block → automationhub → automationhub → block | Ensure requirements.yml exists | Ensure requirements.yml exists | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; not _requirement_file.stat.exists \| bool |
| 2396 | 96.5% | install.yml → Post-install setup → block → automationhub → automationhub → block | Get collection list | Get collection list | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool |
| 2397 | 96.5% | install.yml → Post-install setup → block → automationhub → automationhub → block | Check if collection tarball exists | Check if collection tarball exists | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool |
| 2398 | 96.5% | install.yml → Post-install setup → block → automationhub → automationhub | Set auth header | Set auth header | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _token_credential \| bool |
| 2399 | 96.6% | install.yml → Post-install setup → block → automationhub → automationhub | Fallback to basic auth if token isn't provided | Fallback to basic auth if token isn't provided | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; not _token_credential \| bool |
| 2400 | 96.6% | install.yml → Post-install setup → block → automationhub → automationhub | set_fact | Set a host fact used by later tasks | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_seed_local is not defined; hostvars['localhost']['ansible_hostname'] is defined; ansible_hostname == hostvars['localhost']['ansible_hostname'] |
| 2401 | 96.7% | install.yml → Post-install setup → block → automationhub → automationhub | block | Begin conditional or grouped task block | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length |
| 2402 | 96.7% | install.yml → Post-install setup → block → automationhub → automationhub → block | block | Begin conditional or grouped task block | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length; not _collection_seed_local \| default(false) \| bool |
| 2403 | 96.7% | install.yml → Post-install setup → block → automationhub → automationhub → block → block | Create temporary directory for collections | Create temporary directory for collections | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length; not _collection_seed_local \| default(false) \| bool |
| 2404 | 96.8% | install.yml → Post-install setup → block → automationhub → automationhub → block → block | Copy collections | Copy collections (dest `{{ _collection_tmp.path }}`) | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length; not _collection_seed_local \| default(false) \| bool |
| 2405 | 96.8% | install.yml → Post-install setup → block → automationhub → automationhub → block | Seed {{ destination_repository }} collections | Seed {{ destination_repository }} collections (file `seed_collection.yml`) | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length |
| 2406 | 96.9% | install.yml → Post-install setup → block → automationhub → automationhub → block → seed_collection.yml | file `seed_collection.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/seed_collection.yml` | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length |
| 2407 | 96.9% | install.yml → Post-install setup → block → automationhub → automationhub → block → seed_collection.yml | set_fact | Set a host fact used by later tasks | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length |
| 2408 | 96.9% | install.yml → Post-install setup → block → automationhub → automationhub → block → seed_collection.yml | block | Begin conditional or grouped task block | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length |
| 2409 | 97.0% | install.yml → Post-install setup → block → automationhub → automationhub → block → seed_collection.yml → block | Check if collection exists | Check if collection exists | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length |
| 2410 | 97.0% | install.yml → Post-install setup → block → automationhub → automationhub → block → seed_collection.yml → block | block | Begin conditional or grouped task block | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length; _collection_exists.json.count > 0 |
| 2411 | 97.1% | install.yml → Post-install setup → block → automationhub → automationhub → block → seed_collection.yml → block → block | set_fact | Set a host fact used by later tasks | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length; _collection_exists.json.count > 0 |
| 2412 | 97.1% | install.yml → Post-install setup → block → automationhub → automationhub → block → seed_collection.yml → block → block | Skip if collection already exists | Skip if collection already exists | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length; _collection_exists.json.count > 0 |
| 2413 | 97.1% | install.yml → Post-install setup → block → automationhub → automationhub → block → seed_collection.yml → block | block | Begin conditional or grouped task block | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length; _collection_exists.json.count == 0 |
| 2414 | 97.2% | install.yml → Post-install setup → block → automationhub → automationhub → block → seed_collection.yml → block → block | Create '{{ _collection_namespace }}' namespace | Create '{{ _collection_namespace }}' namespace | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length; _collection_exists.json.count == 0 |
| 2415 | 97.2% | install.yml → Post-install setup → block → automationhub → automationhub → block → seed_collection.yml → block → block | Publish {{ _collection.name }} collection | Publish {{ _collection.name }} collection | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length; _collection_exists.json.count == 0 |
| 2416 | 97.3% | install.yml → Post-install setup → block → automationhub → automationhub → block → seed_collection.yml → block → block | set_fact | Set a host fact used by later tasks | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length; _collection_exists.json.count == 0 |
| 2417 | 97.3% | install.yml → Post-install setup → block → automationhub → automationhub → block → seed_collection.yml → block → block | Move collection to {{ destination_repository }} repo | Move collection to {{ destination_repository }} repo | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length; _collection_exists.json.count == 0 |
| 2418 | 97.3% | install.yml → Post-install setup → block → automationhub → automationhub → block → seed_collection.yml → block → block | Wait until move task is completed | Wait until move task is completed | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length; _collection_exists.json.count == 0 |
| 2419 | 97.4% | install.yml → Post-install setup → block → automationhub → automationhub → block → seed_collection.yml → block → block | set_fact | Set a host fact used by later tasks | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length; _collection_exists.json.count == 0 |
| 2420 | 97.4% | install.yml → Post-install setup → block → automationhub → automationhub → block → seed_collection.yml | block (rescue) | Run rescue tasks for block | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length |
| 2421 | 97.5% | install.yml → Post-install setup → block → automationhub → automationhub → block → seed_collection.yml → block (rescue) | Set seed result | Set seed result | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length |
| 2422 | 97.5% | install.yml → Post-install setup → block → automationhub → automationhub → block → seed_collection.yml | Record collection seed result | Record collection seed result | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length |
| 2423 | 97.5% | install.yml → Post-install setup → block → automationhub → automationhub | block (always) | Run always tasks for block | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length |
| 2424 | 97.6% | install.yml → Post-install setup → block → automationhub → automationhub → block (always) | Record collection seed result | Record collection seed result (dest `{{ _collection_seed_log_file }}`) | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length |
| 2425 | 97.6% | install.yml → Post-install setup → block → automationhub → automationhub → block (always) | Remove copied collections | Remove copied collections | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length; _collection_tmp.path is defined |
| 2426 | 97.7% | install.yml | Post-install insights setup | Play targeting `aap_valid_hosts` | — |
| 2427 | 97.7% | install.yml → Post-install insights setup | include_role | Include and run tasks from an Ansible role (role `misc`, tasks `post_install_insights_setup.yml`) | _rhsm_registered \| bool |
| 2428 | 97.7% | install.yml → Post-install insights setup → misc | role `misc` (`post_install_insights_setup.yml`) | Enter role task list | _rhsm_registered \| bool |
| 2429 | 97.8% | install.yml → Post-install insights setup → misc → misc | file `post_install_insights_setup.yml` | Task file `ansible/automation_platform_installer/roles/misc/tasks/post_install_insights_setup.yml` | _rhsm_registered \| bool |
| 2430 | 97.8% | install.yml → Post-install insights setup → misc → misc | Collect service facts | Collect service facts | _rhsm_registered \| bool |
| 2431 | 97.9% | install.yml → Post-install insights setup → misc → misc | Check if insights-register service exists | Check if insights-register service exists | _rhsm_registered \| bool |
| 2432 | 97.9% | install.yml → Post-install insights setup → misc → misc | Skip insights registration when insights-register service exists | Skip insights registration when insights-register service exists | _rhsm_registered \| bool; _insights_register_exists \| bool |
| 2433 | 97.9% | install.yml → Post-install insights setup → misc → misc | block | Begin conditional or grouped task block | _rhsm_registered \| bool; not _insights_register_exists \| bool; enable_insights_collection \| bool |
| 2434 | 98.0% | install.yml → Post-install insights setup → misc → misc → block | Ensure insights-client is installed | Ensure insights-client is installed | _rhsm_registered \| bool; not _insights_register_exists \| bool; enable_insights_collection \| bool |
| 2435 | 98.0% | install.yml → Post-install insights setup → misc → misc → block | Register with Insights | Register with Insights | _rhsm_registered \| bool; not _insights_register_exists \| bool; enable_insights_collection \| bool |
| 2436 | 98.1% | install.yml → Post-install insights setup → misc → misc | Unregister from Insights | Unregister from Insights | _rhsm_registered \| bool; not _insights_register_exists \| bool; not enable_insights_collection \| bool |
| 2437 | 98.1% | install.yml | Install PCP and configure pmproxy | Play targeting `automationgateway:automationcontroller:automationhub:automationedacontroller:database` | — |
| 2438 | 98.1% | install.yml → Install PCP and configure pmproxy | include_role | Include and run tasks from an Ansible role (role `monitoring`) | setup_monitoring \| default(false) \| bool |
| 2439 | 98.2% | install.yml → Install PCP and configure pmproxy → monitoring | role `monitoring` | Enter role task list | setup_monitoring \| default(false) \| bool |
| 2440 | 98.2% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring | file `main.yml` | Task file `ansible/automation_platform_installer/roles/monitoring/tasks/main.yml` | setup_monitoring \| default(false) \| bool |
| 2441 | 98.3% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring | include_tasks | Include a task file from the current role or playbook (file `setup-pcp.yml`) | setup_monitoring \| default(false) \| bool |
| 2442 | 98.3% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → setup-pcp.yml | file `setup-pcp.yml` | Task file `ansible/automation_platform_installer/roles/monitoring/tasks/setup-pcp.yml` | setup_monitoring \| default(false) \| bool |
| 2443 | 98.3% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → setup-pcp.yml | Setup performance co-pilot, collector daemon and pmproxy | Begin conditional or grouped task block | setup_monitoring \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('automationhub', [])) \| union(groups.get('automationedacontroller', [])) \| union(groups.get('database', [])) \| union(groups.get('automationgateway', [])) |
| 2444 | 98.4% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → setup-pcp.yml → Setup performance co-pilot, collector daemon and pmproxy | Install the PCP RPM and PMDA RPMs | Install the PCP RPM and PMDA RPMs | setup_monitoring \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('automationhub', [])) \| union(groups.get('automationedacontroller', [])) \| union(groups.get('database', [])) \| union(groups.get('automationgateway', [])) |
| 2445 | 98.4% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → setup-pcp.yml → Setup performance co-pilot, collector daemon and pmproxy | Flush handlers | Flush handlers (flush_handlers) | setup_monitoring \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('automationhub', [])) \| union(groups.get('automationedacontroller', [])) \| union(groups.get('database', [])) \| union(groups.get('automationgateway', [])) |
| 2446 | 98.5% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → setup-pcp.yml → Setup performance co-pilot, collector daemon and pmproxy | force restart and enable pcp collection daemons | force restart and enable pcp collection daemons | setup_monitoring \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('automationhub', [])) \| union(groups.get('automationedacontroller', [])) \| union(groups.get('database', [])) \| union(groups.get('automationgateway', [])) |
| 2447 | 98.5% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → setup-pcp.yml → Setup performance co-pilot, collector daemon and pmproxy | verify able to ping metrics | verify able to ping metrics | setup_monitoring \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('automationhub', [])) \| union(groups.get('automationedacontroller', [])) \| union(groups.get('database', [])) \| union(groups.get('automationgateway', [])) |
| 2448 | 98.6% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → setup-pcp.yml | Set the correct the pmlogger collector host | Begin conditional or grouped task block | setup_monitoring \| default(false) \| bool |
| 2449 | 98.6% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → setup-pcp.yml → Set the correct the pmlogger collector host | Set the pmlogger collector host to automationgateway[0] | Set the pmlogger collector host to automationgateway[0] | setup_monitoring \| default(false) \| bool; 'automationgateway' in groups and groups['automationgateway'] \| length != 0 |
| 2450 | 98.6% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → setup-pcp.yml → Set the correct the pmlogger collector host | Set the pmlogger collector host to automationcontroller[0] | Set the pmlogger collector host to automationcontroller[0] | setup_monitoring \| default(false) \| bool; pmlogger_collector_host is not defined and ('automationcontroller' in groups and groups['automationcontroller'] \| length != 0) |
| 2451 | 98.7% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → setup-pcp.yml → Set the correct the pmlogger collector host | Set the pmlogger collector host to automationhub[0] | Set the pmlogger collector host to automationhub[0] | setup_monitoring \| default(false) \| bool; pmlogger_collector_host is not defined and ('automationhub' in groups and groups['automationhub'] \| length != 0) |
| 2452 | 98.7% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → setup-pcp.yml → Set the correct the pmlogger collector host | set the pmlogger collector host to automationedacontroller[0] | set the pmlogger collector host to automationedacontroller[0] | setup_monitoring \| default(false) \| bool; pmlogger_collector_host is not defined and ('automationedacontroller' in groups and groups['automationedacontroller'] \| length != 0) |
| 2453 | 98.8% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → setup-pcp.yml | indicate which host will collect metrics from others | indicate which host will collect metrics from others | setup_monitoring \| default(false) \| bool |
| 2454 | 98.8% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → setup-pcp.yml | enable pmcd remote access | Begin conditional or grouped task block | setup_monitoring \| default(false) \| bool; inventory_hostname != pmlogger_collector_host |
| 2455 | 98.8% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → setup-pcp.yml → enable pmcd remote access | Allow remote access to pmcd | Allow remote access to pmcd | setup_monitoring \| default(false) \| bool; inventory_hostname != pmlogger_collector_host |
| 2456 | 98.9% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → setup-pcp.yml → enable pmcd remote access | start and enable pcp collection daemon | start and enable pcp collection daemon | setup_monitoring \| default(false) \| bool; inventory_hostname != pmlogger_collector_host; enable_remote_pmcd.changed |
| 2457 | 98.9% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → setup-pcp.yml → enable pmcd remote access | check pmcd remote access enabled | check pmcd remote access enabled | setup_monitoring \| default(false) \| bool; inventory_hostname != pmlogger_collector_host |
| 2458 | 99.0% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → setup-pcp.yml → enable pmcd remote access | ensure expected output | ensure expected output | setup_monitoring \| default(false) \| bool; inventory_hostname != pmlogger_collector_host |
| 2459 | 99.0% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → setup-pcp.yml | Setup pmlogger to collect from remote systems | Begin conditional or grouped task block | setup_monitoring \| default(false) \| bool; (inventory_hostname == pmlogger_collector_host) and enable_centralized_pmlogger\|default('True')\|bool |
| 2460 | 99.0% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → setup-pcp.yml → Setup pmlogger to collect from remote systems | Setup config to monitor remote systems | Setup config to monitor remote systems | setup_monitoring \| default(false) \| bool; (inventory_hostname == pmlogger_collector_host) and enable_centralized_pmlogger\|default('True')\|bool |
| 2461 | 99.1% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring | include_tasks | Include a task file from the current role or playbook (file `postgres-pmda.yml`) | setup_monitoring \| default(false) \| bool; inventory_hostname in groups.get('database', []) |
| 2462 | 99.1% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → postgres-pmda.yml | file `postgres-pmda.yml` | Task file `ansible/automation_platform_installer/roles/monitoring/tasks/postgres-pmda.yml` | setup_monitoring \| default(false) \| bool; inventory_hostname in groups.get('database', []) |
| 2463 | 99.2% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → postgres-pmda.yml | Install postgres exporter on database node | Install postgres exporter on database node | setup_monitoring \| default(false) \| bool; inventory_hostname in groups.get('database', []) |
| 2464 | 99.2% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → postgres-pmda.yml | Check if postgres PMDA is already installed | Check if postgres PMDA is already installed | setup_monitoring \| default(false) \| bool; inventory_hostname in groups.get('database', []) |
| 2465 | 99.2% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → postgres-pmda.yml | Install postgres exporter to the perf copilot collector daemon | Install postgres exporter to the perf copilot collector daemon | setup_monitoring \| default(false) \| bool; inventory_hostname in groups.get('database', []); postgres_pmda_check.rc != 0 |
| 2466 | 99.3% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → postgres-pmda.yml | Pause to allow metrics from postgres to show up on pcp | Pause to allow metrics from postgres to show up on pcp | setup_monitoring \| default(false) \| bool; inventory_hostname in groups.get('database', []); postgres_install_result is changed |
| 2467 | 99.3% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → postgres-pmda.yml | verify able to ping metrics | verify able to ping metrics | setup_monitoring \| default(false) \| bool; inventory_hostname in groups.get('database', []) |
| 2468 | 99.4% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring | include_tasks | Include a task file from the current role or playbook (file `nginx-pmda.yml`) | setup_monitoring \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('automationhub', [])) \| union(_automationedacontroller_web_hosts) |
| 2469 | 99.4% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → nginx-pmda.yml | file `nginx-pmda.yml` | Task file `ansible/automation_platform_installer/roles/monitoring/tasks/nginx-pmda.yml` | setup_monitoring \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('automationhub', [])) \| union(_automationedacontroller_web_hosts) |
| 2470 | 99.4% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → nginx-pmda.yml | Install nginx exporter on control, hybrid, eda, and automationhub nodes | Install nginx exporter on control, hybrid, eda, and automationhub nodes | setup_monitoring \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('automationhub', [])) \| union(_automationedacontroller_web_hosts) |
| 2471 | 99.5% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → nginx-pmda.yml | Enable nginx status metrics on Controller, Hub, EDA (only for localhost) | Enable nginx status metrics on Controller, Hub, EDA (only for localhost) | setup_monitoring \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('automationhub', [])) \| union(_automationedacontroller_web_hosts) |
| 2472 | 99.5% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → nginx-pmda.yml | Check if nginx PMDA is already installed | Check if nginx PMDA is already installed | setup_monitoring \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('automationhub', [])) \| union(_automationedacontroller_web_hosts) |
| 2473 | 99.6% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → nginx-pmda.yml | Install nginx exporter to the perf copilot collector daemon | Install nginx exporter to the perf copilot collector daemon | setup_monitoring \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('automationhub', [])) \| union(_automationedacontroller_web_hosts); nginx_pmda_check.rc != 0 |
| 2474 | 99.6% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → nginx-pmda.yml | verify able to ping metrics | verify able to ping metrics | setup_monitoring \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('automationhub', [])) \| union(_automationedacontroller_web_hosts) |
| 2475 | 99.6% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring | include_tasks | Include a task file from the current role or playbook (file `openmetrics-pmda.yml`) | setup_monitoring \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) |
| 2476 | 99.7% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → openmetrics-pmda.yml | file `openmetrics-pmda.yml` | Task file `ansible/automation_platform_installer/roles/monitoring/tasks/openmetrics-pmda.yml` | setup_monitoring \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) |
| 2477 | 99.7% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → openmetrics-pmda.yml | Install openmetrics exporter on Automation Controller control/hybrid nodes | Install openmetrics exporter on Automation Controller control/hybrid nodes | setup_monitoring \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) |
| 2478 | 99.8% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → openmetrics-pmda.yml | Enable openmetrics for callback_reciver, dispatcher and websocket (only for localhost) | Enable openmetrics for callback_reciver, dispatcher and websocket (only for localhost) | setup_monitoring \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) |
| 2479 | 99.8% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → openmetrics-pmda.yml | Create the symlink for openmetrics config under config.d | Create the symlink for openmetrics config under config.d | setup_monitoring \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) |
| 2480 | 99.8% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → openmetrics-pmda.yml | Check if openmetrics PMDA is already installed | Check if openmetrics PMDA is already installed | setup_monitoring \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) |
| 2481 | 99.9% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → openmetrics-pmda.yml | Install openmetrics exporter to the perf copilot collector daemon | Install openmetrics exporter to the perf copilot collector daemon | setup_monitoring \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []); openmetrics_pmda_check.rc != 0 |
| 2482 | 99.9% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → openmetrics-pmda.yml | verify able to ping metrics | verify able to ping metrics | setup_monitoring \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) |
| 2483 | 100.0% | install.yml | Post-install cleanup | Play targeting `aap_valid_hosts` | — |
| 2484 | 100.0% | install.yml → Post-install cleanup | Remove stale packages | Remove stale packages | — |
<!-- prettier-ignore-end -->

## Notes

- **Location** shows the play and role/task-file breadcrumb path from `install.yml`.
- The same role or task file may appear multiple times when invoked from different plays or blocks.
- Recursive cycles (A includes B includes A) are detected and marked as skipped.
- Handler tasks (`notify`) are not executed inline; they run when notified and flushed.
- **Progress** runs from 0% (first row) to 100% (last row).
- **Do not Format Document** on this file — Markdown table formatters pad every column to the widest cell (~400+ char lines). Regenerate with `python3 installer/scripts/extract_install_playbook_tasks.py` instead.
- Inventories in this folder are co-located with this reference — `@`-mention `installer/AAP{XY}/{method}/` for both.
- OpenShift operator examples are in the sibling [`openshift/`](../../../openshift/) folder (no install playbook).
