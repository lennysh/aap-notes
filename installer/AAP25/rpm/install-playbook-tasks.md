# AAP 2.5 — Rpm Install Playbook Tasks

Task execution order for the AAP install playbook, expanded recursively through included roles and task files. Conditional tasks include a `When` column; tasks inside blocks inherit the block condition. **Progress** runs from 0% (first row) to 100% (last row) based on position in the expanded task list.

| Field | Value |
|-------|-------|
| Playbook command | `./setup.sh -i inventory` |
| Source playbook | `collections/ansible_collections/ansible/automation_platform_installer/playbooks/install.yml` |
| Installer dump | `.installer-dumps/2.5/ansible-automation-platform-setup-2.5-23` |
| Total tasks (expanded) | 2547 |

## Task table

<!-- prettier-ignore-start -->
<!-- Do not format this table — editors pad columns to the widest cell. Regenerate instead. -->
| # | Progress | Location | Task | Description | When |
|---:|---:|---|---|---|---|
| 1 | 0.0% | install.yml | Warn if no core component is enabled | Play targeting `localhost` | — |
| 2 | 0.0% | install.yml → Warn if no core component is enabled | Warn if no core component is enabled | Warn if no core component is enabled | not groups['automationgateway'] \| default([]) \| length; not groups['automationcontroller'] \| default([]) \| length; not groups['automationhub'] \| default([]) \| length; not groups['automationedacontroller'] \| default([]) \| length; not groups['database'] \| default([]) \| length |
| 3 | 0.1% | install.yml | Group all valid hosts for AAP installation | Play targeting `tower:automationgateway:automationcontroller:automationhub:automationedacontroller:database:execution_nodes` | — |
| 4 | 0.1% | install.yml → Group all valid hosts for AAP installation | group hosts used in AAP installer | group hosts used in AAP installer | — |
| 5 | 0.2% | install.yml | unnamed play | Play targeting `aap_valid_hosts` | — |
| 6 | 0.2% | install.yml → unnamed play | include_role | Include and run tasks from an Ansible role (role `check_config_static`) | — |
| 7 | 0.2% | install.yml → unnamed play → check_config_static | role `check_config_static` | Enter role task list | — |
| 8 | 0.3% | install.yml → unnamed play → check_config_static → check_config_static | file `main.yml` | Task file `ansible/automation_platform_installer/roles/check_config_static/tasks/main.yml` | — |
| 9 | 0.3% | install.yml → unnamed play → check_config_static → check_config_static | Ensure cluster hosts are not localhost | Ensure cluster hosts are not localhost | groups['automationcontroller'] \| default([]) \| length > 1; ansible_host is match("127.*") or ansible_host is match("localhost*") or ansible_host == "::1" |
| 10 | 0.4% | install.yml → unnamed play → check_config_static → check_config_static | Check that group names do not conflict with instance names | Check that group names do not conflict with instance names | ('instance_group_' + ansible_host) in groups |
| 11 | 0.4% | install.yml → unnamed play → check_config_static → check_config_static | service_facts | Run `service_facts` | — |
| 12 | 0.4% | install.yml → unnamed play → check_config_static → check_config_static | block | Begin conditional or grouped task block | ansible_facts.services['fapolicyd.service'] \| default([]) \| length; ansible_facts.services['fapolicyd.service'].state == 'running' |
| 13 | 0.5% | install.yml → unnamed play → check_config_static → check_config_static → block | Check if fapolicyd is permissive | Check if fapolicyd is permissive | ansible_facts.services['fapolicyd.service'] \| default([]) \| length; ansible_facts.services['fapolicyd.service'].state == 'running' |
| 14 | 0.5% | install.yml → unnamed play → check_config_static → check_config_static → block | fail | Fail the play with an error message | ansible_facts.services['fapolicyd.service'] \| default([]) \| length; ansible_facts.services['fapolicyd.service'].state == 'running'; fapolicy_permissive is failed |
| 15 | 0.5% | install.yml → unnamed play → check_config_static → check_config_static | Perform inventory sanity checks | Begin conditional or grouped task block | — |
| 16 | 0.6% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Detect pre-2.x inventory and offer a migration | Detect pre-2.x inventory and offer a migration | — |
| 17 | 0.6% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Detect unsupported HA inventory file | Detect unsupported HA inventory file | 'primary' in groups or 'secondary' in groups |
| 18 | 0.7% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Check that no instance groups are named automationcontroller | Check that no instance groups are named automationcontroller | item != "automationcontroller"; item.replace('instance_group_', '') == "automationcontroller" |
| 19 | 0.7% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure only one database host exists | Ensure only one database host exists | groups['database'] \| default([]) \| length > 1 |
| 20 | 0.7% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure redis mode is a valid choice | Ensure redis mode is a valid choice | — |
| 21 | 0.8% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure redis cluster is correctly configured | Ensure redis cluster is correctly configured | _redis_mode == 'cluster' |
| 22 | 0.8% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure [redis] group is not defined when redis_mode is standalone | Ensure [redis] group is not defined when redis_mode is standalone | _redis_mode == 'standalone'; groups.get('redis', []) \| length > 0 |
| 23 | 0.9% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure redis use sockets is not set globally | Ensure redis use sockets is not set globally | redis_use_sockets \| default('') \| string \| length > 0 |
| 24 | 0.9% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure postgres_extra_settings is a dictonary | Ensure postgres_extra_settings is a dictonary | postgres_extra_settings is defined |
| 25 | 0.9% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Validate feature flags | Begin conditional or grouped task block | feature_flags is defined |
| 26 | 1.0% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks → Validate feature flags | Ensure feature_flags is a dictonary | Ensure feature_flags is a dictonary | feature_flags is defined |
| 27 | 1.0% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks → Validate feature flags | Ensure feature_flags keys/values are valid | Ensure feature_flags keys/values are valid | feature_flags is defined |
| 28 | 1.1% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure automationhub_pg related variables are defined if more than one component is enabled | Ensure automationhub_pg related variables are defined if more than one component is enabled | groups['automationhub'] \| default([]) \| length; groups['automationcontroller'] \| default([]) \| length or groups['automationedacontroller'] \| default([]) \| length; not automationhub_pg_host \| default('') \| length; not automationhub_db_template \| default([]) \| length |
| 29 | 1.1% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure automationedacontroller_pg related variables are defined if more than one component is enabled | Ensure automationedacontroller_pg related variables are defined if more than one component is enabled | groups['automationedacontroller'] \| default([]) \| length; groups['automationhub'] \| default([]) \| length or groups['automationcontroller'] \| default([]) \| length; not automationedacontroller_pg_host \| default('') \| length; not automationedacontroller_db_template \| default([]) \| length |
| 30 | 1.1% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Fail if Automation Controller host is localhost | Fail if Automation Controller host is localhost | item is match("127.*") or item is match("localhost*") or item == "::1" |
| 31 | 1.2% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Fail if Automation Hub host is localhost | Fail if Automation Hub host is localhost | item is match("127.*") or item is match("localhost*") or item == "::1" |
| 32 | 1.2% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Fail if Automation EDA Controller host is localhost | Fail if Automation EDA Controller host is localhost | item is match("127.*") or item is match("localhost*") or item == "::1" |
| 33 | 1.3% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure that when a database host is specified, that component hosts and ports are defined | Begin conditional or grouped task block | groups['database'] \| default([]) \| length |
| 34 | 1.3% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks → Ensure that when a database host is specified, that component hosts and ports are defined | Ensure for Controller that when a database host is specificed that pg_host and pg_port are specified | Ensure for Controller that when a database host is specificed that pg_host and pg_port are specified | groups['database'] \| default([]) \| length; groups['automationcontroller'] \| default([]) \| length; not pg_host \| default('') \| length or not _pg_port \| default('') \| string \| length; not automationcontroller_db_template \| default([]) \| length |
| 35 | 1.3% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks → Ensure that when a database host is specified, that component hosts and ports are defined | Ensure for Automation Hub that when a database host is specificed that pg_host and pg_port are specified | Ensure for Automation Hub that when a database host is specificed that pg_host and pg_port are specified | groups['database'] \| default([]) \| length; groups['automationhub'] \| default([]) \| length; not automationhub_pg_host \| default('') \| length or not _automationhub_pg_port \| default('') \| string \| length; not automationhub_db_template \| default([]) \| length |
| 36 | 1.4% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks → Ensure that when a database host is specified, that component hosts and ports are defined | Ensure for Automation EDA Controller that when a database host is specificed that pg_host and pg_port are specified | Ensure for Automation EDA Controller that when a database host is specificed that pg_host and pg_port are specified | groups['database'] \| default([]) \| length; groups['automationedacontroller'] \| default([]) \| length; not automationedacontroller_pg_host \| default('') \| length or not _automationedacontroller_pg_port \| default('') \| string \| length; not automationedacontroller_db_template \| default([]) \| length |
| 37 | 1.4% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure that pg_hashed_password is not set | Ensure that pg_hashed_password is not set | pg_hashed_password is defined |
| 38 | 1.5% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure certificates are provided when pg_sslmode is verify-ca | Ensure certificates are provided when pg_sslmode is verify-ca | _pg_sslmode == 'verify-ca'; not pgclient_sslcert \| default('') \| length or not pgclient_sslkey \| default('') \| length |
| 39 | 1.5% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure certificates are provided when automationhub_pg_sslmode is verify-ca | Ensure certificates are provided when automationhub_pg_sslmode is verify-ca | _automationhub_pg_sslmode == 'verify-ca'; not automationhub_pgclient_sslcert \| default('') \| length or not automationhub_pgclient_sslkey \| default('') \| length |
| 40 | 1.5% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure certificates are provided when automationedacontroller_pg_sslmode is verify-ca | Ensure certificates are provided when automationedacontroller_pg_sslmode is verify-ca | _automationedacontroller_pg_sslmode == 'verify-ca'; not automationedacontroller_pgclient_sslcert \| default('') \| length or not automationedacontroller_pgclient_sslkey \| default('') \| length |
| 41 | 1.6% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure certificates are provided when automationgateway_pg_sslmode is verify-ca | Ensure certificates are provided when automationgateway_pg_sslmode is verify-ca | _automationgateway_pg_sslmode == 'verify-ca'; not automationgateway_pgclient_sslcert \| default('') \| length or not automationgateway_pgclient_sslkey \| default('') \| length |
| 42 | 1.6% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure that if we have web cert, we also have key (Controller) | Ensure that if we have web cert, we also have key (Controller) | groups['automationcontroller'] \| default([]) \| length; (web_server_ssl_cert is defined and web_server_ssl_key is not defined) or (web_server_ssl_key is defined and web_server_ssl_cert is not defined) |
| 43 | 1.6% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure that if we have web cert, we also have key (Hub) | Ensure that if we have web cert, we also have key (Hub) | groups['automationhub'] \| default([]) \| length; (automationhub_ssl_cert is defined and automationhub_ssl_key is not defined) or (automationhub_ssl_key is defined and automationhub_ssl_cert is not defined) |
| 44 | 1.7% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure that if we have web cert, we also have key (Gateway) | Ensure that if we have web cert, we also have key (Gateway) | groups['automationgateway'] \| default([]) \| length; (automationgateway_ssl_cert is defined and automationgateway_ssl_key is not defined) or (automationgateway_ssl_key is defined and automationgateway_ssl_cert is not defined) |
| 45 | 1.7% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure that a key is provided if a CA certificate file is also provided for the internally managed CA | Ensure that a key is provided if a CA certificate file is also provided for the internally managed CA | aap_ca_cert_file is defined and aap_ca_key_file is not defined |
| 46 | 1.8% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Preflight check - verify aap_ca_server_hostname is one of the platform components | Preflight check - verify aap_ca_server_hostname is one of the platform components | aap_ca_server_hostname is defined; aap_ca_server_hostname not in ((groups['automationgateway']  \| default([])) + (groups['automationcontroller']  \| default([])) + (groups['automationhub']  \| default([])) + (groups['automationedacontroller']  \| default([]))) |
| 47 | 1.8% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure Gateway host exists | Ensure Gateway host exists | not groups['automationgateway'] \| default([]) \| length |
| 48 | 1.8% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure automationedacontroller_max_running_activations is a positive integer | Ensure automationedacontroller_max_running_activations is a positive integer | automationedacontroller_max_running_activations \| default('') \| string \| length |
| 49 | 1.9% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure all items in EDA CSRF_TRUSTED_ORIGINS list have the required http/s schema | Ensure all items in EDA CSRF_TRUSTED_ORIGINS list have the required http/s schema | automationedacontroller_trusted_origins \| default([]) \| list \| length; item is not regex("^https?://.*") |
| 50 | 1.9% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | HA mode requires an external postgres database with pg_host defined | HA mode requires an external postgres database with pg_host defined | groups['automationcontroller'] \| default([]) \| length > 1; not pg_host \| default('') \| length; not automationcontroller_db_template \| default([]) \| length |
| 51 | 2.0% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | HA mode requires an external postgres database with pg_port defined | HA mode requires an external postgres database with pg_port defined | groups['automationcontroller'] \| default([]) \| length > 1; not _pg_port \| default(0) \| int; not automationcontroller_db_template \| default([]) \| length |
| 52 | 2.0% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Hub HA mode requires an external postgres database with automationhub_pg_host defined | Hub HA mode requires an external postgres database with automationhub_pg_host defined | groups['automationhub'] \| default([]) \| length > 1; not automationhub_pg_host \| default('') \| length; not automationhub_db_template \| default([]) \| length |
| 53 | 2.0% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Hub HA mode requires an external postgres database with automationhub_pg_port defined | Hub HA mode requires an external postgres database with automationhub_pg_port defined | groups['automationhub'] \| default([]) \| length > 1; not automationhub_pg_port \| default(0) \| int; not automationhub_db_template \| default([]) \| length |
| 54 | 2.1% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure registry_username and registry_password are set for registry.redhat.io | Ensure registry_username and registry_password are set for registry.redhat.io | groups['automationcontroller'] \| default([]) \| length or groups['automationhub'] \| default([]) \| length; _registry_url == 'registry.redhat.io'; not registry_username \| default('') \| length or not registry_password \| default('') \| length; not bundle_install \| bool or ( bundle_install \| bool and not _ee_from_hub_only \| bool ) |
| 55 | 2.1% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure that Automation Hub signing script and signing keys are provided when collection signing is enabled | Ensure that Automation Hub signing script and signing keys are provided when collection signing is enabled | groups['automationhub'] \| default([]) \| length; automationhub_create_default_collection_signing_service \| default(false) \| bool; not automationhub_collection_signing_service_key \| default('') \| length or not automationhub_collection_signing_service_script \| default('') \| length |
| 56 | 2.2% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure that Automation Hub signing script and signing keys are provided when container signing is enabled | Ensure that Automation Hub signing script and signing keys are provided when container signing is enabled | groups['automationhub'] \| default([]) \| length; automationhub_create_default_container_signing_service \| default(false) \| bool; not automationhub_container_signing_service_key \| default('') \| length or not automationhub_container_signing_service_script \| default('') \| length |
| 57 | 2.2% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Ensure that Automation Hub collection signing is enabled when auto signing is enabled | Ensure that Automation Hub collection signing is enabled when auto signing is enabled | groups['automationhub'] \| default([]) \| length; automationhub_auto_sign_collections \| default(false) \| bool; not automationhub_create_default_collection_signing_service \| default(false) \| bool |
| 58 | 2.2% | install.yml → unnamed play → check_config_static → check_config_static → Perform inventory sanity checks | Check if automationgateway_main_url starts with a http protocol | Check if automationgateway_main_url starts with a http protocol | automationgateway_main_url \| default('') \| length; automationgateway_main_url is not regex("^https?://.*") |
| 59 | 2.3% | install.yml → unnamed play | include_role | Include and run tasks from an Ansible role (role `config_dynamic`) | — |
| 60 | 2.3% | install.yml → unnamed play → config_dynamic | role `config_dynamic` | Enter role task list | — |
| 61 | 2.4% | install.yml → unnamed play → config_dynamic → config_dynamic | file `main.yml` | Task file `ansible/automation_platform_installer/roles/config_dynamic/tasks/main.yml` | — |
| 62 | 2.4% | install.yml → unnamed play → config_dynamic → config_dynamic | set_fact | Set a host fact used by later tasks | not routable_hostname \| default('') \| length |
| 63 | 2.4% | install.yml → unnamed play → config_dynamic → config_dynamic | Check that only eligible hosts are part of the redis group | Check that only eligible hosts are part of the redis group | groups.get('redis', []) \| intersect(aap_ineligible_redis_nodes) \| length > 0 |
| 64 | 2.5% | install.yml → unnamed play → config_dynamic → config_dynamic | Set database to internal or external | Set database to internal or external | — |
| 65 | 2.5% | install.yml → unnamed play → config_dynamic → config_dynamic | Hub set database to internal or external | Hub set database to internal or external | — |
| 66 | 2.6% | install.yml → unnamed play → config_dynamic → config_dynamic | Automation EDA Controller set database to internal or external | Automation EDA Controller set database to internal or external | — |
| 67 | 2.6% | install.yml → unnamed play → config_dynamic → config_dynamic | Automation Gateway set database to internal or external | Automation Gateway set database to internal or external | — |
| 68 | 2.6% | install.yml → unnamed play → config_dynamic → config_dynamic | Controller database decision | Controller database decision | — |
| 69 | 2.7% | install.yml → unnamed play → config_dynamic → config_dynamic | Hub database decision | Hub database decision | — |
| 70 | 2.7% | install.yml → unnamed play → config_dynamic → config_dynamic | EDA Controller database decision | EDA Controller database decision | — |
| 71 | 2.7% | install.yml → unnamed play → config_dynamic → config_dynamic | Gateway database decision | Gateway database decision | — |
| 72 | 2.8% | install.yml → unnamed play → config_dynamic → config_dynamic | Set postgres host and port to local if not set | Set postgres host and port to local if not set | not pg_host \| default('') \| length or _pg_port == '' |
| 73 | 2.8% | install.yml → unnamed play → config_dynamic → config_dynamic | Set Automation EDA Controller postgres host and port to local if not set | Set Automation EDA Controller postgres host and port to local if not set | not automationedacontroller_pg_host \| default('') \| length or _automationedacontroller_pg_port == '' |
| 74 | 2.9% | install.yml → unnamed play → config_dynamic → config_dynamic | Set Automation Gateway postgres host and port to local if not set | Set Automation Gateway postgres host and port to local if not set | not automationgateway_pg_host \| default('') \| length or _automationgateway_pg_port == '' |
| 75 | 2.9% | install.yml → unnamed play → config_dynamic → config_dynamic | Automation Controller postgres client cert auth decision | Automation Controller postgres client cert auth decision | groups['automationcontroller'] \| default([]) \| length; _pg_sslmode is in ['verify-ca', 'verify-full']; pgclient_sslcert \| default('') \| length; pgclient_sslkey \| default('') \| length |
| 76 | 2.9% | install.yml → unnamed play → config_dynamic → config_dynamic | Automationhub postgres client cert auth decision | Automationhub postgres client cert auth decision | groups['automationhub'] \| default([]) \| length; _automationhub_pg_sslmode is in ['verify-ca', 'verify-full']; automationhub_pgclient_sslcert \| default('') \| length; automationhub_pgclient_sslkey \| default('') \| length |
| 77 | 3.0% | install.yml → unnamed play → config_dynamic → config_dynamic | Automation EDA Controller postgres client cert auth decision | Automation EDA Controller postgres client cert auth decision | groups['automationedacontroller'] \| default([]) \| length; _automationedacontroller_pg_sslmode is in ['verify-ca', 'verify-full']; automationedacontroller_pgclient_sslcert \| default('') \| length; automationedacontroller_pgclient_sslkey \| default('') \| length |
| 78 | 3.0% | install.yml → unnamed play → config_dynamic → config_dynamic | Automation Gateway postgres client cert auth decision | Automation Gateway postgres client cert auth decision | groups['automationgateway'] \| default([]) \| length; _automationgateway_pg_sslmode is in ['verify-ca', 'verify-full']; automationgateway_pgclient_sslcert \| default('') \| length; automationgateway_pgclient_sslkey \| default('') \| length |
| 79 | 3.1% | install.yml → unnamed play → config_dynamic → config_dynamic | Configure postgres certificates for verify-ca | Configure postgres certificates for verify-ca | awx_pg_cert_auth |
| 80 | 3.1% | install.yml → unnamed play → config_dynamic → config_dynamic | Configure automationhub postgres certificates for verify-ca | Configure automationhub postgres certificates for verify-ca | automationhub_pg_cert_auth |
| 81 | 3.1% | install.yml → unnamed play → config_dynamic → config_dynamic | Configure automationedacontroller postgres certificates for verify-ca | Configure automationedacontroller postgres certificates for verify-ca | automationedacontroller_pg_cert_auth |
| 82 | 3.2% | install.yml → unnamed play → config_dynamic → config_dynamic | Configure automationgateway postgres certificates for verify-ca | Configure automationgateway postgres certificates for verify-ca | automationgateway_pg_cert_auth |
| 83 | 3.2% | install.yml → unnamed play → config_dynamic → config_dynamic | Ensure connectivity to hosts and gather facts | Ensure connectivity to hosts and gather facts | — |
| 84 | 3.3% | install.yml → unnamed play → config_dynamic → config_dynamic | Ensure user is root | Ensure user is root | ansible_effective_user_id != config_dynamic_user |
| 85 | 3.3% | install.yml → unnamed play → config_dynamic → config_dynamic | Check if node is registered with RHSM | Check if node is registered with RHSM | — |
| 86 | 3.3% | install.yml → unnamed play → config_dynamic → config_dynamic | set_fact | Set a host fact used by later tasks | — |
| 87 | 3.4% | install.yml → unnamed play → config_dynamic → config_dynamic | Collect ansible_hostname for localhost | Collect ansible_hostname for localhost | — |
| 88 | 3.4% | install.yml → unnamed play → config_dynamic → config_dynamic | Exclude {{ ansible_package_name }} from package upgrade if control node | Exclude {{ ansible_package_name }} from package upgrade if control node | ansible_hostname == hostvars['localhost']['ansible_hostname'] |
| 89 | 3.5% | install.yml → unnamed play → config_dynamic → config_dynamic | include_role | Include and run tasks from an Ansible role (role `automationhub`, tasks `dynamic_hostname_facts`) | inventory_hostname in groups.get('automationhub', []) |
| 90 | 3.5% | install.yml → unnamed play → config_dynamic → config_dynamic → automationhub | role `automationhub` (`dynamic_hostname_facts`) | Enter role task list | inventory_hostname in groups.get('automationhub', []) |
| 91 | 3.5% | install.yml → unnamed play → config_dynamic → config_dynamic → automationhub → automationhub | file `dynamic_hostname_facts.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/dynamic_hostname_facts.yml` | inventory_hostname in groups.get('automationhub', []) |
| 92 | 3.6% | install.yml → unnamed play → config_dynamic → config_dynamic → automationhub → automationhub | Set Hub hostname | Set Hub hostname | inventory_hostname in groups.get('automationhub', []) |
| 93 | 3.6% | install.yml → unnamed play → config_dynamic → config_dynamic → automationhub → automationhub | Determine proper protocol and port | Determine proper protocol and port | inventory_hostname in groups.get('automationhub', []) |
| 94 | 3.7% | install.yml → unnamed play → config_dynamic → config_dynamic → automationhub → automationhub | Determine Automation Hub main URL | Determine Automation Hub main URL | inventory_hostname in groups.get('automationhub', []) |
| 95 | 3.7% | install.yml → unnamed play → config_dynamic → config_dynamic → automationhub → automationhub | Check if automationhub_main_url starts with a http protocol | Check if automationhub_main_url starts with a http protocol | inventory_hostname in groups.get('automationhub', []); _automationhub_main_url is not regex("^https?://.*") |
| 96 | 3.7% | install.yml → unnamed play → config_dynamic → config_dynamic | include_role | Include and run tasks from an Ansible role (role `automationcontroller`, tasks `dynamic_hostname_facts`) | inventory_hostname in groups.get('automationcontroller', []) |
| 97 | 3.8% | install.yml → unnamed play → config_dynamic → config_dynamic → automationcontroller | role `automationcontroller` (`dynamic_hostname_facts`) | Enter role task list | inventory_hostname in groups.get('automationcontroller', []) |
| 98 | 3.8% | install.yml → unnamed play → config_dynamic → config_dynamic → automationcontroller → automationcontroller | file `dynamic_hostname_facts.yml` | Task file `ansible/automation_platform_installer/roles/automationcontroller/tasks/dynamic_hostname_facts.yml` | inventory_hostname in groups.get('automationcontroller', []) |
| 99 | 3.8% | install.yml → unnamed play → config_dynamic → config_dynamic → automationcontroller → automationcontroller | Set controller hostname | Set controller hostname | inventory_hostname in groups.get('automationcontroller', []) |
| 100 | 3.9% | install.yml → unnamed play → config_dynamic → config_dynamic → automationcontroller → automationcontroller | set_fact | Set a host fact used by later tasks | inventory_hostname in groups.get('automationcontroller', []); (_nginx_disable_https \| bool and _nginx_http_port \| string != '80') or (not _nginx_disable_https \| bool and _nginx_https_port \| string != '443') |
| 101 | 3.9% | install.yml → unnamed play → config_dynamic → config_dynamic → automationcontroller → automationcontroller | set_fact | Set a host fact used by later tasks | inventory_hostname in groups.get('automationcontroller', []) |
| 102 | 4.0% | install.yml → unnamed play → config_dynamic → config_dynamic | include_role | Include and run tasks from an Ansible role (role `automationgateway`, tasks `dynamic_hostname_facts`) | inventory_hostname in groups.get('automationgateway', []) |
| 103 | 4.0% | install.yml → unnamed play → config_dynamic → config_dynamic → automationgateway | role `automationgateway` (`dynamic_hostname_facts`) | Enter role task list | inventory_hostname in groups.get('automationgateway', []) |
| 104 | 4.0% | install.yml → unnamed play → config_dynamic → config_dynamic → automationgateway → automationgateway | file `dynamic_hostname_facts.yml` | Task file `ansible/automation_platform_installer/roles/automationgateway/tasks/dynamic_hostname_facts.yml` | inventory_hostname in groups.get('automationgateway', []) |
| 105 | 4.1% | install.yml → unnamed play → config_dynamic → config_dynamic → automationgateway → automationgateway | Set automation gateway hostname and port | Set automation gateway hostname and port | inventory_hostname in groups.get('automationgateway', []) |
| 106 | 4.1% | install.yml → unnamed play → config_dynamic → config_dynamic → automationgateway → automationgateway | Set automation gateway admin URL | Set automation gateway admin URL | inventory_hostname in groups.get('automationgateway', []) |
| 107 | 4.2% | install.yml → unnamed play → config_dynamic → config_dynamic | include_role | Include and run tasks from an Ansible role (role `automationgateway`, tasks `main_url_facts`) | 'automationgateway' in ansible_play_hosts \| map('extract', hostvars) \| map(attribute='group_names') \| flatten; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('automationhub', [])) \| union(groups.get('automationedacontroller', [])) \| union(groups.get('automationgateway', [])) |
| 108 | 4.2% | install.yml → unnamed play → config_dynamic → config_dynamic → automationgateway | role `automationgateway` (`main_url_facts`) | Enter role task list | 'automationgateway' in ansible_play_hosts \| map('extract', hostvars) \| map(attribute='group_names') \| flatten; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('automationhub', [])) \| union(groups.get('automationedacontroller', [])) \| union(groups.get('automationgateway', [])) |
| 109 | 4.2% | install.yml → unnamed play → config_dynamic → config_dynamic → automationgateway → automationgateway | file `main_url_facts.yml` | Task file `ansible/automation_platform_installer/roles/automationgateway/tasks/main_url_facts.yml` | 'automationgateway' in ansible_play_hosts \| map('extract', hostvars) \| map(attribute='group_names') \| flatten; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('automationhub', [])) \| union(groups.get('automationedacontroller', [])) \| union(groups.get('automationgateway', [])) |
| 110 | 4.3% | install.yml → unnamed play → config_dynamic → config_dynamic → automationgateway → automationgateway | Set automation gateway main URL | Set automation gateway main URL | 'automationgateway' in ansible_play_hosts \| map('extract', hostvars) \| map(attribute='group_names') \| flatten; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('automationhub', [])) \| union(groups.get('automationedacontroller', [])) \| union(groups.get('automationgateway', [])) |
| 111 | 4.3% | install.yml → unnamed play → config_dynamic → config_dynamic → automationgateway → automationgateway | Set automation gateway main hostname/port | Set automation gateway main hostname/port | 'automationgateway' in ansible_play_hosts \| map('extract', hostvars) \| map(attribute='group_names') \| flatten; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('automationhub', [])) \| union(groups.get('automationedacontroller', [])) \| union(groups.get('automationgateway', [])) |
| 112 | 4.4% | install.yml → unnamed play → config_dynamic → config_dynamic | include_role | Include and run tasks from an Ansible role (role `automationedacontroller`, tasks `dynamic_hostname_facts`) | inventory_hostname in groups.get('automationedacontroller', []) |
| 113 | 4.4% | install.yml → unnamed play → config_dynamic → config_dynamic → automationedacontroller | role `automationedacontroller` (`dynamic_hostname_facts`) | Enter role task list | inventory_hostname in groups.get('automationedacontroller', []) |
| 114 | 4.4% | install.yml → unnamed play → config_dynamic → config_dynamic → automationedacontroller → automationedacontroller | file `dynamic_hostname_facts.yml` | Task file `ansible/automation_platform_installer/roles/automationedacontroller/tasks/dynamic_hostname_facts.yml` | inventory_hostname in groups.get('automationedacontroller', []) |
| 115 | 4.5% | install.yml → unnamed play → config_dynamic → config_dynamic → automationedacontroller → automationedacontroller | Set controller hostname | Set controller hostname | inventory_hostname in groups.get('automationedacontroller', []) |
| 116 | 4.5% | install.yml → unnamed play → config_dynamic → config_dynamic → automationedacontroller → automationedacontroller | Set EDA hostname | Set EDA hostname | inventory_hostname in groups.get('automationedacontroller', []) |
| 117 | 4.6% | install.yml → unnamed play → config_dynamic → config_dynamic → automationedacontroller → automationedacontroller | Check if provided event-stream public URL begins with a http protocol | Check if provided event-stream public URL begins with a http protocol | inventory_hostname in groups.get('automationedacontroller', []); _automationedacontroller_public_event_stream_url is not regex("^https?://.*") |
| 118 | 4.6% | install.yml → unnamed play → config_dynamic → config_dynamic | include_role | Include and run tasks from an Ansible role (role `automationedacontroller`, tasks `check_node_type`) | — |
| 119 | 4.6% | install.yml → unnamed play → config_dynamic → config_dynamic → automationedacontroller | role `automationedacontroller` (`check_node_type`) | Enter role task list | — |
| 120 | 4.7% | install.yml → unnamed play → config_dynamic → config_dynamic → automationedacontroller → automationedacontroller | file `check_node_type.yml` | Task file `ansible/automation_platform_installer/roles/automationedacontroller/tasks/check_node_type.yml` | — |
| 121 | 4.7% | install.yml → unnamed play → config_dynamic → config_dynamic → automationedacontroller → automationedacontroller | block | Begin conditional or grouped task block | inventory_hostname in groups.get('automationedacontroller', []) |
| 122 | 4.8% | install.yml → unnamed play → config_dynamic → config_dynamic → automationedacontroller → automationedacontroller → block | Set eda_node_type if not defined | Set eda_node_type if not defined | inventory_hostname in groups.get('automationedacontroller', []); not eda_node_type \| default('') \| length |
| 123 | 4.8% | install.yml → unnamed play → config_dynamic → config_dynamic → automationedacontroller → automationedacontroller → block | Check that an eda_node_type exists for all automationedacontroller nodes | Check that an eda_node_type exists for all automationedacontroller nodes | inventory_hostname in groups.get('automationedacontroller', []); eda_node_type not in ['hybrid', 'api', 'worker', 'event-stream'] |
| 124 | 4.8% | install.yml → unnamed play → config_dynamic → config_dynamic → automationedacontroller → automationedacontroller | set_fact | Set a host fact used by later tasks | — |
| 125 | 4.9% | install.yml → unnamed play → config_dynamic → config_dynamic | Ensure that a worker and an api node exist if no hybrid nodes are present for automationedacontroller | Ensure that a worker and an api node exist if no hybrid nodes are present for automationedacontroller | groups['automationedacontroller'] \| default([]) \| length > 0; _automationedacontroller_hybrid_hosts \| length == 0; _automationedacontroller_web_hosts \| length < 1 or _automationedacontroller_worker_hosts \| length < 1 |
| 126 | 4.9% | install.yml → unnamed play → config_dynamic → config_dynamic | Ensure that at least 1 event_stream node exists in inventory with Event-Driven Ansible controller | Ensure that at least 1 event_stream node exists in inventory with Event-Driven Ansible controller | groups['automationedacontroller'] \| default([]) \| length > 0; _automationedacontroller_event_stream_hosts \| length < 1 |
| 127 | 4.9% | install.yml → unnamed play → config_dynamic → config_dynamic | block | Begin conditional or grouped task block | inventory_hostname in groups.get('automationgateway', []) |
| 128 | 5.0% | install.yml → unnamed play → config_dynamic → config_dynamic → block | include_role | Include and run tasks from an Ansible role (role `automationcontroller`, tasks `service_port`) | inventory_hostname in groups.get('automationgateway', []); groups.get('automationcontroller', []) \| length > 0 |
| 129 | 5.0% | install.yml → unnamed play → config_dynamic → config_dynamic → block → automationcontroller | role `automationcontroller` (`service_port`) | Enter role task list | inventory_hostname in groups.get('automationgateway', []); groups.get('automationcontroller', []) \| length > 0 |
| 130 | 5.1% | install.yml → unnamed play → config_dynamic → config_dynamic → block → automationcontroller → automationcontroller | file `service_port.yml` | Task file `ansible/automation_platform_installer/roles/automationcontroller/tasks/service_port.yml` | inventory_hostname in groups.get('automationgateway', []); groups.get('automationcontroller', []) \| length > 0 |
| 131 | 5.1% | install.yml → unnamed play → config_dynamic → config_dynamic → block → automationcontroller → automationcontroller | Set Automation Controller service port for Platform Gateway | Set Automation Controller service port for Platform Gateway | inventory_hostname in groups.get('automationgateway', []); groups.get('automationcontroller', []) \| length > 0 |
| 132 | 5.1% | install.yml → unnamed play → config_dynamic → config_dynamic → block | include_role | Include and run tasks from an Ansible role (role `automationhub`, tasks `service_port`) | inventory_hostname in groups.get('automationgateway', []); groups.get('automationhub', []) \| length > 0 |
| 133 | 5.2% | install.yml → unnamed play → config_dynamic → config_dynamic → block → automationhub | role `automationhub` (`service_port`) | Enter role task list | inventory_hostname in groups.get('automationgateway', []); groups.get('automationhub', []) \| length > 0 |
| 134 | 5.2% | install.yml → unnamed play → config_dynamic → config_dynamic → block → automationhub → automationhub | file `service_port.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/service_port.yml` | inventory_hostname in groups.get('automationgateway', []); groups.get('automationhub', []) \| length > 0 |
| 135 | 5.3% | install.yml → unnamed play → config_dynamic → config_dynamic → block → automationhub → automationhub | Set Automation Hub service port for Platform Gateway | Set Automation Hub service port for Platform Gateway | inventory_hostname in groups.get('automationgateway', []); groups.get('automationhub', []) \| length > 0 |
| 136 | 5.3% | install.yml → unnamed play → config_dynamic → config_dynamic → block | include_role | Include and run tasks from an Ansible role (role `automationedacontroller`, tasks `service_port`) | inventory_hostname in groups.get('automationgateway', []); groups.get('automationedacontroller', []) \| length > 0 |
| 137 | 5.3% | install.yml → unnamed play → config_dynamic → config_dynamic → block → automationedacontroller | role `automationedacontroller` (`service_port`) | Enter role task list | inventory_hostname in groups.get('automationgateway', []); groups.get('automationedacontroller', []) \| length > 0 |
| 138 | 5.4% | install.yml → unnamed play → config_dynamic → config_dynamic → block → automationedacontroller → automationedacontroller | file `service_port.yml` | Task file `ansible/automation_platform_installer/roles/automationedacontroller/tasks/service_port.yml` | inventory_hostname in groups.get('automationgateway', []); groups.get('automationedacontroller', []) \| length > 0 |
| 139 | 5.4% | install.yml → unnamed play → config_dynamic → config_dynamic → block → automationedacontroller → automationedacontroller | Set Automation EDA Controller service port for Platform Gateway | Set Automation EDA Controller service port for Platform Gateway | inventory_hostname in groups.get('automationgateway', []); groups.get('automationedacontroller', []) \| length > 0 |
| 140 | 5.5% | install.yml → unnamed play → config_dynamic → config_dynamic → block → automationedacontroller → automationedacontroller | Set Automation EDA Controller event-stream service port for Platform Gateway | Set Automation EDA Controller event-stream service port for Platform Gateway | inventory_hostname in groups.get('automationgateway', []); groups.get('automationedacontroller', []) \| length > 0 |
| 141 | 5.5% | install.yml | Group nodes by OS distribution | Play targeting `aap_valid_hosts` | — |
| 142 | 5.5% | install.yml → Group nodes by OS distribution | group hosts by distribution | group hosts by distribution | — |
| 143 | 5.6% | install.yml | Group supported distributions | Play targeting `RedHat_8:RedHat_9` | — |
| 144 | 5.6% | install.yml → Group supported distributions | group hosts for supported distributions | group hosts for supported distributions | (ansible_distribution_major_version == "8" and ansible_distribution_version is version_compare('8.10', '>=')) or (ansible_distribution_major_version == "9" and ansible_distribution_version is version_compare('9.6', '>=')) |
| 145 | 5.7% | install.yml | Group deprecated distributions | Play targeting `none` | — |
| 146 | 5.7% | install.yml → Group deprecated distributions | group hosts for supported distributions | group hosts for supported distributions | — |
| 147 | 5.7% | install.yml | Ensure all node OS distributions are supported | Play targeting `aap_valid_hosts:!supported` | — |
| 148 | 5.8% | install.yml → Ensure all node OS distributions are supported | fail for unsupported distribution | fail for unsupported distribution | — |
| 149 | 5.8% | install.yml | Check for deprecated platforms | Play targeting `aap_valid_hosts:&deprecated` | — |
| 150 | 5.9% | install.yml → Check for deprecated platforms | warn for deprecated distribution | warn for deprecated distribution | — |
| 151 | 5.9% | install.yml → Check for deprecated platforms | pause for warning | pause for warning | — |
| 152 | 5.9% | install.yml | Define role discovered variables, usable throughout the playbook | Play targeting `automationgateway:automationcontroller:automationedacontroller:automationhub:database` | — |
| 153 | 6.0% | install.yml → Define role discovered variables, usable throughout the playbook | role `postgres` | Enter role task list | — |
| 154 | 6.0% | install.yml → Define role discovered variables, usable throughout the playbook → postgres | file `main.yml` | Task file `ansible/automation_platform_installer/roles/postgres/tasks/main.yml` | — |
| 155 | 6.0% | install.yml → Define role discovered variables, usable throughout the playbook → postgres | include_tasks | Include a task file from the current role or playbook (file `vars.yml`) | — |
| 156 | 6.1% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → vars.yml | file `vars.yml` | Task file `ansible/automation_platform_installer/roles/postgres/tasks/vars.yml` | — |
| 157 | 6.1% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → vars.yml | include_vars | Load variables from a file | — |
| 158 | 6.2% | install.yml → Define role discovered variables, usable throughout the playbook → postgres | Ensure all locales in system are in UTF-8 | Ensure all locales in system are in UTF-8 (role `preflight`, tasks `locale.yaml`) | — |
| 159 | 6.2% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → preflight | role `preflight` (`locale.yaml`) | Enter role task list | — |
| 160 | 6.2% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → preflight → preflight | file `locale.yaml` | Task file `ansible/automation_platform_installer/roles/preflight/tasks/locale.yaml` | — |
| 161 | 6.3% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → preflight → preflight | Load /etc/locale.conf | Load /etc/locale.conf | — |
| 162 | 6.3% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → preflight → preflight | fail | Fail the play with an error message | item is not regex(".*(utf8\|UTF-8)") |
| 163 | 6.4% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → preflight → preflight | Get the LANG environment variable | Get the LANG environment variable | item is regex("^LANG=.*") |
| 164 | 6.4% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → preflight → preflight | Check if locale is properly installed | Check if locale is properly installed | — |
| 165 | 6.4% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → preflight → preflight | fail | Fail the play with an error message | _locale.rc != 0 |
| 166 | 6.5% | install.yml → Define role discovered variables, usable throughout the playbook → postgres | include_tasks | Include a task file from the current role or playbook (file `preflight.yml`) | not postgres_exec_vars_only \| bool |
| 167 | 6.5% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → preflight.yml | file `preflight.yml` | Task file `ansible/automation_platform_installer/roles/postgres/tasks/preflight.yml` | not postgres_exec_vars_only \| bool |
| 168 | 6.6% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → preflight.yml | Preflight check - Ensure that the Postgres data dir is not a mount point | Preflight check - Ensure that the Postgres data dir is not a mount point | not postgres_exec_vars_only \| bool |
| 169 | 6.6% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → preflight.yml | Fail if the data dir is a mount point | Fail if the data dir is a mount point | not postgres_exec_vars_only \| bool; is_pg_data_mount.rc  \| int == 0 |
| 170 | 6.6% | install.yml → Define role discovered variables, usable throughout the playbook → postgres | include_tasks | Include a task file from the current role or playbook (file `init.yml`) | not postgres_exec_vars_only \| bool |
| 171 | 6.7% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → init.yml | file `init.yml` | Task file `ansible/automation_platform_installer/roles/postgres/tasks/init.yml` | not postgres_exec_vars_only \| bool |
| 172 | 6.7% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → init.yml | init postgresql | init postgresql | not postgres_exec_vars_only \| bool |
| 173 | 6.8% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → init.yml | Open up postgres port permissions | Open up postgres port permissions | not postgres_exec_vars_only \| bool; ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 174 | 6.8% | install.yml → Define role discovered variables, usable throughout the playbook → postgres | include_tasks | Include a task file from the current role or playbook (file `conf.yml`) | not postgres_exec_vars_only \| bool |
| 175 | 6.8% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml | file `conf.yml` | Task file `ansible/automation_platform_installer/roles/postgres/tasks/conf.yml` | not postgres_exec_vars_only \| bool |
| 176 | 6.9% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml | Update postgresql authentication settings | Update postgresql authentication settings (dest `{{ pg_hba_location }}`) | not postgres_exec_vars_only \| bool |
| 177 | 6.9% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml | Apply postgresql general settings | Apply postgresql general settings (dest `{{ pg_conf_location }}`) | not postgres_exec_vars_only \| bool |
| 178 | 7.0% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml | Copy server SSL certificate | Copy server SSL certificate (dest `{{ pg_conf_dir }}/server.crt`) | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is defined; postgres_ssl_key is defined |
| 179 | 7.0% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml | Copy server SSL private key | Copy server SSL private key (dest `{{ pg_conf_dir }}/server.key`) | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is defined; postgres_ssl_key is defined |
| 180 | 7.0% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml | block | Begin conditional or grouped task block | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined |
| 181 | 7.1% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block | Use internal CA for internal database | Use internal CA for internal database (role `certificate_authority`, tasks `sign_service.yml`) | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 182 | 7.1% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority | role `certificate_authority` (`sign_service.yml`) | Enter role task list | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 183 | 7.1% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority | file `sign_service.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/sign_service.yml` | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 184 | 7.2% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority | include_tasks | Include a task file from the current role or playbook (file `init.yml`) | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 185 | 7.2% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority → init.yml | file `init.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/init.yml` | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 186 | 7.3% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority → init.yml | Ensure python cryptography libraries are installed | Ensure python cryptography libraries are installed | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 187 | 7.3% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority | Create {{ aap_ca_servicename }} private key | Create {{ aap_ca_servicename }} private key | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 188 | 7.3% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority | set_fact | Set a host fact used by later tasks | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 189 | 7.4% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority | Check if {{ aap_ca_servicename }} public key exists | Check if {{ aap_ca_servicename }} public key exists | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 190 | 7.4% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; certificate_stat.stat.exists \| bool |
| 191 | 7.5% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Check contents of {{ aap_ca_servicename }} public key | Check contents of {{ aap_ca_servicename }} public key | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; certificate_stat.stat.exists \| bool |
| 192 | 7.5% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; certificate_stat.stat.exists \| bool; aap_service_san_records_internal \| union(aap_service_san_records) \| difference(certificate_check.subject_alt_name) \| length > 0 |
| 193 | 7.5% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 194 | 7.6% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false); aap_service_is_client |
| 195 | 7.6% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Create CSR for {{ aap_ca_servicename }} node/s | Create CSR for {{ aap_ca_servicename }} node/s | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 196 | 7.7% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Sign {{ aap_ca_servicename }} public key | Sign {{ aap_ca_servicename }} public key | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 197 | 7.7% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Write public key to {{ aap_ca_servicename }} node/s | Write public key to {{ aap_ca_servicename }} node/s (dest `{{ aap_service_cert_fullpath }}`) | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 198 | 7.7% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority | Signal restart if certificate pair changes | Signal restart if certificate pair changes | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; (write_key is changed or write_certificate is changed) |
| 199 | 7.8% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block | Use internal CA for external database | Use internal CA for external database (role `certificate_authority`, tasks `sign_service.yml`) | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 200 | 7.8% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority | role `certificate_authority` (`sign_service.yml`) | Enter role task list | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 201 | 7.9% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority | file `sign_service.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/sign_service.yml` | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 202 | 7.9% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority | include_tasks | Include a task file from the current role or playbook (file `init.yml`) | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 203 | 7.9% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority → init.yml | file `init.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/init.yml` | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 204 | 8.0% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority → init.yml | Ensure python cryptography libraries are installed | Ensure python cryptography libraries are installed | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 205 | 8.0% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority | Create {{ aap_ca_servicename }} private key | Create {{ aap_ca_servicename }} private key | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 206 | 8.1% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority | set_fact | Set a host fact used by later tasks | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 207 | 8.1% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority | Check if {{ aap_ca_servicename }} public key exists | Check if {{ aap_ca_servicename }} public key exists | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 208 | 8.1% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; certificate_stat.stat.exists \| bool |
| 209 | 8.2% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Check contents of {{ aap_ca_servicename }} public key | Check contents of {{ aap_ca_servicename }} public key | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; certificate_stat.stat.exists \| bool |
| 210 | 8.2% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; certificate_stat.stat.exists \| bool; aap_service_san_records_internal \| union(aap_service_san_records) \| difference(certificate_check.subject_alt_name) \| length > 0 |
| 211 | 8.2% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 212 | 8.3% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false); aap_service_is_client |
| 213 | 8.3% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Create CSR for {{ aap_ca_servicename }} node/s | Create CSR for {{ aap_ca_servicename }} node/s | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 214 | 8.4% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Sign {{ aap_ca_servicename }} public key | Sign {{ aap_ca_servicename }} public key | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 215 | 8.4% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Write public key to {{ aap_ca_servicename }} node/s | Write public key to {{ aap_ca_servicename }} node/s (dest `{{ aap_service_cert_fullpath }}`) | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 216 | 8.4% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block → certificate_authority → certificate_authority | Signal restart if certificate pair changes | Signal restart if certificate pair changes | not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; (write_key is changed or write_certificate is changed) |
| 217 | 8.5% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml | Create unix socket directory | Create unix socket directory | not postgres_exec_vars_only \| bool |
| 218 | 8.5% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml | Configure postgresql tmpfiles.d | Configure postgresql tmpfiles.d (dest `/usr/lib/tmpfiles.d/postgresql.conf`) | not postgres_exec_vars_only \| bool |
| 219 | 8.6% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml | set_fact | Set a host fact used by later tasks | not postgres_exec_vars_only \| bool; aap_service_restart \| default(false) or pg_hba_conf is changed or pg_general_conf is changed |
| 220 | 8.6% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml | ensure postgresql is running and restart when authentication settings change | ensure postgresql is running and restart when authentication settings change (service `{{ postgres_init_name }}`) | not postgres_exec_vars_only \| bool |
| 221 | 8.6% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml | wait for postgresql restart | wait for postgresql restart | not postgres_exec_vars_only \| bool; postgresql_restart is changed |
| 222 | 8.7% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml | configure postgresql to start automatically | configure postgresql to start automatically (service `{{ postgres_init_name }}`) | not postgres_exec_vars_only \| bool |
| 223 | 8.7% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml | block | Begin conditional or grouped task block | not postgres_exec_vars_only \| bool; groups['automationcontroller'] \| default([]) \| length |
| 224 | 8.8% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block | create the awx postgres user | create the awx postgres user | not postgres_exec_vars_only \| bool; groups['automationcontroller'] \| default([]) \| length; postgres_username is defined and postgres_username != ''; postgres_password is defined and postgres_password != '' |
| 225 | 8.8% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block | create the postgresql database for awx | create the postgresql database for awx | not postgres_exec_vars_only \| bool; groups['automationcontroller'] \| default([]) \| length; postgres_username is defined and postgres_username != ''; postgres_database is defined and postgres_database != '' |
| 226 | 8.8% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml | block | Begin conditional or grouped task block | not postgres_exec_vars_only \| bool; groups['automationhub'] \| default([]) \| length |
| 227 | 8.9% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block | create the automationhub postgres user | create the automationhub postgres user | not postgres_exec_vars_only \| bool; groups['automationhub'] \| default([]) \| length; automationhub_postgres_username is defined and automationhub_postgres_username != ''; automationhub_postgres_password is defined and automationhub_postgres_password != '' |
| 228 | 8.9% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block | create the postgresql database for automationhub | create the postgresql database for automationhub | not postgres_exec_vars_only \| bool; groups['automationhub'] \| default([]) \| length; automationhub_postgres_username is defined and automationhub_postgres_username != ''; automationhub_postgres_database is defined and automationhub_postgres_database != '' |
| 229 | 9.0% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block | enable the postgresql hstore extension | enable the postgresql hstore extension | not postgres_exec_vars_only \| bool; groups['automationhub'] \| default([]) \| length |
| 230 | 9.0% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml | block | Begin conditional or grouped task block | not postgres_exec_vars_only \| bool; groups['automationedacontroller'] \| default([]) \| length |
| 231 | 9.0% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block | create the automationedacontroller postgres user | create the automationedacontroller postgres user | not postgres_exec_vars_only \| bool; groups['automationedacontroller'] \| default([]) \| length; automationedacontroller_postgres_username is defined and automationedacontroller_postgres_username != ''; automationedacontroller_postgres_password is defined and automationedacontroller_postgres_password != '' |
| 232 | 9.1% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block | create the postgresql database for automationedacontroller | create the postgresql database for automationedacontroller | not postgres_exec_vars_only \| bool; groups['automationedacontroller'] \| default([]) \| length; automationedacontroller_postgres_username is defined and automationedacontroller_postgres_username != ''; automationedacontroller_postgres_database is defined and automationedacontroller_postgres_database != '' |
| 233 | 9.1% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml | block | Begin conditional or grouped task block | not postgres_exec_vars_only \| bool; groups['automationgateway'] \| default([]) \| length |
| 234 | 9.2% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block | create the automationgateway postgres user | create the automationgateway postgres user | not postgres_exec_vars_only \| bool; groups['automationgateway'] \| default([]) \| length; automationgateway_postgres_username is defined and automationgateway_postgres_username != ''; automationgateway_postgres_password is defined and automationgateway_postgres_password != '' |
| 235 | 9.2% | install.yml → Define role discovered variables, usable throughout the playbook → postgres → conf.yml → block | create the postgresql database for automationgateway | create the postgresql database for automationgateway | not postgres_exec_vars_only \| bool; groups['automationgateway'] \| default([]) \| length; automationgateway_postgres_username is defined and automationgateway_postgres_username != ''; automationgateway_postgres_database is defined and automationgateway_postgres_database != '' |
| 236 | 9.2% | install.yml | Sanity check | Play targeting `automationgateway:automationcontroller:automationedacontroller:automationhub` | — |
| 237 | 9.3% | install.yml → Sanity check | include_role | Include and run tasks from an Ansible role (role `preflight`) | — |
| 238 | 9.3% | install.yml → Sanity check → preflight | role `preflight` | Enter role task list | — |
| 239 | 9.3% | install.yml → Sanity check → preflight → preflight | file `main.yml` | Task file `ansible/automation_platform_installer/roles/preflight/tasks/main.yml` | — |
| 240 | 9.4% | install.yml → Sanity check → preflight → preflight | Check if service nodes are collocated on the same host | Check if service nodes are collocated on the same host | aap_component_nodes \| map('extract', hostvars, 'routable_hostname') \| list \| unique \| length \| int != aap_component_node_count \| int |
| 241 | 9.4% | install.yml → Sanity check → preflight → preflight | Preflight check - Check if AAP VERSION file exists | Preflight check - Check if AAP VERSION file exists | — |
| 242 | 9.5% | install.yml → Sanity check → preflight → preflight | block | Begin conditional or grouped task block | _aap_version_file.stat.exists \| bool |
| 243 | 9.5% | install.yml → Sanity check → preflight → preflight → block | Preflight check - Obtain AAP version | Preflight check - Obtain AAP version | _aap_version_file.stat.exists \| bool |
| 244 | 9.5% | install.yml → Sanity check → preflight → preflight → block | Preflight check - User must be on AAP 2.4 to upgrade | Preflight check - User must be on AAP 2.4 to upgrade | _aap_version_file.stat.exists \| bool; _aap_version['content'] \| b64decode \| trim is version('2.4', '<') |
| 245 | 9.6% | install.yml → Sanity check → preflight → preflight → block | Preflight check - Ensure AAP 2.4 EDA is not included | Preflight check - Ensure AAP 2.4 EDA is not included | _aap_version_file.stat.exists \| bool; inventory_hostname in groups.get('automationedacontroller', []); _aap_version['content'] \| b64decode \| trim is version('2.4', '==') |
| 246 | 9.6% | install.yml → Sanity check → preflight → preflight | Preflight check - Fail if this machine lacks sufficient RAM. | Preflight check - Fail if this machine lacks sufficient RAM. | ansible_memtotal_mb < required_ram\|int; not ignore_preflight_errors \| bool |
| 247 | 9.7% | install.yml → Sanity check → preflight → preflight | Preflight check - Ensure all locales in system are in UTF-8 | Preflight check - Ensure all locales in system are in UTF-8 (file `locale.yaml`) | — |
| 248 | 9.7% | install.yml → Sanity check → preflight → preflight → locale.yaml | file `locale.yaml` | Task file `ansible/automation_platform_installer/roles/preflight/tasks/locale.yaml` | — |
| 249 | 9.7% | install.yml → Sanity check → preflight → preflight → locale.yaml | Load /etc/locale.conf | Load /etc/locale.conf | — |
| 250 | 9.8% | install.yml → Sanity check → preflight → preflight → locale.yaml | fail | Fail the play with an error message | item is not regex(".*(utf8\|UTF-8)") |
| 251 | 9.8% | install.yml → Sanity check → preflight → preflight → locale.yaml | Get the LANG environment variable | Get the LANG environment variable | item is regex("^LANG=.*") |
| 252 | 9.9% | install.yml → Sanity check → preflight → preflight → locale.yaml | Check if locale is properly installed | Check if locale is properly installed | — |
| 253 | 9.9% | install.yml → Sanity check → preflight → preflight → locale.yaml | fail | Fail the play with an error message | _locale.rc != 0 |
| 254 | 9.9% | install.yml → Sanity check → preflight → preflight | Preflight check - Get /var/log permissions | Preflight check - Get /var/log permissions | — |
| 255 | 10.0% | install.yml → Sanity check → preflight → preflight | Preflight check - clock management services | Preflight check - clock management services | groups.get('aap_valid_hosts') \| length > 1; not ansible_facts.services.get('chronyd.service', {}) or ansible_facts.services['chronyd.service'].state != 'running'; not ansible_facts.services.get('ntpd.service', {}) or ansible_facts.services['ntpd.service'].state != 'running'; not ansible_facts.services.get('systemd-timesyncd.service', {}) or ansible_facts.services['systemd-timesyncd.service'].state != 'running' |
| 256 | 10.0% | install.yml → Sanity check → preflight → preflight | Preflight check - Does /var/log have appropriate permissions? | Preflight check - Does /var/log have appropriate permissions? | not vl_st.stat.roth |
| 257 | 10.1% | install.yml → Sanity check → preflight → preflight | include_role | Include and run tasks from an Ansible role (role `redis`, tasks `check_cluster`) | — |
| 258 | 10.1% | install.yml → Sanity check → preflight → preflight → redis | role `redis` (`check_cluster`) | Enter role task list | — |
| 259 | 10.1% | install.yml → Sanity check → preflight → preflight → redis → redis | file `check_cluster.yml` | Task file `ansible/automation_platform_installer/roles/redis/tasks/check_cluster.yml` | — |
| 260 | 10.2% | install.yml → Sanity check → preflight → preflight → redis → redis | Check for cluster init file on all redis nodes | Check for cluster init file on all redis nodes | — |
| 261 | 10.2% | install.yml → Sanity check → preflight → preflight → redis → redis | Determine if redis was previously installed in cluster mode | Determine if redis was previously installed in cluster mode | redis_cluster_init_file.stat.exists \| bool |
| 262 | 10.3% | install.yml → Sanity check → preflight → preflight → redis → redis | Set redis cluster determination | Set redis cluster determination | — |
| 263 | 10.3% | install.yml → Sanity check → preflight → preflight | Check for standalone and if redis was previously clustered | Check for standalone and if redis was previously clustered | _redis_mode == 'standalone'; _redis_is_clustered |
| 264 | 10.3% | install.yml → Sanity check → preflight → preflight | Preflight check - Fail if installer is configured for postgres certificate auth and if the postgres database is managed by the installer | Preflight check - Fail if installer is configured for postgres certificate auth and if the postgres database is managed by the installer | groups['database'] \| default([]) \| length; awx_pg_cert_auth or automationhub_pg_cert_auth or automationedacontroller_pg_cert_auth or automationgateway_pg_cert_auth |
| 265 | 10.4% | install.yml → Sanity check → preflight → preflight | Preflight - Fail if Automation Gateway hostname is not FQDN or IP address | Preflight - Fail if Automation Gateway hostname is not FQDN or IP address | not _automationgateway_main_url \| urlsplit('hostname') is search("\.") |
| 266 | 10.4% | install.yml → Sanity check → preflight → preflight | Preflight check - Read in controller version | Preflight check - Read in controller version | inventory_hostname not in groups['automationhub'] \| default([]); inventory_hostname not in groups['automationedacontroller'] \| default([]) |
| 267 | 10.4% | install.yml → Sanity check → preflight → preflight | Preflight check - Perform fresh install checks | Begin conditional or grouped task block | result.content is not defined |
| 268 | 10.5% | install.yml → Sanity check → preflight → preflight → Preflight check - Perform fresh install checks | Preflight check - Admin password must be defined for a fresh install | Preflight check - Admin password must be defined for a fresh install | result.content is not defined; admin_password\|default('') == ''; groups['automationcontroller'] \| default([]) \| length |
| 269 | 10.5% | install.yml → Sanity check → preflight → preflight → Preflight check - Perform fresh install checks | Preflight check - Postgres password must be defined for a fresh install | Preflight check - Postgres password must be defined for a fresh install | result.content is not defined; pg_password\|default('') == ''; not awx_pg_cert_auth; not automationcontroller_db_template \| default([]) \| length; groups['automationcontroller'] \| default([]) \| length |
| 270 | 10.6% | install.yml → Sanity check → preflight → preflight → Preflight check - Perform fresh install checks | Preflight check - Automation hub admin password must be defined for a fresh install (if hub is enabled) | Preflight check - Automation hub admin password must be defined for a fresh install (if hub is enabled) | result.content is not defined; automationhub_admin_password\|default('') == ''; groups['automationhub'] \| default([]) \| length |
| 271 | 10.6% | install.yml → Sanity check → preflight → preflight → Preflight check - Perform fresh install checks | Preflight check - Automation hub postgres password must be defined for a fresh install (if hub is enabled and not using client certificate authentication) | Preflight check - Automation hub postgres password must be defined for a fresh install (if hub is enabled and not using client certificate authentication) | result.content is not defined; automationhub_pg_password\|default('') == ''; not automationhub_pg_cert_auth; not automationhub_db_template \| default([]) \| length; groups['automationhub'] \| default([]) \| length |
| 272 | 10.6% | install.yml → Sanity check → preflight → preflight → Preflight check - Perform fresh install checks | Preflight check - Automation EDA controller database password must be defined for a fresh install (if eda is enabled) | Preflight check - Automation EDA controller database password must be defined for a fresh install (if eda is enabled) | result.content is not defined; not automationedacontroller_pg_password \| default('') \| length; not automationedacontroller_pg_cert_auth; not automationedacontroller_db_template \| default([]) \| length; groups['automationedacontroller'] \| default([]) \| length |
| 273 | 10.7% | install.yml → Sanity check → preflight → preflight → Preflight check - Perform fresh install checks | Find mount options for /var, /var/tmp, and /tmp | Find mount options for /var, /var/tmp, and /tmp | result.content is not defined |
| 274 | 10.7% | install.yml → Sanity check → preflight → preflight → Preflight check - Perform fresh install checks | Fail if the filesystem is mounted with noexec | Fail if the filesystem is mounted with noexec | result.content is not defined; ((item is changed) or (item is failed)); not ignore_preflight_errors \| bool |
| 275 | 10.8% | install.yml → Sanity check → preflight → preflight → Preflight check - Perform fresh install checks | Ensure hostname on nodes with receptor installed is not localhost | Ensure hostname on nodes with receptor installed is not localhost | result.content is not defined; ansible_hostname is match('localhost*'); inventory_hostname in groups['automationcontroller'] \| default([]) or inventory_hostname in groups['execution_nodes'] \| default([]) |
| 276 | 10.8% | install.yml → Sanity check → preflight → preflight | Preflight check - Perform upgrade checks | Begin conditional or grouped task block | result.content is defined |
| 277 | 10.8% | install.yml → Sanity check → preflight → preflight → Preflight check - Perform upgrade checks | Preflight check - User must be on Automation controller 4.5 to upgrade | Preflight check - User must be on Automation controller 4.5 to upgrade | result.content is defined; result.content \| b64decode is version_compare('4.5', '<') |
| 278 | 10.9% | install.yml → Sanity check → preflight → preflight → Preflight check - Perform upgrade checks | Preflight check - check for custom virtual environments | Preflight check - check for custom virtual environments | result.content is defined |
| 279 | 10.9% | install.yml → Sanity check → preflight → preflight → Preflight check - Perform upgrade checks | Preflight check - check for custom virtual environment path setting | Preflight check - check for custom virtual environment path setting | result.content is defined |
| 280 | 11.0% | install.yml → Sanity check → preflight → preflight → Preflight check - Perform upgrade checks | Preflight check - warn if custom virtual environments present | Preflight check - warn if custom virtual environments present | result.content is defined; custom_venvs.matched > 0 or custom_venv_paths.stdout != '[]' |
| 281 | 11.0% | install.yml → Sanity check → preflight → preflight → Preflight check - Perform upgrade checks | Preflight check - pause if warning | Preflight check - pause if warning | result.content is defined; custom_venvs.matched > 0 or custom_venv_paths.stdout != '[]' |
| 282 | 11.0% | install.yml → Sanity check → preflight → preflight → Preflight check - Perform upgrade checks | Preflight check - Detect partial installation or upgrade | Preflight check - Detect partial installation or upgrade | result.content is defined |
| 283 | 11.1% | install.yml → Sanity check → preflight → preflight → Preflight check - Perform upgrade checks | Preflight check - check license type | Preflight check - check license type | result.content is defined; migrations_up_to_date.rc == 0 |
| 284 | 11.1% | install.yml → Sanity check → preflight → preflight → Preflight check - Perform upgrade checks | Preflight check - check license type (detailed) | Preflight check - check license type (detailed) | result.content is defined; migrations_up_to_date.rc == 0 |
| 285 | 11.2% | install.yml → Sanity check → preflight → preflight → Preflight check - Perform upgrade checks | Preflight check - check license validity for upgrades | Preflight check - check license validity for upgrades | result.content is defined; 'stdout' in license_type and license_type.stdout == 'open' |
| 286 | 11.2% | install.yml → Sanity check → preflight → preflight | block | Begin conditional or grouped task block | groups['automationcontroller'] \| default([]) \| length; not (groups['database'] \| default([]) \| length); config_dynamic_database == "external"; inventory_hostname == groups['automationcontroller'][0] |
| 287 | 11.2% | install.yml → Sanity check → preflight → preflight → block | Preflight check - Ensure that psycopg2 python library is installed | Preflight check - Ensure that psycopg2 python library is installed | groups['automationcontroller'] \| default([]) \| length; not (groups['database'] \| default([]) \| length); config_dynamic_database == "external"; inventory_hostname == groups['automationcontroller'][0] |
| 288 | 11.3% | install.yml → Sanity check → preflight → preflight → block | set_fact | Set a host fact used by later tasks | groups['automationcontroller'] \| default([]) \| length; not (groups['database'] \| default([]) \| length); config_dynamic_database == "external"; inventory_hostname == groups['automationcontroller'][0]; custom_ca_cert \| default('') \| length; awx_pg_cert_auth |
| 289 | 11.3% | install.yml → Sanity check → preflight → preflight → block | Preflight check - Copy cacert when pg sslmode supports certificate auth | Preflight check - Copy cacert when pg sslmode supports certificate auth (dest `/root/cacert.pem`) | groups['automationcontroller'] \| default([]) \| length; not (groups['database'] \| default([]) \| length); config_dynamic_database == "external"; inventory_hostname == groups['automationcontroller'][0]; verify_custom_ca_cert \| default(false) |
| 290 | 11.4% | install.yml → Sanity check → preflight → preflight → block | Preflight check - Copy pg sslcert when pg sslmode supports certificate auth | Preflight check - Copy pg sslcert when pg sslmode supports certificate auth (dest `/root/pg_client.crt`) | groups['automationcontroller'] \| default([]) \| length; not (groups['database'] \| default([]) \| length); config_dynamic_database == "external"; inventory_hostname == groups['automationcontroller'][0]; awx_pg_cert_auth |
| 291 | 11.4% | install.yml → Sanity check → preflight → preflight → block | Preflight check - Copy pg sslkey when pg sslmode supports certificate auth | Preflight check - Copy pg sslkey when pg sslmode supports certificate auth (dest `/root/pg_client.key`) | groups['automationcontroller'] \| default([]) \| length; not (groups['database'] \| default([]) \| length); config_dynamic_database == "external"; inventory_hostname == groups['automationcontroller'][0]; awx_pg_cert_auth |
| 292 | 11.4% | install.yml → Sanity check → preflight → preflight → block | Preflight check - Ensure User-Provided Postgres Instance is at Version 12 or Higher | Preflight check - Ensure User-Provided Postgres Instance is at Version 12 or Higher | groups['automationcontroller'] \| default([]) \| length; not (groups['database'] \| default([]) \| length); config_dynamic_database == "external"; inventory_hostname == groups['automationcontroller'][0] |
| 293 | 11.5% | install.yml → Sanity check → preflight → preflight → block | set_fact | Set a host fact used by later tasks | groups['automationcontroller'] \| default([]) \| length; not (groups['database'] \| default([]) \| length); config_dynamic_database == "external"; inventory_hostname == groups['automationcontroller'][0] |
| 294 | 11.5% | install.yml → Sanity check → preflight → preflight → block | assert | Validate a condition and fail if it is not met | groups['automationcontroller'] \| default([]) \| length; not (groups['database'] \| default([]) \| length); config_dynamic_database == "external"; inventory_hostname == groups['automationcontroller'][0] |
| 295 | 11.5% | install.yml → Sanity check → preflight → preflight → block | Preflight check - Ensure User-Provided Postgres Instance is UTF-8 | Preflight check - Ensure User-Provided Postgres Instance is UTF-8 | groups['automationcontroller'] \| default([]) \| length; not (groups['database'] \| default([]) \| length); config_dynamic_database == "external"; inventory_hostname == groups['automationcontroller'][0] |
| 296 | 11.6% | install.yml → Sanity check → preflight → preflight → block | Preflight check - remove temporary cacert when its provided | Preflight check - remove temporary cacert when its provided | groups['automationcontroller'] \| default([]) \| length; not (groups['database'] \| default([]) \| length); config_dynamic_database == "external"; inventory_hostname == groups['automationcontroller'][0]; verify_custom_ca_cert \| default(false) |
| 297 | 11.6% | install.yml → Sanity check → preflight → preflight → block | Preflight check - remove temporary pg sslcert and sslkey for pg sslmode when it supports certificate auth | Preflight check - remove temporary pg sslcert and sslkey for pg sslmode when it supports certificate auth | groups['automationcontroller'] \| default([]) \| length; not (groups['database'] \| default([]) \| length); config_dynamic_database == "external"; inventory_hostname == groups['automationcontroller'][0]; awx_pg_cert_auth |
| 298 | 11.7% | install.yml → Sanity check → preflight → preflight → block | set_fact | Set a host fact used by later tasks | groups['automationcontroller'] \| default([]) \| length; not (groups['database'] \| default([]) \| length); config_dynamic_database == "external"; inventory_hostname == groups['automationcontroller'][0] |
| 299 | 11.7% | install.yml → Sanity check → preflight → preflight → block | assert | Validate a condition and fail if it is not met | groups['automationcontroller'] \| default([]) \| length; not (groups['database'] \| default([]) \| length); config_dynamic_database == "external"; inventory_hostname == groups['automationcontroller'][0] |
| 300 | 11.7% | install.yml → Sanity check → preflight → preflight | block | Begin conditional or grouped task block | groups['automationhub'] \| default([]) \| length > 1; inventory_hostname in groups['automationhub'] |
| 301 | 11.8% | install.yml → Sanity check → preflight → preflight → block | Preflight check - Check if /var/lib/pulp exists for Hub HA mode | Preflight check - Check if /var/lib/pulp exists for Hub HA mode | groups['automationhub'] \| default([]) \| length > 1; inventory_hostname in groups['automationhub'] |
| 302 | 11.8% | install.yml → Sanity check → preflight → preflight → block | Preflight check - /var/lib/pulp must exist for Hub HA mode | Preflight check - /var/lib/pulp must exist for Hub HA mode | groups['automationhub'] \| default([]) \| length > 1; inventory_hostname in groups['automationhub']; not var_lib_pulp_dir.stat.exists |
| 303 | 11.9% | install.yml → Sanity check → preflight → preflight | Preflight check - Generate a hub token if fresh install | Preflight check - Generate a hub token if fresh install | groups['automationcontroller'] \| default([]) \| length; groups['automationhub'] \| default([]) \| length; inventory_hostname == groups['automationhub'][0]; not automationhub_api_token \| default('') \| length; generate_automationhub_token is not defined; not _aap_version_file.stat.exists \| bool |
| 304 | 11.9% | install.yml → Sanity check → preflight → preflight | Obtain enabled repo list | Obtain enabled repo list | — |
| 305 | 11.9% | install.yml → Sanity check → preflight → preflight | Ensure EPEL is not enabled | Ensure EPEL is not enabled | item \| trim \| regex_search('^epel.*') |
| 306 | 12.0% | install.yml → Sanity check → preflight → preflight | Check if upgrade_from file exists | Check if upgrade_from file exists | — |
| 307 | 12.0% | install.yml → Sanity check → preflight → preflight | block | Begin conditional or grouped task block | _upgrade_from_file.stat.exists \| bool |
| 308 | 12.1% | install.yml → Sanity check → preflight → preflight → block | Obtain upgrade from version | Obtain upgrade from version | _upgrade_from_file.stat.exists \| bool |
| 309 | 12.1% | install.yml → Sanity check → preflight → preflight → block | Set host AAP version before upgrade | Set host AAP version before upgrade | _upgrade_from_file.stat.exists \| bool |
| 310 | 12.1% | install.yml → Sanity check → preflight → preflight | Set host AAP version before upgrade | Set host AAP version before upgrade | not _upgrade_from_file.stat.exists \| bool |
| 311 | 12.2% | install.yml → Sanity check → preflight → preflight | Set AAP version before upgrade | Set AAP version before upgrade | — |
| 312 | 12.2% | install.yml → Sanity check → preflight → preflight | Ensure AAP directory exists | Ensure AAP directory exists | — |
| 313 | 12.3% | install.yml → Sanity check → preflight → preflight | Record AAP version before upgrade | Record AAP version before upgrade | — |
| 314 | 12.3% | install.yml → Sanity check → preflight → preflight | Check if upgrade from 2.4 | Check if upgrade from 2.4 | — |
| 315 | 12.3% | install.yml | Receptor Sanity check | Play targeting `automationcontroller:execution_nodes` | — |
| 316 | 12.4% | install.yml → Receptor Sanity check | Generate Mesh dot graph topology | Begin conditional or grouped task block | — |
| 317 | 12.4% | install.yml → Receptor Sanity check → Generate Mesh dot graph topology | set_fact | Set a host fact used by later tasks | — |
| 318 | 12.5% | install.yml → Receptor Sanity check → Generate Mesh dot graph topology | Parse Mesh | Parse Mesh | — |
| 319 | 12.5% | install.yml → Receptor Sanity check → Generate Mesh dot graph topology | debug | Print debug information | — |
| 320 | 12.5% | install.yml → Receptor Sanity check | set_fact | Set a host fact used by later tasks | — |
| 321 | 12.6% | install.yml → Receptor Sanity check | include_role | Include and run tasks from an Ansible role (role `receptor`, tasks `preflight`) | — |
| 322 | 12.6% | install.yml → Receptor Sanity check → receptor | role `receptor` (`preflight`) | Enter role task list | — |
| 323 | 12.6% | install.yml → Receptor Sanity check → receptor → receptor | file `preflight.yml` | Task file `ansible/automation_platform_installer/roles/receptor/tasks/preflight.yml` | — |
| 324 | 12.7% | install.yml → Receptor Sanity check → receptor → receptor | Include calculate_mesh | Include calculate_mesh (file `calculate_mesh.yml`) | — |
| 325 | 12.7% | install.yml → Receptor Sanity check → receptor → receptor → calculate_mesh.yml | file `calculate_mesh.yml` | Task file `ansible/automation_platform_installer/roles/receptor/tasks/calculate_mesh.yml` | — |
| 326 | 12.8% | install.yml → Receptor Sanity check → receptor → receptor → calculate_mesh.yml | Calculate Mesh Topology | Calculate Mesh Topology | — |
| 327 | 12.8% | install.yml → Receptor Sanity check → receptor → receptor → calculate_mesh.yml | Review Mesh dot graph topology | Review Mesh dot graph topology | generate_dot_file \| default([]) \| length |
| 328 | 12.8% | install.yml → Receptor Sanity check → receptor → receptor → calculate_mesh.yml | Store Receptor Mesh metadata | Store Receptor Mesh metadata | — |
| 329 | 12.9% | install.yml → Receptor Sanity check → receptor → receptor → calculate_mesh.yml | Store Receptor Deprovision Mesh metadata | Store Receptor Deprovision Mesh metadata | — |
| 330 | 12.9% | install.yml → Receptor Sanity check → receptor → receptor → calculate_mesh.yml | Define receptor mesh facts | Define receptor mesh facts | mesh is defined; mesh.keys() \| default([]) \| length; inventory_hostname in mesh.keys() |
| 331 | 13.0% | install.yml → Receptor Sanity check → receptor → receptor → calculate_mesh.yml | Define receptor deprovision mesh facts | Define receptor deprovision mesh facts | deprovision_mesh is defined; deprovision_mesh.keys() \| default([]) \| length; inventory_hostname in deprovision_mesh.keys() |
| 332 | 13.0% | install.yml → Receptor Sanity check → receptor → receptor | Include DNS asserts | Include DNS asserts (file `assert_dns.yml`) | — |
| 333 | 13.0% | install.yml → Receptor Sanity check → receptor → receptor → assert_dns.yml | file `assert_dns.yml` | Task file `ansible/automation_platform_installer/roles/receptor/tasks/assert_dns.yml` | — |
| 334 | 13.1% | install.yml → Receptor Sanity check → receptor → receptor → assert_dns.yml | Validate if receptor peer name is DNS resolvable | Validate if receptor peer name is DNS resolvable | item \| default([]) \| length; hostvars[item]['node_state'] == 'active'; hostvars[item]['receptor_host_identifier'] \| default([]) \| length; hostvars[item]['receptor_host_identifier'] \| regex_search(regexp_ip) == '' or hostvars[item]['receptor_host_identifier'] \| regex_search(regexp_ip) == None |
| 335 | 13.1% | install.yml → Receptor Sanity check → receptor → receptor → assert_dns.yml | Report error when a receptor node cannot resolve a connected peer name | Report error when a receptor node cannot resolve a connected peer name | _getent.failed \| default(False) \| bool; item.rc != 0 |
| 336 | 13.2% | install.yml → Receptor Sanity check → receptor → receptor | Include SSL asserts | Include SSL asserts (file `assert_ssl.yml`) | — |
| 337 | 13.2% | install.yml → Receptor Sanity check → receptor → receptor → assert_ssl.yml | file `assert_ssl.yml` | Task file `ansible/automation_platform_installer/roles/receptor/tasks/assert_ssl.yml` | — |
| 338 | 13.2% | install.yml → Receptor Sanity check → receptor → receptor → assert_ssl.yml | Ensure custom SSL Certificate Authority files were provided | Ensure custom SSL Certificate Authority files were provided | mesh_ca_certfile is defined or mesh_ca_keyfile is defined |
| 339 | 13.3% | install.yml → Receptor Sanity check → receptor → receptor → assert_ssl.yml | Ensure custom SSL Certificate Authority files matches | Begin conditional or grouped task block | mesh_ca_certfile is defined; mesh_ca_keyfile is defined |
| 340 | 13.3% | install.yml → Receptor Sanity check → receptor → receptor → assert_ssl.yml → Ensure custom SSL Certificate Authority files matches | Verify custom node SSL private key | Verify custom node SSL private key | mesh_ca_certfile is defined; mesh_ca_keyfile is defined |
| 341 | 13.4% | install.yml → Receptor Sanity check → receptor → receptor → assert_ssl.yml → Ensure custom SSL Certificate Authority files matches | Verify custom node SSL x509 key | Verify custom node SSL x509 key | mesh_ca_certfile is defined; mesh_ca_keyfile is defined |
| 342 | 13.4% | install.yml → Receptor Sanity check → receptor → receptor → assert_ssl.yml → Ensure custom SSL Certificate Authority files matches | Ensure custom SSL pair matches | Ensure custom SSL pair matches | mesh_ca_certfile is defined; mesh_ca_keyfile is defined |
| 343 | 13.4% | install.yml → Receptor Sanity check → receptor → receptor → assert_ssl.yml | Ensure custom node SSL files are valid when provided | Ensure custom node SSL files are valid when provided | mesh_node_certfile is defined or mesh_node_keyfile is defined |
| 344 | 13.5% | install.yml → Receptor Sanity check → receptor → receptor → assert_ssl.yml | Ensure custom node SSL files matches | Begin conditional or grouped task block | mesh_node_certfile is defined; mesh_node_keyfile is defined |
| 345 | 13.5% | install.yml → Receptor Sanity check → receptor → receptor → assert_ssl.yml → Ensure custom node SSL files matches | Verify custom node SSL private key | Verify custom node SSL private key | mesh_node_certfile is defined; mesh_node_keyfile is defined |
| 346 | 13.6% | install.yml → Receptor Sanity check → receptor → receptor → assert_ssl.yml → Ensure custom node SSL files matches | Verify custom node SSL x509 key | Verify custom node SSL x509 key | mesh_node_certfile is defined; mesh_node_keyfile is defined |
| 347 | 13.6% | install.yml → Receptor Sanity check → receptor → receptor → assert_ssl.yml → Ensure custom node SSL files matches | Ensure custom SSL pair matches | Ensure custom SSL pair matches | mesh_node_certfile is defined; mesh_node_keyfile is defined |
| 348 | 13.6% | install.yml → Receptor Sanity check → receptor → receptor → assert_ssl.yml → Ensure custom node SSL files matches | Ensure x509 certificate was signed by the expected Certificate Authority | Ensure x509 certificate was signed by the expected Certificate Authority | mesh_node_certfile is defined; mesh_node_keyfile is defined |
| 349 | 13.7% | install.yml | Deactivate and uninstall desired nodes | Play targeting `automationcontroller:execution_nodes` | — |
| 350 | 13.7% | install.yml → Deactivate and uninstall desired nodes | set_fact | Set a host fact used by later tasks | — |
| 351 | 13.7% | install.yml → Deactivate and uninstall desired nodes | Deprovision nodes | Deprovision nodes (role `deprovision`) | node_state is defined; node_state == 'deprovision' |
| 352 | 13.8% | install.yml → Deactivate and uninstall desired nodes → deprovision | role `deprovision` | Enter role task list | node_state is defined; node_state == 'deprovision' |
| 353 | 13.8% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision | file `main.yml` | Task file `ansible/automation_platform_installer/roles/deprovision/tasks/main.yml` | node_state is defined; node_state == 'deprovision' |
| 354 | 13.9% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision | include receptor variables | include receptor variables | node_state is defined; node_state == 'deprovision' |
| 355 | 13.9% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision | include redis variables | include redis variables | node_state is defined; node_state == 'deprovision' |
| 356 | 13.9% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision | List hosts marked for deprovisioning | List hosts marked for deprovisioning | node_state is defined; node_state == 'deprovision' |
| 357 | 14.0% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision | Show deprovision warning disclaimer | Show deprovision warning disclaimer | node_state is defined; node_state == 'deprovision'; deprovision_disclaimer \| bool |
| 358 | 14.0% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision | Ensure that user accepted the deprovision terms | Ensure that user accepted the deprovision terms | node_state is defined; node_state == 'deprovision'; deprovision_disclaimer \| bool |
| 359 | 14.1% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision | Determine if {{ receptor_package_name }} RPM is installed | Determine if {{ receptor_package_name }} RPM is installed | node_state is defined; node_state == 'deprovision' |
| 360 | 14.1% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision | Determine if {{ controller_package_name }} RPM is installed | Determine if {{ controller_package_name }} RPM is installed | node_state is defined; node_state == 'deprovision'; inventory_hostname in groups['automationcontroller'] |
| 361 | 14.1% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision | Determine if Nginx RPM is installed | Determine if Nginx RPM is installed | node_state is defined; node_state == 'deprovision'; inventory_hostname in groups['automationcontroller'] |
| 362 | 14.2% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision | Deprovision node instance from the database. | Deprovision node instance from the database. | node_state is defined; node_state == 'deprovision'; controller_installed is changed or receptor_installed is changed |
| 363 | 14.2% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision | Deprovision Nginx | Deprovision Nginx (file `nginx.yml`) | node_state is defined; node_state == 'deprovision'; nginx_installed is changed |
| 364 | 14.3% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → nginx.yml | file `nginx.yml` | Task file `ansible/automation_platform_installer/roles/deprovision/tasks/nginx.yml` | node_state is defined; node_state == 'deprovision'; nginx_installed is changed |
| 365 | 14.3% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → nginx.yml | Stop and disable Nginx Services | Stop and disable Nginx Services (service `nginx`) | node_state is defined; node_state == 'deprovision'; nginx_installed is changed |
| 366 | 14.3% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → nginx.yml | Remove controller nginx config file | Remove controller nginx config file | node_state is defined; node_state == 'deprovision'; nginx_installed is changed |
| 367 | 14.4% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → nginx.yml | Check for Existence of Nginx.conf File | Check for Existence of Nginx.conf File | node_state is defined; node_state == 'deprovision'; nginx_installed is changed |
| 368 | 14.4% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → nginx.yml | Backup Current Nginx File and Restore Default Configuration File | Begin conditional or grouped task block | node_state is defined; node_state == 'deprovision'; nginx_installed is changed; nginx_conf_file.stat.exists |
| 369 | 14.5% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → nginx.yml → Backup Current Nginx File and Restore Default Configuration File | ansible.builtin.copy | Run `ansible.builtin.copy` | node_state is defined; node_state == 'deprovision'; nginx_installed is changed; nginx_conf_file.stat.exists |
| 370 | 14.5% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → nginx.yml → Backup Current Nginx File and Restore Default Configuration File | file | Manage files, directories, or symlinks | node_state is defined; node_state == 'deprovision'; nginx_installed is changed; nginx_conf_file.stat.exists |
| 371 | 14.5% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → nginx.yml | Check for Existence of nginx.conf.default File | Check for Existence of nginx.conf.default File | node_state is defined; node_state == 'deprovision'; nginx_installed is changed |
| 372 | 14.6% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → nginx.yml | Ensure that Nginx Default Config File is Restored [If Exists] | Ensure that Nginx Default Config File is Restored [If Exists] | node_state is defined; node_state == 'deprovision'; nginx_installed is changed; nginx_conf_default_file.stat.exists |
| 373 | 14.6% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision | Deprovision Receptor | Deprovision Receptor (file `receptor.yml`) | node_state is defined; node_state == 'deprovision'; receptor_installed is changed |
| 374 | 14.7% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → receptor.yml | file `receptor.yml` | Task file `ansible/automation_platform_installer/roles/deprovision/tasks/receptor.yml` | node_state is defined; node_state == 'deprovision'; receptor_installed is changed |
| 375 | 14.7% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → receptor.yml | Stop and disable receptor service | Stop and disable receptor service (service `{{ receptor_package_name }}`) | node_state is defined; node_state == 'deprovision'; receptor_installed is changed |
| 376 | 14.7% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → receptor.yml | Remove Receptor related RPMs | Remove Receptor related RPMs | node_state is defined; node_state == 'deprovision'; receptor_installed is changed |
| 377 | 14.8% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → receptor.yml | Cleanup Receptor files | Cleanup Receptor files (dest `{{ item }}`) | node_state is defined; node_state == 'deprovision'; receptor_installed is changed |
| 378 | 14.8% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → receptor.yml | Remove receptor user | Remove receptor user | node_state is defined; node_state == 'deprovision'; receptor_installed is changed |
| 379 | 14.8% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → receptor.yml | Remove awx user | Remove awx user | node_state is defined; node_state == 'deprovision'; receptor_installed is changed; inventory_hostname in groups['execution_nodes'] |
| 380 | 14.9% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision | Deprovision Automation Controller | Deprovision Automation Controller (file `controller.yml`) | node_state is defined; node_state == 'deprovision'; inventory_hostname in groups['automationcontroller'] and controller_installed.changed \| default(False) \| bool |
| 381 | 14.9% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → controller.yml | file `controller.yml` | Task file `ansible/automation_platform_installer/roles/deprovision/tasks/controller.yml` | node_state is defined; node_state == 'deprovision'; inventory_hostname in groups['automationcontroller'] and controller_installed.changed \| default(False) \| bool |
| 382 | 15.0% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → controller.yml | Determine if {{ controller_package_name }} RPM is installed | Determine if {{ controller_package_name }} RPM is installed | node_state is defined; node_state == 'deprovision'; inventory_hostname in groups['automationcontroller'] and controller_installed.changed \| default(False) \| bool |
| 383 | 15.0% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → controller.yml | Stop and disable controller services | Stop and disable controller services (service `{{ item }}`) | node_state is defined; node_state == 'deprovision'; inventory_hostname in groups['automationcontroller'] and controller_installed.changed \| default(False) \| bool |
| 384 | 15.0% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → controller.yml | Remove controller related RPMs | Remove controller related RPMs | node_state is defined; node_state == 'deprovision'; inventory_hostname in groups['automationcontroller'] and controller_installed.changed \| default(False) \| bool |
| 385 | 15.1% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → controller.yml | Cleanup controller files | Cleanup controller files (dest `{{ item }}`) | node_state is defined; node_state == 'deprovision'; inventory_hostname in groups['automationcontroller'] and controller_installed.changed \| default(False) \| bool |
| 386 | 15.1% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → controller.yml | include_role | Include and run tasks from an Ansible role (role `redis`, tasks `deprovision.yml`) | node_state is defined; node_state == 'deprovision'; inventory_hostname in groups['automationcontroller'] and controller_installed.changed \| default(False) \| bool |
| 387 | 15.2% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → controller.yml → redis | role `redis` (`deprovision.yml`) | Enter role task list | node_state is defined; node_state == 'deprovision'; inventory_hostname in groups['automationcontroller'] and controller_installed.changed \| default(False) \| bool |
| 388 | 15.2% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → controller.yml → redis → redis | file `deprovision.yml` | Task file `ansible/automation_platform_installer/roles/redis/tasks/deprovision.yml` | node_state is defined; node_state == 'deprovision'; inventory_hostname in groups['automationcontroller'] and controller_installed.changed \| default(False) \| bool |
| 389 | 15.2% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → controller.yml → redis → redis | Remove redis rpm | Remove redis rpm | node_state is defined; node_state == 'deprovision'; inventory_hostname in groups['automationcontroller'] and controller_installed.changed \| default(False) \| bool |
| 390 | 15.3% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → controller.yml → redis → redis | Remove remaining redis files and directories | Remove remaining redis files and directories | node_state is defined; node_state == 'deprovision'; inventory_hostname in groups['automationcontroller'] and controller_installed.changed \| default(False) \| bool |
| 391 | 15.3% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → controller.yml | Gather supervisord log files | Gather supervisord log files | node_state is defined; node_state == 'deprovision'; inventory_hostname in groups['automationcontroller'] and controller_installed.changed \| default(False) \| bool |
| 392 | 15.4% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → controller.yml | Cleanup supervisord logs | Cleanup supervisord logs (dest `{{ item.path }}`) | node_state is defined; node_state == 'deprovision'; inventory_hostname in groups['automationcontroller'] and controller_installed.changed \| default(False) \| bool |
| 393 | 15.4% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → controller.yml | Check for awx user | Check for awx user | node_state is defined; node_state == 'deprovision'; inventory_hostname in groups['automationcontroller'] and controller_installed.changed \| default(False) \| bool |
| 394 | 15.4% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → controller.yml | Remove ACL rule for awx user to access rhsm.conf | Remove ACL rule for awx user to access rhsm.conf | node_state is defined; node_state == 'deprovision'; inventory_hostname in groups['automationcontroller'] and controller_installed.changed \| default(False) \| bool; getent_passwd['awx'] \| default([]) \| length |
| 395 | 15.5% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → controller.yml | Disable lingering systemd sessions for awx user | Disable lingering systemd sessions for awx user | node_state is defined; node_state == 'deprovision'; inventory_hostname in groups['automationcontroller'] and controller_installed.changed \| default(False) \| bool |
| 396 | 15.5% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → controller.yml | Pause to allow lingering sessions and processes to stop | Pause to allow lingering sessions and processes to stop | node_state is defined; node_state == 'deprovision'; inventory_hostname in groups['automationcontroller'] and controller_installed.changed \| default(False) \| bool |
| 397 | 15.6% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → controller.yml | Remove awx user | Remove awx user | node_state is defined; node_state == 'deprovision'; inventory_hostname in groups['automationcontroller'] and controller_installed.changed \| default(False) \| bool |
| 398 | 15.6% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision | Deprovision firewall | Deprovision firewall (role `firewall`, tasks `deprovision.yml`) | node_state is defined; node_state == 'deprovision' |
| 399 | 15.6% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → firewall | role `firewall` (`deprovision.yml`) | Enter role task list | node_state is defined; node_state == 'deprovision' |
| 400 | 15.7% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → firewall → firewall | file `deprovision.yml` | Task file `ansible/automation_platform_installer/roles/firewall/tasks/deprovision.yml` | node_state is defined; node_state == 'deprovision' |
| 401 | 15.7% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → firewall → firewall | include_tasks | Include a task file from the current role or playbook (file `discover.yml`) | node_state is defined; node_state == 'deprovision' |
| 402 | 15.8% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → firewall → firewall → discover.yml | file `discover.yml` | Task file `ansible/automation_platform_installer/roles/firewall/tasks/discover.yml` | node_state is defined; node_state == 'deprovision' |
| 403 | 15.8% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → firewall → firewall → discover.yml | Determine if firewalld is running | Determine if firewalld is running | node_state is defined; node_state == 'deprovision' |
| 404 | 15.8% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → firewall → firewall → discover.yml | Determine if firewalld is enabled | Determine if firewalld is enabled | node_state is defined; node_state == 'deprovision' |
| 405 | 15.9% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → firewall → firewall → discover.yml | Determine if iptables is installed | Determine if iptables is installed | node_state is defined; node_state == 'deprovision' |
| 406 | 15.9% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → firewall → firewall → discover.yml | Determine if iptables service is installed | Determine if iptables service is installed | node_state is defined; node_state == 'deprovision' |
| 407 | 15.9% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → firewall → firewall → discover.yml | Check to see if there is an iptables config file | Check to see if there is an iptables config file | node_state is defined; node_state == 'deprovision'; iptables_installed.rc == 0 |
| 408 | 16.0% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → firewall → firewall → discover.yml | Select firewalld | Select firewalld | node_state is defined; node_state == 'deprovision'; firewalld_running.rc == 0 or systemctl_unit_files.stdout_lines \| select('match','firewalld.service\s+enabled') \| list \| length == 1 |
| 409 | 16.0% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → firewall → firewall → discover.yml | Select iptables | Select iptables | node_state is defined; node_state == 'deprovision'; iptables_installed.rc == 0; iptables_exists.stat.exists; iptables_service.stat.exists |
| 410 | 16.1% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → firewall → firewall | set_fact | Set a host fact used by later tasks | node_state is defined; node_state == 'deprovision' |
| 411 | 16.1% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → firewall → firewall | block | Begin conditional or grouped task block | node_state is defined; node_state == 'deprovision'; component_firewall_type \| string == "firewalld" |
| 412 | 16.1% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → firewall → firewall → block | Disable base {{ firewall_component_name }} firewall ports | Disable base {{ firewall_component_name }} firewall ports | node_state is defined; node_state == 'deprovision'; component_firewall_type \| string == "firewalld"; firewall_component_name != "database" |
| 413 | 16.2% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → firewall → firewall → block | Disable standalone database firewall ports for {{ firewall_component_name }} | Disable standalone database firewall ports for {{ firewall_component_name }} | node_state is defined; node_state == 'deprovision'; component_firewall_type \| string == "firewalld"; firewall_component_name != "database" |
| 414 | 16.2% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → firewall → firewall → block | Disable standalone database firewall ports for {{ firewall_component_name }} | Disable standalone database firewall ports for {{ firewall_component_name }} | node_state is defined; node_state == 'deprovision'; component_firewall_type \| string == "firewalld"; firewall_component_name == "database" |
| 415 | 16.3% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → firewall → firewall → block | Disable execution node firewall ports | Disable execution node firewall ports | node_state is defined; node_state == 'deprovision'; component_firewall_type \| string == "firewalld"; listener \| default(False) \| bool and (inventory_hostname in groups['execution_nodes'] \| default([]) or  inventory_hostname in groups['automationcontroller'] \| default([])) |
| 416 | 16.3% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → firewall → firewall | Remove iptables rules for {{ firewall_component_name }} | Remove iptables rules for {{ firewall_component_name }} | node_state is defined; node_state == 'deprovision'; component_firewall_type \| string == "iptables" |
| 417 | 16.3% | install.yml → Deactivate and uninstall desired nodes | Prepare former isolated_nodes for upgrade path | Prepare former isolated_nodes for upgrade path (role `deprovision`, tasks `isolated_nodes`) | node_state is defined; node_state == 'iso_migrate'; inventory_hostname in groups['execution_nodes'] |
| 418 | 16.4% | install.yml → Deactivate and uninstall desired nodes → deprovision | role `deprovision` (`isolated_nodes`) | Enter role task list | node_state is defined; node_state == 'iso_migrate'; inventory_hostname in groups['execution_nodes'] |
| 419 | 16.4% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision | file `isolated_nodes.yml` | Task file `ansible/automation_platform_installer/roles/deprovision/tasks/isolated_nodes.yml` | node_state is defined; node_state == 'iso_migrate'; inventory_hostname in groups['execution_nodes'] |
| 420 | 16.5% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision | Deprovision former isolated node instance from the database. | Deprovision former isolated node instance from the database. | node_state is defined; node_state == 'iso_migrate'; inventory_hostname in groups['execution_nodes'] |
| 421 | 16.5% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision | Determine if the ansible-tower-isolated RPM is installed | Determine if the ansible-tower-isolated RPM is installed | node_state is defined; node_state == 'iso_migrate'; inventory_hostname in groups['execution_nodes'] |
| 422 | 16.5% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision | Proceed with the isolated_nodes migration | Begin conditional or grouped task block | node_state is defined; node_state == 'iso_migrate'; inventory_hostname in groups['execution_nodes']; tower_iso_installed is changed |
| 423 | 16.6% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → Proceed with the isolated_nodes migration | Remove isolated_node related RPMs | Remove isolated_node related RPMs | node_state is defined; node_state == 'iso_migrate'; inventory_hostname in groups['execution_nodes']; tower_iso_installed is changed |
| 424 | 16.6% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → Proceed with the isolated_nodes migration | Cleanup isolated_nodes files | Cleanup isolated_nodes files (dest `{{ item }}`) | node_state is defined; node_state == 'iso_migrate'; inventory_hostname in groups['execution_nodes']; tower_iso_installed is changed |
| 425 | 16.7% | install.yml → Deactivate and uninstall desired nodes → deprovision → deprovision → Proceed with the isolated_nodes migration | Recreate awx home directory | Recreate awx home directory | node_state is defined; node_state == 'iso_migrate'; inventory_hostname in groups['execution_nodes']; tower_iso_installed is changed |
| 426 | 16.7% | install.yml | Prep Tower node(s) | Play targeting `automationcontroller` | — |
| 427 | 16.7% | install.yml → Prep Tower node(s) | role `migrations` | Enter role task list | — |
| 428 | 16.8% | install.yml → Prep Tower node(s) → migrations | file `main.yml` | Task file `ansible/automation_platform_installer/roles/migrations/tasks/main.yml` | — |
| 429 | 16.8% | install.yml | Initialize Ansible Automation Platform internal CA server and service variables | Play targeting `automationgateway:automationcontroller:automationhub:automationedacontroller` | — |
| 430 | 16.8% | install.yml → Initialize Ansible Automation Platform internal CA server and service variables | Build aap_ca_server group | Build aap_ca_server group (role `certificate_authority`, tasks `discover.yml`) | eda_node_type \| default('') != 'worker' |
| 431 | 16.9% | install.yml → Initialize Ansible Automation Platform internal CA server and service variables → certificate_authority | role `certificate_authority` (`discover.yml`) | Enter role task list | eda_node_type \| default('') != 'worker' |
| 432 | 16.9% | install.yml → Initialize Ansible Automation Platform internal CA server and service variables → certificate_authority → certificate_authority | file `discover.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/discover.yml` | eda_node_type \| default('') != 'worker' |
| 433 | 17.0% | install.yml → Initialize Ansible Automation Platform internal CA server and service variables → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | eda_node_type \| default('') != 'worker'; not aap_ca_server_hostname \| default('') \| length |
| 434 | 17.0% | install.yml → Initialize Ansible Automation Platform internal CA server and service variables → certificate_authority → certificate_authority → block | Find an existing certificate authority | Find an existing certificate authority | eda_node_type \| default('') != 'worker'; not aap_ca_server_hostname \| default('') \| length |
| 435 | 17.0% | install.yml → Initialize Ansible Automation Platform internal CA server and service variables → certificate_authority → certificate_authority → block | Generate a list of existing certificate authority results | Generate a list of existing certificate authority results | eda_node_type \| default('') != 'worker'; not aap_ca_server_hostname \| default('') \| length; aap_ca_server_exists.stat.exists |
| 436 | 17.1% | install.yml → Initialize Ansible Automation Platform internal CA server and service variables → certificate_authority → certificate_authority → block | Build list of eligible hosts if CA does not exist | Build list of eligible hosts if CA does not exist | eda_node_type \| default('') != 'worker'; not aap_ca_server_hostname \| default('') \| length; not aap_ca_server_hosts \| default([]) \| length; not aap_ca_backuprestore \| bool |
| 437 | 17.1% | install.yml → Initialize Ansible Automation Platform internal CA server and service variables → certificate_authority → certificate_authority → block | Select the first eliglible host for CA group | Select the first eliglible host for CA group | eda_node_type \| default('') != 'worker'; not aap_ca_server_hostname \| default('') \| length; hostvars['localhost']['aap_ca_server_hosts'] \| default([]) \| length or not aap_ca_backuprestore \| bool |
| 438 | 17.2% | install.yml → Initialize Ansible Automation Platform internal CA server and service variables → certificate_authority → certificate_authority | Add CA server host to inventory group | Add CA server host to inventory group | eda_node_type \| default('') != 'worker'; aap_ca_server_hostname \| default('') \| length |
| 439 | 17.2% | install.yml | Install Ansible Automation Platform internal CA | Play targeting `aap_ca_server[0]` | — |
| 440 | 17.2% | install.yml → Install Ansible Automation Platform internal CA | Configure internal CA | Configure internal CA (role `certificate_authority`) | — |
| 441 | 17.3% | install.yml → Install Ansible Automation Platform internal CA → certificate_authority | role `certificate_authority` | Enter role task list | — |
| 442 | 17.3% | install.yml → Install Ansible Automation Platform internal CA → certificate_authority → certificate_authority | file `main.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/main.yml` | — |
| 443 | 17.4% | install.yml → Install Ansible Automation Platform internal CA → certificate_authority → certificate_authority | include_tasks | Include a task file from the current role or playbook (file `init.yml`) | — |
| 444 | 17.4% | install.yml → Install Ansible Automation Platform internal CA → certificate_authority → certificate_authority → init.yml | file `init.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/init.yml` | — |
| 445 | 17.4% | install.yml → Install Ansible Automation Platform internal CA → certificate_authority → certificate_authority → init.yml | Ensure python cryptography libraries are installed | Ensure python cryptography libraries are installed | — |
| 446 | 17.5% | install.yml → Install Ansible Automation Platform internal CA → certificate_authority → certificate_authority | Create CA directory | Create CA directory | — |
| 447 | 17.5% | install.yml → Install Ansible Automation Platform internal CA → certificate_authority → certificate_authority | Set cipher if needed | Set cipher if needed | aap_ca_pass is defined |
| 448 | 17.6% | install.yml → Install Ansible Automation Platform internal CA → certificate_authority → certificate_authority | Copy user-provided CA key file, if provided | Copy user-provided CA key file, if provided (dest `/etc/ansible-automation-platform/ca/ansible-automation-platform-managed-ca-key.key`) | aap_ca_key_file \| length |
| 449 | 17.6% | install.yml → Install Ansible Automation Platform internal CA → certificate_authority → certificate_authority | Copy user-provided CA certificate file, if provided | Copy user-provided CA certificate file, if provided (dest `/etc/ansible-automation-platform/ca/ansible-automation-platform-managed-ca-cert.crt`) | aap_ca_cert_file \| length |
| 450 | 17.6% | install.yml → Install Ansible Automation Platform internal CA → certificate_authority → certificate_authority | Generate root CA key | Generate root CA key | not aap_ca_key_file \| length |
| 451 | 17.7% | install.yml → Install Ansible Automation Platform internal CA → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | not aap_ca_cert_file \| length |
| 452 | 17.7% | install.yml → Install Ansible Automation Platform internal CA → certificate_authority → certificate_authority → block | Create CSR for CA certificate | Create CSR for CA certificate | not aap_ca_cert_file \| length |
| 453 | 17.8% | install.yml → Install Ansible Automation Platform internal CA → certificate_authority → certificate_authority → block | Create AAP CA certificate | Create AAP CA certificate | not aap_ca_cert_file \| length |
| 454 | 17.8% | install.yml → Install Ansible Automation Platform internal CA → certificate_authority → certificate_authority | Get CA certificate SKI for Signing | Get CA certificate SKI for Signing (file `get_cacert.yml`) | — |
| 455 | 17.8% | install.yml → Install Ansible Automation Platform internal CA → certificate_authority → certificate_authority → get_cacert.yml | file `get_cacert.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/get_cacert.yml` | — |
| 456 | 17.9% | install.yml → Install Ansible Automation Platform internal CA → certificate_authority → certificate_authority → get_cacert.yml | Get AAP CA certificate info | Get AAP CA certificate info | — |
| 457 | 17.9% | install.yml → Install Ansible Automation Platform internal CA → certificate_authority → certificate_authority → get_cacert.yml | set_fact | Set a host fact used by later tasks | — |
| 458 | 17.9% | install.yml → Install Ansible Automation Platform internal CA → certificate_authority → certificate_authority → get_cacert.yml | Slurp AAP CA certificate | Slurp AAP CA certificate | — |
| 459 | 18.0% | install.yml | Install postgres database node | Play targeting `database:automationgateway:automationcontroller:automationedacontroller:automationhub` | — |
| 460 | 18.0% | install.yml → Install postgres database node | Install postgres database node | Begin conditional or grouped task block | — |
| 461 | 18.1% | install.yml → Install postgres database node → Install postgres database node | block | Begin conditional or grouped task block | not groups['database'] \| default([]) \| length |
| 462 | 18.1% | install.yml → Install postgres database node → Install postgres database node → block | set_fact | Set a host fact used by later tasks | not groups['database'] \| default([]) \| length; groups['automationcontroller'] \| default([]) \| length; inventory_hostname == groups['automationcontroller'][0]; config_dynamic_database == 'internal' |
| 463 | 18.1% | install.yml → Install postgres database node → Install postgres database node → block | set_fact | Set a host fact used by later tasks | not groups['database'] \| default([]) \| length; inventory_hostname == groups['automationgateway'][0]; automationgateway_config_dynamic_database == 'internal'; not groups.get('automationcontroller', []) \| length |
| 464 | 18.2% | install.yml → Install postgres database node → Install postgres database node | set_fact | Set a host fact used by later tasks | _do_run_db_install \| default(false) \| bool; groups.get('aap_valid_hosts') \| length > 1 |
| 465 | 18.2% | install.yml → Install postgres database node → Install postgres database node | Add internal CA certificate to trust store | Add internal CA certificate to trust store (role `certificate_authority`, tasks `add_cacert.yml`) | — |
| 466 | 18.3% | install.yml → Install postgres database node → Install postgres database node → certificate_authority | role `certificate_authority` (`add_cacert.yml`) | Enter role task list | — |
| 467 | 18.3% | install.yml → Install postgres database node → Install postgres database node → certificate_authority → certificate_authority | file `add_cacert.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/add_cacert.yml` | — |
| 468 | 18.3% | install.yml → Install postgres database node → Install postgres database node → certificate_authority → certificate_authority | Ensure ca-certificates is installed | Ensure ca-certificates is installed | — |
| 469 | 18.4% | install.yml → Install postgres database node → Install postgres database node → certificate_authority → certificate_authority | Copy custom CA cert | Copy custom CA cert (dest `/etc/pki/ca-trust/source/anchors/ansible-automation-platform-custom-ca-cert.crt`) | custom_ca_cert is defined |
| 470 | 18.4% | install.yml → Install postgres database node → Install postgres database node → certificate_authority → certificate_authority | Place AAP CA certificate | Place AAP CA certificate (dest `/etc/pki/ca-trust/source/anchors/ansible-automation-platform-managed-ca-cert.crt`) | — |
| 471 | 18.5% | install.yml → Install postgres database node → Install postgres database node | Run repo_management (setup) | Run repo_management (setup) (role `repo_management`, tasks `setup`) | — |
| 472 | 18.5% | install.yml → Install postgres database node → Install postgres database node → repo_management | role `repo_management` (`setup`) | Enter role task list | — |
| 473 | 18.5% | install.yml → Install postgres database node → Install postgres database node → repo_management → repo_management | file `setup.yml` | Task file `ansible/automation_platform_installer/roles/repo_management/tasks/setup.yml` | — |
| 474 | 18.6% | install.yml → Install postgres database node → Install postgres database node → repo_management → repo_management | include_vars | Load variables from a file | — |
| 475 | 18.6% | install.yml → Install postgres database node → Install postgres database node → repo_management → repo_management | Install yum repo keys | Install yum repo keys (dest `/etc/pki/rpm-gpg/{{ item }}`) | — |
| 476 | 18.7% | install.yml → Install postgres database node → Install postgres database node → repo_management → repo_management | Install subscription-manager-rhsm-certificates | Install subscription-manager-rhsm-certificates | — |
| 477 | 18.7% | install.yml → Install postgres database node → Install postgres database node → repo_management → repo_management | Determine if rhsm repository exists | Determine if rhsm repository exists | — |
| 478 | 18.7% | install.yml → Install postgres database node → Install postgres database node → repo_management → repo_management | Enable Automation Platform rhsm repository | Enable Automation Platform rhsm repository | ansible_distribution == 'RedHat'; rhsm_repo.stat.exists; not bundle_install \| bool; aw_repo_url is undefined |
| 479 | 18.8% | install.yml → Install postgres database node → Install postgres database node → repo_management → repo_management | Explain Automation Platform rhsm repository failure | Explain Automation Platform rhsm repository failure | aap_rhsm_repo_enable.failed \| default(false) |
| 480 | 18.8% | install.yml → Install postgres database node → Install postgres database node → repo_management → repo_management | block | Begin conditional or grouped task block | aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 481 | 18.9% | install.yml → Install postgres database node → Install postgres database node → repo_management → repo_management → block | Enable module hotfixes flag | Enable module hotfixes flag | aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 482 | 18.9% | install.yml → Install postgres database node → Install postgres database node → repo_management → repo_management → block | Set AAP repo for update | Set AAP repo for update | aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 483 | 18.9% | install.yml → Install postgres database node → Install postgres database node → repo_management → repo_management | Ensure rsync is installed | Ensure rsync is installed | bundle_install \| bool |
| 484 | 19.0% | install.yml → Install postgres database node → Install postgres database node → repo_management → repo_management | Copy bundle packages to repo source directory | Begin conditional or grouped task block | bundle_install \| bool |
| 485 | 19.0% | install.yml → Install postgres database node → Install postgres database node → repo_management → repo_management → Copy bundle packages to repo source directory | Copy bundle packages to repo source directory | Copy bundle packages to repo source directory | bundle_install \| bool |
| 486 | 19.0% | install.yml → Install postgres database node → Install postgres database node → repo_management → repo_management | Copy bundle packages to repo source directory (rescue) | Run rescue tasks for block | bundle_install \| bool |
| 487 | 19.1% | install.yml → Install postgres database node → Install postgres database node → repo_management → repo_management → Copy bundle packages to repo source directory (rescue) | Remove old bundle packages source directory (legacy) | Remove old bundle packages source directory (legacy) | bundle_install \| bool |
| 488 | 19.1% | install.yml → Install postgres database node → Install postgres database node → repo_management → repo_management → Copy bundle packages to repo source directory (rescue) | Copy bundle packages to repo source directory (legacy) | Copy bundle packages to repo source directory (legacy) (dest `{{ bundle_install_folder }}`) | bundle_install \| bool |
| 489 | 19.2% | install.yml → Install postgres database node → Install postgres database node → repo_management → repo_management | Install the Automation Platform bundle repository | Install the Automation Platform bundle repository (dest `/etc/yum.repos.d/ansible-automation-platform.repo`) | bundle_install \| bool |
| 490 | 19.2% | install.yml → Install postgres database node → Install postgres database node → repo_management → repo_management | Remove legacy ansible-tower repository | Remove legacy ansible-tower repository | — |
| 491 | 19.2% | install.yml → Install postgres database node → Install postgres database node → repo_management → repo_management | Install the Automation Platform yum repository. | Install the Automation Platform yum repository. (dest `/etc/yum.repos.d/ansible-automation-platform.repo`) | aw_repo_url is defined; not bundle_install \| bool |
| 492 | 19.3% | install.yml → Install postgres database node → Install postgres database node → repo_management → repo_management | Set repo list for bundle install | Set repo list for bundle install | bundle_install \| bool |
| 493 | 19.3% | install.yml → Install postgres database node → Install postgres database node → repo_management → repo_management | Set repo list for online install | Set repo list for online install | yum_repo is succeeded; yum_repo is not skipped |
| 494 | 19.4% | install.yml → Install postgres database node → Install postgres database node → repo_management → repo_management | Enable Ansible Automation Platform repo | Enable Ansible Automation Platform repo | extra_repos is defined |
| 495 | 19.4% | install.yml → Install postgres database node → Install postgres database node | Run packages_el | Run packages_el (role `packages_el`) | — |
| 496 | 19.4% | install.yml → Install postgres database node → Install postgres database node → packages_el | role `packages_el` | Enter role task list | — |
| 497 | 19.5% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el | file `main.yml` | Task file `ansible/automation_platform_installer/roles/packages_el/tasks/main.yml` | — |
| 498 | 19.5% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el | include packages_el variables | include packages_el variables | — |
| 499 | 19.6% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el | include_tasks | Include a task file from the current role or playbook (file `install_deps.yml`) | — |
| 500 | 19.6% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_deps.yml | file `install_deps.yml` | Task file `ansible/automation_platform_installer/roles/packages_el/tasks/install_deps.yml` | — |
| 501 | 19.6% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_deps.yml | Install playbook dependencies | Install playbook dependencies | — |
| 502 | 19.7% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_deps.yml | block | Begin conditional or grouped task block | ansible_distribution_major_version == "8" |
| 503 | 19.7% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_deps.yml → block | Disable required dnf modules | Disable required dnf modules | ansible_distribution_major_version == "8" |
| 504 | 19.8% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_deps.yml → block | Enable required dnf modules | Enable required dnf modules | ansible_distribution_major_version == "8" |
| 505 | 19.8% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el | include_tasks | Include a task file from the current role or playbook (file `upgrade_postgres_client.yml`) | packages_el_install_tower \| bool |
| 506 | 19.8% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → upgrade_postgres_client.yml | file `upgrade_postgres_client.yml` | Task file `ansible/automation_platform_installer/roles/packages_el/tasks/upgrade_postgres_client.yml` | packages_el_install_tower \| bool |
| 507 | 19.9% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → upgrade_postgres_client.yml | Uninstall existing PostgreSQL Client | Uninstall existing PostgreSQL Client | packages_el_install_tower \| bool |
| 508 | 19.9% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → upgrade_postgres_client.yml | Enable PostgreSQL {{ automationcontroller_postgresql_version }} dnf modules | Enable PostgreSQL {{ automationcontroller_postgresql_version }} dnf modules | packages_el_install_tower \| bool |
| 509 | 20.0% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → upgrade_postgres_client.yml | Install PostgreSQL Client | Install PostgreSQL Client | packages_el_install_tower \| bool |
| 510 | 20.0% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el | include_tasks | Include a task file from the current role or playbook (file `install_controller.yml`) | packages_el_install_tower \| bool |
| 511 | 20.0% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_controller.yml | file `install_controller.yml` | Task file `ansible/automation_platform_installer/roles/packages_el/tasks/install_controller.yml` | packages_el_install_tower \| bool |
| 512 | 20.1% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_controller.yml | Clean Yum Cache for the Automation Platform Repo | Clean Yum Cache for the Automation Platform Repo | packages_el_install_tower \| bool |
| 513 | 20.1% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_controller.yml | Install the Automation Controller RPM. | Install the Automation Controller RPM. | packages_el_install_tower \| bool |
| 514 | 20.1% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_controller.yml | Install fapolicyd rules for Automation Controller if applicable | Install fapolicyd rules for Automation Controller if applicable | packages_el_install_tower \| bool; 'fapolicyd.service' in ansible_facts.services |
| 515 | 20.2% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_controller.yml | Remove stale packages | Remove stale packages | packages_el_install_tower \| bool |
| 516 | 20.2% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_controller.yml | Update existing dependent packages | Update existing dependent packages (role `repo_management`, tasks `update_dependency`) | packages_el_install_tower \| bool |
| 517 | 20.3% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_controller.yml → repo_management | role `repo_management` (`update_dependency`) | Enter role task list | packages_el_install_tower \| bool |
| 518 | 20.3% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_controller.yml → repo_management → repo_management | file `update_dependency.yml` | Task file `ansible/automation_platform_installer/roles/repo_management/tasks/update_dependency.yml` | packages_el_install_tower \| bool |
| 519 | 20.3% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_controller.yml → repo_management → repo_management | Ensure AAP repo for update is set | Ensure AAP repo for update is set | packages_el_install_tower \| bool; not aap_repo_for_update \| default('') \| length |
| 520 | 20.4% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_controller.yml → repo_management → repo_management | Generate a list of AAP packages with an update | Generate a list of AAP packages with an update | packages_el_install_tower \| bool |
| 521 | 20.4% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_controller.yml → repo_management → repo_management | Set AAP packages to update | Set AAP packages to update | packages_el_install_tower \| bool |
| 522 | 20.5% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_controller.yml → repo_management → repo_management | Update packages | Update packages | packages_el_install_tower \| bool; _packages_to_update \| length |
| 523 | 20.5% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_controller.yml | Re-install aap-metrics-utility | Begin conditional or grouped task block | packages_el_install_tower \| bool; _controller_rpm_install is changed |
| 524 | 20.5% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_controller.yml → Re-install aap-metrics-utility | Verify aap-metrics-utility package | Verify aap-metrics-utility package | packages_el_install_tower \| bool; _controller_rpm_install is changed |
| 525 | 20.6% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_controller.yml → Re-install aap-metrics-utility | Re-install aap-metrics-utility | Re-install aap-metrics-utility | packages_el_install_tower \| bool; _controller_rpm_install is changed; _metrics_utility_rpm_verify is failed |
| 526 | 20.6% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el | Uninstall nginx-all-modules | Uninstall nginx-all-modules | — |
| 527 | 20.7% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el | include_tasks | Include a task file from the current role or playbook (file `install_postgres.yml`) | packages_el_install_postgres \| bool |
| 528 | 20.7% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml | file `install_postgres.yml` | Task file `ansible/automation_platform_installer/roles/packages_el/tasks/install_postgres.yml` | packages_el_install_postgres \| bool |
| 529 | 20.7% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml | Check for old PostgreSQL data | Check for old PostgreSQL data | packages_el_install_postgres \| bool |
| 530 | 20.8% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml | Read PG_VERSION file contents | Read PG_VERSION file contents | packages_el_install_postgres \| bool; old_pg_version_file.stat.exists |
| 531 | 20.8% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml | Check if PostgreSQL has already been upgraded to 15 | Check if PostgreSQL has already been upgraded to 15 | packages_el_install_postgres \| bool |
| 532 | 20.9% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml | Check if PostgreSQL has already been upgraded to 13 | Check if PostgreSQL has already been upgraded to 13 | packages_el_install_postgres \| bool |
| 533 | 20.9% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml | Check if PostgreSQL has already been upgraded to 12 | Check if PostgreSQL has already been upgraded to 12 | packages_el_install_postgres \| bool |
| 534 | 20.9% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml | Determine if PostgreSQL 10 is present | Determine if PostgreSQL 10 is present | packages_el_install_postgres \| bool |
| 535 | 21.0% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml | Determine if PostgreSQL 12 is present | Determine if PostgreSQL 12 is present | packages_el_install_postgres \| bool |
| 536 | 21.0% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml | Determine if we should upgrade PostgreSQL | Determine if we should upgrade PostgreSQL | packages_el_install_postgres \| bool |
| 537 | 21.1% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml | block | Begin conditional or grouped task block | packages_el_install_postgres \| bool; not upgrade_postgres \| bool; not upgrade_from_postgres_12 \| bool; not upgrade_from_postgres_10 \| bool |
| 538 | 21.1% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → block | Uninstall old PostgreSQL Client | Uninstall old PostgreSQL Client | packages_el_install_postgres \| bool; not upgrade_postgres \| bool; not upgrade_from_postgres_12 \| bool; not upgrade_from_postgres_10 \| bool; ansible_distribution_major_version == "8" |
| 539 | 21.1% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → block | Enable PostgreSQL {{ automationcontroller_postgresql_version }} dnf modules | Enable PostgreSQL {{ automationcontroller_postgresql_version }} dnf modules | packages_el_install_postgres \| bool; not upgrade_postgres \| bool; not upgrade_from_postgres_12 \| bool; not upgrade_from_postgres_10 \| bool |
| 540 | 21.2% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → block | Install PostgreSQL | Install PostgreSQL | packages_el_install_postgres \| bool; not upgrade_postgres \| bool; not upgrade_from_postgres_12 \| bool; not upgrade_from_postgres_10 \| bool |
| 541 | 21.2% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml | Upgrade PostgreSQL data from 10 --> 12 | Upgrade PostgreSQL data from 10 --> 12 (file `upgrade_from_postgres_10.yml`) | packages_el_install_postgres \| bool; upgrade_from_postgres_10 \| bool; ansible_distribution_major_version == "8" |
| 542 | 21.2% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_from_postgres_10.yml | file `upgrade_from_postgres_10.yml` | Task file `ansible/automation_platform_installer/roles/packages_el/tasks/upgrade_from_postgres_10.yml` | packages_el_install_postgres \| bool; upgrade_from_postgres_10 \| bool; ansible_distribution_major_version == "8" |
| 543 | 21.3% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_from_postgres_10.yml | Stop PostgreSQL 10 | Stop PostgreSQL 10 (service `{{ pg_systemd_name }}`) | packages_el_install_postgres \| bool; upgrade_from_postgres_10 \| bool; ansible_distribution_major_version == "8" |
| 544 | 21.3% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_from_postgres_10.yml | Reset PostgreSQL module | Reset PostgreSQL module | packages_el_install_postgres \| bool; upgrade_from_postgres_10 \| bool; ansible_distribution_major_version == "8" |
| 545 | 21.4% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_from_postgres_10.yml | Enable PostgreSQL 12 dnf modules | Enable PostgreSQL 12 dnf modules | packages_el_install_postgres \| bool; upgrade_from_postgres_10 \| bool; ansible_distribution_major_version == "8" |
| 546 | 21.4% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_from_postgres_10.yml | Install PostgreSQL 12 | Install PostgreSQL 12 | packages_el_install_postgres \| bool; upgrade_from_postgres_10 \| bool; ansible_distribution_major_version == "8" |
| 547 | 21.4% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_from_postgres_10.yml | Upgrade to PostgreSQL 12 | Upgrade to PostgreSQL 12 | packages_el_install_postgres \| bool; upgrade_from_postgres_10 \| bool; ansible_distribution_major_version == "8" |
| 548 | 21.5% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_from_postgres_10.yml | Uninstall postgresql-upgrade package | Uninstall postgresql-upgrade package | packages_el_install_postgres \| bool; upgrade_from_postgres_10 \| bool; ansible_distribution_major_version == "8" |
| 549 | 21.5% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_from_postgres_10.yml | Create file to signify PostgreSQL has been upgraded | Create file to signify PostgreSQL has been upgraded | packages_el_install_postgres \| bool; upgrade_from_postgres_10 \| bool; ansible_distribution_major_version == "8" |
| 550 | 21.6% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml | Upgrade PostgreSQL data from 12 --> 13 | Upgrade PostgreSQL data from 12 --> 13 (file `upgrade_postgres_from_12.yml`) | packages_el_install_postgres \| bool; upgrade_from_postgres_12 \| bool; ansible_distribution_major_version == "8" |
| 551 | 21.6% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_12.yml | file `upgrade_postgres_from_12.yml` | Task file `ansible/automation_platform_installer/roles/packages_el/tasks/upgrade_postgres_from_12.yml` | packages_el_install_postgres \| bool; upgrade_from_postgres_12 \| bool; ansible_distribution_major_version == "8" |
| 552 | 21.6% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_12.yml | Stop PostgreSQL 12 | Stop PostgreSQL 12 (service `{{ pg_systemd_name }}`) | packages_el_install_postgres \| bool; upgrade_from_postgres_12 \| bool; ansible_distribution_major_version == "8" |
| 553 | 21.7% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_12.yml | Reset PostgreSQL module | Reset PostgreSQL module | packages_el_install_postgres \| bool; upgrade_from_postgres_12 \| bool; ansible_distribution_major_version == "8" |
| 554 | 21.7% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_12.yml | Enable PostgreSQL 13 dnf modules | Enable PostgreSQL 13 dnf modules | packages_el_install_postgres \| bool; upgrade_from_postgres_12 \| bool; ansible_distribution_major_version == "8" |
| 555 | 21.8% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_12.yml | Install PostgreSQL 13 | Install PostgreSQL 13 | packages_el_install_postgres \| bool; upgrade_from_postgres_12 \| bool; ansible_distribution_major_version == "8" |
| 556 | 21.8% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_12.yml | Upgrade to PostgreSQL 13 | Upgrade to PostgreSQL 13 | packages_el_install_postgres \| bool; upgrade_from_postgres_12 \| bool; ansible_distribution_major_version == "8" |
| 557 | 21.8% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_12.yml | Uninstall postgresql-upgrade package | Uninstall postgresql-upgrade package | packages_el_install_postgres \| bool; upgrade_from_postgres_12 \| bool; ansible_distribution_major_version == "8" |
| 558 | 21.9% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_12.yml | include_tasks | Include a task file from the current role or playbook (file `install_deps.yml`) | packages_el_install_postgres \| bool; upgrade_from_postgres_12 \| bool; ansible_distribution_major_version == "8" |
| 559 | 21.9% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_12.yml → install_deps.yml | file `install_deps.yml` | Task file `ansible/automation_platform_installer/roles/packages_el/tasks/install_deps.yml` | packages_el_install_postgres \| bool; upgrade_from_postgres_12 \| bool; ansible_distribution_major_version == "8" |
| 560 | 22.0% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_12.yml → install_deps.yml | Install playbook dependencies | Install playbook dependencies | packages_el_install_postgres \| bool; upgrade_from_postgres_12 \| bool; ansible_distribution_major_version == "8" |
| 561 | 22.0% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_12.yml → install_deps.yml | block | Begin conditional or grouped task block | packages_el_install_postgres \| bool; upgrade_from_postgres_12 \| bool; ansible_distribution_major_version == "8"; ansible_distribution_major_version == "8" |
| 562 | 22.0% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_12.yml → install_deps.yml → block | Disable required dnf modules | Disable required dnf modules | packages_el_install_postgres \| bool; upgrade_from_postgres_12 \| bool; ansible_distribution_major_version == "8"; ansible_distribution_major_version == "8" |
| 563 | 22.1% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_12.yml → install_deps.yml → block | Enable required dnf modules | Enable required dnf modules | packages_el_install_postgres \| bool; upgrade_from_postgres_12 \| bool; ansible_distribution_major_version == "8"; ansible_distribution_major_version == "8" |
| 564 | 22.1% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_12.yml | Create file to signify PostgreSQL has been upgraded | Create file to signify PostgreSQL has been upgraded | packages_el_install_postgres \| bool; upgrade_from_postgres_12 \| bool; ansible_distribution_major_version == "8" |
| 565 | 22.2% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml | Upgrade PostgreSQL data from 13 --> 15 | Upgrade PostgreSQL data from 13 --> 15 (file `upgrade_postgres_from_13.yml`) | packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 566 | 22.2% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml | file `upgrade_postgres_from_13.yml` | Task file `ansible/automation_platform_installer/roles/packages_el/tasks/upgrade_postgres_from_13.yml` | packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 567 | 22.2% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml | Stop PostgreSQL 13 | Stop PostgreSQL 13 (service `{{ pg_systemd_name }}`) | packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 568 | 22.3% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml | Reset PostgreSQL module | Reset PostgreSQL module | packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 569 | 22.3% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml | Enable PostgreSQL 15 dnf modules | Enable PostgreSQL 15 dnf modules | packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 570 | 22.3% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml | Install PostgreSQL 15 | Install PostgreSQL 15 | packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 571 | 22.4% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml | Upgrade to PostgreSQL 15 | Upgrade to PostgreSQL 15 | packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 572 | 22.4% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml | Uninstall postgresql-upgrade package | Uninstall postgresql-upgrade package | packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 573 | 22.5% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml | include_tasks | Include a task file from the current role or playbook (file `install_deps.yml`) | packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 574 | 22.5% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml → install_deps.yml | file `install_deps.yml` | Task file `ansible/automation_platform_installer/roles/packages_el/tasks/install_deps.yml` | packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 575 | 22.5% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml → install_deps.yml | Install playbook dependencies | Install playbook dependencies | packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 576 | 22.6% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml → install_deps.yml | block | Begin conditional or grouped task block | packages_el_install_postgres \| bool; upgrade_postgres \| bool; ansible_distribution_major_version == "8" |
| 577 | 22.6% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml → install_deps.yml → block | Disable required dnf modules | Disable required dnf modules | packages_el_install_postgres \| bool; upgrade_postgres \| bool; ansible_distribution_major_version == "8" |
| 578 | 22.7% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml → install_deps.yml → block | Enable required dnf modules | Enable required dnf modules | packages_el_install_postgres \| bool; upgrade_postgres \| bool; ansible_distribution_major_version == "8" |
| 579 | 22.7% | install.yml → Install postgres database node → Install postgres database node → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml | Create file to signify PostgreSQL has been upgraded | Create file to signify PostgreSQL has been upgraded | packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 580 | 22.7% | install.yml → Install postgres database node → Install postgres database node | meta | Ansible meta task (flush handlers, clear facts, etc.) (flush_handlers) | — |
| 581 | 22.8% | install.yml → Install postgres database node → Install postgres database node | include_role | Include and run tasks from an Ansible role (role `postgres`) | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]) |
| 582 | 22.8% | install.yml → Install postgres database node → Install postgres database node → postgres | role `postgres` | Enter role task list | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]) |
| 583 | 22.9% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres | file `main.yml` | Task file `ansible/automation_platform_installer/roles/postgres/tasks/main.yml` | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]) |
| 584 | 22.9% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres | include_tasks | Include a task file from the current role or playbook (file `vars.yml`) | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]) |
| 585 | 22.9% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → vars.yml | file `vars.yml` | Task file `ansible/automation_platform_installer/roles/postgres/tasks/vars.yml` | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]) |
| 586 | 23.0% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → vars.yml | include_vars | Load variables from a file | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]) |
| 587 | 23.0% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres | Ensure all locales in system are in UTF-8 | Ensure all locales in system are in UTF-8 (role `preflight`, tasks `locale.yaml`) | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]) |
| 588 | 23.1% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → preflight | role `preflight` (`locale.yaml`) | Enter role task list | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]) |
| 589 | 23.1% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → preflight → preflight | file `locale.yaml` | Task file `ansible/automation_platform_installer/roles/preflight/tasks/locale.yaml` | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]) |
| 590 | 23.1% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → preflight → preflight | Load /etc/locale.conf | Load /etc/locale.conf | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]) |
| 591 | 23.2% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → preflight → preflight | fail | Fail the play with an error message | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); item is not regex(".*(utf8\|UTF-8)") |
| 592 | 23.2% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → preflight → preflight | Get the LANG environment variable | Get the LANG environment variable | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); item is regex("^LANG=.*") |
| 593 | 23.3% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → preflight → preflight | Check if locale is properly installed | Check if locale is properly installed | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]) |
| 594 | 23.3% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → preflight → preflight | fail | Fail the play with an error message | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); _locale.rc != 0 |
| 595 | 23.3% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres | include_tasks | Include a task file from the current role or playbook (file `preflight.yml`) | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool |
| 596 | 23.4% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → preflight.yml | file `preflight.yml` | Task file `ansible/automation_platform_installer/roles/postgres/tasks/preflight.yml` | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool |
| 597 | 23.4% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → preflight.yml | Preflight check - Ensure that the Postgres data dir is not a mount point | Preflight check - Ensure that the Postgres data dir is not a mount point | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool |
| 598 | 23.4% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → preflight.yml | Fail if the data dir is a mount point | Fail if the data dir is a mount point | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; is_pg_data_mount.rc  \| int == 0 |
| 599 | 23.5% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres | include_tasks | Include a task file from the current role or playbook (file `init.yml`) | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool |
| 600 | 23.5% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → init.yml | file `init.yml` | Task file `ansible/automation_platform_installer/roles/postgres/tasks/init.yml` | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool |
| 601 | 23.6% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → init.yml | init postgresql | init postgresql | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool |
| 602 | 23.6% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → init.yml | Open up postgres port permissions | Open up postgres port permissions | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 603 | 23.6% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres | include_tasks | Include a task file from the current role or playbook (file `conf.yml`) | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool |
| 604 | 23.7% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | file `conf.yml` | Task file `ansible/automation_platform_installer/roles/postgres/tasks/conf.yml` | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool |
| 605 | 23.7% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | Update postgresql authentication settings | Update postgresql authentication settings (dest `{{ pg_hba_location }}`) | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool |
| 606 | 23.8% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | Apply postgresql general settings | Apply postgresql general settings (dest `{{ pg_conf_location }}`) | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool |
| 607 | 23.8% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | Copy server SSL certificate | Copy server SSL certificate (dest `{{ pg_conf_dir }}/server.crt`) | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is defined; postgres_ssl_key is defined |
| 608 | 23.8% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | Copy server SSL private key | Copy server SSL private key (dest `{{ pg_conf_dir }}/server.key`) | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is defined; postgres_ssl_key is defined |
| 609 | 23.9% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | block | Begin conditional or grouped task block | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined |
| 610 | 23.9% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block | Use internal CA for internal database | Use internal CA for internal database (role `certificate_authority`, tasks `sign_service.yml`) | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 611 | 24.0% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority | role `certificate_authority` (`sign_service.yml`) | Enter role task list | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 612 | 24.0% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | file `sign_service.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/sign_service.yml` | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 613 | 24.0% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | include_tasks | Include a task file from the current role or playbook (file `init.yml`) | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 614 | 24.1% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → init.yml | file `init.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/init.yml` | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 615 | 24.1% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → init.yml | Ensure python cryptography libraries are installed | Ensure python cryptography libraries are installed | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 616 | 24.2% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | Create {{ aap_ca_servicename }} private key | Create {{ aap_ca_servicename }} private key | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 617 | 24.2% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | set_fact | Set a host fact used by later tasks | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 618 | 24.2% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | Check if {{ aap_ca_servicename }} public key exists | Check if {{ aap_ca_servicename }} public key exists | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 619 | 24.3% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; certificate_stat.stat.exists \| bool |
| 620 | 24.3% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Check contents of {{ aap_ca_servicename }} public key | Check contents of {{ aap_ca_servicename }} public key | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; certificate_stat.stat.exists \| bool |
| 621 | 24.4% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; certificate_stat.stat.exists \| bool; aap_service_san_records_internal \| union(aap_service_san_records) \| difference(certificate_check.subject_alt_name) \| length > 0 |
| 622 | 24.4% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 623 | 24.4% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false); aap_service_is_client |
| 624 | 24.5% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Create CSR for {{ aap_ca_servicename }} node/s | Create CSR for {{ aap_ca_servicename }} node/s | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 625 | 24.5% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Sign {{ aap_ca_servicename }} public key | Sign {{ aap_ca_servicename }} public key | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 626 | 24.5% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Write public key to {{ aap_ca_servicename }} node/s | Write public key to {{ aap_ca_servicename }} node/s (dest `{{ aap_service_cert_fullpath }}`) | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 627 | 24.6% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | Signal restart if certificate pair changes | Signal restart if certificate pair changes | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; (write_key is changed or write_certificate is changed) |
| 628 | 24.6% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block | Use internal CA for external database | Use internal CA for external database (role `certificate_authority`, tasks `sign_service.yml`) | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 629 | 24.7% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority | role `certificate_authority` (`sign_service.yml`) | Enter role task list | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 630 | 24.7% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | file `sign_service.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/sign_service.yml` | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 631 | 24.7% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | include_tasks | Include a task file from the current role or playbook (file `init.yml`) | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 632 | 24.8% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → init.yml | file `init.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/init.yml` | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 633 | 24.8% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → init.yml | Ensure python cryptography libraries are installed | Ensure python cryptography libraries are installed | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 634 | 24.9% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | Create {{ aap_ca_servicename }} private key | Create {{ aap_ca_servicename }} private key | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 635 | 24.9% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | set_fact | Set a host fact used by later tasks | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 636 | 24.9% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | Check if {{ aap_ca_servicename }} public key exists | Check if {{ aap_ca_servicename }} public key exists | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 637 | 25.0% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; certificate_stat.stat.exists \| bool |
| 638 | 25.0% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Check contents of {{ aap_ca_servicename }} public key | Check contents of {{ aap_ca_servicename }} public key | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; certificate_stat.stat.exists \| bool |
| 639 | 25.1% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; certificate_stat.stat.exists \| bool; aap_service_san_records_internal \| union(aap_service_san_records) \| difference(certificate_check.subject_alt_name) \| length > 0 |
| 640 | 25.1% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 641 | 25.1% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false); aap_service_is_client |
| 642 | 25.2% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Create CSR for {{ aap_ca_servicename }} node/s | Create CSR for {{ aap_ca_servicename }} node/s | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 643 | 25.2% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Sign {{ aap_ca_servicename }} public key | Sign {{ aap_ca_servicename }} public key | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 644 | 25.3% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Write public key to {{ aap_ca_servicename }} node/s | Write public key to {{ aap_ca_servicename }} node/s (dest `{{ aap_service_cert_fullpath }}`) | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 645 | 25.3% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | Signal restart if certificate pair changes | Signal restart if certificate pair changes | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; (write_key is changed or write_certificate is changed) |
| 646 | 25.3% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | Create unix socket directory | Create unix socket directory | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool |
| 647 | 25.4% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | Configure postgresql tmpfiles.d | Configure postgresql tmpfiles.d (dest `/usr/lib/tmpfiles.d/postgresql.conf`) | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool |
| 648 | 25.4% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | set_fact | Set a host fact used by later tasks | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; aap_service_restart \| default(false) or pg_hba_conf is changed or pg_general_conf is changed |
| 649 | 25.5% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | ensure postgresql is running and restart when authentication settings change | ensure postgresql is running and restart when authentication settings change (service `{{ postgres_init_name }}`) | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool |
| 650 | 25.5% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | wait for postgresql restart | wait for postgresql restart | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; postgresql_restart is changed |
| 651 | 25.5% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | configure postgresql to start automatically | configure postgresql to start automatically (service `{{ postgres_init_name }}`) | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool |
| 652 | 25.6% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | block | Begin conditional or grouped task block | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; groups['automationcontroller'] \| default([]) \| length |
| 653 | 25.6% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block | create the awx postgres user | create the awx postgres user | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; groups['automationcontroller'] \| default([]) \| length; postgres_username is defined and postgres_username != ''; postgres_password is defined and postgres_password != '' |
| 654 | 25.6% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block | create the postgresql database for awx | create the postgresql database for awx | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; groups['automationcontroller'] \| default([]) \| length; postgres_username is defined and postgres_username != ''; postgres_database is defined and postgres_database != '' |
| 655 | 25.7% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | block | Begin conditional or grouped task block | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; groups['automationhub'] \| default([]) \| length |
| 656 | 25.7% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block | create the automationhub postgres user | create the automationhub postgres user | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; groups['automationhub'] \| default([]) \| length; automationhub_postgres_username is defined and automationhub_postgres_username != ''; automationhub_postgres_password is defined and automationhub_postgres_password != '' |
| 657 | 25.8% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block | create the postgresql database for automationhub | create the postgresql database for automationhub | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; groups['automationhub'] \| default([]) \| length; automationhub_postgres_username is defined and automationhub_postgres_username != ''; automationhub_postgres_database is defined and automationhub_postgres_database != '' |
| 658 | 25.8% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block | enable the postgresql hstore extension | enable the postgresql hstore extension | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; groups['automationhub'] \| default([]) \| length |
| 659 | 25.8% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | block | Begin conditional or grouped task block | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; groups['automationedacontroller'] \| default([]) \| length |
| 660 | 25.9% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block | create the automationedacontroller postgres user | create the automationedacontroller postgres user | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; groups['automationedacontroller'] \| default([]) \| length; automationedacontroller_postgres_username is defined and automationedacontroller_postgres_username != ''; automationedacontroller_postgres_password is defined and automationedacontroller_postgres_password != '' |
| 661 | 25.9% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block | create the postgresql database for automationedacontroller | create the postgresql database for automationedacontroller | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; groups['automationedacontroller'] \| default([]) \| length; automationedacontroller_postgres_username is defined and automationedacontroller_postgres_username != ''; automationedacontroller_postgres_database is defined and automationedacontroller_postgres_database != '' |
| 662 | 26.0% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | block | Begin conditional or grouped task block | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; groups['automationgateway'] \| default([]) \| length |
| 663 | 26.0% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block | create the automationgateway postgres user | create the automationgateway postgres user | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; groups['automationgateway'] \| default([]) \| length; automationgateway_postgres_username is defined and automationgateway_postgres_username != ''; automationgateway_postgres_password is defined and automationgateway_postgres_password != '' |
| 664 | 26.0% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block | create the postgresql database for automationgateway | create the postgresql database for automationgateway | (config_dynamic_database == 'external') or (automationhub_config_dynamic_database == 'external') or (automationedacontroller_config_dynamic_database == 'external') or (automationgateway_config_dynamic_database == 'external'); inventory_hostname in groups['database'] \| default([]); not postgres_exec_vars_only \| bool; groups['automationgateway'] \| default([]) \| length; automationgateway_postgres_username is defined and automationgateway_postgres_username != ''; automationgateway_postgres_database is defined and automationgateway_postgres_database != '' |
| 665 | 26.1% | install.yml → Install postgres database node → Install postgres database node | include_role | Include and run tasks from an Ansible role (role `postgres`) | _do_run_db_install \| default(false) \| bool |
| 666 | 26.1% | install.yml → Install postgres database node → Install postgres database node → postgres | role `postgres` | Enter role task list | _do_run_db_install \| default(false) \| bool |
| 667 | 26.2% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres | file `main.yml` | Task file `ansible/automation_platform_installer/roles/postgres/tasks/main.yml` | _do_run_db_install \| default(false) \| bool |
| 668 | 26.2% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres | include_tasks | Include a task file from the current role or playbook (file `vars.yml`) | _do_run_db_install \| default(false) \| bool |
| 669 | 26.2% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → vars.yml | file `vars.yml` | Task file `ansible/automation_platform_installer/roles/postgres/tasks/vars.yml` | _do_run_db_install \| default(false) \| bool |
| 670 | 26.3% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → vars.yml | include_vars | Load variables from a file | _do_run_db_install \| default(false) \| bool |
| 671 | 26.3% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres | Ensure all locales in system are in UTF-8 | Ensure all locales in system are in UTF-8 (role `preflight`, tasks `locale.yaml`) | _do_run_db_install \| default(false) \| bool |
| 672 | 26.4% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → preflight | role `preflight` (`locale.yaml`) | Enter role task list | _do_run_db_install \| default(false) \| bool |
| 673 | 26.4% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → preflight → preflight | file `locale.yaml` | Task file `ansible/automation_platform_installer/roles/preflight/tasks/locale.yaml` | _do_run_db_install \| default(false) \| bool |
| 674 | 26.4% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → preflight → preflight | Load /etc/locale.conf | Load /etc/locale.conf | _do_run_db_install \| default(false) \| bool |
| 675 | 26.5% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → preflight → preflight | fail | Fail the play with an error message | _do_run_db_install \| default(false) \| bool; item is not regex(".*(utf8\|UTF-8)") |
| 676 | 26.5% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → preflight → preflight | Get the LANG environment variable | Get the LANG environment variable | _do_run_db_install \| default(false) \| bool; item is regex("^LANG=.*") |
| 677 | 26.6% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → preflight → preflight | Check if locale is properly installed | Check if locale is properly installed | _do_run_db_install \| default(false) \| bool |
| 678 | 26.6% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → preflight → preflight | fail | Fail the play with an error message | _do_run_db_install \| default(false) \| bool; _locale.rc != 0 |
| 679 | 26.6% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres | include_tasks | Include a task file from the current role or playbook (file `preflight.yml`) | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool |
| 680 | 26.7% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → preflight.yml | file `preflight.yml` | Task file `ansible/automation_platform_installer/roles/postgres/tasks/preflight.yml` | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool |
| 681 | 26.7% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → preflight.yml | Preflight check - Ensure that the Postgres data dir is not a mount point | Preflight check - Ensure that the Postgres data dir is not a mount point | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool |
| 682 | 26.7% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → preflight.yml | Fail if the data dir is a mount point | Fail if the data dir is a mount point | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; is_pg_data_mount.rc  \| int == 0 |
| 683 | 26.8% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres | include_tasks | Include a task file from the current role or playbook (file `init.yml`) | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool |
| 684 | 26.8% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → init.yml | file `init.yml` | Task file `ansible/automation_platform_installer/roles/postgres/tasks/init.yml` | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool |
| 685 | 26.9% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → init.yml | init postgresql | init postgresql | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool |
| 686 | 26.9% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → init.yml | Open up postgres port permissions | Open up postgres port permissions | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 687 | 26.9% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres | include_tasks | Include a task file from the current role or playbook (file `conf.yml`) | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool |
| 688 | 27.0% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | file `conf.yml` | Task file `ansible/automation_platform_installer/roles/postgres/tasks/conf.yml` | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool |
| 689 | 27.0% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | Update postgresql authentication settings | Update postgresql authentication settings (dest `{{ pg_hba_location }}`) | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool |
| 690 | 27.1% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | Apply postgresql general settings | Apply postgresql general settings (dest `{{ pg_conf_location }}`) | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool |
| 691 | 27.1% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | Copy server SSL certificate | Copy server SSL certificate (dest `{{ pg_conf_dir }}/server.crt`) | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is defined; postgres_ssl_key is defined |
| 692 | 27.1% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | Copy server SSL private key | Copy server SSL private key (dest `{{ pg_conf_dir }}/server.key`) | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is defined; postgres_ssl_key is defined |
| 693 | 27.2% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | block | Begin conditional or grouped task block | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined |
| 694 | 27.2% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block | Use internal CA for internal database | Use internal CA for internal database (role `certificate_authority`, tasks `sign_service.yml`) | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 695 | 27.3% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority | role `certificate_authority` (`sign_service.yml`) | Enter role task list | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 696 | 27.3% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | file `sign_service.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/sign_service.yml` | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 697 | 27.3% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | include_tasks | Include a task file from the current role or playbook (file `init.yml`) | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 698 | 27.4% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → init.yml | file `init.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/init.yml` | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 699 | 27.4% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → init.yml | Ensure python cryptography libraries are installed | Ensure python cryptography libraries are installed | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 700 | 27.5% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | Create {{ aap_ca_servicename }} private key | Create {{ aap_ca_servicename }} private key | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 701 | 27.5% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | set_fact | Set a host fact used by later tasks | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 702 | 27.5% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | Check if {{ aap_ca_servicename }} public key exists | Check if {{ aap_ca_servicename }} public key exists | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal' |
| 703 | 27.6% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; certificate_stat.stat.exists \| bool |
| 704 | 27.6% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Check contents of {{ aap_ca_servicename }} public key | Check contents of {{ aap_ca_servicename }} public key | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; certificate_stat.stat.exists \| bool |
| 705 | 27.7% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; certificate_stat.stat.exists \| bool; aap_service_san_records_internal \| union(aap_service_san_records) \| difference(certificate_check.subject_alt_name) \| length > 0 |
| 706 | 27.7% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 707 | 27.7% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false); aap_service_is_client |
| 708 | 27.8% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Create CSR for {{ aap_ca_servicename }} node/s | Create CSR for {{ aap_ca_servicename }} node/s | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 709 | 27.8% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Sign {{ aap_ca_servicename }} public key | Sign {{ aap_ca_servicename }} public key | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 710 | 27.8% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Write public key to {{ aap_ca_servicename }} node/s | Write public key to {{ aap_ca_servicename }} node/s (dest `{{ aap_service_cert_fullpath }}`) | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 711 | 27.9% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | Signal restart if certificate pair changes | Signal restart if certificate pair changes | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'internal'; (write_key is changed or write_certificate is changed) |
| 712 | 27.9% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block | Use internal CA for external database | Use internal CA for external database (role `certificate_authority`, tasks `sign_service.yml`) | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 713 | 28.0% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority | role `certificate_authority` (`sign_service.yml`) | Enter role task list | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 714 | 28.0% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | file `sign_service.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/sign_service.yml` | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 715 | 28.0% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | include_tasks | Include a task file from the current role or playbook (file `init.yml`) | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 716 | 28.1% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → init.yml | file `init.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/init.yml` | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 717 | 28.1% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → init.yml | Ensure python cryptography libraries are installed | Ensure python cryptography libraries are installed | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 718 | 28.2% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | Create {{ aap_ca_servicename }} private key | Create {{ aap_ca_servicename }} private key | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 719 | 28.2% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | set_fact | Set a host fact used by later tasks | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 720 | 28.2% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | Check if {{ aap_ca_servicename }} public key exists | Check if {{ aap_ca_servicename }} public key exists | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external' |
| 721 | 28.3% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; certificate_stat.stat.exists \| bool |
| 722 | 28.3% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Check contents of {{ aap_ca_servicename }} public key | Check contents of {{ aap_ca_servicename }} public key | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; certificate_stat.stat.exists \| bool |
| 723 | 28.4% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; certificate_stat.stat.exists \| bool; aap_service_san_records_internal \| union(aap_service_san_records) \| difference(certificate_check.subject_alt_name) \| length > 0 |
| 724 | 28.4% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 725 | 28.4% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false); aap_service_is_client |
| 726 | 28.5% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Create CSR for {{ aap_ca_servicename }} node/s | Create CSR for {{ aap_ca_servicename }} node/s | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 727 | 28.5% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Sign {{ aap_ca_servicename }} public key | Sign {{ aap_ca_servicename }} public key | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 728 | 28.6% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority → block | Write public key to {{ aap_ca_servicename }} node/s | Write public key to {{ aap_ca_servicename }} node/s (dest `{{ aap_service_cert_fullpath }}`) | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 729 | 28.6% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block → certificate_authority → certificate_authority | Signal restart if certificate pair changes | Signal restart if certificate pair changes | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; _postgres_use_ssl \| bool; postgres_ssl_cert is undefined; postgres_ssl_key is undefined; config_dynamic_database == 'external'; (write_key is changed or write_certificate is changed) |
| 730 | 28.6% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | Create unix socket directory | Create unix socket directory | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool |
| 731 | 28.7% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | Configure postgresql tmpfiles.d | Configure postgresql tmpfiles.d (dest `/usr/lib/tmpfiles.d/postgresql.conf`) | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool |
| 732 | 28.7% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | set_fact | Set a host fact used by later tasks | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; aap_service_restart \| default(false) or pg_hba_conf is changed or pg_general_conf is changed |
| 733 | 28.8% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | ensure postgresql is running and restart when authentication settings change | ensure postgresql is running and restart when authentication settings change (service `{{ postgres_init_name }}`) | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool |
| 734 | 28.8% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | wait for postgresql restart | wait for postgresql restart | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; postgresql_restart is changed |
| 735 | 28.8% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | configure postgresql to start automatically | configure postgresql to start automatically (service `{{ postgres_init_name }}`) | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool |
| 736 | 28.9% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | block | Begin conditional or grouped task block | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; groups['automationcontroller'] \| default([]) \| length |
| 737 | 28.9% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block | create the awx postgres user | create the awx postgres user | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; groups['automationcontroller'] \| default([]) \| length; postgres_username is defined and postgres_username != ''; postgres_password is defined and postgres_password != '' |
| 738 | 28.9% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block | create the postgresql database for awx | create the postgresql database for awx | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; groups['automationcontroller'] \| default([]) \| length; postgres_username is defined and postgres_username != ''; postgres_database is defined and postgres_database != '' |
| 739 | 29.0% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | block | Begin conditional or grouped task block | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; groups['automationhub'] \| default([]) \| length |
| 740 | 29.0% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block | create the automationhub postgres user | create the automationhub postgres user | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; groups['automationhub'] \| default([]) \| length; automationhub_postgres_username is defined and automationhub_postgres_username != ''; automationhub_postgres_password is defined and automationhub_postgres_password != '' |
| 741 | 29.1% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block | create the postgresql database for automationhub | create the postgresql database for automationhub | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; groups['automationhub'] \| default([]) \| length; automationhub_postgres_username is defined and automationhub_postgres_username != ''; automationhub_postgres_database is defined and automationhub_postgres_database != '' |
| 742 | 29.1% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block | enable the postgresql hstore extension | enable the postgresql hstore extension | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; groups['automationhub'] \| default([]) \| length |
| 743 | 29.1% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | block | Begin conditional or grouped task block | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; groups['automationedacontroller'] \| default([]) \| length |
| 744 | 29.2% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block | create the automationedacontroller postgres user | create the automationedacontroller postgres user | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; groups['automationedacontroller'] \| default([]) \| length; automationedacontroller_postgres_username is defined and automationedacontroller_postgres_username != ''; automationedacontroller_postgres_password is defined and automationedacontroller_postgres_password != '' |
| 745 | 29.2% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block | create the postgresql database for automationedacontroller | create the postgresql database for automationedacontroller | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; groups['automationedacontroller'] \| default([]) \| length; automationedacontroller_postgres_username is defined and automationedacontroller_postgres_username != ''; automationedacontroller_postgres_database is defined and automationedacontroller_postgres_database != '' |
| 746 | 29.3% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml | block | Begin conditional or grouped task block | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; groups['automationgateway'] \| default([]) \| length |
| 747 | 29.3% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block | create the automationgateway postgres user | create the automationgateway postgres user | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; groups['automationgateway'] \| default([]) \| length; automationgateway_postgres_username is defined and automationgateway_postgres_username != ''; automationgateway_postgres_password is defined and automationgateway_postgres_password != '' |
| 748 | 29.3% | install.yml → Install postgres database node → Install postgres database node → postgres → postgres → conf.yml → block | create the postgresql database for automationgateway | create the postgresql database for automationgateway | _do_run_db_install \| default(false) \| bool; not postgres_exec_vars_only \| bool; groups['automationgateway'] \| default([]) \| length; automationgateway_postgres_username is defined and automationgateway_postgres_username != ''; automationgateway_postgres_database is defined and automationgateway_postgres_database != '' |
| 749 | 29.4% | install.yml → Install postgres database node → Install postgres database node | include_role | Include and run tasks from an Ansible role (role `firewall`) | — |
| 750 | 29.4% | install.yml → Install postgres database node → Install postgres database node → firewall | role `firewall` | Enter role task list | — |
| 751 | 29.5% | install.yml → Install postgres database node → Install postgres database node → firewall → firewall | file `main.yml` | Task file `ansible/automation_platform_installer/roles/firewall/tasks/main.yml` | — |
| 752 | 29.5% | install.yml → Install postgres database node → Install postgres database node → firewall → firewall | include_tasks | Include a task file from the current role or playbook (file `discover.yml`) | — |
| 753 | 29.5% | install.yml → Install postgres database node → Install postgres database node → firewall → firewall → discover.yml | file `discover.yml` | Task file `ansible/automation_platform_installer/roles/firewall/tasks/discover.yml` | — |
| 754 | 29.6% | install.yml → Install postgres database node → Install postgres database node → firewall → firewall → discover.yml | Determine if firewalld is running | Determine if firewalld is running | — |
| 755 | 29.6% | install.yml → Install postgres database node → Install postgres database node → firewall → firewall → discover.yml | Determine if firewalld is enabled | Determine if firewalld is enabled | — |
| 756 | 29.7% | install.yml → Install postgres database node → Install postgres database node → firewall → firewall → discover.yml | Determine if iptables is installed | Determine if iptables is installed | — |
| 757 | 29.7% | install.yml → Install postgres database node → Install postgres database node → firewall → firewall → discover.yml | Determine if iptables service is installed | Determine if iptables service is installed | — |
| 758 | 29.7% | install.yml → Install postgres database node → Install postgres database node → firewall → firewall → discover.yml | Check to see if there is an iptables config file | Check to see if there is an iptables config file | iptables_installed.rc == 0 |
| 759 | 29.8% | install.yml → Install postgres database node → Install postgres database node → firewall → firewall → discover.yml | Select firewalld | Select firewalld | firewalld_running.rc == 0 or systemctl_unit_files.stdout_lines \| select('match','firewalld.service\s+enabled') \| list \| length == 1 |
| 760 | 29.8% | install.yml → Install postgres database node → Install postgres database node → firewall → firewall → discover.yml | Select iptables | Select iptables | iptables_installed.rc == 0; iptables_exists.stat.exists; iptables_service.stat.exists |
| 761 | 29.9% | install.yml → Install postgres database node → Install postgres database node → firewall → firewall | Include {{ 'no' if component_firewall_type == 'none' else component_firewall_type }} tasks | Include {{ 'no' if component_firewall_type == 'none' else component_firewall_type }} tasks (file `{{ component_firewall_type }}.yml`) | firewalld_running.rc == 0 or systemctl_unit_files.stdout_lines \| select('match','firewalld.service\s+enabled') \| list \| length == 1 or ( iptables_installed.rc == 0 and iptables_exists.stat.exists and iptables_service.stat.exists ) |
| 762 | 29.9% | install.yml → Install postgres database node → Install postgres database node → firewall → firewall → {{ component_firewall_type }}.yml | {{ component_firewall_type }}.yml | Include task file `{{ component_firewall_type }}.yml` (relative to current role task directory) | firewalld_running.rc == 0 or systemctl_unit_files.stdout_lines \| select('match','firewalld.service\s+enabled') \| list \| length == 1 or ( iptables_installed.rc == 0 and iptables_exists.stat.exists and iptables_service.stat.exists ) |
| 763 | 29.9% | install.yml → Install postgres database node | Install postgres database node (always) | Run always tasks for block | — |
| 764 | 30.0% | install.yml → Install postgres database node → Install postgres database node (always) | Run repo_management (teardown) | Run repo_management (teardown) (role `repo_management`, tasks `teardown`) | — |
| 765 | 30.0% | install.yml → Install postgres database node → Install postgres database node (always) → repo_management | role `repo_management` (`teardown`) | Enter role task list | — |
| 766 | 30.0% | install.yml → Install postgres database node → Install postgres database node (always) → repo_management → repo_management | file `teardown.yml` | Task file `ansible/automation_platform_installer/roles/repo_management/tasks/teardown.yml` | — |
| 767 | 30.1% | install.yml → Install postgres database node → Install postgres database node (always) → repo_management → repo_management | Disable Automation Platform repository | Disable Automation Platform repository | extra_repos is defined |
| 768 | 30.1% | install.yml → Install postgres database node → Install postgres database node (always) → repo_management → repo_management | Disable Automation Platform RHSM repository | Disable Automation Platform RHSM repository | aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 769 | 30.2% | install.yml | Prepare nodes for redis installation | Play targeting `redis` | — |
| 770 | 30.2% | install.yml → Prepare nodes for redis installation | ansible.builtin.include_role | Include and run tasks from an Ansible role (role `redis`, tasks `deprovision`) | _redis_mode == "cluster"; inventory_hostname in groups.get("automationgateway", []) \| union(groups.get("automationedacontroller", [])); not ansible_facts.services.get('redis-platform.service', {}) |
| 771 | 30.2% | install.yml → Prepare nodes for redis installation → redis | role `redis` (`deprovision`) | Enter role task list | _redis_mode == "cluster"; inventory_hostname in groups.get("automationgateway", []) \| union(groups.get("automationedacontroller", [])); not ansible_facts.services.get('redis-platform.service', {}) |
| 772 | 30.3% | install.yml → Prepare nodes for redis installation → redis → redis | file `deprovision.yml` | Task file `ansible/automation_platform_installer/roles/redis/tasks/deprovision.yml` | _redis_mode == "cluster"; inventory_hostname in groups.get("automationgateway", []) \| union(groups.get("automationedacontroller", [])); not ansible_facts.services.get('redis-platform.service', {}) |
| 773 | 30.3% | install.yml → Prepare nodes for redis installation → redis → redis | Remove redis rpm | Remove redis rpm | _redis_mode == "cluster"; inventory_hostname in groups.get("automationgateway", []) \| union(groups.get("automationedacontroller", [])); not ansible_facts.services.get('redis-platform.service', {}) |
| 774 | 30.4% | install.yml → Prepare nodes for redis installation → redis → redis | Remove remaining redis files and directories | Remove remaining redis files and directories | _redis_mode == "cluster"; inventory_hostname in groups.get("automationgateway", []) \| union(groups.get("automationedacontroller", [])); not ansible_facts.services.get('redis-platform.service', {}) |
| 775 | 30.4% | install.yml | Install cluster redis | Play targeting `redis` | — |
| 776 | 30.4% | install.yml → Install cluster redis | block | Begin conditional or grouped task block | _redis_mode == "cluster" |
| 777 | 30.5% | install.yml → Install cluster redis → block | Run repo_management (setup) | Run repo_management (setup) (role `repo_management`, tasks `setup`) | _redis_mode == "cluster" |
| 778 | 30.5% | install.yml → Install cluster redis → block → repo_management | role `repo_management` (`setup`) | Enter role task list | _redis_mode == "cluster" |
| 779 | 30.6% | install.yml → Install cluster redis → block → repo_management → repo_management | file `setup.yml` | Task file `ansible/automation_platform_installer/roles/repo_management/tasks/setup.yml` | _redis_mode == "cluster" |
| 780 | 30.6% | install.yml → Install cluster redis → block → repo_management → repo_management | include_vars | Load variables from a file | _redis_mode == "cluster" |
| 781 | 30.6% | install.yml → Install cluster redis → block → repo_management → repo_management | Install yum repo keys | Install yum repo keys (dest `/etc/pki/rpm-gpg/{{ item }}`) | _redis_mode == "cluster" |
| 782 | 30.7% | install.yml → Install cluster redis → block → repo_management → repo_management | Install subscription-manager-rhsm-certificates | Install subscription-manager-rhsm-certificates | _redis_mode == "cluster" |
| 783 | 30.7% | install.yml → Install cluster redis → block → repo_management → repo_management | Determine if rhsm repository exists | Determine if rhsm repository exists | _redis_mode == "cluster" |
| 784 | 30.8% | install.yml → Install cluster redis → block → repo_management → repo_management | Enable Automation Platform rhsm repository | Enable Automation Platform rhsm repository | _redis_mode == "cluster"; ansible_distribution == 'RedHat'; rhsm_repo.stat.exists; not bundle_install \| bool; aw_repo_url is undefined |
| 785 | 30.8% | install.yml → Install cluster redis → block → repo_management → repo_management | Explain Automation Platform rhsm repository failure | Explain Automation Platform rhsm repository failure | _redis_mode == "cluster"; aap_rhsm_repo_enable.failed \| default(false) |
| 786 | 30.8% | install.yml → Install cluster redis → block → repo_management → repo_management | block | Begin conditional or grouped task block | _redis_mode == "cluster"; aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 787 | 30.9% | install.yml → Install cluster redis → block → repo_management → repo_management → block | Enable module hotfixes flag | Enable module hotfixes flag | _redis_mode == "cluster"; aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 788 | 30.9% | install.yml → Install cluster redis → block → repo_management → repo_management → block | Set AAP repo for update | Set AAP repo for update | _redis_mode == "cluster"; aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 789 | 31.0% | install.yml → Install cluster redis → block → repo_management → repo_management | Ensure rsync is installed | Ensure rsync is installed | _redis_mode == "cluster"; bundle_install \| bool |
| 790 | 31.0% | install.yml → Install cluster redis → block → repo_management → repo_management | Copy bundle packages to repo source directory | Begin conditional or grouped task block | _redis_mode == "cluster"; bundle_install \| bool |
| 791 | 31.0% | install.yml → Install cluster redis → block → repo_management → repo_management → Copy bundle packages to repo source directory | Copy bundle packages to repo source directory | Copy bundle packages to repo source directory | _redis_mode == "cluster"; bundle_install \| bool |
| 792 | 31.1% | install.yml → Install cluster redis → block → repo_management → repo_management | Copy bundle packages to repo source directory (rescue) | Run rescue tasks for block | _redis_mode == "cluster"; bundle_install \| bool |
| 793 | 31.1% | install.yml → Install cluster redis → block → repo_management → repo_management → Copy bundle packages to repo source directory (rescue) | Remove old bundle packages source directory (legacy) | Remove old bundle packages source directory (legacy) | _redis_mode == "cluster"; bundle_install \| bool |
| 794 | 31.1% | install.yml → Install cluster redis → block → repo_management → repo_management → Copy bundle packages to repo source directory (rescue) | Copy bundle packages to repo source directory (legacy) | Copy bundle packages to repo source directory (legacy) (dest `{{ bundle_install_folder }}`) | _redis_mode == "cluster"; bundle_install \| bool |
| 795 | 31.2% | install.yml → Install cluster redis → block → repo_management → repo_management | Install the Automation Platform bundle repository | Install the Automation Platform bundle repository (dest `/etc/yum.repos.d/ansible-automation-platform.repo`) | _redis_mode == "cluster"; bundle_install \| bool |
| 796 | 31.2% | install.yml → Install cluster redis → block → repo_management → repo_management | Remove legacy ansible-tower repository | Remove legacy ansible-tower repository | _redis_mode == "cluster" |
| 797 | 31.3% | install.yml → Install cluster redis → block → repo_management → repo_management | Install the Automation Platform yum repository. | Install the Automation Platform yum repository. (dest `/etc/yum.repos.d/ansible-automation-platform.repo`) | _redis_mode == "cluster"; aw_repo_url is defined; not bundle_install \| bool |
| 798 | 31.3% | install.yml → Install cluster redis → block → repo_management → repo_management | Set repo list for bundle install | Set repo list for bundle install | _redis_mode == "cluster"; bundle_install \| bool |
| 799 | 31.3% | install.yml → Install cluster redis → block → repo_management → repo_management | Set repo list for online install | Set repo list for online install | _redis_mode == "cluster"; yum_repo is succeeded; yum_repo is not skipped |
| 800 | 31.4% | install.yml → Install cluster redis → block → repo_management → repo_management | Enable Ansible Automation Platform repo | Enable Ansible Automation Platform repo | _redis_mode == "cluster"; extra_repos is defined |
| 801 | 31.4% | install.yml → Install cluster redis → block | include_role | Include and run tasks from an Ansible role (role `redis`) | _redis_mode == "cluster" |
| 802 | 31.5% | install.yml → Install cluster redis → block → redis | role `redis` | Enter role task list | _redis_mode == "cluster" |
| 803 | 31.5% | install.yml → Install cluster redis → block → redis → redis | file `main.yml` | Task file `ansible/automation_platform_installer/roles/redis/tasks/main.yml` | _redis_mode == "cluster" |
| 804 | 31.5% | install.yml → Install cluster redis → block → redis → redis | include_tasks | Include a task file from the current role or playbook (file `vars.yml`) | _redis_mode == "cluster" |
| 805 | 31.6% | install.yml → Install cluster redis → block → redis → redis → vars.yml | file `vars.yml` | Task file `ansible/automation_platform_installer/roles/redis/tasks/vars.yml` | _redis_mode == "cluster" |
| 806 | 31.6% | install.yml → Install cluster redis → block → redis → redis → vars.yml | include_vars | Load variables from a file | _redis_mode == "cluster" |
| 807 | 31.7% | install.yml → Install cluster redis → block → redis → redis → vars.yml | include_vars | Load variables from a file | _redis_mode == "cluster"; install_redis_mode == "cluster" |
| 808 | 31.7% | install.yml → Install cluster redis → block → redis → redis | include_tasks | Include a task file from the current role or playbook (file `module.yml`) | _redis_mode == "cluster" |
| 809 | 31.7% | install.yml → Install cluster redis → block → redis → redis → module.yml | file `module.yml` | Task file `ansible/automation_platform_installer/roles/redis/tasks/module.yml` | _redis_mode == "cluster" |
| 810 | 31.8% | install.yml → Install cluster redis → block → redis → redis → module.yml | Enable redis module stream on RHEL 8 | Begin conditional or grouped task block | _redis_mode == "cluster"; ansible_distribution_major_version \| int == 8 |
| 811 | 31.8% | install.yml → Install cluster redis → block → redis → redis → module.yml → Enable redis module stream on RHEL 8 | Disable Redis module | Disable Redis module | _redis_mode == "cluster"; ansible_distribution_major_version \| int == 8 |
| 812 | 31.9% | install.yml → Install cluster redis → block → redis → redis → module.yml → Enable redis module stream on RHEL 8 | Enable Redis module | Enable Redis module | _redis_mode == "cluster"; ansible_distribution_major_version \| int == 8 |
| 813 | 31.9% | install.yml → Install cluster redis → block → redis → redis | include_tasks | Include a task file from the current role or playbook (file `tasks.yml`) | _redis_mode == "cluster" |
| 814 | 31.9% | install.yml → Install cluster redis → block → redis → redis → tasks.yml | file `tasks.yml` | Task file `ansible/automation_platform_installer/roles/redis/tasks/tasks.yml` | _redis_mode == "cluster" |
| 815 | 32.0% | install.yml → Install cluster redis → block → redis → redis → tasks.yml | Ensure redis package is installed | Ensure redis package is installed | _redis_mode == "cluster" |
| 816 | 32.0% | install.yml → Install cluster redis → block → redis → redis → tasks.yml | Ensure AAP directory exists | Ensure AAP directory exists | _redis_mode == "cluster" |
| 817 | 32.1% | install.yml → Install cluster redis → block → redis → redis → tasks.yml | Ensure redis cert directory exists | Ensure redis cert directory exists | _redis_mode == "cluster" |
| 818 | 32.1% | install.yml → Install cluster redis → block → redis → redis → tasks.yml | Configure TLS certificates for redis | Begin conditional or grouped task block | _redis_mode == "cluster"; not _redis_disable_tls \| bool |
| 819 | 32.1% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → Configure TLS certificates for redis | Copy user provided SSL cert | Begin conditional or grouped task block | _redis_mode == "cluster"; not _redis_disable_tls \| bool; redis_tls_cert \| length |
| 820 | 32.2% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → Configure TLS certificates for redis → Copy user provided SSL cert | Import specified TLS certificate | Import specified TLS certificate | _redis_mode == "cluster"; not _redis_disable_tls \| bool; redis_tls_cert \| length |
| 821 | 32.2% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → Configure TLS certificates for redis → Copy user provided SSL cert | Import specified TLS private key | Import specified TLS private key | _redis_mode == "cluster"; not _redis_disable_tls \| bool; redis_tls_cert \| length |
| 822 | 32.2% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → Configure TLS certificates for redis | Use internal CA | Use internal CA (role `certificate_authority`, tasks `sign_service.yml`) | _redis_mode == "cluster"; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 823 | 32.3% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority | role `certificate_authority` (`sign_service.yml`) | Enter role task list | _redis_mode == "cluster"; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 824 | 32.3% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | file `sign_service.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/sign_service.yml` | _redis_mode == "cluster"; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 825 | 32.4% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | include_tasks | Include a task file from the current role or playbook (file `init.yml`) | _redis_mode == "cluster"; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 826 | 32.4% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → init.yml | file `init.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/init.yml` | _redis_mode == "cluster"; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 827 | 32.4% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → init.yml | Ensure python cryptography libraries are installed | Ensure python cryptography libraries are installed | _redis_mode == "cluster"; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 828 | 32.5% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | Create {{ aap_ca_servicename }} private key | Create {{ aap_ca_servicename }} private key | _redis_mode == "cluster"; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 829 | 32.5% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | set_fact | Set a host fact used by later tasks | _redis_mode == "cluster"; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 830 | 32.6% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | Check if {{ aap_ca_servicename }} public key exists | Check if {{ aap_ca_servicename }} public key exists | _redis_mode == "cluster"; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 831 | 32.6% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | _redis_mode == "cluster"; not _redis_disable_tls \| bool; not redis_tls_cert \| length; certificate_stat.stat.exists \| bool |
| 832 | 32.6% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | Check contents of {{ aap_ca_servicename }} public key | Check contents of {{ aap_ca_servicename }} public key | _redis_mode == "cluster"; not _redis_disable_tls \| bool; not redis_tls_cert \| length; certificate_stat.stat.exists \| bool |
| 833 | 32.7% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | _redis_mode == "cluster"; not _redis_disable_tls \| bool; not redis_tls_cert \| length; certificate_stat.stat.exists \| bool; aap_service_san_records_internal \| union(aap_service_san_records) \| difference(certificate_check.subject_alt_name) \| length > 0 |
| 834 | 32.7% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | _redis_mode == "cluster"; not _redis_disable_tls \| bool; not redis_tls_cert \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 835 | 32.8% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | _redis_mode == "cluster"; not _redis_disable_tls \| bool; not redis_tls_cert \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false); aap_service_is_client |
| 836 | 32.8% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | Create CSR for {{ aap_ca_servicename }} node/s | Create CSR for {{ aap_ca_servicename }} node/s | _redis_mode == "cluster"; not _redis_disable_tls \| bool; not redis_tls_cert \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 837 | 32.8% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | Sign {{ aap_ca_servicename }} public key | Sign {{ aap_ca_servicename }} public key | _redis_mode == "cluster"; not _redis_disable_tls \| bool; not redis_tls_cert \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 838 | 32.9% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | Write public key to {{ aap_ca_servicename }} node/s | Write public key to {{ aap_ca_servicename }} node/s (dest `{{ aap_service_cert_fullpath }}`) | _redis_mode == "cluster"; not _redis_disable_tls \| bool; not redis_tls_cert \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 839 | 32.9% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | Signal restart if certificate pair changes | Signal restart if certificate pair changes | _redis_mode == "cluster"; not _redis_disable_tls \| bool; not redis_tls_cert \| length; (write_key is changed or write_certificate is changed) |
| 840 | 33.0% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → Configure TLS certificates for redis | Trigger redis notifier for internally managed certificate changes | Trigger redis notifier for internally managed certificate changes | _redis_mode == "cluster"; not _redis_disable_tls \| bool; aap_service_restart \| default(false) |
| 841 | 33.0% | install.yml → Install cluster redis → block → redis → redis → tasks.yml | Ensure redis socket directory exists | Ensure redis socket directory exists | _redis_mode == "cluster" |
| 842 | 33.0% | install.yml → Install cluster redis → block → redis → redis → tasks.yml | Ensure Redis socket directory has the correct SELinux context | Ensure Redis socket directory has the correct SELinux context | _redis_mode == "cluster" |
| 843 | 33.1% | install.yml → Install cluster redis → block → redis → redis → tasks.yml | Create tmpfiles.d entry for redis socket directory | Create tmpfiles.d entry for redis socket directory (dest `/etc/tmpfiles.d/redis.conf`) | _redis_mode == "cluster" |
| 844 | 33.1% | install.yml → Install cluster redis → block → redis → redis → tasks.yml | Install redis.conf | Install redis.conf (dest `{{ redis_conf_location }}`) | _redis_mode == "cluster" |
| 845 | 33.2% | install.yml → Install cluster redis → block → redis → redis → tasks.yml | block | Begin conditional or grouped task block | _redis_mode == "cluster"; not redis_use_sockets \| bool |
| 846 | 33.2% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → block | Set gateway redis credentials for clustered | Set gateway redis credentials for clustered | _redis_mode == "cluster"; not redis_use_sockets \| bool |
| 847 | 33.2% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → block | Set Event-Driven Ansible redis credentials | Set Event-Driven Ansible redis credentials | _redis_mode == "cluster"; not redis_use_sockets \| bool; groups.get("automationedacontroller", []) \| length > 0 |
| 848 | 33.3% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → block | Create the redis user ACL file | Create the redis user ACL file | _redis_mode == "cluster"; not redis_use_sockets \| bool |
| 849 | 33.3% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → block | block | Begin conditional or grouped task block | _redis_mode == "cluster"; not redis_use_sockets \| bool; install_redis_mode == "cluster" |
| 850 | 33.3% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → block → block | Set redis hostname | Set redis hostname | _redis_mode == "cluster"; not redis_use_sockets \| bool; install_redis_mode == "cluster" |
| 851 | 33.4% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → block → block | Map redis node hostnames | Map redis node hostnames | _redis_mode == "cluster"; not redis_use_sockets \| bool; install_redis_mode == "cluster" |
| 852 | 33.4% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → block → block | Create redis nodes file | Create redis nodes file | _redis_mode == "cluster"; not redis_use_sockets \| bool; install_redis_mode == "cluster" |
| 853 | 33.5% | install.yml → Install cluster redis → block → redis → redis → tasks.yml → block → block | Install {{ redis_init_name }} service file | Install {{ redis_init_name }} service file (dest `/lib/systemd/system/redis-platform.service`) | _redis_mode == "cluster"; not redis_use_sockets \| bool; install_redis_mode == "cluster" |
| 854 | 33.5% | install.yml → Install cluster redis → block → redis → redis → tasks.yml | start redis and configure to startup automatically | start redis and configure to startup automatically (service `{{ redis_init_name }}`) | _redis_mode == "cluster" |
| 855 | 33.5% | install.yml → Install cluster redis → block | Configure firewall for platform redis | Configure firewall for platform redis (role `firewall`) | _redis_mode == "cluster" |
| 856 | 33.6% | install.yml → Install cluster redis → block → firewall | role `firewall` | Enter role task list | _redis_mode == "cluster" |
| 857 | 33.6% | install.yml → Install cluster redis → block → firewall → firewall | file `main.yml` | Task file `ansible/automation_platform_installer/roles/firewall/tasks/main.yml` | _redis_mode == "cluster" |
| 858 | 33.7% | install.yml → Install cluster redis → block → firewall → firewall | include_tasks | Include a task file from the current role or playbook (file `discover.yml`) | _redis_mode == "cluster" |
| 859 | 33.7% | install.yml → Install cluster redis → block → firewall → firewall → discover.yml | file `discover.yml` | Task file `ansible/automation_platform_installer/roles/firewall/tasks/discover.yml` | _redis_mode == "cluster" |
| 860 | 33.7% | install.yml → Install cluster redis → block → firewall → firewall → discover.yml | Determine if firewalld is running | Determine if firewalld is running | _redis_mode == "cluster" |
| 861 | 33.8% | install.yml → Install cluster redis → block → firewall → firewall → discover.yml | Determine if firewalld is enabled | Determine if firewalld is enabled | _redis_mode == "cluster" |
| 862 | 33.8% | install.yml → Install cluster redis → block → firewall → firewall → discover.yml | Determine if iptables is installed | Determine if iptables is installed | _redis_mode == "cluster" |
| 863 | 33.9% | install.yml → Install cluster redis → block → firewall → firewall → discover.yml | Determine if iptables service is installed | Determine if iptables service is installed | _redis_mode == "cluster" |
| 864 | 33.9% | install.yml → Install cluster redis → block → firewall → firewall → discover.yml | Check to see if there is an iptables config file | Check to see if there is an iptables config file | _redis_mode == "cluster"; iptables_installed.rc == 0 |
| 865 | 33.9% | install.yml → Install cluster redis → block → firewall → firewall → discover.yml | Select firewalld | Select firewalld | _redis_mode == "cluster"; firewalld_running.rc == 0 or systemctl_unit_files.stdout_lines \| select('match','firewalld.service\s+enabled') \| list \| length == 1 |
| 866 | 34.0% | install.yml → Install cluster redis → block → firewall → firewall → discover.yml | Select iptables | Select iptables | _redis_mode == "cluster"; iptables_installed.rc == 0; iptables_exists.stat.exists; iptables_service.stat.exists |
| 867 | 34.0% | install.yml → Install cluster redis → block → firewall → firewall | Include {{ 'no' if component_firewall_type == 'none' else component_firewall_type }} tasks | Include {{ 'no' if component_firewall_type == 'none' else component_firewall_type }} tasks (file `{{ component_firewall_type }}.yml`) | _redis_mode == "cluster"; firewalld_running.rc == 0 or systemctl_unit_files.stdout_lines \| select('match','firewalld.service\s+enabled') \| list \| length == 1 or ( iptables_installed.rc == 0 and iptables_exists.stat.exists and iptables_service.stat.exists ) |
| 868 | 34.1% | install.yml → Install cluster redis → block → firewall → firewall → {{ component_firewall_type }}.yml | {{ component_firewall_type }}.yml | Include task file `{{ component_firewall_type }}.yml` (relative to current role task directory) | _redis_mode == "cluster"; firewalld_running.rc == 0 or systemctl_unit_files.stdout_lines \| select('match','firewalld.service\s+enabled') \| list \| length == 1 or ( iptables_installed.rc == 0 and iptables_exists.stat.exists and iptables_service.stat.exists ) |
| 869 | 34.1% | install.yml → Install cluster redis → block | Force handlers for redis | Force handlers for redis (flush_handlers) | _redis_mode == "cluster" |
| 870 | 34.1% | install.yml → Install cluster redis → block | include_role | Include and run tasks from an Ansible role (role `redis`, tasks `cluster`) | _redis_mode == "cluster"; not _redis_is_clustered |
| 871 | 34.2% | install.yml → Install cluster redis → block → redis | role `redis` (`cluster`) | Enter role task list | _redis_mode == "cluster"; not _redis_is_clustered |
| 872 | 34.2% | install.yml → Install cluster redis → block → redis → redis | file `cluster.yml` | Task file `ansible/automation_platform_installer/roles/redis/tasks/cluster.yml` | _redis_mode == "cluster"; not _redis_is_clustered |
| 873 | 34.2% | install.yml → Install cluster redis → block → redis → redis | Set redis cluster IP address per host | Set redis cluster IP address per host | _redis_mode == "cluster"; not _redis_is_clustered |
| 874 | 34.3% | install.yml → Install cluster redis → block → redis → redis | Create redis ipv4 address list | Create redis ipv4 address list | _redis_mode == "cluster"; not _redis_is_clustered |
| 875 | 34.3% | install.yml → Install cluster redis → block → redis → redis | Create cluster ips list | Create cluster ips list | _redis_mode == "cluster"; not _redis_is_clustered |
| 876 | 34.4% | install.yml → Install cluster redis → block → redis → redis | block | Begin conditional or grouped task block | _redis_mode == "cluster"; not _redis_is_clustered; inventory_hostname == groups["redis"][0] |
| 877 | 34.4% | install.yml → Install cluster redis → block → redis → redis → block | Expand redis-cli options for mTLS if necessary | Expand redis-cli options for mTLS if necessary | _redis_mode == "cluster"; not _redis_is_clustered; inventory_hostname == groups["redis"][0]; not _redis_disable_mtls |
| 878 | 34.4% | install.yml → Install cluster redis → block → redis → redis → block | Create the redis cluster | Create the redis cluster | _redis_mode == "cluster"; not _redis_is_clustered; inventory_hostname == groups["redis"][0] |
| 879 | 34.5% | install.yml → Install cluster redis → block → redis → redis | Create file to track cluster successfully configured | Create file to track cluster successfully configured | _redis_mode == "cluster"; not _redis_is_clustered |
| 880 | 34.5% | install.yml → Install cluster redis | block (always) | Run always tasks for block | _redis_mode == "cluster" |
| 881 | 34.6% | install.yml → Install cluster redis → block (always) | Run repo_management (teardown) | Run repo_management (teardown) (role `repo_management`, tasks `teardown`) | _redis_mode == "cluster"; _redis_mode == "cluster" |
| 882 | 34.6% | install.yml → Install cluster redis → block (always) → repo_management | role `repo_management` (`teardown`) | Enter role task list | _redis_mode == "cluster"; _redis_mode == "cluster" |
| 883 | 34.6% | install.yml → Install cluster redis → block (always) → repo_management → repo_management | file `teardown.yml` | Task file `ansible/automation_platform_installer/roles/repo_management/tasks/teardown.yml` | _redis_mode == "cluster"; _redis_mode == "cluster" |
| 884 | 34.7% | install.yml → Install cluster redis → block (always) → repo_management → repo_management | Disable Automation Platform repository | Disable Automation Platform repository | _redis_mode == "cluster"; _redis_mode == "cluster"; extra_repos is defined |
| 885 | 34.7% | install.yml → Install cluster redis → block (always) → repo_management → repo_management | Disable Automation Platform RHSM repository | Disable Automation Platform RHSM repository | _redis_mode == "cluster"; _redis_mode == "cluster"; aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 886 | 34.8% | install.yml | Install Automation Gateway | Play targeting `automationgateway` | — |
| 887 | 34.8% | install.yml → Install Automation Gateway | block | Begin conditional or grouped task block | — |
| 888 | 34.8% | install.yml → Install Automation Gateway → block | Run repo_management (setup) | Run repo_management (setup) (role `repo_management`, tasks `setup`) | — |
| 889 | 34.9% | install.yml → Install Automation Gateway → block → repo_management | role `repo_management` (`setup`) | Enter role task list | — |
| 890 | 34.9% | install.yml → Install Automation Gateway → block → repo_management → repo_management | file `setup.yml` | Task file `ansible/automation_platform_installer/roles/repo_management/tasks/setup.yml` | — |
| 891 | 35.0% | install.yml → Install Automation Gateway → block → repo_management → repo_management | include_vars | Load variables from a file | — |
| 892 | 35.0% | install.yml → Install Automation Gateway → block → repo_management → repo_management | Install yum repo keys | Install yum repo keys (dest `/etc/pki/rpm-gpg/{{ item }}`) | — |
| 893 | 35.0% | install.yml → Install Automation Gateway → block → repo_management → repo_management | Install subscription-manager-rhsm-certificates | Install subscription-manager-rhsm-certificates | — |
| 894 | 35.1% | install.yml → Install Automation Gateway → block → repo_management → repo_management | Determine if rhsm repository exists | Determine if rhsm repository exists | — |
| 895 | 35.1% | install.yml → Install Automation Gateway → block → repo_management → repo_management | Enable Automation Platform rhsm repository | Enable Automation Platform rhsm repository | ansible_distribution == 'RedHat'; rhsm_repo.stat.exists; not bundle_install \| bool; aw_repo_url is undefined |
| 896 | 35.2% | install.yml → Install Automation Gateway → block → repo_management → repo_management | Explain Automation Platform rhsm repository failure | Explain Automation Platform rhsm repository failure | aap_rhsm_repo_enable.failed \| default(false) |
| 897 | 35.2% | install.yml → Install Automation Gateway → block → repo_management → repo_management | block | Begin conditional or grouped task block | aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 898 | 35.2% | install.yml → Install Automation Gateway → block → repo_management → repo_management → block | Enable module hotfixes flag | Enable module hotfixes flag | aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 899 | 35.3% | install.yml → Install Automation Gateway → block → repo_management → repo_management → block | Set AAP repo for update | Set AAP repo for update | aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 900 | 35.3% | install.yml → Install Automation Gateway → block → repo_management → repo_management | Ensure rsync is installed | Ensure rsync is installed | bundle_install \| bool |
| 901 | 35.3% | install.yml → Install Automation Gateway → block → repo_management → repo_management | Copy bundle packages to repo source directory | Begin conditional or grouped task block | bundle_install \| bool |
| 902 | 35.4% | install.yml → Install Automation Gateway → block → repo_management → repo_management → Copy bundle packages to repo source directory | Copy bundle packages to repo source directory | Copy bundle packages to repo source directory | bundle_install \| bool |
| 903 | 35.4% | install.yml → Install Automation Gateway → block → repo_management → repo_management | Copy bundle packages to repo source directory (rescue) | Run rescue tasks for block | bundle_install \| bool |
| 904 | 35.5% | install.yml → Install Automation Gateway → block → repo_management → repo_management → Copy bundle packages to repo source directory (rescue) | Remove old bundle packages source directory (legacy) | Remove old bundle packages source directory (legacy) | bundle_install \| bool |
| 905 | 35.5% | install.yml → Install Automation Gateway → block → repo_management → repo_management → Copy bundle packages to repo source directory (rescue) | Copy bundle packages to repo source directory (legacy) | Copy bundle packages to repo source directory (legacy) (dest `{{ bundle_install_folder }}`) | bundle_install \| bool |
| 906 | 35.5% | install.yml → Install Automation Gateway → block → repo_management → repo_management | Install the Automation Platform bundle repository | Install the Automation Platform bundle repository (dest `/etc/yum.repos.d/ansible-automation-platform.repo`) | bundle_install \| bool |
| 907 | 35.6% | install.yml → Install Automation Gateway → block → repo_management → repo_management | Remove legacy ansible-tower repository | Remove legacy ansible-tower repository | — |
| 908 | 35.6% | install.yml → Install Automation Gateway → block → repo_management → repo_management | Install the Automation Platform yum repository. | Install the Automation Platform yum repository. (dest `/etc/yum.repos.d/ansible-automation-platform.repo`) | aw_repo_url is defined; not bundle_install \| bool |
| 909 | 35.7% | install.yml → Install Automation Gateway → block → repo_management → repo_management | Set repo list for bundle install | Set repo list for bundle install | bundle_install \| bool |
| 910 | 35.7% | install.yml → Install Automation Gateway → block → repo_management → repo_management | Set repo list for online install | Set repo list for online install | yum_repo is succeeded; yum_repo is not skipped |
| 911 | 35.7% | install.yml → Install Automation Gateway → block → repo_management → repo_management | Enable Ansible Automation Platform repo | Enable Ansible Automation Platform repo | extra_repos is defined |
| 912 | 35.8% | install.yml → Install Automation Gateway → block | block | Begin conditional or grouped task block | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone' |
| 913 | 35.8% | install.yml → Install Automation Gateway → block → block | include_role | Include and run tasks from an Ansible role (role `redis`) | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone' |
| 914 | 35.9% | install.yml → Install Automation Gateway → block → block → redis | role `redis` | Enter role task list | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone' |
| 915 | 35.9% | install.yml → Install Automation Gateway → block → block → redis → redis | file `main.yml` | Task file `ansible/automation_platform_installer/roles/redis/tasks/main.yml` | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone' |
| 916 | 35.9% | install.yml → Install Automation Gateway → block → block → redis → redis | include_tasks | Include a task file from the current role or playbook (file `vars.yml`) | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone' |
| 917 | 36.0% | install.yml → Install Automation Gateway → block → block → redis → redis → vars.yml | file `vars.yml` | Task file `ansible/automation_platform_installer/roles/redis/tasks/vars.yml` | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone' |
| 918 | 36.0% | install.yml → Install Automation Gateway → block → block → redis → redis → vars.yml | include_vars | Load variables from a file | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone' |
| 919 | 36.1% | install.yml → Install Automation Gateway → block → block → redis → redis → vars.yml | include_vars | Load variables from a file | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; install_redis_mode == "cluster" |
| 920 | 36.1% | install.yml → Install Automation Gateway → block → block → redis → redis | include_tasks | Include a task file from the current role or playbook (file `module.yml`) | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone' |
| 921 | 36.1% | install.yml → Install Automation Gateway → block → block → redis → redis → module.yml | file `module.yml` | Task file `ansible/automation_platform_installer/roles/redis/tasks/module.yml` | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone' |
| 922 | 36.2% | install.yml → Install Automation Gateway → block → block → redis → redis → module.yml | Enable redis module stream on RHEL 8 | Begin conditional or grouped task block | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; ansible_distribution_major_version \| int == 8 |
| 923 | 36.2% | install.yml → Install Automation Gateway → block → block → redis → redis → module.yml → Enable redis module stream on RHEL 8 | Disable Redis module | Disable Redis module | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; ansible_distribution_major_version \| int == 8 |
| 924 | 36.3% | install.yml → Install Automation Gateway → block → block → redis → redis → module.yml → Enable redis module stream on RHEL 8 | Enable Redis module | Enable Redis module | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; ansible_distribution_major_version \| int == 8 |
| 925 | 36.3% | install.yml → Install Automation Gateway → block → block → redis → redis | include_tasks | Include a task file from the current role or playbook (file `tasks.yml`) | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone' |
| 926 | 36.3% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml | file `tasks.yml` | Task file `ansible/automation_platform_installer/roles/redis/tasks/tasks.yml` | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone' |
| 927 | 36.4% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml | Ensure redis package is installed | Ensure redis package is installed | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone' |
| 928 | 36.4% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml | Ensure AAP directory exists | Ensure AAP directory exists | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone' |
| 929 | 36.4% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml | Ensure redis cert directory exists | Ensure redis cert directory exists | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone' |
| 930 | 36.5% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml | Configure TLS certificates for redis | Begin conditional or grouped task block | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not _redis_disable_tls \| bool |
| 931 | 36.5% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → Configure TLS certificates for redis | Copy user provided SSL cert | Begin conditional or grouped task block | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not _redis_disable_tls \| bool; redis_tls_cert \| length |
| 932 | 36.6% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → Configure TLS certificates for redis → Copy user provided SSL cert | Import specified TLS certificate | Import specified TLS certificate | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not _redis_disable_tls \| bool; redis_tls_cert \| length |
| 933 | 36.6% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → Configure TLS certificates for redis → Copy user provided SSL cert | Import specified TLS private key | Import specified TLS private key | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not _redis_disable_tls \| bool; redis_tls_cert \| length |
| 934 | 36.6% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → Configure TLS certificates for redis | Use internal CA | Use internal CA (role `certificate_authority`, tasks `sign_service.yml`) | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 935 | 36.7% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority | role `certificate_authority` (`sign_service.yml`) | Enter role task list | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 936 | 36.7% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | file `sign_service.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/sign_service.yml` | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 937 | 36.8% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | include_tasks | Include a task file from the current role or playbook (file `init.yml`) | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 938 | 36.8% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → init.yml | file `init.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/init.yml` | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 939 | 36.8% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → init.yml | Ensure python cryptography libraries are installed | Ensure python cryptography libraries are installed | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 940 | 36.9% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | Create {{ aap_ca_servicename }} private key | Create {{ aap_ca_servicename }} private key | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 941 | 36.9% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | set_fact | Set a host fact used by later tasks | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 942 | 37.0% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | Check if {{ aap_ca_servicename }} public key exists | Check if {{ aap_ca_servicename }} public key exists | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 943 | 37.0% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not _redis_disable_tls \| bool; not redis_tls_cert \| length; certificate_stat.stat.exists \| bool |
| 944 | 37.0% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | Check contents of {{ aap_ca_servicename }} public key | Check contents of {{ aap_ca_servicename }} public key | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not _redis_disable_tls \| bool; not redis_tls_cert \| length; certificate_stat.stat.exists \| bool |
| 945 | 37.1% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not _redis_disable_tls \| bool; not redis_tls_cert \| length; certificate_stat.stat.exists \| bool; aap_service_san_records_internal \| union(aap_service_san_records) \| difference(certificate_check.subject_alt_name) \| length > 0 |
| 946 | 37.1% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not _redis_disable_tls \| bool; not redis_tls_cert \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 947 | 37.2% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not _redis_disable_tls \| bool; not redis_tls_cert \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false); aap_service_is_client |
| 948 | 37.2% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | Create CSR for {{ aap_ca_servicename }} node/s | Create CSR for {{ aap_ca_servicename }} node/s | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not _redis_disable_tls \| bool; not redis_tls_cert \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 949 | 37.2% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | Sign {{ aap_ca_servicename }} public key | Sign {{ aap_ca_servicename }} public key | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not _redis_disable_tls \| bool; not redis_tls_cert \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 950 | 37.3% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | Write public key to {{ aap_ca_servicename }} node/s | Write public key to {{ aap_ca_servicename }} node/s (dest `{{ aap_service_cert_fullpath }}`) | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not _redis_disable_tls \| bool; not redis_tls_cert \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 951 | 37.3% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | Signal restart if certificate pair changes | Signal restart if certificate pair changes | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not _redis_disable_tls \| bool; not redis_tls_cert \| length; (write_key is changed or write_certificate is changed) |
| 952 | 37.4% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → Configure TLS certificates for redis | Trigger redis notifier for internally managed certificate changes | Trigger redis notifier for internally managed certificate changes | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not _redis_disable_tls \| bool; aap_service_restart \| default(false) |
| 953 | 37.4% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml | Ensure redis socket directory exists | Ensure redis socket directory exists | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone' |
| 954 | 37.4% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml | Ensure Redis socket directory has the correct SELinux context | Ensure Redis socket directory has the correct SELinux context | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone' |
| 955 | 37.5% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml | Create tmpfiles.d entry for redis socket directory | Create tmpfiles.d entry for redis socket directory (dest `/etc/tmpfiles.d/redis.conf`) | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone' |
| 956 | 37.5% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml | Install redis.conf | Install redis.conf (dest `{{ redis_conf_location }}`) | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone' |
| 957 | 37.5% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml | block | Begin conditional or grouped task block | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not redis_use_sockets \| bool |
| 958 | 37.6% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → block | Set gateway redis credentials for clustered | Set gateway redis credentials for clustered | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not redis_use_sockets \| bool |
| 959 | 37.6% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → block | Set Event-Driven Ansible redis credentials | Set Event-Driven Ansible redis credentials | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not redis_use_sockets \| bool; groups.get("automationedacontroller", []) \| length > 0 |
| 960 | 37.7% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → block | Create the redis user ACL file | Create the redis user ACL file | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not redis_use_sockets \| bool |
| 961 | 37.7% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → block | block | Begin conditional or grouped task block | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not redis_use_sockets \| bool; install_redis_mode == "cluster" |
| 962 | 37.7% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → block → block | Set redis hostname | Set redis hostname | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not redis_use_sockets \| bool; install_redis_mode == "cluster" |
| 963 | 37.8% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → block → block | Map redis node hostnames | Map redis node hostnames | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not redis_use_sockets \| bool; install_redis_mode == "cluster" |
| 964 | 37.8% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → block → block | Create redis nodes file | Create redis nodes file | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not redis_use_sockets \| bool; install_redis_mode == "cluster" |
| 965 | 37.9% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml → block → block | Install {{ redis_init_name }} service file | Install {{ redis_init_name }} service file (dest `/lib/systemd/system/redis-platform.service`) | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone'; not redis_use_sockets \| bool; install_redis_mode == "cluster" |
| 966 | 37.9% | install.yml → Install Automation Gateway → block → block → redis → redis → tasks.yml | start redis and configure to startup automatically | start redis and configure to startup automatically (service `{{ redis_init_name }}`) | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone' |
| 967 | 37.9% | install.yml → Install Automation Gateway → block → block | set_fact | Set a host fact used by later tasks | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone' |
| 968 | 38.0% | install.yml → Install Automation Gateway → block → block | Force handlers for redis | Force handlers for redis (flush_handlers) | inventory_hostname == groups['automationgateway'][0]; _redis_mode == 'standalone' |
| 969 | 38.0% | install.yml → Install Automation Gateway → block | include_role | Include and run tasks from an Ansible role (role `nginx`) | — |
| 970 | 38.1% | install.yml → Install Automation Gateway → block → nginx | role `nginx` | Enter role task list | — |
| 971 | 38.1% | install.yml → Install Automation Gateway → block → nginx → nginx | file `main.yml` | Task file `ansible/automation_platform_installer/roles/nginx/tasks/main.yml` | — |
| 972 | 38.1% | install.yml → Install Automation Gateway → block → nginx → nginx | include_tasks | Include a task file from the current role or playbook (file `module.yml`) | — |
| 973 | 38.2% | install.yml → Install Automation Gateway → block → nginx → nginx → module.yml | file `module.yml` | Task file `ansible/automation_platform_installer/roles/nginx/tasks/module.yml` | — |
| 974 | 38.2% | install.yml → Install Automation Gateway → block → nginx → nginx → module.yml | Enable nginx module stream | Begin conditional or grouped task block | — |
| 975 | 38.3% | install.yml → Install Automation Gateway → block → nginx → nginx → module.yml → Enable nginx module stream | Disable Nginx module | Disable Nginx module | — |
| 976 | 38.3% | install.yml → Install Automation Gateway → block → nginx → nginx → module.yml → Enable nginx module stream | Enable Nginx module | Enable Nginx module | — |
| 977 | 38.3% | install.yml → Install Automation Gateway → block → nginx → nginx | include_tasks | Include a task file from the current role or playbook (file `tasks.yml`) | — |
| 978 | 38.4% | install.yml → Install Automation Gateway → block → nginx → nginx → tasks.yml | file `tasks.yml` | Task file `ansible/automation_platform_installer/roles/nginx/tasks/tasks.yml` | — |
| 979 | 38.4% | install.yml → Install Automation Gateway → block → nginx → nginx → tasks.yml | Install nginx | Install nginx | — |
| 980 | 38.5% | install.yml → Install Automation Gateway → block → nginx → nginx → tasks.yml | SELinux configuration for nginx | Begin conditional or grouped task block | ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 981 | 38.5% | install.yml → Install Automation Gateway → block → nginx → nginx → tasks.yml → SELinux configuration for nginx | Ensure libsemanage-python is installed | Ensure libsemanage-python is installed | ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 982 | 38.5% | install.yml → Install Automation Gateway → block → nginx → nginx → tasks.yml → SELinux configuration for nginx | Open up permissions on nginx. | Open up permissions on nginx. | ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 983 | 38.6% | install.yml → Install Automation Gateway → block | include_role | Include and run tasks from an Ansible role (role `automationgateway`) | — |
| 984 | 38.6% | install.yml → Install Automation Gateway → block → automationgateway | role `automationgateway` | Enter role task list | — |
| 985 | 38.6% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | file `main.yml` | Task file `ansible/automation_platform_installer/roles/automationgateway/tasks/main.yml` | — |
| 986 | 38.7% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Load OS specific variables | Load OS specific variables | — |
| 987 | 38.7% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Map redis url to Gateway for non-clustered redis | Map redis url to Gateway for non-clustered redis | _redis_mode != "cluster" |
| 988 | 38.8% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Map redis url to Gateway for clustered redis | Map redis url to Gateway for clustered redis | _redis_mode == "cluster" |
| 989 | 38.8% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Map redis hostnames to Gateway | Map redis hostnames to Gateway | _redis_mode == "cluster" |
| 990 | 38.8% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Check installed version of PG | Check installed version of PG | — |
| 991 | 38.9% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | block | Begin conditional or grouped task block | pg_installed_version.failed or pg_installed_version.stdout \| trim \| int != automationgateway_postgresql_version \| int |
| 992 | 38.9% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → block | Disable PostgreSQL module | Disable PostgreSQL module | pg_installed_version.failed or pg_installed_version.stdout \| trim \| int != automationgateway_postgresql_version \| int; not pg_installed_version.failed; ansible_distribution_major_version == "8" |
| 993 | 39.0% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → block | Enable PostgreSQL {{ automationgateway_postgresql_version }} dnf modules | Enable PostgreSQL {{ automationgateway_postgresql_version }} dnf modules | pg_installed_version.failed or pg_installed_version.stdout \| trim \| int != automationgateway_postgresql_version \| int |
| 994 | 39.0% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → block | Install PostgreSQL client {{ automationgateway_postgresql_version }} | Install PostgreSQL client {{ automationgateway_postgresql_version }} | pg_installed_version.failed or pg_installed_version.stdout \| trim \| int != automationgateway_postgresql_version \| int |
| 995 | 39.0% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Install automation-gateway | Install automation-gateway | — |
| 996 | 39.1% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Update existing dependent packages | Update existing dependent packages (role `repo_management`, tasks `update_dependency`) | — |
| 997 | 39.1% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → repo_management | role `repo_management` (`update_dependency`) | Enter role task list | — |
| 998 | 39.2% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → repo_management → repo_management | file `update_dependency.yml` | Task file `ansible/automation_platform_installer/roles/repo_management/tasks/update_dependency.yml` | — |
| 999 | 39.2% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → repo_management → repo_management | Ensure AAP repo for update is set | Ensure AAP repo for update is set | not aap_repo_for_update \| default('') \| length |
| 1000 | 39.2% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → repo_management → repo_management | Generate a list of AAP packages with an update | Generate a list of AAP packages with an update | — |
| 1001 | 39.3% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → repo_management → repo_management | Set AAP packages to update | Set AAP packages to update | — |
| 1002 | 39.3% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → repo_management → repo_management | Update packages | Update packages | _packages_to_update \| length |
| 1003 | 39.4% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Trigger service restart if package changed | Trigger service restart if package changed | _package_update_result is changed |
| 1004 | 39.4% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Apply envoy config | Apply envoy config | — |
| 1005 | 39.4% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Modify dependent service list for postgresql | Modify dependent service list for postgresql (role `systemd`, tasks `postgresql`) | — |
| 1006 | 39.5% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → systemd | role `systemd` (`postgresql`) | Enter role task list | — |
| 1007 | 39.5% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → systemd → systemd | file `postgresql.yml` | Task file `ansible/automation_platform_installer/roles/systemd/tasks/postgresql.yml` | — |
| 1008 | 39.6% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → systemd → systemd | block | Begin conditional or grouped task block | _internal_database \| bool |
| 1009 | 39.6% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → systemd → systemd → block | Create directory for service configuration override | Create directory for service configuration override | _internal_database \| bool |
| 1010 | 39.6% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → systemd → systemd → block | Add postgresql to dependent service list | Add postgresql to dependent service list | _internal_database \| bool |
| 1011 | 39.7% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → systemd → systemd | Ensure postgresql isn't set as a dependent service | Ensure postgresql isn't set as a dependent service | not _internal_database \| bool |
| 1012 | 39.7% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Make dependent services part of gateway target | Make dependent services part of gateway target (role `systemd`) | — |
| 1013 | 39.7% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → systemd | role `systemd` | Enter role task list | — |
| 1014 | 39.8% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → systemd → systemd | file `main.yml` | Task file `ansible/automation_platform_installer/roles/systemd/tasks/main.yml` | — |
| 1015 | 39.8% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → systemd → systemd | Create directory for dependent service configuration override | Create directory for dependent service configuration override | — |
| 1016 | 39.9% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → systemd → systemd | Add dependent service configuration override | Add dependent service configuration override | — |
| 1017 | 39.9% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Enable automation-gateway target and service | Enable automation-gateway target and service | — |
| 1018 | 39.9% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | include_tasks | Include a task file from the current role or playbook (file `secret_key.yml`) | — |
| 1019 | 40.0% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → secret_key.yml | file `secret_key.yml` | Task file `ansible/automation_platform_installer/roles/automationgateway/tasks/secret_key.yml` | — |
| 1020 | 40.0% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → secret_key.yml | Check for SECRET_KEY on all nodes | Check for SECRET_KEY on all nodes | — |
| 1021 | 40.1% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → secret_key.yml | block | Begin conditional or grouped task block | automationgateway_secret_key_path.stat.exists |
| 1022 | 40.1% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → secret_key.yml → block | Get SECRET_KEY if found | Get SECRET_KEY if found | automationgateway_secret_key_path.stat.exists |
| 1023 | 40.1% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → secret_key.yml → block | Set SECRET_KEY if found | Set SECRET_KEY if found | automationgateway_secret_key_path.stat.exists; automationgateway_secret_key_slurp['content'] \| default('') \| length |
| 1024 | 40.2% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → secret_key.yml | Set SECRET_KEY if not found | Set SECRET_KEY if not found | not automationgateway_secret_key_slurp['content'] \| default('') \| length |
| 1025 | 40.2% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → secret_key.yml | Set SECRET_KEY for all nodes | Set SECRET_KEY for all nodes | — |
| 1026 | 40.3% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → secret_key.yml | Set SECRET_KEY file | Set SECRET_KEY file (dest `/etc/ansible-automation-platform/gateway/SECRET_KEY`) | — |
| 1027 | 40.3% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | include_tasks | Include a task file from the current role or playbook (file `certificates.yml`) | — |
| 1028 | 40.3% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml | file `certificates.yml` | Task file `ansible/automation_platform_installer/roles/automationgateway/tasks/certificates.yml` | — |
| 1029 | 40.4% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml | Configure postgres client certs | Begin conditional or grouped task block | automationgateway_pg_cert_auth |
| 1030 | 40.4% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Configure postgres client certs | Copy postgres client cert | Copy postgres client cert (dest `/etc/ansible-automation-platform/gateway/pg_client.crt`) | automationgateway_pg_cert_auth |
| 1031 | 40.5% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Configure postgres client certs | Copy postgres client key | Copy postgres client key (dest `/etc/ansible-automation-platform/gateway/pg_client.key`) | automationgateway_pg_cert_auth |
| 1032 | 40.5% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml | block | Begin conditional or grouped task block | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool |
| 1033 | 40.5% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → block | Copy user-provided SSL certificates | Begin conditional or grouped task block | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool; automationgateway_ssl_cert \| default('') \| length |
| 1034 | 40.6% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → block → Copy user-provided SSL certificates | Copy SSL certificate | Copy SSL certificate (dest `/etc/ansible-automation-platform/gateway/gateway.cert`) | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool; automationgateway_ssl_cert \| default('') \| length |
| 1035 | 40.6% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → block → Copy user-provided SSL certificates | Copy SSL private key | Copy SSL private key (dest `/etc/ansible-automation-platform/gateway/gateway.key`) | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool; automationgateway_ssl_cert \| default('') \| length |
| 1036 | 40.7% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → block | Use internal CA | Use internal CA (role `certificate_authority`, tasks `sign_service.yml`) | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool; automationgateway_ssl_cert is not defined |
| 1037 | 40.7% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → block → certificate_authority | role `certificate_authority` (`sign_service.yml`) | Enter role task list | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool; automationgateway_ssl_cert is not defined |
| 1038 | 40.7% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → block → certificate_authority → certificate_authority | file `sign_service.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/sign_service.yml` | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool; automationgateway_ssl_cert is not defined |
| 1039 | 40.8% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → block → certificate_authority → certificate_authority | include_tasks | Include a task file from the current role or playbook (file `init.yml`) | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool; automationgateway_ssl_cert is not defined |
| 1040 | 40.8% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → block → certificate_authority → certificate_authority → init.yml | file `init.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/init.yml` | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool; automationgateway_ssl_cert is not defined |
| 1041 | 40.8% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → block → certificate_authority → certificate_authority → init.yml | Ensure python cryptography libraries are installed | Ensure python cryptography libraries are installed | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool; automationgateway_ssl_cert is not defined |
| 1042 | 40.9% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → block → certificate_authority → certificate_authority | Create {{ aap_ca_servicename }} private key | Create {{ aap_ca_servicename }} private key | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool; automationgateway_ssl_cert is not defined |
| 1043 | 40.9% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → block → certificate_authority → certificate_authority | set_fact | Set a host fact used by later tasks | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool; automationgateway_ssl_cert is not defined |
| 1044 | 41.0% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → block → certificate_authority → certificate_authority | Check if {{ aap_ca_servicename }} public key exists | Check if {{ aap_ca_servicename }} public key exists | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool; automationgateway_ssl_cert is not defined |
| 1045 | 41.0% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → block → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool; automationgateway_ssl_cert is not defined; certificate_stat.stat.exists \| bool |
| 1046 | 41.0% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → block → certificate_authority → certificate_authority → block | Check contents of {{ aap_ca_servicename }} public key | Check contents of {{ aap_ca_servicename }} public key | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool; automationgateway_ssl_cert is not defined; certificate_stat.stat.exists \| bool |
| 1047 | 41.1% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → block → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool; automationgateway_ssl_cert is not defined; certificate_stat.stat.exists \| bool; aap_service_san_records_internal \| union(aap_service_san_records) \| difference(certificate_check.subject_alt_name) \| length > 0 |
| 1048 | 41.1% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → block → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool; automationgateway_ssl_cert is not defined; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1049 | 41.2% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → block → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool; automationgateway_ssl_cert is not defined; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false); aap_service_is_client |
| 1050 | 41.2% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → block → certificate_authority → certificate_authority → block | Create CSR for {{ aap_ca_servicename }} node/s | Create CSR for {{ aap_ca_servicename }} node/s | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool; automationgateway_ssl_cert is not defined; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1051 | 41.2% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → block → certificate_authority → certificate_authority → block | Sign {{ aap_ca_servicename }} public key | Sign {{ aap_ca_servicename }} public key | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool; automationgateway_ssl_cert is not defined; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1052 | 41.3% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → block → certificate_authority → certificate_authority → block | Write public key to {{ aap_ca_servicename }} node/s | Write public key to {{ aap_ca_servicename }} node/s (dest `{{ aap_service_cert_fullpath }}`) | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool; automationgateway_ssl_cert is not defined; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1053 | 41.3% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → block → certificate_authority → certificate_authority | Signal restart if certificate pair changes | Signal restart if certificate pair changes | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool; automationgateway_ssl_cert is not defined; (write_key is changed or write_certificate is changed) |
| 1054 | 41.4% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → block | Trigger nginx notifier for internally managed certificate changes | Trigger nginx notifier for internally managed certificate changes | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool; aap_service_restart \| default(false) |
| 1055 | 41.4% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → block | set_fact | Set a host fact used by later tasks | not automationgateway_disable_https \| bool or not automationgatewayproxy_disable_https \| bool |
| 1056 | 41.4% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml | Handle caching client certs when mTLS is enabled for AAP caching | Begin conditional or grouped task block | not _redis_disable_mtls |
| 1057 | 41.5% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching | Copy user-provided cache client certificates | Begin conditional or grouped task block | not _redis_disable_mtls; automationgateway_cache_cert \| default('') \| length |
| 1058 | 41.5% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → Copy user-provided cache client certificates | Copy caching mTLS certificate | Copy caching mTLS certificate (dest `/etc/ansible-automation-platform/gateway/cache.cert`) | not _redis_disable_mtls; automationgateway_cache_cert \| default('') \| length |
| 1059 | 41.6% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → Copy user-provided cache client certificates | Copy caching mTLS private key | Copy caching mTLS private key (dest `/etc/ansible-automation-platform/gateway/cache.key`) | not _redis_disable_mtls; automationgateway_cache_cert \| default('') \| length |
| 1060 | 41.6% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching | Sign caching client certs | Sign caching client certs (role `certificate_authority`, tasks `sign_service.yml`) | not _redis_disable_mtls; automationgateway_cache_cert is not defined |
| 1061 | 41.6% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority | role `certificate_authority` (`sign_service.yml`) | Enter role task list | not _redis_disable_mtls; automationgateway_cache_cert is not defined |
| 1062 | 41.7% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority | file `sign_service.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/sign_service.yml` | not _redis_disable_mtls; automationgateway_cache_cert is not defined |
| 1063 | 41.7% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority | include_tasks | Include a task file from the current role or playbook (file `init.yml`) | not _redis_disable_mtls; automationgateway_cache_cert is not defined |
| 1064 | 41.8% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority → init.yml | file `init.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/init.yml` | not _redis_disable_mtls; automationgateway_cache_cert is not defined |
| 1065 | 41.8% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority → init.yml | Ensure python cryptography libraries are installed | Ensure python cryptography libraries are installed | not _redis_disable_mtls; automationgateway_cache_cert is not defined |
| 1066 | 41.8% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority | Create {{ aap_ca_servicename }} private key | Create {{ aap_ca_servicename }} private key | not _redis_disable_mtls; automationgateway_cache_cert is not defined |
| 1067 | 41.9% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority | set_fact | Set a host fact used by later tasks | not _redis_disable_mtls; automationgateway_cache_cert is not defined |
| 1068 | 41.9% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority | Check if {{ aap_ca_servicename }} public key exists | Check if {{ aap_ca_servicename }} public key exists | not _redis_disable_mtls; automationgateway_cache_cert is not defined |
| 1069 | 41.9% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | not _redis_disable_mtls; automationgateway_cache_cert is not defined; certificate_stat.stat.exists \| bool |
| 1070 | 42.0% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority → block | Check contents of {{ aap_ca_servicename }} public key | Check contents of {{ aap_ca_servicename }} public key | not _redis_disable_mtls; automationgateway_cache_cert is not defined; certificate_stat.stat.exists \| bool |
| 1071 | 42.0% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | not _redis_disable_mtls; automationgateway_cache_cert is not defined; certificate_stat.stat.exists \| bool; aap_service_san_records_internal \| union(aap_service_san_records) \| difference(certificate_check.subject_alt_name) \| length > 0 |
| 1072 | 42.1% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | not _redis_disable_mtls; automationgateway_cache_cert is not defined; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1073 | 42.1% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | not _redis_disable_mtls; automationgateway_cache_cert is not defined; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false); aap_service_is_client |
| 1074 | 42.1% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority → block | Create CSR for {{ aap_ca_servicename }} node/s | Create CSR for {{ aap_ca_servicename }} node/s | not _redis_disable_mtls; automationgateway_cache_cert is not defined; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1075 | 42.2% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority → block | Sign {{ aap_ca_servicename }} public key | Sign {{ aap_ca_servicename }} public key | not _redis_disable_mtls; automationgateway_cache_cert is not defined; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1076 | 42.2% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority → block | Write public key to {{ aap_ca_servicename }} node/s | Write public key to {{ aap_ca_servicename }} node/s (dest `{{ aap_service_cert_fullpath }}`) | not _redis_disable_mtls; automationgateway_cache_cert is not defined; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1077 | 42.3% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority | Signal restart if certificate pair changes | Signal restart if certificate pair changes | not _redis_disable_mtls; automationgateway_cache_cert is not defined; (write_key is changed or write_certificate is changed) |
| 1078 | 42.3% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching | Trigger automation-gateway notifier for internally managed certificate changes | Trigger automation-gateway notifier for internally managed certificate changes | not _redis_disable_mtls; aap_service_restart \| default(false) |
| 1079 | 42.3% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching | set_fact | Set a host fact used by later tasks | not _redis_disable_mtls |
| 1080 | 42.4% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Apply gateway supervisor config | Apply gateway supervisor config (dest `/etc/supervisord.d/gateway.ini`) | — |
| 1081 | 42.4% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Apply gateway uwsgi config | Apply gateway uwsgi config (dest `/etc/ansible-automation-platform/gateway/uwsgi.ini`) | — |
| 1082 | 42.5% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Apply gateway nginx config | Apply gateway nginx config (dest `/etc/nginx/conf.d/automation-gateway.conf`) | — |
| 1083 | 42.5% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | set_fact | Set a host fact used by later tasks | not automationgateway_db_template \| default([]) \| length |
| 1084 | 42.5% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Define and check cache directory | Define and check cache directory | — |
| 1085 | 42.6% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Configure Gateway settings file | Configure Gateway settings file (dest `{{ automationgateway_settings_file }}`) | — |
| 1086 | 42.6% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Push Gateway proxy config file | Push Gateway proxy config file | — |
| 1087 | 42.7% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Migrate the gateway database schema | Migrate the gateway database schema | — |
| 1088 | 42.7% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | include_tasks | Include a task file from the current role or playbook (file `super_user.yml`) | inventory_hostname == groups['automationgateway'][0] |
| 1089 | 42.7% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → super_user.yml | file `super_user.yml` | Task file `ansible/automation_platform_installer/roles/automationgateway/tasks/super_user.yml` | inventory_hostname == groups['automationgateway'][0] |
| 1090 | 42.8% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → super_user.yml | create django super user | create django super user | inventory_hostname == groups['automationgateway'][0] |
| 1091 | 42.8% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → super_user.yml | update django super user password | update django super user password | inventory_hostname == groups['automationgateway'][0]; automationgateway_install_admin_password != '' |
| 1092 | 42.9% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Configure the static directory | Configure the static directory | — |
| 1093 | 42.9% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Collect static | Collect static | — |
| 1094 | 42.9% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Ensure gateway services are started | Ensure gateway services are started | — |
| 1095 | 43.0% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Ensure supervisord service is enabled/started | Ensure supervisord service is enabled/started | — |
| 1096 | 43.0% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | include_role | Include and run tasks from an Ansible role (role `nginx`, tasks `finalize.yml`) | — |
| 1097 | 43.0% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → nginx | role `nginx` (`finalize.yml`) | Enter role task list | — |
| 1098 | 43.1% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → nginx → nginx | file `finalize.yml` | Task file `ansible/automation_platform_installer/roles/nginx/tasks/finalize.yml` | — |
| 1099 | 43.1% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → nginx → nginx | Install nginx.conf | Install nginx.conf (dest `/etc/nginx/nginx.conf`) | — |
| 1100 | 43.2% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → nginx → nginx | Start nginx and configure to startup automatically | Start nginx and configure to startup automatically (service `nginx`) | — |
| 1101 | 43.2% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Flush handlers | Flush handlers (flush_handlers) | — |
| 1102 | 43.2% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | Initialize authenticators | Initialize authenticators | — |
| 1103 | 43.3% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | include_tasks | Include a task file from the current role or playbook (file `deregister_service_nodes.yml`) | inventory_hostname == groups['automationgateway'][0] |
| 1104 | 43.3% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → deregister_service_nodes.yml | file `deregister_service_nodes.yml` | Task file `ansible/automation_platform_installer/roles/automationgateway/tasks/deregister_service_nodes.yml` | inventory_hostname == groups['automationgateway'][0] |
| 1105 | 43.4% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → deregister_service_nodes.yml | Get service nodes | Get service nodes | inventory_hostname == groups['automationgateway'][0] |
| 1106 | 43.4% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → deregister_service_nodes.yml | ansible.builtin.set_fact | Run `ansible.builtin.set_fact` | inventory_hostname == groups['automationgateway'][0] |
| 1107 | 43.4% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → deregister_service_nodes.yml | Deregister unused automation platform gateway service nodes | Deregister unused automation platform gateway service nodes | inventory_hostname == groups['automationgateway'][0]; item not in gateway_proxy_config.gateway_service_nodes \| d([]) \| map(attribute='name') \| list |
| 1108 | 43.5% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway | include_tasks | Include a task file from the current role or playbook (file `register_services.yml`) | — |
| 1109 | 43.5% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → register_services.yml | file `register_services.yml` | Task file `ansible/automation_platform_installer/roles/automationgateway/tasks/register_services.yml` | — |
| 1110 | 43.6% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → register_services.yml | Register services | Begin conditional or grouped task block | — |
| 1111 | 43.6% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → register_services.yml → Register services | Update automation platform gateway settings | Update automation platform gateway settings | — |
| 1112 | 43.6% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → register_services.yml → Register services | Update automation platform gateway http ports | Update automation platform gateway http ports | — |
| 1113 | 43.7% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → register_services.yml → Register services | Update automation platform gateway service clusters | Update automation platform gateway service clusters | — |
| 1114 | 43.7% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → register_services.yml → Register services | Update automation platform gateway service nodes | Update automation platform gateway service nodes | — |
| 1115 | 43.8% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → register_services.yml → Register services | Update automation platform gateway services | Update automation platform gateway services | — |
| 1116 | 43.8% | install.yml → Install Automation Gateway → block → automationgateway → automationgateway → register_services.yml → Register services | Update automation platform gateway routes | Update automation platform gateway routes | — |
| 1117 | 43.8% | install.yml → Install Automation Gateway → block | Configure firewall | Configure firewall (role `firewall`) | — |
| 1118 | 43.9% | install.yml → Install Automation Gateway → block → firewall | role `firewall` | Enter role task list | — |
| 1119 | 43.9% | install.yml → Install Automation Gateway → block → firewall → firewall | file `main.yml` | Task file `ansible/automation_platform_installer/roles/firewall/tasks/main.yml` | — |
| 1120 | 44.0% | install.yml → Install Automation Gateway → block → firewall → firewall | include_tasks | Include a task file from the current role or playbook (file `discover.yml`) | — |
| 1121 | 44.0% | install.yml → Install Automation Gateway → block → firewall → firewall → discover.yml | file `discover.yml` | Task file `ansible/automation_platform_installer/roles/firewall/tasks/discover.yml` | — |
| 1122 | 44.0% | install.yml → Install Automation Gateway → block → firewall → firewall → discover.yml | Determine if firewalld is running | Determine if firewalld is running | — |
| 1123 | 44.1% | install.yml → Install Automation Gateway → block → firewall → firewall → discover.yml | Determine if firewalld is enabled | Determine if firewalld is enabled | — |
| 1124 | 44.1% | install.yml → Install Automation Gateway → block → firewall → firewall → discover.yml | Determine if iptables is installed | Determine if iptables is installed | — |
| 1125 | 44.1% | install.yml → Install Automation Gateway → block → firewall → firewall → discover.yml | Determine if iptables service is installed | Determine if iptables service is installed | — |
| 1126 | 44.2% | install.yml → Install Automation Gateway → block → firewall → firewall → discover.yml | Check to see if there is an iptables config file | Check to see if there is an iptables config file | iptables_installed.rc == 0 |
| 1127 | 44.2% | install.yml → Install Automation Gateway → block → firewall → firewall → discover.yml | Select firewalld | Select firewalld | firewalld_running.rc == 0 or systemctl_unit_files.stdout_lines \| select('match','firewalld.service\s+enabled') \| list \| length == 1 |
| 1128 | 44.3% | install.yml → Install Automation Gateway → block → firewall → firewall → discover.yml | Select iptables | Select iptables | iptables_installed.rc == 0; iptables_exists.stat.exists; iptables_service.stat.exists |
| 1129 | 44.3% | install.yml → Install Automation Gateway → block → firewall → firewall | Include {{ 'no' if component_firewall_type == 'none' else component_firewall_type }} tasks | Include {{ 'no' if component_firewall_type == 'none' else component_firewall_type }} tasks (file `{{ component_firewall_type }}.yml`) | firewalld_running.rc == 0 or systemctl_unit_files.stdout_lines \| select('match','firewalld.service\s+enabled') \| list \| length == 1 or ( iptables_installed.rc == 0 and iptables_exists.stat.exists and iptables_service.stat.exists ) |
| 1130 | 44.3% | install.yml → Install Automation Gateway → block → firewall → firewall → {{ component_firewall_type }}.yml | {{ component_firewall_type }}.yml | Include task file `{{ component_firewall_type }}.yml` (relative to current role task directory) | firewalld_running.rc == 0 or systemctl_unit_files.stdout_lines \| select('match','firewalld.service\s+enabled') \| list \| length == 1 or ( iptables_installed.rc == 0 and iptables_exists.stat.exists and iptables_service.stat.exists ) |
| 1131 | 44.4% | install.yml → Install Automation Gateway | block (always) | Run always tasks for block | — |
| 1132 | 44.4% | install.yml → Install Automation Gateway → block (always) | Run repo_management (teardown) | Run repo_management (teardown) (role `repo_management`, tasks `teardown`) | — |
| 1133 | 44.5% | install.yml → Install Automation Gateway → block (always) → repo_management | role `repo_management` (`teardown`) | Enter role task list | — |
| 1134 | 44.5% | install.yml → Install Automation Gateway → block (always) → repo_management → repo_management | file `teardown.yml` | Task file `ansible/automation_platform_installer/roles/repo_management/tasks/teardown.yml` | — |
| 1135 | 44.5% | install.yml → Install Automation Gateway → block (always) → repo_management → repo_management | Disable Automation Platform repository | Disable Automation Platform repository | extra_repos is defined |
| 1136 | 44.6% | install.yml → Install Automation Gateway → block (always) → repo_management → repo_management | Disable Automation Platform RHSM repository | Disable Automation Platform RHSM repository | aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 1137 | 44.6% | install.yml | Install Automation Controller and Execution nodes | Play targeting `automationcontroller:execution_nodes` | — |
| 1138 | 44.7% | install.yml → Install Automation Controller and Execution nodes | Install Automation Controller and Execution nodes | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1139 | 44.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes | set_fact | Set a host fact used by later tasks | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1140 | 44.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes | Add internal CA certificate to trust store | Add internal CA certificate to trust store (role `certificate_authority`, tasks `add_cacert.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1141 | 44.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → certificate_authority | role `certificate_authority` (`add_cacert.yml`) | Enter role task list | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1142 | 44.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → certificate_authority → certificate_authority | file `add_cacert.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/add_cacert.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1143 | 44.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → certificate_authority → certificate_authority | Ensure ca-certificates is installed | Ensure ca-certificates is installed | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1144 | 44.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → certificate_authority → certificate_authority | Copy custom CA cert | Copy custom CA cert (dest `/etc/pki/ca-trust/source/anchors/ansible-automation-platform-custom-ca-cert.crt`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); custom_ca_cert is defined |
| 1145 | 44.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → certificate_authority → certificate_authority | Place AAP CA certificate | Place AAP CA certificate (dest `/etc/pki/ca-trust/source/anchors/ansible-automation-platform-managed-ca-cert.crt`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1146 | 45.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes | Flush handlers | Flush handlers (flush_handlers) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1147 | 45.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes | Run repo_management (setup) | Run repo_management (setup) (role `repo_management`, tasks `setup`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1148 | 45.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → repo_management | role `repo_management` (`setup`) | Enter role task list | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1149 | 45.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → repo_management → repo_management | file `setup.yml` | Task file `ansible/automation_platform_installer/roles/repo_management/tasks/setup.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1150 | 45.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → repo_management → repo_management | include_vars | Load variables from a file | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1151 | 45.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → repo_management → repo_management | Install yum repo keys | Install yum repo keys (dest `/etc/pki/rpm-gpg/{{ item }}`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1152 | 45.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → repo_management → repo_management | Install subscription-manager-rhsm-certificates | Install subscription-manager-rhsm-certificates | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1153 | 45.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → repo_management → repo_management | Determine if rhsm repository exists | Determine if rhsm repository exists | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1154 | 45.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → repo_management → repo_management | Enable Automation Platform rhsm repository | Enable Automation Platform rhsm repository | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); ansible_distribution == 'RedHat'; rhsm_repo.stat.exists; not bundle_install \| bool; aw_repo_url is undefined |
| 1155 | 45.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → repo_management → repo_management | Explain Automation Platform rhsm repository failure | Explain Automation Platform rhsm repository failure | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); aap_rhsm_repo_enable.failed \| default(false) |
| 1156 | 45.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → repo_management → repo_management | block | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 1157 | 45.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → repo_management → repo_management → block | Enable module hotfixes flag | Enable module hotfixes flag | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 1158 | 45.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → repo_management → repo_management → block | Set AAP repo for update | Set AAP repo for update | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 1159 | 45.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → repo_management → repo_management | Ensure rsync is installed | Ensure rsync is installed | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); bundle_install \| bool |
| 1160 | 45.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → repo_management → repo_management | Copy bundle packages to repo source directory | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); bundle_install \| bool |
| 1161 | 45.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → repo_management → repo_management → Copy bundle packages to repo source directory | Copy bundle packages to repo source directory | Copy bundle packages to repo source directory | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); bundle_install \| bool |
| 1162 | 45.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → repo_management → repo_management | Copy bundle packages to repo source directory (rescue) | Run rescue tasks for block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); bundle_install \| bool |
| 1163 | 45.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → repo_management → repo_management → Copy bundle packages to repo source directory (rescue) | Remove old bundle packages source directory (legacy) | Remove old bundle packages source directory (legacy) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); bundle_install \| bool |
| 1164 | 45.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → repo_management → repo_management → Copy bundle packages to repo source directory (rescue) | Copy bundle packages to repo source directory (legacy) | Copy bundle packages to repo source directory (legacy) (dest `{{ bundle_install_folder }}`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); bundle_install \| bool |
| 1165 | 45.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → repo_management → repo_management | Install the Automation Platform bundle repository | Install the Automation Platform bundle repository (dest `/etc/yum.repos.d/ansible-automation-platform.repo`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); bundle_install \| bool |
| 1166 | 45.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → repo_management → repo_management | Remove legacy ansible-tower repository | Remove legacy ansible-tower repository | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1167 | 45.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → repo_management → repo_management | Install the Automation Platform yum repository. | Install the Automation Platform yum repository. (dest `/etc/yum.repos.d/ansible-automation-platform.repo`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); aw_repo_url is defined; not bundle_install \| bool |
| 1168 | 45.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → repo_management → repo_management | Set repo list for bundle install | Set repo list for bundle install | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); bundle_install \| bool |
| 1169 | 45.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → repo_management → repo_management | Set repo list for online install | Set repo list for online install | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); yum_repo is succeeded; yum_repo is not skipped |
| 1170 | 45.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → repo_management → repo_management | Enable Ansible Automation Platform repo | Enable Ansible Automation Platform repo | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); extra_repos is defined |
| 1171 | 46.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes | Automation Controller only tasks | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1172 | 46.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks | include_role | Include and run tasks from an Ansible role (role `redis`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1173 | 46.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis | role `redis` | Enter role task list | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1174 | 46.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis | file `main.yml` | Task file `ansible/automation_platform_installer/roles/redis/tasks/main.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1175 | 46.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis | include_tasks | Include a task file from the current role or playbook (file `vars.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1176 | 46.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → vars.yml | file `vars.yml` | Task file `ansible/automation_platform_installer/roles/redis/tasks/vars.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1177 | 46.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → vars.yml | include_vars | Load variables from a file | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1178 | 46.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → vars.yml | include_vars | Load variables from a file | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; install_redis_mode == "cluster" |
| 1179 | 46.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis | include_tasks | Include a task file from the current role or playbook (file `module.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1180 | 46.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → module.yml | file `module.yml` | Task file `ansible/automation_platform_installer/roles/redis/tasks/module.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1181 | 46.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → module.yml | Enable redis module stream on RHEL 8 | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; ansible_distribution_major_version \| int == 8 |
| 1182 | 46.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → module.yml → Enable redis module stream on RHEL 8 | Disable Redis module | Disable Redis module | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; ansible_distribution_major_version \| int == 8 |
| 1183 | 46.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → module.yml → Enable redis module stream on RHEL 8 | Enable Redis module | Enable Redis module | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; ansible_distribution_major_version \| int == 8 |
| 1184 | 46.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis | include_tasks | Include a task file from the current role or playbook (file `tasks.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1185 | 46.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml | file `tasks.yml` | Task file `ansible/automation_platform_installer/roles/redis/tasks/tasks.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1186 | 46.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml | Ensure redis package is installed | Ensure redis package is installed | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1187 | 46.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml | Ensure AAP directory exists | Ensure AAP directory exists | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1188 | 46.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml | Ensure redis cert directory exists | Ensure redis cert directory exists | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1189 | 46.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml | Configure TLS certificates for redis | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _redis_disable_tls \| bool |
| 1190 | 46.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → Configure TLS certificates for redis | Copy user provided SSL cert | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _redis_disable_tls \| bool; redis_tls_cert \| length |
| 1191 | 46.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → Configure TLS certificates for redis → Copy user provided SSL cert | Import specified TLS certificate | Import specified TLS certificate | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _redis_disable_tls \| bool; redis_tls_cert \| length |
| 1192 | 46.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → Configure TLS certificates for redis → Copy user provided SSL cert | Import specified TLS private key | Import specified TLS private key | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _redis_disable_tls \| bool; redis_tls_cert \| length |
| 1193 | 46.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → Configure TLS certificates for redis | Use internal CA | Use internal CA (role `certificate_authority`, tasks `sign_service.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 1194 | 46.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority | role `certificate_authority` (`sign_service.yml`) | Enter role task list | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 1195 | 46.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | file `sign_service.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/sign_service.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 1196 | 46.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | include_tasks | Include a task file from the current role or playbook (file `init.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 1197 | 47.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → init.yml | file `init.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/init.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 1198 | 47.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → init.yml | Ensure python cryptography libraries are installed | Ensure python cryptography libraries are installed | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 1199 | 47.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | Create {{ aap_ca_servicename }} private key | Create {{ aap_ca_servicename }} private key | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 1200 | 47.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | set_fact | Set a host fact used by later tasks | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 1201 | 47.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | Check if {{ aap_ca_servicename }} public key exists | Check if {{ aap_ca_servicename }} public key exists | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 1202 | 47.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _redis_disable_tls \| bool; not redis_tls_cert \| length; certificate_stat.stat.exists \| bool |
| 1203 | 47.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | Check contents of {{ aap_ca_servicename }} public key | Check contents of {{ aap_ca_servicename }} public key | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _redis_disable_tls \| bool; not redis_tls_cert \| length; certificate_stat.stat.exists \| bool |
| 1204 | 47.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _redis_disable_tls \| bool; not redis_tls_cert \| length; certificate_stat.stat.exists \| bool; aap_service_san_records_internal \| union(aap_service_san_records) \| difference(certificate_check.subject_alt_name) \| length > 0 |
| 1205 | 47.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _redis_disable_tls \| bool; not redis_tls_cert \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1206 | 47.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _redis_disable_tls \| bool; not redis_tls_cert \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false); aap_service_is_client |
| 1207 | 47.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | Create CSR for {{ aap_ca_servicename }} node/s | Create CSR for {{ aap_ca_servicename }} node/s | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _redis_disable_tls \| bool; not redis_tls_cert \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1208 | 47.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | Sign {{ aap_ca_servicename }} public key | Sign {{ aap_ca_servicename }} public key | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _redis_disable_tls \| bool; not redis_tls_cert \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1209 | 47.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | Write public key to {{ aap_ca_servicename }} node/s | Write public key to {{ aap_ca_servicename }} node/s (dest `{{ aap_service_cert_fullpath }}`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _redis_disable_tls \| bool; not redis_tls_cert \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1210 | 47.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | Signal restart if certificate pair changes | Signal restart if certificate pair changes | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _redis_disable_tls \| bool; not redis_tls_cert \| length; (write_key is changed or write_certificate is changed) |
| 1211 | 47.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → Configure TLS certificates for redis | Trigger redis notifier for internally managed certificate changes | Trigger redis notifier for internally managed certificate changes | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _redis_disable_tls \| bool; aap_service_restart \| default(false) |
| 1212 | 47.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml | Ensure redis socket directory exists | Ensure redis socket directory exists | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1213 | 47.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml | Ensure Redis socket directory has the correct SELinux context | Ensure Redis socket directory has the correct SELinux context | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1214 | 47.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml | Create tmpfiles.d entry for redis socket directory | Create tmpfiles.d entry for redis socket directory (dest `/etc/tmpfiles.d/redis.conf`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1215 | 47.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml | Install redis.conf | Install redis.conf (dest `{{ redis_conf_location }}`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1216 | 47.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml | block | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not redis_use_sockets \| bool |
| 1217 | 47.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → block | Set gateway redis credentials for clustered | Set gateway redis credentials for clustered | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not redis_use_sockets \| bool |
| 1218 | 47.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → block | Set Event-Driven Ansible redis credentials | Set Event-Driven Ansible redis credentials | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not redis_use_sockets \| bool; groups.get("automationedacontroller", []) \| length > 0 |
| 1219 | 47.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → block | Create the redis user ACL file | Create the redis user ACL file | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not redis_use_sockets \| bool |
| 1220 | 47.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → block | block | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not redis_use_sockets \| bool; install_redis_mode == "cluster" |
| 1221 | 47.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → block → block | Set redis hostname | Set redis hostname | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not redis_use_sockets \| bool; install_redis_mode == "cluster" |
| 1222 | 48.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → block → block | Map redis node hostnames | Map redis node hostnames | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not redis_use_sockets \| bool; install_redis_mode == "cluster" |
| 1223 | 48.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → block → block | Create redis nodes file | Create redis nodes file | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not redis_use_sockets \| bool; install_redis_mode == "cluster" |
| 1224 | 48.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml → block → block | Install {{ redis_init_name }} service file | Install {{ redis_init_name }} service file (dest `/lib/systemd/system/redis-platform.service`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not redis_use_sockets \| bool; install_redis_mode == "cluster" |
| 1225 | 48.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → redis → redis → tasks.yml | start redis and configure to startup automatically | start redis and configure to startup automatically (service `{{ redis_init_name }}`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1226 | 48.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks | include_role | Include and run tasks from an Ansible role (role `nginx`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1227 | 48.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → nginx | role `nginx` | Enter role task list | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1228 | 48.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → nginx → nginx | file `main.yml` | Task file `ansible/automation_platform_installer/roles/nginx/tasks/main.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1229 | 48.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → nginx → nginx | include_tasks | Include a task file from the current role or playbook (file `module.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1230 | 48.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → nginx → nginx → module.yml | file `module.yml` | Task file `ansible/automation_platform_installer/roles/nginx/tasks/module.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1231 | 48.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → nginx → nginx → module.yml | Enable nginx module stream | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1232 | 48.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → nginx → nginx → module.yml → Enable nginx module stream | Disable Nginx module | Disable Nginx module | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1233 | 48.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → nginx → nginx → module.yml → Enable nginx module stream | Enable Nginx module | Enable Nginx module | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1234 | 48.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → nginx → nginx | include_tasks | Include a task file from the current role or playbook (file `tasks.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1235 | 48.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → nginx → nginx → tasks.yml | file `tasks.yml` | Task file `ansible/automation_platform_installer/roles/nginx/tasks/tasks.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1236 | 48.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → nginx → nginx → tasks.yml | Install nginx | Install nginx | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1237 | 48.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → nginx → nginx → tasks.yml | SELinux configuration for nginx | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 1238 | 48.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → nginx → nginx → tasks.yml → SELinux configuration for nginx | Ensure libsemanage-python is installed | Ensure libsemanage-python is installed | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 1239 | 48.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → nginx → nginx → tasks.yml → SELinux configuration for nginx | Open up permissions on nginx. | Open up permissions on nginx. | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 1240 | 48.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks | Run packages_el | Run packages_el (role `packages_el`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1241 | 48.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el | role `packages_el` | Enter role task list | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1242 | 48.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el | file `main.yml` | Task file `ansible/automation_platform_installer/roles/packages_el/tasks/main.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1243 | 48.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el | include packages_el variables | include packages_el variables | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1244 | 48.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el | include_tasks | Include a task file from the current role or playbook (file `install_deps.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1245 | 48.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_deps.yml | file `install_deps.yml` | Task file `ansible/automation_platform_installer/roles/packages_el/tasks/install_deps.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1246 | 48.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_deps.yml | Install playbook dependencies | Install playbook dependencies | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1247 | 48.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_deps.yml | block | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; ansible_distribution_major_version == "8" |
| 1248 | 49.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_deps.yml → block | Disable required dnf modules | Disable required dnf modules | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; ansible_distribution_major_version == "8" |
| 1249 | 49.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_deps.yml → block | Enable required dnf modules | Enable required dnf modules | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; ansible_distribution_major_version == "8" |
| 1250 | 49.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el | include_tasks | Include a task file from the current role or playbook (file `upgrade_postgres_client.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_tower \| bool |
| 1251 | 49.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → upgrade_postgres_client.yml | file `upgrade_postgres_client.yml` | Task file `ansible/automation_platform_installer/roles/packages_el/tasks/upgrade_postgres_client.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_tower \| bool |
| 1252 | 49.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → upgrade_postgres_client.yml | Uninstall existing PostgreSQL Client | Uninstall existing PostgreSQL Client | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_tower \| bool |
| 1253 | 49.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → upgrade_postgres_client.yml | Enable PostgreSQL {{ automationcontroller_postgresql_version }} dnf modules | Enable PostgreSQL {{ automationcontroller_postgresql_version }} dnf modules | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_tower \| bool |
| 1254 | 49.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → upgrade_postgres_client.yml | Install PostgreSQL Client | Install PostgreSQL Client | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_tower \| bool |
| 1255 | 49.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el | include_tasks | Include a task file from the current role or playbook (file `install_controller.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_tower \| bool |
| 1256 | 49.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_controller.yml | file `install_controller.yml` | Task file `ansible/automation_platform_installer/roles/packages_el/tasks/install_controller.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_tower \| bool |
| 1257 | 49.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_controller.yml | Clean Yum Cache for the Automation Platform Repo | Clean Yum Cache for the Automation Platform Repo | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_tower \| bool |
| 1258 | 49.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_controller.yml | Install the Automation Controller RPM. | Install the Automation Controller RPM. | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_tower \| bool |
| 1259 | 49.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_controller.yml | Install fapolicyd rules for Automation Controller if applicable | Install fapolicyd rules for Automation Controller if applicable | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_tower \| bool; 'fapolicyd.service' in ansible_facts.services |
| 1260 | 49.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_controller.yml | Remove stale packages | Remove stale packages | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_tower \| bool |
| 1261 | 49.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_controller.yml | Update existing dependent packages | Update existing dependent packages (role `repo_management`, tasks `update_dependency`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_tower \| bool |
| 1262 | 49.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_controller.yml → repo_management | role `repo_management` (`update_dependency`) | Enter role task list | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_tower \| bool |
| 1263 | 49.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_controller.yml → repo_management → repo_management | file `update_dependency.yml` | Task file `ansible/automation_platform_installer/roles/repo_management/tasks/update_dependency.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_tower \| bool |
| 1264 | 49.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_controller.yml → repo_management → repo_management | Ensure AAP repo for update is set | Ensure AAP repo for update is set | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_tower \| bool; not aap_repo_for_update \| default('') \| length |
| 1265 | 49.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_controller.yml → repo_management → repo_management | Generate a list of AAP packages with an update | Generate a list of AAP packages with an update | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_tower \| bool |
| 1266 | 49.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_controller.yml → repo_management → repo_management | Set AAP packages to update | Set AAP packages to update | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_tower \| bool |
| 1267 | 49.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_controller.yml → repo_management → repo_management | Update packages | Update packages | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_tower \| bool; _packages_to_update \| length |
| 1268 | 49.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_controller.yml | Re-install aap-metrics-utility | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_tower \| bool; _controller_rpm_install is changed |
| 1269 | 49.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_controller.yml → Re-install aap-metrics-utility | Verify aap-metrics-utility package | Verify aap-metrics-utility package | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_tower \| bool; _controller_rpm_install is changed |
| 1270 | 49.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_controller.yml → Re-install aap-metrics-utility | Re-install aap-metrics-utility | Re-install aap-metrics-utility | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_tower \| bool; _controller_rpm_install is changed; _metrics_utility_rpm_verify is failed |
| 1271 | 49.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el | Uninstall nginx-all-modules | Uninstall nginx-all-modules | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1272 | 49.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el | include_tasks | Include a task file from the current role or playbook (file `install_postgres.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool |
| 1273 | 50.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml | file `install_postgres.yml` | Task file `ansible/automation_platform_installer/roles/packages_el/tasks/install_postgres.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool |
| 1274 | 50.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml | Check for old PostgreSQL data | Check for old PostgreSQL data | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool |
| 1275 | 50.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml | Read PG_VERSION file contents | Read PG_VERSION file contents | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; old_pg_version_file.stat.exists |
| 1276 | 50.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml | Check if PostgreSQL has already been upgraded to 15 | Check if PostgreSQL has already been upgraded to 15 | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool |
| 1277 | 50.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml | Check if PostgreSQL has already been upgraded to 13 | Check if PostgreSQL has already been upgraded to 13 | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool |
| 1278 | 50.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml | Check if PostgreSQL has already been upgraded to 12 | Check if PostgreSQL has already been upgraded to 12 | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool |
| 1279 | 50.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml | Determine if PostgreSQL 10 is present | Determine if PostgreSQL 10 is present | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool |
| 1280 | 50.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml | Determine if PostgreSQL 12 is present | Determine if PostgreSQL 12 is present | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool |
| 1281 | 50.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml | Determine if we should upgrade PostgreSQL | Determine if we should upgrade PostgreSQL | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool |
| 1282 | 50.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml | block | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; not upgrade_postgres \| bool; not upgrade_from_postgres_12 \| bool; not upgrade_from_postgres_10 \| bool |
| 1283 | 50.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → block | Uninstall old PostgreSQL Client | Uninstall old PostgreSQL Client | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; not upgrade_postgres \| bool; not upgrade_from_postgres_12 \| bool; not upgrade_from_postgres_10 \| bool; ansible_distribution_major_version == "8" |
| 1284 | 50.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → block | Enable PostgreSQL {{ automationcontroller_postgresql_version }} dnf modules | Enable PostgreSQL {{ automationcontroller_postgresql_version }} dnf modules | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; not upgrade_postgres \| bool; not upgrade_from_postgres_12 \| bool; not upgrade_from_postgres_10 \| bool |
| 1285 | 50.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → block | Install PostgreSQL | Install PostgreSQL | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; not upgrade_postgres \| bool; not upgrade_from_postgres_12 \| bool; not upgrade_from_postgres_10 \| bool |
| 1286 | 50.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml | Upgrade PostgreSQL data from 10 --> 12 | Upgrade PostgreSQL data from 10 --> 12 (file `upgrade_from_postgres_10.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_from_postgres_10 \| bool; ansible_distribution_major_version == "8" |
| 1287 | 50.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_from_postgres_10.yml | file `upgrade_from_postgres_10.yml` | Task file `ansible/automation_platform_installer/roles/packages_el/tasks/upgrade_from_postgres_10.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_from_postgres_10 \| bool; ansible_distribution_major_version == "8" |
| 1288 | 50.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_from_postgres_10.yml | Stop PostgreSQL 10 | Stop PostgreSQL 10 (service `{{ pg_systemd_name }}`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_from_postgres_10 \| bool; ansible_distribution_major_version == "8" |
| 1289 | 50.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_from_postgres_10.yml | Reset PostgreSQL module | Reset PostgreSQL module | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_from_postgres_10 \| bool; ansible_distribution_major_version == "8" |
| 1290 | 50.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_from_postgres_10.yml | Enable PostgreSQL 12 dnf modules | Enable PostgreSQL 12 dnf modules | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_from_postgres_10 \| bool; ansible_distribution_major_version == "8" |
| 1291 | 50.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_from_postgres_10.yml | Install PostgreSQL 12 | Install PostgreSQL 12 | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_from_postgres_10 \| bool; ansible_distribution_major_version == "8" |
| 1292 | 50.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_from_postgres_10.yml | Upgrade to PostgreSQL 12 | Upgrade to PostgreSQL 12 | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_from_postgres_10 \| bool; ansible_distribution_major_version == "8" |
| 1293 | 50.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_from_postgres_10.yml | Uninstall postgresql-upgrade package | Uninstall postgresql-upgrade package | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_from_postgres_10 \| bool; ansible_distribution_major_version == "8" |
| 1294 | 50.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_from_postgres_10.yml | Create file to signify PostgreSQL has been upgraded | Create file to signify PostgreSQL has been upgraded | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_from_postgres_10 \| bool; ansible_distribution_major_version == "8" |
| 1295 | 50.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml | Upgrade PostgreSQL data from 12 --> 13 | Upgrade PostgreSQL data from 12 --> 13 (file `upgrade_postgres_from_12.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_from_postgres_12 \| bool; ansible_distribution_major_version == "8" |
| 1296 | 50.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_12.yml | file `upgrade_postgres_from_12.yml` | Task file `ansible/automation_platform_installer/roles/packages_el/tasks/upgrade_postgres_from_12.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_from_postgres_12 \| bool; ansible_distribution_major_version == "8" |
| 1297 | 50.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_12.yml | Stop PostgreSQL 12 | Stop PostgreSQL 12 (service `{{ pg_systemd_name }}`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_from_postgres_12 \| bool; ansible_distribution_major_version == "8" |
| 1298 | 50.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_12.yml | Reset PostgreSQL module | Reset PostgreSQL module | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_from_postgres_12 \| bool; ansible_distribution_major_version == "8" |
| 1299 | 51.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_12.yml | Enable PostgreSQL 13 dnf modules | Enable PostgreSQL 13 dnf modules | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_from_postgres_12 \| bool; ansible_distribution_major_version == "8" |
| 1300 | 51.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_12.yml | Install PostgreSQL 13 | Install PostgreSQL 13 | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_from_postgres_12 \| bool; ansible_distribution_major_version == "8" |
| 1301 | 51.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_12.yml | Upgrade to PostgreSQL 13 | Upgrade to PostgreSQL 13 | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_from_postgres_12 \| bool; ansible_distribution_major_version == "8" |
| 1302 | 51.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_12.yml | Uninstall postgresql-upgrade package | Uninstall postgresql-upgrade package | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_from_postgres_12 \| bool; ansible_distribution_major_version == "8" |
| 1303 | 51.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_12.yml | include_tasks | Include a task file from the current role or playbook (file `install_deps.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_from_postgres_12 \| bool; ansible_distribution_major_version == "8" |
| 1304 | 51.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_12.yml → install_deps.yml | file `install_deps.yml` | Task file `ansible/automation_platform_installer/roles/packages_el/tasks/install_deps.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_from_postgres_12 \| bool; ansible_distribution_major_version == "8" |
| 1305 | 51.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_12.yml → install_deps.yml | Install playbook dependencies | Install playbook dependencies | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_from_postgres_12 \| bool; ansible_distribution_major_version == "8" |
| 1306 | 51.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_12.yml → install_deps.yml | block | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_from_postgres_12 \| bool; ansible_distribution_major_version == "8"; ansible_distribution_major_version == "8" |
| 1307 | 51.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_12.yml → install_deps.yml → block | Disable required dnf modules | Disable required dnf modules | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_from_postgres_12 \| bool; ansible_distribution_major_version == "8"; ansible_distribution_major_version == "8" |
| 1308 | 51.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_12.yml → install_deps.yml → block | Enable required dnf modules | Enable required dnf modules | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_from_postgres_12 \| bool; ansible_distribution_major_version == "8"; ansible_distribution_major_version == "8" |
| 1309 | 51.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_12.yml | Create file to signify PostgreSQL has been upgraded | Create file to signify PostgreSQL has been upgraded | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_from_postgres_12 \| bool; ansible_distribution_major_version == "8" |
| 1310 | 51.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml | Upgrade PostgreSQL data from 13 --> 15 | Upgrade PostgreSQL data from 13 --> 15 (file `upgrade_postgres_from_13.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 1311 | 51.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml | file `upgrade_postgres_from_13.yml` | Task file `ansible/automation_platform_installer/roles/packages_el/tasks/upgrade_postgres_from_13.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 1312 | 51.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml | Stop PostgreSQL 13 | Stop PostgreSQL 13 (service `{{ pg_systemd_name }}`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 1313 | 51.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml | Reset PostgreSQL module | Reset PostgreSQL module | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 1314 | 51.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml | Enable PostgreSQL 15 dnf modules | Enable PostgreSQL 15 dnf modules | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 1315 | 51.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml | Install PostgreSQL 15 | Install PostgreSQL 15 | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 1316 | 51.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml | Upgrade to PostgreSQL 15 | Upgrade to PostgreSQL 15 | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 1317 | 51.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml | Uninstall postgresql-upgrade package | Uninstall postgresql-upgrade package | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 1318 | 51.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml | include_tasks | Include a task file from the current role or playbook (file `install_deps.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 1319 | 51.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml → install_deps.yml | file `install_deps.yml` | Task file `ansible/automation_platform_installer/roles/packages_el/tasks/install_deps.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 1320 | 51.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml → install_deps.yml | Install playbook dependencies | Install playbook dependencies | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 1321 | 51.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml → install_deps.yml | block | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_postgres \| bool; ansible_distribution_major_version == "8" |
| 1322 | 51.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml → install_deps.yml → block | Disable required dnf modules | Disable required dnf modules | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_postgres \| bool; ansible_distribution_major_version == "8" |
| 1323 | 51.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml → install_deps.yml → block | Enable required dnf modules | Enable required dnf modules | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_postgres \| bool; ansible_distribution_major_version == "8" |
| 1324 | 52.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → packages_el → packages_el → install_postgres.yml → upgrade_postgres_from_13.yml | Create file to signify PostgreSQL has been upgraded | Create file to signify PostgreSQL has been upgraded | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; packages_el_install_postgres \| bool; upgrade_postgres \| bool |
| 1325 | 52.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks | meta | Ansible meta task (flush handlers, clear facts, etc.) (flush_handlers) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1326 | 52.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks | include_role | Include and run tasks from an Ansible role (role `rsyslog`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1327 | 52.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → rsyslog | role `rsyslog` | Enter role task list | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1328 | 52.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → rsyslog → rsyslog | file `main.yml` | Task file `ansible/automation_platform_installer/roles/rsyslog/tasks/main.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1329 | 52.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → rsyslog → rsyslog | Configure the Rsyslog socket directory. | Configure the Rsyslog socket directory. | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1330 | 52.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → rsyslog → rsyslog | Create tmpfiles.d entry for rsyslog socket directory | Create tmpfiles.d entry for rsyslog socket directory (dest `/etc/tmpfiles.d/rsyslog.conf`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1331 | 52.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks | include_role | Include and run tasks from an Ansible role (role `automationcontroller`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1332 | 52.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller | role `automationcontroller` | Enter role task list | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1333 | 52.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller | file `main.yml` | Task file `ansible/automation_platform_installer/roles/automationcontroller/tasks/main.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1334 | 52.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller | include_tasks | Include a task file from the current role or playbook (file `tasks.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1335 | 52.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | file `tasks.yml` | Task file `ansible/automation_platform_installer/roles/automationcontroller/tasks/tasks.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1336 | 52.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | add awx to the redis group so it can access the socket | add awx to the redis group so it can access the socket | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1337 | 52.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Configure the settings directory. | Configure the settings directory. | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1338 | 52.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Configure the socket directory. | Configure the socket directory. | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1339 | 52.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Configure the socket directory for tmpfiles.d | Configure the socket directory for tmpfiles.d (dest `/etc/tmpfiles.d/tower.conf`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1340 | 52.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Configure the system user's home directory. | Configure the system user's home directory. | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1341 | 52.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Configure the system user's .ssh directory. | Configure the system user's .ssh directory. | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1342 | 52.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Configure the projects directory. | Configure the projects directory. | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1343 | 52.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Configure the job output directory. | Configure the job output directory. | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1344 | 52.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Configure the static directory. | Configure the static directory. | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1345 | 52.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Add `conf.d` directory. | Add `conf.d` directory. | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1346 | 52.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Configure the log directory. | Configure the log directory. | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1347 | 52.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Update awx_supervisord logrotate config file | Update awx_supervisord logrotate config file (dest `/etc/logrotate.d/awx_supervisord`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1348 | 52.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Update hourly cron logrotate invocation | Update hourly cron logrotate invocation (dest `/etc/cron.hourly/logrotate`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1349 | 52.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Update automation-controller-service configuration | Update automation-controller-service configuration (dest `/etc/sysconfig/automation-controller`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1350 | 53.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Update automation-controller service configuration | Update automation-controller service configuration (dest `/etc/systemd/system/automation-controller.service`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1351 | 53.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Update automation-controller service configuration (postgresql) | Update automation-controller service configuration (postgresql) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1352 | 53.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Update dependent service configuration | Update dependent service configuration | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1353 | 53.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Update dependent service configuration | Update dependent service configuration (dest `/etc/systemd/system/{{ item }}.service.d/override.conf`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1354 | 53.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Reload systemd configuration | Reload systemd configuration | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1355 | 53.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Enable the automation-controller service | Enable the automation-controller service (service `automation-controller`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1356 | 53.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Check if this is a fresh installation | Check if this is a fresh installation | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1357 | 53.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Replace the secret key if secret_key_override is defined and installation is new | Replace the secret key if secret_key_override is defined and installation is new (dest `/etc/tower/SECRET_KEY`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; inventory_hostname == groups['automationcontroller'][0]; secret_key_override is defined; previous_install_status.stat.exists == false |
| 1358 | 53.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Fetch the secret key from the primary node. | Fetch the secret key from the primary node. | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; inventory_hostname == groups['automationcontroller'][0] |
| 1359 | 53.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Fail play when grabbing SECRET_KEY fails | Fail play when grabbing SECRET_KEY fails | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; hostvars[groups['automationcontroller'][0]]['secret_key'].content is not defined |
| 1360 | 53.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Place the secret key on secondary nodes. | Place the secret key on secondary nodes. (dest `/etc/tower/SECRET_KEY`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; inventory_hostname != groups['automationcontroller'][0] |
| 1361 | 53.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | block | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not automationcontroller_db_template \| default([]) \| length |
| 1362 | 53.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml → block | Get database settings | Get database settings | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not automationcontroller_db_template \| default([]) \| length |
| 1363 | 53.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml → block | Ingest database settings | Ingest database settings | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not automationcontroller_db_template \| default([]) \| length; results.rc == 0 |
| 1364 | 53.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml → block | Set database vars | Set database vars | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not automationcontroller_db_template \| default([]) \| length; results.rc != 0 |
| 1365 | 53.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml → block | Set database vars | Set database vars | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not automationcontroller_db_template \| default([]) \| length; results.rc == 0 |
| 1366 | 53.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml → block | Set database template using default password | Set database template using default password | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not automationcontroller_db_template \| default([]) \| length |
| 1367 | 53.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Configure postgres client certs | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; awx_pg_cert_auth |
| 1368 | 53.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml → Configure postgres client certs | Copy postgres client cert | Copy postgres client cert (dest `/etc/tower/pg_client.crt`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; awx_pg_cert_auth |
| 1369 | 53.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml → Configure postgres client certs | Copy postgres client key | Copy postgres client key (dest `/etc/tower/pg_client.key`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; awx_pg_cert_auth |
| 1370 | 53.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Configure database settings. | Configure database settings. (dest `/etc/tower/conf.d/postgres.py`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1371 | 53.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Handles /etc/tower/conf.d/channels.py | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; inventory_hostname == groups['automationcontroller'][0] |
| 1372 | 53.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml → Handles /etc/tower/conf.d/channels.py | Check for existance of /etc/tower/conf.d/channels.py | Check for existance of /etc/tower/conf.d/channels.py | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; inventory_hostname == groups['automationcontroller'][0] |
| 1373 | 53.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml → Handles /etc/tower/conf.d/channels.py | Generate broadcast websocket secret when /etc/tower/conf.d/channels.py is not present | Generate broadcast websocket secret when /etc/tower/conf.d/channels.py is not present | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; inventory_hostname == groups['automationcontroller'][0]; not channels_py.stat.exists |
| 1374 | 53.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml → Handles /etc/tower/conf.d/channels.py | Deploy /etc/tower/conf.d/channels.py | Deploy /etc/tower/conf.d/channels.py (dest `/etc/tower/conf.d/channels.py`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; inventory_hostname == groups['automationcontroller'][0]; channels_py.stat.exists == false and broadcast_websocket_secret is defined |
| 1375 | 54.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml → Handles /etc/tower/conf.d/channels.py | Slurp /etc/tower/conf.d/channels.py | Slurp /etc/tower/conf.d/channels.py | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; inventory_hostname == groups['automationcontroller'][0] |
| 1376 | 54.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Sync /etc/tower/conf.d/channels.py to all '[automationcontroller]' nodes | Sync /etc/tower/conf.d/channels.py to all '[automationcontroller]' nodes (dest `/etc/tower/conf.d/channels.py`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; groups['automationcontroller'] \| default([]) \| length; hostvars[groups['automationcontroller'][0]]['channels_py_override'] \| default([]) \| length; inventory_hostname in groups['automationcontroller']; inventory_hostname != groups['automationcontroller'][0] |
| 1377 | 54.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Configure the Insights URL | Configure the Insights URL (dest `/etc/tower/conf.d/insights.py`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1378 | 54.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Configure Subscription usage model | Configure Subscription usage model (dest `/etc/tower/conf.d/subscription_usage_model.py`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1379 | 54.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Initialize instance groups for host | Initialize instance groups for host | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1380 | 54.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Determine instance groups for host | Determine instance groups for host | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; item is match("(^automationcontroller\|^instance_group_.+)$") |
| 1381 | 54.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Configure CLUSTER_HOST_ID setting | Configure CLUSTER_HOST_ID setting (dest `/etc/tower/conf.d/cluster_host_id.py`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1382 | 54.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Remove memcached settings | Remove memcached settings | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1383 | 54.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Install settings file if it does not already exist. | Install settings file if it does not already exist. (dest `/etc/tower/settings.py`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1384 | 54.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Configure callback receiver workers | Configure callback receiver workers (dest `/etc/tower/conf.d/callback_receiver_workers.py`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; ansible_processor_vcpus > 4 |
| 1385 | 54.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Generate gateway secret | Generate gateway secret (role `automationgateway`, tasks `service_token`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1386 | 54.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml → automationgateway | role `automationgateway` (`service_token`) | Enter role task list | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1387 | 54.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml → automationgateway → automationgateway | file `service_token.yml` | Task file `ansible/automation_platform_installer/roles/automationgateway/tasks/service_token.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1388 | 54.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml → automationgateway → automationgateway | Generate service secret | Generate service secret | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1389 | 54.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Configure automation gateway | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; groups.get('automationgateway', []) \| length > 0 |
| 1390 | 54.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml → Configure automation gateway | template | Render a Jinja2 template to a file on the host (dest `/etc/tower/conf.d/gateway.py`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; groups.get('automationgateway', []) \| length > 0 |
| 1391 | 54.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Check for pending migrations | Check for pending migrations | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; inventory_hostname == groups['automationcontroller'][0] |
| 1392 | 54.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Handle the DB migration | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; hostvars[groups['automationcontroller'][0]]['pending_db_migration'] is defined; hostvars[groups['automationcontroller'][0]]['pending_db_migration'].stdout\|int != 0 |
| 1393 | 54.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml → Handle the DB migration | Stop supervisor due to database schema upgrade | Stop supervisor due to database schema upgrade (service `supervisord`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; hostvars[groups['automationcontroller'][0]]['pending_db_migration'] is defined; hostvars[groups['automationcontroller'][0]]['pending_db_migration'].stdout\|int != 0 |
| 1394 | 54.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml → Handle the DB migration | Wait for supervisor to stop. | Wait for supervisor to stop. | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; hostvars[groups['automationcontroller'][0]]['pending_db_migration'] is defined; hostvars[groups['automationcontroller'][0]]['pending_db_migration'].stdout\|int != 0 |
| 1395 | 54.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml → Handle the DB migration | Migrate the database schema (may take awhile when upgrading). | Migrate the database schema (may take awhile when upgrading). | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; hostvars[groups['automationcontroller'][0]]['pending_db_migration'] is defined; hostvars[groups['automationcontroller'][0]]['pending_db_migration'].stdout\|int != 0; inventory_hostname == groups['automationcontroller'][0] |
| 1396 | 54.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Handle the DB migration (rescue) | Run rescue tasks for block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; hostvars[groups['automationcontroller'][0]]['pending_db_migration'] is defined; hostvars[groups['automationcontroller'][0]]['pending_db_migration'].stdout\|int != 0 |
| 1397 | 54.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml → Handle the DB migration (rescue) | Database Migration failed, please check the suggest KCS | Database Migration failed, please check the suggest KCS | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; hostvars[groups['automationcontroller'][0]]['pending_db_migration'] is defined; hostvars[groups['automationcontroller'][0]]['pending_db_migration'].stdout\|int != 0; tower_migrate is defined; inventory_hostname == groups['automationcontroller'][0]; 'cryptography.fernet.InvalidToken' in tower_migrate.stderr |
| 1398 | 54.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Checking for RBAC migration log | Checking for RBAC migration log | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1399 | 54.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Moving RBAC migration log | Moving RBAC migration log | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; rbac_log.stat.exists |
| 1400 | 54.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Get the controller version | Get the controller version | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1401 | 55.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Store the controller version | Store the controller version (dest `/var/lib/awx/.tower_version`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1402 | 55.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Get configuration files to set permissions on | Get configuration files to set permissions on | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1403 | 55.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Clean up any poorly-permissioned configuration files on upgrade | Clean up any poorly-permissioned configuration files on upgrade | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1404 | 55.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Set ACL rule for awx user to access rhsm.conf | Set ACL rule for awx user to access rhsm.conf | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1405 | 55.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Check for super users | Check for super users | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; inventory_hostname == groups['automationcontroller'][0] |
| 1406 | 55.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | create django super user if it does not exist | create django super user if it does not exist | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; inventory_hostname == groups['automationcontroller'][0] and super_check.rc > 0 |
| 1407 | 55.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | update django super user password | update django super user password | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; awx_install_admin_password != '' and inventory_hostname == groups['automationcontroller'][0] |
| 1408 | 55.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Collect static files. | Collect static files. | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1409 | 55.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Fixup log directory permissions post-migration | Fixup log directory permissions post-migration | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1410 | 55.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → tasks.yml | Copy Candlepin RHSM Certificate | Copy Candlepin RHSM Certificate (dest `/etc/tower/candlepin-redhat-ca.crt`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1411 | 55.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller | include_tasks | Include a task file from the current role or playbook (file `nginx.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1412 | 55.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml | file `nginx.yml` | Task file `ansible/automation_platform_installer/roles/automationcontroller/tasks/nginx.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1413 | 55.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml | Ensure awx is in the nginx group | Ensure awx is in the nginx group | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1414 | 55.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml | Get static root | Get static root | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1415 | 55.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml | Find custom error pages | Find custom error pages | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1416 | 55.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml | Set custom error pages | Set custom error pages | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1417 | 55.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml | Get base directory | Get base directory | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1418 | 55.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml | Copy index.html | Copy index.html | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1419 | 55.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml | Copy logo file | Copy logo file | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1420 | 55.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml | block | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool |
| 1421 | 55.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml → block | Copy user-provided SSL certificates, if we have them | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool; web_server_ssl_cert is defined |
| 1422 | 55.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml → block → Copy user-provided SSL certificates, if we have them | Copy SSL certificate | Copy SSL certificate (dest `/etc/tower/tower.cert`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool; web_server_ssl_cert is defined |
| 1423 | 55.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml → block → Copy user-provided SSL certificates, if we have them | Copy SSL private key | Copy SSL private key (dest `/etc/tower/tower.key`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool; web_server_ssl_cert is defined |
| 1424 | 55.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml → block | Use internal CA | Use internal CA (role `certificate_authority`, tasks `sign_service.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool; web_server_ssl_cert is not defined |
| 1425 | 55.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml → block → certificate_authority | role `certificate_authority` (`sign_service.yml`) | Enter role task list | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool; web_server_ssl_cert is not defined |
| 1426 | 56.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml → block → certificate_authority → certificate_authority | file `sign_service.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/sign_service.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool; web_server_ssl_cert is not defined |
| 1427 | 56.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml → block → certificate_authority → certificate_authority | include_tasks | Include a task file from the current role or playbook (file `init.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool; web_server_ssl_cert is not defined |
| 1428 | 56.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml → block → certificate_authority → certificate_authority → init.yml | file `init.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/init.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool; web_server_ssl_cert is not defined |
| 1429 | 56.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml → block → certificate_authority → certificate_authority → init.yml | Ensure python cryptography libraries are installed | Ensure python cryptography libraries are installed | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool; web_server_ssl_cert is not defined |
| 1430 | 56.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml → block → certificate_authority → certificate_authority | Create {{ aap_ca_servicename }} private key | Create {{ aap_ca_servicename }} private key | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool; web_server_ssl_cert is not defined |
| 1431 | 56.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml → block → certificate_authority → certificate_authority | set_fact | Set a host fact used by later tasks | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool; web_server_ssl_cert is not defined |
| 1432 | 56.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml → block → certificate_authority → certificate_authority | Check if {{ aap_ca_servicename }} public key exists | Check if {{ aap_ca_servicename }} public key exists | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool; web_server_ssl_cert is not defined |
| 1433 | 56.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml → block → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool; web_server_ssl_cert is not defined; certificate_stat.stat.exists \| bool |
| 1434 | 56.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml → block → certificate_authority → certificate_authority → block | Check contents of {{ aap_ca_servicename }} public key | Check contents of {{ aap_ca_servicename }} public key | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool; web_server_ssl_cert is not defined; certificate_stat.stat.exists \| bool |
| 1435 | 56.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml → block → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool; web_server_ssl_cert is not defined; certificate_stat.stat.exists \| bool; aap_service_san_records_internal \| union(aap_service_san_records) \| difference(certificate_check.subject_alt_name) \| length > 0 |
| 1436 | 56.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml → block → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool; web_server_ssl_cert is not defined; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1437 | 56.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml → block → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool; web_server_ssl_cert is not defined; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false); aap_service_is_client |
| 1438 | 56.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml → block → certificate_authority → certificate_authority → block | Create CSR for {{ aap_ca_servicename }} node/s | Create CSR for {{ aap_ca_servicename }} node/s | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool; web_server_ssl_cert is not defined; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1439 | 56.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml → block → certificate_authority → certificate_authority → block | Sign {{ aap_ca_servicename }} public key | Sign {{ aap_ca_servicename }} public key | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool; web_server_ssl_cert is not defined; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1440 | 56.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml → block → certificate_authority → certificate_authority → block | Write public key to {{ aap_ca_servicename }} node/s | Write public key to {{ aap_ca_servicename }} node/s (dest `{{ aap_service_cert_fullpath }}`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool; web_server_ssl_cert is not defined; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1441 | 56.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml → block → certificate_authority → certificate_authority | Signal restart if certificate pair changes | Signal restart if certificate pair changes | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool; web_server_ssl_cert is not defined; (write_key is changed or write_certificate is changed) |
| 1442 | 56.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml → block | Trigger nginx notifier for internally managed certificate changes | Trigger nginx notifier for internally managed certificate changes | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool; aap_service_webserver_restart \| default(False) |
| 1443 | 56.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml → block | set_fact | Set a host fact used by later tasks | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not _nginx_disable_https \| bool |
| 1444 | 56.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → automationcontroller → automationcontroller → nginx.yml | Add automation controller nginx configuration | Add automation controller nginx configuration | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1445 | 56.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks | include_role | Include and run tasks from an Ansible role (role `supervisor`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1446 | 56.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → supervisor | role `supervisor` | Enter role task list | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1447 | 56.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → supervisor → supervisor | file `main.yml` | Task file `ansible/automation_platform_installer/roles/supervisor/tasks/main.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1448 | 56.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → supervisor → supervisor | include_tasks | Include a task file from the current role or playbook (file `vars.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1449 | 56.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → supervisor → supervisor → vars.yml | file `vars.yml` | Task file `ansible/automation_platform_installer/roles/supervisor/tasks/vars.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1450 | 56.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → supervisor → supervisor → vars.yml | include supervisor variables | include supervisor variables | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1451 | 57.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → supervisor → supervisor | include_tasks | Include a task file from the current role or playbook (file `tasks.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not supervisor_exec_vars_only \| bool |
| 1452 | 57.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → supervisor → supervisor → tasks.yml | file `tasks.yml` | Task file `ansible/automation_platform_installer/roles/supervisor/tasks/tasks.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not supervisor_exec_vars_only \| bool |
| 1453 | 57.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → supervisor → supervisor → tasks.yml | check current somaxconn value | check current somaxconn value | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not supervisor_exec_vars_only \| bool |
| 1454 | 57.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → supervisor → supervisor → tasks.yml | check current net.ipv4.tcp_max_syn_backlog value | check current net.ipv4.tcp_max_syn_backlog value | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not supervisor_exec_vars_only \| bool |
| 1455 | 57.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → supervisor → supervisor → tasks.yml | Set somaxconn to value greater than or equal to uwsgi_listen_queue_size in supervisor config | Set somaxconn to value greater than or equal to uwsgi_listen_queue_size in supervisor config | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not supervisor_exec_vars_only \| bool; (uwsgi_listen_queue_size\|int) > (somaxconn['content'] \| b64decode \| int) |
| 1456 | 57.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → supervisor → supervisor → tasks.yml | Set tcp_max_syn_backlog to value greater than or equal to uwsgi_listen_queue_size in supervisor config | Set tcp_max_syn_backlog to value greater than or equal to uwsgi_listen_queue_size in supervisor config | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not supervisor_exec_vars_only \| bool; (uwsgi_listen_queue_size\|int) > (tcp_max_syn_backlog['content'] \| b64decode \| int) |
| 1457 | 57.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → supervisor → supervisor → tasks.yml | install uwsgi config | install uwsgi config (dest `/etc/tower/uwsgi.ini`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not supervisor_exec_vars_only \| bool |
| 1458 | 57.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → supervisor → supervisor → tasks.yml | Enforce libffi shared memory | Enforce libffi shared memory | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not supervisor_exec_vars_only \| bool |
| 1459 | 57.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → supervisor → supervisor → tasks.yml | install supervisor config | install supervisor config (dest `/etc/supervisord.d/tower.ini`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not supervisor_exec_vars_only \| bool |
| 1460 | 57.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → supervisor → supervisor → tasks.yml | Override supervisord logrotate rules | Override supervisord logrotate rules (dest `/etc/logrotate.d/supervisor`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not supervisor_exec_vars_only \| bool |
| 1461 | 57.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → supervisor → supervisor → tasks.yml | Recreate systemd tmp files | Recreate systemd tmp files | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not supervisor_exec_vars_only \| bool |
| 1462 | 57.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → supervisor → supervisor → tasks.yml | start/enable supervisord process | start/enable supervisord process (service `supervisord`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not supervisor_exec_vars_only \| bool |
| 1463 | 57.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → supervisor → supervisor → tasks.yml | Wait for the supervisor socket | Wait for the supervisor socket | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not supervisor_exec_vars_only \| bool |
| 1464 | 57.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks | include_role | Include and run tasks from an Ansible role (role `nginx`, tasks `finalize.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1465 | 57.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → nginx | role `nginx` (`finalize.yml`) | Enter role task list | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1466 | 57.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → nginx → nginx | file `finalize.yml` | Task file `ansible/automation_platform_installer/roles/nginx/tasks/finalize.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1467 | 57.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → nginx → nginx | Install nginx.conf | Install nginx.conf (dest `/etc/nginx/nginx.conf`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1468 | 57.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → nginx → nginx | Start nginx and configure to startup automatically | Start nginx and configure to startup automatically (service `nginx`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1469 | 57.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks | include_role | Include and run tasks from an Ansible role (role `misc`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1470 | 57.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → misc | role `misc` | Enter role task list | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1471 | 57.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → misc → misc | file `main.yml` | Task file `ansible/automation_platform_installer/roles/misc/tasks/main.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1472 | 57.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → misc → misc | Remove the old celeryd.py config file (if it exists) | Remove the old celeryd.py config file (if it exists) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1473 | 57.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → misc → misc | Check if /etc/tower/conf.d/ha.py exists | Check if /etc/tower/conf.d/ha.py exists | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1474 | 57.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → misc → misc | If Ansible does not provide a system UUID, get a random one. | If Ansible does not provide a system UUID, get a random one. | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; ha_py.failed \| bool |
| 1475 | 57.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → misc → misc | Register the uuid from the command line | Register the uuid from the command line | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; ha_py.failed \| bool and generated_uuid is defined |
| 1476 | 57.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → misc → misc | Read the uuid from /etc/tower/conf.d/ha.py | Read the uuid from /etc/tower/conf.d/ha.py | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not ha_py.failed \| bool |
| 1477 | 58.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → misc → misc | Store uuid from /etc/tower/conf.d/ha.py | Store uuid from /etc/tower/conf.d/ha.py | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; not ha_py.failed \| bool; ha_py_content is defined |
| 1478 | 58.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → misc → misc | Write a ha.conf file to /etc/tower that contains the system UUID. | Write a ha.conf file to /etc/tower that contains the system UUID. (dest `/etc/tower/conf.d/ha.py`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; ha_py.failed \| bool |
| 1479 | 58.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → misc → misc | Check if tower queue is present | Check if tower queue is present | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller'] |
| 1480 | 58.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → Automation Controller only tasks → misc → misc | Unregister tower queue | Unregister tower queue | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['automationcontroller']; '[tower capacity=' in register_tower.stdout |
| 1481 | 58.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes | Ensure ansible-automation-platform-common exists on Execution Nodes | Ensure ansible-automation-platform-common exists on Execution Nodes | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); inventory_hostname in groups['execution_nodes'] \| default([]) |
| 1482 | 58.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes | include_role | Include and run tasks from an Ansible role (role `receptor`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1483 | 58.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor | role `receptor` | Enter role task list | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1484 | 58.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | file `main.yml` | Task file `ansible/automation_platform_installer/roles/receptor/tasks/main.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1485 | 58.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Assert Receptor Network Mesh data | Assert Receptor Network Mesh data (file `preflight.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined |
| 1486 | 58.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml | file `preflight.yml` | Task file `ansible/automation_platform_installer/roles/receptor/tasks/preflight.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined |
| 1487 | 58.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml | Include calculate_mesh | Include calculate_mesh (file `calculate_mesh.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined |
| 1488 | 58.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml → calculate_mesh.yml | file `calculate_mesh.yml` | Task file `ansible/automation_platform_installer/roles/receptor/tasks/calculate_mesh.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined |
| 1489 | 58.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml → calculate_mesh.yml | Calculate Mesh Topology | Calculate Mesh Topology | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined |
| 1490 | 58.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml → calculate_mesh.yml | Review Mesh dot graph topology | Review Mesh dot graph topology | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined; generate_dot_file \| default([]) \| length |
| 1491 | 58.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml → calculate_mesh.yml | Store Receptor Mesh metadata | Store Receptor Mesh metadata | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined |
| 1492 | 58.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml → calculate_mesh.yml | Store Receptor Deprovision Mesh metadata | Store Receptor Deprovision Mesh metadata | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined |
| 1493 | 58.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml → calculate_mesh.yml | Define receptor mesh facts | Define receptor mesh facts | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined; mesh is defined; mesh.keys() \| default([]) \| length; inventory_hostname in mesh.keys() |
| 1494 | 58.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml → calculate_mesh.yml | Define receptor deprovision mesh facts | Define receptor deprovision mesh facts | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined; deprovision_mesh is defined; deprovision_mesh.keys() \| default([]) \| length; inventory_hostname in deprovision_mesh.keys() |
| 1495 | 58.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml | Include DNS asserts | Include DNS asserts (file `assert_dns.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined |
| 1496 | 58.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml → assert_dns.yml | file `assert_dns.yml` | Task file `ansible/automation_platform_installer/roles/receptor/tasks/assert_dns.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined |
| 1497 | 58.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml → assert_dns.yml | Validate if receptor peer name is DNS resolvable | Validate if receptor peer name is DNS resolvable | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined; item \| default([]) \| length; hostvars[item]['node_state'] == 'active'; hostvars[item]['receptor_host_identifier'] \| default([]) \| length; hostvars[item]['receptor_host_identifier'] \| regex_search(regexp_ip) == '' or hostvars[item]['receptor_host_identifier'] \| regex_search(regexp_ip) == None |
| 1498 | 58.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml → assert_dns.yml | Report error when a receptor node cannot resolve a connected peer name | Report error when a receptor node cannot resolve a connected peer name | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined; _getent.failed \| default(False) \| bool; item.rc != 0 |
| 1499 | 58.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml | Include SSL asserts | Include SSL asserts (file `assert_ssl.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined |
| 1500 | 58.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml → assert_ssl.yml | file `assert_ssl.yml` | Task file `ansible/automation_platform_installer/roles/receptor/tasks/assert_ssl.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined |
| 1501 | 58.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml → assert_ssl.yml | Ensure custom SSL Certificate Authority files were provided | Ensure custom SSL Certificate Authority files were provided | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined; mesh_ca_certfile is defined or mesh_ca_keyfile is defined |
| 1502 | 59.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml → assert_ssl.yml | Ensure custom SSL Certificate Authority files matches | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined; mesh_ca_certfile is defined; mesh_ca_keyfile is defined |
| 1503 | 59.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml → assert_ssl.yml → Ensure custom SSL Certificate Authority files matches | Verify custom node SSL private key | Verify custom node SSL private key | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined; mesh_ca_certfile is defined; mesh_ca_keyfile is defined |
| 1504 | 59.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml → assert_ssl.yml → Ensure custom SSL Certificate Authority files matches | Verify custom node SSL x509 key | Verify custom node SSL x509 key | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined; mesh_ca_certfile is defined; mesh_ca_keyfile is defined |
| 1505 | 59.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml → assert_ssl.yml → Ensure custom SSL Certificate Authority files matches | Ensure custom SSL pair matches | Ensure custom SSL pair matches | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined; mesh_ca_certfile is defined; mesh_ca_keyfile is defined |
| 1506 | 59.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml → assert_ssl.yml | Ensure custom node SSL files are valid when provided | Ensure custom node SSL files are valid when provided | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined; mesh_node_certfile is defined or mesh_node_keyfile is defined |
| 1507 | 59.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml → assert_ssl.yml | Ensure custom node SSL files matches | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined; mesh_node_certfile is defined; mesh_node_keyfile is defined |
| 1508 | 59.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml → assert_ssl.yml → Ensure custom node SSL files matches | Verify custom node SSL private key | Verify custom node SSL private key | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined; mesh_node_certfile is defined; mesh_node_keyfile is defined |
| 1509 | 59.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml → assert_ssl.yml → Ensure custom node SSL files matches | Verify custom node SSL x509 key | Verify custom node SSL x509 key | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined; mesh_node_certfile is defined; mesh_node_keyfile is defined |
| 1510 | 59.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml → assert_ssl.yml → Ensure custom node SSL files matches | Ensure custom SSL pair matches | Ensure custom SSL pair matches | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined; mesh_node_certfile is defined; mesh_node_keyfile is defined |
| 1511 | 59.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → preflight.yml → assert_ssl.yml → Ensure custom node SSL files matches | Ensure x509 certificate was signed by the expected Certificate Authority | Ensure x509 certificate was signed by the expected Certificate Authority | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh is not defined; mesh_node_certfile is defined; mesh_node_keyfile is defined |
| 1512 | 59.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Install receptor | Install receptor | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1513 | 59.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Install receptor dependencies | Install receptor dependencies | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_workable_dependencies is defined; node_type in receptor_workable_types |
| 1514 | 59.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Install ansible-runner for execution plane | Install ansible-runner for execution plane | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type == "execution" |
| 1515 | 59.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Update existing dependent packages | Update existing dependent packages (role `repo_management`, tasks `update_dependency`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1516 | 59.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → repo_management | role `repo_management` (`update_dependency`) | Enter role task list | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1517 | 59.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → repo_management → repo_management | file `update_dependency.yml` | Task file `ansible/automation_platform_installer/roles/repo_management/tasks/update_dependency.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1518 | 59.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → repo_management → repo_management | Ensure AAP repo for update is set | Ensure AAP repo for update is set | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); not aap_repo_for_update \| default('') \| length |
| 1519 | 59.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → repo_management → repo_management | Generate a list of AAP packages with an update | Generate a list of AAP packages with an update | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1520 | 59.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → repo_management → repo_management | Set AAP packages to update | Set AAP packages to update | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1521 | 59.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → repo_management → repo_management | Update packages | Update packages | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); _packages_to_update \| length |
| 1522 | 59.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Invoke Receptor TLS | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool |
| 1523 | 59.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS | Reconfigure Receptor CA (forced) | Reconfigure Receptor CA (forced) (dest `{{ receptor_tls_dir }}`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; (receptor_tls_reconfigure \| bool) or (mesh_ca_certfile \| default('') \| length) or (mesh_ca_keyfile \| default('') \| length) |
| 1524 | 59.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS | Create Receptor CA directory | Create Receptor CA directory (dest `{{ receptor_tls_ca_dir }}`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool |
| 1525 | 59.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS | Configure Receptor Certificate Authority | Configure Receptor Certificate Authority (file `tls_ca.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; groups['automationcontroller'] \| default([]) \| length; inventory_hostname == groups['automationcontroller'][0] |
| 1526 | 59.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca.yml | file `tls_ca.yml` | Task file `ansible/automation_platform_installer/roles/receptor/tasks/tls_ca.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; groups['automationcontroller'] \| default([]) \| length; inventory_hostname == groups['automationcontroller'][0] |
| 1527 | 59.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca.yml | Copy custom SSL Certificate Authority files | Copy custom SSL Certificate Authority files (dest `{{ item.dest }}`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; groups['automationcontroller'] \| default([]) \| length; inventory_hostname == groups['automationcontroller'][0]; mesh_ca_certfile \| default('') \| length; mesh_ca_keyfile \| default('') \| length |
| 1528 | 60.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca.yml | Verify if Receptor Mesh CA exists | Verify if Receptor Mesh CA exists | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; groups['automationcontroller'] \| default([]) \| length; inventory_hostname == groups['automationcontroller'][0] |
| 1529 | 60.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca.yml | Create Receptor Mesh CA | Create Receptor Mesh CA | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; groups['automationcontroller'] \| default([]) \| length; inventory_hostname == groups['automationcontroller'][0]; receptor_ca_exists is failed |
| 1530 | 60.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca.yml | Set permissions for the Receptor Mesh CA files | Set permissions for the Receptor Mesh CA files (dest `{{ item }}`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; groups['automationcontroller'] \| default([]) \| length; inventory_hostname == groups['automationcontroller'][0] |
| 1531 | 60.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca.yml | Gather Receptor Mesh CA Metadata | Gather Receptor Mesh CA Metadata | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; groups['automationcontroller'] \| default([]) \| length; inventory_hostname == groups['automationcontroller'][0] |
| 1532 | 60.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca.yml | Store Receptor Mesh CA Expiry Date as a Fact | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; groups['automationcontroller'] \| default([]) \| length; inventory_hostname == groups['automationcontroller'][0] |
| 1533 | 60.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca.yml → Store Receptor Mesh CA Expiry Date as a Fact | set_fact | Set a host fact used by later tasks | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; groups['automationcontroller'] \| default([]) \| length; inventory_hostname == groups['automationcontroller'][0] |
| 1534 | 60.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca.yml → Store Receptor Mesh CA Expiry Date as a Fact | set_fact | Set a host fact used by later tasks | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; groups['automationcontroller'] \| default([]) \| length; inventory_hostname == groups['automationcontroller'][0] |
| 1535 | 60.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca.yml → Store Receptor Mesh CA Expiry Date as a Fact | set_fact | Set a host fact used by later tasks | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; groups['automationcontroller'] \| default([]) \| length; inventory_hostname == groups['automationcontroller'][0] |
| 1536 | 60.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca.yml → Store Receptor Mesh CA Expiry Date as a Fact | set_fact | Set a host fact used by later tasks | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; groups['automationcontroller'] \| default([]) \| length; inventory_hostname == groups['automationcontroller'][0] |
| 1537 | 60.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca.yml → Store Receptor Mesh CA Expiry Date as a Fact | set_fact | Set a host fact used by later tasks | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; groups['automationcontroller'] \| default([]) \| length; inventory_hostname == groups['automationcontroller'][0] |
| 1538 | 60.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca.yml | Re-check if Receptor Mesh CA files are present | Re-check if Receptor Mesh CA files are present | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; groups['automationcontroller'] \| default([]) \| length; inventory_hostname == groups['automationcontroller'][0] |
| 1539 | 60.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca.yml | Assert if any Receptor Mesh CA file is missing | Assert if any Receptor Mesh CA file is missing | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; groups['automationcontroller'] \| default([]) \| length; inventory_hostname == groups['automationcontroller'][0] |
| 1540 | 60.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca.yml | Slurp Receptor Mesh CA files | Slurp Receptor Mesh CA files | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; groups['automationcontroller'] \| default([]) \| length; inventory_hostname == groups['automationcontroller'][0] |
| 1541 | 60.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS | Generate Receptor Mesh Nodes certificates | Generate Receptor Mesh Nodes certificates (file `tls_nodes.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool |
| 1542 | 60.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_nodes.yml | file `tls_nodes.yml` | Task file `ansible/automation_platform_installer/roles/receptor/tasks/tls_nodes.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool |
| 1543 | 60.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_nodes.yml | Copy custom node SSL files | Copy custom node SSL files (dest `{{ item.dest }}`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; mesh_node_certfile \| default('') \| length; mesh_node_keyfile \| default('') \| length |
| 1544 | 60.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_nodes.yml | Verify if Mesh Nodes certificate exists | Verify if Mesh Nodes certificate exists | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool |
| 1545 | 60.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_nodes.yml | Obtain all ipv4_addresses from node | Obtain all ipv4_addresses from node | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool |
| 1546 | 60.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_nodes.yml | Validate if receptor_host_identifier is an ipaddress | Validate if receptor_host_identifier is an ipaddress | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool |
| 1547 | 60.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_nodes.yml | Include the iPAddress x509 extension on the receptor certificate when receptor_host_identifier is an IP address and is not assigned to any NIC at the node | Include the iPAddress x509 extension on the receptor certificate when receptor_host_identifier is an IP address and is not assigned to any NIC at the node | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; _dnsname_valid_ipadddr != None and _dnsname_valid_ipadddr \| length > 0; receptor_host_identifier not in receptor_ipaddress_cmd |
| 1548 | 60.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_nodes.yml | Adds dnsname option to the receptor certificate | Adds dnsname option to the receptor certificate | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; _dnsname_valid_ipadddr == '' or _dnsname_valid_ipadddr == None |
| 1549 | 60.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_nodes.yml | Create Mesh node certificate | Create Mesh node certificate | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; _mesh_node_crt is failed |
| 1550 | 60.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_nodes.yml | Adjust Mesh Node private key permission | Adjust Mesh Node private key permission (dest `{{ receptor_tls_keyfile }}`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool |
| 1551 | 60.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_nodes.yml | Slurp Mesh Node CSR | Slurp Mesh Node CSR | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; receptor_tls_csr_pending is changed |
| 1552 | 60.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_nodes.yml | Transfer Receptor Mesh Node CSR to be signed | Transfer Receptor Mesh Node CSR to be signed (dest `{{ hostvars[inventory_hostname]['receptor_tls_csr']['source'] }}`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; receptor_tls_csr \| default([]) \| length; receptor_tls_csr_pending is changed |
| 1553 | 61.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS | Sign Receptor Mesh Node Certificates | Sign Receptor Mesh Node Certificates (file `tls_ca_sign.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool |
| 1554 | 61.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca_sign.yml | file `tls_ca_sign.yml` | Task file `ansible/automation_platform_installer/roles/receptor/tasks/tls_ca_sign.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool |
| 1555 | 61.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca_sign.yml | Receptor Mesh CA pending CSR(s) | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; receptor_tls_csr \| default([]) \| length; receptor_tls_csr_pending is changed |
| 1556 | 61.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca_sign.yml → Receptor Mesh CA pending CSR(s) | Sign Receptor Mesh node certificate | Sign Receptor Mesh node certificate | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; receptor_tls_csr \| default([]) \| length; receptor_tls_csr_pending is changed |
| 1557 | 61.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca_sign.yml → Receptor Mesh CA pending CSR(s) | Slurp Receptor Mesh CA certificates | Slurp Receptor Mesh CA certificates | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; receptor_tls_csr \| default([]) \| length; receptor_tls_csr_pending is changed |
| 1558 | 61.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca_sign.yml → Receptor Mesh CA pending CSR(s) | Copy Receptor Mesh certificate to final node | Copy Receptor Mesh certificate to final node (dest `{{ mesh_ca_nodes_data['source'] }}`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; receptor_tls_csr \| default([]) \| length; receptor_tls_csr_pending is changed; mesh_ca_nodes_data \| default([]) \| length |
| 1559 | 61.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca_sign.yml → Receptor Mesh CA pending CSR(s) | Cleanup signed Receptor Mesh Certificates | Cleanup signed Receptor Mesh Certificates (dest `{{ mesh_ca_nodes_data['source'] }}`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; receptor_tls_csr \| default([]) \| length; receptor_tls_csr_pending is changed; inventory_hostname != groups['automationcontroller'][0] |
| 1560 | 61.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca_sign.yml → Receptor Mesh CA pending CSR(s) | Gather Certificate Sign Requests files to be removed | Gather Certificate Sign Requests files to be removed | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; receptor_tls_csr \| default([]) \| length; receptor_tls_csr_pending is changed |
| 1561 | 61.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca_sign.yml → Receptor Mesh CA pending CSR(s) | Remove directory content | Remove directory content | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; receptor_tls_csr \| default([]) \| length; receptor_tls_csr_pending is changed |
| 1562 | 61.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca_sign.yml | Distribute Receptor Mesh CA to automationcontroller nodes | Distribute Receptor Mesh CA to automationcontroller nodes (dest `{{ item['source'] }}`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; hostvars[groups['automationcontroller'][0]]['mesh_ca_data']['results'] \| default([]) \| length; groups['automationcontroller'] \| default([]) \| length; inventory_hostname in groups['automationcontroller']; inventory_hostname != groups['automationcontroller'][0] |
| 1563 | 61.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → Invoke Receptor TLS → tls_ca_sign.yml | Distribute Receptor Mesh CA certificate to all execution_nodes | Distribute Receptor Mesh CA certificate to all execution_nodes (dest `{{ item['source'] }}`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_tls \| bool; hostvars[groups['automationcontroller'][0]]['mesh_ca_data']['results'] \| default([]) \| length; groups['execution_nodes'] \| default([]) \| length; inventory_hostname in groups['execution_nodes']; item['source'] == receptor_ca_mesh_certfile |
| 1564 | 61.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Create awx group | Create awx group | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1565 | 61.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Adds awx user to receptor group | Adds awx user to receptor group | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1566 | 61.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Configure the user's home directory | Configure the user's home directory | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1567 | 61.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Include datadir asserts | Include datadir asserts (file `assert_datadir.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_datadir is defined; receptor_datadir != '' |
| 1568 | 61.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → assert_datadir.yml | file `assert_datadir.yml` | Task file `ansible/automation_platform_installer/roles/receptor/tasks/assert_datadir.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_datadir is defined; receptor_datadir != '' |
| 1569 | 61.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → assert_datadir.yml | Ensure receptor_datadir exists with correct permissions | Ensure receptor_datadir exists with correct permissions | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_datadir is defined; receptor_datadir != '' |
| 1570 | 61.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → assert_datadir.yml | Check if '{{ run_receptor_as }}' user can write to receptor_datadir | Check if '{{ run_receptor_as }}' user can write to receptor_datadir | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_datadir is defined; receptor_datadir != '' |
| 1571 | 61.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → assert_datadir.yml | Fail if '{{ run_receptor_as }}' user cannot write to receptor_datadir | Fail if '{{ run_receptor_as }}' user cannot write to receptor_datadir | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_datadir is defined; receptor_datadir != ''; awx_dir_stat.failed \| bool |
| 1572 | 61.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → assert_datadir.yml | Ensure util-linux package is installed for findmnt availability | Ensure util-linux package is installed for findmnt availability | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_datadir is defined; receptor_datadir != '' |
| 1573 | 61.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → assert_datadir.yml | Check if receptor_datadir is on tmpfs using findmnt | Check if receptor_datadir is on tmpfs using findmnt | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_datadir is defined; receptor_datadir != '' |
| 1574 | 61.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → assert_datadir.yml | Create tmpfiles.d configuration if receptor_datadir is on tmpfs | Create tmpfiles.d configuration if receptor_datadir is on tmpfs | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); receptor_datadir is defined; receptor_datadir != ''; fstype_output.stdout != ''; fstype_output.stdout.strip() == 'tmpfs' |
| 1575 | 61.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Ensure soft/hard file descriptors limits | Ensure soft/hard file descriptors limits (dest `/etc/security/limits.d/awx.conf`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1576 | 61.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Create and and distribute keys for signing work | Create and and distribute keys for signing work (file `work_sign_keys.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1577 | 61.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → work_sign_keys.yml | file `work_sign_keys.yml` | Task file `ansible/automation_platform_installer/roles/receptor/tasks/work_sign_keys.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1578 | 61.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → work_sign_keys.yml | Create Work signing keys | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); groups['automationcontroller'] \| default([]) \| length; inventory_hostname == groups['automationcontroller'][0] |
| 1579 | 62.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → work_sign_keys.yml → Create Work signing keys | Generate Private RSA key for signing work | Generate Private RSA key for signing work | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); groups['automationcontroller'] \| default([]) \| length; inventory_hostname == groups['automationcontroller'][0] |
| 1580 | 62.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → work_sign_keys.yml → Create Work signing keys | Generate public RSA key for signing work | Generate public RSA key for signing work | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); groups['automationcontroller'] \| default([]) \| length; inventory_hostname == groups['automationcontroller'][0] |
| 1581 | 62.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → work_sign_keys.yml → Create Work signing keys | Slurp work signing keys | Slurp work signing keys | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); groups['automationcontroller'] \| default([]) \| length; inventory_hostname == groups['automationcontroller'][0] |
| 1582 | 62.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → work_sign_keys.yml | Distribute Work Sign keys to automationcontroller nodes | Distribute Work Sign keys to automationcontroller nodes (dest `{{ item['source'] }}`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); groups['automationcontroller'] \| default([]) \| length; hostvars[groups['automationcontroller'][0]]['signing_keys']['results'] \| default([]) \| length; inventory_hostname in groups['automationcontroller']; inventory_hostname != groups['automationcontroller'][0] |
| 1583 | 62.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → work_sign_keys.yml | Distribute work sign public keys to all execution_nodes | Distribute work sign public keys to all execution_nodes (dest `{{ item['source'] }}`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); groups['automationcontroller'] \| default([]) \| length; hostvars[groups['automationcontroller'][0]]['signing_keys']['results'] \| default([]) \| length; groups['execution_nodes'] \| default([]) \| length; inventory_hostname in groups['execution_nodes']; item['source'] == work_sign_public_keyfile; node_type == 'execution' |
| 1584 | 62.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Allow awx user to read receptor configuration | Allow awx user to read receptor configuration (dest `/etc/receptor`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1585 | 62.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Ensure systemd override directory exists | Ensure systemd override directory exists (dest `/etc/systemd/system/receptor.service.d`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1586 | 62.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Override receptor's systemd service runuser | Override receptor's systemd service runuser (dest `/etc/systemd/system/receptor.service.d/override.conf`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1587 | 62.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Configure the awx-receptor socket directory. | Configure the awx-receptor socket directory. | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1588 | 62.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Create tmpfiles.d entry for awx-receptor socket directory | Create tmpfiles.d entry for awx-receptor socket directory (dest `/etc/tmpfiles.d/awx-receptor.conf`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1589 | 62.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Create tmpfiles.d entry for receptor socket directory | Create tmpfiles.d entry for receptor socket directory (dest `/etc/tmpfiles.d/receptor.conf`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1590 | 62.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Configure firewall | Configure firewall (role `firewall`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1591 | 62.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → firewall | role `firewall` | Enter role task list | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1592 | 62.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → firewall → firewall | file `main.yml` | Task file `ansible/automation_platform_installer/roles/firewall/tasks/main.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1593 | 62.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → firewall → firewall | include_tasks | Include a task file from the current role or playbook (file `discover.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1594 | 62.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → firewall → firewall → discover.yml | file `discover.yml` | Task file `ansible/automation_platform_installer/roles/firewall/tasks/discover.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1595 | 62.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → firewall → firewall → discover.yml | Determine if firewalld is running | Determine if firewalld is running | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1596 | 62.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → firewall → firewall → discover.yml | Determine if firewalld is enabled | Determine if firewalld is enabled | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1597 | 62.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → firewall → firewall → discover.yml | Determine if iptables is installed | Determine if iptables is installed | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1598 | 62.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → firewall → firewall → discover.yml | Determine if iptables service is installed | Determine if iptables service is installed | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1599 | 62.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → firewall → firewall → discover.yml | Check to see if there is an iptables config file | Check to see if there is an iptables config file | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); iptables_installed.rc == 0 |
| 1600 | 62.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → firewall → firewall → discover.yml | Select firewalld | Select firewalld | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); firewalld_running.rc == 0 or systemctl_unit_files.stdout_lines \| select('match','firewalld.service\s+enabled') \| list \| length == 1 |
| 1601 | 62.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → firewall → firewall → discover.yml | Select iptables | Select iptables | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); iptables_installed.rc == 0; iptables_exists.stat.exists; iptables_service.stat.exists |
| 1602 | 62.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → firewall → firewall | Include {{ 'no' if component_firewall_type == 'none' else component_firewall_type }} tasks | Include {{ 'no' if component_firewall_type == 'none' else component_firewall_type }} tasks (file `{{ component_firewall_type }}.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); firewalld_running.rc == 0 or systemctl_unit_files.stdout_lines \| select('match','firewalld.service\s+enabled') \| list \| length == 1 or ( iptables_installed.rc == 0 and iptables_exists.stat.exists and iptables_service.stat.exists ) |
| 1603 | 62.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → firewall → firewall → {{ component_firewall_type }}.yml | {{ component_firewall_type }}.yml | Include task file `{{ component_firewall_type }}.yml` (relative to current role task directory) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); firewalld_running.rc == 0 or systemctl_unit_files.stdout_lines \| select('match','firewalld.service\s+enabled') \| list \| length == 1 or ( iptables_installed.rc == 0 and iptables_exists.stat.exists and iptables_service.stat.exists ) |
| 1604 | 63.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Configure podman | Configure podman (role `podman`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types |
| 1605 | 63.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman | role `podman` | Enter role task list | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types |
| 1606 | 63.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman | file `main.yml` | Task file `ansible/automation_platform_installer/roles/podman/tasks/main.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types |
| 1607 | 63.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman | Allocate subuids for {{ run_podman_as }} user | Allocate subuids for {{ run_podman_as }} user | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types |
| 1608 | 63.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman | Allocate subgids for {{ run_podman_as }} user | Allocate subgids for {{ run_podman_as }} user | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types |
| 1609 | 63.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman | Configure keyring size if needed | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types; (ansible_memtotal_mb // 100) > 200 |
| 1610 | 63.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman → Configure keyring size if needed | Set maxkeys | Set maxkeys | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types; (ansible_memtotal_mb // 100) > 200 |
| 1611 | 63.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman → Configure keyring size if needed | Set maxbytes | Set maxbytes | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types; (ansible_memtotal_mb // 100) > 200 |
| 1612 | 63.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman | Create directory for crun runtime config | Create directory for crun runtime config | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types |
| 1613 | 63.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman | Configure podman default runtime | Configure podman default runtime (dest `{{ run_podman_home_prefix }}/{{ run_podman_as }}/.config/containers/containers.conf`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types |
| 1614 | 63.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman | Create mounts.conf from template | Create mounts.conf from template (dest `{{ run_podman_home_prefix }}/{{ run_podman_as }}/.config/containers/mounts.conf`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types; not podman_copy_mounts_conf \| bool |
| 1615 | 63.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman | Copy user provided mounts.conf | Copy user provided mounts.conf (dest `{{ run_podman_home_prefix }}/{{ run_podman_as }}/.config/containers/mounts.conf`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types; podman_copy_mounts_conf \| bool |
| 1616 | 63.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman | Configure SELinux context mapping | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types; ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 1617 | 63.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman → Configure SELinux context mapping | Ensure policycoreutils-python-utils is installed | Ensure policycoreutils-python-utils is installed | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types; ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 1618 | 63.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman → Configure SELinux context mapping | Determine podman container storage dir variable | Determine podman container storage dir variable | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types; ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 1619 | 63.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman → Configure SELinux context mapping | Check current SELinux context mapping | Check current SELinux context mapping | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types; ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 1620 | 63.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman → Configure SELinux context mapping | Delete custom SELinux context mapping | Delete custom SELinux context mapping | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types; ansible_selinux is defined; ansible_selinux.status == "enabled"; semanage_current.stdout_lines\|length; "/var/lib/containers" not in semanage_current |
| 1621 | 63.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman → Configure SELinux context mapping | Register expected SELinux context mapping | Register expected SELinux context mapping | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types; ansible_selinux is defined; ansible_selinux.status == "enabled"; semanage_current.failed \| bool or semanage_deleted.changed \| bool |
| 1622 | 63.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman → Configure SELinux context mapping | Check if container storage dir exists | Check if container storage dir exists | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types; ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 1623 | 63.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman → Configure SELinux context mapping | Perform SELinux relabeling for existing files | Perform SELinux relabeling for existing files | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types; ansible_selinux is defined; ansible_selinux.status == "enabled"; _podman_container_storage_stat.stat.exists \| bool and (podman_force_selinux_relabel \| bool or selinux_manage.changed \| bool) |
| 1624 | 63.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman | Ensure registries.conf.d exists | Ensure registries.conf.d exists | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types |
| 1625 | 63.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman | Force fully qualified image names to be provided to podman pull | Force fully qualified image names to be provided to podman pull (dest `/etc/containers/registries.conf.d/force-fully-qualified-images.conf`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types |
| 1626 | 63.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman | Create tmpfiles.d entry for podman | Create tmpfiles.d entry for podman | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types |
| 1627 | 63.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman | block | Begin conditional or grouped task block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types; subuids is changed or subgids is changed |
| 1628 | 63.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman → block | Create temporary directory for podman | Create temporary directory for podman | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types; subuids is changed or subgids is changed |
| 1629 | 63.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman → block | Ensure changes are applied to podman | Ensure changes are applied to podman | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types; subuids is changed or subgids is changed |
| 1630 | 64.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman | block (always) | Run always tasks for block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types; subuids is changed or subgids is changed |
| 1631 | 64.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman → block (always) | Remove podman temporary directory | Remove podman temporary directory | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types; subuids is changed or subgids is changed; podman_tmp.path is defined |
| 1632 | 64.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman | Configure XDG_RUNTIME_DIR Environment variable for "{{ run_podman_as }}" user | Configure XDG_RUNTIME_DIR Environment variable for "{{ run_podman_as }}" user | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types |
| 1633 | 64.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman | Configure DBUS_SESSION_BUS_ADDRESS Environment variable for "{{ run_podman_as }}" user | Configure DBUS_SESSION_BUS_ADDRESS Environment variable for "{{ run_podman_as }}" user | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types |
| 1634 | 64.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman | Enable lingering systemd sessions for {{ run_podman_as }} user | Enable lingering systemd sessions for {{ run_podman_as }} user | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types |
| 1635 | 64.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman | Reset podman for {{ run_podman_as }} user to apply configs | Reset podman for {{ run_podman_as }} user to apply configs | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types |
| 1636 | 64.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → podman → podman | Enable podman socket for {{ run_podman_as }} and restart | Enable podman socket for {{ run_podman_as }} and restart (service `podman.socket`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in receptor_workable_types; podman_enable_socket \| bool |
| 1637 | 64.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | set_fact | Set a host fact used by later tasks | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1638 | 64.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Disable minTLS13 if any node is FIPS enabled | Disable minTLS13 if any node is FIPS enabled | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1639 | 64.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Deploy receptor config | Deploy receptor config (dest `/etc/receptor/receptor.conf`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1640 | 64.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Start Receptor service | Start Receptor service (service `receptor`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1641 | 64.4% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor | Register instances and queues and check mesh connectivity | Register instances and queues and check mesh connectivity (file `register_instances.yml`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1642 | 64.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → register_instances.yml | file `register_instances.yml` | Task file `ansible/automation_platform_installer/roles/receptor/tasks/register_instances.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1643 | 64.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → register_instances.yml | Get UUID for execution nodes | Get UUID for execution nodes | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type == 'execution' |
| 1644 | 64.5% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → register_instances.yml | Set UUID option for execution_nodes | Set UUID option for execution_nodes | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type == 'execution' |
| 1645 | 64.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → register_instances.yml | Generate UUID for hop nodes | Generate UUID for hop nodes | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type == 'hop' |
| 1646 | 64.6% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → register_instances.yml | Set UUID option for hop nodes | Set UUID option for hop nodes | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type == 'hop' |
| 1647 | 64.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → register_instances.yml | Set UUID option for control / hybrid nodes | Set UUID option for control / hybrid nodes | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); node_type in ['control', 'hybrid'] and system_uuid is defined |
| 1648 | 64.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → register_instances.yml | Register the node instance in the database. | Register the node instance in the database. | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1649 | 64.7% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → register_instances.yml | Set listener port option for nodes | Set listener port option for nodes | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); listener is defined and listener |
| 1650 | 64.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → register_instances.yml | Register controlplane nodes to controlplane queue | Register controlplane nodes to controlplane queue | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1651 | 64.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → register_instances.yml | Register work nodes to default queue | Register work nodes to default queue | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1652 | 64.8% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → register_instances.yml | Register instance_group_* groups | Register instance_group_* groups | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); item.startswith('instance_group_'); mesh_groups[item] \| length |
| 1653 | 64.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → register_instances.yml | Flush handlers | Flush handlers (flush_handlers) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1654 | 64.9% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → register_instances.yml | Validate connectivity for Mesh peers | Validate connectivity for Mesh peers | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1655 | 65.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → register_instances.yml | Compose Mesh Report | Compose Mesh Report | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1656 | 65.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → register_instances.yml | Receptor Mesh Report | Receptor Mesh Report | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh_report is defined |
| 1657 | 65.0% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes → receptor → receptor → register_instances.yml | Report error when mesh connectivity issue is detected | Report error when mesh connectivity issue is detected | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); mesh_ping.failed \| default(False) \| bool |
| 1658 | 65.1% | install.yml → Install Automation Controller and Execution nodes | Install Automation Controller and Execution nodes (always) | Run always tasks for block | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1659 | 65.1% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes (always) | Run repo_management (teardown) | Run repo_management (teardown) (role `repo_management`, tasks `teardown`) | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1660 | 65.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes (always) → repo_management | role `repo_management` (`teardown`) | Enter role task list | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1661 | 65.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes (always) → repo_management → repo_management | file `teardown.yml` | Task file `ansible/automation_platform_installer/roles/repo_management/tasks/teardown.yml` | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate') |
| 1662 | 65.2% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes (always) → repo_management → repo_management | Disable Automation Platform repository | Disable Automation Platform repository | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); extra_repos is defined |
| 1663 | 65.3% | install.yml → Install Automation Controller and Execution nodes → Install Automation Controller and Execution nodes (always) → repo_management → repo_management | Disable Automation Platform RHSM repository | Disable Automation Platform RHSM repository | node_state is defined; (node_state == 'active' or node_state == 'iso_migrate'); aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 1664 | 65.3% | install.yml | Post-Receptor setup | Play targeting `automationcontroller:execution_nodes` | — |
| 1665 | 65.4% | install.yml → Post-Receptor setup | Register Automation Controller and Execution node links | Begin conditional or grouped task block | node_state is defined; node_state == 'active' |
| 1666 | 65.4% | install.yml → Post-Receptor setup → Register Automation Controller and Execution node links | set_fact | Set a host fact used by later tasks | node_state is defined; node_state == 'active' |
| 1667 | 65.4% | install.yml → Post-Receptor setup → Register Automation Controller and Execution node links | include_role | Include and run tasks from an Ansible role (role `receptor`, tasks `peers`) | node_state is defined; node_state == 'active' |
| 1668 | 65.5% | install.yml → Post-Receptor setup → Register Automation Controller and Execution node links → receptor | role `receptor` (`peers`) | Enter role task list | node_state is defined; node_state == 'active' |
| 1669 | 65.5% | install.yml → Post-Receptor setup → Register Automation Controller and Execution node links → receptor → receptor | file `peers.yml` | Task file `ansible/automation_platform_installer/roles/receptor/tasks/peers.yml` | node_state is defined; node_state == 'active' |
| 1670 | 65.6% | install.yml → Post-Receptor setup → Register Automation Controller and Execution node links → receptor → receptor | register the peers for a node, with removal of old peers | register the peers for a node, with removal of old peers | node_state is defined; node_state == 'active' |
| 1671 | 65.6% | install.yml | Install Automation Hub node | Play targeting `automationhub` | — |
| 1672 | 65.6% | install.yml → Install Automation Hub node | block | Begin conditional or grouped task block | — |
| 1673 | 65.7% | install.yml → Install Automation Hub node → block | Add internal CA certificate to trust store | Add internal CA certificate to trust store (role `certificate_authority`, tasks `add_cacert.yml`) | — |
| 1674 | 65.7% | install.yml → Install Automation Hub node → block → certificate_authority | role `certificate_authority` (`add_cacert.yml`) | Enter role task list | — |
| 1675 | 65.8% | install.yml → Install Automation Hub node → block → certificate_authority → certificate_authority | file `add_cacert.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/add_cacert.yml` | — |
| 1676 | 65.8% | install.yml → Install Automation Hub node → block → certificate_authority → certificate_authority | Ensure ca-certificates is installed | Ensure ca-certificates is installed | — |
| 1677 | 65.8% | install.yml → Install Automation Hub node → block → certificate_authority → certificate_authority | Copy custom CA cert | Copy custom CA cert (dest `/etc/pki/ca-trust/source/anchors/ansible-automation-platform-custom-ca-cert.crt`) | custom_ca_cert is defined |
| 1678 | 65.9% | install.yml → Install Automation Hub node → block → certificate_authority → certificate_authority | Place AAP CA certificate | Place AAP CA certificate (dest `/etc/pki/ca-trust/source/anchors/ansible-automation-platform-managed-ca-cert.crt`) | — |
| 1679 | 65.9% | install.yml → Install Automation Hub node → block | Run repo_management (setup) | Run repo_management (setup) (role `repo_management`, tasks `setup`) | — |
| 1680 | 65.9% | install.yml → Install Automation Hub node → block → repo_management | role `repo_management` (`setup`) | Enter role task list | — |
| 1681 | 66.0% | install.yml → Install Automation Hub node → block → repo_management → repo_management | file `setup.yml` | Task file `ansible/automation_platform_installer/roles/repo_management/tasks/setup.yml` | — |
| 1682 | 66.0% | install.yml → Install Automation Hub node → block → repo_management → repo_management | include_vars | Load variables from a file | — |
| 1683 | 66.1% | install.yml → Install Automation Hub node → block → repo_management → repo_management | Install yum repo keys | Install yum repo keys (dest `/etc/pki/rpm-gpg/{{ item }}`) | — |
| 1684 | 66.1% | install.yml → Install Automation Hub node → block → repo_management → repo_management | Install subscription-manager-rhsm-certificates | Install subscription-manager-rhsm-certificates | — |
| 1685 | 66.1% | install.yml → Install Automation Hub node → block → repo_management → repo_management | Determine if rhsm repository exists | Determine if rhsm repository exists | — |
| 1686 | 66.2% | install.yml → Install Automation Hub node → block → repo_management → repo_management | Enable Automation Platform rhsm repository | Enable Automation Platform rhsm repository | ansible_distribution == 'RedHat'; rhsm_repo.stat.exists; not bundle_install \| bool; aw_repo_url is undefined |
| 1687 | 66.2% | install.yml → Install Automation Hub node → block → repo_management → repo_management | Explain Automation Platform rhsm repository failure | Explain Automation Platform rhsm repository failure | aap_rhsm_repo_enable.failed \| default(false) |
| 1688 | 66.3% | install.yml → Install Automation Hub node → block → repo_management → repo_management | block | Begin conditional or grouped task block | aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 1689 | 66.3% | install.yml → Install Automation Hub node → block → repo_management → repo_management → block | Enable module hotfixes flag | Enable module hotfixes flag | aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 1690 | 66.3% | install.yml → Install Automation Hub node → block → repo_management → repo_management → block | Set AAP repo for update | Set AAP repo for update | aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 1691 | 66.4% | install.yml → Install Automation Hub node → block → repo_management → repo_management | Ensure rsync is installed | Ensure rsync is installed | bundle_install \| bool |
| 1692 | 66.4% | install.yml → Install Automation Hub node → block → repo_management → repo_management | Copy bundle packages to repo source directory | Begin conditional or grouped task block | bundle_install \| bool |
| 1693 | 66.5% | install.yml → Install Automation Hub node → block → repo_management → repo_management → Copy bundle packages to repo source directory | Copy bundle packages to repo source directory | Copy bundle packages to repo source directory | bundle_install \| bool |
| 1694 | 66.5% | install.yml → Install Automation Hub node → block → repo_management → repo_management | Copy bundle packages to repo source directory (rescue) | Run rescue tasks for block | bundle_install \| bool |
| 1695 | 66.5% | install.yml → Install Automation Hub node → block → repo_management → repo_management → Copy bundle packages to repo source directory (rescue) | Remove old bundle packages source directory (legacy) | Remove old bundle packages source directory (legacy) | bundle_install \| bool |
| 1696 | 66.6% | install.yml → Install Automation Hub node → block → repo_management → repo_management → Copy bundle packages to repo source directory (rescue) | Copy bundle packages to repo source directory (legacy) | Copy bundle packages to repo source directory (legacy) (dest `{{ bundle_install_folder }}`) | bundle_install \| bool |
| 1697 | 66.6% | install.yml → Install Automation Hub node → block → repo_management → repo_management | Install the Automation Platform bundle repository | Install the Automation Platform bundle repository (dest `/etc/yum.repos.d/ansible-automation-platform.repo`) | bundle_install \| bool |
| 1698 | 66.7% | install.yml → Install Automation Hub node → block → repo_management → repo_management | Remove legacy ansible-tower repository | Remove legacy ansible-tower repository | — |
| 1699 | 66.7% | install.yml → Install Automation Hub node → block → repo_management → repo_management | Install the Automation Platform yum repository. | Install the Automation Platform yum repository. (dest `/etc/yum.repos.d/ansible-automation-platform.repo`) | aw_repo_url is defined; not bundle_install \| bool |
| 1700 | 66.7% | install.yml → Install Automation Hub node → block → repo_management → repo_management | Set repo list for bundle install | Set repo list for bundle install | bundle_install \| bool |
| 1701 | 66.8% | install.yml → Install Automation Hub node → block → repo_management → repo_management | Set repo list for online install | Set repo list for online install | yum_repo is succeeded; yum_repo is not skipped |
| 1702 | 66.8% | install.yml → Install Automation Hub node → block → repo_management → repo_management | Enable Ansible Automation Platform repo | Enable Ansible Automation Platform repo | extra_repos is defined |
| 1703 | 66.8% | install.yml → Install Automation Hub node → block | include_role | Include and run tasks from an Ansible role (role `redis`) | — |
| 1704 | 66.9% | install.yml → Install Automation Hub node → block → redis | role `redis` | Enter role task list | — |
| 1705 | 66.9% | install.yml → Install Automation Hub node → block → redis → redis | file `main.yml` | Task file `ansible/automation_platform_installer/roles/redis/tasks/main.yml` | — |
| 1706 | 67.0% | install.yml → Install Automation Hub node → block → redis → redis | include_tasks | Include a task file from the current role or playbook (file `vars.yml`) | — |
| 1707 | 67.0% | install.yml → Install Automation Hub node → block → redis → redis → vars.yml | file `vars.yml` | Task file `ansible/automation_platform_installer/roles/redis/tasks/vars.yml` | — |
| 1708 | 67.0% | install.yml → Install Automation Hub node → block → redis → redis → vars.yml | include_vars | Load variables from a file | — |
| 1709 | 67.1% | install.yml → Install Automation Hub node → block → redis → redis → vars.yml | include_vars | Load variables from a file | install_redis_mode == "cluster" |
| 1710 | 67.1% | install.yml → Install Automation Hub node → block → redis → redis | include_tasks | Include a task file from the current role or playbook (file `module.yml`) | — |
| 1711 | 67.2% | install.yml → Install Automation Hub node → block → redis → redis → module.yml | file `module.yml` | Task file `ansible/automation_platform_installer/roles/redis/tasks/module.yml` | — |
| 1712 | 67.2% | install.yml → Install Automation Hub node → block → redis → redis → module.yml | Enable redis module stream on RHEL 8 | Begin conditional or grouped task block | ansible_distribution_major_version \| int == 8 |
| 1713 | 67.2% | install.yml → Install Automation Hub node → block → redis → redis → module.yml → Enable redis module stream on RHEL 8 | Disable Redis module | Disable Redis module | ansible_distribution_major_version \| int == 8 |
| 1714 | 67.3% | install.yml → Install Automation Hub node → block → redis → redis → module.yml → Enable redis module stream on RHEL 8 | Enable Redis module | Enable Redis module | ansible_distribution_major_version \| int == 8 |
| 1715 | 67.3% | install.yml → Install Automation Hub node → block → redis → redis | include_tasks | Include a task file from the current role or playbook (file `tasks.yml`) | — |
| 1716 | 67.4% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml | file `tasks.yml` | Task file `ansible/automation_platform_installer/roles/redis/tasks/tasks.yml` | — |
| 1717 | 67.4% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml | Ensure redis package is installed | Ensure redis package is installed | — |
| 1718 | 67.4% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml | Ensure AAP directory exists | Ensure AAP directory exists | — |
| 1719 | 67.5% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml | Ensure redis cert directory exists | Ensure redis cert directory exists | — |
| 1720 | 67.5% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml | Configure TLS certificates for redis | Begin conditional or grouped task block | not _redis_disable_tls \| bool |
| 1721 | 67.6% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → Configure TLS certificates for redis | Copy user provided SSL cert | Begin conditional or grouped task block | not _redis_disable_tls \| bool; redis_tls_cert \| length |
| 1722 | 67.6% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → Configure TLS certificates for redis → Copy user provided SSL cert | Import specified TLS certificate | Import specified TLS certificate | not _redis_disable_tls \| bool; redis_tls_cert \| length |
| 1723 | 67.6% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → Configure TLS certificates for redis → Copy user provided SSL cert | Import specified TLS private key | Import specified TLS private key | not _redis_disable_tls \| bool; redis_tls_cert \| length |
| 1724 | 67.7% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → Configure TLS certificates for redis | Use internal CA | Use internal CA (role `certificate_authority`, tasks `sign_service.yml`) | not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 1725 | 67.7% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority | role `certificate_authority` (`sign_service.yml`) | Enter role task list | not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 1726 | 67.8% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | file `sign_service.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/sign_service.yml` | not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 1727 | 67.8% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | include_tasks | Include a task file from the current role or playbook (file `init.yml`) | not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 1728 | 67.8% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → init.yml | file `init.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/init.yml` | not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 1729 | 67.9% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → init.yml | Ensure python cryptography libraries are installed | Ensure python cryptography libraries are installed | not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 1730 | 67.9% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | Create {{ aap_ca_servicename }} private key | Create {{ aap_ca_servicename }} private key | not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 1731 | 67.9% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | set_fact | Set a host fact used by later tasks | not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 1732 | 68.0% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | Check if {{ aap_ca_servicename }} public key exists | Check if {{ aap_ca_servicename }} public key exists | not _redis_disable_tls \| bool; not redis_tls_cert \| length |
| 1733 | 68.0% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | not _redis_disable_tls \| bool; not redis_tls_cert \| length; certificate_stat.stat.exists \| bool |
| 1734 | 68.1% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | Check contents of {{ aap_ca_servicename }} public key | Check contents of {{ aap_ca_servicename }} public key | not _redis_disable_tls \| bool; not redis_tls_cert \| length; certificate_stat.stat.exists \| bool |
| 1735 | 68.1% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | not _redis_disable_tls \| bool; not redis_tls_cert \| length; certificate_stat.stat.exists \| bool; aap_service_san_records_internal \| union(aap_service_san_records) \| difference(certificate_check.subject_alt_name) \| length > 0 |
| 1736 | 68.1% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | not _redis_disable_tls \| bool; not redis_tls_cert \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1737 | 68.2% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | not _redis_disable_tls \| bool; not redis_tls_cert \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false); aap_service_is_client |
| 1738 | 68.2% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | Create CSR for {{ aap_ca_servicename }} node/s | Create CSR for {{ aap_ca_servicename }} node/s | not _redis_disable_tls \| bool; not redis_tls_cert \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1739 | 68.3% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | Sign {{ aap_ca_servicename }} public key | Sign {{ aap_ca_servicename }} public key | not _redis_disable_tls \| bool; not redis_tls_cert \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1740 | 68.3% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority → block | Write public key to {{ aap_ca_servicename }} node/s | Write public key to {{ aap_ca_servicename }} node/s (dest `{{ aap_service_cert_fullpath }}`) | not _redis_disable_tls \| bool; not redis_tls_cert \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1741 | 68.3% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → Configure TLS certificates for redis → certificate_authority → certificate_authority | Signal restart if certificate pair changes | Signal restart if certificate pair changes | not _redis_disable_tls \| bool; not redis_tls_cert \| length; (write_key is changed or write_certificate is changed) |
| 1742 | 68.4% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → Configure TLS certificates for redis | Trigger redis notifier for internally managed certificate changes | Trigger redis notifier for internally managed certificate changes | not _redis_disable_tls \| bool; aap_service_restart \| default(false) |
| 1743 | 68.4% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml | Ensure redis socket directory exists | Ensure redis socket directory exists | — |
| 1744 | 68.5% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml | Ensure Redis socket directory has the correct SELinux context | Ensure Redis socket directory has the correct SELinux context | — |
| 1745 | 68.5% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml | Create tmpfiles.d entry for redis socket directory | Create tmpfiles.d entry for redis socket directory (dest `/etc/tmpfiles.d/redis.conf`) | — |
| 1746 | 68.5% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml | Install redis.conf | Install redis.conf (dest `{{ redis_conf_location }}`) | — |
| 1747 | 68.6% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml | block | Begin conditional or grouped task block | not redis_use_sockets \| bool |
| 1748 | 68.6% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → block | Set gateway redis credentials for clustered | Set gateway redis credentials for clustered | not redis_use_sockets \| bool |
| 1749 | 68.7% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → block | Set Event-Driven Ansible redis credentials | Set Event-Driven Ansible redis credentials | not redis_use_sockets \| bool; groups.get("automationedacontroller", []) \| length > 0 |
| 1750 | 68.7% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → block | Create the redis user ACL file | Create the redis user ACL file | not redis_use_sockets \| bool |
| 1751 | 68.7% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → block | block | Begin conditional or grouped task block | not redis_use_sockets \| bool; install_redis_mode == "cluster" |
| 1752 | 68.8% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → block → block | Set redis hostname | Set redis hostname | not redis_use_sockets \| bool; install_redis_mode == "cluster" |
| 1753 | 68.8% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → block → block | Map redis node hostnames | Map redis node hostnames | not redis_use_sockets \| bool; install_redis_mode == "cluster" |
| 1754 | 68.9% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → block → block | Create redis nodes file | Create redis nodes file | not redis_use_sockets \| bool; install_redis_mode == "cluster" |
| 1755 | 68.9% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml → block → block | Install {{ redis_init_name }} service file | Install {{ redis_init_name }} service file (dest `/lib/systemd/system/redis-platform.service`) | not redis_use_sockets \| bool; install_redis_mode == "cluster" |
| 1756 | 68.9% | install.yml → Install Automation Hub node → block → redis → redis → tasks.yml | start redis and configure to startup automatically | start redis and configure to startup automatically (service `{{ redis_init_name }}`) | — |
| 1757 | 69.0% | install.yml → Install Automation Hub node → block | include_role | Include and run tasks from an Ansible role (role `nginx`) | — |
| 1758 | 69.0% | install.yml → Install Automation Hub node → block → nginx | role `nginx` | Enter role task list | — |
| 1759 | 69.0% | install.yml → Install Automation Hub node → block → nginx → nginx | file `main.yml` | Task file `ansible/automation_platform_installer/roles/nginx/tasks/main.yml` | — |
| 1760 | 69.1% | install.yml → Install Automation Hub node → block → nginx → nginx | include_tasks | Include a task file from the current role or playbook (file `module.yml`) | — |
| 1761 | 69.1% | install.yml → Install Automation Hub node → block → nginx → nginx → module.yml | file `module.yml` | Task file `ansible/automation_platform_installer/roles/nginx/tasks/module.yml` | — |
| 1762 | 69.2% | install.yml → Install Automation Hub node → block → nginx → nginx → module.yml | Enable nginx module stream | Begin conditional or grouped task block | — |
| 1763 | 69.2% | install.yml → Install Automation Hub node → block → nginx → nginx → module.yml → Enable nginx module stream | Disable Nginx module | Disable Nginx module | — |
| 1764 | 69.2% | install.yml → Install Automation Hub node → block → nginx → nginx → module.yml → Enable nginx module stream | Enable Nginx module | Enable Nginx module | — |
| 1765 | 69.3% | install.yml → Install Automation Hub node → block → nginx → nginx | include_tasks | Include a task file from the current role or playbook (file `tasks.yml`) | — |
| 1766 | 69.3% | install.yml → Install Automation Hub node → block → nginx → nginx → tasks.yml | file `tasks.yml` | Task file `ansible/automation_platform_installer/roles/nginx/tasks/tasks.yml` | — |
| 1767 | 69.4% | install.yml → Install Automation Hub node → block → nginx → nginx → tasks.yml | Install nginx | Install nginx | — |
| 1768 | 69.4% | install.yml → Install Automation Hub node → block → nginx → nginx → tasks.yml | SELinux configuration for nginx | Begin conditional or grouped task block | ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 1769 | 69.4% | install.yml → Install Automation Hub node → block → nginx → nginx → tasks.yml → SELinux configuration for nginx | Ensure libsemanage-python is installed | Ensure libsemanage-python is installed | ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 1770 | 69.5% | install.yml → Install Automation Hub node → block → nginx → nginx → tasks.yml → SELinux configuration for nginx | Open up permissions on nginx. | Open up permissions on nginx. | ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 1771 | 69.5% | install.yml → Install Automation Hub node → block | include_role | Include and run tasks from an Ansible role (role `automationhub`) | — |
| 1772 | 69.6% | install.yml → Install Automation Hub node → block → automationhub | role `automationhub` | Enter role task list | — |
| 1773 | 69.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub | file `main.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/main.yml` | — |
| 1774 | 69.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub | Check installed version of PG | Check installed version of PG | — |
| 1775 | 69.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub | block | Begin conditional or grouped task block | pg_installed_version.failed or pg_installed_version.stdout \| trim \| int != automationhub_postgresql_version \| int |
| 1776 | 69.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub → block | Disable PostgreSQL module | Disable PostgreSQL module | pg_installed_version.failed or pg_installed_version.stdout \| trim \| int != automationhub_postgresql_version \| int; not pg_installed_version.failed; ansible_distribution_major_version == "8" |
| 1777 | 69.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → block | Enable PostgreSQL {{ automationhub_postgresql_version }} | Enable PostgreSQL {{ automationhub_postgresql_version }} | pg_installed_version.failed or pg_installed_version.stdout \| trim \| int != automationhub_postgresql_version \| int |
| 1778 | 69.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → block | Install PostgreSQL {{ automationhub_postgresql_version }} | Install PostgreSQL {{ automationhub_postgresql_version }} | pg_installed_version.failed or pg_installed_version.stdout \| trim \| int != automationhub_postgresql_version \| int |
| 1779 | 69.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub | Generate a Pulp secret | Generate a Pulp secret | — |
| 1780 | 69.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub | Determine plugins list | Determine plugins list | — |
| 1781 | 69.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub | Generate gateway secret | Generate gateway secret (role `automationgateway`, tasks `service_token`) | — |
| 1782 | 70.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → automationgateway | role `automationgateway` (`service_token`) | Enter role task list | — |
| 1783 | 70.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → automationgateway → automationgateway | file `service_token.yml` | Task file `ansible/automation_platform_installer/roles/automationgateway/tasks/service_token.yml` | — |
| 1784 | 70.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → automationgateway → automationgateway | Generate service secret | Generate service secret | — |
| 1785 | 70.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub | Default pulp settings | Default pulp settings | — |
| 1786 | 70.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub | Override working_directory when using shared storage | Override working_directory when using shared storage | groups['automationhub'] \| length > 1 |
| 1787 | 70.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub | Combine pulp settings with additional settings | Combine pulp settings with additional settings | — |
| 1788 | 70.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub | Combine pulp settings with feature flags if they exist | Combine pulp settings with feature flags if they exist | feature_flags is defined |
| 1789 | 70.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub | set_fact | Set a host fact used by later tasks | automationhub_importer_settings is defined |
| 1790 | 70.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub | Configure Pulp | Configure Pulp (file `pulp_common.yml`) | — |
| 1791 | 70.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml | file `pulp_common.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/pulp_common.yml` | — |
| 1792 | 70.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml | pulp_install_plugins_normalized_yml | pulp_install_plugins_normalized_yml | — |
| 1793 | 70.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml | pulp_install_plugins_normalized | pulp_install_plugins_normalized | — |
| 1794 | 70.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml | Load OS specific variables | Load OS specific variables | — |
| 1795 | 70.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml | Gather package facts | Gather package facts | — |
| 1796 | 70.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml | Enumerate default system PATH | Enumerate default system PATH | — |
| 1797 | 70.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml | Set the default system PATH as a fact | Set the default system PATH as a fact | — |
| 1798 | 70.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml | import_tasks | Import a task file (static include) (file `user.yml`) | — |
| 1799 | 70.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → user.yml | file `user.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/user.yml` | — |
| 1800 | 70.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → user.yml | Make sure {{ pulp_group }} group exists | Make sure {{ pulp_group }} group exists | — |
| 1801 | 70.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → user.yml | Create user {{ pulp_user }} | Create user {{ pulp_user }} | — |
| 1802 | 70.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → user.yml | Check if the redis group exists | Check if the redis group exists | — |
| 1803 | 70.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → user.yml | Add user pulp to the redis group | Add user pulp to the redis group | redis_group.rc == 0 |
| 1804 | 70.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → user.yml | Reset ssh conn to allow user changes to affect when ssh user and pulp user are the same | Reset ssh conn to allow user changes to affect when ssh user and pulp user are the same (reset_connection) | — |
| 1805 | 70.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml | import_tasks | Import a task file (static include) (file `dirs.yml`) | — |
| 1806 | 70.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → dirs.yml | file `dirs.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/dirs.yml` | — |
| 1807 | 70.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → dirs.yml | Make sure {{ pulp_user_home }} is world executable, and exists | Make sure {{ pulp_user_home }} is world executable, and exists | — |
| 1808 | 71.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → dirs.yml | Create deploy root dir for pulp | Create deploy root dir for pulp | — |
| 1809 | 71.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → dirs.yml | Create media dir for Pulp | Create media dir for Pulp | __pulp_common_merged_pulp_settings.media_root \| length > 0 |
| 1810 | 71.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → dirs.yml | Create cache dir for Pulp | Create cache dir for Pulp | — |
| 1811 | 71.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → dirs.yml | Create file upload temp dir for Pulp | Create file upload temp dir for Pulp | — |
| 1812 | 71.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → dirs.yml | Create assets (static) dir for Pulp | Create assets (static) dir for Pulp | — |
| 1813 | 71.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → dirs.yml | Check if artifact directory exists at old path | Check if artifact directory exists at old path | — |
| 1814 | 71.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → dirs.yml | Move artifact directory inside MEDIA_ROOT | Move artifact directory inside MEDIA_ROOT | old_artifact_dir_stat.stat.exists |
| 1815 | 71.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → dirs.yml | Create artifact dir for Pulp | Create artifact dir for Pulp | — |
| 1816 | 71.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → dirs.yml | Create scripts directory to hold user-provided scripts | Create scripts directory to hold user-provided scripts | — |
| 1817 | 71.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → dirs.yml | Create cert directory to hold token authentication key | Create cert directory to hold token authentication key | — |
| 1818 | 71.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml | import_tasks | Import a task file (static include) (file `install.yml`) | — |
| 1819 | 71.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml | file `install.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/install.yml` | — |
| 1820 | 71.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml | Install prerequisites | Install prerequisites | — |
| 1821 | 71.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml | Gather package facts | Gather package facts | — |
| 1822 | 71.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml | Installed packages | Installed packages | — |
| 1823 | 71.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml | Get PATH | Get PATH | — |
| 1824 | 71.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml | Set PATH as a fact | Set PATH as a fact | — |
| 1825 | 71.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml | Get LD_LIBRARY_PATH | Get LD_LIBRARY_PATH | — |
| 1826 | 71.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml | Set LD_LIBRARY_PATH as a fact | Set LD_LIBRARY_PATH as a fact | — |
| 1827 | 71.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml | install pulp | install pulp (file `install_packages.yml`) | — |
| 1828 | 71.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → install_packages.yml | file `install_packages.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/install_packages.yml` | — |
| 1829 | 71.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → install_packages.yml | Set a fact for the version of pulp-container installed prior to upgrade | Set a fact for the version of pulp-container installed prior to upgrade | ansible_facts.packages[item] is defined |
| 1830 | 71.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → install_packages.yml | Install the packages | Install the packages | — |
| 1831 | 71.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → install_packages.yml | Update existing dependent packages | Update existing dependent packages (role `repo_management`, tasks `update_dependency`) | — |
| 1832 | 71.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → install_packages.yml → repo_management | role `repo_management` (`update_dependency`) | Enter role task list | — |
| 1833 | 72.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → install_packages.yml → repo_management → repo_management | file `update_dependency.yml` | Task file `ansible/automation_platform_installer/roles/repo_management/tasks/update_dependency.yml` | — |
| 1834 | 72.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → install_packages.yml → repo_management → repo_management | Ensure AAP repo for update is set | Ensure AAP repo for update is set | not aap_repo_for_update \| default('') \| length |
| 1835 | 72.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → install_packages.yml → repo_management → repo_management | Generate a list of AAP packages with an update | Generate a list of AAP packages with an update | — |
| 1836 | 72.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → install_packages.yml → repo_management → repo_management | Set AAP packages to update | Set AAP packages to update | — |
| 1837 | 72.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → install_packages.yml → repo_management → repo_management | Update packages | Update packages | _packages_to_update \| length |
| 1838 | 72.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → install_packages.yml | Trigger collectstatic and pulpcore service restart if package changed | Trigger collectstatic and pulpcore service restart if package changed | _package_update_result is changed |
| 1839 | 72.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → install_packages.yml | Gather package facts | Gather package facts | — |
| 1840 | 72.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → install_packages.yml | Set __pulpcore_version to the RPM that was installed | Set __pulpcore_version to the RPM that was installed | pulpcore_version \| default('') \| length == 0 |
| 1841 | 72.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml | Refresh facts in case /var/lib/pulp was mounted earlier | Refresh facts in case /var/lib/pulp was mounted earlier | — |
| 1842 | 72.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml | Include tasks to ensure that /var/lib/pulp subdirs have the SELinux label | Include tasks to ensure that /var/lib/pulp subdirs have the SELinux label (file `selinux_remount.yml`) | pulp_install_selinux_policies\|bool or (ansible_facts.selinux.status == "enabled" and pulp_install_selinux_policies == "auto"); ansible_facts.mounts \| selectattr('mount', 'match', '^/var/lib/pulp$') \| list \| count == 1 |
| 1843 | 72.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → selinux_remount.yml | file `selinux_remount.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/selinux_remount.yml` | pulp_install_selinux_policies\|bool or (ansible_facts.selinux.status == "enabled" and pulp_install_selinux_policies == "auto"); ansible_facts.mounts \| selectattr('mount', 'match', '^/var/lib/pulp$') \| list \| count == 1 |
| 1844 | 72.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → selinux_remount.yml | Test setting SELinux context on /var/lib/pulp/.selinux-test | Test setting SELinux context on /var/lib/pulp/.selinux-test (dest `/var/lib/pulp/.selinux-test`) | pulp_install_selinux_policies\|bool or (ansible_facts.selinux.status == "enabled" and pulp_install_selinux_policies == "auto"); ansible_facts.mounts \| selectattr('mount', 'match', '^/var/lib/pulp$') \| list \| count == 1 |
| 1845 | 72.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → selinux_remount.yml | /etc/fstab: Set opt "{{ __remount_opt }}" for mount point {{ __remount_point }} | /etc/fstab: Set opt "{{ __remount_opt }}" for mount point {{ __remount_point }} | pulp_install_selinux_policies\|bool or (ansible_facts.selinux.status == "enabled" and pulp_install_selinux_policies == "auto"); ansible_facts.mounts \| selectattr('mount', 'match', '^/var/lib/pulp$') \| list \| count == 1; not pulp_selinux_test.secontext \| regex_search('pulpcore_var_lib_t'); not ansible_facts.mounts \| selectattr('mount', 'match', '^/var/lib/pulp$') \| map(attribute='options') \| first \| regex_search('context=') |
| 1846 | 72.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → selinux_remount.yml | Remount the static dir | Begin conditional or grouped task block | pulp_install_selinux_policies\|bool or (ansible_facts.selinux.status == "enabled" and pulp_install_selinux_policies == "auto"); ansible_facts.mounts \| selectattr('mount', 'match', '^/var/lib/pulp$') \| list \| count == 1; fstab.changed |
| 1847 | 72.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → selinux_remount.yml → Remount the static dir | If {{ __remount_point }} changed, unmount | If {{ __remount_point }} changed, unmount | pulp_install_selinux_policies\|bool or (ansible_facts.selinux.status == "enabled" and pulp_install_selinux_policies == "auto"); ansible_facts.mounts \| selectattr('mount', 'match', '^/var/lib/pulp$') \| list \| count == 1; fstab.changed |
| 1848 | 72.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → selinux_remount.yml → Remount the static dir | If {{ __remount_point }} changed, remount | If {{ __remount_point }} changed, remount | pulp_install_selinux_policies\|bool or (ansible_facts.selinux.status == "enabled" and pulp_install_selinux_policies == "auto"); ansible_facts.mounts \| selectattr('mount', 'match', '^/var/lib/pulp$') \| list \| count == 1; fstab.changed |
| 1849 | 72.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → selinux_remount.yml | Remount the static dir (rescue) | Run rescue tasks for block | pulp_install_selinux_policies\|bool or (ansible_facts.selinux.status == "enabled" and pulp_install_selinux_policies == "auto"); ansible_facts.mounts \| selectattr('mount', 'match', '^/var/lib/pulp$') \| list \| count == 1; fstab.changed |
| 1850 | 72.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → selinux_remount.yml → Remount the static dir (rescue) | Stop pulpcore service | Stop pulpcore service (service `pulpcore.service`) | pulp_install_selinux_policies\|bool or (ansible_facts.selinux.status == "enabled" and pulp_install_selinux_policies == "auto"); ansible_facts.mounts \| selectattr('mount', 'match', '^/var/lib/pulp$') \| list \| count == 1; fstab.changed |
| 1851 | 72.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → selinux_remount.yml → Remount the static dir (rescue) | Unmount /var/lib/pulp/pulpcore_static | Unmount /var/lib/pulp/pulpcore_static | pulp_install_selinux_policies\|bool or (ansible_facts.selinux.status == "enabled" and pulp_install_selinux_policies == "auto"); ansible_facts.mounts \| selectattr('mount', 'match', '^/var/lib/pulp$') \| list \| count == 1; fstab.changed |
| 1852 | 72.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → selinux_remount.yml → Remount the static dir (rescue) | If {{ __remount_point }} changed, unmount (retry) | If {{ __remount_point }} changed, unmount (retry) | pulp_install_selinux_policies\|bool or (ansible_facts.selinux.status == "enabled" and pulp_install_selinux_policies == "auto"); ansible_facts.mounts \| selectattr('mount', 'match', '^/var/lib/pulp$') \| list \| count == 1; fstab.changed |
| 1853 | 72.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → selinux_remount.yml → Remount the static dir (rescue) | If {{ __remount_point }} changed, remount (retry) | If {{ __remount_point }} changed, remount (retry) | pulp_install_selinux_policies\|bool or (ansible_facts.selinux.status == "enabled" and pulp_install_selinux_policies == "auto"); ansible_facts.mounts \| selectattr('mount', 'match', '^/var/lib/pulp$') \| list \| count == 1; fstab.changed |
| 1854 | 72.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → selinux_remount.yml → Remount the static dir (rescue) | Remount /var/lib/pulp/pulpcore_static | Remount /var/lib/pulp/pulpcore_static | pulp_install_selinux_policies\|bool or (ansible_facts.selinux.status == "enabled" and pulp_install_selinux_policies == "auto"); ansible_facts.mounts \| selectattr('mount', 'match', '^/var/lib/pulp$') \| list \| count == 1; fstab.changed; static_dir_unmount.changed |
| 1855 | 72.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → install.yml → selinux_remount.yml → Remount the static dir (rescue) | Start pulpcore service | Start pulpcore service (service `pulpcore.service`) | pulp_install_selinux_policies\|bool or (ansible_facts.selinux.status == "enabled" and pulp_install_selinux_policies == "auto"); ansible_facts.mounts \| selectattr('mount', 'match', '^/var/lib/pulp$') \| list \| count == 1; fstab.changed |
| 1856 | 72.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml | import_tasks | Import a task file (static include) (file `configure.yml`) | — |
| 1857 | 72.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml | file `configure.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/configure.yml` | — |
| 1858 | 72.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml | Obtain current pulp settings | Begin conditional or grouped task block | _aap24_upgrade \| bool |
| 1859 | 73.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → Obtain current pulp settings | Check if pulp configuration file exists | Check if pulp configuration file exists | _aap24_upgrade \| bool |
| 1860 | 73.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → Obtain current pulp settings | Obtain current pulp settings | Obtain current pulp settings | _aap24_upgrade \| bool; _pulp_settings_file.stat.exists \| bool |
| 1861 | 73.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml | Create configuration directory for Pulp | Create configuration directory for Pulp | — |
| 1862 | 73.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml | Create configuration file for Pulp | Create configuration file for Pulp (dest `{{ pulp_settings_file }}`) | — |
| 1863 | 73.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml | copy external auth settings when upgrading from AAP 2.4 | copy external auth settings when upgrading from AAP 2.4 (file `copy_external_auth_settings.yml`) | _aap24_upgrade \| bool |
| 1864 | 73.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → copy_external_auth_settings.yml | file `copy_external_auth_settings.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/copy_external_auth_settings.yml` | _aap24_upgrade \| bool |
| 1865 | 73.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → copy_external_auth_settings.yml | Obtain external auth settings (SSO) | Begin conditional or grouped task block | _aap24_upgrade \| bool; _aap24_pulp_settings.get('content', '') \| b64decode \| regex_search('KEYCLOAK_HOST') |
| 1866 | 73.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → copy_external_auth_settings.yml → Obtain external auth settings (SSO) | Load SSO variables | Load SSO variables | _aap24_upgrade \| bool; _aap24_pulp_settings.get('content', '') \| b64decode \| regex_search('KEYCLOAK_HOST') |
| 1867 | 73.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → copy_external_auth_settings.yml → Obtain external auth settings (SSO) | ansible.builtin.set_fact | Run `ansible.builtin.set_fact` | _aap24_upgrade \| bool; _aap24_pulp_settings.get('content', '') \| b64decode \| regex_search('KEYCLOAK_HOST'); item \| split \| first \| lower in sso_settings_for_upgrade |
| 1868 | 73.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → copy_external_auth_settings.yml | Obtain external auth settings (LDAP) | Begin conditional or grouped task block | _aap24_upgrade \| bool; _aap24_pulp_settings.get('content', '') \| b64decode \| regex_search('AUTHENTICATION_BACKEND_PRESET.*ldap') |
| 1869 | 73.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → copy_external_auth_settings.yml → Obtain external auth settings (LDAP) | ansible.builtin.set_fact | Run `ansible.builtin.set_fact` | _aap24_upgrade \| bool; _aap24_pulp_settings.get('content', '') \| b64decode \| regex_search('AUTHENTICATION_BACKEND_PRESET.*ldap') |
| 1870 | 73.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → copy_external_auth_settings.yml → Obtain external auth settings (LDAP) | ansible.builtin.set_fact | Run `ansible.builtin.set_fact` | _aap24_upgrade \| bool; _aap24_pulp_settings.get('content', '') \| b64decode \| regex_search('AUTHENTICATION_BACKEND_PRESET.*ldap'); item.startswith('AUTH_LDAP') |
| 1871 | 73.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → copy_external_auth_settings.yml | Gather external auth setting | Gather external auth setting | _aap24_upgrade \| bool |
| 1872 | 73.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → copy_external_auth_settings.yml | Update pulp settings file | Update pulp settings file | _aap24_upgrade \| bool; _hub_external_auth \| length |
| 1873 | 73.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → copy_external_auth_settings.yml | Save external auth settings | Save external auth settings | _aap24_upgrade \| bool; _hub_external_auth \| length |
| 1874 | 73.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml | Create local configuration file for Pulp | Create local configuration file for Pulp | — |
| 1875 | 73.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml | Create wrapper to call pulpcore-manager | Create wrapper to call pulpcore-manager (dest `/usr/local/bin/pulpcore-manager`) | — |
| 1876 | 73.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml | Set up galaxy-importer config | Set up galaxy-importer config (file `galaxy_importer.yml`) | galaxy_importer_settings is defined |
| 1877 | 73.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → galaxy_importer.yml | file `galaxy_importer.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/galaxy_importer.yml` | galaxy_importer_settings is defined |
| 1878 | 73.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → galaxy_importer.yml | Create galaxy-importer directory | Create galaxy-importer directory | galaxy_importer_settings is defined |
| 1879 | 73.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → galaxy_importer.yml | Write galaxy-importer config | Write galaxy-importer config (dest `/etc/galaxy-importer/galaxy-importer.cfg`) | galaxy_importer_settings is defined |
| 1880 | 73.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml | Set up API access log | Set up API access log (file `setup_galaxy_api_access_log.yml`) | — |
| 1881 | 73.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → setup_galaxy_api_access_log.yml | file `setup_galaxy_api_access_log.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/setup_galaxy_api_access_log.yml` | — |
| 1882 | 73.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → setup_galaxy_api_access_log.yml | Create Galaxy api access log file | Create Galaxy api access log file | — |
| 1883 | 73.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → setup_galaxy_api_access_log.yml | Populate service facts | Populate service facts | — |
| 1884 | 74.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → setup_galaxy_api_access_log.yml | Set environment variable for Galaxy api access logging | Set environment variable for Galaxy api access logging | — |
| 1885 | 74.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → setup_galaxy_api_access_log.yml | Trigger pulpcore-api restart if service exists | Trigger pulpcore-api restart if service exists | ansible_facts.services['pulpcore-api.service'] is defined |
| 1886 | 74.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml | Set gpg socket directory for Hub HA | Set gpg socket directory for Hub HA | groups['automationhub'] \| length > 1; not pulp_gnupg_socket_dir \| default('') \| length |
| 1887 | 74.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml | Override gpg socket directory | Begin conditional or grouped task block | pulp_gnupg_socket_dir \| default('') \| length |
| 1888 | 74.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → Override gpg socket directory | Create gpg socket directory | Create gpg socket directory | pulp_gnupg_socket_dir \| default('') \| length |
| 1889 | 74.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → Override gpg socket directory | Create tmpfiles.d entry for gpg socket directory | Create tmpfiles.d entry for gpg socket directory | pulp_gnupg_socket_dir \| default('') \| length |
| 1890 | 74.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → Override gpg socket directory | Create {{ pulp_user }} gpg directory | Create {{ pulp_user }} gpg directory | pulp_gnupg_socket_dir \| default('') \| length |
| 1891 | 74.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → Override gpg socket directory | Set gpg socket path | Set gpg socket path | pulp_gnupg_socket_dir \| default('') \| length |
| 1892 | 74.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml | Set up collection signing | Set up collection signing (file `collection_signing_service.yml`) | automationhub_create_default_collection_signing_service \| bool |
| 1893 | 74.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → collection_signing_service.yml | file `collection_signing_service.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/collection_signing_service.yml` | automationhub_create_default_collection_signing_service \| bool |
| 1894 | 74.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → collection_signing_service.yml | Import the collection signing service gpg key file | Import the collection signing service gpg key file (dest `{{ pulp_certs_dir }}/galaxy_signing_service.gpg`) | automationhub_create_default_collection_signing_service \| bool |
| 1895 | 74.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → collection_signing_service.yml | Import the collection signing service script | Import the collection signing service script (dest `{{ pulp_scripts_dir }}/collection_sign.sh`) | automationhub_create_default_collection_signing_service \| bool |
| 1896 | 74.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → collection_signing_service.yml | Commands run as pulp_user | Begin conditional or grouped task block | automationhub_create_default_collection_signing_service \| bool |
| 1897 | 74.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → collection_signing_service.yml → Commands run as pulp_user | Inspect collection signing service GPG key file | Inspect collection signing service GPG key file | automationhub_create_default_collection_signing_service \| bool |
| 1898 | 74.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → collection_signing_service.yml → Commands run as pulp_user | Import collection signing service GPG key file | Import collection signing service GPG key file | automationhub_create_default_collection_signing_service \| bool |
| 1899 | 74.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → collection_signing_service.yml → Commands run as pulp_user | Trust the collection signing service GPG key | Trust the collection signing service GPG key | automationhub_create_default_collection_signing_service \| bool |
| 1900 | 74.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml | Set up container signing | Set up container signing (file `container_signing_service.yml`) | automationhub_create_default_container_signing_service \| bool |
| 1901 | 74.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → container_signing_service.yml | file `container_signing_service.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/container_signing_service.yml` | automationhub_create_default_container_signing_service \| bool |
| 1902 | 74.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → container_signing_service.yml | Import the container signing service gpg key file | Import the container signing service gpg key file (dest `{{ pulp_certs_dir }}/container_signing_service.gpg`) | automationhub_create_default_container_signing_service \| bool |
| 1903 | 74.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → container_signing_service.yml | Import the container signing service script | Import the container signing service script (dest `{{ pulp_scripts_dir }}/container_sign.sh`) | automationhub_create_default_container_signing_service \| bool |
| 1904 | 74.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → container_signing_service.yml | Commands run as pulp_user | Begin conditional or grouped task block | automationhub_create_default_container_signing_service \| bool |
| 1905 | 74.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → container_signing_service.yml → Commands run as pulp_user | Inspect container signing service GPG key file | Inspect container signing service GPG key file | automationhub_create_default_container_signing_service \| bool |
| 1906 | 74.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → container_signing_service.yml → Commands run as pulp_user | Import container signing service GPG key file | Import container signing service GPG key file | automationhub_create_default_container_signing_service \| bool |
| 1907 | 74.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → configure.yml → container_signing_service.yml → Commands run as pulp_user | Trust the container signing service GPG key | Trust the container signing service GPG key | automationhub_create_default_container_signing_service \| bool |
| 1908 | 74.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml | Install pulpcore service | Install pulpcore service (dest `/lib/systemd/system/pulpcore.service`) | — |
| 1909 | 74.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml | Flush handlers | Flush handlers (flush_handlers) | — |
| 1910 | 75.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml | Set state of pulpcore app (block) | Begin conditional or grouped task block | — |
| 1911 | 75.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → Set state of pulpcore app (block) | Set state of pulpcore app | Set state of pulpcore app (service `pulpcore.service`) | — |
| 1912 | 75.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml | Set state of pulpcore app (block) (rescue) | Run rescue tasks for block | — |
| 1913 | 75.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → Set state of pulpcore app (block) (rescue) | Set state of pulpcore app (retry) | Set state of pulpcore app (retry) (service `pulpcore.service`) | — |
| 1914 | 75.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → Set state of pulpcore app (block) (rescue) | Print status of pulpcore services and fail | Print status of pulpcore services and fail (file `print_status_and_fail.yml`) | result_restart_service is not defined |
| 1915 | 75.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → Set state of pulpcore app (block) (rescue) → print_status_and_fail.yml | file `print_status_and_fail.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/print_status_and_fail.yml` | result_restart_service is not defined |
| 1916 | 75.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → Set state of pulpcore app (block) (rescue) → print_status_and_fail.yml | Get status of pulp services | Get status of pulp services | result_restart_service is not defined |
| 1917 | 75.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → Set state of pulpcore app (block) (rescue) → print_status_and_fail.yml | Show output of pulp services | Show output of pulp services | result_restart_service is not defined |
| 1918 | 75.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_common.yml → Set state of pulpcore app (block) (rescue) → print_status_and_fail.yml | Fail | Fail | result_restart_service is not defined |
| 1919 | 75.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub | Configure certificates | Configure certificates (file `certificates.yml`) | — |
| 1920 | 75.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml | file `certificates.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/certificates.yml` | — |
| 1921 | 75.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml | Create cert directory to hold certificates | Create cert directory to hold certificates | — |
| 1922 | 75.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml | Configure SSL certificates for web server | Begin conditional or grouped task block | not automationhub_disable_https \| bool |
| 1923 | 75.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Configure SSL certificates for web server | Copy user provided SSL cert | Begin conditional or grouped task block | not automationhub_disable_https \| bool; automationhub_ssl_cert \| default('') \| length |
| 1924 | 75.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Configure SSL certificates for web server → Copy user provided SSL cert | Import specified TLS certificate | Import specified TLS certificate | not automationhub_disable_https \| bool; automationhub_ssl_cert \| default('') \| length |
| 1925 | 75.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Configure SSL certificates for web server → Copy user provided SSL cert | Import specified TLS private key | Import specified TLS private key | not automationhub_disable_https \| bool; automationhub_ssl_cert \| default('') \| length |
| 1926 | 75.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Configure SSL certificates for web server | Use internal CA | Use internal CA (role `certificate_authority`, tasks `sign_service.yml`) | not automationhub_disable_https \| bool; not automationhub_ssl_cert \| default('') \| length |
| 1927 | 75.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Configure SSL certificates for web server → certificate_authority | role `certificate_authority` (`sign_service.yml`) | Enter role task list | not automationhub_disable_https \| bool; not automationhub_ssl_cert \| default('') \| length |
| 1928 | 75.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Configure SSL certificates for web server → certificate_authority → certificate_authority | file `sign_service.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/sign_service.yml` | not automationhub_disable_https \| bool; not automationhub_ssl_cert \| default('') \| length |
| 1929 | 75.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Configure SSL certificates for web server → certificate_authority → certificate_authority | include_tasks | Include a task file from the current role or playbook (file `init.yml`) | not automationhub_disable_https \| bool; not automationhub_ssl_cert \| default('') \| length |
| 1930 | 75.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Configure SSL certificates for web server → certificate_authority → certificate_authority → init.yml | file `init.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/init.yml` | not automationhub_disable_https \| bool; not automationhub_ssl_cert \| default('') \| length |
| 1931 | 75.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Configure SSL certificates for web server → certificate_authority → certificate_authority → init.yml | Ensure python cryptography libraries are installed | Ensure python cryptography libraries are installed | not automationhub_disable_https \| bool; not automationhub_ssl_cert \| default('') \| length |
| 1932 | 75.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Configure SSL certificates for web server → certificate_authority → certificate_authority | Create {{ aap_ca_servicename }} private key | Create {{ aap_ca_servicename }} private key | not automationhub_disable_https \| bool; not automationhub_ssl_cert \| default('') \| length |
| 1933 | 75.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Configure SSL certificates for web server → certificate_authority → certificate_authority | set_fact | Set a host fact used by later tasks | not automationhub_disable_https \| bool; not automationhub_ssl_cert \| default('') \| length |
| 1934 | 75.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Configure SSL certificates for web server → certificate_authority → certificate_authority | Check if {{ aap_ca_servicename }} public key exists | Check if {{ aap_ca_servicename }} public key exists | not automationhub_disable_https \| bool; not automationhub_ssl_cert \| default('') \| length |
| 1935 | 76.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Configure SSL certificates for web server → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | not automationhub_disable_https \| bool; not automationhub_ssl_cert \| default('') \| length; certificate_stat.stat.exists \| bool |
| 1936 | 76.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Configure SSL certificates for web server → certificate_authority → certificate_authority → block | Check contents of {{ aap_ca_servicename }} public key | Check contents of {{ aap_ca_servicename }} public key | not automationhub_disable_https \| bool; not automationhub_ssl_cert \| default('') \| length; certificate_stat.stat.exists \| bool |
| 1937 | 76.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Configure SSL certificates for web server → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | not automationhub_disable_https \| bool; not automationhub_ssl_cert \| default('') \| length; certificate_stat.stat.exists \| bool; aap_service_san_records_internal \| union(aap_service_san_records) \| difference(certificate_check.subject_alt_name) \| length > 0 |
| 1938 | 76.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Configure SSL certificates for web server → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | not automationhub_disable_https \| bool; not automationhub_ssl_cert \| default('') \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1939 | 76.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Configure SSL certificates for web server → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | not automationhub_disable_https \| bool; not automationhub_ssl_cert \| default('') \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false); aap_service_is_client |
| 1940 | 76.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Configure SSL certificates for web server → certificate_authority → certificate_authority → block | Create CSR for {{ aap_ca_servicename }} node/s | Create CSR for {{ aap_ca_servicename }} node/s | not automationhub_disable_https \| bool; not automationhub_ssl_cert \| default('') \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1941 | 76.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Configure SSL certificates for web server → certificate_authority → certificate_authority → block | Sign {{ aap_ca_servicename }} public key | Sign {{ aap_ca_servicename }} public key | not automationhub_disable_https \| bool; not automationhub_ssl_cert \| default('') \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1942 | 76.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Configure SSL certificates for web server → certificate_authority → certificate_authority → block | Write public key to {{ aap_ca_servicename }} node/s | Write public key to {{ aap_ca_servicename }} node/s (dest `{{ aap_service_cert_fullpath }}`) | not automationhub_disable_https \| bool; not automationhub_ssl_cert \| default('') \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 1943 | 76.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Configure SSL certificates for web server → certificate_authority → certificate_authority | Signal restart if certificate pair changes | Signal restart if certificate pair changes | not automationhub_disable_https \| bool; not automationhub_ssl_cert \| default('') \| length; (write_key is changed or write_certificate is changed) |
| 1944 | 76.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Configure SSL certificates for web server | Trigger nginx notifier for internally managed certificate changes | Trigger nginx notifier for internally managed certificate changes | not automationhub_disable_https \| bool; aap_service_restart \| default(false) |
| 1945 | 76.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Configure SSL certificates for web server | set_fact | Set a host fact used by later tasks | not automationhub_disable_https \| bool |
| 1946 | 76.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml | Copy pg client certs for verify-ca | Begin conditional or grouped task block | automationhub_pg_cert_auth |
| 1947 | 76.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Copy pg client certs for verify-ca | Copy postgres client cert | Copy postgres client cert (dest `{{ pulp_certs_dir }}/pg_client.crt`) | automationhub_pg_cert_auth |
| 1948 | 76.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub → certificates.yml → Copy pg client certs for verify-ca | Copy postgres client key | Copy postgres client key (dest `{{ pulp_certs_dir }}/pg_client.key`) | automationhub_pg_cert_auth |
| 1949 | 76.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub | Configure database for Pulp | Configure database for Pulp (file `pulp_database_config.yml`) | — |
| 1950 | 76.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml | file `pulp_database_config.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/pulp_database_config.yml` | — |
| 1951 | 76.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml | Pick & set the correct host to run pulp_database_config based on existing keys | Begin conditional or grouped task block | not pulp_db_fields_key \| length |
| 1952 | 76.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml → Pick & set the correct host to run pulp_database_config based on existing keys | Check if any hosts already have the database fields encryption key | Check if any hosts already have the database fields encryption key | not pulp_db_fields_key \| length |
| 1953 | 76.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml → Pick & set the correct host to run pulp_database_config based on existing keys | Pick & set the sole host used to run the remainder of the pulp_database_config role, if any hosts have the key | Pick & set the sole host used to run the remainder of the pulp_database_config role, if any hosts have the key | not pulp_db_fields_key \| length; item.stat.exists |
| 1954 | 76.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml | Pick & set the sole host to run pulp_database_config if no host has the key, or if the key is to be imported | Pick & set the sole host to run pulp_database_config if no host has the key, or if the key is to be imported | hostvars['localhost']['pulp_database_config_host'] is not defined |
| 1955 | 76.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml | Import Pulp Database Encryption Key | Import Pulp Database Encryption Key (dest `{{ __pulp_common_merged_pulp_settings.db_encryption_key }}`) | pulp_db_fields_key \| length |
| 1956 | 76.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml | Generate Pulp Database Encryption Key | Begin conditional or grouped task block | not pulp_db_fields_key \| length |
| 1957 | 76.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml → Generate Pulp Database Encryption Key | import_tasks | Import a task file (static include) (file `generate_database_fields_key.yml`) | not pulp_db_fields_key \| length |
| 1958 | 76.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml → Generate Pulp Database Encryption Key → generate_database_fields_key.yml | file `generate_database_fields_key.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/generate_database_fields_key.yml` | not pulp_db_fields_key \| length |
| 1959 | 76.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml → Generate Pulp Database Encryption Key → generate_database_fields_key.yml | Check for existing Pulp Database Encryption Key | Check for existing Pulp Database Encryption Key | not pulp_db_fields_key \| length |
| 1960 | 76.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml → Generate Pulp Database Encryption Key → generate_database_fields_key.yml | Install the openssl command | Install the openssl command | not pulp_db_fields_key \| length; not __pulp_db_fields_key_file.stat.exists |
| 1961 | 77.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml → Generate Pulp Database Encryption Key → generate_database_fields_key.yml | Generate Pulp Database Encryption Key | Generate Pulp Database Encryption Key | not pulp_db_fields_key \| length; not __pulp_db_fields_key_file.stat.exists |
| 1962 | 77.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml → Generate Pulp Database Encryption Key → generate_database_fields_key.yml | Ensure correct permissions on new Pulp Database Encryption Key | Ensure correct permissions on new Pulp Database Encryption Key | not pulp_db_fields_key \| length |
| 1963 | 77.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml → Generate Pulp Database Encryption Key | Get the database fields key | Get the database fields key | not pulp_db_fields_key \| length |
| 1964 | 77.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml → Generate Pulp Database Encryption Key | Copy Pulp Database Encryption Key | Copy Pulp Database Encryption Key (dest `{{ __pulp_common_merged_pulp_settings.db_encryption_key }}`) | not pulp_db_fields_key \| length |
| 1965 | 77.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml | Run database migrations, manage the admin account and set sync task | Begin conditional or grouped task block | — |
| 1966 | 77.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml → Run database migrations, manage the admin account and set sync task | Run database migrations | Run database migrations | — |
| 1967 | 77.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml → Run database migrations, manage the admin account and set sync task | Check if admin account has been created | Check if admin account has been created | — |
| 1968 | 77.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml → Run database migrations, manage the admin account and set sync task | Set the Pulp admin user's password | Set the Pulp admin user's password | ((__pulp_database_config_admin_account_exists.stdout == "False") \| default(false)) or automationhub_force_change_admin_password \| bool |
| 1969 | 77.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml → Run database migrations, manage the admin account and set sync task | Schedule resource sync task | Schedule resource sync task | — |
| 1970 | 77.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml | Run the pulpcore-manager container-repair-media-type command (packages mode) | Run the pulpcore-manager container-repair-media-type command (packages mode) | pulp_container_old_version is defined; automationhub_container_repair_media_type \| bool or (automationhub_container_repair_media_type == "auto" and ((pulp_container_old_version is version("2.9.5", "<=")) or (pulp_container_old_version is version("2.10.0", ">=") and pulp_container_old_version is version("2.10.4", "<=")) or (pulp_container_old_version is version("2.11.0", ">=") and pulp_container_old_version is version("2.11.1", "<=")) or (pulp_container_old_version is version("2.12.0", ">=") and pulp_container_old_version is version("2.12.2", "<=")) or pulp_container_old_version is version("2.13.0", "==")) and ((ansible_facts.packages[pulp_pkg_name_prefix + 'pulp-container'][0].version is version("2.9.7", ">=") and ansible_facts.packages[pulp_pkg_name_prefix + 'pulp-container'][0].version is version("2.10.0", "<") ) or (ansible_facts.packages[pulp_pkg_name_prefix + 'pulp-container'][0].version is version("2.10.8", ">=") and ansible_facts.packages[pulp_pkg_name_prefix + 'pulp-container'][0].version is version("2.11.0", "<")) or (ansible_facts.packages[pulp_pkg_name_prefix + 'pulp-container'][0].version is version("2.11.1", ">=") and ansible_facts.packages[pulp_pkg_name_prefix + 'pulp-container'][0].version is version("2.12.0", "<")) or (ansible_facts.packages[pulp_pkg_name_prefix + 'pulp-container'][0].version is version("2.12.3", ">=") and ansible_facts.packages[pulp_pkg_name_prefix + 'pulp-container'][0].version is version("2.13.0", "<")) or ansible_facts.packages[pulp_pkg_name_prefix + 'pulp-container'][0].version is version("2.13.2", ">=")) ) |
| 1971 | 77.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml | Show output from the repair command | Show output from the repair command | — |
| 1972 | 77.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml | Add the galaxy collection signing service to the Pulp application | Add the galaxy collection signing service to the Pulp application | automationhub_create_default_collection_signing_service |
| 1973 | 77.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_database_config.yml | Add the galaxy container signing service to the Pulp application | Add the galaxy container signing service to the Pulp application | automationhub_create_default_container_signing_service |
| 1974 | 77.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub | Set up Pulp API | Set up Pulp API (file `pulp_api.yml`) | — |
| 1975 | 77.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_api.yml | file `pulp_api.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/pulp_api.yml` | — |
| 1976 | 77.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_api.yml | Ensure python-cryptography is installed | Ensure python-cryptography is installed | — |
| 1977 | 77.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_api.yml | import_tasks | Import a task file (static include) (file `generate_token_auth_key.yml`) | pulp_token_auth_key is undefined |
| 1978 | 77.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_api.yml → generate_token_auth_key.yml | file `generate_token_auth_key.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/generate_token_auth_key.yml` | pulp_token_auth_key is undefined |
| 1979 | 77.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_api.yml → generate_token_auth_key.yml | Check if any of said hosts already have the token authentication private key | Check if any of said hosts already have the token authentication private key | pulp_token_auth_key is undefined |
| 1980 | 77.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_api.yml → generate_token_auth_key.yml | Pick & set the host to copy the token auth private key from, if any hosts have the key | Pick & set the host to copy the token auth private key from, if any hosts have the key | pulp_token_auth_key is undefined; hostvars['localhost']['__pulp_token_auth_key_host'] is not defined; item.stat.exists |
| 1981 | 77.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_api.yml → generate_token_auth_key.yml | Pick & set the sole host to generate the token auth private key if no host has the key | Pick & set the sole host to generate the token auth private key if no host has the key | pulp_token_auth_key is undefined; hostvars['localhost']['__pulp_token_auth_key_host'] is not defined |
| 1982 | 77.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_api.yml → generate_token_auth_key.yml | Generate the token auth private key | Generate the token auth private key | pulp_token_auth_key is undefined |
| 1983 | 77.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_api.yml → generate_token_auth_key.yml | Get the token auth private key from the host that has it or that was used to generate it | Get the token auth private key from the host that has it or that was used to generate it | pulp_token_auth_key is undefined |
| 1984 | 77.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_api.yml → generate_token_auth_key.yml | Copy the token auth private key to all the other hosts | Copy the token auth private key to all the other hosts (dest `{{ pulp_certs_dir }}/token_private_key.pem`) | pulp_token_auth_key is undefined |
| 1985 | 77.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_api.yml | import_tasks | Import a task file (static include) (file `import_token_auth_key.yml`) | pulp_token_auth_key is defined |
| 1986 | 78.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_api.yml → import_token_auth_key.yml | file `import_token_auth_key.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/import_token_auth_key.yml` | pulp_token_auth_key is defined |
| 1987 | 78.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_api.yml → import_token_auth_key.yml | Import specified token authentication key | Import specified token authentication key (dest `{{ pulp_certs_dir }}/token_private_key.pem`) | pulp_token_auth_key is defined |
| 1988 | 78.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_api.yml | Extract token authentication public key | Extract token authentication public key | — |
| 1989 | 78.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_api.yml | Install pulpcore-api service files | Install pulpcore-api service files (dest `/lib/systemd/system/pulpcore-api.service`) | — |
| 1990 | 78.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_api.yml | Set the pulpcore-api service state | Set the pulpcore-api service state (service `pulpcore-api.service`) | — |
| 1991 | 78.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub | Set up Pulp Content | Set up Pulp Content (file `pulp_content.yml`) | — |
| 1992 | 78.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_content.yml | file `pulp_content.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/pulp_content.yml` | — |
| 1993 | 78.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_content.yml | Install pulpcore-content service file | Install pulpcore-content service file (dest `/lib/systemd/system/pulpcore-content.service`) | — |
| 1994 | 78.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_content.yml | Set state of pulp content app | Set state of pulp content app (service `pulpcore-content.service`) | — |
| 1995 | 78.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub | Set up Pulp Workers | Set up Pulp Workers (file `pulp_workers.yml`) | — |
| 1996 | 78.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_workers.yml | file `pulp_workers.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/pulp_workers.yml` | — |
| 1997 | 78.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_workers.yml | Install service files | Install service files (dest `/lib/systemd/system/pulpcore-worker@.service`) | — |
| 1998 | 78.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_workers.yml | Get services | Get services | — |
| 1999 | 78.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_workers.yml | pulpcore resource manager | Begin conditional or grouped task block | ansible_facts.services['pulpcore-resource-manager.service'] is defined; __pulpcore_version is version('3.14', '>=') |
| 2000 | 78.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_workers.yml → pulpcore resource manager | Disable pulpcore-resource-manager service | Disable pulpcore-resource-manager service (service `pulpcore-resource-manager.service`) | ansible_facts.services['pulpcore-resource-manager.service'] is defined; __pulpcore_version is version('3.14', '>=') |
| 2001 | 78.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_workers.yml → pulpcore resource manager | Remove pulpcore-resource-manager service | Remove pulpcore-resource-manager service | ansible_facts.services['pulpcore-resource-manager.service'] is defined; __pulpcore_version is version('3.14', '>=') |
| 2002 | 78.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_workers.yml → pulpcore resource manager | Reload systemd for the pulpcore-resource-manager removal | Reload systemd for the pulpcore-resource-manager removal | ansible_facts.services['pulpcore-resource-manager.service'] is defined; __pulpcore_version is version('3.14', '>=') |
| 2003 | 78.6% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_workers.yml | Get already existing workers | Get already existing workers | — |
| 2004 | 78.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_workers.yml | Get the highest worker ID | Get the highest worker ID | __pulp_workers_worker_svcs is defined |
| 2005 | 78.7% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_workers.yml | Scale down workers | Scale down workers (service `pulpcore-worker@{{ __pulp_workers_worker_index }}.service`) | __pulp_workers_worker_max is defined; __pulp_workers_worker_max\|int + 1 > pulp_workers + 1 |
| 2006 | 78.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → pulp_workers.yml | Set states of new pulp workers | Set states of new pulp workers (service `pulpcore-worker@{{ __pulp_workers_worker_index }}.service`) | — |
| 2007 | 78.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub | Configure Nginx for automation hub | Configure Nginx for automation hub (file `nginx.yml`) | — |
| 2008 | 78.8% | install.yml → Install Automation Hub node → block → automationhub → automationhub → nginx.yml | file `nginx.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/nginx.yml` | — |
| 2009 | 78.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub → nginx.yml | Create static dir & ACME dir | Create static dir & ACME dir | — |
| 2010 | 78.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub → nginx.yml | Create directory for Pulp Nginx snippets | Create directory for Pulp Nginx snippets | — |
| 2011 | 78.9% | install.yml → Install Automation Hub node → block → automationhub → automationhub → nginx.yml | Find pulp obsolete symlinks | Find pulp obsolete symlinks | — |
| 2012 | 79.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → nginx.yml | Remove pulp obsolete symlinks | Remove pulp obsolete symlinks | — |
| 2013 | 79.0% | install.yml → Install Automation Hub node → block → automationhub → automationhub → nginx.yml | Set python interpreter | Set python interpreter | — |
| 2014 | 79.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub → nginx.yml | Get webserver snippets path | Get webserver snippets path | — |
| 2015 | 79.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub → nginx.yml | Install webserver snippets | Install webserver snippets | — |
| 2016 | 79.1% | install.yml → Install Automation Hub node → block → automationhub → automationhub → nginx.yml | Replace X-Forwarded-Proto header | Replace X-Forwarded-Proto header | — |
| 2017 | 79.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub → nginx.yml | Add automation hub nginx configuration | Add automation hub nginx configuration | — |
| 2018 | 79.2% | install.yml → Install Automation Hub node → block → automationhub → automationhub → nginx.yml | Configure UI redirect page | Begin conditional or grouped task block | — |
| 2019 | 79.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub → nginx.yml → Configure UI redirect page | Ensure directory exists for UI redirect | Ensure directory exists for UI redirect | — |
| 2020 | 79.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub → nginx.yml → Configure UI redirect page | Copy index.html | Copy index.html | — |
| 2021 | 79.3% | install.yml → Install Automation Hub node → block → automationhub → automationhub → nginx.yml → Configure UI redirect page | Ensure static media directory exists | Ensure static media directory exists | — |
| 2022 | 79.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → nginx.yml → Configure UI redirect page | Copy logo file | Copy logo file | — |
| 2023 | 79.4% | install.yml → Install Automation Hub node → block → automationhub → automationhub → nginx.yml | Trigger pulpcore-api restart on all hosts | Trigger pulpcore-api restart on all hosts | _hub_redirect_result is changed |
| 2024 | 79.5% | install.yml → Install Automation Hub node → block → automationhub → automationhub | Create the automation-hub service symlink | Create the automation-hub service symlink | — |
| 2025 | 79.5% | install.yml → Install Automation Hub node → block | include_role | Include and run tasks from an Ansible role (role `nginx`, tasks `finalize.yml`) | — |
| 2026 | 79.5% | install.yml → Install Automation Hub node → block → nginx | role `nginx` (`finalize.yml`) | Enter role task list | — |
| 2027 | 79.6% | install.yml → Install Automation Hub node → block → nginx → nginx | file `finalize.yml` | Task file `ansible/automation_platform_installer/roles/nginx/tasks/finalize.yml` | — |
| 2028 | 79.6% | install.yml → Install Automation Hub node → block → nginx → nginx | Install nginx.conf | Install nginx.conf (dest `/etc/nginx/nginx.conf`) | — |
| 2029 | 79.7% | install.yml → Install Automation Hub node → block → nginx → nginx | Start nginx and configure to startup automatically | Start nginx and configure to startup automatically (service `nginx`) | — |
| 2030 | 79.7% | install.yml → Install Automation Hub node → block | Enable firewall | Enable firewall (role `firewall`) | — |
| 2031 | 79.7% | install.yml → Install Automation Hub node → block → firewall | role `firewall` | Enter role task list | — |
| 2032 | 79.8% | install.yml → Install Automation Hub node → block → firewall → firewall | file `main.yml` | Task file `ansible/automation_platform_installer/roles/firewall/tasks/main.yml` | — |
| 2033 | 79.8% | install.yml → Install Automation Hub node → block → firewall → firewall | include_tasks | Include a task file from the current role or playbook (file `discover.yml`) | — |
| 2034 | 79.9% | install.yml → Install Automation Hub node → block → firewall → firewall → discover.yml | file `discover.yml` | Task file `ansible/automation_platform_installer/roles/firewall/tasks/discover.yml` | — |
| 2035 | 79.9% | install.yml → Install Automation Hub node → block → firewall → firewall → discover.yml | Determine if firewalld is running | Determine if firewalld is running | — |
| 2036 | 79.9% | install.yml → Install Automation Hub node → block → firewall → firewall → discover.yml | Determine if firewalld is enabled | Determine if firewalld is enabled | — |
| 2037 | 80.0% | install.yml → Install Automation Hub node → block → firewall → firewall → discover.yml | Determine if iptables is installed | Determine if iptables is installed | — |
| 2038 | 80.0% | install.yml → Install Automation Hub node → block → firewall → firewall → discover.yml | Determine if iptables service is installed | Determine if iptables service is installed | — |
| 2039 | 80.0% | install.yml → Install Automation Hub node → block → firewall → firewall → discover.yml | Check to see if there is an iptables config file | Check to see if there is an iptables config file | iptables_installed.rc == 0 |
| 2040 | 80.1% | install.yml → Install Automation Hub node → block → firewall → firewall → discover.yml | Select firewalld | Select firewalld | firewalld_running.rc == 0 or systemctl_unit_files.stdout_lines \| select('match','firewalld.service\s+enabled') \| list \| length == 1 |
| 2041 | 80.1% | install.yml → Install Automation Hub node → block → firewall → firewall → discover.yml | Select iptables | Select iptables | iptables_installed.rc == 0; iptables_exists.stat.exists; iptables_service.stat.exists |
| 2042 | 80.2% | install.yml → Install Automation Hub node → block → firewall → firewall | Include {{ 'no' if component_firewall_type == 'none' else component_firewall_type }} tasks | Include {{ 'no' if component_firewall_type == 'none' else component_firewall_type }} tasks (file `{{ component_firewall_type }}.yml`) | firewalld_running.rc == 0 or systemctl_unit_files.stdout_lines \| select('match','firewalld.service\s+enabled') \| list \| length == 1 or ( iptables_installed.rc == 0 and iptables_exists.stat.exists and iptables_service.stat.exists ) |
| 2043 | 80.2% | install.yml → Install Automation Hub node → block → firewall → firewall → {{ component_firewall_type }}.yml | {{ component_firewall_type }}.yml | Include task file `{{ component_firewall_type }}.yml` (relative to current role task directory) | firewalld_running.rc == 0 or systemctl_unit_files.stdout_lines \| select('match','firewalld.service\s+enabled') \| list \| length == 1 or ( iptables_installed.rc == 0 and iptables_exists.stat.exists and iptables_service.stat.exists ) |
| 2044 | 80.2% | install.yml → Install Automation Hub node | block (always) | Run always tasks for block | — |
| 2045 | 80.3% | install.yml → Install Automation Hub node → block (always) | Run repo_management (teardown) | Run repo_management (teardown) (role `repo_management`, tasks `teardown`) | — |
| 2046 | 80.3% | install.yml → Install Automation Hub node → block (always) → repo_management | role `repo_management` (`teardown`) | Enter role task list | — |
| 2047 | 80.4% | install.yml → Install Automation Hub node → block (always) → repo_management → repo_management | file `teardown.yml` | Task file `ansible/automation_platform_installer/roles/repo_management/tasks/teardown.yml` | — |
| 2048 | 80.4% | install.yml → Install Automation Hub node → block (always) → repo_management → repo_management | Disable Automation Platform repository | Disable Automation Platform repository | extra_repos is defined |
| 2049 | 80.4% | install.yml → Install Automation Hub node → block (always) → repo_management → repo_management | Disable Automation Platform RHSM repository | Disable Automation Platform RHSM repository | aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 2050 | 80.5% | install.yml | Install Automation EDA Controller | Play targeting `automationedacontroller` | — |
| 2051 | 80.5% | install.yml → Install Automation EDA Controller | block | Begin conditional or grouped task block | — |
| 2052 | 80.6% | install.yml → Install Automation EDA Controller → block | Run repo_management (setup) | Run repo_management (setup) (role `repo_management`, tasks `setup`) | — |
| 2053 | 80.6% | install.yml → Install Automation EDA Controller → block → repo_management | role `repo_management` (`setup`) | Enter role task list | — |
| 2054 | 80.6% | install.yml → Install Automation EDA Controller → block → repo_management → repo_management | file `setup.yml` | Task file `ansible/automation_platform_installer/roles/repo_management/tasks/setup.yml` | — |
| 2055 | 80.7% | install.yml → Install Automation EDA Controller → block → repo_management → repo_management | include_vars | Load variables from a file | — |
| 2056 | 80.7% | install.yml → Install Automation EDA Controller → block → repo_management → repo_management | Install yum repo keys | Install yum repo keys (dest `/etc/pki/rpm-gpg/{{ item }}`) | — |
| 2057 | 80.8% | install.yml → Install Automation EDA Controller → block → repo_management → repo_management | Install subscription-manager-rhsm-certificates | Install subscription-manager-rhsm-certificates | — |
| 2058 | 80.8% | install.yml → Install Automation EDA Controller → block → repo_management → repo_management | Determine if rhsm repository exists | Determine if rhsm repository exists | — |
| 2059 | 80.8% | install.yml → Install Automation EDA Controller → block → repo_management → repo_management | Enable Automation Platform rhsm repository | Enable Automation Platform rhsm repository | ansible_distribution == 'RedHat'; rhsm_repo.stat.exists; not bundle_install \| bool; aw_repo_url is undefined |
| 2060 | 80.9% | install.yml → Install Automation EDA Controller → block → repo_management → repo_management | Explain Automation Platform rhsm repository failure | Explain Automation Platform rhsm repository failure | aap_rhsm_repo_enable.failed \| default(false) |
| 2061 | 80.9% | install.yml → Install Automation EDA Controller → block → repo_management → repo_management | block | Begin conditional or grouped task block | aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 2062 | 81.0% | install.yml → Install Automation EDA Controller → block → repo_management → repo_management → block | Enable module hotfixes flag | Enable module hotfixes flag | aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 2063 | 81.0% | install.yml → Install Automation EDA Controller → block → repo_management → repo_management → block | Set AAP repo for update | Set AAP repo for update | aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 2064 | 81.0% | install.yml → Install Automation EDA Controller → block → repo_management → repo_management | Ensure rsync is installed | Ensure rsync is installed | bundle_install \| bool |
| 2065 | 81.1% | install.yml → Install Automation EDA Controller → block → repo_management → repo_management | Copy bundle packages to repo source directory | Begin conditional or grouped task block | bundle_install \| bool |
| 2066 | 81.1% | install.yml → Install Automation EDA Controller → block → repo_management → repo_management → Copy bundle packages to repo source directory | Copy bundle packages to repo source directory | Copy bundle packages to repo source directory | bundle_install \| bool |
| 2067 | 81.1% | install.yml → Install Automation EDA Controller → block → repo_management → repo_management | Copy bundle packages to repo source directory (rescue) | Run rescue tasks for block | bundle_install \| bool |
| 2068 | 81.2% | install.yml → Install Automation EDA Controller → block → repo_management → repo_management → Copy bundle packages to repo source directory (rescue) | Remove old bundle packages source directory (legacy) | Remove old bundle packages source directory (legacy) | bundle_install \| bool |
| 2069 | 81.2% | install.yml → Install Automation EDA Controller → block → repo_management → repo_management → Copy bundle packages to repo source directory (rescue) | Copy bundle packages to repo source directory (legacy) | Copy bundle packages to repo source directory (legacy) (dest `{{ bundle_install_folder }}`) | bundle_install \| bool |
| 2070 | 81.3% | install.yml → Install Automation EDA Controller → block → repo_management → repo_management | Install the Automation Platform bundle repository | Install the Automation Platform bundle repository (dest `/etc/yum.repos.d/ansible-automation-platform.repo`) | bundle_install \| bool |
| 2071 | 81.3% | install.yml → Install Automation EDA Controller → block → repo_management → repo_management | Remove legacy ansible-tower repository | Remove legacy ansible-tower repository | — |
| 2072 | 81.3% | install.yml → Install Automation EDA Controller → block → repo_management → repo_management | Install the Automation Platform yum repository. | Install the Automation Platform yum repository. (dest `/etc/yum.repos.d/ansible-automation-platform.repo`) | aw_repo_url is defined; not bundle_install \| bool |
| 2073 | 81.4% | install.yml → Install Automation EDA Controller → block → repo_management → repo_management | Set repo list for bundle install | Set repo list for bundle install | bundle_install \| bool |
| 2074 | 81.4% | install.yml → Install Automation EDA Controller → block → repo_management → repo_management | Set repo list for online install | Set repo list for online install | yum_repo is succeeded; yum_repo is not skipped |
| 2075 | 81.5% | install.yml → Install Automation EDA Controller → block → repo_management → repo_management | Enable Ansible Automation Platform repo | Enable Ansible Automation Platform repo | extra_repos is defined |
| 2076 | 81.5% | install.yml → Install Automation EDA Controller → block | include_role | Include and run tasks from an Ansible role (role `nginx`) | — |
| 2077 | 81.5% | install.yml → Install Automation EDA Controller → block → nginx | role `nginx` | Enter role task list | — |
| 2078 | 81.6% | install.yml → Install Automation EDA Controller → block → nginx → nginx | file `main.yml` | Task file `ansible/automation_platform_installer/roles/nginx/tasks/main.yml` | — |
| 2079 | 81.6% | install.yml → Install Automation EDA Controller → block → nginx → nginx | include_tasks | Include a task file from the current role or playbook (file `module.yml`) | — |
| 2080 | 81.7% | install.yml → Install Automation EDA Controller → block → nginx → nginx → module.yml | file `module.yml` | Task file `ansible/automation_platform_installer/roles/nginx/tasks/module.yml` | — |
| 2081 | 81.7% | install.yml → Install Automation EDA Controller → block → nginx → nginx → module.yml | Enable nginx module stream | Begin conditional or grouped task block | — |
| 2082 | 81.7% | install.yml → Install Automation EDA Controller → block → nginx → nginx → module.yml → Enable nginx module stream | Disable Nginx module | Disable Nginx module | — |
| 2083 | 81.8% | install.yml → Install Automation EDA Controller → block → nginx → nginx → module.yml → Enable nginx module stream | Enable Nginx module | Enable Nginx module | — |
| 2084 | 81.8% | install.yml → Install Automation EDA Controller → block → nginx → nginx | include_tasks | Include a task file from the current role or playbook (file `tasks.yml`) | — |
| 2085 | 81.9% | install.yml → Install Automation EDA Controller → block → nginx → nginx → tasks.yml | file `tasks.yml` | Task file `ansible/automation_platform_installer/roles/nginx/tasks/tasks.yml` | — |
| 2086 | 81.9% | install.yml → Install Automation EDA Controller → block → nginx → nginx → tasks.yml | Install nginx | Install nginx | — |
| 2087 | 81.9% | install.yml → Install Automation EDA Controller → block → nginx → nginx → tasks.yml | SELinux configuration for nginx | Begin conditional or grouped task block | ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 2088 | 82.0% | install.yml → Install Automation EDA Controller → block → nginx → nginx → tasks.yml → SELinux configuration for nginx | Ensure libsemanage-python is installed | Ensure libsemanage-python is installed | ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 2089 | 82.0% | install.yml → Install Automation EDA Controller → block → nginx → nginx → tasks.yml → SELinux configuration for nginx | Open up permissions on nginx. | Open up permissions on nginx. | ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 2090 | 82.1% | install.yml → Install Automation EDA Controller → block | include_role | Include and run tasks from an Ansible role (role `automationedacontroller`) | — |
| 2091 | 82.1% | install.yml → Install Automation EDA Controller → block → automationedacontroller | role `automationedacontroller` | Enter role task list | — |
| 2092 | 82.1% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | file `main.yml` | Task file `ansible/automation_platform_installer/roles/automationedacontroller/tasks/main.yml` | — |
| 2093 | 82.2% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | Load OS specific variables | Load OS specific variables | — |
| 2094 | 82.2% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | set_fact | Set a host fact used by later tasks | — |
| 2095 | 82.2% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | Map redis hostnames to Event-Driven Ansible | Map redis hostnames to Event-Driven Ansible | _redis_mode == "cluster" |
| 2096 | 82.3% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | Check installed version of PG | Check installed version of PG | — |
| 2097 | 82.3% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | block | Begin conditional or grouped task block | pg_installed_version.failed or pg_installed_version.stdout \| trim \| int != automationedacontroller_postgresql_version \| int |
| 2098 | 82.4% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → block | Disable PostgreSQL module | Disable PostgreSQL module | pg_installed_version.failed or pg_installed_version.stdout \| trim \| int != automationedacontroller_postgresql_version \| int; not pg_installed_version.failed; ansible_distribution_major_version == "8" |
| 2099 | 82.4% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → block | Enable PostgreSQL {{ automationedacontroller_postgresql_version }} dnf modules | Enable PostgreSQL {{ automationedacontroller_postgresql_version }} dnf modules | pg_installed_version.failed or pg_installed_version.stdout \| trim \| int != automationedacontroller_postgresql_version \| int |
| 2100 | 82.4% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → block | Install PostgreSQL client {{ automationedacontroller_postgresql_version }} | Install PostgreSQL client {{ automationedacontroller_postgresql_version }} | pg_installed_version.failed or pg_installed_version.stdout \| trim \| int != automationedacontroller_postgresql_version \| int |
| 2101 | 82.5% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | include_tasks | Include a task file from the current role or playbook (file `deprovision.yml`) | — |
| 2102 | 82.5% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → deprovision.yml | file `deprovision.yml` | Task file `ansible/automation_platform_installer/roles/automationedacontroller/tasks/deprovision.yml` | — |
| 2103 | 82.6% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → deprovision.yml | Remove old automation-eda-controller nginx config | Remove old automation-eda-controller nginx config | — |
| 2104 | 82.6% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → deprovision.yml | Ensure API nginx is not installed on worker nodes | Ensure API nginx is not installed on worker nodes | eda_node_type in ['event-stream', 'worker'] |
| 2105 | 82.6% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → deprovision.yml | Ensure event stream nginx is not installed on api or worker specific nodes | Ensure event stream nginx is not installed on api or worker specific nodes | eda_node_type in ['api', 'worker'] |
| 2106 | 82.7% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → deprovision.yml | block | Begin conditional or grouped task block | eda_node_type in ['api', 'event-stream'] |
| 2107 | 82.7% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → deprovision.yml → block | Ensure worker nginx is only installed on worker nodes | Ensure worker nginx is only installed on worker nodes | eda_node_type in ['api', 'event-stream'] |
| 2108 | 82.8% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → deprovision.yml → block | Stop and remove all running activations on non-worker nodes | Stop and remove all running activations on non-worker nodes | eda_node_type in ['api', 'event-stream']; ansible_facts.services['podman.service'] \| default([]) \| length; ansible_facts.services['podman.service'].state == 'running' |
| 2109 | 82.8% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → deprovision.yml → block | Disable linger on all non-worker nodes | Disable linger on all non-worker nodes | eda_node_type in ['api', 'event-stream'] |
| 2110 | 82.8% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → deprovision.yml → block | Ensure worker service RPMs are absent | Ensure worker service RPMs are absent | eda_node_type in ['api', 'event-stream'] |
| 2111 | 82.9% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → deprovision.yml | Remove event stream specific packages from non event stream nodes | Remove event stream specific packages from non event stream nodes | inventory_hostname not in _automationedacontroller_event_stream_hosts |
| 2112 | 82.9% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → deprovision.yml | Ensure standard service package is not installed on event stream only nodes | Ensure standard service package is not installed on event stream only nodes | eda_node_type == 'event-stream' |
| 2113 | 83.0% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | Check if EDA user exists | Check if EDA user exists | — |
| 2114 | 83.0% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | block | Begin conditional or grouped task block | eda_check_user is not changed |
| 2115 | 83.0% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → block | Ensure EDA user is part of systemd-journal group | Ensure EDA user is part of systemd-journal group | eda_check_user is not changed |
| 2116 | 83.1% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → block | Temporarily disable linger if EDA is not part of systemd-journal group | Temporarily disable linger if EDA is not part of systemd-journal group | eda_check_user is not changed; eda_systemd_journal_group is changed; inventory_hostname in _automationedacontroller_worker_hosts |
| 2117 | 83.1% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | Install automation-eda-controller | Install automation-eda-controller | — |
| 2118 | 83.2% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | Install base services RPM for all nodes except event stream only | Install base services RPM for all nodes except event stream only | eda_node_type != 'event-stream' |
| 2119 | 83.2% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | Install services RPM for worker nodes | Install services RPM for worker nodes | inventory_hostname in _automationedacontroller_worker_hosts |
| 2120 | 83.2% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | Install services RPM for event stream nodes | Install services RPM for event stream nodes | inventory_hostname in _automationedacontroller_event_stream_hosts |
| 2121 | 83.3% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | Update existing dependent packages | Update existing dependent packages (role `repo_management`, tasks `update_dependency`) | — |
| 2122 | 83.3% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → repo_management | role `repo_management` (`update_dependency`) | Enter role task list | — |
| 2123 | 83.3% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → repo_management → repo_management | file `update_dependency.yml` | Task file `ansible/automation_platform_installer/roles/repo_management/tasks/update_dependency.yml` | — |
| 2124 | 83.4% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → repo_management → repo_management | Ensure AAP repo for update is set | Ensure AAP repo for update is set | not aap_repo_for_update \| default('') \| length |
| 2125 | 83.4% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → repo_management → repo_management | Generate a list of AAP packages with an update | Generate a list of AAP packages with an update | — |
| 2126 | 83.5% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → repo_management → repo_management | Set AAP packages to update | Set AAP packages to update | — |
| 2127 | 83.5% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → repo_management → repo_management | Update packages | Update packages | _packages_to_update \| length |
| 2128 | 83.5% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | include_tasks | Include a task file from the current role or playbook (file `provision_worker.yml`) | inventory_hostname in _automationedacontroller_worker_hosts |
| 2129 | 83.6% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml | file `provision_worker.yml` | Task file `ansible/automation_platform_installer/roles/automationedacontroller/tasks/provision_worker.yml` | inventory_hostname in _automationedacontroller_worker_hosts |
| 2130 | 83.6% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml | Tune down number of gunicorn workers on worker nodes | Tune down number of gunicorn workers on worker nodes | inventory_hostname in _automationedacontroller_worker_hosts; eda_node_type == 'worker' |
| 2131 | 83.7% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml | Apply EDA worker nginx config | Apply EDA worker nginx config (dest `/etc/nginx/conf.d/automation-eda-controller-worker.conf`) | inventory_hostname in _automationedacontroller_worker_hosts |
| 2132 | 83.7% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml | Modify dependent service list for postgresql | Modify dependent service list for postgresql (role `systemd`, tasks `postgresql`) | inventory_hostname in _automationedacontroller_worker_hosts |
| 2133 | 83.7% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → systemd | role `systemd` (`postgresql`) | Enter role task list | inventory_hostname in _automationedacontroller_worker_hosts |
| 2134 | 83.8% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → systemd → systemd | file `postgresql.yml` | Task file `ansible/automation_platform_installer/roles/systemd/tasks/postgresql.yml` | inventory_hostname in _automationedacontroller_worker_hosts |
| 2135 | 83.8% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → systemd → systemd | block | Begin conditional or grouped task block | inventory_hostname in _automationedacontroller_worker_hosts; _internal_database \| bool |
| 2136 | 83.9% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → systemd → systemd → block | Create directory for service configuration override | Create directory for service configuration override | inventory_hostname in _automationedacontroller_worker_hosts; _internal_database \| bool |
| 2137 | 83.9% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → systemd → systemd → block | Add postgresql to dependent service list | Add postgresql to dependent service list | inventory_hostname in _automationedacontroller_worker_hosts; _internal_database \| bool |
| 2138 | 83.9% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → systemd → systemd | Ensure postgresql isn't set as a dependent service | Ensure postgresql isn't set as a dependent service | inventory_hostname in _automationedacontroller_worker_hosts; not _internal_database \| bool |
| 2139 | 84.0% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml | Make dependent services part of EDA target | Make dependent services part of EDA target (role `systemd`) | inventory_hostname in _automationedacontroller_worker_hosts |
| 2140 | 84.0% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → systemd | role `systemd` | Enter role task list | inventory_hostname in _automationedacontroller_worker_hosts |
| 2141 | 84.1% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → systemd → systemd | file `main.yml` | Task file `ansible/automation_platform_installer/roles/systemd/tasks/main.yml` | inventory_hostname in _automationedacontroller_worker_hosts |
| 2142 | 84.1% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → systemd → systemd | Create directory for dependent service configuration override | Create directory for dependent service configuration override | inventory_hostname in _automationedacontroller_worker_hosts |
| 2143 | 84.1% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → systemd → systemd | Add dependent service configuration override | Add dependent service configuration override | inventory_hostname in _automationedacontroller_worker_hosts |
| 2144 | 84.2% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml | Stop automation-eda-controller workers | Stop automation-eda-controller workers | inventory_hostname in _automationedacontroller_worker_hosts |
| 2145 | 84.2% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml | Enable automation-eda-controller workers | Enable automation-eda-controller workers (service `automation-eda-controller-default-worker@{{ item }}.service`) | inventory_hostname in _automationedacontroller_worker_hosts |
| 2146 | 84.2% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml | Enable automation-eda-controller activation workers | Enable automation-eda-controller activation workers (service `automation-eda-controller-activation-worker@{{ item }}.service`) | inventory_hostname in _automationedacontroller_worker_hosts |
| 2147 | 84.3% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml | include_role | Include and run tasks from an Ansible role (role `podman`) | inventory_hostname in _automationedacontroller_worker_hosts |
| 2148 | 84.3% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman | role `podman` | Enter role task list | inventory_hostname in _automationedacontroller_worker_hosts |
| 2149 | 84.4% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman | file `main.yml` | Task file `ansible/automation_platform_installer/roles/podman/tasks/main.yml` | inventory_hostname in _automationedacontroller_worker_hosts |
| 2150 | 84.4% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman | Allocate subuids for {{ run_podman_as }} user | Allocate subuids for {{ run_podman_as }} user | inventory_hostname in _automationedacontroller_worker_hosts |
| 2151 | 84.4% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman | Allocate subgids for {{ run_podman_as }} user | Allocate subgids for {{ run_podman_as }} user | inventory_hostname in _automationedacontroller_worker_hosts |
| 2152 | 84.5% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman | Configure keyring size if needed | Begin conditional or grouped task block | inventory_hostname in _automationedacontroller_worker_hosts; (ansible_memtotal_mb // 100) > 200 |
| 2153 | 84.5% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman → Configure keyring size if needed | Set maxkeys | Set maxkeys | inventory_hostname in _automationedacontroller_worker_hosts; (ansible_memtotal_mb // 100) > 200 |
| 2154 | 84.6% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman → Configure keyring size if needed | Set maxbytes | Set maxbytes | inventory_hostname in _automationedacontroller_worker_hosts; (ansible_memtotal_mb // 100) > 200 |
| 2155 | 84.6% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman | Create directory for crun runtime config | Create directory for crun runtime config | inventory_hostname in _automationedacontroller_worker_hosts |
| 2156 | 84.6% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman | Configure podman default runtime | Configure podman default runtime (dest `{{ run_podman_home_prefix }}/{{ run_podman_as }}/.config/containers/containers.conf`) | inventory_hostname in _automationedacontroller_worker_hosts |
| 2157 | 84.7% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman | Create mounts.conf from template | Create mounts.conf from template (dest `{{ run_podman_home_prefix }}/{{ run_podman_as }}/.config/containers/mounts.conf`) | inventory_hostname in _automationedacontroller_worker_hosts; not podman_copy_mounts_conf \| bool |
| 2158 | 84.7% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman | Copy user provided mounts.conf | Copy user provided mounts.conf (dest `{{ run_podman_home_prefix }}/{{ run_podman_as }}/.config/containers/mounts.conf`) | inventory_hostname in _automationedacontroller_worker_hosts; podman_copy_mounts_conf \| bool |
| 2159 | 84.8% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman | Configure SELinux context mapping | Begin conditional or grouped task block | inventory_hostname in _automationedacontroller_worker_hosts; ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 2160 | 84.8% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman → Configure SELinux context mapping | Ensure policycoreutils-python-utils is installed | Ensure policycoreutils-python-utils is installed | inventory_hostname in _automationedacontroller_worker_hosts; ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 2161 | 84.8% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman → Configure SELinux context mapping | Determine podman container storage dir variable | Determine podman container storage dir variable | inventory_hostname in _automationedacontroller_worker_hosts; ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 2162 | 84.9% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman → Configure SELinux context mapping | Check current SELinux context mapping | Check current SELinux context mapping | inventory_hostname in _automationedacontroller_worker_hosts; ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 2163 | 84.9% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman → Configure SELinux context mapping | Delete custom SELinux context mapping | Delete custom SELinux context mapping | inventory_hostname in _automationedacontroller_worker_hosts; ansible_selinux is defined; ansible_selinux.status == "enabled"; semanage_current.stdout_lines\|length; "/var/lib/containers" not in semanage_current |
| 2164 | 85.0% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman → Configure SELinux context mapping | Register expected SELinux context mapping | Register expected SELinux context mapping | inventory_hostname in _automationedacontroller_worker_hosts; ansible_selinux is defined; ansible_selinux.status == "enabled"; semanage_current.failed \| bool or semanage_deleted.changed \| bool |
| 2165 | 85.0% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman → Configure SELinux context mapping | Check if container storage dir exists | Check if container storage dir exists | inventory_hostname in _automationedacontroller_worker_hosts; ansible_selinux is defined; ansible_selinux.status == "enabled" |
| 2166 | 85.0% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman → Configure SELinux context mapping | Perform SELinux relabeling for existing files | Perform SELinux relabeling for existing files | inventory_hostname in _automationedacontroller_worker_hosts; ansible_selinux is defined; ansible_selinux.status == "enabled"; _podman_container_storage_stat.stat.exists \| bool and (podman_force_selinux_relabel \| bool or selinux_manage.changed \| bool) |
| 2167 | 85.1% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman | Ensure registries.conf.d exists | Ensure registries.conf.d exists | inventory_hostname in _automationedacontroller_worker_hosts |
| 2168 | 85.1% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman | Force fully qualified image names to be provided to podman pull | Force fully qualified image names to be provided to podman pull (dest `/etc/containers/registries.conf.d/force-fully-qualified-images.conf`) | inventory_hostname in _automationedacontroller_worker_hosts |
| 2169 | 85.2% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman | Create tmpfiles.d entry for podman | Create tmpfiles.d entry for podman | inventory_hostname in _automationedacontroller_worker_hosts |
| 2170 | 85.2% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman | block | Begin conditional or grouped task block | inventory_hostname in _automationedacontroller_worker_hosts; subuids is changed or subgids is changed |
| 2171 | 85.2% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman → block | Create temporary directory for podman | Create temporary directory for podman | inventory_hostname in _automationedacontroller_worker_hosts; subuids is changed or subgids is changed |
| 2172 | 85.3% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman → block | Ensure changes are applied to podman | Ensure changes are applied to podman | inventory_hostname in _automationedacontroller_worker_hosts; subuids is changed or subgids is changed |
| 2173 | 85.3% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman | block (always) | Run always tasks for block | inventory_hostname in _automationedacontroller_worker_hosts; subuids is changed or subgids is changed |
| 2174 | 85.3% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman → block (always) | Remove podman temporary directory | Remove podman temporary directory | inventory_hostname in _automationedacontroller_worker_hosts; subuids is changed or subgids is changed; podman_tmp.path is defined |
| 2175 | 85.4% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman | Configure XDG_RUNTIME_DIR Environment variable for "{{ run_podman_as }}" user | Configure XDG_RUNTIME_DIR Environment variable for "{{ run_podman_as }}" user | inventory_hostname in _automationedacontroller_worker_hosts |
| 2176 | 85.4% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman | Configure DBUS_SESSION_BUS_ADDRESS Environment variable for "{{ run_podman_as }}" user | Configure DBUS_SESSION_BUS_ADDRESS Environment variable for "{{ run_podman_as }}" user | inventory_hostname in _automationedacontroller_worker_hosts |
| 2177 | 85.5% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman | Enable lingering systemd sessions for {{ run_podman_as }} user | Enable lingering systemd sessions for {{ run_podman_as }} user | inventory_hostname in _automationedacontroller_worker_hosts |
| 2178 | 85.5% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman | Reset podman for {{ run_podman_as }} user to apply configs | Reset podman for {{ run_podman_as }} user to apply configs | inventory_hostname in _automationedacontroller_worker_hosts |
| 2179 | 85.5% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml → podman → podman | Enable podman socket for {{ run_podman_as }} and restart | Enable podman socket for {{ run_podman_as }} and restart (service `podman.socket`) | inventory_hostname in _automationedacontroller_worker_hosts; podman_enable_socket \| bool |
| 2180 | 85.6% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_worker.yml | Define automation-eda-controller-worker queue name | Define automation-eda-controller-worker queue name | inventory_hostname in _automationedacontroller_worker_hosts |
| 2181 | 85.6% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | include_tasks | Include a task file from the current role or playbook (file `secret_key.yml`) | — |
| 2182 | 85.7% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → secret_key.yml | file `secret_key.yml` | Task file `ansible/automation_platform_installer/roles/automationedacontroller/tasks/secret_key.yml` | — |
| 2183 | 85.7% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → secret_key.yml | Check for SECRET_KEY on all nodes | Check for SECRET_KEY on all nodes | — |
| 2184 | 85.7% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → secret_key.yml | block | Begin conditional or grouped task block | automationedacontroller_secret_key_path.stat.exists |
| 2185 | 85.8% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → secret_key.yml → block | Get SECRET_KEY if found | Get SECRET_KEY if found | automationedacontroller_secret_key_path.stat.exists |
| 2186 | 85.8% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → secret_key.yml → block | Set SECRET_KEY if found | Set SECRET_KEY if found | automationedacontroller_secret_key_path.stat.exists; automationedacontroller_secret_key_slurp['content'] \| default('') \| length |
| 2187 | 85.9% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → secret_key.yml | Set SECRET_KEY if not found | Set SECRET_KEY if not found | not automationedacontroller_secret_key_slurp['content'] \| default('') \| length |
| 2188 | 85.9% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → secret_key.yml | Set SECRET_KEY for all nodes | Set SECRET_KEY for all nodes | — |
| 2189 | 85.9% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → secret_key.yml | Set SECRET_KEY file | Set SECRET_KEY file (dest `/etc/ansible-automation-platform/eda/SECRET_KEY`) | — |
| 2190 | 86.0% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | include_tasks | Include a task file from the current role or playbook (file `settings.yml`) | — |
| 2191 | 86.0% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → settings.yml | file `settings.yml` | Task file `ansible/automation_platform_installer/roles/automationedacontroller/tasks/settings.yml` | — |
| 2192 | 86.1% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → settings.yml | Delete environment file for settings migration | Delete environment file for settings migration | — |
| 2193 | 86.1% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → settings.yml | set_fact | Set a host fact used by later tasks | not automationedacontroller_db_template \| default('') \| length |
| 2194 | 86.1% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → settings.yml | Generate gateway secret | Generate gateway secret (role `automationgateway`, tasks `service_token`) | — |
| 2195 | 86.2% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → settings.yml → automationgateway | role `automationgateway` (`service_token`) | Enter role task list | — |
| 2196 | 86.2% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → settings.yml → automationgateway → automationgateway | file `service_token.yml` | Task file `ansible/automation_platform_installer/roles/automationgateway/tasks/service_token.yml` | — |
| 2197 | 86.3% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → settings.yml → automationgateway → automationgateway | Generate service secret | Generate service secret | — |
| 2198 | 86.3% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → settings.yml | Configure EDA Controller settings file | Configure EDA Controller settings file (dest `/etc/ansible-automation-platform/eda/settings.yaml`) | — |
| 2199 | 86.3% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | include_tasks | Include a task file from the current role or playbook (file `certificates.yml`) | — |
| 2200 | 86.4% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml | file `certificates.yml` | Task file `ansible/automation_platform_installer/roles/automationedacontroller/tasks/certificates.yml` | — |
| 2201 | 86.4% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml | Configure postgres client certs | Begin conditional or grouped task block | automationedacontroller_pg_cert_auth |
| 2202 | 86.4% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Configure postgres client certs | Copy postgres client cert | Copy postgres client cert (dest `/etc/ansible-automation-platform/eda/pg_client.crt`) | automationedacontroller_pg_cert_auth |
| 2203 | 86.5% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Configure postgres client certs | Copy postgres client key | Copy postgres client key (dest `/etc/ansible-automation-platform/eda/pg_client.key`) | automationedacontroller_pg_cert_auth |
| 2204 | 86.5% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml | block | Begin conditional or grouped task block | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts) |
| 2205 | 86.6% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block | Copy user-provided SSL certificates | Begin conditional or grouped task block | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts); automationedacontroller_ssl_cert \| default('') \| length |
| 2206 | 86.6% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → Copy user-provided SSL certificates | Copy SSL certificate | Copy SSL certificate (dest `/etc/ansible-automation-platform/eda/server.cert`) | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts); automationedacontroller_ssl_cert \| default('') \| length |
| 2207 | 86.6% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → Copy user-provided SSL certificates | Copy SSL private key | Copy SSL private key (dest `/etc/ansible-automation-platform/eda/server.key`) | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts); automationedacontroller_ssl_cert \| default('') \| length |
| 2208 | 86.7% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block | Use internal CA | Use internal CA (role `certificate_authority`, tasks `sign_service.yml`) | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts); automationedacontroller_ssl_cert is not defined |
| 2209 | 86.7% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority | role `certificate_authority` (`sign_service.yml`) | Enter role task list | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts); automationedacontroller_ssl_cert is not defined |
| 2210 | 86.8% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority | file `sign_service.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/sign_service.yml` | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts); automationedacontroller_ssl_cert is not defined |
| 2211 | 86.8% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority | include_tasks | Include a task file from the current role or playbook (file `init.yml`) | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts); automationedacontroller_ssl_cert is not defined |
| 2212 | 86.8% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority → init.yml | file `init.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/init.yml` | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts); automationedacontroller_ssl_cert is not defined |
| 2213 | 86.9% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority → init.yml | Ensure python cryptography libraries are installed | Ensure python cryptography libraries are installed | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts); automationedacontroller_ssl_cert is not defined |
| 2214 | 86.9% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority | Create {{ aap_ca_servicename }} private key | Create {{ aap_ca_servicename }} private key | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts); automationedacontroller_ssl_cert is not defined |
| 2215 | 87.0% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority | set_fact | Set a host fact used by later tasks | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts); automationedacontroller_ssl_cert is not defined |
| 2216 | 87.0% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority | Check if {{ aap_ca_servicename }} public key exists | Check if {{ aap_ca_servicename }} public key exists | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts); automationedacontroller_ssl_cert is not defined |
| 2217 | 87.0% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts); automationedacontroller_ssl_cert is not defined; certificate_stat.stat.exists \| bool |
| 2218 | 87.1% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority → block | Check contents of {{ aap_ca_servicename }} public key | Check contents of {{ aap_ca_servicename }} public key | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts); automationedacontroller_ssl_cert is not defined; certificate_stat.stat.exists \| bool |
| 2219 | 87.1% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts); automationedacontroller_ssl_cert is not defined; certificate_stat.stat.exists \| bool; aap_service_san_records_internal \| union(aap_service_san_records) \| difference(certificate_check.subject_alt_name) \| length > 0 |
| 2220 | 87.2% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts); automationedacontroller_ssl_cert is not defined; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 2221 | 87.2% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts); automationedacontroller_ssl_cert is not defined; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false); aap_service_is_client |
| 2222 | 87.2% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority → block | Create CSR for {{ aap_ca_servicename }} node/s | Create CSR for {{ aap_ca_servicename }} node/s | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts); automationedacontroller_ssl_cert is not defined; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 2223 | 87.3% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority → block | Sign {{ aap_ca_servicename }} public key | Sign {{ aap_ca_servicename }} public key | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts); automationedacontroller_ssl_cert is not defined; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 2224 | 87.3% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority → block | Write public key to {{ aap_ca_servicename }} node/s | Write public key to {{ aap_ca_servicename }} node/s (dest `{{ aap_service_cert_fullpath }}`) | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts); automationedacontroller_ssl_cert is not defined; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 2225 | 87.4% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority | Signal restart if certificate pair changes | Signal restart if certificate pair changes | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts); automationedacontroller_ssl_cert is not defined; (write_key is changed or write_certificate is changed) |
| 2226 | 87.4% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block | Trigger nginx notifier for internally managed certificate changes | Trigger nginx notifier for internally managed certificate changes | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts); aap_service_restart \| default(false) |
| 2227 | 87.4% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block | set_fact | Set a host fact used by later tasks | inventory_hostname in _automationedacontroller_web_hosts \| union(_automationedacontroller_event_stream_hosts) |
| 2228 | 87.5% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml | block | Begin conditional or grouped task block | inventory_hostname in _automationedacontroller_worker_hosts |
| 2229 | 87.5% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block | Sign internal websocket certificates | Sign internal websocket certificates (role `certificate_authority`, tasks `sign_service.yml`) | inventory_hostname in _automationedacontroller_worker_hosts |
| 2230 | 87.5% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority | role `certificate_authority` (`sign_service.yml`) | Enter role task list | inventory_hostname in _automationedacontroller_worker_hosts |
| 2231 | 87.6% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority | file `sign_service.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/sign_service.yml` | inventory_hostname in _automationedacontroller_worker_hosts |
| 2232 | 87.6% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority | include_tasks | Include a task file from the current role or playbook (file `init.yml`) | inventory_hostname in _automationedacontroller_worker_hosts |
| 2233 | 87.7% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority → init.yml | file `init.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/init.yml` | inventory_hostname in _automationedacontroller_worker_hosts |
| 2234 | 87.7% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority → init.yml | Ensure python cryptography libraries are installed | Ensure python cryptography libraries are installed | inventory_hostname in _automationedacontroller_worker_hosts |
| 2235 | 87.7% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority | Create {{ aap_ca_servicename }} private key | Create {{ aap_ca_servicename }} private key | inventory_hostname in _automationedacontroller_worker_hosts |
| 2236 | 87.8% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority | set_fact | Set a host fact used by later tasks | inventory_hostname in _automationedacontroller_worker_hosts |
| 2237 | 87.8% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority | Check if {{ aap_ca_servicename }} public key exists | Check if {{ aap_ca_servicename }} public key exists | inventory_hostname in _automationedacontroller_worker_hosts |
| 2238 | 87.9% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | inventory_hostname in _automationedacontroller_worker_hosts; certificate_stat.stat.exists \| bool |
| 2239 | 87.9% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority → block | Check contents of {{ aap_ca_servicename }} public key | Check contents of {{ aap_ca_servicename }} public key | inventory_hostname in _automationedacontroller_worker_hosts; certificate_stat.stat.exists \| bool |
| 2240 | 87.9% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | inventory_hostname in _automationedacontroller_worker_hosts; certificate_stat.stat.exists \| bool; aap_service_san_records_internal \| union(aap_service_san_records) \| difference(certificate_check.subject_alt_name) \| length > 0 |
| 2241 | 88.0% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | inventory_hostname in _automationedacontroller_worker_hosts; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 2242 | 88.0% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | inventory_hostname in _automationedacontroller_worker_hosts; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false); aap_service_is_client |
| 2243 | 88.1% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority → block | Create CSR for {{ aap_ca_servicename }} node/s | Create CSR for {{ aap_ca_servicename }} node/s | inventory_hostname in _automationedacontroller_worker_hosts; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 2244 | 88.1% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority → block | Sign {{ aap_ca_servicename }} public key | Sign {{ aap_ca_servicename }} public key | inventory_hostname in _automationedacontroller_worker_hosts; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 2245 | 88.1% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority → block | Write public key to {{ aap_ca_servicename }} node/s | Write public key to {{ aap_ca_servicename }} node/s (dest `{{ aap_service_cert_fullpath }}`) | inventory_hostname in _automationedacontroller_worker_hosts; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 2246 | 88.2% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block → certificate_authority → certificate_authority | Signal restart if certificate pair changes | Signal restart if certificate pair changes | inventory_hostname in _automationedacontroller_worker_hosts; (write_key is changed or write_certificate is changed) |
| 2247 | 88.2% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block | Trigger automation-eda-controller notifier for internally managed certificate changes | Trigger automation-eda-controller notifier for internally managed certificate changes | inventory_hostname in _automationedacontroller_worker_hosts; aap_service_restart \| default(false) |
| 2248 | 88.3% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → block | set_fact | Set a host fact used by later tasks | inventory_hostname in _automationedacontroller_worker_hosts |
| 2249 | 88.3% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml | Handle caching client certs when mTLS is enabled for AAP caching | Begin conditional or grouped task block | not _redis_disable_mtls |
| 2250 | 88.3% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching | Copy user-provided cache client certificates | Begin conditional or grouped task block | not _redis_disable_mtls; automationedacontroller_cache_cert \| default('') \| length |
| 2251 | 88.4% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → Copy user-provided cache client certificates | Copy caching mTLS certificate | Copy caching mTLS certificate (dest `/etc/ansible-automation-platform/eda/cache.cert`) | not _redis_disable_mtls; automationedacontroller_cache_cert \| default('') \| length |
| 2252 | 88.4% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → Copy user-provided cache client certificates | Copy caching mTLS private key | Copy caching mTLS private key (dest `/etc/ansible-automation-platform/eda/cache.key`) | not _redis_disable_mtls; automationedacontroller_cache_cert \| default('') \| length |
| 2253 | 88.5% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching | Sign caching client certs | Sign caching client certs (role `certificate_authority`, tasks `sign_service.yml`) | not _redis_disable_mtls; not automationedacontroller_cache_cert \| default('') \| length |
| 2254 | 88.5% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority | role `certificate_authority` (`sign_service.yml`) | Enter role task list | not _redis_disable_mtls; not automationedacontroller_cache_cert \| default('') \| length |
| 2255 | 88.5% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority | file `sign_service.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/sign_service.yml` | not _redis_disable_mtls; not automationedacontroller_cache_cert \| default('') \| length |
| 2256 | 88.6% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority | include_tasks | Include a task file from the current role or playbook (file `init.yml`) | not _redis_disable_mtls; not automationedacontroller_cache_cert \| default('') \| length |
| 2257 | 88.6% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority → init.yml | file `init.yml` | Task file `ansible/automation_platform_installer/roles/certificate_authority/tasks/init.yml` | not _redis_disable_mtls; not automationedacontroller_cache_cert \| default('') \| length |
| 2258 | 88.6% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority → init.yml | Ensure python cryptography libraries are installed | Ensure python cryptography libraries are installed | not _redis_disable_mtls; not automationedacontroller_cache_cert \| default('') \| length |
| 2259 | 88.7% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority | Create {{ aap_ca_servicename }} private key | Create {{ aap_ca_servicename }} private key | not _redis_disable_mtls; not automationedacontroller_cache_cert \| default('') \| length |
| 2260 | 88.7% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority | set_fact | Set a host fact used by later tasks | not _redis_disable_mtls; not automationedacontroller_cache_cert \| default('') \| length |
| 2261 | 88.8% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority | Check if {{ aap_ca_servicename }} public key exists | Check if {{ aap_ca_servicename }} public key exists | not _redis_disable_mtls; not automationedacontroller_cache_cert \| default('') \| length |
| 2262 | 88.8% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | not _redis_disable_mtls; not automationedacontroller_cache_cert \| default('') \| length; certificate_stat.stat.exists \| bool |
| 2263 | 88.8% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority → block | Check contents of {{ aap_ca_servicename }} public key | Check contents of {{ aap_ca_servicename }} public key | not _redis_disable_mtls; not automationedacontroller_cache_cert \| default('') \| length; certificate_stat.stat.exists \| bool |
| 2264 | 88.9% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | not _redis_disable_mtls; not automationedacontroller_cache_cert \| default('') \| length; certificate_stat.stat.exists \| bool; aap_service_san_records_internal \| union(aap_service_san_records) \| difference(certificate_check.subject_alt_name) \| length > 0 |
| 2265 | 88.9% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority | block | Begin conditional or grouped task block | not _redis_disable_mtls; not automationedacontroller_cache_cert \| default('') \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 2266 | 89.0% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority → block | set_fact | Set a host fact used by later tasks | not _redis_disable_mtls; not automationedacontroller_cache_cert \| default('') \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false); aap_service_is_client |
| 2267 | 89.0% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority → block | Create CSR for {{ aap_ca_servicename }} node/s | Create CSR for {{ aap_ca_servicename }} node/s | not _redis_disable_mtls; not automationedacontroller_cache_cert \| default('') \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 2268 | 89.0% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority → block | Sign {{ aap_ca_servicename }} public key | Sign {{ aap_ca_servicename }} public key | not _redis_disable_mtls; not automationedacontroller_cache_cert \| default('') \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 2269 | 89.1% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority → block | Write public key to {{ aap_ca_servicename }} node/s | Write public key to {{ aap_ca_servicename }} node/s (dest `{{ aap_service_cert_fullpath }}`) | not _redis_disable_mtls; not automationedacontroller_cache_cert \| default('') \| length; write_key is changed or write_public_key \| bool or (certificate_check.authority_key_identifier \| default('') \| string != hostvars[groups['aap_ca_server'][0]]['aap_ca_cert_ski'] \| string) or not certificate_check.valid_at.valid_now \| default(false) |
| 2270 | 89.1% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching → certificate_authority → certificate_authority | Signal restart if certificate pair changes | Signal restart if certificate pair changes | not _redis_disable_mtls; not automationedacontroller_cache_cert \| default('') \| length; (write_key is changed or write_certificate is changed) |
| 2271 | 89.2% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → certificates.yml → Handle caching client certs when mTLS is enabled for AAP caching | Trigger automation-eda-controller notifier for internally managed certificate changes | Trigger automation-eda-controller notifier for internally managed certificate changes | not _redis_disable_mtls; aap_service_restart \| default(false) |
| 2272 | 89.2% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | include_tasks | Include a task file from the current role or playbook (file `provision_api.yml`) | inventory_hostname in _automationedacontroller_web_hosts |
| 2273 | 89.2% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_api.yml | file `provision_api.yml` | Task file `ansible/automation_platform_installer/roles/automationedacontroller/tasks/provision_api.yml` | inventory_hostname in _automationedacontroller_web_hosts |
| 2274 | 89.3% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_api.yml | Apply EDA API nginx config | Apply EDA API nginx config (dest `/etc/nginx/conf.d/automation-eda-controller-api.conf`) | inventory_hostname in _automationedacontroller_web_hosts |
| 2275 | 89.3% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_api.yml | Configure the static directory | Configure the static directory | inventory_hostname in _automationedacontroller_web_hosts |
| 2276 | 89.4% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_api.yml | Collect static files | Collect static files | inventory_hostname in _automationedacontroller_web_hosts |
| 2277 | 89.4% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_api.yml | Copy index.html | Copy index.html | inventory_hostname in _automationedacontroller_web_hosts |
| 2278 | 89.4% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_api.yml | Ensure static media directory exists | Ensure static media directory exists | inventory_hostname in _automationedacontroller_web_hosts |
| 2279 | 89.5% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_api.yml | Copy logo file | Copy logo file | inventory_hostname in _automationedacontroller_web_hosts |
| 2280 | 89.5% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_api.yml | block | Begin conditional or grouped task block | inventory_hostname in _automationedacontroller_web_hosts; inventory_hostname == _automationedacontroller_web_hosts[0] |
| 2281 | 89.6% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_api.yml → block | Migrate the EDA Controller database schema | Migrate the EDA Controller database schema | inventory_hostname in _automationedacontroller_web_hosts; inventory_hostname == _automationedacontroller_web_hosts[0] |
| 2282 | 89.6% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_api.yml → block | Populate initial data | Populate initial data | inventory_hostname in _automationedacontroller_web_hosts; inventory_hostname == _automationedacontroller_web_hosts[0] |
| 2283 | 89.6% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_api.yml → block | create django super user | create django super user | inventory_hostname in _automationedacontroller_web_hosts; inventory_hostname == _automationedacontroller_web_hosts[0] |
| 2284 | 89.7% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_api.yml → block | update django super user password | update django super user password | inventory_hostname in _automationedacontroller_web_hosts; inventory_hostname == _automationedacontroller_web_hosts[0]; automationedacontroller_install_admin_password != '' |
| 2285 | 89.7% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | include_tasks | Include a task file from the current role or playbook (file `provision_event_stream.yml`) | inventory_hostname in _automationedacontroller_event_stream_hosts |
| 2286 | 89.7% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_event_stream.yml | file `provision_event_stream.yml` | Task file `ansible/automation_platform_installer/roles/automationedacontroller/tasks/provision_event_stream.yml` | inventory_hostname in _automationedacontroller_event_stream_hosts |
| 2287 | 89.8% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_event_stream.yml | Apply EDA event stream nginx config | Apply EDA event stream nginx config (dest `/etc/nginx/conf.d/automation-eda-controller-event-stream.conf`) | inventory_hostname in _automationedacontroller_event_stream_hosts |
| 2288 | 89.8% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_event_stream.yml | Create directory for dependent service configuration override | Create directory for dependent service configuration override | inventory_hostname in _automationedacontroller_event_stream_hosts |
| 2289 | 89.9% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → provision_event_stream.yml | Add environment variable for gunicorn worker | Add environment variable for gunicorn worker | inventory_hostname in _automationedacontroller_event_stream_hosts |
| 2290 | 89.9% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | Create systemd override directory | Create systemd override directory | — |
| 2291 | 89.9% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | Create systemd override file | Create systemd override file (dest `/etc/systemd/system/automation-eda-controller.service.d/override.conf`) | — |
| 2292 | 90.0% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | Reload systemd daemon | Reload systemd daemon | eda_systemd_override is changed |
| 2293 | 90.0% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | block | Begin conditional or grouped task block | eda_node_type != 'event-stream' |
| 2294 | 90.1% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → block | Enable automation-eda-controller services | Enable automation-eda-controller services | eda_node_type != 'event-stream' |
| 2295 | 90.1% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → block | Ensure automation-eda-controller services are started after install | Ensure automation-eda-controller services are started after install | eda_node_type != 'event-stream' |
| 2296 | 90.1% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller | block | Begin conditional or grouped task block | inventory_hostname in _automationedacontroller_event_stream_hosts |
| 2297 | 90.2% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → block | Enable automation-eda-controller-event-stream services | Enable automation-eda-controller-event-stream services | inventory_hostname in _automationedacontroller_event_stream_hosts |
| 2298 | 90.2% | install.yml → Install Automation EDA Controller → block → automationedacontroller → automationedacontroller → block | Ensure automation-eda-controller-event-stream services are started after install | Ensure automation-eda-controller-event-stream services are started after install | inventory_hostname in _automationedacontroller_event_stream_hosts |
| 2299 | 90.3% | install.yml → Install Automation EDA Controller → block | include_role | Include and run tasks from an Ansible role (role `nginx`, tasks `finalize.yml`) | — |
| 2300 | 90.3% | install.yml → Install Automation EDA Controller → block → nginx | role `nginx` (`finalize.yml`) | Enter role task list | — |
| 2301 | 90.3% | install.yml → Install Automation EDA Controller → block → nginx → nginx | file `finalize.yml` | Task file `ansible/automation_platform_installer/roles/nginx/tasks/finalize.yml` | — |
| 2302 | 90.4% | install.yml → Install Automation EDA Controller → block → nginx → nginx | Install nginx.conf | Install nginx.conf (dest `/etc/nginx/nginx.conf`) | — |
| 2303 | 90.4% | install.yml → Install Automation EDA Controller → block → nginx → nginx | Start nginx and configure to startup automatically | Start nginx and configure to startup automatically (service `nginx`) | — |
| 2304 | 90.5% | install.yml → Install Automation EDA Controller → block | Configure firewall | Configure firewall (role `firewall`) | — |
| 2305 | 90.5% | install.yml → Install Automation EDA Controller → block → firewall | role `firewall` | Enter role task list | — |
| 2306 | 90.5% | install.yml → Install Automation EDA Controller → block → firewall → firewall | file `main.yml` | Task file `ansible/automation_platform_installer/roles/firewall/tasks/main.yml` | — |
| 2307 | 90.6% | install.yml → Install Automation EDA Controller → block → firewall → firewall | include_tasks | Include a task file from the current role or playbook (file `discover.yml`) | — |
| 2308 | 90.6% | install.yml → Install Automation EDA Controller → block → firewall → firewall → discover.yml | file `discover.yml` | Task file `ansible/automation_platform_installer/roles/firewall/tasks/discover.yml` | — |
| 2309 | 90.7% | install.yml → Install Automation EDA Controller → block → firewall → firewall → discover.yml | Determine if firewalld is running | Determine if firewalld is running | — |
| 2310 | 90.7% | install.yml → Install Automation EDA Controller → block → firewall → firewall → discover.yml | Determine if firewalld is enabled | Determine if firewalld is enabled | — |
| 2311 | 90.7% | install.yml → Install Automation EDA Controller → block → firewall → firewall → discover.yml | Determine if iptables is installed | Determine if iptables is installed | — |
| 2312 | 90.8% | install.yml → Install Automation EDA Controller → block → firewall → firewall → discover.yml | Determine if iptables service is installed | Determine if iptables service is installed | — |
| 2313 | 90.8% | install.yml → Install Automation EDA Controller → block → firewall → firewall → discover.yml | Check to see if there is an iptables config file | Check to see if there is an iptables config file | iptables_installed.rc == 0 |
| 2314 | 90.8% | install.yml → Install Automation EDA Controller → block → firewall → firewall → discover.yml | Select firewalld | Select firewalld | firewalld_running.rc == 0 or systemctl_unit_files.stdout_lines \| select('match','firewalld.service\s+enabled') \| list \| length == 1 |
| 2315 | 90.9% | install.yml → Install Automation EDA Controller → block → firewall → firewall → discover.yml | Select iptables | Select iptables | iptables_installed.rc == 0; iptables_exists.stat.exists; iptables_service.stat.exists |
| 2316 | 90.9% | install.yml → Install Automation EDA Controller → block → firewall → firewall | Include {{ 'no' if component_firewall_type == 'none' else component_firewall_type }} tasks | Include {{ 'no' if component_firewall_type == 'none' else component_firewall_type }} tasks (file `{{ component_firewall_type }}.yml`) | firewalld_running.rc == 0 or systemctl_unit_files.stdout_lines \| select('match','firewalld.service\s+enabled') \| list \| length == 1 or ( iptables_installed.rc == 0 and iptables_exists.stat.exists and iptables_service.stat.exists ) |
| 2317 | 91.0% | install.yml → Install Automation EDA Controller → block → firewall → firewall → {{ component_firewall_type }}.yml | {{ component_firewall_type }}.yml | Include task file `{{ component_firewall_type }}.yml` (relative to current role task directory) | firewalld_running.rc == 0 or systemctl_unit_files.stdout_lines \| select('match','firewalld.service\s+enabled') \| list \| length == 1 or ( iptables_installed.rc == 0 and iptables_exists.stat.exists and iptables_service.stat.exists ) |
| 2318 | 91.0% | install.yml → Install Automation EDA Controller | block (always) | Run always tasks for block | — |
| 2319 | 91.0% | install.yml → Install Automation EDA Controller → block (always) | Run repo_management (teardown) | Run repo_management (teardown) (role `repo_management`, tasks `teardown`) | — |
| 2320 | 91.1% | install.yml → Install Automation EDA Controller → block (always) → repo_management | role `repo_management` (`teardown`) | Enter role task list | — |
| 2321 | 91.1% | install.yml → Install Automation EDA Controller → block (always) → repo_management → repo_management | file `teardown.yml` | Task file `ansible/automation_platform_installer/roles/repo_management/tasks/teardown.yml` | — |
| 2322 | 91.2% | install.yml → Install Automation EDA Controller → block (always) → repo_management → repo_management | Disable Automation Platform repository | Disable Automation Platform repository | extra_repos is defined |
| 2323 | 91.2% | install.yml → Install Automation EDA Controller → block (always) → repo_management → repo_management | Disable Automation Platform RHSM repository | Disable Automation Platform RHSM repository | aap_rhsm_repo_enable is succeeded; aap_rhsm_repo_enable is not skipped |
| 2324 | 91.2% | install.yml | Post-install setup | Play targeting `automationcontroller:automationedacontroller:automationhub[0]:execution_nodes` | — |
| 2325 | 91.3% | install.yml → Post-install setup | Set automation hub image base path | Set automation hub image base path | groups['automationhub'] \| default([]) \| length |
| 2326 | 91.3% | install.yml → Post-install setup | include_role | Include and run tasks from an Ansible role (role `misc`, tasks `setup_ee_images`) | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool) |
| 2327 | 91.4% | install.yml → Post-install setup → misc | role `misc` (`setup_ee_images`) | Enter role task list | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool) |
| 2328 | 91.4% | install.yml → Post-install setup → misc → misc | file `setup_ee_images.yml` | Task file `ansible/automation_platform_installer/roles/misc/tasks/setup_ee_images.yml` | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool) |
| 2329 | 91.4% | install.yml → Post-install setup → misc → misc | block | Begin conditional or grouped task block | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool) |
| 2330 | 91.5% | install.yml → Post-install setup → misc → misc → block | Ensure skopeo exists | Ensure skopeo exists | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool) |
| 2331 | 91.5% | install.yml → Post-install setup → misc → misc → block | Set list of images to upload for controller and execution nodes | Set list of images to upload for controller and execution nodes | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('execution_nodes', [])) |
| 2332 | 91.6% | install.yml → Post-install setup → misc → misc → block | Set list of images to upload for hub | Set list of images to upload for hub | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); inventory_hostname in groups.get('automationhub', []) |
| 2333 | 91.6% | install.yml → Post-install setup → misc → misc → block | Set list of images to upload for EDA | Set list of images to upload for EDA | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); inventory_hostname in _automationedacontroller_web_hosts |
| 2334 | 91.6% | install.yml → Post-install setup → misc → misc → block | Wait until Pulp workers are ready | Wait until Pulp workers are ready | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); groups.get('automationhub', []) \| length > 0 |
| 2335 | 91.7% | install.yml → Post-install setup → misc → misc → block | Set up images | Begin conditional or grouped task block | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); inventory_hostname in groups['automationhub'] \| default([]); not bundle_install \| default(false) \| bool |
| 2336 | 91.7% | install.yml → Post-install setup → misc → misc → block → Set up images | Set source registry credential (username) | Set source registry credential (username) | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); inventory_hostname in groups['automationhub'] \| default([]); not bundle_install \| default(false) \| bool; registry_username \| default('') \| length; not registry_password \| default('') \| length |
| 2337 | 91.8% | install.yml → Post-install setup → misc → misc → block → Set up images | Set source registry credential (username and password) | Set source registry credential (username and password) | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); inventory_hostname in groups['automationhub'] \| default([]); not bundle_install \| default(false) \| bool; registry_username \| default('') \| length; registry_password \| default('') \| length |
| 2338 | 91.8% | install.yml → Post-install setup → misc → misc → block → Set up images | Push images to Automation Hub registry | Push images to Automation Hub registry | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); inventory_hostname in groups['automationhub'] \| default([]); not bundle_install \| default(false) \| bool |
| 2339 | 91.8% | install.yml → Post-install setup → misc → misc → block → Set up images | Catch error when not all images are processed | Catch error when not all images are processed | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); inventory_hostname in groups['automationhub'] \| default([]); not bundle_install \| default(false) \| bool; _push_ee_result.results is not defined |
| 2340 | 91.9% | install.yml → Post-install setup → misc → misc → block → Set up images | Parse error messages | Parse error messages | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); inventory_hostname in groups['automationhub'] \| default([]); not bundle_install \| default(false) \| bool; item \| default({}) is failed |
| 2341 | 91.9% | install.yml → Post-install setup → misc → misc → block | Set up images (bundle) | Set up images (bundle) (file `push_bundle_ee_image.yml`) | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool |
| 2342 | 91.9% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml | file `push_bundle_ee_image.yml` | Task file `ansible/automation_platform_installer/roles/misc/tasks/push_bundle_ee_image.yml` | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool |
| 2343 | 92.0% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml | block | Begin conditional or grouped task block | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool |
| 2344 | 92.0% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block | Create temporary directory for EE image | Create temporary directory for EE image | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool |
| 2345 | 92.1% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block | Copy bundled EE image | Begin conditional or grouped task block | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool |
| 2346 | 92.1% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block → Copy bundled EE image | Copy bundled EE image | Copy bundled EE image | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool |
| 2347 | 92.1% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block | Copy bundled EE image (rescue) | Run rescue tasks for block | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool |
| 2348 | 92.2% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block → Copy bundled EE image (rescue) | Copy bundled EE image (legacy) | Copy bundled EE image (legacy) (dest `{{ ee_image_tmp.path }}`) | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool |
| 2349 | 92.2% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block | Uncompress bundled EE image | Uncompress bundled EE image | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool |
| 2350 | 92.3% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block | Set permission for the uncompressed images | Set permission for the uncompressed images | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool |
| 2351 | 92.3% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block | Push EE image to Automation Hub registry | Push EE image to Automation Hub registry | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; inventory_hostname in groups['automationhub'] \| default([]) |
| 2352 | 92.3% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block | Parse error message | Parse error message | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool |
| 2353 | 92.4% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block | set_fact | Set a host fact used by later tasks | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool |
| 2354 | 92.4% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block | Set up EE image locally on controller and execution nodes | Set up EE image locally on controller and execution nodes (file `push_bundle_image_locally.yml`) | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('execution_nodes', [])) |
| 2355 | 92.5% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block → push_bundle_image_locally.yml | file `push_bundle_image_locally.yml` | Task file `ansible/automation_platform_installer/roles/misc/tasks/push_bundle_image_locally.yml` | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('execution_nodes', [])) |
| 2356 | 92.5% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block → push_bundle_image_locally.yml | block | Begin conditional or grouped task block | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('execution_nodes', [])) |
| 2357 | 92.5% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block → push_bundle_image_locally.yml → block | Create temporary directory for skopeo | Create temporary directory for skopeo | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('execution_nodes', [])) |
| 2358 | 92.6% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block → push_bundle_image_locally.yml → block | Set local push path for registry_url | Set local push path for registry_url | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('execution_nodes', [])); not (_ee_from_hub_only \| bool and groups['automationhub'] \| default([]) \| length) |
| 2359 | 92.6% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block → push_bundle_image_locally.yml → block | Set local push path for hub | Set local push path for hub | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('execution_nodes', [])); groups['automationhub'] \| default([]) \| length |
| 2360 | 92.7% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block → push_bundle_image_locally.yml → block | Push {{ item }} locally | Push {{ item }} locally | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('execution_nodes', [])) |
| 2361 | 92.7% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block → push_bundle_image_locally.yml → block | Catch error when not all images are processed | Catch error when not all images are processed | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('execution_nodes', [])); _push_local_result.results is not defined |
| 2362 | 92.7% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block → push_bundle_image_locally.yml → block | Parse error messages | Parse error messages | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('execution_nodes', [])); _ee_local_push_error \| default({}) is failed |
| 2363 | 92.8% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block → push_bundle_image_locally.yml | block (always) | Run always tasks for block | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('execution_nodes', [])) |
| 2364 | 92.8% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block → push_bundle_image_locally.yml → block (always) | Remove skopeo temporary directory | Remove skopeo temporary directory | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('execution_nodes', [])); _skopeo_tmp.path is defined |
| 2365 | 92.9% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block | Set up DE image locally on EDA controller node | Set up DE image locally on EDA controller node (file `push_bundle_image_locally.yml`) | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; inventory_hostname in _automationedacontroller_worker_hosts |
| 2366 | 92.9% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block → push_bundle_image_locally.yml | file `push_bundle_image_locally.yml` | Task file `ansible/automation_platform_installer/roles/misc/tasks/push_bundle_image_locally.yml` | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; inventory_hostname in _automationedacontroller_worker_hosts |
| 2367 | 92.9% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block → push_bundle_image_locally.yml | block | Begin conditional or grouped task block | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; inventory_hostname in _automationedacontroller_worker_hosts |
| 2368 | 93.0% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block → push_bundle_image_locally.yml → block | Create temporary directory for skopeo | Create temporary directory for skopeo | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; inventory_hostname in _automationedacontroller_worker_hosts |
| 2369 | 93.0% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block → push_bundle_image_locally.yml → block | Set local push path for registry_url | Set local push path for registry_url | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; inventory_hostname in _automationedacontroller_worker_hosts; not (_ee_from_hub_only \| bool and groups['automationhub'] \| default([]) \| length) |
| 2370 | 93.0% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block → push_bundle_image_locally.yml → block | Set local push path for hub | Set local push path for hub | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; inventory_hostname in _automationedacontroller_worker_hosts; groups['automationhub'] \| default([]) \| length |
| 2371 | 93.1% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block → push_bundle_image_locally.yml → block | Push {{ item }} locally | Push {{ item }} locally | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; inventory_hostname in _automationedacontroller_worker_hosts |
| 2372 | 93.1% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block → push_bundle_image_locally.yml → block | Catch error when not all images are processed | Catch error when not all images are processed | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; inventory_hostname in _automationedacontroller_worker_hosts; _push_local_result.results is not defined |
| 2373 | 93.2% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block → push_bundle_image_locally.yml → block | Parse error messages | Parse error messages | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; inventory_hostname in _automationedacontroller_worker_hosts; _ee_local_push_error \| default({}) is failed |
| 2374 | 93.2% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block → push_bundle_image_locally.yml | block (always) | Run always tasks for block | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; inventory_hostname in _automationedacontroller_worker_hosts |
| 2375 | 93.2% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block → push_bundle_image_locally.yml → block (always) | Remove skopeo temporary directory | Remove skopeo temporary directory | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; inventory_hostname in _automationedacontroller_worker_hosts; _skopeo_tmp.path is defined |
| 2376 | 93.3% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block | set_fact | Set a host fact used by later tasks | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; _ee_error_msg \| length |
| 2377 | 93.3% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml | block (always) | Run always tasks for block | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool |
| 2378 | 93.4% | install.yml → Post-install setup → misc → misc → block → push_bundle_ee_image.yml → block (always) | Remove copied EE image | Remove copied EE image | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); bundle_install \| default(false) \| bool; ee_image_tmp.path is defined |
| 2379 | 93.4% | install.yml → Post-install setup → misc → misc → block | Parse error messages | Parse error messages | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool) |
| 2380 | 93.4% | install.yml → Post-install setup → misc → misc → block | Ensure images are pushed | Ensure images are pushed | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); _image_push_error_messages \| default([]) \| length |
| 2381 | 93.5% | install.yml → Post-install setup → misc → misc | block (always) | Run always tasks for block | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool) |
| 2382 | 93.5% | install.yml → Post-install setup → misc → misc → block (always) | Remove skopeo if added by installer | Remove skopeo if added by installer | inventory_hostname in groups['automationhub'] \| default([]) or (node_type \| default('') != 'hop' and node_state \| default('') == 'active' and bundle_install \| bool) or (inventory_hostname in groups['automationedacontroller'] \| default([]) and bundle_install \| bool); installed_skopeo is changed |
| 2383 | 93.6% | install.yml → Post-install setup | Execute Post-Installation Steps | Begin conditional or grouped task block | node_state is defined; node_state == 'active' |
| 2384 | 93.6% | install.yml → Post-install setup → Execute Post-Installation Steps | include_role | Include and run tasks from an Ansible role (role `automationcontroller`, tasks `post_install_setup`) | node_state is defined; node_state == 'active'; inventory_hostname in groups['automationcontroller'] |
| 2385 | 93.6% | install.yml → Post-install setup → Execute Post-Installation Steps → automationcontroller | role `automationcontroller` (`post_install_setup`) | Enter role task list | node_state is defined; node_state == 'active'; inventory_hostname in groups['automationcontroller'] |
| 2386 | 93.7% | install.yml → Post-install setup → Execute Post-Installation Steps → automationcontroller → automationcontroller | file `post_install_setup.yml` | Task file `ansible/automation_platform_installer/roles/automationcontroller/tasks/post_install_setup.yml` | node_state is defined; node_state == 'active'; inventory_hostname in groups['automationcontroller'] |
| 2387 | 93.7% | install.yml → Post-install setup → Execute Post-Installation Steps → automationcontroller → automationcontroller | block | Begin conditional or grouped task block | node_state is defined; node_state == 'active'; inventory_hostname in groups['automationcontroller']; groups['automationhub'] \| default([]) \| length |
| 2388 | 93.8% | install.yml → Post-install setup → Execute Post-Installation Steps → automationcontroller → automationcontroller → block | Create execution environment list for Hub | Create execution environment list for Hub | node_state is defined; node_state == 'active'; inventory_hostname in groups['automationcontroller']; groups['automationhub'] \| default([]) \| length |
| 2389 | 93.8% | install.yml → Post-install setup → Execute Post-Installation Steps → automationcontroller → automationcontroller → block | Update global_job_execution_environments for Hub (non-bundle) | Update global_job_execution_environments for Hub (non-bundle) | node_state is defined; node_state == 'active'; inventory_hostname in groups['automationcontroller']; groups['automationhub'] \| default([]) \| length; not bundle_install \| bool; not _ee_from_hub_only \| bool |
| 2390 | 93.8% | install.yml → Post-install setup → Execute Post-Installation Steps → automationcontroller → automationcontroller → block | Update execution environment images for Hub (bundle) | Update execution environment images for Hub (bundle) | node_state is defined; node_state == 'active'; inventory_hostname in groups['automationcontroller']; groups['automationhub'] \| default([]) \| length; bundle_install \| bool; not _ee_from_hub_only \| bool |
| 2391 | 93.9% | install.yml → Post-install setup → Execute Post-Installation Steps → automationcontroller → automationcontroller → block | Update execution environment images for Hub registry only | Update execution environment images for Hub registry only | node_state is defined; node_state == 'active'; inventory_hostname in groups['automationcontroller']; groups['automationhub'] \| default([]) \| length; _ee_from_hub_only \| bool |
| 2392 | 93.9% | install.yml → Post-install setup → Execute Post-Installation Steps → automationcontroller → automationcontroller | Set Execution Environment configuration | Set Execution Environment configuration (dest `/etc/tower/conf.d/execution_environments.py`) | node_state is defined; node_state == 'active'; inventory_hostname in groups['automationcontroller'] |
| 2393 | 94.0% | install.yml → Post-install setup → Execute Post-Installation Steps → automationcontroller → automationcontroller | Set default image for Container Groups | Set default image for Container Groups (dest `/etc/tower/conf.d/container_groups.py`) | node_state is defined; node_state == 'active'; inventory_hostname in groups['automationcontroller'] |
| 2394 | 94.0% | install.yml → Post-install setup → Execute Post-Installation Steps | include_role | Include and run tasks from an Ansible role (role `misc`, tasks `post_install_setup`) | node_state is defined; node_state == 'active'; inventory_hostname == groups['automationcontroller'][0] |
| 2395 | 94.0% | install.yml → Post-install setup → Execute Post-Installation Steps → misc | role `misc` (`post_install_setup`) | Enter role task list | node_state is defined; node_state == 'active'; inventory_hostname == groups['automationcontroller'][0] |
| 2396 | 94.1% | install.yml → Post-install setup → Execute Post-Installation Steps → misc → misc | file `post_install_setup.yml` | Task file `ansible/automation_platform_installer/roles/misc/tasks/post_install_setup.yml` | node_state is defined; node_state == 'active'; inventory_hostname == groups['automationcontroller'][0] |
| 2397 | 94.1% | install.yml → Post-install setup → Execute Post-Installation Steps → misc → misc | Create the default organization if it is needed. | Create the default organization if it is needed. | node_state is defined; node_state == 'active'; inventory_hostname == groups['automationcontroller'][0]; create_preload_data \| bool |
| 2398 | 94.1% | install.yml → Post-install setup → Execute Post-Installation Steps → misc → misc | Create the default execution environments if needed (without authentication). | Create the default execution environments if needed (without authentication). | node_state is defined; node_state == 'active'; inventory_hostname == groups['automationcontroller'][0]; registry_username is not defined or bundle_install \| default(false) \| bool |
| 2399 | 94.2% | install.yml → Post-install setup → Execute Post-Installation Steps → misc → misc | Create the default execution environments if needed (with authentication). | Create the default execution environments if needed (with authentication). | node_state is defined; node_state == 'active'; inventory_hostname == groups['automationcontroller'][0]; registry_username is defined |
| 2400 | 94.2% | install.yml → Post-install setup → Execute Post-Installation Steps → misc → misc | Migrate teams and organizations to Gateway | Migrate teams and organizations to Gateway (role `automationgateway`, tasks `data_migration`) | node_state is defined; node_state == 'active'; inventory_hostname == groups['automationcontroller'][0]; _migrate_data \| default(true) \| bool |
| 2401 | 94.3% | install.yml → Post-install setup → Execute Post-Installation Steps → misc → misc → automationgateway | role `automationgateway` (`data_migration`) | Enter role task list | node_state is defined; node_state == 'active'; inventory_hostname == groups['automationcontroller'][0]; _migrate_data \| default(true) \| bool |
| 2402 | 94.3% | install.yml → Post-install setup → Execute Post-Installation Steps → misc → misc → automationgateway → automationgateway | file `data_migration.yml` | Task file `ansible/automation_platform_installer/roles/automationgateway/tasks/data_migration.yml` | node_state is defined; node_state == 'active'; inventory_hostname == groups['automationcontroller'][0]; _migrate_data \| default(true) \| bool |
| 2403 | 94.3% | install.yml → Post-install setup → Execute Post-Installation Steps → misc → misc → automationgateway → automationgateway | Migrate data | Migrate data | node_state is defined; node_state == 'active'; inventory_hostname == groups['automationcontroller'][0]; _migrate_data \| default(true) \| bool |
| 2404 | 94.4% | install.yml → Post-install setup → Execute Post-Installation Steps | Start the automation-controller service | Start the automation-controller service (service `automation-controller`) | node_state is defined; node_state == 'active'; inventory_hostname in groups['automationcontroller'] |
| 2405 | 94.4% | install.yml → Post-install setup | include_role | Include and run tasks from an Ansible role (role `automationhub`, tasks `post_install_setup.yml`) | inventory_hostname in groups['automationhub'] \| default([]) |
| 2406 | 94.5% | install.yml → Post-install setup → automationhub | role `automationhub` (`post_install_setup.yml`) | Enter role task list | inventory_hostname in groups['automationhub'] \| default([]) |
| 2407 | 94.5% | install.yml → Post-install setup → automationhub → automationhub | file `post_install_setup.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/post_install_setup.yml` | inventory_hostname in groups['automationhub'] \| default([]) |
| 2408 | 94.5% | install.yml → Post-install setup → automationhub → automationhub | Migrate teams and organizations to Gateway | Migrate teams and organizations to Gateway (role `automationgateway`, tasks `data_migration`) | inventory_hostname in groups['automationhub'] \| default([]); _migrate_data \| default(true) \| bool |
| 2409 | 94.6% | install.yml → Post-install setup → automationhub → automationhub → automationgateway | role `automationgateway` (`data_migration`) | Enter role task list | inventory_hostname in groups['automationhub'] \| default([]); _migrate_data \| default(true) \| bool |
| 2410 | 94.6% | install.yml → Post-install setup → automationhub → automationhub → automationgateway → automationgateway | file `data_migration.yml` | Task file `ansible/automation_platform_installer/roles/automationgateway/tasks/data_migration.yml` | inventory_hostname in groups['automationhub'] \| default([]); _migrate_data \| default(true) \| bool |
| 2411 | 94.7% | install.yml → Post-install setup → automationhub → automationhub → automationgateway → automationgateway | Migrate data | Migrate data | inventory_hostname in groups['automationhub'] \| default([]); _migrate_data \| default(true) \| bool |
| 2412 | 94.7% | install.yml → Post-install setup → automationhub → automationhub | Create credentials and change collection order for 'Default' organization | Begin conditional or grouped task block | inventory_hostname in groups['automationhub'] \| default([]); groups['automationcontroller'] \| default([]) \| length |
| 2413 | 94.7% | install.yml → Post-install setup → automationhub → automationhub → Create credentials and change collection order for 'Default' organization | include_tasks | Include a task file from the current role or playbook (file `generate_token.yml`) | inventory_hostname in groups['automationhub'] \| default([]); groups['automationcontroller'] \| default([]) \| length; not automationhub_api_token \| default('') \| length; generate_automationhub_token \| bool |
| 2414 | 94.8% | install.yml → Post-install setup → automationhub → automationhub → Create credentials and change collection order for 'Default' organization → generate_token.yml | file `generate_token.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/generate_token.yml` | inventory_hostname in groups['automationhub'] \| default([]); groups['automationcontroller'] \| default([]) \| length; not automationhub_api_token \| default('') \| length; generate_automationhub_token \| bool |
| 2415 | 94.8% | install.yml → Post-install setup → automationhub → automationhub → Create credentials and change collection order for 'Default' organization → generate_token.yml | Get Hub API token | Get Hub API token | inventory_hostname in groups['automationhub'] \| default([]); groups['automationcontroller'] \| default([]) \| length; not automationhub_api_token \| default('') \| length; generate_automationhub_token \| bool |
| 2416 | 94.9% | install.yml → Post-install setup → automationhub → automationhub → Create credentials and change collection order for 'Default' organization → generate_token.yml | set_fact | Set a host fact used by later tasks | inventory_hostname in groups['automationhub'] \| default([]); groups['automationcontroller'] \| default([]) \| length; not automationhub_api_token \| default('') \| length; generate_automationhub_token \| bool |
| 2417 | 94.9% | install.yml → Post-install setup → automationhub → automationhub → Create credentials and change collection order for 'Default' organization → generate_token.yml | Save Hub token | Save Hub token (dest `{{ pulp_user_home + '/admin_token' }}`) | inventory_hostname in groups['automationhub'] \| default([]); groups['automationcontroller'] \| default([]) \| length; not automationhub_api_token \| default('') \| length; generate_automationhub_token \| bool |
| 2418 | 94.9% | install.yml → Post-install setup → automationhub → automationhub → Create credentials and change collection order for 'Default' organization | Create Hub credentials and associate with EEs | Create Hub credentials and associate with EEs | inventory_hostname in groups['automationhub'] \| default([]); groups['automationcontroller'] \| default([]) \| length |
| 2419 | 95.0% | install.yml → Post-install setup | include_role | Include and run tasks from an Ansible role (role `automationedacontroller`, tasks `post_install_setup.yml`) | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0] |
| 2420 | 95.0% | install.yml → Post-install setup → automationedacontroller | role `automationedacontroller` (`post_install_setup.yml`) | Enter role task list | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0] |
| 2421 | 95.1% | install.yml → Post-install setup → automationedacontroller → automationedacontroller | file `post_install_setup.yml` | Task file `ansible/automation_platform_installer/roles/automationedacontroller/tasks/post_install_setup.yml` | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0] |
| 2422 | 95.1% | install.yml → Post-install setup → automationedacontroller → automationedacontroller | Migrate teams and organizations to Gateway | Migrate teams and organizations to Gateway (role `automationgateway`, tasks `data_migration`) | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; _migrate_data \| default(true) \| bool |
| 2423 | 95.1% | install.yml → Post-install setup → automationedacontroller → automationedacontroller → automationgateway | role `automationgateway` (`data_migration`) | Enter role task list | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; _migrate_data \| default(true) \| bool |
| 2424 | 95.2% | install.yml → Post-install setup → automationedacontroller → automationedacontroller → automationgateway → automationgateway | file `data_migration.yml` | Task file `ansible/automation_platform_installer/roles/automationgateway/tasks/data_migration.yml` | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; _migrate_data \| default(true) \| bool |
| 2425 | 95.2% | install.yml → Post-install setup → automationedacontroller → automationedacontroller → automationgateway → automationgateway | Migrate data | Migrate data | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; _migrate_data \| default(true) \| bool |
| 2426 | 95.2% | install.yml → Post-install setup → automationedacontroller → automationedacontroller | Set credential for registry_url | Set credential for registry_url | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; not _ee_from_hub_only \| bool; registry_username \| default() \| length; registry_password \| default() \| length |
| 2427 | 95.3% | install.yml → Post-install setup → automationedacontroller → automationedacontroller | Set hub info | Begin conditional or grouped task block | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; groups['automationhub'] \| default([]) \| length |
| 2428 | 95.3% | install.yml → Post-install setup → automationedacontroller → automationedacontroller → Set hub info | ansible.builtin.set_fact | Run `ansible.builtin.set_fact` | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; groups['automationhub'] \| default([]) \| length |
| 2429 | 95.4% | install.yml → Post-install setup → automationedacontroller → automationedacontroller → Set hub info | ansible.builtin.set_fact | Run `ansible.builtin.set_fact` | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; groups['automationhub'] \| default([]) \| length |
| 2430 | 95.4% | install.yml → Post-install setup → automationedacontroller → automationedacontroller → Set hub info | Set credential for Hub | Set credential for Hub | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; groups['automationhub'] \| default([]) \| length |
| 2431 | 95.4% | install.yml → Post-install setup → automationedacontroller → automationedacontroller → Set hub info | Set DE for Hub | Set DE for Hub | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; groups['automationhub'] \| default([]) \| length |
| 2432 | 95.5% | install.yml → Post-install setup → automationedacontroller → automationedacontroller | Set DE and Credential list for registry_url only | Set DE and Credential list for registry_url only | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; not groups['automationhub'] \| default([]) \| length |
| 2433 | 95.5% | install.yml → Post-install setup → automationedacontroller → automationedacontroller | Set DE and Credential list for Hub only | Set DE and Credential list for Hub only | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; groups['automationhub'] \| default([]) \| length; _ee_from_hub_only \| bool |
| 2434 | 95.6% | install.yml → Post-install setup → automationedacontroller → automationedacontroller | Set DE and Credential list for registry_url and Hub | Set DE and Credential list for registry_url and Hub | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; groups['automationhub'] \| default([]) \| length; not _ee_from_hub_only \| bool |
| 2435 | 95.6% | install.yml → Post-install setup → automationedacontroller → automationedacontroller | Create credentials | Begin conditional or grouped task block | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; _cred_list \| length |
| 2436 | 95.6% | install.yml → Post-install setup → automationedacontroller → automationedacontroller → Create credentials | Create credentials | Create credentials | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; _cred_list \| length |
| 2437 | 95.7% | install.yml → Post-install setup → automationedacontroller → automationedacontroller | Create credentials (rescue) | Run rescue tasks for block | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; _cred_list \| length |
| 2438 | 95.7% | install.yml → Post-install setup → automationedacontroller → automationedacontroller → Create credentials (rescue) | Parse messages | Parse messages | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; _cred_list \| length; item is failed |
| 2439 | 95.8% | install.yml → Post-install setup → automationedacontroller → automationedacontroller → Create credentials (rescue) | Print error messages from credential creation | Print error messages from credential creation | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; _cred_list \| length; _eda_cred_error_messages \| default('') \| length |
| 2440 | 95.8% | install.yml → Post-install setup → automationedacontroller → automationedacontroller | Create credentials (always) | Run always tasks for block | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; _cred_list \| length |
| 2441 | 95.8% | install.yml → Post-install setup → automationedacontroller → automationedacontroller → Create credentials (always) | Parse messages | Parse messages | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; _cred_list \| length; item is succeeded |
| 2442 | 95.9% | install.yml → Post-install setup → automationedacontroller → automationedacontroller → Create credentials (always) | Print messages from credential creation | Print messages from credential creation | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; _cred_list \| length; _eda_cred_messages \| default('') \| length |
| 2443 | 95.9% | install.yml → Post-install setup → automationedacontroller → automationedacontroller | Create decision environments | Begin conditional or grouped task block | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; _de_list \| length |
| 2444 | 96.0% | install.yml → Post-install setup → automationedacontroller → automationedacontroller → Create decision environments | Create decision environments | Create decision environments | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; _de_list \| length |
| 2445 | 96.0% | install.yml → Post-install setup → automationedacontroller → automationedacontroller | Create decision environments (rescue) | Run rescue tasks for block | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; _de_list \| length |
| 2446 | 96.0% | install.yml → Post-install setup → automationedacontroller → automationedacontroller → Create decision environments (rescue) | Parse messages | Parse messages | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; _de_list \| length; item is failed |
| 2447 | 96.1% | install.yml → Post-install setup → automationedacontroller → automationedacontroller → Create decision environments (rescue) | Print error messages from DE creation | Print error messages from DE creation | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; _de_list \| length; _eda_de_error_messages \| default('') \| length |
| 2448 | 96.1% | install.yml → Post-install setup → automationedacontroller → automationedacontroller | Create decision environments (always) | Run always tasks for block | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; _de_list \| length |
| 2449 | 96.2% | install.yml → Post-install setup → automationedacontroller → automationedacontroller → Create decision environments (always) | Parse messages | Parse messages | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; _de_list \| length; item is succeeded |
| 2450 | 96.2% | install.yml → Post-install setup → automationedacontroller → automationedacontroller → Create decision environments (always) | Print messages from DE creation | Print messages from DE creation | groups['automationedacontroller'] \| default([]) \| length; inventory_hostname == groups['automationedacontroller'][0]; _de_list \| length; _eda_de_messages \| default('') \| length |
| 2451 | 96.2% | install.yml → Post-install setup | block | Begin conditional or grouped task block | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool |
| 2452 | 96.3% | install.yml → Post-install setup → block | Set supported repositories for seeding | Set supported repositories for seeding | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool |
| 2453 | 96.3% | install.yml → Post-install setup → block | Seed collections | Seed collections (role `automationhub`, tasks `seed_collection_for_repository`) | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool |
| 2454 | 96.3% | install.yml → Post-install setup → block → automationhub | role `automationhub` (`seed_collection_for_repository`) | Enter role task list | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool |
| 2455 | 96.4% | install.yml → Post-install setup → block → automationhub → automationhub | file `seed_collection_for_repository.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/seed_collection_for_repository.yml` | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool |
| 2456 | 96.4% | install.yml → Post-install setup → block → automationhub → automationhub | Ensure repository is valid for seeding | Ensure repository is valid for seeding | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; destination_repository not in _collection_seed_supported_repositories |
| 2457 | 96.5% | install.yml → Post-install setup → block → automationhub → automationhub | include_tasks | Include a task file from the current role or playbook (file `generate_token.yml`) | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; not automationhub_api_token \| default('') \| length; generate_automationhub_token \| bool |
| 2458 | 96.5% | install.yml → Post-install setup → block → automationhub → automationhub → generate_token.yml | file `generate_token.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/generate_token.yml` | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; not automationhub_api_token \| default('') \| length; generate_automationhub_token \| bool |
| 2459 | 96.5% | install.yml → Post-install setup → block → automationhub → automationhub → generate_token.yml | Get Hub API token | Get Hub API token | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; not automationhub_api_token \| default('') \| length; generate_automationhub_token \| bool |
| 2460 | 96.6% | install.yml → Post-install setup → block → automationhub → automationhub → generate_token.yml | set_fact | Set a host fact used by later tasks | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; not automationhub_api_token \| default('') \| length; generate_automationhub_token \| bool |
| 2461 | 96.6% | install.yml → Post-install setup → block → automationhub → automationhub → generate_token.yml | Save Hub token | Save Hub token (dest `{{ pulp_user_home + '/admin_token' }}`) | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; not automationhub_api_token \| default('') \| length; generate_automationhub_token \| bool |
| 2462 | 96.7% | install.yml → Post-install setup → block → automationhub → automationhub | Ensure credential is provided | Ensure credential is provided | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; not automationhub_api_token \| default('') \| length; not automationgateway_admin_password \| default('') \| length |
| 2463 | 96.7% | install.yml → Post-install setup → block → automationhub → automationhub | set_fact | Set a host fact used by later tasks | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; not _bundled_collection_root \| default('') \| length |
| 2464 | 96.7% | install.yml → Post-install setup → block → automationhub → automationhub | set_fact | Set a host fact used by later tasks | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; not _collection_seed_log_root \| default('') \| length |
| 2465 | 96.8% | install.yml → Post-install setup → block → automationhub → automationhub | set_fact | Set a host fact used by later tasks | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool |
| 2466 | 96.8% | install.yml → Post-install setup → block → automationhub → automationhub | set_fact | Set a host fact used by later tasks | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool |
| 2467 | 96.9% | install.yml → Post-install setup → block → automationhub → automationhub | block | Begin conditional or grouped task block | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool |
| 2468 | 96.9% | install.yml → Post-install setup → block → automationhub → automationhub → block | Check for requirements.yml | Check for requirements.yml | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool |
| 2469 | 96.9% | install.yml → Post-install setup → block → automationhub → automationhub → block | Ensure requirements.yml exists | Ensure requirements.yml exists | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; not _requirement_file.stat.exists \| bool |
| 2470 | 97.0% | install.yml → Post-install setup → block → automationhub → automationhub → block | Get collection list | Get collection list | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool |
| 2471 | 97.0% | install.yml → Post-install setup → block → automationhub → automationhub → block | Check if collection tarball exists | Check if collection tarball exists | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool |
| 2472 | 97.1% | install.yml → Post-install setup → block → automationhub → automationhub | Set auth header | Set auth header | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _token_credential \| bool |
| 2473 | 97.1% | install.yml → Post-install setup → block → automationhub → automationhub | Fallback to basic auth if token isn't provided | Fallback to basic auth if token isn't provided | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; not _token_credential \| bool |
| 2474 | 97.1% | install.yml → Post-install setup → block → automationhub → automationhub | set_fact | Set a host fact used by later tasks | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_seed_local is not defined; hostvars['localhost']['ansible_hostname'] is defined; ansible_hostname == hostvars['localhost']['ansible_hostname'] |
| 2475 | 97.2% | install.yml → Post-install setup → block → automationhub → automationhub | block | Begin conditional or grouped task block | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length |
| 2476 | 97.2% | install.yml → Post-install setup → block → automationhub → automationhub → block | block | Begin conditional or grouped task block | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length; not _collection_seed_local \| default(false) \| bool |
| 2477 | 97.3% | install.yml → Post-install setup → block → automationhub → automationhub → block → block | Create temporary directory for collections | Create temporary directory for collections | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length; not _collection_seed_local \| default(false) \| bool |
| 2478 | 97.3% | install.yml → Post-install setup → block → automationhub → automationhub → block → block | Copy collections | Copy collections (dest `{{ _collection_tmp.path }}`) | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length; not _collection_seed_local \| default(false) \| bool |
| 2479 | 97.3% | install.yml → Post-install setup → block → automationhub → automationhub → block | Seed {{ destination_repository }} collections | Seed {{ destination_repository }} collections (file `seed_collection.yml`) | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length |
| 2480 | 97.4% | install.yml → Post-install setup → block → automationhub → automationhub → block → seed_collection.yml | file `seed_collection.yml` | Task file `ansible/automation_platform_installer/roles/automationhub/tasks/seed_collection.yml` | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length |
| 2481 | 97.4% | install.yml → Post-install setup → block → automationhub → automationhub → block → seed_collection.yml | set_fact | Set a host fact used by later tasks | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length |
| 2482 | 97.4% | install.yml → Post-install setup → block → automationhub → automationhub → block → seed_collection.yml | block | Begin conditional or grouped task block | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length |
| 2483 | 97.5% | install.yml → Post-install setup → block → automationhub → automationhub → block → seed_collection.yml → block | Check if collection exists | Check if collection exists | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length |
| 2484 | 97.5% | install.yml → Post-install setup → block → automationhub → automationhub → block → seed_collection.yml → block | block | Begin conditional or grouped task block | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length; _collection_exists.json.count > 0 |
| 2485 | 97.6% | install.yml → Post-install setup → block → automationhub → automationhub → block → seed_collection.yml → block → block | set_fact | Set a host fact used by later tasks | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length; _collection_exists.json.count > 0 |
| 2486 | 97.6% | install.yml → Post-install setup → block → automationhub → automationhub → block → seed_collection.yml → block → block | Skip if collection already exists | Skip if collection already exists | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length; _collection_exists.json.count > 0 |
| 2487 | 97.6% | install.yml → Post-install setup → block → automationhub → automationhub → block → seed_collection.yml → block | block | Begin conditional or grouped task block | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length; _collection_exists.json.count == 0 |
| 2488 | 97.7% | install.yml → Post-install setup → block → automationhub → automationhub → block → seed_collection.yml → block → block | Create '{{ _collection_namespace }}' namespace | Create '{{ _collection_namespace }}' namespace | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length; _collection_exists.json.count == 0 |
| 2489 | 97.7% | install.yml → Post-install setup → block → automationhub → automationhub → block → seed_collection.yml → block → block | Publish {{ _collection.name }} collection | Publish {{ _collection.name }} collection | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length; _collection_exists.json.count == 0 |
| 2490 | 97.8% | install.yml → Post-install setup → block → automationhub → automationhub → block → seed_collection.yml → block → block | set_fact | Set a host fact used by later tasks | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length; _collection_exists.json.count == 0 |
| 2491 | 97.8% | install.yml → Post-install setup → block → automationhub → automationhub → block → seed_collection.yml → block → block | Move collection to {{ destination_repository }} repo | Move collection to {{ destination_repository }} repo | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length; _collection_exists.json.count == 0 |
| 2492 | 97.8% | install.yml → Post-install setup → block → automationhub → automationhub → block → seed_collection.yml → block → block | Wait until move task is completed | Wait until move task is completed | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length; _collection_exists.json.count == 0 |
| 2493 | 97.9% | install.yml → Post-install setup → block → automationhub → automationhub → block → seed_collection.yml → block → block | set_fact | Set a host fact used by later tasks | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length; _collection_exists.json.count == 0 |
| 2494 | 97.9% | install.yml → Post-install setup → block → automationhub → automationhub → block → seed_collection.yml | block (rescue) | Run rescue tasks for block | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length |
| 2495 | 98.0% | install.yml → Post-install setup → block → automationhub → automationhub → block → seed_collection.yml → block (rescue) | Set seed result | Set seed result | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length |
| 2496 | 98.0% | install.yml → Post-install setup → block → automationhub → automationhub → block → seed_collection.yml | Record collection seed result | Record collection seed result | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length |
| 2497 | 98.0% | install.yml → Post-install setup → block → automationhub → automationhub | block (always) | Run always tasks for block | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length |
| 2498 | 98.1% | install.yml → Post-install setup → block → automationhub → automationhub → block (always) | Record collection seed result | Record collection seed result (dest `{{ _collection_seed_log_file }}`) | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length |
| 2499 | 98.1% | install.yml → Post-install setup → block → automationhub → automationhub → block (always) | Remove copied collections | Remove copied collections | inventory_hostname in groups['automationhub'] \| default([]); bundle_install \| bool; automationhub_seed_collections \| default(true) \| bool; _collection_tarball_check.results \| selectattr('stat.exists', 'true') \| length; _collection_tmp.path is defined |
| 2500 | 98.2% | install.yml | Post-install insights setup | Play targeting `aap_valid_hosts` | — |
| 2501 | 98.2% | install.yml → Post-install insights setup | include_role | Include and run tasks from an Ansible role (role `misc`, tasks `post_install_insights_setup.yml`) | _rhsm_registered \| bool |
| 2502 | 98.2% | install.yml → Post-install insights setup → misc | role `misc` (`post_install_insights_setup.yml`) | Enter role task list | _rhsm_registered \| bool |
| 2503 | 98.3% | install.yml → Post-install insights setup → misc → misc | file `post_install_insights_setup.yml` | Task file `ansible/automation_platform_installer/roles/misc/tasks/post_install_insights_setup.yml` | _rhsm_registered \| bool |
| 2504 | 98.3% | install.yml → Post-install insights setup → misc → misc | Collect service facts | Collect service facts | _rhsm_registered \| bool |
| 2505 | 98.4% | install.yml → Post-install insights setup → misc → misc | Check if insights-register service exists | Check if insights-register service exists | _rhsm_registered \| bool |
| 2506 | 98.4% | install.yml → Post-install insights setup → misc → misc | Skip insights registration when insights-register service exists | Skip insights registration when insights-register service exists | _rhsm_registered \| bool; _insights_register_exists \| bool |
| 2507 | 98.4% | install.yml → Post-install insights setup → misc → misc | block | Begin conditional or grouped task block | _rhsm_registered \| bool; not _insights_register_exists \| bool; enable_insights_collection \| bool |
| 2508 | 98.5% | install.yml → Post-install insights setup → misc → misc → block | Ensure insights-client is installed | Ensure insights-client is installed | _rhsm_registered \| bool; not _insights_register_exists \| bool; enable_insights_collection \| bool |
| 2509 | 98.5% | install.yml → Post-install insights setup → misc → misc → block | Register with Insights | Register with Insights | _rhsm_registered \| bool; not _insights_register_exists \| bool; enable_insights_collection \| bool |
| 2510 | 98.5% | install.yml → Post-install insights setup → misc → misc | Unregister from Insights | Unregister from Insights | _rhsm_registered \| bool; not _insights_register_exists \| bool; not enable_insights_collection \| bool |
| 2511 | 98.6% | install.yml | Install PCP and configure pmproxy | Play targeting `automationgateway:automationcontroller:automationhub:automationedacontroller:database` | — |
| 2512 | 98.6% | install.yml → Install PCP and configure pmproxy | include_role | Include and run tasks from an Ansible role (role `monitoring`) | setup_monitoring \| default(false) \| bool |
| 2513 | 98.7% | install.yml → Install PCP and configure pmproxy → monitoring | role `monitoring` | Enter role task list | setup_monitoring \| default(false) \| bool |
| 2514 | 98.7% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring | file `main.yml` | Task file `ansible/automation_platform_installer/roles/monitoring/tasks/main.yml` | setup_monitoring \| default(false) \| bool |
| 2515 | 98.7% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring | include_tasks | Include a task file from the current role or playbook (file `setup-pcp.yml`) | setup_monitoring \| default(false) \| bool |
| 2516 | 98.8% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → setup-pcp.yml | file `setup-pcp.yml` | Task file `ansible/automation_platform_installer/roles/monitoring/tasks/setup-pcp.yml` | setup_monitoring \| default(false) \| bool |
| 2517 | 98.8% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → setup-pcp.yml | Setup performance co-pilot, collector daemon and pmproxy | Begin conditional or grouped task block | setup_monitoring \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('automationhub', [])) \| union(groups.get('automationedacontroller', [])) \| union(groups.get('database', [])) \| union(groups.get('automationgateway', [])) |
| 2518 | 98.9% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → setup-pcp.yml → Setup performance co-pilot, collector daemon and pmproxy | Install the PCP RPM and PMDA RPMs | Install the PCP RPM and PMDA RPMs | setup_monitoring \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('automationhub', [])) \| union(groups.get('automationedacontroller', [])) \| union(groups.get('database', [])) \| union(groups.get('automationgateway', [])) |
| 2519 | 98.9% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → setup-pcp.yml → Setup performance co-pilot, collector daemon and pmproxy | Flush handlers | Flush handlers (flush_handlers) | setup_monitoring \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('automationhub', [])) \| union(groups.get('automationedacontroller', [])) \| union(groups.get('database', [])) \| union(groups.get('automationgateway', [])) |
| 2520 | 98.9% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → setup-pcp.yml → Setup performance co-pilot, collector daemon and pmproxy | verify able to ping metrics | verify able to ping metrics | setup_monitoring \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('automationhub', [])) \| union(groups.get('automationedacontroller', [])) \| union(groups.get('database', [])) \| union(groups.get('automationgateway', [])) |
| 2521 | 99.0% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → setup-pcp.yml | Set the correct the pmlogger collector host | Begin conditional or grouped task block | setup_monitoring \| default(false) \| bool |
| 2522 | 99.0% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → setup-pcp.yml → Set the correct the pmlogger collector host | Set the pmlogger collector host to automationgateway[0] | Set the pmlogger collector host to automationgateway[0] | setup_monitoring \| default(false) \| bool; 'automationgateway' in groups and groups['automationgateway'] \| length != 0 |
| 2523 | 99.1% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → setup-pcp.yml → Set the correct the pmlogger collector host | Set the pmlogger collector host to automationcontroller[0] | Set the pmlogger collector host to automationcontroller[0] | setup_monitoring \| default(false) \| bool; pmlogger_collector_host is not defined and ('automationcontroller' in groups and groups['automationcontroller'] \| length != 0) |
| 2524 | 99.1% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → setup-pcp.yml → Set the correct the pmlogger collector host | Set the pmlogger collector host to automationhub[0] | Set the pmlogger collector host to automationhub[0] | setup_monitoring \| default(false) \| bool; pmlogger_collector_host is not defined and ('automationhub' in groups and groups['automationhub'] \| length != 0) |
| 2525 | 99.1% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → setup-pcp.yml → Set the correct the pmlogger collector host | set the pmlogger collector host to automationedacontroller[0] | set the pmlogger collector host to automationedacontroller[0] | setup_monitoring \| default(false) \| bool; pmlogger_collector_host is not defined and ('automationedacontroller' in groups and groups['automationedacontroller'] \| length != 0) |
| 2526 | 99.2% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → setup-pcp.yml | indicate which host will collect metrics from others | indicate which host will collect metrics from others | setup_monitoring \| default(false) \| bool |
| 2527 | 99.2% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → setup-pcp.yml | enable pmcd remote access | Begin conditional or grouped task block | setup_monitoring \| default(false) \| bool; inventory_hostname != pmlogger_collector_host |
| 2528 | 99.3% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → setup-pcp.yml → enable pmcd remote access | Allow remote access to pmcd | Allow remote access to pmcd | setup_monitoring \| default(false) \| bool; inventory_hostname != pmlogger_collector_host |
| 2529 | 99.3% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → setup-pcp.yml → enable pmcd remote access | start and enable pcp collection daemon | start and enable pcp collection daemon | setup_monitoring \| default(false) \| bool; inventory_hostname != pmlogger_collector_host; enable_remote_pmcd.changed |
| 2530 | 99.3% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → setup-pcp.yml → enable pmcd remote access | check pmcd remote access enabled | check pmcd remote access enabled | setup_monitoring \| default(false) \| bool; inventory_hostname != pmlogger_collector_host |
| 2531 | 99.4% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → setup-pcp.yml → enable pmcd remote access | ensure expected output | ensure expected output | setup_monitoring \| default(false) \| bool; inventory_hostname != pmlogger_collector_host |
| 2532 | 99.4% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → setup-pcp.yml | Setup pmlogger to collect from remote systems | Begin conditional or grouped task block | setup_monitoring \| default(false) \| bool; (inventory_hostname == pmlogger_collector_host) and enable_centralized_pmlogger\|default('True')\|bool |
| 2533 | 99.5% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → setup-pcp.yml → Setup pmlogger to collect from remote systems | Setup config to monitor remote systems | Setup config to monitor remote systems | setup_monitoring \| default(false) \| bool; (inventory_hostname == pmlogger_collector_host) and enable_centralized_pmlogger\|default('True')\|bool |
| 2534 | 99.5% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring | include_tasks | Include a task file from the current role or playbook (file `postgres-pmda.yml`) | setup_monitoring \| default(false) \| bool; inventory_hostname in groups.get('database', []) |
| 2535 | 99.5% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → postgres-pmda.yml | file `postgres-pmda.yml` | Task file `ansible/automation_platform_installer/roles/monitoring/tasks/postgres-pmda.yml` | setup_monitoring \| default(false) \| bool; inventory_hostname in groups.get('database', []) |
| 2536 | 99.6% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → postgres-pmda.yml | Install postgres exporter on database node | Install postgres exporter on database node | setup_monitoring \| default(false) \| bool; inventory_hostname in groups.get('database', []) |
| 2537 | 99.6% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → postgres-pmda.yml | Install postgres exporter to the perf copilot collector daemon | Install postgres exporter to the perf copilot collector daemon | setup_monitoring \| default(false) \| bool; inventory_hostname in groups.get('database', []) |
| 2538 | 99.6% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → postgres-pmda.yml | Pause to allow metrics from postgres to show up on pcp | Pause to allow metrics from postgres to show up on pcp | setup_monitoring \| default(false) \| bool; inventory_hostname in groups.get('database', []) |
| 2539 | 99.7% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → postgres-pmda.yml | verify able to ping metrics | verify able to ping metrics | setup_monitoring \| default(false) \| bool; inventory_hostname in groups.get('database', []) |
| 2540 | 99.7% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring | include_tasks | Include a task file from the current role or playbook (file `nginx-pmda.yml`) | setup_monitoring \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('automationhub', [])) \| union(_automationedacontroller_web_hosts) |
| 2541 | 99.8% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → nginx-pmda.yml | file `nginx-pmda.yml` | Task file `ansible/automation_platform_installer/roles/monitoring/tasks/nginx-pmda.yml` | setup_monitoring \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('automationhub', [])) \| union(_automationedacontroller_web_hosts) |
| 2542 | 99.8% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → nginx-pmda.yml | Install nginx exporter on control, hybrid, eda, and automationhub nodes | Install nginx exporter on control, hybrid, eda, and automationhub nodes | setup_monitoring \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('automationhub', [])) \| union(_automationedacontroller_web_hosts) |
| 2543 | 99.8% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → nginx-pmda.yml | Enable nginx status metrics on Controller, Hub, EDA (only for localhost) | Enable nginx status metrics on Controller, Hub, EDA (only for localhost) | setup_monitoring \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('automationhub', [])) \| union(_automationedacontroller_web_hosts) |
| 2544 | 99.9% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → nginx-pmda.yml | Install nginx exporter to the perf copilot collector daemon | Install nginx exporter to the perf copilot collector daemon | setup_monitoring \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('automationhub', [])) \| union(_automationedacontroller_web_hosts) |
| 2545 | 99.9% | install.yml → Install PCP and configure pmproxy → monitoring → monitoring → nginx-pmda.yml | verify able to ping metrics | verify able to ping metrics | setup_monitoring \| default(false) \| bool; inventory_hostname in groups.get('automationcontroller', []) \| union(groups.get('automationhub', [])) \| union(_automationedacontroller_web_hosts) |
| 2546 | 100.0% | install.yml | Post-install cleanup | Play targeting `aap_valid_hosts` | — |
| 2547 | 100.0% | install.yml → Post-install cleanup | Remove stale packages | Remove stale packages | — |
<!-- prettier-ignore-end -->

## Notes

- **Location** shows the play and role/task-file breadcrumb path from `install.yml`.
- The same role or task file may appear multiple times when invoked from different plays or blocks.
- Recursive cycles (A includes B includes A) are detected and marked as skipped.
- Handler tasks (`notify`) are not executed inline; they run when notified and flushed.
- **Progress** runs from 0% (first row) to 100% (last row).
- **Do not Format Document** on this file — Markdown table formatters pad every column to the widest cell (~400+ char lines). Regenerate with `python3 installer/scripts/extract_install_playbook_tasks.py` instead.
- Inventories in this folder are co-located with this reference — `@`-mention `installer/AAP{XY}/{method}/` for both.
- OpenShift operator examples are in the sibling [`openshift/`](../../../openshift/) folder (no install playbook).
